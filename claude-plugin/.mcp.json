{
  "mcpServers": {
    "memory-agents": {
      "command": "${CLAUDE_PLUGIN_ROOT}/.venv/bin/python",
      "args": [
        "-m",
        "src.api.local_mcp"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}/..",
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/.."
      }
    }
  }
}
