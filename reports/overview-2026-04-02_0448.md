---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T04:48:50.455575
model: qwen2.5-coder:7b
mode: overview
files_total: 238
errors: 0
run_time_s: 368.7
---

# Funktions-Übersicht — 2026-04-02_0448

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 238
- **Fehler:** 0
- **Laufzeit:** 368.7s

## api

### app/Http/Controllers/ContactController.php
Die Datei ContactController.php enthält einen Controller für den Umgang mit Kontakteinheiten. Er verarbeitet Anfragen, validiert die Eingaben und speichert sie in der Datenbank. Anschließend sendet er Bestätigungs- und Rückfragee-Mails.

---

### app/Http/Controllers/Controller.php
Abstrakte Basisklasse für HTTP-Controller in Laravel-Apps

---

### app/Http/Controllers/DsgvoController.php
Die Datei enthält einen Controller für DSGVO-Funktionen, mit Endpunkten zum Exportieren von Benutzerdaten und zum Löschen des Kontos.

---

### app/Http/Controllers/LangdockWebhookController.php
Die Datei handhabt Webhooks von Langdock. Sie validiert die eingehenden Anfragen, extrahiert notwendige Daten und sendet diese an einen Job für den Langdock-Agenten.

---

### app/Http/Controllers/PaperRagController.php
Der Controller verarbeitet die Ingestion von Papieren und durchführt Suchoperationen. Wichtigste Klassen/Funktionen: PaperRagController, ingest(), search(). Externe Abhängigkeiten: Http (API-Abfragen), DB (Datenbank-Zugriff).

---

### routes/api.php
Definiert API-Endpunkte für Authentifizierung und Webhooks.

---

### routes/console.php
Die Datei 'routes/console.php' definiert zwei Artisan-Kommandos: 'inspire' zur Anzeige einer einladenden Zitat und 'deploy:notify' zur Versand einer E-Mail-Benachrichtigung über einen Deployment.

---

### routes/web.php
Definiert API-Endpunkte für verschiedene Anfragen und Routen in einem Laravel-Projekt, einschließlich Kontaktformular, Impressum, Datenschutzbestimmungen, Benutzerverwaltung und Forschungsergebnisse.

---

## config

### docker-compose.dev.yml
Docker-Compose-Konfiguration zur lokalen Entwicklung, ändert den Port des Web-Services von 6481 auf 6480.

---

### docker-compose.yml
Docker-Compose-Datei zur Konfiguration eines komplexen Multi-Dienst-Anwendungsstapels mit Nginx, PHP-FPM, PostgreSQL und Redis. Enthaält Services wie Webserver, PHP-FPM, CLI, Testumgebung, Queue-Arbeiter.

---

### .env.example
Konfigurationsdatei für ein Laravel-Projekt, enthält Datenbank- und Anwendungs-Einstellungen.

---

### config/app.php
Konfigurationsdatei für Laravel-Anwendung, definiert grundlegende Einstellungen wie Name, Umgebung, Debug-Modus, URL und Zeitzone.

---

### config/auth.php
Diese Datei enthält Konfigurationen für die Authentifizierung in einem Laravel-Anwendungsprojekt, einschließlich Standardgurten, Benutzeranbieter und Passwortzurücksetzung. Es verwendet Eloquent als Datenbank-Abstraktion.

---

### config/cache.php
Konfiguration der Cache-Strategien und -Speicheroptionen.

---

### config/cookie-consent.php
Konfiguration des Cookie-Zustimmungsdialogs mit Einstellungen für Aktivierung, Cookie-Namen und Lebensdauer.

---

### config/database.php
Konfiguration der Datenbankverbindungen für Laravel-Anwendungen.

---

### config/filesystems.php
Konfiguration der Dateispeicherungsdisks für das Laravel-Framework. Unterstützt lokale und cloud-basierte Speicherungsmethoden wie S3.

---

### config/fortify.php
Konfigurationsdatei für Laravel-Fortify, definiert Authentifizierungseinstellungen und Routen.

---

### config/logging.php
Konfiguration für das Logging-System. Definiert die Standardkanal und andere Kanäle wie 'single', 'daily', 'slack' und 'papertrail'. Nutzt Monolog als Logging-Bibliothek.

---

### config/mail.php
Konfiguration für verschiedene E-Mail-Sending-Mailer in Laravel-App

---

### config/queue.php
Konfiguration für Queue-Verbindungen in Laravel-Anwendungen. Stellt verschiedene Queue-Typen wie 'database', 'beanstalkd', 'sqs' und 'redis' zur Verfügung.

---

### config/services.php
Konfigurationsdatei für Drittanbieterdienste mit API-Schlüsseln und Einstellungen.

---

### config/session.php
Konfiguration der Laravel-Sitzung mit Optionen wie Driver, Lebensdauer, Verschlüsselung und Datenbankverbindung.

---

### docker/common/php-fpm/php-dev.ini
Konfiguration für PHP-FPM in Entwicklungsmodus mit aktiviertem OPcache Timestamp-Check und Fehleranzeige.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Definiert ein Vorlage für Bug-Berichte mit Markdown-Formatierung, Textfeldern und Dropdown-Menüs.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Eine Konfigurationsdatei für die Erstellung von Feature-Anfragen mit bestimmten Feldern und Validierungen.

---

### .github/ISSUE_TEMPLATE/refactor.yml
YAML-Konfiguration für GitHub-Issue-Template zur Code-Verbesserung (Refactoring). Enthält Fragen zu Beschreibung, Betroffenen Dateien und Akzeptanzkriterien.

---

### .github/workflows/lint.yml
Dieses GitHub Workflow führt Linting auf PHP und JavaScript Code durch, wenn Pushes oder Pull Requests an spezifische Branches vorgenommen werden.

---

### .github/workflows/tests.yml
Die Datei konfiguriert CI-Tests für ein Projekt, die bei bestimmten Ereignissen (Pushes oder Pull Requests) auf GitHub Actions ausgeführt werden. Sie führt Checks wie PHP-Setup, Node.js-Installation, Abhängigkeiten installieren, Umgebungsdateien kopieren, Schlüssel generieren und Tests ausführen.

---

## data_access

### app/Models/ChatMessage.php
Definiert ein Eloquent-Modell für Chat-Nachrichten, einschließlich Beziehungen zu Benutzern und Webhooks.

---

### app/Models/Consent.php
Eloquent Model für Zustimmungen, beinhaltet Felder wie UserID, IP-Adresse (anonymisiert), Zustimmungskategorien, Datum der Zustimmung und Widerrufdatum. Bezieht sich auf einen User-Modell.

---

### app/Models/Contact.php
Definiert ein Eloquent-Model für Kontakte in einer Datenbank. Stellt Zugriff auf die Contact-Tabelle mit den Feldern name, company, email, project_type, message und timeline.

---

### app/Models/PageView.php
Definiert ein Eloquent-Modell für die Darstellung von PageViews in der Datenbank mit Feldern für Pfad, anonyme IP, User-Agent und Besucheranzahl.

---

### app/Models/User.php
Die Datei enthält die User-Klasse, die ein Benutzermodell darstellt. Sie vererbt von Authenticatable und hat Attribute wie name, email und password. Die Klasse bietet Methoden für den Zugriff auf Benutzereigenschaften und Status.

---

### app/Models/Webhook.php
Model for webhooks with methods to retrieve and manipulate webhook URLs based on user and frontend object.

---

### app/Models/Recherche/P1Komponente.php
Definiert das Eloquent-Modell für P1-Komponenten in der Datenbank. Verwaltet UUIDs und Beziehungen zu Projekten.

---

### app/Models/Recherche/P1Kriterium.php
Definiert das Eloquent-Modell P1Kriterium zur Datenbanktabelle p1_kriterien. Verwendet UUIDs und hat einen Many-to-One-Bezug zu Projekt.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Definiert ein Eloquent-Model für die Tabelle 'p1_strukturmodell_wahl' mit einer Beziehung zur Projekt-Tabelle.

---

### app/Models/Recherche/P1Warnsignal.php
Definiert das Eloquent-Modell für P1-Warnsignale mit Zuordnung zum Projekt.

---

### app/Models/Recherche/P2Cluster.php
```json
{
  "file_summary": "Definiert das Eloquent-Modell für die Tabelle 'p2_cluster'. Enthält Attribute wie 'cluster_label' und 'beschreibung', eine Beziehung zu dem 'Projekt'-Modell und externe Abhängigkeiten auf 'HasUuids' und 'BelongsTo'.
}
```

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Definiert ein Eloquent-Model für die Tabelle 'p2_mapping_suchstring_komponenten'. Enthält Attribute wie projekt_id, komponente_label, suchbegriffe, sprache und trunkierung_genutzt. Hat eine Beziehung zu einer Projekt-Klasse.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Definiert ein Eloquent-Model für die Tabelle 'p2_review_typ_entscheidung', enthält Relation zu Projekt und speichert Bewertungen für Reviewtypen.

---

### app/Models/Recherche/P2Trefferliste.php
Definiert das Eloquent-Model P2Trefferliste für die Datenbanktabelle p2_trefferlisten. Implementiert UUIDs und verfügt über eine Beziehung zu dem Projekt-Model.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p3_datenbankmatrix', um Zugriff auf die Datenbankmatrix-Daten zu ermöglichen. Bietet Methoden für das Abrufen der zugehörigen Projekt-Beziehungen.

---

### app/Models/Recherche/P3Disziplin.php
Definiert eine Eloquent-Modelklasse für P3Disziplin in der Datenbank. Verwendet UUIDs und einen foreign key zur Projekt-Tabelle.

---

### app/Models/Recherche/P3GeografischerFilter.php
Definiert ein Eloquent-Model für die Tabelle p3_geografische_filter mit UUIDs, Beziehungen zu Projekten und datentypischen Zuordnungen.

---

### app/Models/Recherche/P3GraueLiteratur.php
Definiert ein Eloquent-Modell für die Tabelle 'p3_graue_literatur', um Zugriff auf Daten in der Datenbank zu ermöglichen. Stellt eine Beziehung zur 'Projekt'-Tabelle her.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Definiert das Eloquent-Model für das P4Anpassungsprotokoll mit Uuid, Zeitstempel deaktiviert, Fillable und Casting. Enthält eine Beziehung zur P4Suchstring-MODEL.

---

### app/Models/Recherche/P4Suchstring.php
Model für P4Suchstrings in der Datenbank mit Eloquent ORM. Enthält Beziehungen zu Projekten und Protokollen.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Definiert eine Eloquent-Modellklasse für die Tabelle p4_thesaurus_mapping mit Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P5PrismaZahlen.php
Definiert das Eloquent-Modell für P5 Prisma Zahlen mit Beziehungen und Datenbank-Zeiten.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Definiert das Eloquent-Model für die Tabelle 'p5_screening_entscheidungen', einschließlich der Beziehung zu 'P5Treffer'.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Definiert das P5ScreeningKriterium-Modell mit einer UUID, ohne Timestamps, und bezieht sich auf ein Projekt.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Definiert eine Eloquent-Model-Klasse für Entcheidungen im P5-Tool. Verbindet Entcheidungen mit Projekten und speichert grundlegende Metadaten.

---

### app/Models/Recherche/P5Treffer.php
Definiert das Eloquent-Model P5Treffer für Datenbankzugriff auf Tabelle p5_treffer. Enthält Beziehungen zu anderen Modellen wie Projekt, Duplikate und Screenings.

---

### app/Models/Recherche/P6Luckenanalyse.php
Definiert ein Eloquent-Model für die Tabelle p6_luckenanalyse mit Beziehungen und zulässigen Feldern.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Definiert das Eloquent-Modell für die Tabelle 'p6_qualitaetsbewertung' mit Uuid, ohne Zeitenstempel. Enthält Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P7Datenextraktion.php
Definiert eine Eloquent-MODEL Klasse für die Datenextraktion in der Tabelle p7_datenextraktion mit einem UUID-Primärschlüssel und einer Beziehung zur P5Treffer-Tabelle.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Definiert eine Eloquent-Model-Klasse für die Tabelle p7_grade_einschaetzung. Enthält Methoden zur Verbindung mit der Projekt-Tabelle.

---

### app/Models/Recherche/P7MusterKonsistenz.php
Definiert das Datenmodell für die Tabelle 'p7_muster_konsistenz' mit dem Modul 'HasUuids'. Beinhaltet eine Beziehung zu dem Modell 'Projekt'.

---

### app/Models/Recherche/P7SyntheseMethode.php
Eloquent Model für die Tabelle 'p7_synthese_methode' mit UUIDs, ohne Timestamps. Enthält eine Beziehung zu Projekt.

---

### app/Models/Recherche/P8Limitation.php
Definiert die Datenmodellklasse P8Limitation mit einer Beziehung zu Projekten. Verwendet UUIDs und arbeitet mit der Tabelle p8_limitationen in der Datenbank.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Definiert das Eloquent-Model für die Tabelle p8_reproduzierbarkeitspruefung, um Daten der Recherche mit Bezug auf die Reproduzierbarkeit zu speichern.

---

### app/Models/Recherche/P8Suchprotokoll.php
Die Datei P8Suchprotokoll.php definiert eine Eloquent-Model-Klasse für das Datenbankschema 'p8_suchprotokoll'. Sie enthält Attribute wie 'suchstring_id', 'datenbank', und 'suchdatum', und verbindet sich mit der Klasse P4Suchstring über einen foreign key. Das Model nutzt UUIDs und kastet bestimmte Felder.

---

### app/Models/Recherche/P8UpdatePlan.php
Definiert ein Eloquent-Model für den Datenzugriff auf die Tabelle 'p8_update_plan'. Stellt Methoden zur Beziehung zum Projekt bereit.

---

### app/Models/Recherche/PaperEmbedding.php
Definiert eine Eloquent-Model-Klasse für PaperEmbeddings in der Datenbank.

---

### app/Models/Recherche/Phase.php
Definiert ein Eloquent-Model für die Tabelle 'phasen', enthält Attribute wie 'phase_nr', 'titel', 'status' und eine Beziehung zur Klasse 'Projekt'.

---

### app/Models/Recherche/Projekt.php
Diese Datei enthält die Model-Klasse für Projekte in einer Forschungsanwendung. Sie definiert die Struktur der Projekte und ihre Beziehungen zu anderen Modellen wie Benutzern, Phasen und verschiedenen Forschungsschritten (P1, P2, P3). Das Modell nutzt Eloquent von Laravel für ORM-Funktionen, Spatie Activitylog zur Protokollierung Änderungen und UUIDs. Die Klasse hat Methoden zur Definition der Beziehungen zwischen Projekten und anderen Datenstrukturen.

---

### database/factories/UserFactory.php
Generiert User-Modelle für Tests, mit Optionen zur Manipulation von Eigenschaften.

---

### database/factories/Recherche/ProjektFactory.php
Die Datei ProjektFactory.php definiert einen Factory für das Model Projekt. Sie erstellt zufällige Projektdaten, die in der Datenbank gespeichert werden können.

---

### database/migrations/0001_01_01_000000_create_users_table.php
This PHP file defines database migration classes for creating tables like 'users', 'password_reset_tokens', and 'sessions' using Laravel's Schema builder. It includes methods to create these tables (up) and drop them (down).

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Erstellt Tabellen für Cache-Daten und Cache-Sperrungen in der Datenbank.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Dieses PHP-Skript erstellt Migrationsdateien für die Laravel-Datenbank, um Tabelle für Aufgaben (jobs), Aufgabensätze (job_batches) und fehlgeschlagene Aufgaben (failed_jobs) zu definieren.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Lässt Änderungen an der users-Tabelle vor und zurücknehmen. Fügt Spalten für Zwei-Faktor-Authentifizierung hinzu und entfernt sie wieder.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Die Datei erstellt oder löscht eine Datenbank-Tabelle namens 'contacts' mit verschiedenen Feldern für Kontakte.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Diese Datei enthält eine Laravel-Migration zur Erstellung und Entfernung der 'page_views' Tabelle, die den Besuchszähler für Seiten speichert.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Die Datei erstellt Migrationen für die Berechtigungs- und Rollentabellen in einer Datenbank. Sie definiert Modelle für Berechtigungen, Rollen und ihre Beziehungen.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Dieses PHP-Skript ist eine Laravel-Migration, die neue Spalten in der 'page_views'-Tabelle hinzufügt und wieder entfernt. Wichtigste Klassen: Migration, Schema, Blueprint.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Die Datei erstellt oder löscht eine Tabelle 'consents' in der Datenbank, die Benutzerzustimmungen speichert.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Erstellt SQLite-Tabelle für Recherche-Daten und aktualisiert eine vorhandene 'users'-Tabelle.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei enthält Migrationen für PostgreSQL-Datenbanken, die eine UUID-Erweiterung und pg_trgm-Extension installieren und benutzerdefinierte ENUM-Typen erstellen.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Erstellt Migrationsdatei für Projekte und Phasen-Tabelle in PostgreSQL. Definiert UUIDs, Foreign Keys und zusätzliche Spalten.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
Dieses PHP-Skript ist eine Migration zur Erstellung neuer Tabellen in einer PostgreSQL-Datenbank. Es überprüft die Existenz der Tabellen und erstellt sie bei Bedarf mit entsprechenden Feldern und Fremdschlußeln.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Erstellt Migrationen für PostgreSQL um Tabellen 'p2_review_typ_entscheidung', 'p2_mapping_suchstring_komponenten', 'p2_trefferlisten' und 'p2_cluster' zu erstellen. Jede Tabelle hat eine UUID PRIMARY KEY, einen projekt_id FOREIGN KEY auf die Tabelle 'projekte' und weitere Spalten mit verschiedenen Datenfeldern.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Diese Datei enthält Migrationen für PostgreSQL, um Tabellen 'p3_disziplinen', 'p3_datenbankmatrix', 'p3_geografische_filter' und 'p3_graue_literatur' zu erstellen. Jede Tabelle verfügt über eine UUID-Spalte als Primärschlüssel und foreign key Beziehungen zur Tabelle 'projekte'. Constraints werden hinzugefügt, um bestimmte Werte für Spalten wie 'art' und 'zugang' zu gestzen.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
Dieses PHP-Skript erstellt Migrationen für PostgreSQL-Tabellen 'p4_suchstrings', 'p4_thesaurus_mapping' und 'p4_anpassungsprotokoll'. Es überprüft, ob die Tabellen bereits existieren, bevor sie erzeugt werden. Der Skript verwendet Laravel-Migrationssyntax.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Diese Datei enthält Migrationen für PostgreSQL-Datenbanken, um Tabellen wie p5_prisma_zahlen, p5_screening_kriterien und p5_treffer zu erstellen. Sie überprüft vorab, ob die Tabelle bereits existiert, bevor sie created wird.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Dieses PHP-Skript erstellt Migrationsdateien für PostgreSQL, die Tabellen 'p6_qualitaetsbewertung' und 'p6_luckenanalyse' in der Datenbank erstellt oder aktualisiert.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Dieser PHP-Datei handelt sich um eine Laravel-Migration, die PostgreSQL-Schemas für verschiedene Tabellen erstellt und aktualisiert. Die wichtigsten Klassen sind Migration und Schema Blueprint. Sie verwenden externe Abhängigkeiten wie DB::raw() und uuid_generate_v4().

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Die Datei erstellt Migrationen für PostgreSQL-Datenbanktabellen wie 'p8_suchprotokoll', 'p8_limitationen', 'p8_reproduzierbarkeitspruefung' und 'p8_update_plan'. Sie überprüft, ob die Tabellen bereits existieren, bevor sie sie anlegt. Jede Tabelle hat spezifische Spalten und Fremdschlüsselbeziehungen.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Diese Datei enthält Migrationsklauseln für PostgreSQL, die verschiedene Indizes auf Tabellen erstellen oder löschen, um die Leistung von Full-Text-Suche und Fremdschlüsselabfragen zu verbessern.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Eine PostgreSQL-Funktion zur Berechnung von Prisma-Zahlen für einen bestimmten Projekt-ID. Die Funktion gibt aggregierte Daten zu Duplikaten und Screening-Ergebnissen zurück.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Dieses Skript erstellt die Tabelle 'chat_messages' in PostgreSQL mit UUIDs, foreign keys und CHECK-Bedingungen.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Die Datei erstellt und entfernt die 'webhooks'-Tabelle in einer PostgreSQL-Datenbank, fügt eine 'webhook_id' zu 'chat_messages' hinzu und verknüpft sie mit der 'webhooks'-Tabelle.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Migrates the database to add a table for paper embeddings with specific PostgreSQL features.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Datei erstellt eine Tabelle 'activity_log' zur Aufzeichnung von Aktivitäten in der Datenbank.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
This migration file adds a `status` column to the `users` table with specific enumerated values and handles database-specific syntax for PostgreSQL and SQLite. It includes functions for both migrating up (adding column) and down (dropping column).

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Migrates the database to add a 'frontend_object' column with ENUM type to the 'webhooks' table. Handles PostgreSQL only.

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
Diese Datei enthält Migrationen für die Datenbank, um das 'password'-Feld in der 'webhooks' Tabelle für PostgreSQL-Datenbanken nullable zu machen und einen Standardwert hinzuzufügen. Die Migration kann rückgängig gemacht werden, um das Feld nicht nullable wiederzuerstellen.

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
Lädt PostgreSQL-Spezifische Migrationen, fügt der Tabelle 'webhooks' das Spalte 'secret' hinzu und entfernt sie bei Rückwirkung.

---

### database/seeders/DatabaseSeeder.php
Dieses PHP-Skript ist ein Datenbankseeder, der den Anwendungsentwicklungsumfeld initialisiert. Es führt mehrere Seeder aus, um die Datenbank mit Rollen, Benutzern und anderen Datensätzen vorzubereiten.

---

### database/seeders/RechercheDemoSeeder.php
Dieses Seeder initializes a demo project for research in the field of AI-supported image analysis in radiology. It seeds webhooks, phases, and various components like P1 to P7.

---

### database/seeders/RoleSeeder.php
Das Script erstellt zwei Rollen: 'admin' und 'user'. Es verwendet die Spatie Permission-Bibliothek für das Verwalten von Berechtigungen.

---

### database/sql/create_langdock_agent_user.sql
Erstellt und konfiguriert die Datenbankrolle 'langdock_agent' mit eingeschränkten Rechten auf bestimmte Tabellen und Funktionen im Schema 'public'.

---

## temp_dummy

### .github/pull_request_template.md
README file for GitHub pull request template.

---

## tests

### tests/Pest.php
Dieses PHP-Skript definiert einen Testfall mit Pest und erweitert ihn um die PHPUnit-Testklasse. Es fügt eine benutzerdefinierte Erwartungsmethode 'toBeOne' hinzu und enthält eine globale Hilfsfunktion 'something'.

---

### tests/TestCase.php
Abstrakte Basisklasse für TestCases im Laravel-Framework

---

### tests/Feature/ContactTest.php
Die Datei enthält Tests für die Kontaktaufnahme-Funktion. Sie prüft die Validität der Eingaben und überprüft, ob die Daten erfolgreich in der Datenbank gespeichert werden.

---

### tests/Feature/DashboardTest.php
```json
{
  "file_summary": "Dieser Testdatei prüft die Zugriffsberechtigungen auf den Dashboard-Endpunkt. Die wichtigsten Klassen sind `User`. Externe Abhängigkeiten umfassen `route()` und `actingAs()`.
}
```

---

### tests/Feature/ExampleTest.php
Tests the home route to ensure it returns a successful response (HTTP 200).

---

### tests/Feature/LegalPagesTest.php
Die Datei enthält Tests für die Rendition der Impressum, Datenschutz- und Geschäftsbedingungen-Seiten. Die Tests überprüfen den Status des HTTP-Antwortes auf 200 und stellen sicher, dass bestimmte Inhalte sichtbar sind.

---

### tests/Feature/ProjektPolicyTest.php
Tests für die ProjektPolicy, um Zugriff auf Projekte basierend auf Benutzerrollen zu kontrollieren.

---

### tests/Feature/RechercheTest.php
Dieses Skript enthält Tests für das Recherche-Feature. Es verwendet Livewire für die Interaktionen und Queue für den Asynchronen Prozessierung von Jobs.

---

### tests/Feature/WebhookTest.php
This file contains tests for a webhook endpoint, validating signature, timestamp, and queue handling.

---

### tests/Feature/WelcomeTest.php
Die Datei tests/Feature/WelcomeTest.php enthält Testfunktionen für die Willkommensseite. Es testet, ob die Seite erfolgreich geladen wird, ob der Besuchszähler korrekt erhöht wird, ob eine neue PageView-Eintragung erstellt wird, wenn keine vorhanden ist, und ob verschiedene Benutzergruppen unterschiedliche Links sehen.

---

### tests/Feature/Auth/AuthenticationTest.php
Die Datei enthält Tests für die Authentifizierungsfunktionalität. Sie testet das Anmelden, die erfolgreiche Authentifizierung mit korrektem Passwort, den Fehlversuch bei der Authentifizierung mit falschem Passwort, die Weiterleitung von Nutzern mit Aktiviertem Zwei-Faktor und das Abmelden.

---

### tests/Feature/Auth/EmailVerificationTest.php
Dieses PHP-Testskript prüft die Funktionalität der E-Mail-Bestätigung für Benutzer. Es enthält Tests für das Rendern der Bestätigungsseite, die erfolgreiche Bestätigung mit gültiger Hash und die Reaktion auf ungültige Hashes oder bereits bestätigte Konten.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Die Datei enthält einen Test, der überprüft, ob die Passwortbestätigungsinformationen auf der Seite angezeigt werden können. Sie verwendet die Laravel-Framework-Testing-Hilfsmethoden und das User-Modell.

---

### tests/Feature/Auth/PasswordResetTest.php
Tests für das Passwort zurücksetzen-Feature, einschließlich der Generierung von Linken und des Prozesses des Passwortes zurückzusetzen.

---

### tests/Feature/Auth/RegistrationTest.php
Dieses Testskript überprüft die Registrierungsfunktionalität durch das Rendern der Anmeldeformulars und das erfolgreiche Registrieren eines neuen Benutzers.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Die Datei enthält Test-CASEs für die Zweistellennachweis-Funktionalität in einer Laravel-Anwendung. Sie überprüft, ob der Benutzer zur Login-Seite umgeleitet wird, wenn er nicht authentifiziert ist, und ob die Zweistellennachweis-Seite gerendert werden kann.

---

### tests/Feature/Chat/BigResearchChatTest.php
Die Datei enthält Testfälle für den BigResearchChat-Feature. Sie erstellen Benutzer und Webhooks, senden Nachrichten über eine API und überprüfen die Datenbankinteraktionen sowie die Verarbeitung von HTTP-Anforderungen.

---

### tests/Feature/Recherche/PhaseP1Test.php
Tests für die Funktionen der Phase P1 der Recherche. Prüft das Erstellen, Bearbeiten, Validieren und Löschen von Strukturmodellwählungen sowie den Zugriff von Benutzern auf diese Funktionen.

---

### tests/Feature/Recherche/PhaseP2Test.php
Testklasse für P2-Review-Typen und Clustereinstellungen. Erstellt, bearbeitet und löscht Review-Typen sowie Cluster in Zusammenhang mit Projekten.

---

### tests/Feature/Recherche/PhaseP3Test.php
Dieses Skript enthält Tests für die Funktionalität der Phase P3 einer Recherche. Es testet das Erstellen, Bearbeiten und Löschen von Datenbankmatrizen sowie das Hinzufügen von Disziplinen zu Projekten.

---

### tests/Feature/Recherche/PhaseP4Test.php
Die Datei enthält Tests für die Funktionalität des P4-Suchstrings in der Recherche-Phase. Sie testet das Erstellen, Validieren, Bearbeiten und Löschen von Suchstrings sowie die Speicherung der Filter in der Datenbank.

---

### tests/Feature/Recherche/ProjektDetailTest.php
Die Datei enthält Tests für das Projekt-Detail in einer Rechercheanwendung. Sie testet das Rendern des Detailbildschirms, die Wechselung der Registerkarten, den Schutz der Seite vor Nicht-Ownern, die Anzeige von Phasenkomponenten und den Link zum Projektübersicht.

---

### tests/Feature/Settings/AppearanceTest.php
Tests für die Darstellung und Funktionalität der Erscheinungseinstellungen eines Benutzers. Testet, ob die Buttons mit den richtigen data-mode Werten angezeigt werden, ob der appearance-toggle Container vorhanden ist, ob ein client-seitiges Script zum Ändern der Erscheinung vorhanden ist, ob die Seite nur für eingeloggte Benutzer zugänglich ist und ob die Seite korrekt geladen wird.

---

### tests/Feature/Settings/PasswordUpdateTest.php
Die Datei enthält Tests für die Passwortaktualisierungsfunktionalität in einer Anwendung. Sie prüft, ob ein Benutzer sein Passwort korrekt aktualisieren kann und ob ein falsches aktuelles Passwort einen Fehler zurückgibt.

---

### tests/Feature/Settings/ProfileUpdateTest.php
Dieses PHP-Skript enthält Tests für die Benutzerprofileinstellungen und -löschung in einer Anwendung. Es verwendet Laravel Livewire für Komponententests.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
Die Datei enthält Tests für die Two-Factor-Authentifizierung-Einstellungen. Es prüft, ob der Zweistufige Authentifizierungsseiten gerendert werden kann, ob sie bei aktivierter Authentifizierung eine Passwortbestätigung erfordert und ob der Zugriff verboten ist, wenn die Authentifizierung deaktiviert ist. Es testet auch, wie sich die Authentifizierung verhält, wenn die Bestätigung zwischen Anfragen aufgegeben wird.

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei `WebhooksTest.php` enthält Tests für die Funktionalität der Webhook-Einstellungen in einer Laravel-Anwendung. Es verwendet Livewire zur Interaktion mit dem Frontend und prüft Methoden wie das Speichern, Aktualisieren und Validieren von Webhooks.

---

### tests/Unit/ExampleTest.php
Dies ist ein Testskript für PHPUnit, das überprüft, ob der Wert 'true' tatsächlich wahr ist.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Die Datei ListContacts.php definiert eine Benutzer-Oberfläche zur Anzeige einer Liste von Kontakten, indem sie die Klasse ListRecords erweitert.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert die Ansichts-Klasse für den Kontakt-Betrachtungs-Endpunkt in Filament. Verwendet externe Klasse ViewRecord.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Diese Datei definiert die Seite für die Bearbeitung von Benutzern im Filament-Admin-Bereich. Die wichtigste Klasse ist EditUser, die von EditRecord erbt und die Ressource UserResource verwendet.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Diese Datei definiert eine Seite zur Liste von Benutzern in der Filament UI.

---

### resources/views/dashboard.blade.php
Diese Datei rendert das Dashboard für Benutzer, zeigt willkommenstext, Schnelllinks zu Recherche und Adminbereich, sowie die letzten Projekte.

---

### resources/views/welcome.blade.php
Eine Blade-Template-Datei für die Willkommensseite von Linn Games, mit HTML, CSS und JavaScript-Komponenten.

---

### resources/views/components/action-message.blade.php
Die Datei definiert ein Vue.js Komponente für eine Aktion-Nachricht, die nach einer bestimmten Eingabe angezeigt wird und automatisch nach 2 Sekunden versteckt.

---

### resources/views/components/app-logo-icon.blade.php
SVG-Bild eines Logos mit Interaktiven Attributen

---

### resources/views/components/app-logo.blade.php
Die Datei rendert ein Logo mit dem Icon 'x-app-logo-icon' und den Text 'Laravel Starter Kit'.

---

### resources/views/components/auth-header.blade.php
Die Datei auth-header.blade.php ist ein Blade-Vorlagenteil für eine UI-Komponente, die einen Überschriftstitel und eine Beschreibung in der Mitte einer Seite anzeigt.

---

### resources/views/components/auth-session-status.blade.php
Renders an authentication session status message if provided.

---

### resources/views/components/placeholder-pattern.blade.php
Renders a placeholder SVG with a repeating diagonal pattern.

---

### resources/views/components/layouts/app.blade.php
Renders a sidebar layout with a title and a main content area.

---

### resources/views/components/layouts/auth.blade.php
Definiert eine Authentifizierungslayout-View mit einem einfachen Design.

---

### resources/views/components/layouts/app/header.blade.php
Diese Datei definiert den Header für das App-Layou

---

### resources/views/components/layouts/app/sidebar.blade.php
Diese Datei definiert das Layout der Seitenleiste für die Anwendung. Sie enthält ein Sidebar-Menü mit verschiedenen Navigationspunkten wie Dashboard und Recherche. Das Menü ist auf mobilförmige Geräte anpassbar und verfügt über einen Schließbutton. Die Sidebar kann durch Klicken auf eine Backdrop verborgen oder angezeigt werden.

---

### resources/views/components/layouts/auth/card.blade.php
Diese Datei definiert ein Authentifizierungs-Layout für eine Anwendung. Sie enthält die Struktur eines Card-Components mit einer Logo-Ansicht und einem Inhaltsbereich, der durch das `$slot`-Placeholder definiert wird. Die Datei verwendet Blade-Tags zur Verbindung von PHP-Funktionen und Vue.js-LiveWire-Skripten.

---

### resources/views/components/layouts/auth/simple.blade.php
Eine Vue.js Komponente für ein einfaches Authentifizierungs-Layout mit Logo, Navigation und Cookie-Banner.

---

### resources/views/components/layouts/auth/split.blade.php
Die Datei ist ein Blade-Template für eine Authentifizierungsseite mit einer linken Sidebar und rechter Hauptbereich. Sie enthält externe Abhängigkeiten wie Laravel, Bootstrap, Tailwind CSS und Alpine.js.

---

### resources/views/components/settings/layout.blade.php
Diese Datei definiert den Layout für die Einstellungen in einer UI, mit Links zu verschiedenen Einstellungsoptionen wie Profil, Passwort, Zwei-Faktor-Authentifizierung, Erscheinung und Webhooks.

---

### resources/views/emails/contact-confirmation.blade.php
Diese Datei ist eine Blade-Vuew für eine E-Mail-Bestätigung nach einer Kontaktaufnahme. Sie zeigt den Namen des Anfragenden, den Projekttyp, den Zeitrahmen (wenn vorhanden) und die Nachricht an.

---

### resources/views/emails/contact-inquiry.blade.php
Renders an HTML email template for a contact inquiry, displaying details like name, company, email, project type, timeline, and message. Externally depends on PHP variables passed to the view.

---

### resources/views/emails/deploy-notification.blade.php
HTML-Vorlage für eine Bereitstellungsbenachrichtigung via E-Mail

---

### resources/views/layouts/legal.blade.php
HTML Layout für Rechtliche Informationen mit benutzerdefinierten Stilen und Struktur.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für die Website und das Online-Spiel von Linn.Games. Sie definiert den Geltungsbereich, die Registrierungs- und Nutzungsvorschriften, Kostenstruktur, digitale Käufe, Haftungsausschluss und Sanktionen bei Verstößen.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei ist eine Blade-Template-Datei für die Datenschutzerklärung einer Website und eines Spiels. Sie enthält Informationen über die verantwortliche Stelle, die Arten der verarbeiteten Daten, die Zwecke der Verarbeitung sowie Rechtsgrundlagen. Die Datei verwendet externe Abhängigkeiten wie Blade-Syntax für die Erstellung von HTML-Inhalten.

---

### resources/views/legal/impressum.blade.php
Die Datei enthält den Impressum für die Website, der Informationen über den Inhaber und Kontaktinformationen enthält. Sie erweitert eine grundlegende Legal-Layoutdatei.

---

### resources/views/livewire/auth/confirm-password.blade.php
Dies ist ein Blade-Template für die Passwortbestätigung auf der Benutzeroberfläche. Es enthält eine Formular zur Eingabe des Passworts und zeigt Fehlermeldungen an, wenn erforderliche Daten nicht korrekt eingegeben wurden.

---

### resources/views/livewire/auth/forgot-password.blade.php
Einfache Login-Page für den Reset des Passworts. Zeigt einen Formular zur Eingabe der E-Mail-Adresse und sendet ein Link zum Zurücksetzen des Passworts, wenn die E-Mail gültig ist.

---

### resources/views/livewire/auth/login.blade.php
Diese Datei enthält das Blade-Vorlage für den Login-Bildschirm einer Authentifizierung. Sie verwendet Komponenten wie <x-auth-header>, <x-auth-session-status> und ein Formular zur Eingabe von Benutzerdaten (Email, Passwort) sowie Einloggungsinformationen (Erinnerung). Es verarbeitet CSRF-Token und Fehlermeldungen. Der Login-Button leitet auf einen Controller-Routenendpunkt 'login.store'.

---

### resources/views/livewire/auth/register.blade.php
Die Datei ist ein Blade-Vorlage für den Registrierungsformular in der Authentifizierung. Sie enthält das Layout, Formularfelder für Name, E-Mail und Passwort sowie Fehlermeldungen. Der Formularaktion wird der Route 'register.store' zugeordnet.

---

### resources/views/livewire/auth/reset-password.blade.php
Diese Datei enthält ein Blade-Templat für das Passwort zurücksetzen in einer Authentifizierungs-Anwendung. Es verwendet Komponenten wie <x-auth-header> und <x-auth-session-status>, um den Benutzer über die aktuelle Sitzung zu informieren. Das Formular sendet einen POST-Antrag auf den Pfad '/password/update' mit dem neuen Passwort, der E-Mail-Adresse und einem Token zur Bestätigung des Verifizierungslinks.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Dieser Blade-Template ist für die Two-Factor-Authentifizierungs-Challenge in einer Anwendung. Er enthält eine Benutzeroberfläche zum Eingeben eines Authentifizierungs-Codes oder einer Notfallwiederherstellungscode sowie einen Formular für den Login.

---

### resources/views/livewire/auth/verify-email.blade.php
Ein Blade-Vorlage für die E-Mail-Bestätigungsoberfläche in einer Authentifizierungsanwendung. Zeigt eine Nachricht, wenn ein Überprüfungslink gesendet wurde, und two Formulare zum erneuten Senden des Links und zum Abmelden.

---

### resources/views/livewire/chat/big-research-chat.blade.php
```json
{
  "file_summary": "Diese Datei enthält ein Livewire-Komponente zur big-research-chat-Funktion. Es verarbeitet Benutzermessages, sendet sie an einen Webhook und zeigt die Chatnachrichten an. Die wichtigsten Klassen sind Component, ChatMessage und Webhook. Externe Abhängigkeiten sind Illuminate\Support\Facades\Auth, Illuminate\Support\Facades\Http und Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/phase-p1.blade.php
Dies ist eine Livewire-Komponente für die Verwaltung von P1-Strukturmodellwahl, Komponenten, Kriterien und Warnsignale in einem Projekt. Sie bietet CRUD-Operationen für diese Entitäten.

---

### resources/views/livewire/recherche/phase-p2.blade.php
Diese Datei enthält ein Livewire-Komponenten-Skript für die Bearbeitung von Daten im Projekt PHASE P2. Es definiert Formulare und Funktionen zur Verwaltung von ReviewTypEntscheidungen, Clustern, MappingSuchstringKomponenten und Trefferlisten. Die Komponente bietet CRUD-Funktionalität und ist durch externe Modelle (Projekt, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, P2Trefferliste) und Authentifizierung geschützt.

---

### resources/views/livewire/recherche/phase-p3.blade.php
Livewire-Komponente für die Recherche-Phase P3. Manages forms for Datenbankmatrix, Disziplin, GeografischerFilter und GraueLiteratur.

---

### resources/views/livewire/recherche/phase-p4.blade.php
Livewire Component for P4 research phase. Manages search strings, thesaurus mappings, and adjustment protocols for a project.

---

### resources/views/livewire/recherche/phase-p5.blade.php
Eine Livewire-Komponente zur Anzeige und Bearbeitung von Prisma-Zahlen, Screening-Kriterien, Screening-Tool und Treffer-Screening für einen Projekt. Stellt Formulare zur CRUD-Operationen bereit.

---

### resources/views/livewire/recherche/phase-p6.blade.php
Livewire-Komponente für die Recherche-Phase P6. Stellt Formulare zur Qualitätsbewertung und Lückenanalyse bereit.

---

### resources/views/livewire/recherche/phase-p7.blade.php
Diese Datei ist ein Livewire-Komponenten-View für die Bearbeitung von Projektphasen P7 in einer Recherche. Er enthält Formulare und Funktionen zur Verwaltung von Synthese-Methode, Datenextraktion, Muster & Konsistenz sowie GRADE-Einschätzung. Die Komponente verwendet externe Modelle für die Datenbank-Zugriffe.

---

### resources/views/livewire/recherche/phase-p8.blade.php
Dieses File enthält eine Livewire-Komponente für die Verwaltung eines Projekts mit Formularen für Suchprotokoll, Limitation, Reproduzierbarkeit und Update-Plan. Die Komponente nutzt Modelle wie Projekt, P4Suchstring, P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung und P8UpdatePlan sowie externe Abhängigkeiten wie Authentifizierung.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Die Datei 'projekt-detail.blade.php' ist eine Vue.js Komponente zur Anzeige und Bearbeitung eines Projekts mit Tabularenansicht. Sie verwendet externe Abhängigkeiten wie Laravel, Livewire und Blade Templates. Die wichtigsten Funktionen sind das Laden des Projekts, Wechsel der Registerkarten und Abruf des Status der Projektphasen.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Eine Livewire-Komponente zum Anzeigen und Verwalten von Projekten eines Benutzers. Bereitet eine Liste der Projekte vor und bietet Optionen zur Projekt-Lösung.

---

### resources/views/livewire/recherche/research-input.blade.php
Diese Datei enthält ein Livewire-Komponente für eine Forschungsinput-Formular, die den Benutzer in einen Forschungsprojekt einbindet und den TriggerLangdockAgent mit den erforderlichen Daten startet.

---

### resources/views/livewire/settings/appearance.blade.php
Eine Livewire-Komponente für die Anzeigeeinstellungen des Benutzers, einschließlich Farbschemasetzung (Light, Dark, System).

---

### resources/views/livewire/settings/delete-user-form.blade.php
Ein Livewire-Komponente zur Benutzerkontenlöschung. Stellt einen Formular zur Bestätigung der Löschung anbietet und überprüft das Passwort.

---

### resources/views/livewire/settings/password.blade.php
Eine Livewire-Komponente zur Aktualisierung des Passworts des angemeldeten Benutzers. Die Komponente überprüft die aktuellen und neuen Passwörter, validiert sie und aktualisiert das Passwort im Datenbank.

---

### resources/views/livewire/settings/profile.blade.php
Diese Datei enthält ein Livewire-Komponenten für das Profil des angemeldeten Benutzers. Es ermöglicht es dem Benutzer, sein Name und E-Mail-Adresse zu aktualisieren und eine Verifizierungs-E-Mail erneut zu senden. Die Komponente verwendet die Authentifizierung und Validierung von Laravel sowie externe Abhängigkeiten wie User-Modelle und Session.

---

### resources/views/livewire/settings/two-factor.blade.php
Diese Datei ist ein Livewire-Component für die Verwaltung des Zwei-Faktor-Authentifizierungs (2FA) in der Anwendung. Er ermöglicht das Aktivieren, Deaktivieren und Bestätigen des 2FA für den eingelognten Benutzer. Die Komponente verwendet externe Aktionen zur Interaktion mit dem Fortify-Paket für die Verwaltung von 2FA.

---

### resources/views/livewire/settings/webhooks.blade.php
Livewire-Komponente zur Verwaltung von Webhooks für Dashboard-Chats und Recherchen. Bereitet Daten vor, speichert Änderungen in der Datenbank und aktualisiert die Ansicht.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
Livewire-Komponente zur Verwaltung von 2FA-Recovery-Codes

---

### resources/views/partials/head.blade.php
Diese Datei enthält die Kopfteil (head) eines HTML-Dokuments mit Metadaten, Titel, Favicons und Schriftarten. Sie initialisiert das Dark-Mode basierend auf Benutzereinstellungen und lädt CSS und JavaScript mit Vite.

---

### resources/views/partials/settings-heading.blade.php
Zeigt den Überschriftsbereich für die Einstellungen mit Titel und Beschreibung.

---

### resources/views/recherche/index.blade.php
Diese Datei ist die Blade-Vuew für die Recherceseite. Sie enthält den Layout und zwei Livewires: 'research-input' und 'projekt-liste'.

---

### resources/views/recherche/show.blade.php
Diese Datei rendert die Detailansicht eines Projekts mit der Hilfe von Laravel Blade und Livewire.

---

## uncategorized

### Readme.md
Readme.md beschreibt eine Laravel-Anwendung für die Literaturrecherche mit KI-integriertem Support. Es enthält Details zur Architektur, verwendeten Technologien und wichtigen Funktionen wie Authentifizierung, Projektverwaltung und Admin-Panel.

---

### artisan
Die Datei 'artisan' ist ein PHP-Skript zur Ausführung von Laravel-Kommandozeilenbefehlen. Es bootstrapt die Laravel-Anwendung und verarbeitet den übergebenen Befehl.

---

### basemodel.md
Definiert ein PostgreSQL-Datenbankmodell für eine Systematische Literaturrecherche. Enthält ENUM-Typen für verschiedene Phasenstatus und review-typen, sowie Tabellen für Projekte und Phasentracking.

---

### composer.json
composer.json: Definiert Abhängigkeiten und Konfiguration für das Laravel-Projekt 'nileneb/app-linn-games'.

---

### CONTRIBUTING.md
Das CONTRIBUTING.md enthält Richtlinien für die Teilnahme am Projekt app.linn.games, einschließlich Branch-Konventionen, Merge-Fluss, Arbeitsablauf pro Issue, Lokalen Entwicklung, Coding-Standards, Datenbank-Änderungen und Tests.

---

### deploy.sh
Skript zum Produktionsbuild und -deploy der app.linn.games-Anwendung. Stellt sicher, dass Docker installiert ist, führt Vorabprüfungen durch, baut die Images (falls nicht übersprungen), startet die Infrastruktur, führt Datenbankmigrationen durch, optimiert Cache, löscht veraltete Volumes und startet alle Dienste. Sendet eine Deploy-Benachrichtigung.

---

### package.json
Konfiguration für ein Vite-Projekt mit Tailwind CSS, Axios und anderen Abhängigkeiten.

---

### phpunit.xml
Konfigurationsdatei für PHPUnit mit Testsuiten und Umgebungsvariablen.

---

### vite.config.js
Konfiguration für Vite-Setup mit Laravel-Vite-Plugin und TailwindCSS. Definiert Eingabe-Pfade, Plugin-Funktionen und Server-Einstellungen.

---

### .dockerignore
Die .dockerignore-Datei definiert Dateien und Ordner, die nicht in Docker-Builds einbezogen werden.

---

### .editorconfig
EditorConfig-Datei zur Standardisierung von Codeformatierungen.

---

### app/Actions/Fortify/CreateNewUser.php
Diese Datei enthält eine Klasse `CreateNewUser` die als Implementierung des Interfaces `CreatesNewUsers` fungiert. Sie validiert die Eingaben für einen neuen Benutzer und erstellt den Benutzer im System.

---

### app/Actions/Fortify/PasswordValidationRules.php
Diese Datei enthält ein Trait zur Definition standardmäßiger Passwortvalidierungsregeln. Es definiert eine Methode, die ein Array von Regeln zurückgibt.

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei ResetUserPassword.php ist eine Klasse, die das Zurücksetzen eines Benutzerpassworts verarbeitet. Sie verwendet Laravel-Fortify für die Passwortvalidierung und aktualisiert das Passwort im Datenbankmodell User.

---

### app/Filament/Resources/ContactResource.php
Diese Datei definiert eine Filament-Ressource für den Kontaktmodell mit Formular- und Tabellendarstellungen sowie View und Delete-Aktionen.

---

### app/Filament/Resources/UserResource.php
Definiert eine Filament-Ressource für die Benutzerverwaltung, einschließlich Formular- und Tabelle-Konfigurationen sowie Seiten für das Anzeigen und Bearbeiten von Nutzern.

---

### app/Http/Middleware/TrackPageView.php
Tracks page views for non-bot GET requests, anonymizing IP and user agent.

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware zur Überprüfung der Signatur von Langdock-Webhooks. Prüft Timestamp, Signature und prevents Replay-Angriffe.

---

### app/Http/Middleware/VerifyMcpToken.php
Middleware zum Überprüfen des MCP-Authentifizierungs-Tokens.

---

### app/Jobs/IngestPaperJob.php
Dieses Job verarbeitet Papierinhalte in Chunks, fordert EMBEDDINGS von Ollama an und speichert sie in der Datenbank.

---

### app/Jobs/TriggerLangdockAgent.php
Dieses PHP-Skript implementiert ein Background-Job in Laravel, der eine HTTP-Anfrage an den Langdock-Agent sendet. Der Job wird mit User-ID, Projekt-ID und Eingabe Daten versehen.

---

### app/Livewire/Actions/Logout.php
Die Datei Logout.php ist eine Livewire-Aktion zum Abmelden des aktuellen Benutzers. Sie verwendet das Auth-Facade zur Abmeldung und das Session-Facade, um die Sitzung zu invalidieren und neu zu generieren. Anschließend wird der Benutzer auf die Startseite weitergeleitet.

---

### app/Mail/ContactConfirmationMail.php
Diese Datei definiert eine Mail-Klasse für die Bestätigung einer Kontaktanfrage. Sie verwendet Laravel's Mailables und Queueable zur Versendung der E-Mail asynchron.

---

### app/Mail/ContactInquiryMail.php
Diese Datei definiert eine Mailable-Klasse für die Verarbeitung und Queueing von Kontaktanfragen via E-Mail. Sie verwendet externe Abhängigkeiten wie Illuminate's Mailable, Contact-Model und Queueable Trait.

---

### app/Mail/DeployNotificationMail.php
Dieses Skript definiert eine Klasse für die Erstellung einer E-Mail-Benachrichtigung für erfolgreiche Bereitstellungen. Die Klasse verwendet Laravel's Mailable-Konvention und hat einen Konstruktor, um die Bereitstellungsdetails zu übergeben. Es gibt Methoden zum Definieren der Envelope (Betreff) und des Inhalts der E-Mail, die eine Ansicht verwenden.

---

### app/Policies/ProjektPolicy.php
Definiert Zugriffsrechte für Projekte. Nutzer kann alle Projekte anzeigen und eigenständige Projekte anlegen, ändern oder löschen.

---

### app/Providers/AppServiceProvider.php
Initialisiert Sicherheitsrichtlinien und URL-Schema in Produktionsumgebung.

---

### app/Providers/FortifyServiceProvider.php
Konfiguriert Fortify-Actions, Views und Rate-Limiting für die Authentifizierung.

---

### app/Providers/VoltServiceProvider.php
Registriert und bootet die Livewire-Volt-Erweiterung mit den angegebenen Pfaden.

---

### app/Providers/Filament/AdminPanelProvider.php
Konfiguriert den Admin-Bereich von Filament mit Authentifizierung und Middleware.

---

### bootstrap/app.php
Initialisiert eine Laravel-Anwendung mit Routen und Middleware.

---

### bootstrap/providers.php
Listet alle Diensteleiter für eine Anwendung auf, einschließlich Filament-Admin-Panels und Fortify-Sicherheit.

---

### docker/common/nginx/default.conf
Nginx Konfiguration für Docker-Container mit PHP-FPM und Livewire. Stellt HTTP-Services zur Verfügung, verarbeitet statische Dateien, sorgt für Caching und authentifiziert MCP PostgreSQL Endpoint (SSE).

---

### docker/common/nginx/docker-entrypoint.sh
Dieses Skript initialisiert den Nginx-Container. Es kopiert ein Vorlagendatei, ersetzt einen Platzhalter durch eine Umgebungsvariable und führt das Startskript des Containers aus.

---

### docker/common/nginx/dockerfile
Dockerfile zur Konfiguration eines Nginx-Containers mit benutzerdefiniertem Konfigurationsdatei, Secrets-Injektions-Skript und statischen Dateien.

---

### docker/common/nginx/nginx.conf
Nginx Konfiguration für Laravel-Anwendungen, mit Einstellungen für Worker-Verbindungen, MIME-Typen, X-Forwarded-Header Vertrauen, Gzip-Kompression und Datei-Upload-Limits.

---

### docker/common/php-fpm/dockerfile
Dockerfile zur Erstellung von PHP-FPM-Images für unterschiedliche Umgebungen (Build, Production, Testing). Installiert Systemdependencies, PHP-Erweiterungen, Composer und npm abhängigkeiten. Baut Frontend-Assets in der Build-Umgebung.

---

### docker/common/php-fpm/entrypoint.sh
Dieses Skript sorgt für die Bereitstellung von Frontend-Assets, Wartet auf Datenbankverbindung, führt Migrations und Cache-Befehle bei Produktionsumgebungen durch, und führt den Hauptbefehl (php-fpm oder queue:work) aus.

---

### docker/common/postgres/dockerfile
Dockerfile zur Erstellung eines PostgreSQL 16-Basisimages mit installierten Erweiterungen hypopg und pgvector.

---

### docker/common/postgres/init-extensions.sql
Initialisiert PostgreSQL-Erweiterungen.

---

### public/index.php
Die Datei bootstrapt eine Laravel-Anwendung und verarbeitet eingehende HTTP-Anfragen.

---

### public/robots.txt
Konfiguriert die Suchmaschinenkrawler, um den gesamten Inhalt der Website zugänglich zu lassen.

---

### public/.htaccess
Apache .htaccess file for URL rewriting and handling of headers. Redirects trailing slashes, sends requests to the front controller.

---

### resources/css/app.css
CSS Datei mit Tailwind CSS Variablen und Theme-Einstellungen für ein Dark Mode.

---

### storage/app/.gitignore
Gitignore File for Laravel Project, ignoring all files except 'private/', 'public/', and '.gitignore'

---

### .github/copilot-instructions.md
Diese Datei enthält Anweisungen für das GitHub Copilot Tool in Bezug auf die Entwicklung eines Projekts unter Verwendung von Laravel, Livewire, Filament und anderen Technologien. Sie deckt sich mit Stack-Informationen, Architekturentscheidungen, Datenbankstruktur, Testing sowie Deployment-Anleitungen.

---

### .github/instructions/app_linn_games_custom.instructions.md
Diese Datei ist die Hauptdokumentation für das Projekt app.linn.games, eine KI-gestützte Research-Management-Plattform. Sie enthält Informationen zur Architektur, Technologie-Stack, Verzeichnisstruktur und wichtigen Endpunkten wie Langdock-Agenten, Queue-Jobs und Datenbankzugriff.

---

### .github/instructions/docker-dev-workflow.instructions.md
Die Datei beschreibt den Entwicklungsworkflow für eine Docker-basierte Anwendung auf app.linn.games. Sie definiert Containerkonfigurationen, Code-Deployment-Mechanismen und Rebuild-Bedingungen für Entwicklungsumgebung.

---
