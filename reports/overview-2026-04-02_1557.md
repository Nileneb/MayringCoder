---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T15:57:12.595243
model: qwen2.5-coder:7b
run_id: qwen2.5-coder:7b
mode: overview
files_total: 249
errors: 0
run_time_s: 433.4
---

# Funktions-Übersicht — 2026-04-02_1557

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 249
- **Fehler:** 0
- **Laufzeit:** 433.4s

## api

### app/Http/Controllers/ContactController.php
Dieser Controller verarbeitet Anfragen zum Kontaktformular. Er validiert die eingehenden Daten, speichert sie in der Datenbank und sendet Bestätigungs- und Kundennachrichten an verschiedene Adressen.

---

### app/Http/Controllers/Controller.php
Abstrakte Klasse für alle HTTP-Controller in der Anwendung.

---

### app/Http/Controllers/DsgvoController.php
Die Datei enthält die DsgvoController Klasse mit zwei Methoden: export und deleteAccount. Die export-Methode exportiert Benutzerdaten, während die deleteAccount-Methode das Konto des Anwenders löscht.

---

### app/Http/Controllers/LangdockWebhookController.php
Dieser Controller verarbeitet Webhooks von Langdock, validiert die eingehenden Daten und dispatcht eine Job zum Triggern des Langdock-Agents.

---

### app/Http/Controllers/PaperRagController.php
Die Datei PaperRagController.php enthält zwei Hauptfunktionen: 'ingest' für die Aufnahme von Papieren und 'search' für eine Suchfunktion. Beide nutzen Jobs, HTTP-Anfragen an externe APIs sowie Datenbankabfragen.

---

### routes/api.php
Definiert API-Endpunkte für Authentifizierung und Datenverarbeitung. Stellt Controller zur Verarbeitung von Webhooks und Papier-Dokumenten zur Verfügung.

---

### routes/console.php
Definiert zwei Artisan-Kommandos: 'inspire' zur Anzeige eines inspirierenden Zitats und 'deploy:notify' zur Versendung einer Deploy-Benachrichtigung an eine E-Mail-Adresse.

---

### routes/web.php
Definiert API-Endpunkte für verschiedene Routen, einschließlich Kontaktformular, Datenschutzbestimmungen, Profil-Einstellungen und Forschungsprojekte.

---

## config

### docker-compose.dev.yml
Konfiguriert den nginx-Port für Entwicklungsmodus auf 6480.

---

### docker-compose.yml
Konfiguration für ein Docker-Compose-Projekt mit verschiedenen Diensten wie PHP-FPM, Webserver, Datenbanken und Queue-Worker.

---

### .env.example
Konfigurationsdatei für eine Laravel-Anwendung, enthält Datenbank-, Session- und Mail-Einstellungen sowie API-Schlüssel.

---

### config/app.php
Konfigurationsdatei für Laravel-Anwendungen, enthält Einstellungen wie Name, Umgebung, Debug-Modus, URL, Zeitzon, Lokale und Verschlüsselung.

---

### config/auth.php
Konfiguration für Authentifizierung und Passwortzurücksetzung in Laravel-Anwendungen. Definiert Guard, Provider, Passwortresets und Konfirmationstimeout.

---

### config/cache.php
Konfiguration der Cache-Einstellungen für verschiedene Storage-Driver wie Database, Memcached, Redis usw.

---

### config/cookie-consent.php
Konfigurationsdatei für den Cookie-Zustimmungsdialog, einschließlich Einstellung der Aktivierung, des Cookie-Namens und der Dauer.

---

### config/database.php
Konfigurationsdatei für Datenbank-Verbindungen und Migrationen.

---

### config/filesystems.php
Konfiguration der Dateispeicheroptionen für verschiedene Anwendungsaspekte wie lokales Speichern, öffentliches Speichern und Amazon S3.

---

### config/fortify.php
Konfiguration für Laravel-Fortify mit Einstellungen für Authentifizierung, Passwortzurücksetzung, Benutzernamen und mehr.

---

### config/logging.php
Konfiguration für Monolog-Log-Channel in Laravel-Anwendungen, einschließlich standarder und benutzerdefinierter Kanäle.

---

### config/mail.php
Konfiguration für das Versenden von E-Mails mit verschiedenen Transports wie SMTP, Sendmail, Postmark etc.

---

### config/queue.php
Konfiguration der Queue-Verbindungen in Laravel, einschließlich der Standardverbindung und Optionen für verschiedene Backends wie Datenbank, Beanstalkd, SQS und Redis. Stellt auch Einstellungen für Job-Batching und fehlgeschlagene Queue-Jobs bereit.

---

### config/services.php
Konfigurationsdatei für dritte Party-Dienste wie Mailgun, Postmark, AWS und mehr. Enthält API-Schlüssel und -geheimnisse aus der Umgebung.

---

### config/session.php
Konfiguration der Laravel-Session mit Einstellungen wie Driver, Lifetime, Encryption und mehr.

---

### docker/common/php-fpm/php-dev.ini
Konfigurationsdatei für PHP-FPM in Entwicklungsmodus, aktiviert durch Bind-Mount. Aktiviert OPcache-Timestamp-Überprüfung und Fehleranzeige.

---

### docs/api.yaml
API-Dokumentation für app.linn.games mit Endpunkten zur Benutzer-Authentifizierung, Webhook-Verarbeitung, Papier-Erteilung und Vektorsuche.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Ermöglicht das Erstellen von Bug-Reports mit Vorabdefinierten Feldern in GitHub Issues.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Ermöglicht das Einreichen von Funktionsvorschlägen mit zusammenfassenden Beschreibungen, Motivationen, Vorgeschlagenen Lösungen und Akzeptanzkriterien.

---

### .github/ISSUE_TEMPLATE/refactor.yml
Konfiguration für Issue-Tempate zum Code-Refactoring. Enthält Felder für Beschreibung, betroffene Dateien und Akzeptanzkriterien.

---

### .github/workflows/lint.yml
Die Datei ist ein GitHub Actions Workflow zur Codequalitätüberprüfung durch Pint in PHP-Projekten bei Pushes und Pull Requests.

---

### .github/workflows/tests.yml
Automatisiert die Ausführung von Tests für das Projekt. Unterstützt Push-Operationen und Pull Requests auf spezifischen Branches. Setzt up PHP, Node.js, installiert Abhängigkeiten, generiert Anwendungsschlüssel und führt Tests durch.

---

## data_access

### app/Models/ChatMessage.php
Definiert das ChatMessage-Modell für die Datenbank-Tabelle 'chat_messages', mit Beziehungen zu User und Webhook.

---

### app/Models/Consent.php
Modell für Einwilligungen. Enthält Felder wie Benutzer-ID, anonymisierte IP, eingewilligte Kategorien, Zustimmungs- und Widerruf-Zeiten. Bezieht sich auf den Benutzer.

---

### app/Models/Contact.php
Definiert eine Eloquent-Model-Klasse für die Kontaktdaten in der Datenbank.

---

### app/Models/PageView.php
Definiert ein Eloquent-Model für die speicherung von PageViews in der Datenbank.

---

### app/Models/User.php
Diese Datei definiert eine Eloquent-Modell-Klasse für den Benutzer in einer Anwendung. Es bietet Authentifizierungsfunktionen, Rollendienste, Aktivitätsprotokollfunktionen und zusätzliche benutzerdefinierte Methoden wie 'initials', 'isActive' und 'isTrial'. Die Klasse nutzt externe Abhängigkeiten wie Laravel Fortify, Spatie Permission und Spatie Activitylog.

---

### app/Models/Webhook.php
Definiert das Webhook-Modell mit Eigenschaften, Methoden und Beziehungen zur User und ChatMessage-Modelle.

---

### app/Models/Recherche/P1Komponente.php
Definiert das Eloquent-Model für P1Komponenten in der Datenbank. Stellt Eigenschaften und Beziehungen zur Projekt-Tabelle bereit.

---

### app/Models/Recherche/P1Kriterium.php
Definiert ein Eloquent-Model für die Tabelle 'p1_kriterien', das UUIDs verwendet und einen Bezug zu dem Projekt hat.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Definiert ein Eloquent-Modell für die Tabelle 'p1_strukturmodell_wahl', enthält Felder für Projekt-ID, Modell-Auswahl, Auswahlstatus und Begründung. Beinhaltet einen Many-To-One-Bezug zu einer Projektabstammung.

---

### app/Models/Recherche/P1Warnsignal.php
Definiert das Eloquent-Model P1Warnsignal mit Eigenschaften wie Projekt-ID, LFD-Nr. und Warnsignal-Angaben. Bezieht sich auf das Projekt-Modell.

---

### app/Models/Recherche/P2Cluster.php
Definiert die Datenmodell-Klasse P2Cluster für den Datenzugriff auf die Tabelle 'p2_cluster' in der Datenbank. Die Klasse enthält Attribute wie projekt_id, cluster_id, cluster_label, beschreibung, treffer_schaetzung und relevanz. Sie verfügt über eine Beziehung zur Projektklasse.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Definiert eine Eloquent-Model-Klasse für die Tabelle p2_mapping_suchstring_komponenten mit Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Definiert ein Eloquent-Model für die Tabelle 'p2_review_typ_entscheidung', das eine Beziehung zu einem Projekt hat. Unterstützt UUIDs und enthält Felder für den Review-Typ, die Entscheidung und die Begründung.

---

### app/Models/Recherche/P2Trefferliste.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p2_trefferlisten'. Speichert Informationen zu Rechercheergebnissen und verbindet sich mit der 'Projekt'-Klasse.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Definiert das Eloquent-Model für die Tabelle 'p3_datenbankmatrix', enthält Felder für Projekt-ID, Datenbank, Disziplin, Abdeckung, Besonderheit, Zugang und Empfehlung. Bezieht sich auf das Projekt-MODEL.

---

### app/Models/Recherche/P3Disziplin.php
Definiert ein Eloquent-Modell für die Tabelle p3_disziplinen mit Attributen wie projekt_id, disziplin, art, relevanz und anmerkung. Enthält eine Beziehung zu einer Projekt-Instanz.

---

### app/Models/Recherche/P3GeografischerFilter.php
Definiert eine Eloquent-Model-Klasse für den Datenzugriff auf die Tabelle 'p3_geografische_filter'. Stellt CRUD-Operationen für Geografische Filter bereit, die mit Projekten verknüpft sind.

---

### app/Models/Recherche/P3GraueLiteratur.php
Definiert eine Eloquent-Model-Klasse für die Tabelle p3_graue_literatur, mit einer Beziehung zu Projekt.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Definiert das Eloquent-Model für den P4-Anpassungsprotokoll-Tabelle. Stellt CRUD-Operationen zur Verfügung und verbindet sich mit der P4Suchstring-Tabelle.

---

### app/Models/Recherche/P4Suchstring.php
Definiert das Eloquent-Modell für den Suchstring in der Datenbank mit Beziehungen zu Projekten und Protokollen.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Diese Datei definiert eine Eloquent-Model-Klasse für die Tabelle p4_thesaurus_mapping. Sie verfügt über eine UUID und beinhaltet Felder für Projektverknüpfung sowie verschiedene freitextliche und terminologische Angaben.

---

### app/Models/Recherche/P5PrismaZahlen.php
Definiert ein Eloquent-Model für die Tabelle p5_prisma_zahlen. Stellt Zugriff auf Recherche-Daten mit Beziehung zu Projekten.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Definiert die Datenmodellklasse P5ScreeningEntscheidung für die Tabelle p5_screening_entscheidungen. Stellt einen zugehörigen Treffer-Beziehungsmodell zur Verfügung.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Definiert das Model für P5ScreeningKriterium mit Beziehungen zu Projekten.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Definiert die Datenmodellklasse P5ToolEntscheidung für die Tabelle p5_tool_entscheidung mit einer Beziehung zu dem Projekt. Unterstützt UUIDs und castet 'gewaehlt' als boolean.

---

### app/Models/Recherche/P5Treffer.php
Definiert eine Eloquent-Model-Klasse für P5Treffer in der Datenbank. Enthält Beziehungen zu anderen Modellen wie Projekt, Duplikate und Bewertungen.

---

### app/Models/Recherche/P6Luckenanalyse.php
Definiert das Eloquent-Model für P6-Lückenanalyse mit UUIDs, ohne Zeitspalten und Beziehung zu Projekten.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Definiert ein Eloquent-Model für P6Qualitaetsbewertungen mit UUIDs, Tabellenspalten und Beziehung zur P5Treffer-Tabelle.

---

### app/Models/Recherche/P7Datenextraktion.php
Definiert ein Eloquent-Model für die Datenextraktion in der Recherche-Tabelle mit Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Definiert ein Eloquent-Model für die Tabelle p7_grade_einschaetzung mit Beziehungen zu Projekten.

---

### app/Models/Recherche/P7MusterKonsistenz.php
Definiert das Eloquent-Modell P7MusterKonsistenz für die Tabelle 'p7_muster_konsistenz' mit einem UUID und Beziehungen zu anderen Tabellen. Unterstützt massenzuweisung und Array-Speicherung.

---

### app/Models/Recherche/P7SyntheseMethode.php
Klasse für die Modellierung der P7SyntheseMethode-Tabelle in der Datenbank. Enthält Beziehungen zu Projekten und definiert die fillable-Eigenschaften sowie einen Casting für das 'gewaehlt'-Feld.

---

### app/Models/Recherche/P8Limitation.php
Diese Datei enthält die Model-Klasse P8Limitation für die Datenbanktabelle 'p8_limitationen'. Sie verfügt über eine Beziehung zu der Projekt-Klasse und definiert die Felder, die mit dem Modell übersetzt werden können.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Definiert eine Eloquent-Model-Klasse für die Tabelle p8_reproduzierbarkeitspruefung mit Attributen wie projekt_id, pruefpunkt, erfuellt und anmerkung. Implementiert ein Beziehungs-Methode zur Projekt-Tabelle.

---

### app/Models/Recherche/P8Suchprotokoll.php
Die Datei P8Suchprotokoll.php enthält eine Eloquent-Modellklasse für die Datenbanktabelle p8_suchprotokoll. Sie definiert die Struktur der Tabellenspalten, ihre Datentypen sowie Verbindungen zu anderen Modellen.

---

### app/Models/Recherche/P8UpdatePlan.php
Definiert das Recherche-Modell P8UpdatePlan mit UUIDs, Tabellenbezug zu Projekt und datenbankischen Feldern.

---

### app/Models/Recherche/PaperEmbedding.php
Eloquent Model für PaperEmbedding mit UUIDs und Beziehung zu Projekt.

---

### app/Models/Recherche/Phase.php
Definiert eine Eloquent-Model-Klasse für Phasen in einem Projekt mit UUIDs, Tabellennamen und Timestamp-Ausлючung. Beinhaltet eine Beziehung zu Projekten.

---

### app/Models/Recherche/Projekt.php
Definiert ein Eloquent-Modell für Projekte mit Methoden zur Beziehung zu verschiedenen Phasen und Unterphasen sowie zu Nutzerdaten. Implementiert UUIDs, Audit-Logging und zentrale Casts für Datenbankspalten.

---

### database/factories/UserFactory.php
```json
{
  "file_summary": "Dieser Code generiert fake User-Daten für Testing mit Laravel. Hauptfunktionen: definition(), unverified(), withoutTwoFactor(). Abhängigkeiten: Illuminate\Database\Eloquent\Factories\Factory, Hash, Str."
}
```

---

### database/factories/Recherche/ProjektFactory.php
Diese Datei generiert Fiktive Projekte für die Datenbank.

---

### database/migrations/0001_01_01_000000_create_users_table.php
Die Datei erstellt und löscht die Tabellen 'users', 'password_reset_tokens' und 'sessions' in der Datenbank.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Dieser Datei erstellt und löscht zwei Tabellen: 'cache' und 'cache_locks'. Die 'cache'-Tabelle speichert Cache-Daten, während die 'cache_locks'-Tabelle Sperren für Cache-Operationen verwendet.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Dieses Skript erstellt Migrationen für die Tabelle 'jobs', 'job_batches' und 'failed_jobs'. Es definiert die Struktur der Tabellen mit diversen Feldern wie ID, Queue, Payload, Versuchen usw. Die Funktion `up()` führt die Migration durch, während `down()` sie rückgängig macht.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Die Datei enthält Migrations-Definitionen, um zwei-Faktor-Sicherheitsfunktionen zu users-Tabelle hinzuzufügen und wieder zu entfernen.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Migrates the 'contacts' table with fields for name, company, email, project type, message, timeline and timestamps.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Dieser PHP-Code definiert eine Datenbank-Migration für die Tabelle 'page_views'. Die Migration erstellt die Tabelle mit ID, Besuchszähler und Zeitstempeln. Der 'up'-Methode steht der Erstellung der Tabelle zur Verfügung, während der 'down'-Methode die Tabelle wieder löscht.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Dieses PHP-Skript erstellt die notwendigen Datenbanktabellen für eine Rechtliche Management-Systeme, wie Rollen und Berechtigungen. Die Tabellen werden basierend auf der Konfiguration in `config/permission.php` definiert. Die Migration ist durchführen oder rückgängig machen (down) möglich.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Diese Datei enthält Migrationsklasse zur Änderung der `page_views`-Tabelle, indem sie neue Spalten hinzufügt und alte entfernt.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Migrates a 'consents' table to store user consents, including user ID, IP address, consent categories, and timestamps.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Dieses PHP-Skript ist eine SQLite-mitfremd Kompatibilität für die Recherche-Kerntabellen des Projekts. Es führt Migrationen durch, um Tabellen wie users, projekte, phasen und mehr zu erstellen oder zu aktualisieren.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei enthält Migrationsklasse für PostgreSQL-DB, um benutzerdefinierte ENUM-Typen und externe Erweiterungen zu erstellen und zu entfernen.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Erstellt und ändert Tabellen für Projekte und Phasen in einer PostgreSQL-Datenbank. Überprüft das DB-Treiber, bevor die Migration durchgeführt wird.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
This file defines database migration classes for creating and dropping tables in a PostgreSQL database. It includes models for project-related data such as structure model choices, components, criteria, and warning signals.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Dieses PHP-Skript definiert Migrationen für PostgreSQL, um Tabellen für P2-Daten zu erstellen. Es beinhaltet Klassen für Migrationen, Funktionen 'up' und 'down', sowie externe Abhängigkeiten wie Illuminate-DB-Migrationen.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Erstellt und löscht PostgreSQL-Tabellen für verschiedene Datenpunkte in einem Projekt.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
Die Datei enthält Migrationen für PostgreSQL, um Tabellen 'p4_suchstrings', 'p4_thesaurus_mapping' und 'p4_anpassungsprotokoll' zu erstellen. Sie enthält auch die Funktionen `up` und `down`, um die Tabellen hinzuzufügen oder zu löschen.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Dieser Datei handelt es sich um eine Laravel-Migration, die PostgreSQL-Schemata für die Tabellen p5_prisma_zahlen, p5_screening_kriterien, p5_treffer, p5_screening_entscheidungen und p5_tool_entscheidung erstellt. Die Migration überprüft, ob der verwendete Datenbanktreiber PostgreSQL ist und führt相应的 CREATE TABLE-Anweisungen aus. Jede Tabelle enthält spezifische Felder, um Daten zu speichern. Das down()-Methode löscht diese Tabellen bei Rückwirkung.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Dieses PHP-Skript erstellt und verwaltet Datenbanktabellen für Qualitätsbewertungen und Lückenanalyse in einer PostgreSQL-Datenbank.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Migrates PostgreSQL tables for specific project data, including synthesis methods, data extraction, pattern consistency, and grade estimation.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Diese Datei enthält Laravel-Migrationen für PostgreSQL, um Tabellen 'p8_suchprotokoll', 'p8_limitationen', 'p8_reproduzierbarkeitspruefung' und 'p8_update_plan' zu erstellen. Sie überprüft, ob der DB-Treiber PostgreSQL ist, bevor die Tabellen erstellt werden. Die Migrationen enthalten auch Foreign-Key-Constraint und eine Prüfbedingung.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Erstellt oder löscht PostgreSQL-Indizes für verschiedene Tabellen in einer Datenbankmigration.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Diese Datei enthält eine Migration für PostgreSQL, die eine Funktion berechnet, um PRISMA-2020-Kennzahlen für ein Recherche-Projekt direkt auf der Datenbank-Ebene zu berechnen. Die Funktion zählt die Werte für das PRISMA-Flussdiagramm basierend auf den Tabellen p5_treffer und p5_screening_entscheidungen.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Die Datei enthält eine Migration, um die Tabelle 'chat_messages' für PostgreSQL zu erstellen. Sie definiert eine UUID-Spalte als Primärschlüssel, eine Fremdschlüssel-Referenz auf die 'users'-Tabelle, einen Rollenfeld mit den Werten 'user' oder 'assistant', einen Textinhalt und einen Zeitaufzeichner.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Eine Laravel-Migration, die eine Tabelle 'webhooks' erstellt und in die Tabelle 'chat_messages' ein Foreign Key hinzufügt.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Dieses PHP-Skript erstellt eine Migration zur Erstellung der Tabelle 'paper_embeddings' im PostgreSQL-Datenbank. Es fügt die notwendigen Felder hinzu und erstellt eine Vektor-Indexierung für effiziente Textsuche.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Dieses Skript erstellt die Tabelle 'activity_log' für die Aufzeichnung von Aktivitäten in der Datenbank. Es definiert Felder für Logname, Beschreibung, Benutzer und Ereignisse.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
Migrates the users table by adding a status column with PostgreSQL-specific ENUM handling and SQLite fallback.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Migrates the database to add a new column 'frontend_object' of type ENUM with values 'dashboard_chat' and 'recherche_start' in the 'webhooks' table. Handles PostgreSQL specific DDL and ensures idempotency.

---

### database/seeders/DatabaseSeeder.php
Dieser Seeder füllt die Datenbank mit Initialdaten, einschließlich der Erstellung eines Admin-Benutzers und dem Aufrufen weiterer Seeders.

---

### database/seeders/RechercheDemoSeeder.php
Das Skript füllt eine Datenbank mit Demo-Daten für einen Forschungsprojekt in der KI-gestützten Bildanalyse in der Radiologie. Es erstellt Projekte, Phasen, Strukturmodelle, Komponenten, Kriterien, Trefferlisten und weitere Modelle.

---

### database/seeders/RoleSeeder.php
Dieser Seeder erstellt zwei Rollen: 'admin' und 'user' im web-Guard.

---

### database/sql/create_langdock_agent_user.sql
Erstellt einen DB-User 'langdock_agent' mit restricted read/write access to Recherche tables, incl. PRISMA helper function.

---

## temp_dummy

### .github/pull_request_template.md
Markdown-Template für GitHub-Pull Requests

---

## tests

### tests/Pest.php
Pest.php ist eine Testdatei für PHPUnit, die Funktionen zur Erweiterung des TestCase-Classes und der Expectations-API bereitstellt. Sie enthält auch eine benutzerdefinierte Funktion 'something()'.

---

### tests/TestCase.php
Abstrakte Test-Klasse für Laravel-Anwendungen, erbt von BaseTestCase

---

### tests/Feature/ContactTest.php
Tests für die Kontaktfunktion. Überprüft die Validität der Formulareingaben, speichert gültige Daten in der DB und überprüft API-Antworten.

---

### tests/Feature/DashboardTest.php
Testet, ob Gäste zur Login-Seite umgeleitet werden und authentifizierte Benutzer auf den Dashboard-Zugriff haben.

---

### tests/Feature/ExampleTest.php
Ein Testfall für das Home-Routen-Endpunkt, der überprüft, ob eine erfolgreiche HTTP-Statuscode 200 zurückgegeben wird.

---

### tests/Feature/IngestPaperJobTest.php
Testklasse für den IngestPaperJob, um sicherzustellen, dass er korrekt ausgeführt wird.

---

### tests/Feature/LegalPagesTest.php
Die Datei enthält Tests für die Rendition von Legal-Seiten wie Impressum, Datenschutz und AGB.

---

### tests/Feature/PaperRagTest.php
Tests für den Ingest- und RAG-Such-API-Endpunkt, einschließlich Authentifizierung, Validierung von Eingaben und Integration mit Ollama.

---

### tests/Feature/ProjektPolicyTest.php
Tests für die Zugriffssteuerung von Projekten über eine Policy, welche nur den Benutzer, der den Projekt erstellt hat, Zugriff auf die Aktionen 'view', 'update' und 'delete' erlaubt. Jeder authentifizierte Benutzer kann ein neues Projekt erstellen.

---

### tests/Feature/RechercheTest.php
Dieses Testskript enthält Tests für die Recherche- und Projektverwaltung in einer Anwendung. Es verwendet Laravel's Testing-Framework, die Queue, User-Fixtures und Livewire-Komponenten.

---

### tests/Feature/WebhookTest.php
Tests für den Webhook-Endpunkt, um die Validität des Anfragemeldes zu überprüfen und sicherzustellen, dass er alle erforderlichen Felder enthält, eine gültige Signatur hat und nicht abgelaufen ist.

---

### tests/Feature/WelcomeTest.php
Die Datei enthält Test-Cases für den Willkommens-Page. Es überprüft die Rückgabe eines erfolgreichen HTTP-Status, das Erhöhen des Besuchszählers und das Anlegen eines Page-Views bei der ersten Anfrage. Zudem wird geprüft, ob der Benutzer Login-, Registrierungs- oder Dashboard-Links sieht, je nach Authentifizierungszustand.

---

### tests/Feature/Auth/AuthenticationTest.php
Die Datei enthält Testfunktionen für die Authentifizierungsfunktionalität in einer Laravel-Anwendung. Es testet die Login-Schirm Darstellung, das Anmelden von Benutzern mit korrekter und falscher Passworteingabe, den Umweg zur Zwei-Faktor-Authentifizierung sowie den Abmeldeprozess.

---

### tests/Feature/Auth/EmailVerificationTest.php
Testet die Funktionen zur Benutzer-E-Mail-Bestätigung, einschließlich der Anzeige des Bestätigungssehens, der erfolgreichen Verifizierung, des Fehlers bei einer ungültigen Hash und der Umleitung für bereits verifizierte Benutzer.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Tests the rendering of the password confirmation screen for authenticated users.

---

### tests/Feature/Auth/PasswordResetTest.php
Die Datei enthält Test-Funktionen für das Passwort-Zurücksetzen in der Anwendung. Die wichtigsten Klassen und Funktionen sind 'User' und 'ResetPassword'. Externe Abhängigkeiten umfassen Laravel's Notification API, User-Factory und Route-API.

---

### tests/Feature/Auth/RegistrationTest.php
Diese Datei enthält Tests für die Registrierungsfunktion. Sie überprüft, ob der Registrierungsformular gerendert werden kann und, ob neue Benutzer erfolgreich registriert werden können.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Tests für das Two-Factor-Authentifizierungschallenge. Überprüft, ob die Anfrage nicht authentifiziert wird und das Challenge-Formular gerendert werden kann.

---

### tests/Feature/Chat/BigResearchChatTest.php
Die Datei enthält Tests für die BigResearchChat-Funktion in einer Chat-Anwendung. Sie testet das Senden von Nachrichten, die Validierung des Benutzereingangs, das Verhalten bei Fehlern und das Löschen der persönlichen Nachrichten-Historie.

---

### tests/Feature/Recherche/PhaseP1Test.php
Dieses PHP-Testskript enthält mehrere PHPUnit-Tests für die Funktionen des Projekts 'PhaseP1'. Es testet das Erstellen, Bearbeiten, Löschen und Validieren von Strukturmodellen, Komponenten und Kriterien sowie Berechtigungen für fremde Benutzer. Die Tests interagieren mit der Datenbank und nutzen Laravel's Factory und Assertions.

---

### tests/Feature/Recherche/PhaseP2Test.php
Die Datei enthält Testfunktionen für die Klassen P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente und P2Trefferliste. Jede Klasse hat Tests für das Erstellen, Bearbeiten, Validieren und Löschen von Instanzen. Die Tests nutzen Livewire-Volt zur Interaktion mit dem Frontend und verifizieren die Datenbankinteraktionen.

---

### tests/Feature/Recherche/PhaseP3Test.php
Dieser Datei sind mehrere PHPUnit-Tests enthalten, die Funktionen für die Erstellung, Bearbeitung und Löschung von Datenbankmatrizen, Disziplinen, geografischen Filtern und grauer Literatur in einem Projekt testen. Die Tests verwenden Laravel's TestCase-Klasse und Arten von Livewire-Volt-Tests. Sie verifizieren auch die Validierung der Eingaben.

---

### tests/Feature/Recherche/PhaseP4Test.php
Dieses PHP-Testskript verwendet Laravel und Livewire um Tests für die Klassen Projekt, P4Suchstring, P4ThesaurusMapping und P4Anpassungsprotokoll in einer Forschungsauftragsphase durchzuführen. Es erstellt, bearbeitet, löscht und validiert Einträge in der Datenbank.

---

### tests/Feature/Recherche/PhaseP5Test.php
Die Datei enthält Tests für verschiedene Funktionen der Phase P5 in einem Forschungsmanagement-System. Es testet die Erstellung, Bearbeitung und Löschung von PRISMA-Zahlen, Screening-Kriterien, Tool-Entscheidungen und Screening-Entscheidungen. Die Tests verwenden Laravel's Testing Framework und Livewire für die Interaktion mit der Benutzeroberfläche.

---

### tests/Feature/Recherche/PhaseP6Test.php
Tests für Qualitätsbewertung und Lückenanalyse in Phase P6. Funktionen: Erstellen, Bearbeiten, Löschen und Validierung.

---

### tests/Feature/Recherche/PhaseP7Test.php
Die Datei enthält Tests für verschiedene Aspekte der Phase P7 in einer Forschungsprojektverwaltung. Sie testet die Funktionalität von Synthese-Methode, Datenextraktion, Muster und Konsistenz sowie GRADE-Einschätzung. Jeder Test erstellt, bearbeitet oder löscht ein Objekt und überprüft das Ergebnis in der Datenbank.

---

### tests/Feature/Recherche/PhaseP8Test.php
Dieser Test-Datei enthält Tests für die Recherche-Phase P8 in einem Projekt. Sie erstellt, bearbeitet und löscht Suchprotokolle, Limitationen, Reproduzierbarkeitsprüfungen und Update-Pläne. Die Tests validieren die Funktionalität und stellen sicher, dass nur der Projektleiter Zugriff hat.

---

### tests/Feature/Recherche/ProjektDetailTest.php
Tests für die ProjektDetail-Komponente, einschließlich der Rendern von Tabs und der Berechtigungsprüfung.

---

### tests/Feature/Settings/AppearanceTest.php
Dieses Testskript für Livewire-Components überprüft die Darstellung und Funktionalität der Erscheinungseinstellungen. Es verwendet Mock-Daten, authentifizierte Benutzer und prüft HTML-Ausgaben.

---

### tests/Feature/Settings/PasswordUpdateTest.php
Dieses PHP-Testskript verwendet Laravel und Livewire um die Funktionen der Benutzerpasswortverwaltung zu testen. Es überprüft, ob ein Passwort erfolgreich aktualisiert werden kann und ob eine korrekte aktuelle Passphrase erforderlich ist.

---

### tests/Feature/Settings/ProfileUpdateTest.php
Dieses PHP-Testskript verwendet Livewire-Volt und PHPUnit um die Funktionen der Benutzer-Profilverwaltung zu testen, einschließlich Anzeige der Profilseite, Aktualisierung von Profilverhalten, Änderung des E-Mail-Status und das Löschen des Kontos mit Passwortbestätigung.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
Tests für die Two-Factor-Authentifizierungsfunktionen. Überprüft die Darstellung und Funktionalität der Einstellungsseite sowie das Verhalten bei verschiedenen Konfigurationszuständen.

---

### tests/Feature/Settings/WebhooksTest.php
Tests für das Speichern und Entfernen von Webhooks in der Benutzereinstellungen. Nutzt Livewire-Volt für Tests und bietet Funktionen zur URL-Validierung.

---

### tests/Unit/ExampleTest.php
Dies ist eine unit-test-Datei, die prüft, ob der Wert 'true' tatsächlich 'true' ist.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Klassendefinition für die Seite zur Liste von Kontakten im Filament-Backend.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert die Ansicht für den Kontakt in der Benutzeroberfläche (UI) mit Filament. Die wichtigste Klasse ist ViewContact, die von ViewRecord erbt und die Ressource ContactResource verwendet.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Diese Datei definiert eine benutzerdefinierte EditPage-Klasse für den Benutzerverwaltungsbereich. Sie erbt von Filament's EditRecord und verbindet sich mit der UserResource, um die Bearbeitung von Benutzern zu ermöglichen.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
zeigt eine Liste von Benutzern an

---

### resources/views/dashboard.blade.php
Die Datei rendert das Dashboard für Benutzer mit Quicklinks, dem letzen Projekten und einem Chat-Element.

---

### resources/views/welcome.blade.php
Diese Datei enthält das HTML-Vorlage für die Willkommen-Seite von Linn Games. Es definiert das Layout mit Header, Hero-Bereich und Hauptnavigation. Die Seite nutzt CSS-Animationen und externe Ressourcen wie Google Fonts und ein Vite-basiertes CSS-Framework.

---

### resources/views/components/action-message.blade.php
Blade-Template zur Anzeige einer Nachricht, die nach einer bestimmten Aktion erscheint und nach 2 Sekunden automatisch versteckt wird.

---

### resources/views/components/app-logo-icon.blade.php
Ein SVG-Icon für die Anwendung, der als Blade-Vorlage in Laravel verwendet wird.

---

### resources/views/components/app-logo.blade.php
Renders a component with an app logo and name.

---

### resources/views/components/auth-header.blade.php
Diese Datei enthält ein Blade-Vorlagenteil für einen Authentifizierungsheader, der den Titel und eine Beschreibung anzeigt.

---

### resources/views/components/auth-session-status.blade.php
zeigt den Anmeldestatus an, wenn verfügbar

---

### resources/views/components/placeholder-pattern.blade.php
Generiert ein SVG-Pattern mit einer Diagonalen Linie als Platzhalter.

---

### resources/views/components/layouts/app.blade.php
Blade template für die App-Basislayout, enthält einen Seitenleisten mit einem Titel und ein Hauptinhaltsbereich.

---

### resources/views/components/layouts/auth.blade.php
Renders a simple authentication layout with optional title.

---

### resources/views/components/layouts/app/header.blade.php
```json
{
  "response": "Der Text ist zu lang und kann nicht vollständig dargestellt werden. Bitte geben Sie eine spezifischere Frage oder beschreiben Sie den Bereich des Textes, den Sie benötigen."
}
```

---

### resources/views/components/layouts/app/sidebar.blade.php
```json
{
  "sidebar": {
    "elements": [
      {
        "type": "button",
        "label": "Dashboard",
        "action": "navigate",
        "url": "/dashboard"
      },
      {
        "type": "navigation",
        "label": "Menu",
        "items": [
          { "label": "Home", "url": "/" },
          { "label": "About", "url": "/about" }
        ]
      },
      {
        "type": "button",
        "label": "Logout",
        "action": "logout"
      }
    ],
    "userMenu": {
      "visible": false,
      "items": [
        { "label": "Profile", "url": "/profile" },
        { "label": "Settings", "url": "/settings" },
        { "label": "Logout", "action": "logout" }
      ]
    }
  }
}
```

---

### resources/views/components/layouts/auth/card.blade.php
Renders a card layout for authentication pages with Laravel Blade templating.

---

### resources/views/components/layouts/auth/simple.blade.php
Ein einfaches Authentifizierungs-Layout für Anwendungen, das das Anmeldebildschirm-Erlebnis gestaltet. Enthält ein Logo und Platzhalter für Inhalte.

---

### resources/views/components/layouts/auth/split.blade.php
Diese Datei enthält das Layout für die Authentifizierungsseite, mit einem linken Sidebar für den Logo und Zitat und einem rechten Bereich für den Hauptinhalt. Sie verwendet externe Komponenten wie x-app-logo-icon und hat Abhängigkeiten zu Laravel, Inspriring und einer Cookie-Bestimmung.

---

### resources/views/components/settings/layout.blade.php
Die Datei layout.blade.php definiert den Layout für Einstellungenseiten in einer UI. Es enthält eine Sidebar mit Navigationslinks zu verschiedenen Einstellungen wie Profil, Passwort, Zwei-Faktor-Authentifizierung und Weitere Optionen. Das Hauptinhaltsfeld kann variabel Inhalte anzeigt.

---

### resources/views/emails/contact-confirmation.blade.php
Diese Datei enthält eine Blade-Templatdatei für die Bestätigungsmail bei Kontaktabfragen an Linn Games. Sie generiert eine HTML-Mail, die den Benutzer über die erfolgreiche Übermittlung seiner Anfrage informiert und die Details der Anfrage mitteilt.

---

### resources/views/emails/contact-inquiry.blade.php
Dies ist ein Blade-Tempalte für eine Kontaktanfrage-E-Mail in einer Webanwendung. Es zeigt Details wie Name, Firma, E-Mail, Projekttyp, Zeitrahmen und Nachricht an.

---

### resources/views/emails/deploy-notification.blade.php
HTML-Vorlage für eine Benachrichtigung bei erfolgreichem Deployment, enthält Informationen über das neueste Release.

---

### resources/views/layouts/legal.blade.php
HTML-Layout für rechtliche Informationen mit Header, Content-Bereich und Footer. Inklusive CSS-Styling, Backlink zu Startseite und automatisches Jahr im Footer.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für eine Online-Spielswebsite. Die AGB decken die Nutzung des Dienstes, Nutzungsregeln, Kosten, digitale Käufe, Haftung und Kündigungsrecht ab.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei enthält eine Datenschutzerklärung für ein Website-Spiel, die Informationen über verarbeitete Daten, Zwecke der Verarbeitung, Rechte der Betroffenen und Maßnahmen zur Datensicherheit enthält. Es bezieht sich auf Registrierungsdaten, Authentifizierungsdaten, Spieldaten, Nutzungsdaten und Kontaktanfragen. Es erläutert auch die Aufbewahrung von Daten, externe Dienste wie Langdock sowie die Berechtigungen der Nutzer.

---

### resources/views/legal/impressum.blade.php
Renders the imprint page with company details and legal links.

---

### resources/views/livewire/auth/confirm-password.blade.php
Diese Datei enthält eine Blade-Vorlage für das Bestätigungspasswort in der Authentifizierungssection einer Anwendung. Sie verwendet Komponenten wie x-auth-header und x-auth-session-status, um den Benutzer zur Bestätigung seiner Passwortsitzung aufzufordern.

---

### resources/views/livewire/auth/forgot-password.blade.php
Renders a password reset form for users, handling email submission and displaying session status.

---

### resources/views/livewire/auth/login.blade.php
Die Datei implementiert das Login-Funktionalität für Benutzer. Sie enthält Formularelemente für E-Mail, Passwort und Remember-Me-Optionen. Das Formular sendet Daten an den Endpunkt 'login.store' für die Verarbeitung. Die Datei nutzt Blade-Tags, um dynamische Inhalte wie Fehlermeldungen oder Authentifizierungsstatus anzuzeigen.

---

### resources/views/livewire/auth/register.blade.php
Diese Datei enthält das Blade-Vorlagencode für die Benutzerregistrierungsseite. Es stellt ein Formular zur Eingabe des Namens, der E-Mail-Adresse und der Passwörter bereit. Das Formular sendet die Daten an den Endpunkt '/register/store' für die Verarbeitung. Die Seite verwendet externe Komponenten wie x-auth-header, x-auth-session-status und x-layouts.auth.

---

### resources/views/livewire/auth/reset-password.blade.php
Die Datei ist eine Blade-Vorlage für das Passwort-zurücksetzen-Formular in einer Authentifizierungsschnittstelle. Sie verwendet Komponenten wie x-auth-header, x-auth-session-status und ein Formular zum Ändern des Passworts. Die Funktionen umfassen das Anzeigen der Benutzeroberfläche, die Präsentation von Fehlermeldungen und den Abschluss des Passwortzurücksetzung-Prozesses.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Diese Datei enthält eine Vue.js Komponente für den Two-Factor-Authentifizierungsprozess, die bei der Anmeldung mit einem Authentifikator oder einer Wiederherstellungscode eingeben erlaubt. Sie bietet eine Benutzeroberfläche mit Eingabefeldern für die Authentifikations- und Wiederherstellungscode sowie ein Formular zum Übermitteln der Daten.

---

### resources/views/livewire/auth/verify-email.blade.php
Eine Blade-Vorlage für die E-Mail-Bestätigung in einer Authentifizierungsanwendung. Zeigt eine Nachricht mit den Anweisungen zur Bestätigung der E-Mail und Bereitstellung von Optionen, um das Verifizierungslink erneut zu senden oder sich abzumelden.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Diese Datei enthält eine Livewire-Komponente für einen big research chat, die Nachrichten zwischen Benutzer und Assistenten speichert, über HTTP-Anfragen an einen Webhook sendet und den Chat-Verlauf anzeigt.

---

### resources/views/livewire/recherche/phase-p1.blade.php
Livewire-Komponente für die Verwaltung von Strukturmodellwahl, Komponenten, Kriterien und Warnsignale eines Projekts. CRUD-Operationen für jedes Element.

---

### resources/views/livewire/recherche/phase-p2.blade.php
Livewire-Komponente zur Verwaltung von Entwicklungsphasen P2 in Projekten. Unterstützt die CRUD-Operationen für ReviewTypEntscheidung, Cluster, MappingSuchstringKomponente und Trefferliste.

---

### resources/views/livewire/recherche/phase-p3.blade.php
Livewire-Komponente für die Verwaltung von Datenbanken, Disziplinen, Geografischen Filtern und Grauer Literatur im Zusammenhang mit einem Projekt.

---

### resources/views/livewire/recherche/phase-p4.blade.php
Eine Livewire-Komponente für die Verwaltung von Suchstrings, Thesaurus-Mapping und Anpassungsprotokollen eines Projekts. Stellt CRUD-Operationen für diese Entitäten zur Verfügung.

---

### resources/views/livewire/recherche/phase-p5.blade.php
Dies ist eine Livewire-Komponente für den Verwaltung von Prisma-Zahlen, Screening-Kriterien, Tool-Entscheidungen und Treffer-Screening in einem Projekt. Sie bietet CRUD-Funktionalität und validiert eingebene Daten.

---

### resources/views/livewire/recherche/phase-p6.blade.php
Livewire-Komponente zur Verwaltung von Qualitätsbewertungen und Lückenanalysen für ein Projekt. Stellt Formulare für die Erstellung, Bearbeitung und Löschung dieser Daten bereit. Interagiert mit der DB über Eloquent-Modelle P5Treffer, P6Qualitaetsbewertung und P6Luckenanalyse.

---

### resources/views/livewire/recherche/phase-p7.blade.php
Diese Datei ist eine Livewire-Komponente für die Projektphase P7 in einer Forschung. Sie verwaltp die Synthese-Methode, Datenextraktion, Muster & Konsistenz sowie GRADE-Informationen eines Projekts. Die Komponente bietet CRUD-Funktionalität für diese Datenstruktur.

---

### resources/views/livewire/recherche/phase-p8.blade.php
Livewire-Komponente für die Verwaltung von Projektdaten wie Suchprotokoll, Limitationen, Reproduzierbarkeit und Update-Plänen. Die Komponente bietet CRUD-Funktionen für jedes Element und verwendet externe Modelle und Datenbank-Zugriffe.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Diese Datei implementiert ein Livewire-Komponenten für die Detailansicht eines Projekts. Es zeigt den Projekt-Header, eine Tab-Bar mit Phasen und dynamische Inhalte basierend auf der aktiven Phase.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Diese Datei enthält eine Vue.js Komponente, die ein Projektlisten-UI bereitstellt. Sie listet Projekte eines angemeldeten Benutzers an und bietet Optionen zum Löschen von Projekten.

---

### resources/views/livewire/recherche/research-input.blade.php
Dieses Blade-Template enthält ein Formular zur Eingabe einer Forschungsfrage. Bei Absenden wird die Frage validiert, gespeichert und an den Langdock-Agenten übermittelt. Danach wird der Benutzer auf das Projekt-Detail-Bildschirm weitergeleitet.

---

### resources/views/livewire/settings/appearance.blade.php
Die Datei implementiert einen Benutzeroberflächen-Component für die Änderung der Erscheinungseinstellungen eines Accounts. Es verwendet Livewire und bietet Buttons zum Wechseln zwischen Lichtmodus, Dunkelmodus und Systemstandard. Der Zustand wird in localStorage gespeichert.

---

### resources/views/livewire/settings/delete-user-form.blade.php
Die Datei implementiert ein Livewire-Komponente zum Löschen eines Benutzerkontos. Sie enthält Klassen für das Öffnen und Schließen des Modals sowie die Validierung und Durchführung des Löschvorgangs.

---

### resources/views/livewire/settings/password.blade.php
Eine Blaue-Vier-PHP-Komponente für das Ändern des Passworts eines authentifizierten Benutzers. Zeigt ein Formular zum Eingeben der aktuellen und neuen Passworte sowie zur Bestätigung. Überprüft die Validität und aktualisiert das Passwort im Backend, wenn gültig.

---

### resources/views/livewire/settings/profile.blade.php
Dieses Datei rendert ein Benutzerprofil-Formular, das den Namen und die E-Mail des angemeldeten Benutzers aktualisieren lässt. Es enthält Validierung und erlaubt es, eine Verifizierungs-E-Mail zu erneut senden.

---

### resources/views/livewire/settings/two-factor.blade.php
Dieser Livewire-Komponente verwaltet die Einstellungen für die zweifaktorige Authentifizierung eines Benutzers. Sie ermöglicht es dem Benutzer, die Zwei-Faktoren-Authentifizierung zu aktivieren, zu überprüfen, zu deaktivieren und wiederzugeben. Die Komponente verwendet externe Aktionen wie EnableTwoFactorAuthentication, ConfirmTwoFactorAuthentication und DisableTwoFactorAuthentication.

---

### resources/views/livewire/settings/webhooks.blade.php
Livewire-Komponente zur Verwaltung von Webhooks für Dashboard-Chat und Recherche. Lädt benutzer-spezifische URLs, speichert und löscht Webhook-Einträge.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
Livewire Component for managing two-factor authentication recovery codes in a Laravel application. It includes functionality to load, regenerate, and display recovery codes.

---

### resources/views/partials/head.blade.php
Die Datei enthält HTML-Meta-Tags und Links für ein Frontend Projekt, einschließlich Favicons, Schriften und Dark-Mode-Einstellungen.

---

### resources/views/partials/settings-heading.blade.php
Diese Datei enthält das HTML für den Überschriftsbereich der Einstellungen, einschließlich des Titels 'Settings' und einer Beschreibung. Keine interne Logik oder externe Abhängigkeiten.

---

### resources/views/recherche/index.blade.php
Die Datei enthält die Blade-Vue-Templatemethode für die 'Recherche'-Seite. Sie importiert ein Layout, zeigt einen Titel und zwei Livewire-Komponenten an: eine Eingabe- und eine Projekt-Liste.

---

### resources/views/recherche/show.blade.php
Zeigt das Detail eines Projekts in einer Rechercheansicht. Nutzt Livewire für dynamisches Inhalt.

---

## uncategorized

### Readme.md
Linn.Games — Laravel-Anwendung mit KI-gestützter systematischer Literaturrecherche. Inklusive Authentifizierung, Admin-Panel, Kontaktformular, CI/CD und Datenbankfunktionen.

---

### artisan
Startet eine Laravel-Kommandozeilenanwendung und verarbeitet eingehende Befehle.

---

### basemodel.md
Definiert ein Datenbankmodell für eine Systematische Literaturrecherche-Software mit Tabellen für Projekte, Phasen, Strukturmodelle, Kriterien und Datenbankauswahl. Nutzt externe Erweiterungen wie 'uuid-ossp' und 'pg_trgm'.

---

### CLAUDE.md
Dokumentation für das CLAUDE.ai/code Projekt, inklusive Entwicklungsmöglichkeiten, Tests und Build/Deploy-Scripte. Beschreibt die Architektur, die wichtigsten Komponenten und Patterns sowie Sicherheitsmaßnahmen.

---

### composer.json
Die Datei ist eine Composer-Konfigurationsdatei für ein Laravel-Projekt mit KI-gestützter Literaturrecherche. Sie definiert Abhängigkeiten, Autoload-Pfade und Skripte für das Setup und Entwicklungsprozess.

---

### CONTRIBUTING.md
Dokumentation zur Beitragshandlung, Merge-Fluss und Arbeitsablauf für Issues sowie Entwicklungsanweisungen für die lokale Umgebung.

---

### deploy.sh
Das Skript führt einen Produktionsbuild und -deploy für app.linn.games durch, beinhaltet Vorbereitungen, Buildprozess, Infrastrukturstart, Datenbankmigration und Cache-Optimierung.

---

### package.json
Definiert Abhängigkeiten und Skripte für ein Vite-basiertes Projekt mit Tailwind CSS.

---

### phpunit.xml
Konfiguration für PHPUnit-Tests mit Angaben zur Umgebung und Test-Suites.

---

### vite.config.js
Konfiguration für Vite mit Laravel-Plugin und Tailwind CSS. Definiert Eingabe-Dateien, Plugins und Server-Einstellungen.

---

### .dockerignore
Exklusiv liste von Dateien und Verzeichnissen, die nicht in Docker-Images mitgesendet werden sollten.

---

### .editorconfig
EditorConfig-Datei zur Konfiguration von Codeformatierung.

---

### app/Actions/Fortify/CreateNewUser.php
Dieser Code definiert eine Klasse CreateNewUser, die das Erstellen eines neuen Benutzers in der Anwendung verwaltp. Sie implementiert das CreatesNewUsers-Interface und nutzt Laravel's Fortify für die Registrierung. Die Klasse verwendet Validatoren zur Überprüfung der Eingabe und speichert den neuen Benutzer in der Datenbank.

---

### app/Actions/Fortify/PasswordValidationRules.php
Definiert die Standardregeln für Passwortvalidierung in Laravel-Fortify.

---

### app/Actions/Fortify/ResetUserPassword.php
Klasse für das Zurücksetzen des Passworts eines Benutzers. Implementiert das Interface ResetsUserPasswords und verwendet Validator für die Eingabevalidierung.

---

### app/Filament/Resources/ContactResource.php
Definiert eine Filament-Ressource für Kontakte mit Formular und Tabelle, ohne Bearbeitungsfunktionen.

---

### app/Filament/Resources/UserResource.php
Definiert eine Filament-Ressource für den Benutzer-Modell mit Formular und Tabelle. Stellt CRUD-Operationen für Benutzer bereit.

---

### app/Http/Middleware/TrackPageView.php
Middleware to track page views, excluding bots. Records unique visits per path with anonymized IP and user agent.

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware to verify Langdock webhook signature for replay protection and authentication.

---

### app/Http/Middleware/VerifyMcpToken.php
Middleware to verify MCP authentication token from header.

---

### app/Jobs/IngestPaperJob.php
Die Datei implementiert einen Job zur Ingestion von Papierinhalten, die in_chunks verarbeitet werden. Jeder Chunk wird an eine externe API (Ollama) gesendet, um ein Embedding zu generieren, das dann in einer Datenbank gespeichert wird.

---

### app/Jobs/TriggerLangdockAgent.php
This file defines a Laravel job to trigger a Langdock agent. It handles the queueing, uniqueness, and processing of triggering a webhook with user and project details.

---

### app/Livewire/Actions/Logout.php
```json
{
  "file_summary": "Diese Datei enthält eine Klasse 'Logout' im Namespace App\Livewire\Actions, die den aktuellen Benutzer aus der Anwendung mithilfe von Auth und Session abmeldet und dann zur Startseite weiterleitet."
}
```

---

### app/Mail/ContactConfirmationMail.php
Die Datei definiert eine Mail-Klasse zur Bestätigung einer Kontaktaufnahme. Sie implementiert die ShouldQueue-Schnittstelle für asynchrones Versenden.

---

### app/Mail/ContactInquiryMail.php
Klasse für die Erstellung einer E-Mail zur Kontakteinfrage. Sendet eine E-Mail mit dem Anliegen des Kontakts an den Absender.

---

### app/Mail/DeployNotificationMail.php
Klasse für eine E-Mail-Benachrichtigung über erfolgreiche Bereitstellungen. Verwendet Laravel-Mailables zur Erstellung der E-Mail-Inhalt und -Metadaten.

---

### app/Policies/ProjektPolicy.php
Definiert Policy für Zugriff auf Projekte. Erlaubt alle Aktionen nur für den Eigentümer des Projekts.

---

### app/Providers/AppServiceProvider.php
Initialisiert Gate-Policy für Projekte und Rate-Limiter für MCP und Webhooks. Aktiviert HTTPS bei Produktion.

---

### app/Providers/FortifyServiceProvider.php
Bietet Fortify-Konfigurationen für Anzeige, Aktionen und速率限制。

---

### app/Providers/VoltServiceProvider.php
Registriert und initialisiert die Livewire-Volt-Integration, um Livewire-Komponenten zu laden.

---

### app/Providers/Filament/AdminPanelProvider.php
Konfiguriert den Filament-Admin-Panelfür die Authentifizierung und Middleware.

---

### bootstrap/app.php
Initialisiert eine Laravel-Anwendung mit Routen, Middleware und Fehlert Handling.

---

### bootstrap/providers.php
Definiert ein Array von Dienstleister-Klassen für eine Laravel-Anwendung, einschließlich Filament-Admin-Panels und anderen fortgeschrittener Authentifizierungs- und Verwaltungs-Funktionalitäten.

---

### docker/common/nginx/default.conf
Nginx configuration for a web application, including routing, caching, and proxying to various backend services like PHP-FPM, MCP PostgreSQL, Paper Search MCP, and Ollama Embedding.

---

### docker/common/nginx/docker-entrypoint.sh
Dieses Skript generiert eine Nginx-Konfigurationsdatei aus einer Vorlage, ersetzt einen Platzhalter durch einen MCP-Auth-Token und führt dann den übergebenen Befehl aus.

---

### docker/common/nginx/dockerfile
Baut ein Nginx-Containerimage mit benutzerdefinierten Konfigurationen und Einbindung von statischen Dateien. Führt einen Eintrypoint-Skript aus, um Secrets zu injizieren.

---

### docker/common/nginx/nginx.conf
Konfiguration für Nginx, um ein grundlegendes Setup mit Laravel zu gestalten. Einstellungen für Worker-Verbindungen, MIME-Typen, Header-Erweiterung, Kompression und Datei-Upload-Grenzen.

---

### docker/common/php-fpm/dockerfile
Builds PHP-FPM Docker images for Laravel applications, including build, production and testing stages.

---

### docker/common/php-fpm/entrypoint.sh
Bash-script for PHP-FPM container entrypoint, syncing assets, waiting for DB, running migrations, caching config, and executing main command.

---

### docker/common/postgres/dockerfile
Dockerfile für PostgreSQL 16 mit HypoPG und pgVector Installation.

---

### docker/common/postgres/init-extensions.sql
Initiiert PostgreSQL-Erweiterungen wie hypopg, pg_stat_statements und vector.

---

### docs/overview-2026-04-02_0617.md
Die Datei enthält API-Controller für verschiedene Funktionen wie Kontaktanfragen, Datenschutz, Langdock-Webhooks und PaperRag-Operationen. Sie definiert auch Routen für API-Anfragen sowie Docker Compose-Dateien und Konfigurationsdateien.

---

### public/index.php
Startet die Laravel-Anwendung und verarbeitet eingehende HTTP-Anfragen.

---

### public/robots.txt
Eine Konfigurationsdatei für Suchmaschinen, die die Zugriffsberechtigungen auf den Website-Inhalte steuert.

---

### public/site.webmanifest
JSON-Datei für die Web App Manifest spezifiziert Icons, Farben und Anzeigemodus für eine Offline-Aktionsempfindliche Website.

---

### public/.htaccess
Konfiguriert die URL-Umleitung und den Authentifizierungsheader in einer Apache Webserver-Konfiguration.

---

### resources/css/app.css
Stellt CSS-Stile für eine Anwendung bereit, die TailwindCSS verwendet. Importiert Stile von Laravel-Pagination und definiert benutzerdefinierte Variante 'dark' sowie ein thematische Farbschema.

---

### resources/css/welcome.css
CSS-Datei für ein modernes, animiertes Landing Page. Enthält Farbvariablen, Styles für Header, Navigation, Cursor-Glow, Partikeln und Scroll-Reveal-Animationen.

---

### resources/js/welcome.js
Diese Datei enthält JavaScript-Funktionen für ein Interaktives Website-Layout mit Features wie automatischer Jahreszahl, Karteikarten, Navigation, Header-Effekte, Cursor-Glow, Scroll-Reveal-Animations, glatten Bildlauf und einer Kontaktformularabfrage. Sie verwendet externe APIs zur Datenanzeige.

---

### storage/app/.gitignore
.gitignore file to specify which files should be ignored by Git.

---

### .claude/agents/code-quality-architect.md
Dies ist eine Dokumentation für den Code-Qualität-Architekt-Assistenten. Er bietet Unterstützung bei der Planung komplexer Aufgaben, Best Practices in Code prüfen, Redundanzen eliminieren und Konfigurationen validieren. Der Assistent nutzt externe APIs und speichert Informationen in einer Datei.

---

### .github/copilot-instructions.md
README für Entwicklungsrichtlinien eines Laravel-Anwendungsprojekts

---

### .github/instructions/app_linn_games_custom.instructions.md
Dokumentation der Architektur und Entwicklung von app.linn.games. Enthält Projektübersicht, verwendete Technologien, Verzeichnisstruktur, Datenmodell, Authentifizierung/Autorisierung, KI-Integration (Langdock), und Webhook-Sicherheit.

---

### .github/instructions/docker-dev-workflow.instructions.md
Die Datei beschreibt den Entwicklungsworkflow für ein Docker-basiertes Projekt mit 10 Containern. Sie behandelt Code-Deployment, Bind-Mounts, Volumes, Rebuilds bei Änderungen und Dev-vs.-Production-Umgebung.

---
