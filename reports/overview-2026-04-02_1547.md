---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T15:47:45.801547
model: codellama:latest
run_id: codellama:latest
mode: overview
files_total: 250
errors: 1
run_time_s: 1987.8
---

# Funktions-Übersicht — 2026-04-02_1547

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 250
- **Fehler:** 1
- **Laufzeit:** 1987.8s

## api

### app/Http/Controllers/ContactController.php
Die Datei `app/Http/Controllers/ContactController.php` enthält einen HTTP-Controller für die Verarbeitung von Kontaktanfragen auf der Website von Linn.games. Der Controller definiert eine Methode namens `store`, die eine HTTP POST-Anfrage verarbeitet und eine E-Mail an die Info-Adresse von Linn.games sendet, bevor sie eine Antwort mit einer Bestätigung zurück gibt.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `App\Http\Controllers\ContactController`: Der Controller für die Verarbeitung von Kontaktanfragen.
* `Illuminate\Http\Request`: Die Request-Klasse von Laravel, die die Anfrageinformationen des HTTP-Requests enthält.
* `Illuminate\Support\Facades\Mail`: Die Mail-Facade von Laravel, die es ermöglicht, E-Mails zu senden.
* `App\Models\Contact`: Die Contact-Modellklasse von Linn.games, die die Eigenschaften und Methoden für eine Kontaktanfrage definiert.
* `App\Mail\ContactInquiryMail`: Die ContactInquiryMail-Klasse von Linn.games, die die E-Mail-Nachricht für die Anfrageverarbeitung definiert.
* `App\Mail\ContactConfirmationMail`: Die ContactConfirmationMail-Klasse von Linn.games, die die E-Mail-Bestätigung für eine erfolgreiche Anfrage definiert.

Die Datei importiert auch die Laravel-Standardbibliotheken und -Module, um die Anforderungen der Webanwendung zu erfüllen.

---

### app/Http/Controllers/Controller.php
File Summary: This is a PHP abstract class file that defines the basic structure of an HTTP controller in Laravel. The class provides a set of methods and properties that are common to all controllers in the application, such as the ability to handle requests and responses, validate input data, and perform redirects. It also contains various abstract methods that must be implemented by any subclass.

External Dependencies: None.

Classes/Functions/Endpoints:

* `Controller` - An abstract class that serves as a base for all HTTP controllers in the application.
* `handle()` - A method that handles incoming requests and calls the appropriate action method to handle them.
* `dispatch()` - A method that executes an action method and returns the response.
* `redirect()` - A method that redirects the user to a different URL.
* `validate()` - A method that validates input data against a set of rules.

Important Classes/Functions/Endpoints:

* `Controller` - The base class for all HTTP controllers in the application.
* `handle()` - The method that handles incoming requests and calls the appropriate action method to handle them.
* `dispatch()` - The method that executes an action method and returns the response.

---

### app/Http/Controllers/DsgvoController.php
Die Datei `app/Http/Controllers/DsgvoController.php` enthält eine Klasse mit zwei Methoden: `export()` und `deleteAccount()`.

* Die Methode `export()` exportiert die Daten des aktuellen Benutzers in einer JSON-Datei, wobei die zurückgegebenen Daten nur das Feld `id` der Benutzer-Tabelle, die Felder `name`, `email`, `created_at` und `updated_at` der Benutzer-Tabelle sowie eine Liste aller verknüpften Kontakte (`contacts`) und Einwilligungen (`consents`) enthalten. Die exportierte JSON-Datei wird mit einem Content-Disposition-Header markiert, um den Browser dazu zu bringen, die Datei herunterzuladen.
* Die Methode `deleteAccount()` löscht das Benutzerkonto und alle verknüpften Daten. Zunächst wird der aktuelle Benutzer aus der Sitzung geloggt, anschließend wird die Sitzung invalidiert und ein neuer Token generiert. Schließlich wird das Benutzerkonto gelöscht. Die Methode gibt eine JSON-Antwort mit einem Nachrichtentext zurück, der angibt, dass das Konto und alle verknüpften Daten gelöscht wurden.

---

### app/Http/Controllers/LangdockWebhookController.php
This file is part of the Laravel framework and contains a controller for handling webhook requests from the Langdock service. The `LangdockWebhookController` class defines a single method, `handle()`, which validates the incoming request data using the `validate()` method provided by the Laravel Request class. It then dispatches a job to trigger an agent on the server that will process the input and send it to the Langdock service for analysis. Finally, it returns a JSON response with a status of "queued" indicating that the job has been successfully queued for processing.

The file imports several external dependencies:

* `Illuminate\Http\Request`: The request class used to validate incoming data and access HTTP parameters.
* `App\Jobs\TriggerLangdockAgent`: The job class used to trigger the agent on the server that will process the input from Langdock.
* `Illuminate\Support\Facades\Response`: The response class used to return a JSON response with the status of "queued".

---

### app/Http/Controllers/PaperRagController.php
Die Datei `app/Http/Controllers/PaperRagController.php` enthält eine Klasse namens `PaperRagController`, die zwei Methoden implementiert: `ingest()` und `search()`.

**Ingest-Methode:**

* Die Methode wird aufgerufen, wenn ein neuer Papierrag in das System eingefügt werden soll.
* Sie validiert die übergebenen Daten mit einem RequestValidator und setzt den Job `IngestPaperJob` in der Warteschlange.
* Der Job wird ausgeführt, um den eingegebenen Papierrag zu verarbeiten und in der Datenbank zu speichern.
* Die Methode gibt eine JSON-Antwort zurück, die nur den Status des Vorgangs enthält (in diesem Fall "queued").

**Search-Methode:**

* Diese Methode wird aufgerufen, wenn ein Suchanfrage für Papierräge eingeht.
* Sie validiert die übergebenen Daten mit einem RequestValidator und verwendet den `Http`-Wrapper von Laravel um eine Anfrage an den Embedding-Service zu senden.
* Wenn der Embedding-Service nicht erreichbar ist, wird eine JSON-Antwort mit dem Fehler "Embedding service unavailable" zurückgegeben.
* Andernfalls wird die Suche ausgeführt und die Suchergebnisse werden in einer JSON-Antwort zurückgegeben.

Die Methode `ingest()` verwendet den `Jobs`-Wrapper von Laravel, um einen Job namens `IngestPaperJob` in der Warteschlange zu setzen. Der Job ist zuständig für die Verarbeitung des eingegebenen Papierrags und speichert ihn in der Datenbank.

Die Methode `search()` verwendet den `Http`-Wrapper von Laravel um eine Anfrage an den Embedding-Service zu senden, um die Suche durchzuführen. Wenn der Embedding-Service nicht erreichbar ist, wird eine JSON-Antwort mit dem Fehler "Embedding service unavailable" zurückgegeben. Andernfalls wird die Suche ausgeführt und die Suchergebnisse werden in einer JSON-Antwort zurückgegeben.

Die Methode `ingest()` verwendet den `DB`-Wrapper von Laravel, um eine Datenbankabfrage auszuführen, um den Papierrag in der Datenbank zu speichern. Die Methode `search()` verwendet ebenfalls den `DB`-Wrapper, um eine Datenbankabfrage auszuführen, um die Suchergebnisse zurückzugeben.

Die Klasse `PaperRagController` importiert auch den `RequestValidator`, um die übergebenen Daten zu validieren, und den `Http`-Wrapper von Laravel, um eine Anfrage an den Embedding-Service zu senden. Es gibt auch einen `Jobs`-Wrapper von Laravel, der verwendet wird, um den Job `IngestPaperJob` in der Warteschlange zu setzen.

---

### routes/api.php
This is an API route file for a Laravel application. It defines several routes and their corresponding controller actions using the `Route` class provided by Laravel's routing system.

The file starts by importing two classes: `LangdockWebhookController` and `PaperRagController`. These are the controllers that will handle the incoming requests for the API endpoints defined in this file.

Next, the file defines a middleware group that applies two middlewares to all routes within the group. The first middleware verifies the signature of the incoming request using the `VerifyLangdockSignature` class, and the second middleware throttles the requests using the `throttle:webhooks` and `throttle:mcp` middlewares.

The file then defines three routes:

1. `/user`: This route returns the current user object from the authenticated request (using the `auth:sanctum` middleware).
2. `/webhooks/langdock`: This route handles incoming webhook requests from the Langdock service using the `LangdockWebhookController`. The middlewares applied to this route verify the signature of the incoming request and throttle the requests.
3. `/papers/ingest` and `/papers/rag-search`: These routes handle incoming requests for ingesting papers into the application and searching for paper rags, respectively, using the `PaperRagController`. The middlewares applied to these routes verify the token of the incoming request and throttle the requests.

Overall, this file defines the API endpoints and their corresponding controller actions for a Laravel application.

---

### routes/console.php
Die Datei `routes/console.php` ist eine PHP-Datei, die dem Laravel Framework-Bereich gehört und für das Konfigurieren von Artisan-Kommandos verwendet wird. Die Datei enthält zwei Kommandos:

* `inspire`: Ein einfaches Beispiel-Kommando, das einen inspirierenden Zitat auswählt und es im Konsolenfenster ausgibt.
* `deploy:notify`: Ein weiteres Beispiel-Kommando, das eine E-Mail an einen bestimmten Empfänger (bene@linn.games) sendet, um den Versand von E-Mails zu überprüfen.

Die Datei enthält externe Abhängigkeiten, indem sie die Klassen `Illuminate\Foundation\Inspiring` und `Illuminate\Support\Facades\Mail` importiert, sowie die Funktion `now()` aus dem Laravel-Framework verwendet, um die aktuelle Zeit zu bestimmen.

Die Datei enthält keine Bewertung oder Fehlerbehebung, da es sich hierbei nur um Beispielkommandos handelt, die nicht direkt für eine Produktionsumgebung ausgeführt werden sollten. Die Datei ist jedoch wichtig, da sie das Konfigurieren und Aufrufen von Artisan-Kommandos in der Laravel-Umgebung ermöglicht.

---

### routes/web.php
This file defines routes for a Laravel application. It uses the `Route` facade to define various endpoints that can be accessed by the user. The file also imports several classes and functions from other namespaces, including `App\Http\Controllers`, `App\Models`, `Illuminate\Support\Facades`, and `Laravel\Fortify`.

The routes defined in this file include:

* `/`: A route that returns a view for the homepage.
* `/contact`: A route that allows users to submit a contact form.
* `/Impressum.html`: A route that displays the impressum (legal information) page.
* `/dsgvo.html`: A route that displays the dsgvo (data protection) page.
* `/AGB.html`: A route that displays the AGD (general terms and conditions) page.
* `dashboard`: A route that returns a view for the user's dashboard, which is only accessible to authenticated users who have verified their email address.
* `settings/profile`: A route that allows users to edit their profile information.
* `settings/password`: A route that allows users to change their password.
* `settings/appearance`: A route that allows users to customize the appearance of their dashboard.
* `settings/webhooks`: A route that allows users to manage webhooks for their account.
* `settings/two-factor`: A route that allows users to manage two-factor authentication for their account, if it is enabled and configured.
* `dsgvo/export`: A route that allows users to export their personal data in a machine-readable format.
* `dsgvo/delete-account`: A route that allows users to delete their account permanently.
* `recherche`: A route that displays the research page, where users can search for projects.
* `recherche/{projekt}`: A route that displays a specific project, based on its slug.

---

## config

### docker-compose.dev.yml
Die Datei `docker-compose.dev.yml` enthält eine Konfiguration für den Docker Compose-Service, der die Webanwendung startet. In dieser Datei wird der nginx-Port von 6481 auf 6480 geändert, da Port 6481 durch Visual Studio Code belegt ist. Die Datei verwendet das YAML-Format und enthält keine externen Abhängigkeiten oder API-Aufrufe.

---

### docker-compose.yml
This is a `docker-compose.yml` file that defines the services and networks needed to run a Linn API. The file includes the following services:

* `web`: A Nginx web server that serves the application's frontend.
* `php-fpm`: A PHP FastCGI Process Manager (FPM) that runs the application's backend.
* `php-cli`: A PHP CLI for running command line scripts.
* `php-test`: A PHP FPM with a testing environment, used for running unit tests.
* `queue-worker`: A background worker that processes queued tasks.
* `postgres`: A PostgreSQL database server.
* `redis`: A Redis in-memory data store.
* `postgres-mcp`: A container that runs the PostgreSQL MCP (Microservices Command and Control) service.
* `ollama`: A container that runs the Ollama language processor.
* `mcp-paper-search`: A container that runs the Linn API's paper search microservice.

The file also defines a network called `linn-network` for all these services to communicate with each other. Additionally, it specifies volumes for persisting data between container restarts and for sharing data between containers.

---

### .env.example
Die Datei `.env.example` ist eine Beispielumgebungsdatei für die Laravel-Anwendung Linn Games. Sie enthält wichtige Konfigurationen und Optionen, die von der Anwendung verwendet werden. Hier sind einige der wichtigsten Funktionen dieser Datei:

* `APP_NAME`: Der Name der Anwendung, das heißt "Linn Games".
* `APP_ENV`: Die Umgebung in der die Anwendung läuft, hier ist dies "local".
* `APP_KEY`: Ein eindeutiger Schlüssel für die Anwendung.
* `APP_DEBUG`: Der Debug-Modus der Anwendung. Wenn dieser auf "true" gesetzt ist, werden Fehler im Browser-Konsolenfenster angezeigt.
* `APP_URL`: Die URL des Anwendungs-Hosts.
* `APP_LOCALE`: Die Standardsprache der Anwendung. Hier ist dies "en".
* `APP_FALLBACK_LOCALE`: Die Fallback-Sprache, wenn die Standardsprache nicht unterstützt wird. Hier ist dies auch "en".
* `APP_FAKER_LOCALE`: Die Faker-Sprache, die für generische Daten verwendet wird. Hier ist dies auch "en_US".
* `APP_MAINTENANCE_DRIVER`: Der Wartungs-Driver der Anwendung. Hier ist dies "file".
* `APP_MAINTENANCE_STORE`: Der Wartungs-Store der Anwendung. Hier ist dies "database".
* `BCRYPT_ROUNDS`: Die Anzahl der Runden für das BCRYPT-Kryptografiealgorithmus. Hier ist dies "12".
* `LOG_CHANNEL`: Der Protokollkanal, der von der Anwendung verwendet wird. Hier ist dies "stack".
* `LOG_STACK`: Der Protokollstapel, der von der Anwendung verwendet wird. Hier ist dies "single".
* `LOG_DEPRECATIONS_CHANNEL`: Der Kanal für deprecations, der von der Anwendung verwendet wird. Hier ist dies "null".
* `LOG_LEVEL`: Die Protokollebene, die von der Anwendung verwendet wird. Hier ist dies "debug".
* `DB_CONNECTION`: Die Datenbank-Verbindung, die von der Anwendung verwendet wird. Hier ist dies "pgsql".
* `DB_HOST`: Der Hostname der Datenbank. Hier ist dies "postgres".
* `DB_PORT`: Der Port der Datenbank. Hier ist dies "5432".
* `DB_DATABASE`: Der Name der Datenbank. Hier ist dies "linn_games".
* `DB_USERNAME`: Der Benutzername für den Zugriff auf die Datenbank. Hier ist dies "postgres".
* `DB_PASSWORD`: Das Passwort für den Zugriff auf die Datenbank. Hier ist dies "change-me".
* `SESSION_DRIVER`: Der Session-Driver, der von der Anwendung verwendet wird. Hier ist dies "redis".
* `SESSION_LIFETIME`: Die Gültigkeitsdauer einer Sitzung. Hier ist dies "120" (2 Stunden).
* `SESSION_ENCRYPT`: Ob die Sitzungen verschlüsselt sind oder nicht. Hier ist dies "false".
* `SESSION_PATH`: Der Session-Pfad. Hier ist dies "/".
* `SESSION_DOMAIN`: Der Domainname, unter dem die Sitzungen gespeichert werden sollen. Hier ist dies "null".
* `BROADCAST_CONNECTION`: Die Verbindung für die Broadcasting-Aktivität. Hier ist dies "log".
* `FILESYSTEM_DISK`: Der Dateisystem-Datenträger, der von der Anwendung verwendet wird. Hier ist dies "local".
* `QUEUE_CONNECTION`: Die Warteschlangen-Verbindung, die von der Anwendung verwendet wird. Hier ist dies "redis".
* `CACHE_STORE`: Der Cache-Store, der von der Anwendung verwendet wird. Hier ist dies "redis".
* `MEMCACHED_HOST`: Der Hostname des Memcached-Servers. Hier ist dies "127.0.0.1".
* `REDIS_CLIENT`: Der Redis-Client, der von der Anwendung verwendet wird. Hier ist dies "predis".
* `REDIS_HOST`: Der Hostname des Redis-Servers. Hier ist dies "redis".
* `REDIS_PASSWORD`: Das Passwort für den Zugriff auf den Redis-Server. Hier ist dies "null".
* `REDIS_PORT`: Der Port des Redis-Servers. Hier ist dies "6379".
* `MAIL_MAILER`: Der E-Mail-Mailer, der von der Anwendung verwendet wird. Hier ist dies "smtp".
* `MAIL_SCHEME`: Das Schema für den SMTP-Server. Hier ist dies "null".
* `MAIL_HOST`: Der Hostname des SMTP-Servers. Hier ist dies "127.0.0.1".
* `MAIL_PORT`: Der Port des SMTP-Servers. Hier ist dies "2525".
* `MAIL_USERNAME`: Der Benutzername für den Zugriff auf den SMTP-Server. Hier ist dies "null".
* `MAIL_PASSWORD`: Das Passwort für den Zugriff auf den SMTP-Server. Hier ist dies "null".
* `MAIL_FROM_ADDRESS`: Die E-Mail-Adresse, von der die Anwendung E-Mails sendet. Hier ist dies "info@linn.games".
* `MAIL_FROM_NAME`: Der Name des Absenders. Hier ist dies "${APP_NAME}".
* `AWS_ACCESS_KEY_ID`: Die AWS Access Key ID. Hier ist dies ein leerer String.
* `AWS_SECRET_ACCESS_KEY`: Die AWS Secret Access Key. Hier ist dies ein leerer String.
* `AWS_DEFAULT_REGION`: Die AWS Default Region. Hier ist dies "us-east-1".
* `AWS_BUCKET`: Der AWS Bucket. Hier ist dies ein leerer String.
* `AWS_USE_PATH_STYLE_ENDPOINT`: Ob der AWS Endpunkt im Pfad-Stil verwendet wird oder nicht. Hier ist dies "false".
* `LANGDOCK_API_KEY`: Die Langdock API Key. Hier ist dies ein leerer String.
* `LANGDOCK_WEBHOOK_URL`: Die URL des Langdock Webhooks. Hier ist dies ein leerer String.
* `LANGDOCK_WEBHOOK_SECRET`: Das Geheimnis des Langdock Webhooks. Hier ist dies ein leerer String.
* `OLLAMA_URL`: Die URL des Ollama Embedding Service. Hier ist dies "http://localhost:11434".
* `POSTGRES_DATABASE`: Der Name der PostgreSQL-Datenbank. Hier ist dies "linn_games".
* `POSTGRES_USERNAME`: Der Benutzername für den Zugriff auf die PostgreSQL-Datenbank. Hier ist dies "postgres".
* `POSTGRES_PASSWORD`: Das Passwort für den Zugriff auf die PostgreSQL-Datenbank. Hier ist dies "change-me".
* `MCP_AUTH_TOKEN`: Der Authentifizierungs-Token des MCP. Hier ist dies ein leerer String.
* `LANGDOCK_DB_USERNAME`: Der Benutzername für den Zugriff auf die Langdock DB. Hier ist dies ein leerer String.
* `LANGDOCK_DB_PASSWORD`: Das Passwort für den Zugriff auf die Langdock DB. Hier ist dies ein leerer String.
* `VITE_APP_NAME`: Der Name der Vite-Anwendung. Hier ist dies "${APP_NAME}".

---

### config/app.php
Die `config/app.php`-Datei ist eine Konfigurationsdatei für die Laravel-Anwendung. In dieser Datei werden einige wichtige Einstellungen festgelegt, die für die gesamte Anwendung gelten. Hier sind die wichtigsten Funktionen und Klassen in dieser Datei:

* `name` - Der Name der Anwendung wird hier festgelegt. Der Wert von `APP_NAME` wird verwendet, wenn der Framework Benachrichtigungen oder andere UI-Elemente benötigt, die den Namen der Anwendung anzeigen müssen.
* `env` - Die Umgebung der Anwendung wird hier festgelegt. Der Wert von `APP_ENV` wird verwendet, um die Konfiguration von Diensten zu bestimmen, die von der Anwendung verwendet werden sollen.
* `debug` - Wenn der Debug-Modus aktiviert ist, zeigen Fehler mit Stapelüberwachungen auf. Der Wert von `APP_DEBUG` wird verwendet, um festzustellen, ob der Debug-Modus aktiviert ist oder nicht.
* `url` - Die URL der Anwendung wird hier festgelegt. Der Wert von `APP_URL` wird verwendet, wenn der Framework Benachrichtigungen oder andere UI-Elemente benötigt, die die URL der Anwendung anzeigen müssen.
* `timezone` - Hier wird die Standardzeitzone der Anwendung festgelegt. Der Wert von `APP_TIMEZONE` wird verwendet, um die Zeitzone für Laravel's date und time functions festzulegen.
* `locale` - Hier wird die Standardlocale der Anwendung festgelegt. Der Wert von `APP_LOCALE` wird verwendet, um die Konfiguration von Übersetzungen zu bestimmen.
* `fallback_locale` - Hier wird die Fallback-Locale der Anwendung festgelegt. Der Wert von `APP_FALLBACK_LOCALE` wird verwendet, wenn eine bestimmte Sprache nicht verfügbar ist.
* `faker_locale` - Hier wird die Faker-Locale der Anwendung festgelegt. Der Wert von `APP_FAKER_LOCALE` wird verwendet, um die Locale für Laravel's faker-Funktionen festzulegen.
* `cipher` - Hier wird die Verschlüsselungsmethode für Laravel's Encryption-Dienst festgelegt. Der Wert von `APP_CIPHER` wird verwendet, um eine sichere Verschlüsselung zu verwenden.
* `key` - Hier wird der Verschlüsselungsschlüssel für Laravel's Encryption-Dienst festgelegt. Der Wert von `APP_KEY` muss ein 32-Zeichen-String sein, um sicherzustellen, dass alle verschlüsselten Werte sicher sind.
* `previous_keys` - Hier wird eine Liste der vorherigen Verschlüsselungsschlüssel für Laravel's Encryption-Dienst festgelegt. Der Wert von `APP_PREVIOUS_KEYS` sollte ein Komma-separierter String mit mindestens 32 Zeichen sein, um sicherzustellen, dass alle verschlüsselten Werte sicher sind.
* `maintenance` - Hier wird der Driver für Laravel's Maintenance-Modus festgelegt. Der Wert von `APP_MAINTENANCE_DRIVER` kann "file" oder "cache" sein. Der Wert von `APP_MAINTENANCE_STORE` gibt an, ob die Statusüberschrift für den Maintenance-Modus in der Datei oder in der Datenbank gespeichert werden soll.

---

### config/auth.php
This file is part of a Laravel application and contains the configuration settings for the authentication system. The file defines the following:

* `defaults`: Sets the default guard and password broker used by the application.
* `guards`: Defines the authentication guards that are available to the application. Each guard has a driver and provider associated with it.
* `providers`: Defines the user providers that can be used by the application. This includes both database-based and Eloquent-based providers.
* `passwords`: Sets the configuration options for password reset functionality, including the table used to store tokens and the user provider that is invoked to retrieve users.
* `password_timeout`: Defines the number of seconds before a password confirmation window expires and users are asked to re-enter their password via the confirmation screen.

The file imports the `env` function from the Laravel framework to allow for environment variable overrides. It also uses the `App\Models\User::class` constant to reference the user model class.

---

### config/cache.php
This file is part of the Laravel framework, specifically in the "config" directory. It contains configuration settings for the cache mechanism used by the application. The file defines several cache stores that can be used to store data in different ways, such as using an array, a database, a file system, memcached, Redis, DynamoDB, or Octane. Each cache store is defined with a driver and other configuration options, depending on the type of cache store being used.

The main purpose of this file is to configure the various cache stores available in the application and to define the default cache store that will be used if another one is not explicitly specified when running a cache operation. The "default" option specifies which cache store should be used by default, while the "stores" array allows you to define multiple cache stores for the same driver.

The file also defines the cache key prefix, which can be useful in situations where there are other applications using the same cache. The cache key prefix is a string that will be prepended to every cache key before it is stored or retrieved. This can help avoid collisions between different applications that use the same cache store.

In summary, this file allows you to configure the cache mechanism used by the application and define the default cache store that should be used. It also provides a way to define multiple cache stores for the same driver and a way to configure the cache key prefix.

---

### config/cookie-consent.php
File Summary: This file contains configuration settings for the cookie consent feature of a Laravel application. The file defines two configuration options: `enabled`, which determines whether the cookie consent dialog should be displayed to users, and `cookie_name`, which specifies the name of the cookie that stores information about the user's decision to accept the conditions. Additionally, the `cookie_lifetime` setting sets the duration of the cookie in days, with a default value of 365 (12 months).

Important Classes and Functions:

* The file returns an associative array containing configuration options for the cookie consent feature.
* The `env()` function is used to retrieve environment variables from the `.env` file.

External Dependencies:

* This file does not have any external dependencies beyond the Laravel framework itself.

---

### config/database.php
The file `config/database.php` is a configuration file for the Laravel database management system. It contains various settings and connections that allow you to interact with different databases in your application.

Here are the main features of this file:

* `default`: This section defines the default connection name for the database. It can be set to any of the connections defined in the `connections` section below. The default value is `sqlite`.
* `connections`: This section contains a list of available database connections, each with its own settings. Each connection has a unique name and a driver (e.g., `mysql`, `pgsql`, etc.).
* `migrations`: This section defines the migration repository table that keeps track of all the migrations that have run in your application. You can use this information to determine which migrations are missing or need to be updated.
* `redis`: This section contains settings for Redis, an open-source key-value store that provides a richer set of commands than typical key-value systems such as Memcached.

In summary, the main purpose of this file is to define database connections and migrations for your Laravel application. It allows you to configure and manage multiple databases within your application.

---

### config/filesystems.php
This is a configuration file for Laravel's filesystem management system. It defines the default disk and various disks that can be used to store files in different locations. The file also contains settings for symbolic links that will be created when running the `storage:link` Artisan command.

The main functions of this file are:

* Define the default filesystem disk, which is set to "local" by default.
* List various disks that can be used for storing files, including "public", "s3", and "local". Each disk has a unique name, driver, root directory, URL, visibility, endpoint, and other settings.
* Define symbolic links that will be created when running the `storage:link` Artisan command. These links are typically used to create a shortcut from the public directory of the project to the storage directory where files are stored.

The file is in JSON format, which makes it easy to read and modify. The main sections of the file are:

* The "default" section defines the default filesystem disk that should be used by the framework.
* The "disks" section lists various disks that can be used for storing files. Each disk has a unique name, driver, root directory, URL, visibility, endpoint, and other settings.
* The "links" section defines symbolic links that will be created when running the `storage:link` Artisan command. These links are typically used to create a shortcut from the public directory of the project to the storage directory where files are stored.

---

### config/fortify.php
This file is part of the Laravel framework and contains configuration settings for the Fortify package. The Fortify package provides a set of features to help secure your application by adding additional security measures such as password hashing, rate limiting, and two-factor authentication.

The file defines several configuration options:

* `guard`: Specifies which authentication guard to use when authenticating users. This value should correspond with one of the guards that is already present in your "auth" configuration file.
* `passwords`: Specifies which password broker Fortify can use when a user is resetting their password. This configured value should match one of the password brokers setup in your "auth" configuration file.
* `username`: Defines which model attribute should be considered as your application's "username" field. Typically, this might be the email address of the users but you are free to change this value here.
* `email`: Defines which model attribute should be considered as your application's "email" field. Typically, this might be the email address of the users but you are free to change this value here.
* `lowercase_usernames`: Specifies whether usernames should be lowercased before saving them in the database, as some database system string fields are case sensitive. You may disable this for your application if necessary.
* `home`: Specifies the path where users will get redirected during authentication or password reset when the operations are successful and the user is authenticated. You are free to change this value.
* `prefix`: Specifies which prefix Fortify will assign to all the routes that it registers with the application. If necessary, you may change subdomain under which all of the Fortify routes will be available.
* `middleware`: Specifies which middleware Fortify will assign to the routes that it registers with the application. If necessary, you may change these middleware but typically this provided default is preferred.
* `limiters`: Specifies a custom rate limiter for login requests. By default, Fortify will throttle logins to five requests per minute for every email and IP address combination. However, if you would like to specify a custom rate limiter to call then you may specify it here.
* `views`: Specifies whether the routes returning views should be disabled. This may be especially true if you're writing a custom single-page application.
* `features`: A list of Fortify features that are enabled by default. You can remove any of these features or add additional ones as needed. The available features are: registration, reset passwords, email verification, update profile information, update passwords, and two-factor authentication.

---

### config/logging.php
This file is part of the Laravel framework (<https://github.com/laravel/framework>). It defines the logging configuration for the application, including the default log channel and the various channels available for logging. The `config/logging.php` file includes several key elements:

1. The default log channel, which can be set using the `LOG_CHANNEL` environment variable. If no value is specified, it defaults to `stack`.
2. A list of supported log channels, including `single`, `daily`, `slack`, `syslog`, `errorlog`, and `monolog`. Each channel has a driver, which specifies the logging mechanism to use (e.g., `single` uses a single file for all logs).
3. The `channels` array defines the specific configuration for each log channel. For example, the `daily` channel logs to a separate file every day and sets the log level to debug.
4. The `processors` array specifies the processors that should be used for each log channel. In this case, only the `PsrLogMessageProcessor` is defined, which is responsible for adding context data to each log message.
5. The `level` variable defines the minimum level of logs that should be written to a given channel. For example, setting the `LOG_LEVEL` environment variable to `debug` will cause all debug-level logs to be written to the `single` and `daily` channels, but only critical-level logs to be written to the `syslog` channel.
6. The `replace_placeholders` variable specifies whether placeholders in log messages should be replaced with their actual values. This is useful for debugging purposes, as it allows you to see the values of variables in your code in your log messages.
7. The `handlers` array defines the handlers that should be used for each channel. For example, the `stream` handler is used for the `stderr` channel to write logs to `php://stderr`, and the `syslog` handler is used for the `syslog` channel to write logs to a system log.
8. The `formatter` variable specifies the formatter that should be used to format log messages before they are written to the log file. In this case, the `LOG_STDERR_FORMATTER` environment variable is set to `line`, which means that each log message will be written on a new line in the log file.
9. The `ignore_exceptions` variable specifies whether exceptions should be ignored for a given channel. In this case, all exceptions are ignored for the `stack` channel.
10. The `emergency` path variable specifies the location of the emergency log file.

Overall, this configuration file allows you to define multiple log channels and specify the minimum level of logs that should be written to each channel. It also provides a way to customize the log format and add context data to your log messages.

---

### config/mail.php
This file is a part of the Laravel framework and contains configuration for email sending in the application. It sets up various mailers, including SMTP, Postmark, and Sendmail, which are used to send emails from the application. The `default` option specifies the default mailer that should be used when sending emails. The `mailers` array allows you to specify additional mailers with their respective settings.

The `from` option sets up a global "From" address for all emails sent by the application. You can change this setting in the `.env` file or set it explicitly here.

Overall, this file is used to configure email sending in Laravel and allows you to specify multiple mailers with their respective settings.

---

### config/queue.php
This file contains the configuration for the queuing system in a Laravel application. The main functionality of this file is to define the connection and driver options for the various queue drivers available in Laravel, such as database, sync, beanstalkd, sqs, redis, deferred, background, failover, and null.

The `default` key specifies the default queue connection name. This should be set to one of the connections defined in the `connections` array below. The `connections` array defines a list of available queue drivers, each with its own configuration options.

For example, the `database` driver is configured to use a specific database connection (`env('DB_QUEUE_CONNECTION')`) and table (`env('DB_QUEUE_TABLE', 'jobs')`). The `queue` key specifies the queue name for this connection. The `retry_after` key specifies the number of seconds to wait before retrying a job that has failed.

The `failed` key defines the configuration for handling failed jobs. By default, Laravel will store failed jobs in the database using UUIDs as primary keys. This can be changed by setting the `driver` key to one of the other supported drivers (e.g., "dynamodb", "file", or "null").

Overall, this file is used to configure the queuing system in a Laravel application and define the different connections and drivers available for handling jobs.

---

### config/services.php
This is a configuration file for third-party services in a Laravel application. It defines the credentials and settings for various services, such as Mailgun, Postmark, AWS, Slack, Langdock, MCP, and Ollama.

The file uses the `env()` function to load environment variables that are defined in the `.env` file. These variables contain the API keys, tokens, and other settings required for each service.

Here's a summary of the functions in this file:

* It defines an array with the names of the services used by the application (e.g., Mailgun, Postmark, AWS, Slack, Langdock, MCP, Ollama).
* Each service has its own section in the array, which contains the necessary credentials and settings for that service. For example, the `mailgun` section defines the API key required to send emails through Mailgun, while the `postmark` section defines the API key required to send emails through Postmark.
* The `ses` section defines the AWS access key ID and secret access key used to send emails through Amazon SES.
* The `slack` section defines the bot user OAuth token and channel used for sending notifications to Slack.
* The `langdock` section defines the API key and webhook URL used for integrating with Langdock.
* The `mcp` section defines the authentication token used for authenticating with MCP.
* The `ollama` section defines the URL and secret used for communicating with Ollama.

Overall, this file is used to configure the services that are used by the application, and it allows for easy management of these services' settings.

---

### config/session.php
This file configures session management in a Laravel application. The main classes and functions involved are:

* `Session` class: This is the core class for handling sessions in Laravel, which provides methods for retrieving, setting, and destroying session data.
* `SessionManager` class: This class is responsible for managing the driver used for storing session data, as well as configuring the settings for the session itself.
* `CookieGuard` class: This class is responsible for storing session data in cookies, which are sent to the client and can be retrieved by the application on subsequent requests.
* `FileSessionHandler` class: This class provides a session driver that stores session data in files on the server.
* `DatabaseSessionHandler` class: This class provides a session driver that stores session data in a database.
* `MemcachedSessionHandler` class: This class provides a session driver that stores session data in a Memcached cache store.
* `RedisSessionHandler` class: This class provides a session driver that stores session data in a Redis cache store.

The file also defines several configuration options for the session, including the default driver (`driver`), the lifetime of the session (`lifetime`), whether the session should expire when the browser is closed (`expire_on_close`), and whether to encrypt the session data (`encrypt`). It also specifies the location where the session files are stored (`files`) and the database connection used for storing session data (`connection`). Additionally, it configures various settings for the cookie that stores the session ID (`cookie`, `path`, `domain`, `secure`, `http_only`, and `same_site`).

---

### docker/common/php-fpm/php-dev.ini
Die Datei "docker/common/php-fpm/php-dev.ini" ist eine PHP ini-Konfigurationsdatei, die speziell für Entwicklungszwecke erstellt wurde. Sie enthält einige Einstellungen, die es ermöglichen, die Leistung des OPcache-Cache-Managers zu verbessern und den Fehlerbehandlungsumfang zu erweitern.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `[opcache]`: Dies ist ein PHP-Ini-Abschnitt, der die Einstellungen für den OPcache Cache-Manager enthält.
* `opcache.validate_timestamps = 1`: Diese Einstellung erzwingt, dass der OPcache Cache-Manager bei jedem Request die Dateienamen des Skripts erneut überprüfen muss, um sicherzustellen, dass sie noch gültig sind.
* `opcache.revalidate_freq = 0`: Diese Einstellung deaktiviert den automatischen Ablauf der OPcache-Einträge nach einer bestimmten Zeitspanne (Revalidation Frequency). Dadurch wird sichergestellt, dass die Dateien immer wieder überprüft werden, wenn sie geändert wurden.
* `[PHP]`: Dies ist ein PHP-Ini-Abschnitt, der die Einstellungen für den PHP-Interpreter enthält.
* `display_errors = On`: Diese Einstellung aktiviert die Anzeige von Fehlermeldungen an den Benutzer.
* `error_reporting = E_ALL`: Diese Einstellung definiert, welche Fehlerarten und -stufen für den Benutzer sichtbar sind. Hier wird "E_ALL" verwendet, um alle Fehler und Warnungen anzuzeigen.

---

### docs/api.yaml
Die Datei `api.yaml` enthält die Definitionen für verschiedene Endpunkte einer Web-API, die von der Research-Management-Plattform app.linn.games verwendet werden. Hier sind die wichtigsten Funktionen und Klassen:

* `getUser`: Gibt den authentifizierten Benutzer zurück.
* `handleLangdockWebhook`: Ein eingehender Webhook vom Langdock-KI-Agent. Startet einen TriggerLangdockAgent-Job in der Redis-Queue.
* `ingestPaper`: Nimmt einen Paper-Text entgegen, teilt ihn in Chunks auf und dispatcht einen IngestPaperJob. Die Chunks werden via Ollama (nomic-embed-text) als Vektoren in pgvector gespeichert.
* `ragSearch`: Sucht in den paper_embeddings via Cosine Distance. Die Suchanfrage wird zuerst durch Ollama (nomic-embed-text) als Vektor eingebettet und dann gegen pgvector abgeglichen.
* `User`: Klasse für Benutzer, die authentifiziert werden müssen. Es gibt Eigenschaften wie `id`, `name`, `email`, `status`, `email_verified_at` und weitere.
* `PaperChunk`: Klasse für Paper-Chunks, die in der Suche verwendet werden. Es gibt Eigenschaften wie `id`, `projekt_id`, `source`, `paper_id`, `title`, `chunk_index`, `text_chunk`, `metadata` und `similarity`.

Es gibt auch einige Import-Anweisungen, die für die Verwendung der API-Endpunkte benötigt werden. Zum Beispiel wird die Ollama-API (nomic-embed-text) verwendet, um den Paper-Text in Vektoren zu konvertieren.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
This issue template is for reporting a bug in the code. The following fields are included:

* Description: A short description of the problem and what is expected to happen instead.
* Steps to reproduce: A list of steps that can be taken to reproduce the error.
* Severity: The severity of the bug, with options for "critical" (data loss or security), "high" (core functionality affected), "medium" (workaround available), and "low" (cosmetic or edge case).
* Affected areas: A list of files, routes, or services that are affected by the bug.
* Acceptance criteria: A list of criteria that must be met before the bug is considered fixed.

The issue template uses a textarea element for each field to allow for detailed descriptions and lists of steps. The severity and acceptance criteria fields use dropdown menus to make it easier to select options.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
This is a GitHub issue template for submitting feature requests. It includes the following fields:

* Summary: A short summary of the proposed feature.
* Motivation / Problem: The problem that the feature will solve.
* Vorgeschlagene Lösung: The proposed solution to the problem.
* Betroffene Bereiche: The affected files, routes, services, or models.
* Breaking Change?: Whether the proposed feature is a breaking change (e.g., requires migration) or not.
* Akzeptanzkriterien: The acceptance criteria for the feature to be considered complete.

The template uses the "markdown" field type to allow users to format their responses in Markdown, and the "textarea" field type to allow them to provide longer answers. The "dropdown" field type is used to create a drop-down menu with options, and the "required" validation ensures that the user must select an option before submitting the issue.

---

### .github/ISSUE_TEMPLATE/refactor.yml
This file contains the necessary information for a refactored pull request. It includes a description of the proposed changes, the affected areas, and the acceptance criteria.

---

### .github/workflows/lint.yml
This is a GitHub Actions workflow file that defines a continuous integration (CI) pipeline for the project. The pipeline includes several jobs, which are executed in sequence:

1. Checkout the code from the repository using the `actions/checkout` action.
2. Set up the PHP environment using the `shivammathur/setup-php` action.
3. Add Flux credentials loaded from ENV using the `composer config` command.
4. Install dependencies using Composer and NPM.
5. Run Pint, a code analysis tool for PHP, using the `vendor/bin/pint` command.
6. Commit changes to the repository (optional).

The pipeline is triggered on push events for the branches `develop`, `main`, and any branch that starts with `feature/` or `fix/`. The permissions required by the pipeline are limited to writing to the repository's contents.

---

### .github/workflows/tests.yml
This is a GitHub Actions workflow file named `tests.yml` that defines a test job for running tests in the repository. The file uses the `composer` and `npm` tools to install dependencies and build assets, and then runs tests using `pest`.

Here are the main features of this file:

* It is triggered on push events to the `develop`, `main`, and `feature/**` branches, as well as pull requests to these branches.
* It uses the `ubuntu-latest` runner image to run the tests.
* It sets up a PHP environment with version 8.4 and installs Composer v2 and Node v22 using the `shivammathur/setup-php` and `actions/setup-node` actions, respectively.
* It installs dependencies using Composer and copies the `.env.example` file to `.env`.
* It generates an application key for Laravel using `php artisan key:generate`.
* It builds assets using Node v22 with the `npm run build` command.
* It runs tests using `pest`, which is a PHP test runner that supports running tests in parallel.

This workflow file is useful for automating testing in a Laravel application, as it sets up a testing environment with dependencies installed and tests built using Composer and Node, respectively.

---

## data_access

### app/Models/ChatMessage.php
Die Datei "app/Models/ChatMessage.php" enthält eine PHP-Klasse, die das Modell für den Eloquent-ORM (Laravel) darstellt. Die Klasse erweitert die Basisklasse "Illuminate\Database\Eloquent\Model" und definiert eine Tabelle namens "chat_messages".

Die wichtigsten Funktionen der Datei sind:

* Die Verwendung des Traits "HasUuids" von Laravel, um UUIDs für die Modell-Instanzen zu generieren.
* Die Definition einer Eloquent-Relation zwischen dem Modell "ChatMessage" und dem Modell "User", um die Beziehung zwischen einem Chat-Nachrichtenmeldung und einem Benutzer darzustellen.
* Die Definition einer Eloquent-Relation zwischen dem Modell "ChatMessage" und dem Modell "Webhook", um die Beziehung zwischen einer Chat-Nachricht und einem Webhook darzustellen.
* Die Verwendung der Funktion "belongsTo()" von Laravel, um eine Beziehung zwischen zwei Modellen herzustellen.

Die Datei importiert außerdem die Klasse "User" aus dem Namespace "App\Models".

Insgesamt beschreibt die Datei "app/Models/ChatMessage.php" ein Modell für Chat-Nachrichtenmeldungen in einer Laravel-Anwendung.

---

### app/Models/Consent.php
Die Datei `app/Models/Consent.php` definiert eine Klasse `Consent`, die als Modell für die Datenbanktabelle "consents" fungiert. Diese Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und verwendet die Eloquent-ORM-Komponente von Laravel, um die Model-Methoden zu definieren.

Die wichtigsten Funktionen dieser Datei sind:

* Die Definition der Klasse `Consent` mit den benötigten Eigenschaften und Methoden für die Datenbankinteraktion.
* Die Verwendung des Eloquent-ORM-Modells, um die Datenbanktabelle "consents" zu modellieren.
* Die Verwendung von Laravel-Funktionen wie z.B. `fillable` und `casts`, um das Validieren und Umwandeln von Daten für die Datenbank zu vereinfachen.
* Die Definition der Beziehung zwischen der Klasse `Consent` und der Klasse `User` über den Eloquent-Relationship-Mechanismus.

Die Datei benutzt außerdem die Laravel-Komponente "Illuminate\Database\Eloquent\Model" und importiert die benötigten Abhängigkeiten, um die Datenbankinteraktion durchzuführen.

---

### app/Models/Contact.php
This file contains the Contact class, which is a model used to interact with the contacts table in the database.

---

### app/Models/PageView.php
Datei: app/Models/PageView.php
Kategorie: data_access

* Diese Datei enthält eine PHP-Klasse namens "PageView", die von Eloquent-Modell abgeleitet ist und es ermöglicht, Seitenansichten in einer Datenbank zu speichern.
* Die Klasse hat vier Felder: "path" (Pfad der Seite), "ip_anonymous" (IP-Adresse des Benutzers), "user_agent" (Benutzer-Agent) und "visits" (Anzahl der Seitenansichten).
* Die Klasse importiert den "Illuminate\Database\Eloquent\Model"-Namespace, um die Fähigkeit zum Speichern von Daten in einer Datenbank zu haben.
* Es gibt keine externe Abhängigkeiten, da diese Klasse nur die Funktion hat, Daten in einer Datenbank zu speichern und zu lesen.

---

### app/Models/User.php
* This file contains a PHP class that defines a user model for an application. The class is part of the Laravel framework and uses various traits to provide additional functionality.
* The class extends the `Authenticatable` class from the `Illuminate\Foundation\Auth\User` namespace, which provides basic authentication capabilities.
* The `HasFactory` trait is used to define a factory for the user model, which allows for the creation of new users using the `create()` method.
* The `HasRoles` trait is used to provide role-based authorization functionality.
* The `LogsActivity` trait is used to log changes to the user model in a activity log.
* The `Notifiable` trait is used to allow the user to receive notifications, such as password resets.
* The `TwoFactorAuthenticatable` trait is used to provide two-factor authentication functionality.
* The class has several attributes that are mass assignable, including `name`, `email`, and `password`. These attributes are assigned using the `$fillable` property.
* The class also has several hidden attributes, such as `password`, `two_factor_secret`, and `two_factor_recovery_codes`, which are not serialized when the object is converted to an array or JSON.
* The `casts()` method is used to define casting rules for the model. In this case, the `email_verified_at` attribute is cast as a datetime, and the `password` attribute is cast as a hashed value.
* The class has a method called `initials()`, which returns the initials of the user's name. This method uses the `Str::of()` function to split the name into an array of words, take the first two words, and create a new string with the initial letters of each word.
* The class also implements the `Loggable` interface, which allows for logging changes to the model using the activity log. The `getActivitylogOptions()` method is used to define which attributes should be logged, as well as whether only dirty attributes should be logged or all attributes.
* The class has two methods called `isActive()` and `isTrial()`, which return boolean values indicating whether the user's status is active or on trial. These methods use the `status` attribute to determine the current status of the user.

---

### app/Models/Webhook.php
Die Datei `app/Models/Webhook.php` enthält eine Klasse für die Behandlung von Webhooks in einer Anwendung. Die Klasse erweitert die Eloquent-Model-Klasse und hat folgende Funktionen:

* Eine Konstante `FRONTEND_OBJECTS`, die eine Liste der bekannten Frontend-Objekte mit 1:1 Zuordnung pro User enthält.
* Einige Felder, die den Namen, die Slug, die URL und das Frontend-Objekt eines Webhooks darstellen.
* Methoden `callUrl()` und `forUser()`, die die Url des Webhooks und den konfigurierten Webhook für ein bestimmtes Frontend-Objekt für einen bestimmten User finden.
* Relationen zur Klasse `User` und zur Klasse `ChatMessage`, um eine 1:n-Beziehung zwischen Webhooks und Nutzern und zwischen Webhooks und Chat-Nachrichten zu modellieren.

Die Datei verwendet auch die Traits `HasUuids` und `SoftDeletes` von Laravel, um die ID des Webhooks als UUID zu speichern und das Löschen von Webhooks zu erlauben.

Die Klasse `Webhook` ist für die Behandlung der Konfiguration und des Aufrufs von Webhooks in einer Anwendung zuständig. Sie bietet eine Möglichkeit, den konfigurierten Webhook für ein bestimmtes Frontend-Objekt und einen bestimmten User zu finden, und ermöglicht auch die Verbindung zwischen Webhooks und Nutzern oder Chat-Nachrichten.

---

### app/Models/Recherche/P1Komponente.php
This file defines a Eloquent model for the P1Komponente class in the Recherche namespace. The model represents a component in the P1 project and has relationships with other models, such as the Projekt model.

---

### app/Models/Recherche/P1Kriterium.php
This is a PHP class file for the `P1Kriterium` model in an application. The class extends the `Model` class provided by Laravel and defines some properties and methods for working with the `p1_kriterien` table in the database.

Here's a breakdown of the contents of the file:

* `use Illuminate\Database\Eloquent\Concerns\HasUuids;` - This line imports the `HasUuids` trait, which provides functionality for generating UUIDs (Universally Unique Identifiers) for the model.
* `use Illuminate\Database\Eloquent\Model;` - This line imports the `Model` class provided by Laravel.
* `use Illuminate\Database\Eloquent\Relations\BelongsTo;` - This line imports the `BelongsTo` relationship, which is used to define a one-to-many relationship between two models.
* `class P1Kriterium extends Model` - This line defines the `P1Kriterium` class that extends the `Model` class provided by Laravel.
* `protected $table = 'p1_kriterien';` - This line sets the name of the table that the model will use for storing data.
* `public $timestamps = false;` - This line disables the automatic management of timestamps for the model, which are typically used to track when a record was created or updated.
* `protected $fillable = [ 'projekt_id', 'kriterium_typ', 'beschreibung', 'begruendung', 'quellbezug'];` - This line defines the attributes that can be mass-assigned to the model.
* `public function projekt(): BelongsTo { return $this->belongsTo(Projekt::class, 'projekt_id'); }` - This line defines a relationship between the `P1Kriterium` and `Projekt` models, where each `P1Kriterium` can belong to one `Projekt`. The method returns a `BelongsTo` relationship, which allows you to access the related `Projekt` model.

In summary, this class provides a way to interact with the `p1_kriterien` table in the database and defines some relationships between the `P1Kriterium` and `Projekt` models.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Diese Datei definiert eine Klasse "P1Strukturmodellwahl" in der Modell-Kategorie "data_access". Sie ist für den Zugriff auf die Datenbanktabelle "p1_strukturmodell_wahl" zuständig und enthält Eigenschaften wie das Modell, die Wahl und den Grund.

---

### app/Models/Recherche/P1Warnsignal.php
This PHP file defines a Eloquent model for the 'p1_warnsignale' table in the database. The model contains fields and methods to interact with the table, including accessing the related 'projekt' model.

---

### app/Models/Recherche/P2Cluster.php
This file is a PHP class that represents the P2Cluster model in the Laravel framework. It extends the Model class and defines properties and methods for accessing and manipulating data related to P2 clusters.

The class uses the HasUuids trait to generate UUIDs for the cluster_id field. This allows for more efficient and flexible handling of UUIDs in the database.

The protected $table property specifies the name of the table used by this model. The public $timestamps property is set to false, indicating that the model does not have timestamps.

The $fillable array specifies which properties are mass assignable, meaning they can be assigned values during model creation or updates. In this case, the cluster_id, cluster_label, beschreibung, treffer_schaetzung, and relevanz fields are defined as fillable.

The $casts property defines casting rules for the treffer_schaetzung field. This allows for more efficient handling of integers in the database.

Finally, the projekt() method is defined to return a BelongsTo relationship with the Projekt model. This allows for easy access to the project associated with this cluster.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Die Datei "app/Models/Recherche/P2MappingSuchstringKomponente.php" enthält eine Klasse für das Modell von Suchbegriffen in einem Projekt. Die Klasse erweitert die `Illuminate\Database\Eloquent\Model`-Klasse und definiert die Eigenschaften und Beziehungen des Modells.

* Die `$table`-Variable legt den Namen der Tabelle im Datenbankschema fest, in dem das Modell gespeichert wird.
* Der `$timestamps` Wert ist auf `false` gesetzt, da die Tabelle keine Timestamp-Spalten enthält.
* Die `$fillable`-Variable definiert die Spalten, die bei der Erstellung oder Aktualisierung des Modells automatisch gefüllt werden dürfen.
* Die `$casts`-Variable definiert die Datentypen der Spalten und konvertiert sie für den Zugriff auf die Daten in der Tabelle.
* Die `projekt()`-Methode definiert eine Beziehung zwischen dem Modell und der Tabelle "projekte" und gibt an, dass das Modell einen Eintrag in der Tabelle "projekte" verknüpft.

Die Datei enthält auch den namespace `App\Models\Recherche`, der angibt, dass die Klasse für das Modell in einem Ordner namens "Recherche" innerhalb des Namespace `App\Models` liegt.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
*This file contains a PHP class for managing reviews of project proposals. It extends the Eloquent Model class and uses the HasUuids trait to enable UUIDs for the ID column. The table name is specified as 'p2_review_typ_entscheidung'. Timestamps are not used in this model.*

*The class has four fillable properties: 'projekt_id', 'review_typ', 'passt', and 'begruendung'. The 'passt' property is cast to a boolean type. There is also a relationship defined between the P2ReviewTypEntscheidung model and the Projekt model, which allows for retrieving the project associated with a review.*

*The output format is in JSON, with a single key-value pair: "file_summary" and its value is a brief description of the file's functions. In this case, the summary is that the file contains an Eloquent model class for managing reviews of project proposals.*

---

### app/Models/Recherche/P2Trefferliste.php
Die Datei `app/Models/Recherche/P2Trefferliste.php` ist eine Eloquent-Model-Klasse in der Laravel-Anwendung und enthält Methoden zur Verarbeitung von Daten in der `p2_trefferlisten`-Datenbanktabelle.

Die Klasse erweitert die `Illuminate\Database\Eloquent\Model` -Klasse und verwendet das `HasUuids` -Trait, um UUIDs für die `id` -Spalte der Tabelle zu generieren.

Die Tabelle `p2_trefferlisten` ist eine Datenbanktabelle, die von der Anwendung verwendet wird, um Suchergebnisse in einer Datenbank zu speichern. Die Klasse definiert die Struktur der Tabellenspalten und stellt Methoden zur Verarbeitung dieser Spalten bereit.

Die `projekt_id` -Spalte bezieht sich auf eine andere Tabelle in der Datenbank, die Projektinformationen enthält. Die `datenbank`-Spalte enthält den Namen der Datenbank, in der die Suche durchgeführt wurde.

Die `suchstring`-Spalte enthält den Suchbegriff, der für die Suche verwendet wird. Die `treffer_gesamt`-Spalte enthält die Gesamtanzahl der Treffer für den angegebenen Suchbegriff. Die `einschaetzung`-Spalte enthält eine Bewertung des Treffergebnisses und kann leer sein.

Die `anpassung_notwendig`-Spalte ist ein boolesches Flag, das angibt, ob die Suche weitere Optimierungen erfordert. Die `suchdatum`-Spalte enthält das Datum, an dem die Suche durchgeführt wurde.

Die Klasse definiert auch eine Beziehung zu einer anderen Tabelle in der Datenbank mithilfe der `projekt()`-Methode, die eine `BelongsTo` -Beziehung zur `Projekt`-Tabelle herstellt.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Die Datei `app/Models/Recherche/P3Datenbankmatrix.php` definiert eine Eloquent-Model-Klasse für das Speichern von Daten in der `p3_datenbankmatrix`-Tabelle der Datenbank. Diese Klasse erweitert die `Illuminate\Database\Eloquent\Model`-Klasse und verwendet den Trait `HasUuids`, um UUIDs (Universally Unique Identifiers) zu generieren und zu verwenden, wenn neue Datensätze in der Tabelle hinzugefügt werden.

Die Klasse definiert auch einige wichtige Attribute und Methoden:

* `table`: Der Name der Tabelle in der Datenbank, in der die Daten gespeichert werden sollen.
* `timestamps`: Ein boolescher Wert, der angibt, ob für die Datensätze Zeitstempel (Timestamp) gesetzt werden sollten. In diesem Fall ist dies auf `false` festgelegt, da es keine Zeitstempel in der Tabelle gibt.
* `fillable`: Eine Liste von Spaltennamen, die für das Speichern von Daten in der Tabelle verwendet werden dürfen. Hier sind alle Spaltennames mit Ausnahme der `id`-Spalte (die automatisch vom Eloquent-Framework generiert wird) aufgelistet.
* `casts`: Eine Liste von Spaltennamen und ihren entsprechenden Typen, die für das Speichern von Daten in der Tabelle verwendet werden dürfen. Hier ist der `empfohlen`-Spalte ein boolescher Wert zugewiesen.
* `projekt(): BelongsTo`: Eine Beziehung zur `Projekt`-Model-Klasse, die definiert, dass jede `P3Datenbankmatrix`-Instanz zu einer einzelnen `Projekt`-Instanz gehört.

Insgesamt entspricht die Klasse einer Model-Klasse in Laravel und dient dazu, Daten in der `p3_datenbankmatrix`-Tabelle der Datenbank zu speichern und zu verwalten.

---

### app/Models/Recherche/P3Disziplin.php
This file contains the P3Disziplin model class for accessing the p3_disziplinen table in the database. It uses the Eloquent ORM to interact with the table and defines relationships with other models.

---

### app/Models/Recherche/P3GeografischerFilter.php
Die Datei `app/Models/Recherche/P3GeografischerFilter.php` enthält eine Klasse namens `P3GeografischerFilter`, die ein Modell für die Tabelle `p3_geografische_filter` in der Datenbank ist. Die Klasse erweitert die Basisklasse `Model` aus Laravel und verwendet den Trait `HasUuids`, um UUID-Werte für die ID-Spalte zu verwenden.

Die Tabelle `p3_geografische_filter` enthält Filterdaten für ein Projekt, das in der Datenbank gespeichert ist. Die Klasse `P3GeografischerFilter` definiert einige Eigenschaften und Methoden, die für die Arbeit mit den Daten in dieser Tabelle nützlich sind.

Die wichtigsten Funktionen dieser Datei sind:

* Die Definition einer Klasse namens `P3GeografischerFilter`, die eine Verbindung zur Datenbank herstellt und die Filterdaten für ein bestimmtes Projekt speichert.
* Der Zugriff auf die Datenbanktabellen `p3_geografische_filter` und `projekte`, um Daten zu lesen, zu schreiben oder zu aktualisieren.
* Die Definition von Eigenschaften und Methoden, die es ermöglichen, Filterdaten für ein Projekt zu speichern, zu lesen und zu aktualisieren.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* `P3GeografischerFilter`: Die Klasse, die das Modell für die Tabelle `p3_geografische_filter` definiert.
* `Projekt`: Die Klasse, die das Modell für die Tabelle `projekte` definiert.
* `/api/v1/filters/create`: Der Endpunkt, der verwendet wird, um Filterdaten für ein bestimmtes Projekt zu erstellen oder zu aktualisieren.
* `/api/v1/filters/read`: Der Endpunkt, der verwendet wird, um Filterdaten für ein bestimmtes Projekt zu lesen.
* `/api/v1/filters/update`: Der Endpunkt, der verwendet wird, um Filterdaten für ein bestimmtes Projekt zu aktualisieren.

Die wichtigsten外部依存関係 sind:

* `Illuminate\Database\Eloquent\Model`: Die Basisklasse für alle Eloquent-Modelle in Laravel.
* `HasUuids`: Der Trait, der verwendet wird, um UUID-Werte für die ID-Spalte zu verwenden.

---

### app/Models/Recherche/P3GraueLiteratur.php
This file defines a data model for P3GraueLiteratur in the Recherche category. It is a Laravel Eloquent Model that represents the p3_graue_literatur table in the database. The class uses the HasUuids trait, which provides UUID support for the model. The fillable properties of the model are projekt_id, quelle, typ, url, suchpfad, relevanz, and anmerkung. Additionally, the model has a belongsTo relationship with the Projekt class, which allows it to access the project that the literature belongs to.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Die Datei "app/Models/Recherche/P4Anpassungsprotokoll.php" enthält eine PHP-Klasse, die ein Modell für den Eloquent-ORM in Laravel darstellt und die Datenbanktabellen "p4_anpassungsprotokoll" verknüpft.

* Die Klasse erbt von der `Illuminate\Database\Eloquent\Model`-Klasse und verwendet das Trait `HasUuids` um UUIDs zu generieren.
* Die Klasse definiert eine Datenbanktabelle namens "p4_anpassungsprotokoll" und enthält folgende Eigenschaften:
	+ "suchstring_id": Eine ID, die eine Beziehung mit der Tabelle "p4_suchstring" herstellt.
	+ "version": Die Version des Suchstrings.
	+ "datum": Das Datum der Anpassung.
	+ "aenderung": Die Art der Anpassung (z.B. "Neuer Filter").
	+ "grund": Der Grund für die Anpassung (z.B. "Es gibt mehr Treffer").
	+ "treffer_vorher": Die Anzahl der Suchergebnisse vor der Anpassung.
	+ "treffer_nachher": Die Anzahl der Suchergebnisse nach der Anpassung.
	+ "entscheidung": Die Entscheidung über die Anpassung (z.B. "Akzeptiert").
* Die Klasse definiert auch eine Beziehung zwischen dem Modell und einer anderen Tabelle namens "p4_suchstring" über die Eigenschaft "suchstring".

Die Datei importiert auch die `Illuminate\Database\Eloquent\Concerns\HasUuids`-Klasse, um UUIDs zu generieren. Die Klasse verwendet auch das Laravel-Trait `BelongsTo`, um eine Beziehung mit der Tabelle "p4_suchstring" herzustellen.

---

### app/Models/Recherche/P4Suchstring.php
Die Datei `app/Models/Recherche/P4Suchstring.php` enthält eine Klasse namens `P4Suchstring`, die eine Datenmodell-Klasse für Suchbegriffe in der Suche ist. Sie definiert die Eigenschaften und Methoden, die von dieser Klasse benötigt werden, um Suchbegriffe zu verwalten und zu speichern.

Die Klasse erbt von `Illuminate\Database\Eloquent\Model` und verwendet das Trait `HasUuids`, um UUIDs (Universally Unique Identifiers) für die Objekte dieser Klasse zu generieren. Sie definiert auch eine Tabelle mit dem Namen `p4_suchstrings`, in der Suchbegriffe gespeichert sind.

Die Klasse hat folgende Eigenschaften und Methoden:

* Die Eigenschaft `$table` enthält den Namen der Datenbanktabelle, in der die Suchbegriffe gespeichert werden.
* Die Eigenschaft `$timestamps` ist auf `false` festgelegt, um zu verhindern, dass Laravel automatische Timestamp-Felder für die Objekte dieser Klasse erstellt.
* Die Eigenschaft `$fillable` definiert, welche Eigenschaften von der Klasse automatisch gefüllt werden dürfen, wenn neue Objekte erstellt werden. In diesem Fall sind dies alle Spalten in der Datenbanktabelle `p4_suchstrings`, mit Ausnahme des Feldes `id`.
* Die Eigenschaft `$casts` definiert, wie die Werte von bestimmten Feldern vor der Verwendung in der Anwendung interpretiert werden sollten. In diesem Fall sind dies die Felder `gesetzte_filter`, `treffer_anzahl` und `suchdatum`.
* Die Methode `projekt()` definiert eine Beziehung zwischen `P4Suchstring`-Objekten und `Projekt`-Objekten. Sie gibt an, dass ein Suchbegriff zu einem bestimmten Projekt gehört.
* Die Methode `anpassungsprotokoll()` definiert eine Beziehung zwischen `P4Suchstring`-Objekten und `P4Anpassungsprotokoll`-Objekten. Sie gibt an, dass ein Suchbegriff zu einem bestimmten Anpassungsprotokoll gehört.
* Die Methode `suchprotokolle()` definiert eine Beziehung zwischen `P4Suchstring`-Objekten und `P8Suchprotokoll`-Objekten. Sie gibt an, dass ein Suchbegriff zu einem bestimmten Suchprotokoll gehört.

Insgesamt definiert diese Klasse einen Datenmodell-Typ für Suchbegriffe in der Suche, der die Fähigkeit hat, Suchbegriffe zu verwalten und zu speichern, sowie Beziehungen zwischen Suchbegriffen und anderen Objekten in der Anwendung.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Die Datei `app/Models/Recherche/P4ThesaurusMapping.php` ist eine PHP-Klasse, die das Modell für die Beziehung zwischen einem Projekt und den Mapping-Daten einer Thesaurus-Datei darstellt.

* Die Klasse enthält die folgenden wichtigen Funktionen:
	+ `HasUuids`: Diese Trait-Klasse stellt eine Möglichkeit dar, UUIDs (Universally Unique Identifiers) für Eloquent-Modelle zu verwenden, um sie eindeutig und reproduzierbar zu machen.
	+ `Model`: Diese Klasse ist die Basisklasse aller Modelle in der Laravel-Anwendung und stellt eine abstrakte Möglichkeit dar, Daten aus einer Datenbank oder einem anderen Datenspeicher abzurufen.
	+ `BelongsTo`: Diese Funktion gibt an, dass das Modell eine Beziehung zu einem anderen Modell hat, d.h. es ist ein "Belonging"-Modell. In diesem Fall bezieht sich die Beziehung auf das Projekt, das dem Mapping-Datensatz zugeordnet ist.
* Die Klasse enthält auch einige wichtige Eigenschaften:
	+ `table`: Diese Eigenschaft gibt den Namen der Tabelle an, in der die Daten des Modells gespeichert sind. In diesem Fall lautet der Name `p4_thesaurus_mapping`.
	+ `timestamps`: Diese Eigenschaft steuert, ob für das Modell Zeitstempel erstellt werden sollen. In diesem Fall ist dies nicht erforderlich, da die Mapping-Daten keine Zeitangaben enthalten.
	+ `fillable`: Diese Eigenschaft gibt an, welche Eigenschaften des Modells gefüllt werden dürfen. In diesem Fall sind dies die folgenden Eigenschaften: `projekt_id`, `freitext_de`, `freitext_en`, `mesh_term`, `emtree_term`, `psycinfo_term` und `anmerkung`.
* Die Klasse hat auch einen Konstruktor, der das Modell mit den Standard-Einstellungen initialisiert.

Die wichtigsten Methoden in dieser Klasse sind die Funktionen für die Beziehungen zu anderen Modellen: `projekt()`, die eine Beziehung zwischen dem Mapping-Modell und einem Projekt darstellt.

---

### app/Models/Recherche/P5PrismaZahlen.php
Die Datei `app/Models/Recherche/P5PrismaZahlen.php` ist eine Modell-Klasse in der Laravel-Anwendung. Sie enthält verschiedene Funktionen und Eigenschaften, die es erlauben, mit der Datenbank zu interagieren und Informationen darüber abzufragen. Hier sind die wichtigsten Funktionen und Eigenschaften dieser Datei:

* `namespace App\Models\Recherche`: Die Klasse befindet sich im Namespace `App\Models\Recherche`.
* `use Illuminate\Database\Eloquent\Concerns\HasUuids;`: Diese Anweisung importiert das Trait `HasUuids` aus der Laravel-Bibliothek. Dies ermöglicht es, UUIDs (Universally Unique Identifiers) in der Klasse zu verwenden.
* `use Illuminate\Database\Eloquent\Model;`: Diese Anweisung importiert die Basisklasse `Model` aus der Laravel-Bibliothek. Sie ist eine Oberklasse für alle Eloquent-Modelle und erbt von ihr.
* `class P5PrismaZahlen extends Model`: Die Klasse `P5PrismaZahlen` erbt von der Basisklasse `Model`. Sie ist eine eigene Modell-Klasse, die speziell für den Zugriff auf die Tabelle `p5_prisma_zahlen` in der Datenbank optimiert wurde.
* `protected $table = 'p5_prisma_zahlen';`: Diese Variable gibt an, dass die Klasse mit der Tabelle `p5_prisma_zahlen` verbunden ist.
* `public $timestamps = false;`: Diese Eigenschaft deaktiviert das automatische Setzen von Timestamp-Werten für das Modell (Erstellungs- und Update-Zeitstempel).
* `protected $fillable = [...];`: Diese Variable speichert eine Liste von Spaltennamen, die in der Datenbank aktualisiert werden dürfen.
* `protected $casts = [...];`: Diese Variable ermöglicht das Konvertieren bestimmter Datentypen in andere während des Modells. In diesem Fall wird das Timestamp-Feld (`aktualisiert_am`) in ein DateTime-Objekt konvertiert.
* `public function projekt(): BelongsTo;`: Diese Methode definiert eine Beziehung zwischen der Klasse und dem `Projekt`-Modell. Sie sagt aus, dass ein `P5PrismaZahlen`-Objekt einem `Projekt`-Objekt zugeordnet werden kann.

Insgesamt ist die Datei `app/Models/Recherche/P5PrismaZahlen.php` eine Modell-Klasse, die es ermöglicht, mit der Datenbank zu interagieren und Informationen darüber abzufragen. Sie enthält auch Funktionen und Eigenschaften, die es erlauben, die Tabelle `p5_prisma_zahlen` effizient zu verarbeiten und darauf zuzugreifen.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
This file defines a model class for P5ScreeningEntscheidung in the App\Models\Recherche namespace. The class represents a decision made during a p5 screening process and includes properties such as the level of the decision, the entitlement, and the reviewer.

---

### app/Models/Recherche/P5ScreeningKriterium.php
* Die Datei "P5ScreeningKriterium.php" in der App-Datenbank enthält eine Klasse für das Modell "P5ScreeningKriterium".
* Die Klasse erweitert die Basisklasse "Model" aus dem Paket "Illuminate\Database\Eloquent\Concerns".
* Die Klasse verwendet die Trait-Klasse "HasUuids" um UUIDs für die Objekte der Klasse zu generieren.
* Die Tabelle, in der sich die Daten des Modells befinden, wird über das Attribut "$table" definiert und ist "p5_screening_kriterien".
* Die Eigenschaft "$timestamps" ist auf "false" gesetzt, um anzugeben, dass es keine Zeitstempel in der Tabelle gibt.
* Die Eigenschaft "$fillable" definiert, welche Attribute des Objekts als "füllbar" markiert sind und daher bei der Massen-Assignment-Methode verwendet werden können. In diesem Fall sind dies die Felder "projekt_id", "level", "kriterium_typ", "beschreibung" und "beispiel".
* Die Methode "projekt()" ist ein Relationship-Method, das eine BelongTo-Beziehung zwischen dem Modell "P5ScreeningKriterium" und dem Modell "Projekt" definiert. Sie gibt an, dass ein P5ScreeningKriterium zu einem Projekt gehört.
* Die Datei importiert auch das Paket "Illuminate\Database\Eloquent\Model", welches die Basisklasse für alle Eloquent-Modelle in Laravel darstellt.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Die Datei app/Models/Recherche/P5ToolEntscheidung.php enthält eine Klasse namens P5ToolEntscheidung, die als Model für die Tabelle p5_tool_entscheidung in der Datenbank fungiert. Die Klasse verwendet die Illuminate\Database\Eloquent\Model-Klasse und erweitert sie um die Funktionen von HasUuids.

---

### app/Models/Recherche/P5Treffer.php
The `app/Models/Recherche/P5Treffer.php` file contains a PHP class that represents a model for the `p5_treffer` table in the database.

Here is a summary of the main functionalities and external dependencies of this file:

* The `P5Treffer` class extends the `Model` class provided by Laravel and uses the `HasUuids` trait to generate UUIDs for its ID field.
* The class has several properties that are defined in the `$fillable` array, including `projekt_id`, `record_id`, `titel`, `autoren`, `jahr`, `journal`, `doi`, `abstract`, `datenbank_quelle`, `ist_duplikat`, and `duplikat_von`. These properties are used to store data for the corresponding fields in the `p5_treffer` table.
* The class also has several methods that define relationships between the `P5Treffer` model and other models in the application, including `projekt()`, `duplikatOriginal()`, `duplikate()`, `screeningEntscheidungen()`, `qualitaetsbewertung()`, and `datenextraktion()`. These methods are used to establish relationships between instances of the `P5Treffer` class and instances of other classes in the application.
* The file imports several external dependencies, including the `Illuminate\Database\Eloquent\Model` class, the `HasUuids` trait, and the `Projekt` model. These dependencies are used to provide the basic functionality for working with Eloquent models in Laravel.
* The file also uses the `casts` property to specify that certain fields should be treated as integers or dates when they are retrieved from the database. This is done by defining a `$casts` array in the class that maps the column names to the corresponding cast types.

---

### app/Models/Recherche/P6Luckenanalyse.php
Die Datei `app/Models/Recherche/P6Luckenanalyse.php` enthält eine Klasse namens `P6Luckenanalyse`, die eine Eloquent-Model-Klasse in Laravel ist.

Die Klasse definiert ein Modell für den Zugriff auf die Tabelle `p6_luckenanalyse` in der Datenbank und verwendet die Traits `HasUuids` und `Timestamp`.

Die Klasse hat folgende Eigenschaften:

* `$table`: Der Name der Tabelle, die das Modell darstellt. In diesem Fall ist es `p6_luckenanalyse`.
* `$timestamps`: Gibt an, ob dem Modell automatische Zeitstempel hinzugefügt werden sollen (create_at und update_at). Hier wird dies deaktiviert.
* `$fillable`: Eine Liste der Felder, die in der Tabelle enthalten sind und auf die zugegriffen werden darf. In diesem Fall sind es `projekt_id`, `fehlender_aspekt`, `fehlender_studientyp`, `moegliche_konsequenz` und `empfehlung`.

Die Klasse definiert auch eine Beziehung zu einem anderen Modell namens `Projekt`, das mithilfe des Relationships `belongsTo` definiert ist. In diesem Fall gehört ein P6Luckenanalyse-Modell zu einem Projekt, und die ID des Projekts wird im Feld `projekt_id` gespeichert.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Die Datei `app/Models/Recherche/P6Qualitaetsbewertung.php` in Laravel ist eine Klasse, die ein Modell für die Tabelle `p6_qualitaetsbewertung` im Datenbankschema darstellt. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model`, die von Laravel bereitgestellt wird, und verwendet dabei das Trait `HasUuids`.

Die Klasse definiert eine Reihe von Eigenschaften und Methoden, die es ermöglichen, mit der Tabelle zu interagieren. Hier sind einige wichtige Aspekte:

* Die Tabelle wird durch den Attribut `$table` festgelegt.
* Der Timestamp-Zugriff ist deaktiviert, da die Tabelle keine Timestamps hat.
* Einige Eigenschaften wie `treffer_id`, `studientyp`, `rob_tool` usw. können auf der Tabelle gesetzt werden.
* Es gibt eine Beziehung zwischen der Klasse und der Tabelle `p5_treffer`, die durch die Methode `treffer()` definiert wird. Diese Beziehung ist vom Typ `BelongsTo`.
* Die Eigenschaften `im_review_behalten` und `anmerkung` können als boolean-Werte interpretiert werden, da das Trait `HasUuids` verwendet wird, um diese Felder als UUIDs zu interpretieren.
* Die Eigenschaft `bewertet_am` kann als Datum interpretiert werden, da das Trait `HasUuids` verwendet wird, um diese Felder als UUIDs zu interpretieren.

---

### app/Models/Recherche/P7Datenextraktion.php
Die Datei `app/Models/Recherche/P7Datenextraktion.php` enthält eine Klasse namens `P7Datenextraktion`, die ein Modell für die Tabelle `p7_datenextraktion` in der Datenbank darstellt. Die Klasse erbt von `Illuminate\Database\Eloquent\Model` und verwendet die Traits `HasUuids`.

Die wichtigsten Funktionen dieser Klasse sind:

* Das Modell wird mit den Feldern `id`, `treffer_id`, `land`, `stichprobe_kontext`, `phaenomen_intervention`, `outcome_ergebnis`, `hauptbefund`, `qualitaetsurteil` und `anmerkung` definiert.
* Die Klasse enthält eine Beziehung zwischen der Tabelle `p7_datenextraktion` und der Tabelle `p5_treffer` über die Felder `id` und `treffer_id`.
* Die Klasse verwendet die Traits `HasUuids`, um UUIDs für die Felder `id` zu generieren.
* Die Klasse definiert keine Methoden oder Funktionen, da sie nur als Modell fungiert.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Die Datei `app/Models/Recherche/P7GradeEinschaetzung.php` enthält eine Klasse, die die Funktionalität eines Modells im Laravel-Framework darstellt. Die Klasse ist Teil des Moduls "Recherche" und dient zur Speicherung von Daten in einer Datenbanktabellennamed `p7_grade_einschaetzung`.

Die Klasse verwendet das Trait `HasUuids`, das es ermöglicht, UUID-Werte für die IDs der Datensätze zu generieren und zu verwenden. Der Modul hat auch eine Beziehung zum Projektmodell durch die Verwendung der `belongsTo`-Methode.

Die Klasse definiert mehrere Attribute, darunter die Namen der Tabellenspalten, die Felder, die als "fillable" markiert sind, sowie die Cast-Regeln für die Datentypen von bestimmten Spalten. Die Beziehung zum Projektmodul wird durch die Verwendung der `projekt_id`-Spalte definiert, die als Fremdschlüssel verwendet wird.

Insgesamt ist diese Klasse eine wichtige Komponente des Moduls "Recherche" und ermöglicht es, Daten in einer Datenbanktabellen zu speichern und zu verwalten.

---

### app/Models/Recherche/P7MusterKonsistenz.php
This file defines a PHP class named `P7MusterKonsistenz` that extends the Eloquent ORM model. It is located in the `app/Models/Recherche` directory and has the namespace `App\Models\Recherche`.

The class uses the `HasUuids` trait, which allows it to generate UUIDs for its primary key. The table name is set to `p7_muster_konsistenz`. Timestamps are disabled for this model.

The `$fillable` property specifies the fields that can be mass-assigned when creating or updating records in the database. The `unterstuetzende_quellen`, `widersprechende_quellen`, and `moegliche_erklaerung` fields are set to arrays, which means they can hold multiple values.

The `projekt()` method returns a relationship with the `Projekt` class, indicating that a record in this table belongs to a record in the `Projekt` table. The `belongsTo` method is used to define this relationship.

---

### app/Models/Recherche/P7SyntheseMethode.php
Die Datei enthält eine Klasse namens P7SyntheseMethode, die in der Illuminate\Database\Eloquent\Model-Klasse von Laravel abgeleitet ist und dem Modell eines Eloquent-Framework-Modells zugeordnet ist. Die Klasse hat einen UUID-Index, um eindeutige Datensätze darzustellen. Der Tabellenname der Klasse lautet p7_synthese_methode. Es gibt eine Beziehung zwischen den Modellen P7SyntheseMethode und Projekt, die durch das belongsTo-Methodenobjekt definiert wird.

---

### app/Models/Recherche/P8Limitation.php
This PHP file defines a model class for the P8Limitation table in the database. The class is used to interact with the table and retrieve data from it.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
* Die Datei `app/Models/Recherche/P8Reproduzierbarkeitspruefung.php` ist eine Eloquent-Model-Klasse in Laravel, die den Zugriff auf die Tabelle `p8_reproduzierbarkeitspruefung` im Datenbankmodul ermöglicht.
* Die Klasse benutzt die Trait-Funktion `HasUuids`, um UUIDs anstelle von Auto-Inkrement-IDs zu verwenden.
* Die Tabelle hat eine Beziehung zur `Projekt`-Tabelle über die Foreign Key `projekt_id`. Diese wird mit dem Eloquent-Relationship-Method `belongsTo` definiert.
* Die Klasse hat auch eine Reihe von Attributen und Methoden, die es ermöglichen, die Daten in der Tabelle zu manipulieren und zu lesen.
* Es gibt 3 Attribute: `projekt_id`, `pruefpunkt` und `erfuellt`. `pruefpunkt` ist ein Enum-Typ, der den Prüfpunkt darstellt, auf dem die Reproduzierbarkeitsprüfung durchgeführt wurde. `erfuellt` ist ein Boolescher Wert, der angibt, ob die Prüfung erfolgreich abgeschlossen wurde.
* Es gibt auch eine Cast-Methode, um den Wert von `erfuellt` zu einem booleschen Wert zu konvertieren.
* Die Klasse hat keine eigenen Methoden und Funktionen, sondern nutzt die Eloquent-ORM-Funktionalität zur Interaktion mit der Datenbank.

---

### app/Models/Recherche/P8Suchprotokoll.php
Die Datei "app/Models/Recherche/P8Suchprotokoll.php" enthält eine Klasse mit dem Namen "P8Suchprotokoll", die eine Datenmodell-Klasse für das Suchprotokoll ist. Die Klasse erweitert die Basisklasse "Illuminate\Database\Eloquent\Model" und verwendet die Trait-Methode "HasUuids" um UUIDs zu verwenden.

Die Klasse hat die folgenden Attribute:

* $table: enthält den Namen der Tabelle, in der die Suchprotokolle gespeichert sind (p8_suchprotokoll).
* $timestamps: ist auf "false" gesetzt, da die Tabelle keine Timestamps-Spalten hat.
* $fillable: enthält eine Liste von Spalten, die für das Einfügen und Aktualisieren von Daten in der Tabelle verwendet werden können.
* $casts: enthält eine Liste von Spalten, die als bestimmte Typen formatiert werden sollten (z.B. "date" für Datumsangaben).

Die Klasse definiert auch eine Beziehung zwischen sich selbst und der Klasse "App\Models\Recherche\P4Suchstring". Die Methode "suchstring()" gibt eine BelongsTo-Beziehung zurück, die es ermöglicht, Suchprotokolle zu einem bestimmten Suchstring zu verknüpfen.

---

### app/Models/Recherche/P8UpdatePlan.php
Die Datei `app/Models/Recherche/P8UpdatePlan.php` enthält eine PHP-Klasse für das Speichern und Verwalten von Update-Plänen in einer Datenbank. Die Klasse vererbt von der Eloquent-Model-Klasse in Laravel und verwendet das Trait `HasUuids`, um UUIDs zu generieren.

Die wichtigsten Funktionen der Klasse sind:

* Die Verbindung zur Datenbank über die Eigenschaft `$table`.
* Das Speichern von Update-Plänen mit dem `create`-Methoden der Eloquent-Model-Klasse.
* Das Lesen und Aktualisieren von Update-Plänen mit den `find`- und `update`-Methoden der Eloquent-Model-Klasse.
* Die Definition von Beziehungen zu anderen Tabellen über die Methoden `belongsTo` und `hasMany`.
* Die Verwendung des Traits `HasUuids`, um UUIDs zu generieren.

Die Klasse verwendet auch das Trait `HasCast`, um das Attribut `naechstes_update` als Datum zu interpretieren.

Die Klasse ist in der `App\Models\Recherche`-Namespace und hängt von den Laravel-Frameworks ab, insbesondere von Eloquent.

---

### app/Models/Recherche/PaperEmbedding.php
This file defines the PaperEmbedding model for the research project. It extends the Laravel Eloquent Model class and defines a custom primary key using the HasUuids trait. The model has a belongsTo relationship with the Projekt model, which is used to link this paper embedding to a specific project. The fields of the model are defined as fillable, which means they can be mass assigned. The metadata field is cast as an array, and the erstellt_am field is cast as a datetime object.

---

### app/Models/Recherche/Phase.php
File: app/Models/Recherche/Phase.php
Category: data_access

This file contains a PHP class named "Phase" that extends the Eloquent model from Laravel's framework. The class is defined in the "App\Models\Recherche" namespace and uses the "HasUuids" trait to generate unique IDs for the records in the "phasen" table.

The class has a few properties and methods:

* $table: Defines the name of the database table that the model is associated with, which is "phasen".
* $timestamps: Indicates whether the model should track changes to its timestamps, which is set to false in this case.
* $fillable: An array of columns that are mass assignable for the model, which includes all columns except for the "id" column.
* $casts: Defines how certain attributes should be casted, such as the "phase_nr" and "abgeschlossen_am" fields which are casted to integers and datetime respectively.
* projekt(): A method that defines a relationship between the Phase model and the Projekt model. This is a "belongsTo" relationship, where each phase belongs to one project. The method returns an instance of the "BelongsTo" class, which allows for querying and manipulating the related project data.

In total, this file defines a PHP class that represents a single record in the "phasen" table of the database and provides methods to interact with it.

---

### app/Models/Recherche/Projekt.php
Die Datei `app/Models/Recherche/Projekt.php` ist eine Klasse in einer Laravel-Anwendung, die die Modelle für das Projekt-Management system enthält.

* Die Klasse erweitert die `Model`-Klasse aus dem Framework und verwendet die Traits `HasFactory`, `HasUuids`, und `LogsActivity`.
* Die Tabelle des Modells ist `projekte` und hat keine Timestamps.
* Die Eigenschaften, die gefüllt werden können, sind: `user_id`, `titel`, `forschungsfrage`, `review_typ`, `verantwortlich`, `startdatum`, `notizen`.
* Es gibt verschiedene Beziehungen zwischen der Klasse und anderen Modellen in der Anwendung, wie zum Beispiel eine Beziehung zu den `User`-Modellen über die `user()`-Methode.
* Die Klasse verwendet auch die Trait `LogsActivity`, um Aktivitäten im Projekt zu protokollieren.
* Es gibt viele weitere Methoden und Eigenschaften in der Klasse, aber ich habe nur eine kurze Beschreibung von ihnen gegeben. Wenn du mehr Informationen benötigst, kannst du die Datei betrachten oder die Dokumentation der Laravel-Anwendung lesen.

---

### database/factories/UserFactory.php
File: database/factories/UserFactory.php
Category: data_access

This file contains a factory for the User model. The factory is used to create test data for the User model in the application's tests.

The factory defines several methods that are used to generate test data for the User model, including the `definition` method which returns an array of attributes for a new user record, and the `unverified` and `withoutTwoFactor` methods which modify the default state of the user record.

The file also imports several external dependencies, including the `Illuminate\Database\Eloquent\Factories\Factory` class from the Laravel framework, and the `Hash`, `Str`, and `fake` classes from the Laravel Faker package. These dependencies are used to generate test data for the User model.

The file does not contain any error-handling or validation logic, as it is intended solely for generating test data. The file's primary purpose is to provide a convenient way to create new user records in the application's tests.

---

### database/factories/Recherche/ProjektFactory.php
This file is part of a Laravel project and contains a factory class for the `Projekt` model in the `Recherche` namespace. The factory class is used to create test data for the project, specifically for the `Projekt` model.

The `Recherche\ProjektFactory` class extends the `Illuminate\Database\Eloquent\Factories\Factory` class and defines a `definition()` method that returns an array of attributes for a new instance of the `Projekt` model. The attributes include the `user_id`, which is generated using the `User::factory()` method, as well as the `titel` and `forschungsfrage`, which are both randomly generated using the `fake()->sentence()` and `fake()->paragraph()` methods from the Faker library.

The `Recherche\ProjektFactory` class is not directly related to any external dependencies, but it does import the `User::factory()` method from the `App\Models\User` model. This allows the factory to create new instances of the `Projekt` model with a random user ID.

---

### database/migrations/0001_01_01_000000_create_users_table.php
This file creates three tables in the database: `users`, `password_reset_tokens`, and `sessions`. The `users` table contains basic information about users, such as their name and email address. The `password_reset_tokens` table stores a token for each user's password reset request, which can be used to verify the user's identity. The `sessions` table stores data about the user's active sessions, including their IP address and user agent.

The file uses the Laravel Framework's migration system to create these tables. The `use Illuminate\Database\Migrations\Migration;` statement imports the Migration class from the Laravel framework. The `return new class extends Migration` line creates a new class that inherits from the Migration class and defines its own `up()` and `down()` methods to run the migration and roll it back, respectively.

The `up()` method first creates the `users` table using the `Schema::create()` method. It then creates the `password_reset_tokens` and `sessions` tables using similar methods. The `down()` method simply drops these tables if they exist.

This file does not contain any external dependencies or imports, other than the Laravel Framework's migration system.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
This is a migration file for creating two tables in the database: `cache` and `cache_locks`.

The `cache` table has three columns:

* `key`: a string that serves as the primary key of the table.
* `value`: a medium text column that stores the value of the cache entry.
* `expiration`: an integer column that represents the expiration time for the cache entry.

The `cache_locks` table has three columns:

* `key`: a string that serves as the primary key of the table.
* `owner`: a string column that stores the owner of the lock.
* `expiration`: an integer column that represents the expiration time for the lock.

The migration file uses the Laravel database migration system to create these tables in the database. The `up` method creates the `cache` and `cache_locks` tables, while the `down` method drops them if they exist.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
This is a Laravel migration file that creates three tables: `jobs`, `job_batches`, and `failed_jobs`.

The `jobs` table stores information about the jobs that are waiting to be processed. It has columns for the job's ID, queue name, payload, number of attempts, reserved time, available time, and created time.

The `job_batches` table stores information about the batches of jobs that are being processed. It has columns for the batch ID, name, total number of jobs, pending jobs, failed jobs, and options.

The `failed_jobs` table stores information about the jobs that have failed to be processed. It has columns for the job's UUID, connection, queue, payload, exception, and failed time.

This migration file uses Laravel's built-in database schema builder to create these tables. The `use Illuminate\Database\Migrations\Migration` class is used to define the structure of the migration, while the `up` method is used to create the tables, and the `down` method is used to drop them if needed.

This file does not include any external dependencies or imports, as it only uses Laravel's built-in database schema builder and migration classes.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
This file adds two new columns to the 'users' table in the database: 'two_factor_secret', 'two_factor_recovery_codes', and 'two_factor_confirmed_at'. These columns are used for implementing a two-factor authentication system.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
The file `database/migrations/2026_03_30_100000_create_contacts_table.php` is a migration for Laravel that creates a new table called "contacts" in the database. The migration contains two methods: `up()` and `down()`.

The `up()` method creates a new table with the following columns:

* `id`: an auto-incrementing primary key (type integer)
* `name`: a string column for the contact's name
* `company`: a string column for the contact's company (optional)
* `email`: a string column for the contact's email address
* `project_type`: a string column for the type of project the contact is interested in (e.g. "web development", "app development")
* `message`: a text column for any additional message or details the contact may have provided
* `timeline`: a string column for the timeline of the project (optional)
* `created_at` and `updated_at`: timestamp columns to keep track of when the record was created and last updated

The `down()` method drops the "contacts" table if it exists.

This migration is part of the Laravel framework's data access layer, which allows developers to easily interact with the database and perform CRUD (create, read, update, delete) operations on tables. The `Illuminate\Database` namespace is imported at the top of the file, along with the `Migration` class and `Blueprint` classes used in the migration.

The migration uses the `Schema` facade to create a new table called "contacts" and define its columns. The `id` column is set as the primary key for the table, which means it will be used as a unique identifier for each record in the table. The `timeline` column is set as optional, meaning it may or may not contain a value depending on the specific use case.

Overall, this migration provides a simple way to create a new "contacts" table in the database and define its columns, which can be used for storing and retrieving contact information from the user.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Datei: database/migrations/2026_03_30_100001_create_page_views_table.php

* Kategorie: data_access
* Importiert: Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\Schema
* Fügt eine neue Tabelle "page_views" hinzu, die eine ID, visits (Standardwert 0), und timestamps enthält.
* Die Tabelle wird mit der Methode "create()" erstellt.
* Wenn die Tabelle bereits existiert, wird sie mithilfe der Methode "dropIfExists()" gelöscht.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
The file database/migrations/2026_03_31_000001_create_permission_tables.php contains a migration class that creates the necessary tables for storing permissions and roles in a Laravel application.

The migration class extends the Illuminate\Database\Migrations\Migration class, which provides a convenient way to create and manage migrations in Laravel. The up() method is called when running the migration, and it performs the following actions:

1. It retrieves the table names and column names from the configuration file config/permission.php using the `config()` helper function. These tables are used to store permissions, roles, and their relationships between them.
2. It creates the permissions table by calling the `Schema::create()` method with the `$tableNames['permissions']` variable as its argument. The created table has an ID column, a name column, a guard_name column, and two timestamps columns. Additionally, it creates a unique index on the combination of the name and guard_name columns.
3. It creates the roles table by calling the `Schema::create()` method with the `$tableNames['roles']` variable as its argument. The created table has an ID column, a name column, a guard_name column, and two timestamps columns. Additionally, it creates a unique index on the combination of the name and guard_name columns.
4. It creates the model_has_permissions table by calling the `Schema::create()` method with the `$tableNames['model_has_permissions']` variable as its argument. The created table has an ID column, a pivot role column (defaults to 'role_id'), a pivot permission column (defaults to 'permission_id'), and two timestamps columns. Additionally, it creates a foreign key reference from the pivot role column to the roles table's ID column, a primary key on the combination of the pivot role column, the pivot permission column, and the model type column, and an index on the combination of the pivot permission column, the pivot role column, and the model type column.
5. It creates the model_has_roles table by calling the `Schema::create()` method with the `$tableNames['model_has_roles']` variable as its argument. The created table has an ID column, a pivot role column (defaults to 'role_id'), a pivot permission column (defaults to 'permission_id'), and two timestamps columns. Additionally, it creates a foreign key reference from the pivot role column to the roles table's ID column, a primary key on the combination of the pivot role column, the pivot permission column, and the model type column, and an index on the combination of the pivot permission column, the pivot role column, and the model type column.
6. It creates the role_has_permissions table by calling the `Schema::create()` method with the `$tableNames['role_has_permissions']` variable as its argument. The created table has two pivot columns (defaults to 'permission_id' and 'role_id'), and two timestamps columns. Additionally, it creates a foreign key reference from both pivot columns to their respective tables, and a primary key on the combination of both pivot columns.
7. It clears the cache for the permission package using the `cache()` helper function with the store and key specified in the configuration file config/permission.php.

Finally, the down() method is called when rolling back the migration, and it performs the following actions:

1. It drops the model_has_permissions table if it exists by calling the `Schema::dropIfExists()` method with the `$tableNames['model_has_permissions']` variable as its argument.
2. It drops the model_has_roles table if it exists by calling the `Schema::dropIfExists()` method with the `$tableNames['model_has_roles']` variable as its argument.
3. It drops the permissions table if it exists by calling the `Schema::dropIfExists()` method with the `$tableNames['permissions']` variable as its argument.
4. It drops the roles table if it exists by calling the `Schema::dropIfExists()` method with the `$tableNames['roles']` variable as its argument.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Die Datei fügt den Spalten 'path', 'ip_anonymous' und 'user_agent' der Tabelle 'page_views' hinzu. Die Spalten sind optional und enthalten Informationen über die Anfrage, von der die Seite aufgerufen wurde.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Maximal 10-zeilige Beschreibung der Datei-Funktionen: Diese Datei enthält eine Migration (database/migrations/2026_03_31_000003_create_consents_table.php), die einen Endpunkt für das Speichern von Benutzer-Einwilligungen in der Datenbank erstellt.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Die Datei `database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php` enthält eine SQLite-kompatible Version der Recherche-Kern-Tabellen für Testzwecke. Sie importiert die folgenden Klassen und Funktionen:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse aller Migrationen.
* `Illuminate\Database\Schema\Blueprint`: Eine Klasse, die es ermöglicht, Tabellendefinitionen zu erstellen.
* `Illuminate\Support\Facades\DB`: Ein Wrapper für den Eloquent-ORM-Datenbankzugriff.
* `Illuminate\Support\Facades\Schema`: Eine Schnittstelle zum Erstellen und Verwalten von Tabellendefinitionen.

Die Migration enthält verschiedene Endpunkte, die für die Erstellung und Verwaltung der Recherche-Tabellen zuständig sind:

1. `up()`: Führt die Migration aus. Wenn der Datenbanktreiber 'pgsql' ist, wird die Migration übersprungen. Andernfalls werden die Tabellen erstellt, wenn sie nicht bereits vorhanden sind.
2. `down()`: Rollbacket die Migration und entfernt die Tabellen.
3. `safeDown()`: Führt das Rollback der Migration aus und entfernt die Tabellen.
4. `getDescription()`: Gibt eine kurze Beschreibung der Migration zurück.
5. `getName()`: Gibt den Namen der Migration zurück.
6. `getPath()`: Gibt den Pfad der Migration zurück.
7. `getMigrationFiles()`: Gibt ein Array mit den Migrationsdateien zurück.
8. `getNotes()`: Gibt eine Notiz zur Migration zurück.
9. `setNotes($notes)`: Legt die Notizen für die Migration fest.
10. `getMigrationTime()`: Gibt das Zeitstempel der Migration zurück.

Die Migration enthält auch diverse Klassen und Funktionen, um die Recherche-Tabellen zu erstellen und zu verwalten:

* `projekte`: Die Tabelle für die Projekte, in denen die Recherche durchgeführt wird.
* `phasen`: Die Tabelle für die verschiedenen Phasen des Projektes.
* `p5_treffer`: Die Tabelle für die tatsächlichen Suchergebnisse.
* `p1_strukturmodell_wahl`: Die Tabelle, in der der Strukturmodell-Wahlvorgang dokumentiert wird.
* `p1_komponenten`: Die Tabelle, in der die verschiedenen Komponenten des Modells dokumentiert werden.
* `p1_kriterien`: Die Tabelle, in der die Kriterien für die Auswahl von Strukturmodellen dokumentiert werden.
* `p1_warnsignale`: Die Tabelle, in der die Warnmeldungen für das Modell dokumentiert werden.
* `p2_review_typ_entscheidung`: Die Tabelle, in der die Entscheidungen über die Reviews dokumentiert werden.
* `p2_cluster`: Die Tabelle, in der die Cluster des Modells dokumentiert werden.
* `p2_mapping_suchstring_komponenten`: Die Tabelle, in der die Zuordnung von Suchbegriffen zu Komponenten dokumentiert wird.
* `p2_trefferlisten`: Die Tabelle, in der die Suchergebnisse für das Modell dokumentiert werden.
* `p3_disziplinen`: Die Tabelle, in der die Disziplinen des Modells dokumentiert werden.
* `p3_datenbankmatrix`: Die Tabelle, in der die Datenbank-Matrix für das Modell dokumentiert wird.

Insgesamt erstellt diese Migration 14 Tabellen und 21 Endpunkte, um die Recherche-Tabellen zu erstellen und zu verwalten.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei `database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php` enthält einen PHP-Code, der eine Reihe von Funktionen und Klassen definiert. Hier ist eine Zusammenfassung der wichtigsten Funktionen und Klassen in maximal 10 Zeilen:

* Die `Migration`-Klasse wird als Basisklasse für die Migrationsklasse verwendet. Sie enthält Methoden wie `up` und `down`, die verwendet werden, um Datenbankänderungen durchzuführen. In dieser Datei wird `up` verwendet, um eine Reihe von PostgreSQL-Extensions (ENUMs) zu erstellen.
* Die `DB`-Klasse ist ein Helper für den Zugriff auf die Datenbank und enthält Methoden wie `statement`, `scalar` und `getDriverName`. In dieser Datei wird `statement` verwendet, um SQL-Anweisungen auszuführen, und `scalar` zum Überprüfen, ob eine bestimmte Tabelle oder Enum existiert.
* Die `Illuminate\Support\Facades\DB`-Klasse ist ein statischer Zugriff auf die Datenbankklassen von Laravel. Sie enthält Methoden wie `statement`, `scalar` und `getDriverName`. In dieser Datei wird `statement` verwendet, um SQL-Anweisungen auszuführen, und `scalar` zum Überprüfen, ob eine bestimmte Tabelle oder Enum existiert.
* Die `$enums`-Variable enthält ein Array mit den Namen und Werten von PostgreSQL-ENUMs, die in der Datenbank erstellt werden sollen. Jedes Eintrag hat das Format `<Name>: '<Value1>', '<Value2>', ...'<ValueN>'`.
* Die `foreach`-Schleife durchläuft das `$enums`-Array und erstellt für jeden Eintrag eine neue PostgreSQL-ENUM, wenn sie nicht bereits existiert. Das Statement wird mit der `DB::statement`-Methode ausgeführt.
* Die `down`-Methode enthält Code, der verwendet wird, um die ENUMs zu löschen, falls sie existieren. Sie durchläuft das `$types`-Array und führt für jeden Eintrag eine `DROP TYPE IF EXISTS`-Anweisung aus, um die ENUM zu löschen. Die `CASCADE`-Klausel ist erforderlich, um auch alle Abhängigkeiten von der ENUM zu entfernen.
* Die `down`-Methode wird nicht ausgeführt, wenn das Datenbank-Driver nicht PostgreSQL ist. Dies kann passieren, wenn die Migration auf anderen Datenbanken wie MySQL oder SQLite verwendet werden soll.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
The file `database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php` is a migration file for creating tables in a PostgreSQL database using the Laravel framework. The file defines two new tables: `projekte` and `phasen`.

The `projekte` table has columns for storing information about projects, including their ID, title, research question, responsible person, start date, notes, last update time, and creation time. The `projekt_id` column is a foreign key that references the primary key of the `users` table. The `review_typ` column is a new column that was added to the `projekte` table with an SQL statement using the `ALTER TABLE` statement.

The `phasen` table has columns for storing information about project phases, including their ID, project ID, phase number, title, notes, and completion date. The `projekt_id` column is a foreign key that references the primary key of the `projekte` table. The `phase_nr` column is a unique constraint that ensures each phase has a unique combination of project ID and phase number.

The migration file also includes a down() method for reversing the changes made by the up() method, in case the migration needs to be rolled back.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
This is a Laravel database migration file for creating tables related to the "Projekt 1" software development project. The file creates the following tables:

* `p1_strukturmodell_wahl`
* `p1_komponenten`
* `p1_kriterien`
* `p1_warnsignale`

The tables are defined using Laravel's schema builder and have foreign key constraints to the `projekte` table. The migration also includes statements for creating columns with specific data types, such as `uuid`, `smallInteger`, and `jsonb`.

The `down()` method is used to reverse the migration, which drops the tables if they exist. This ensures that the database remains in the same state even if the migration fails or is rolled back.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Die Datei enthält eine PHP-Klasse, die sich um die Verwaltung von Tabellen mit dem Präfix "p2_" in der Datenbank kümmert. Sie implementiert die Migration-Schnittstelle und enthält Methoden für das Erstellen und Löschen der Tabellen, sowie zum Füllen der Tabelle mit Daten.

Die wichtigsten Klassen und Funktionen sind:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse für Migrationen, die die Schnittstelle zwischen der PHP-Klasse und dem Laravel-Framework definiert.
* `Illuminate\Database\Schema\Blueprint`: Eine Klasse, die es ermöglicht, Tabellendefinitionen in einer deklarativen Weise zu erstellen.
* `Illuminate\Support\Facades\DB`: Eine Facade für den Datenbankzugriff, der es ermöglicht, Datenbankanweisungen direkt aus PHP-Code auszuführen.
* `Illuminate\Support\Facades\Schema`: Eine Facade für die Schema-API von Laravel, die es ermöglicht, das Schema der Datenbank zu verwalten.
* `Illuminate\Database\Query\Builder`: Eine Klasse, die es ermöglicht, SQL-Anweisungen direkt aus PHP-Code auszuführen.

Die Datei definiert auch externe Abhängigkeiten:

* Die Laravel-Framework-Komponente "database" wird verwendet, um den Zugriff auf die Datenbank zu ermöglichen.
* Die Laravel-Framework-Komponente "schema" wird verwendet, um das Schema der Datenbank zu verwalten.

Die Datei enthält auch mehrere Methoden, um die verschiedenen Funktionen der Klasse zu implementieren:

* `up`: Diese Methode wird aufgerufen, wenn die Migration angewendet werden soll. Sie erstellt oder füllt die Tabellen mit Daten.
* `down`: Diese Methode wird aufgerufen, wenn die Migration rückgängig gemacht werden soll. Sie löscht die Tabellen.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
This file is a migration for the Laravel framework, specifically for the database. It contains several classes that define table structures and relationships for the following tables:

* `p3_disziplinen`
* `p3_datenbankmatrix`
* `p3_geografische_filter`
* `p3_graue_literatur`

The migration class is a subclass of the `Illuminate\Database\Migrations\Migration` class, which provides some useful methods for managing migrations. The `up` method creates the tables and their respective columns if they do not already exist, while the `down` method drops the tables if they exist.

The migration file also includes some foreign key constraints to ensure data integrity between related tables. For example, the `p3_disziplinen` table has a foreign key constraint that references the `projekte` table, and the `p3_datenbankmatrix` table has a foreign key constraint that references both the `projekte` and `p3_disziplinen` tables.

Overall, this migration file is responsible for defining the structure of the database tables used by the Laravel application.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
* Die Datei `database/migrations/2026_03_31_100005_create_p4_tables.php` enthält eine Klasse, die von der Illuminate\Database\Migrations\Migration-Klasse erbt.
* Die Klasse hat zwei Methoden: `up()` und `down()`. Die Methode `up()` wird verwendet, um die Tabelle `p4_suchstrings` zu erstellen, während die Methode `down()` dazu dient, die Tabelle `p4_suchstrings` wieder zu entfernen.
* In der Methode `up()` werden die Tabellen `p4_thesaurus_mapping`, `p4_anpassungsprotokoll` und `p4_suchstrings` erstellt. Jede Tabelle hat eine eindeutige ID, eine Projekt-ID, einen Suchstring, ein Datum für die Suche und einen Textfeld für die Anpassung.
* Die Methode `down()` entfernt die Tabellen `p4_anpassungsprotokoll`, `p4_thesaurus_mapping` und `p4_suchstrings`.
* Die Klasse importiert den Facade-Alias `DB` von Illuminate\Support\Facades\DB, der verwendet wird, um die Datenbankverbindung herzustellen.
* Die Klasse importiert auch den Alias `Schema` von Illuminate\Database\Schema\Builder, der verwendet wird, um Tabellen zu erstellen und zu entfernen.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
This is a database migration file for Laravel that creates several tables for the P5 (Prisma 5) data access project. The following are the main functions of this file:

1. Creates the `p5_prisma_zahlen` table, which stores statistics about the Prisma 5 process.
2. Creates the `p5_screening_kriterien` table, which stores information about the screening criteria used in the Prisma 5 process.
3. Creates the `p5_treffer` table, which stores information about the hits (records) identified by the Prisma 5 algorithm.
4. Creates the `p5_screening_entscheidungen` table, which stores information about the screening decisions made by the reviewer.
5. Creates the `p5_tool_entscheidung` table, which stores information about the tool recommendations made by the reviewer.
6. Adds foreign key constraints to ensure referential integrity between the tables.
7. Drops the tables if they already exist.

The file uses Laravel's database migration framework to create and manage the database schema. The `use Illuminate\Database\Migrations\Migration;` statement at the top of the file imports the Migration class, which is used to define the database migrations. The `use Illuminate\Database\Schema\Blueprint;` statement imports the Blueprint class, which is used to create and modify tables. The `use Illuminate\Support\Facades\DB;` statement imports the DB facade, which provides an interface for interacting with the database. The `use Illuminate\Support\Facades\Schema;` statement imports the Schema facade, which provides an interface for managing the database schema.

The `return new class extends Migration` statement at the bottom of the file creates a new instance of the Migration class and defines its methods. The `up()` method is called when the migration is executed in the "up" direction (i.e., when the migration is applied). The `down()` method is called when the migration is rolled back (i.e., when the migration is reverted).

The file contains several `if` statements that check whether certain tables or columns already exist before creating them. This is necessary because the migration may be executed multiple times, and it's important to avoid errors caused by creating duplicate tables or columns. The `DB::getDriverName()` method is used to determine the database driver being used (in this case, PostgreSQL). The `Schema::hasTable('table_name')` method is used to check if a table exists in the current database schema. The `Schema::create('table_name', function ($table) { ... })` method is used to create a new table with the specified name and columns. The `$table` variable represents the Blueprint instance, which allows us to define the table's structure using methods such as `id()`, `string('column_name')`, etc.

The file also contains several foreign key constraints that ensure referential integrity between the tables. These constraints are defined using the `Schema::table()` method and the `$table->foreign('foreign_key_column')` syntax. The `cascadeOnDelete()` method is used to specify that when a record in one table is deleted, all related records in other tables should also be deleted.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
This file contains a migration for a Laravel application. The migration creates two new tables: `p6_qualitaetsbewertung` and `p6_luckenanalyse`.

The `p6_qualitaetsbewertung` table has columns for an ID, a reference to the `p5_treffer` table, a text column for the main problem, a boolean column for whether the assessment should be kept in review, a text column for an annotation, and a date column for when the assessment was made. The table also has columns for studientyp, rob_tool, and gesamturteil that are not nullable but have default values of `NULL`.

The `p6_luckenanalyse` table has columns for an ID, a reference to the `projekte` table, a text column for the missing aspect, a text column for the missing studientyp, a text column for possible consequences, and a text column for recommendations. The table also has a foreign key constraint on the `projekte` table.

The migration also includes code to drop these tables if they exist.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
This file is a migration for the Laravel framework, specifically for PostgreSQL. It contains a class that extends the `Migration` class and has two methods: `up()` and `down()`. The `up()` method creates tables in the database, while the `down()` method drops those same tables.

The `p7_synthese_methode`, `p7_datenextraktion`, `p7_muster_konsistenz`, and `p7_grade_einschaetzung` tables are created in this migration. These tables contain columns for the primary key, foreign key constraints, and other columns as specified in the comments of the code.

The `use` statements at the top of the file import the necessary classes to use the Laravel framework and PostgreSQL. The `Illuminate\Support\Facades\DB` class is used to interact with the database.

This migration also includes some checks to ensure that the table creation only happens if the driver being used is PostgreSQL, which is important for databases that support multiple drivers.

The file output is in JSON format and contains a summary of the file's functions. It lists the main classes and functions involved in the migration.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Die Datei `database/migrations/2026_03_31_100009_create_p8_tables.php` enthält eine PHP-Klasse, die als Migration dient. Die Klasse definiert vier Tabellen:

* `p8_suchprotokoll`: Eine Tabelle für das Speichern von Suchprotokollen.
* `p8_limitationen`: Eine Tabelle für das Speichern von Limitations.
* `p8_reproduzierbarkeitspruefung`: Eine Tabelle für die Speicherung von Reproduzierbarkeitsprüfungen.
* `p8_update_plan`: Eine Tabelle für den Plan der Updates.

Die Klasse definiert auch einige Foreign Keys und Check-Constraints, um die Integrität der Daten zu gewährleisten.

Die Klasse verwendet die Laravel-Facades `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint` und `Illuminate\Support\Facades\DB`. Die Facade `Illuminate\Support\Facades\Schema` wird auch verwendet, um das Schema der Tabelle zu erstellen.

Die Klasse definiert auch eine Methode `down`, die verwendet wird, um die Tabelle bei einem Rollback oder bei einer Fehlerbehebung wieder zu löschen.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Die Datei `database/migrations/2026_03_31_100010_create_recherche_indices.php` ist eine Migration-Datei für das Laravel-Projekt, die Indexes für verschiedene Tabellen erstellt. Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Illuminate\Database\Migrations\Migration`: Die Klasse `Migration` stellt eine Migration dar, die von Laravel verwendet wird, um Datenbankänderungen durchzuführen.
* `Illuminate\Support\Facades\DB`: Die Klasse `DB` ist eine Facade für das Laravel-Datenbank-Managementsystem. Sie ermöglicht den Zugriff auf die Datenbank und das Erstellen von Indexes.
* `up()`: Diese Funktion wird aufgerufen, um die Migration auszuführen. Sie erstellt Indexes für verschiedene Tabellen in der Datenbank.
* `down()`: Diese Funktion wird aufgerufen, um die Migration rückgängig zu machen. Sie löscht die erstellten Indexes und setzt sie zurück.

Die wichtigsten Funktionen in dieser Datei sind:

* `DB::statement('CREATE INDEX IF NOT EXISTS idx_phasen_projekt ON phasen(projekt_id)');`: Diese Zeile erstellt einen Index namens `idx_phasen_projekt` für die `phasen`-Tabelle, der auf die Spalte `projekt_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p1_komp_projekt ON p1_komponenten(projekt_id)');`: Diese Zeile erstellt einen Index namens `idx_p1_komp_projekt` für die `p1_komponenten`-Tabelle, der auf die Spalte `projekt_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p5_treffer_projekt ON p5_treffer(projekt_id)');`: Diese Zeile erstellt einen Index namens `idx_p5_treffer_projekt` für die `p5_treffer`-Tabelle, der auf die Spalte `projekt_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p5_treffer_record ON p5_treffer(record_id)');`: Diese Zeile erstellt einen Index namens `idx_p5_treffer_record` für die `p5_treffer`-Tabelle, der auf die Spalte `record_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p5_treffer_duplikat ON p5_treffer(ist_duplikat)');`: Diese Zeile erstellt einen Index namens `idx_p5_treffer_duplikat` für die `p5_treffer`-Tabelle, der auf die Spalte `ist_duplikat` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p5_screening_treffer ON p5_screening_entscheidungen(treffer_id)');`: Diese Zeile erstellt einen Index namens `idx_p5_screening_treffer` für die `p5_screening_entscheidungen`-Tabelle, der auf die Spalte `treffer_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p6_bewertung_treffer ON p6_qualitaetsbewertung(treffer_id)');`: Diese Zeile erstellt einen Index namens `idx_p6_bewertung_treffer` für die `p6_qualitaetsbewertung`-Tabelle, der auf die Spalte `treffer_id` verweist.
* `DB::statement('CREATE INDEX IF NOT EXISTS idx_p7_extraktion_treffer ON p7_datenextraktion(treffer_id)');`: Diese Zeile erstellt einen Index namens `idx_p7_extraktion_treffer` für die `p7_datenextraktion`-Tabelle, der auf die Spalte `treffer_id` verweist.
* `DB::statement("CREATE INDEX IF NOT EXISTS idx_p5_treffer_abstract_fts ON p5_treffer USING gin(to_tsvector('german', coalesce(abstract, '')))");`: Diese Zeile erstellt einen Volltextsuchindex namens `idx_p5_treffer_abstract_fts` für die `p5_treffer`-Tabelle, der auf den Spalten `abstract` verweist.
* `DB::statement("CREATE INDEX IF NOT EXISTS idx_p5_treffer_titel_fts ON p5_treffer USING gin(to_tsvector('german', coalesce(titel, '')))");`: Diese Zeile erstellt einen Volltextsuchindex namens `idx_p5_treffer_titel_fts` für die `p5_treffer`-Tabelle, der auf den Spalten `titel` verweist.

Die Datei definiert auch eine Migration-Klasse, die die Funktionen `up()` und `down()` enthält, um die Migration auszuführen oder rückgängig zu machen.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Die Datei stellt eine PostgreSQL-Funktion dar, die als Migration zum Datenbankmodell hinzugefügt wird. Die Funktion heißt `berechne_prisma_zahlen` und nimmt einen UUID-Wert als Eingabeparameter an.

Die Funktion berechnet die PRISMA-2020-Kennzahlen für ein Recherche-Projekt direkt auf der Datenbankebene. Sie zählt die Werte für das PRISMA-Flussdiagramm: Identifiziert → Duplikate → L1-Ausschluss → Volltext → L2-Ausschluss → Final.

Die Funktion verwendet eine SQL-Abfrage, um die gewünschten Kennzahlen zu berechnen und gibt sie als Tabelle mit folgenden Spalten zurück:

* `identifiziert_gesamt`: Die Gesamtanzahl der identifizierten Treffer
* `duplikate`: Die Anzahl der duplikierten Treffer
* `nach_deduplizierung`: Die Anzahl der nicht-duplikeren Treffer nach Deduplizierung
* `ausgeschlossen_l1`: Die Anzahl der Treffer, die ausgeschlossen sind (L1-Ausschluss)
* `volltext_geprueft`: Die Anzahl der Treffer, für die das Volltext-Prüfungsscreening durchgeführt wurde
* `ausgeschlossen_l2`: Die Anzahl der Treffer, die ausgeschlossen sind (L2-Ausschluss)
* `eingeschlossen_final`: Die Anzahl der Treffer, die im Endergebnis eingeschlossen werden (Final)

Die Funktion verwendet die folgenden Tabellen und Spalten:

* `p5_treffer`: Die Tabelle mit den Recherche-Treffern
* `p5_screening_entscheidungen`: Die Tabelle mit den Screening-Entscheidungen
* `ist_duplikat`: Eine Spalte in der Tabelle `p5_treffer`, die angibt, ob ein Treffer ein Duplikat ist oder nicht.
* `level`: Eine Spalte in der Tabelle `p5_screening_entscheidungen`, die den Screening-Level des Treffers angibt (L1-Ausschluss, Volltext, L2-Ausschluss).
* `entscheidung`: Eine Spalte in der Tabelle `p5_screening_entscheidungen`, die die Entscheidung zu dem Treffer angibt (eingeschlossen oder ausgeschlossen).

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Die Datei `database/migrations/2026_03_31_200000_create_chat_messages_table.php` ist eine Datenbank-Migration, die eine neue Tabelle namens "chat_messages" erstellt. Die Tabelle hat die folgenden Spalten:

* `id`: eine UUID-Spalte als Primärschlüssel und Standardwert für `uuid_generate_v4()`.
* `user_id`: eine Foreign Key, die auf die Tabelle "users" verweist.
* `role`: eine Spalte vom Typ "text" mit einer maximalen Länge von 20 Zeichen, die nur den Wert "user" oder "assistant" annimmt.
* `content`: eine Spalte vom Typ "text", die den tatsächlichen Inhalt des Chat-Nachrichtenobjekts enthält.
* `created_at`: eine Spalte vom Typ "timestampTz", die den Zeitpunkt der Erstellung der Nachricht angibt und Standardwert für `now()`.

Die Migration gibt auch eine Bedingung an, dass die Tabelle nur erstellt wird, wenn die verwendete Datenbank-Engine PostgreSQL ist. Dies ist nötig, weil die Funktion `uuid_generate_v4()` nur von PostgreSQL unterstützt wird.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
This is a Laravel migration file that creates a new table called "webhooks" in the database. The table has several columns, including an ID column with a UUID primary key, a user_id foreign key that references the users table, and a name, slug, url, frontend_object, and created_at timestamp column.

The migration also creates a unique index on the combination of the user_id and frontend_object columns, and sets up a foreign key constraint between the webhooks table and the chat_messages table that references the webhook_id column in the chat_messages table.

This migration is intended to be used with Laravel's migrations feature, which allows developers to easily create and manage database schema changes during the development of an application.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
The file `database/migrations/2026_03_31_200002_create_paper_embeddings_table.php` is a migration script for creating a new table in the database named "paper_embeddings". The table will contain information about papers, such as their IDs, titles, and text content. Additionally, it will include a column for storing the embedding vectors of the papers.

The file uses the Laravel framework's Migration class to define the structure of the table. It creates a primary key `id` for each row in the table, and foreign key constraints to ensure referential integrity between the table and other tables in the database.

The file also defines an index on the `projekt_id` column, which is used for faster lookups by project ID. The `source` and `paper_id` columns are also indexed, as they will be used frequently for filtering and searching.

Finally, the file creates a vector column named `embedding`, which contains the embedding vectors of the papers. It then creates an index on this column using the `ivfflat` algorithm, which is optimized for vector similarity searches.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
This migration file creates a new database table called `activity_log` in Laravel. The table has the following columns:

* `id`: A unique identifier for each log entry.
* `log_name`: The name of the log, which can be used to categorize different types of logs. This column is nullable and indexed.
* `description`: A brief description of the action that occurred, such as "Created user account" or "Updated user profile".
* `subject_id`: The ID of the subject (user or model) that triggered the log event. This column is nullable and supports both UUID and integer primary keys.
* `subject_type`: The type of the subject (user or model) that triggered the log event.
* `event`: The name of the event that occurred, such as "created" or "updated".
* `causer_id`: The ID of the user or system that caused the log event. This column is nullable and supports both UUID and integer primary keys.
* `causer_type`: The type of the user or system that caused the log event.
* `attribute_changes`: A JSON column containing information about any changes made to the subject, such as "name" and "email".
* `properties`: A JSON column containing additional properties related to the log event, such as "IP address" and "device type".
* `created_at` and `updated_at`: Timestamps for when the log entry was created and last updated.

The migration also creates indexes on the `subject_type`, `subject_id`, `causer_type`, and `causer_id` columns to improve query performance.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
This is a Laravel migration file that adds a new column to the `users` table in the database. The new column is called `status`, and it will be an enum with four possible values: "trial", "active", "suspended", and "cancelled". The default value for this column will be "trial".

The migration also creates a custom data type called `user_status` in PostgreSQL, which is an enumerated data type that represents the four possible values for the `status` column. This data type is used to enforce the consistency of the `status` column and ensure that only valid values are stored in it.

The migration also adds a default value for the `status` column using the `ALTER TABLE` statement, which sets the default value for the column to "trial". This ensures that all new rows in the table will have the default value for the `status` column set.

Overall, this migration is designed to add a new column to the `users` table with enum data type and a default value, while also creating a custom data type in PostgreSQL to enforce consistency of the data stored in that column.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
* This file is a migration for a Laravel project, specifically for the `webhooks` table.
* The migration creates an enum type called `webhook_frontend_object` in the PostgreSQL database, which represents different types of frontend objects that can be associated with webhooks.
* The migration also adds two columns to the `webhooks` table: `frontend_object`, which is of the newly created enum type, and `user_id`, which is a foreign key referencing the `users` table.
* Finally, the migration creates a unique index on the `user_id` and `frontend_object` columns to enforce that each user can only have one webhook for each frontend object.

---

### database/seeders/DatabaseSeeder.php
Die Datei `database/seeders/DatabaseSeeder.php` ist ein Seeder-Datei, die verwendet wird, um eine Test-Datenbank zu füllen. Sie importiert die `User`-Klasse und ruft den `call()`-Methoden von zwei anderen Seedern auf: `RoleSeeder::class` und `RechercheDemoSeeder::class`.

Die `RoleSeeder`-Klasse ist verantwortlich für das Erstellen von Rollen und Benutzerrollen in der Datenbank. Die `RechercheDemoSeeder`-Klasse ist verantwortlich für das Füllen der Datenbank mit Beispieldaten für die Suche.

Die Datei verwendet auch die `Illuminate\Database\Seeder` Klasse, um die Seed-Prozess zu starten und die Methode `run()` auszuführen. In dieser Methode werden die beiden anderen Seeders aufgerufen und die Benutzer-Rollen mit den Beispieldaten für die Suche erstellt.

Es ist wichtig zu beachten, dass diese Datei keine Prosa oder Kommentare enthält, da sie nur eine beschreibende Zeile hat. Es gibt auch keine Bewertung oder Fehlerbehandlung in dieser Datei, da es sich um eine Seeder-Datei handelt und die Verwendung von `WithoutModelEvents` ist nicht erforderlich.

---

### database/seeders/RechercheDemoSeeder.php
Diese Datei enthält eine Recherchetabelle mit verschiedenen Suchstrings für die Datenbanken PubMed und Embase. Der Suchstring wird verwendet, um nach relevanten Publikationen zu suchen, die den von einer klinischen Interventionsstudie verwendeten Suchbegriff enthalten.

---

### database/seeders/RoleSeeder.php
Die Datei `database/seeders/RoleSeeder.php` ist ein Seeder-Objekt, das die Rolle "admin" und "user" in der Datenbank erstellt.

Es importiert den `Spatie\Permission\Models\Role`-Klassennamen und verwendet die `firstOrCreate()`-Methode, um sicherzustellen, dass die Rollen in der Datenbank vorhanden sind, bevor sie verwendet werden. Die Methode gibt einen Eintrag zurück oder erstellt einen neuen Eintrag, wenn er nicht vorhanden ist.

Diese Datei ist Teil des Laravel-Framework und wird für die Einrichtung von Berechtigungen in der Anwendung verwendet.

---

### database/sql/create_langdock_agent_user.sql
The file `database/sql/create_langdock_agent_user.sql` is a SQL script that creates a new user named `langdock_agent` with restricted read and write access to specific tables in the Recherche schema of the Linn Games database. The user can only perform SELECT, INSERT, UPDATE, DELETE operations on the following tables:

* Projekte
* Phasen
* P1_strukturmodell_wahl
* P1_komponenten
* P1_kriterien
* P1_warnsignale
* P2_review_typ_entscheidung
* P2_mapping_suchstring_komponenten
* P2_trefferlisten
* P2_cluster
* P3_disziplinen
* P3_datenbankmatrix
* P3_geografische_filter
* P3_graue_literatur
* P4_suchstrings
* P4_thesaurus_mapping
* P4_anpassungsprotokoll
* P5_prisma_zahlen
* P5_screening_kriterien
* P5_treffer
* P5_screening_entscheidungen
* P5_tool_entscheidung
* P6_qualitaetsbewertung
* P6_luckenanalyse
* P7_synthese_methode
* P7_datenextraktion
* P7_muster_konsistenz
* P7_grade_einschaetzung
* P8_suchprotokoll
* P8_limitationen
* P8_reproduzierbarkeitspruefung
* P8_update_plan
* paper_embeddings

The user also has access to the PRISMA helper function `berechne_prisma_zahlen(UUID)`. Additionally, the user is denied access to certain sensitive tables such as password_reset_tokens, sessions, cache, cache_locks, jobs, job_batches, failed_jobs, permissions, roles, model_has_permissions, model_has_roles, role_has_permissions, consents, contacts, page_views.

The script is run as a superuser or DB owner against the application database to create the user. It is important to note that this script should only be executed by trusted users and with appropriate permissions to prevent unauthorized access to sensitive data.

---

## temp_dummy

### .github/pull_request_template.md
Pull request template for .github/pull_request_template.md

---

## tests

### tests/Pest.php
This is a PHP file for testing using Pest, which provides a set of test case classes and expectations methods for writing unit tests.

The `pest()` function is used to bind the test case class to the current test file. In this case, it is bound to the `Tests\TestCase` class provided by Laravel's framework.

The `expect()` function is used to access a set of "expectations" methods that can be used to assert different things in test cases. It also allows for extending the Expectation API at any time.

Finally, the file defines a global helper function named `something()`, which can be used as a shortcut for testing specific conditions in test cases.

---

### tests/TestCase.php
The file `tests/TestCase.php` is a test case in the Laravel framework. It is an abstract class that serves as a base class for other test cases in the `Tests` namespace.

It imports the `Illuminate\Foundation\Testing\TestCase` class, which provides a set of methods for testing Laravel applications. The `TestCase` class is intended to be extended by other classes that define specific tests.

The `TestCase` class does not contain any external dependencies or APIs, and it does not interact with a database. It is purely an abstract class used as a base class for other test cases.

---

### tests/Feature/ContactTest.php
Diese Datei enthält Tests für die Kontaktformular-Funktion in einer Laravel-Anwendung. Die wichtigsten Klassen, Funktionen und Endpunkte sind:

* `ContactTest`: Der Testklasse, die die Kontaktformular-Funktion testet.
* `route('contact.store')`: Der Endpunkt, über den die Anfrage an das Kontaktformular gesendet wird.
* `App\Models\Contact`: Die Model-Klasse für das Kontaktformular.
* `$this->postJson()`: Eine Methode, um eine POST-Anfrage an den Endpunkt zu senden und die Antwort als JSON-Objekt zu verarbeiten.
* `assertStatus(200)`: Ein Assertion, um sicherzustellen, dass die Anfrage erfolgreich war und ein 200er HTTP-Statuscode zurückgegeben wurde.
* `assertJson(['success' => true, 'message' => 'Anfrage erfolgreich gesendet.'])`: Ein Assertion, um sicherzustellen, dass die Antwort eine JSON-Zeichenkette enthält und einen bestimmten Wert für das Feld `success` hat.
* `assertDatabaseHas()`: Ein Assertion, um sicherzustellen, dass ein Eintrag in der Datenbank vorhanden ist, der den übergebenen Kriterien entspricht.

---

### tests/Feature/DashboardTest.php
This file contains two test cases for the dashboard feature. The first test case asserts that guests are redirected to the login page when they try to access the dashboard. The second test case asserts that authenticated users can visit the dashboard and receive a successful response.

---

### tests/Feature/ExampleTest.php
This file contains test cases for the home route in the Laravel application. The test case uses the 'get' method to send a GET request to the '/' endpoint, and then asserts that the response status code is 200.

---

### tests/Feature/IngestPaperJobTest.php
Diese Datei enthält Unit-Tests für die `IngestPaperJob`-Klasse, die für das Ingestieren von Paper-Objekten in der Datenbank zuständig ist. Die Klasse verwendet den Ollama-Service, um eine künstliche Vectorkennzeichnung (Embedding) des Textes zu generieren und in die Datenbank einzufügen.

Die wichtigsten Funktionen dieser Datei sind:

* `test('ingest paper job can be dispatched')`: Diese Tests überprüft, ob die `IngestPaperJob`-Klasse ordnungsgemäß initialisiert werden kann und ob sie korrekt in die Warteschlange geplant wird.
* `test('ingest paper job stores correct parameters')`: Dieser Test überprüft, ob die Klasse die richtigen Parameter bei der Erstellung des Jobs speichert und diese korrekt an den Ollama-Service weiterleitet.
* `test('ingest paper job fails when ollama returns error')`: Dieser Test simuliert einen Fehler bei der Ollama-API und überprüft, ob die Klasse den Fehler richtig behandelt und dass keine Chunks in die Datenbank eingefügt werden.
* `test('ingest paper job retries up to 3 times')`: Diese Tests überprüfen, ob die Klasse drei Mal versucht, eine Verbindung zum Ollama-Service herzustellen, bevor sie den Vorgang abbricht.
* `test('ingest paper job chunks long text correctly')`: Dieser Test überprüft, ob die Klasse den langen Text in kleinere Chunks aufteilt und diese korrekt an den Ollama-Service weiterleitet.
* `test('ingest paper job handles text shorter than chunk size')`: Dieser Test simuliert einen Fehler bei der Einfügung des Vectors in die SQLite-Datenbank und überprüft, ob die Klasse den Fehler richtig behandelt und dass keine Chunks in die Datenbank eingefügt werden.

---

### tests/Feature/LegalPagesTest.php
This file contains tests for the impressum, dsgvo and agb pages of a website. The tests are written using PHPUnit and are located in the `tests/Feature` directory.

The file imports several external dependencies, including Laravel's testing framework (`$this->get()`) and the route helper function (`route()`).

The file defines three test functions:

1. `impressum page can be rendered`: This test checks that the impressum page can be accessed and that it contains certain text.
2. `dsgvo page can be rendered`: This test checks that the dsgvo page can be accessed and that it contains certain text.
3. `agb page can be rendered`: This test checks that the agb page can be accessed and that it contains certain text.

The tests are defined using PHPUnit's `test()` function, which takes a closure as an argument. The closure is executed when the test is run, and should contain the code to be tested. In this case, the closure uses the `$this->get()` method to access the route for the page being tested, and then asserts that the response has a certain status code (200) and contains certain text.

Overall, this file is a collection of tests that check the functionality of the impressum, dsgvo, and agb pages of a website.

---

### tests/Feature/PaperRagTest.php
This file contains tests for the `PaperRag` class in Laravel. The `PaperRagTest` class defines a set of test methods that test different aspects of the `PaperRag` class, such as ingesting papers and searching for RAG scores using the Ollama API.

The tests in this file use the Laravel Testbench to mock external dependencies and assert the expected behavior of the `PaperRag` class. The tests also demonstrate the use of helper functions such as `mcpHeaders()` and `ollamaUrl()`, which are used to configure authentication headers for the Ollama API and construct the URL for the RAG search endpoint, respectively.

Here's a summary of the key points in this file:

* The `PaperRagTest` class defines test methods that test various aspects of the `PaperRag` class, such as ingesting papers and searching for RAG scores using the Ollama API.
* The tests use the Laravel Testbench to mock external dependencies and assert the expected behavior of the `PaperRag` class.
* Helper functions such as `mcpHeaders()` and `ollamaUrl()` are used to configure authentication headers for the Ollama API and construct the URL for the RAG search endpoint, respectively.
* The tests demonstrate the use of various HTTP methods (e.g., POST, GET) and assert the expected status codes and error messages.

---

### tests/Feature/ProjektPolicyTest.php
This is a PHPUnit test file for the `ProjektPolicy` class. The file contains several test methods that verify different scenarios related to authorization and access control of projects. Here's a brief summary of each method:

1. `owner can view their project`: This test verifies that the owner of a project can view it.
2. `non-owner cannot view project`: This test verifies that a non-owner of a project cannot view it.
3. `owner can update their project`: This test verifies that the owner of a project can update it.
4. `non-owner cannot update project`: This test verifies that a non-owner of a project cannot update it.
5. `owner can delete their project`: This test verifies that the owner of a project can delete it.
6. `non-owner cannot delete project`: This test verifies that a non-owner of a project cannot delete it.
7. `any authenticated user can create a project`: This test verifies that any authenticated user can create a new project.

The file uses the `User` model and the `Projekt` model to create fake users and projects, respectively. It also imports the `ProjektPolicy` class to verify the authorization logic for each scenario.

---

### tests/Feature/RechercheTest.php
This is a PHP file containing test cases for the Recherche functionality in a web application. The tests are written using Laravel's built-in Testing Framework and use the `test()` function to define each test case.

The tests cover various aspects of the Recherche functionality, such as:

* Authenticated user can create a research project
* Research project creation dispatches TriggerLangdockAgent job
* Research input requires eingabe field
* Project list shows only own projects
* Guest cannot access recherche page
* User can view own project detail
* User cannot view another users' project

The test cases use the `Volt` class to define the test inputs and assertions, and the `Queue` facade to mock queueing of jobs. The tests also use Laravel's built-in testing helpers for setting up the application environment and asserting on HTTP responses and database records.

---

### tests/Feature/WebhookTest.php
This is a collection of tests for the webhook functionality in a Laravel application. The tests are written using the PHPUnit framework and use the `tests` namespace to distinguish them from other types of tests.

The file consists of multiple test methods, each of which tests a specific aspect of the webhook functionality. Some of these tests check that the webhook rejects requests with invalid signatures or timestamps, while others check that it accepts valid requests and queues a task for processing.

Here are the 10 most important functions in this file:

1. `use Illuminate\Support\Facades\Config;` - This line imports the `Config` facade, which allows us to access the application's configuration settings.
2. `use Illuminate\Support\Facades\Queue;` - This line imports the `Queue` facade, which provides an interface for interacting with the Laravel queue system.
3. `test('webhook rejects request without signature', function () { ... });` - This test checks that the webhook rejects requests with missing signatures.
4. `test('webhook rejects request with invalid signature', function () { ... });` - This test checks that the webhook rejects requests with invalid signatures.
5. `test('webhook rejects request without timestamp', function () { ... });` - This test checks that the webhook rejects requests with missing timestamps.
6. `test('webhook rejects request with expired timestamp', function () { ... });` - This test checks that the webhook rejects requests with expired timestamps.
7. `test('webhook accepts request with valid signature and timestamp', function () { ... });` - This test checks that the webhook accepts requests with valid signatures and timestamps and queues a task for processing.
8. `test('webhook rejects request with missing eingabe field', function () { ... });` - This test checks that the webhook rejects requests with missing input fields.
9. `test('webhook rejects request with non-existent projekt_id', function () { ... });` - This test checks that the webhook rejects requests with non-existent project IDs.
10. `test('webhook rejects request with non-existent user_id', function () { ... });` - This test checks that the webhook rejects requests with non-existent user IDs.

---

### tests/Feature/WelcomeTest.php
This is a set of unit tests for a Laravel application. The file contains 5 tests, each test covers a specific feature or scenario of the application.

Here are the summaries of the functionalities of each test:

1. `welcome page returns successful response`: This test verifies that the welcome page (the homepage) returns a successful HTTP response (status code 200).
2. `welcome page increments visit counter`: This test checks that when the welcome page is visited, the visit counter is incremented by one. The test uses the `assertDatabaseHas` method to check that the expected number of visits is equal to the actual number of visits in the database.
3. `welcome page creates page_view record if none exists`: This test verifies that when the welcome page is visited for the first time, a new `page_view` record is created in the database with a visit count of 1. The test uses the `expect` method to check that the expected number of visits is equal to the actual number of visits in the database.
4. `guest sees login and register links`: This test verifies that a guest user can see the "Login" and "Registrieren" links on the welcome page. The test uses the `assertSee` method to check that the expected strings are present in the response body of the HTTP request.
5. `authenticated user sees dashboard link`: This test verifies that an authenticated user can see the "Dashboard" link on the welcome page. The test uses the `actingAs` method to simulate an authenticated user and checks that the expected string is present in the response body of the HTTP request.

The file also imports several classes:

* `App\Models\PageView`: This class represents the `page_view` model, which is used to store information about page visits.
* `App\Models\User`: This class represents the `user` model, which is used to store information about users who are authenticated to the application.

The file also uses several external dependencies:

* The `route` function is used to generate URLs for the application's routes.
* The `get` method is used to send an HTTP GET request to the server.
* The `assertStatus` method is used to check that the response status code matches a certain value.
* The `assertSee` method is used to check that a certain string is present in the response body of the HTTP request.
* The `assertDatabaseHas` method is used to check that a certain condition is true for a record in the database.
* The `expect` method is used to check that two values are equal.

---

### tests/Feature/Auth/AuthenticationTest.php
This file contains unit tests for the authentication functionality of a Laravel application. The test file uses the PHPUnit framework to write and run individual tests, which check the behavior of the application's login screen, authentication process, and logout feature.

The `use` statement imports the necessary classes and models needed for testing, such as `App\Models\User`, `Laravel\Fortify\Features`, and other relevant classes.

The tests themselves are written in a functional testing style, using the `test()` function provided by PHPUnit to define individual test cases. Each test case includes a set of assertions that verify specific behaviors or outcomes. For example, the first test checks whether the login screen can be rendered successfully, while the second test authenticates a user using the login screen and verifies that they are redirected to the dashboard after successful authentication.

The tests also include some error handling and guard clauses to ensure that the application behaves as expected in certain situations. For instance, the third test checks whether an invalid password results in an error message being displayed, while the fourth test verifies that a user with two-factor authentication enabled is redirected to the two-factor challenge page upon attempting to log in.

Finally, the last test case ensures that the logout feature works correctly by logging out a user and verifying that they are no longer authenticated.

---

### tests/Feature/Auth/EmailVerificationTest.php
This file contains tests for the email verification feature in a Laravel application. The tests are written using the PHPUnit testing framework and use the `test` method to define each test case.

The first test, `email verification screen can be rendered`, creates an unverified user and then attempts to render the email verification notice screen. It asserts that the response has a status code of 200 (OK).

The second test, `email can be verified`, creates an unverified user and generates a temporary signed URL for verifying their email address using the `URL::temporarySignedRoute` method. The test then simulates a GET request to this URL and asserts that the `Verified` event is dispatched, which means that the user's email address has been verified. It also checks that the user's `hasVerifiedEmail()` method returns true and that the response redirects to the dashboard with a query parameter indicating that the email address has been verified.

The third test, `email is not verified with invalid hash`, creates an unverified user and generates a temporary signed URL with a wrong hash value. The test then simulates a GET request to this URL and asserts that the user's `hasVerifiedEmail()` method returns false.

The fourth test, `already verified user visiting verification link is redirected without firing event again`, creates a verified user and generates a temporary signed URL for verifying their email address using the same `URL::temporarySignedRoute` method as in the second test. The test then simulates a GET request to this URL, which should not trigger the `Verified` event because the user's email address is already verified. The test asserts that the user's `hasVerifiedEmail()` method returns true and that the response redirects to the dashboard with a query parameter indicating that the email address has been verified.

Overall, these tests cover different scenarios for verifying an email address in a Laravel application using temporary signed URLs. They also demonstrate how to use PHPUnit to write test cases for such functionality.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
This file appears to be a unit test for the password confirmation feature in an application. The test is using the `actingAs` method provided by Laravel's testing framework to authenticate a user and then making a GET request to the `/password/confirm` route. The test asserts that the response status code is 200, indicating that the password confirmation screen was successfully rendered.

The test file uses the `User` model provided by Laravel's authentication system to create a new user and authenticate it for the test. It also imports the `route` helper function from the `Illuminate\Support\Facades\Route` namespace, which allows the test to generate URLs to the `/password/confirm` route.

The test does not appear to have any dependencies on external libraries or APIs, other than the Laravel framework itself. The only external dependency that is used in this test is the `actingAs` method provided by the testing framework, which is used to authenticate the user for the test.

---

### tests/Feature/Auth/PasswordResetTest.php
This file contains four tests for the password reset functionality in a Laravel application.

* The first test checks that the password reset link screen can be rendered.
* The second test sends a password reset request and asserts that a notification was sent to the user's email address.
* The third test renders the password reset screen with a valid token.
* The fourth test resets the password using a valid token and checks that the session has no errors and redirects to the login page.

The file uses the `Illuminate\Auth\Notifications\ResetPassword` class to send password reset notifications, and it also imports the `User` model from the application's user table. The tests use the `Notification::fake()` method to disable notification sending for testing purposes.

---

### tests/Feature/Auth/RegistrationTest.php
Die Datei tests/Feature/Auth/RegistrationTest.php enthält zwei Tests zur Überprüfung der Registrierungs-Seite und des Registrierungsprozesses.

* Der erste Test überprüft, ob die Registrierungsseite ordnungsgemäß gerendert werden kann und ein Statuscode von 200 zurückgegeben wird.
* Der zweite Test erstellt einen neuen Benutzer und überprüft, ob der Prozess erfolgreich ist, indem er sicherstellt, dass keine Fehler aufgetreten sind und dass der Benutzer authentifiziert wurde.

Die Datei verwendet die Laravel-Funktionen `get` und `post`, um HTTP-Anfragen an den Server zu senden, um die Registrierungsseite und das Registrierungsprozess zu testen.

Es ist wichtig zu beachten, dass diese Tests nur eine begrenzte Menge von Funktionen testen können, da sie nicht alle Aspekte des Registrierungsprozesses abdecken. Es gibt jedoch eine Menge an Funktionen, die von dieser Datei getestet werden, wie zum Beispiel:

* Die Verarbeitung der Anfrage auf der Registrierungsseite und das Rendern der Seite
* Die Überprüfung der Eingaben des Benutzers (Name, E-Mail-Adresse und Passwort)
* Die Authentifizierung des Benutzers nach erfolgreichem Registrierungsprozess

Es gibt auch externe Abhängigkeiten in dieser Datei, wie zum Beispiel die Verwendung von Laravel's Routing-System für das Rendern der Registrierungsseite und das Übermitteln von Anfragen.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Die Datei `tests/Feature/Auth/TwoFactorChallengeTest.php` enthält zwei Tests für die Funktion "two factor challenge". Die erste Test-Funktion überprüft, ob der Benutzer authentifiziert ist, bevor er auf den Endpunkt "two-factor.login" zugreifen kann. Wenn dies nicht der Fall ist, wird ein Fehler ausgegeben und der Test übersprungen.

Die zweite Test-Funktion testet, ob die Funktion "two factor challenge" korrekt ausgeführt werden kann. Sie erstellt einen Benutzer und versucht, auf den Endpunkt "two-factor.login" zuzugreifen, indem sie sich mit dem Passwort "password" einloggt. Wenn dies erfolgreich ist, wird der Test bestätigt.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `App\Models\User`: Die Benutzer-Klasse, die für den Zugriff auf den Benutzer verwendet wird.
* `Laravel\Fortify\Features`: Eine statische Klasse, die zum Aktivieren oder Deaktivieren von Funktionen wie "two factor authentication" verwendet wird.
* `Tests/Feature/Auth/TwoFactorChallengeTest.php`: Die Test-Datei, in der die Test-Funktionen definiert sind.

---

### tests/Feature/Chat/BigResearchChatTest.php
Die Datei `tests/Feature/Chat/BigResearchChatTest.php` enthält Tests für die Funktionen der Big Research Chat-Komponente. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte:

* `User`: Die Benutzerklasse dient zum Erstellen von Benutzern in der Datenbank.
* `Webhook`: Die Webhook-Klasse dient zum Erstellen von Webhooks in der Datenbank.
* `ChatMessage`: Die ChatMessage-Klasse dient zum Erstellen von Nachrichten in der Datenbank.
* `Volt`: Die Volt-Klasse ist eine PHPUnit-Erweiterung, die es ermöglicht, Livewire-Komponenten zu testen.
* `Http::fake()`: Diese Funktion wird verwendet, um simulierte HTTP-Antworten in der Datenbank zu erstellen.
* `assertHasErrors(['message'])`: Diese Funktion wird verwendet, um zu überprüfen, ob ein Formular ein bestimmtes Feld überprüft hat.
* `assertDontSee()`: Diese Funktion wird verwendet, um zu überprüfen, ob eine bestimmte Zeichenfolge in der Antwort nicht vorhanden ist.
* `assertSet('loading', false)`: Diese Funktion wird verwendet, um zu überprüfen, ob ein bestimmter Wert in einem Livewire-Formular gesetzt wurde.
* `getMessages()`: Diese Funktion dient zum Abrufen von Nachrichten aus der Datenbank.

Die Tests sind für die folgenden Funktionen und Endpunkte zuständig:

* Senden einer Nachricht
* Überprüfen, ob eine Nachricht als Pflichtfeld validiert wird
* Überprüfen, ob keine Nachrichten gespeichert werden, wenn es keinen Webhook gibt
* Überprüfen, ob ein HTTP-Fehler eine Fehlermeldung erzeugt
* Überprüfen, ob eine Verbindungsexception eine Fehlermeldung erzeugt
* Löschen aller Nachrichten für einen Benutzer
* Rendern von Nachrichten in chronologischer Reihenfolge
* Überprüfen, ob keine Nachrichten angezeigt werden, wenn es keinen Webhook gibt
* Überprüfen, ob eine Nachricht maximal 10000 Zeichen lang ist.

---

### tests/Feature/Recherche/PhaseP1Test.php
{
    "file_summary": "Maximal 10-zeilige Beschreibung der Datei-Funktionen: Datei tests/Feature/Recherche/PhaseP1Test.php enthält Funktionen zur Tests von Phase P1 der Recherchebereiche in einer Webanwendung"
  }

Diese Datei enthält Tests für die Implementierung von Phase P1 der Recherchebereiche in einer Webanwendung. Die wichtigsten Klassen und Funktionen sind:

* `App\Models\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}`: Diese Klassen stellen die Modelle dar, die in Phase P1 verwendet werden.
* `App\Models\User`: Diese Klasse repräsentiert einen Benutzer der Anwendung.
* `Livewire\Volt\Volt`: Die Klasse Volt ist eine Komponenten-Komponente von Livewire und wird verwendet, um die Formular-Komponente im Test zu erstellen.
* `tests/Feature/Recherche/PhaseP1Test.php`: Diese Datei enthält die Tests für Phase P1 der Recherchebereiche in einer Webanwendung.

Die external dependencies sind:

* Imports: `use App\Models\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}; use Livewire\Volt\Volt;`
* APIs: keine
* DB-Zugriffe: `User` und `Projekt` Modelle werden verwendet, um Benutzer und Projekte in der Datenbank zu erstellen.

---

### tests/Feature/Recherche/PhaseP2Test.php
Die Datei `tests/Feature/Recherche/PhaseP2Test.php` enthält Tests für verschiedene Funktionen im Bereich der Phase P2 der Recherchebereitstellung. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte:

* `User`: Die Klasse `User` wird verwendet, um Benutzer zu erstellen und Authentifizieren.
* `Projekt`: Die Klasse `Projekt` repräsentiert ein Rechercheprojekt und enthält Informationen wie die Fragestellung, das Zielpublikum und den Autor.
* `P2ReviewTypEntscheidung`: Die Klasse `P2ReviewTypEntscheidung` enthält eine Entscheidung über den Review-Typ für ein Projekt und kann bearbeitet werden.
* `P2Cluster`: Die Klasse `P2Cluster` repräsentiert einen Cluster in einem Rechercheprojekt und enthält Informationen wie den Cluster-Label, die Treffer-Schätzung und die Relevanz.
* `P2MappingSuchstringKomponente`: Die Klasse `P2MappingSuchstringKomponente` enthält eine Zuordnung von Suchbegriffen zu Komponenten in einem Rechercheprojekt und kann bearbeitet werden.
* `P2Trefferliste`: Die Klasse `P2Trefferliste` repräsentiert eine Trefferliste für ein Rechercheprojekt und enthält Informationen wie die Datenbank, der Suchstring, die Gesamtzahl der Treffer und die Einschätzung.

Die Datei enthält Tests für die folgenden Funktionen:

* Erstellen einer Review-Entscheidung (`newRev`): Hier können Sie einen neuen Review-Typ für ein Projekt erstellen und speichern.
* Bearbeiten einer Review-Entscheidung (`editRev`): Hier können Sie eine bestehende Review-Entscheidung bearbeiten und speichern.
* Löschen einer Review-Entscheidung (`deleteRev`): Hier können Sie eine bestehende Review-Entscheidung löschen.
* Erstellen eines Clusters (`newClu`): Hier können Sie einen neuen Cluster erstellen und speichern.
* Bearbeiten eines Clusters (`editClu`): Hier können Sie einen bestehenden Cluster bearbeiten und speichern.
* Löschen eines Clusters (`deleteClu`): Hier können Sie einen bestehenden Cluster löschen.
* Erstellen einer Mapping-Entscheidung (`newMap`): Hier können Sie eine neue Zuordnung von Suchbegriffen zu Komponenten erstellen und speichern.
* Bearbeiten einer Mapping-Entscheidung (`editMap`): Hier können Sie eine bestehende Zuordnung von Suchbegriffen zu Komponenten bearbeiten und speichern.
* Löschen einer Mapping-Entscheidung (`deleteMap`): Hier können Sie eine bestehende Zuordnung von Suchbegriffen zu Komponenten löschen.
* Erstellen einer Trefferliste (`newTref`): Hier können Sie eine neue Trefferliste erstellen und speichern.
* Bearbeiten einer Trefferliste (`editTref`): Hier können Sie eine bestehende Trefferliste bearbeiten und speichern.
* Löschen einer Trefferliste (`deleteTref`): Hier können Sie eine bestehende Trefferliste löschen.

---

### tests/Feature/Recherche/PhaseP3Test.php
Die Datei enthält Funktionen zur Verwaltung der Datenbankmatrix, Disziplin, Geografischen Filter und Grauer Literatur für das Projekt. Es gibt Tests zur Überprüfung der Formulare und des Saves der einzelnen Bestandteile.

---

### tests/Feature/Recherche/PhaseP4Test.php
Diese Datei enthält Funktionen zur Verwaltung von Suchstrings und Anpassungsprotokollen für das Phase-P4-Projekt. Die wichtigsten Klassen und Funktionen sind: P4Suchstring (modell), P4ThesaurusMapping (modell), P4Anpassungsprotokoll (modell), suchstringErstellen (controller-Funktion), thesaurusErstellen (controller-Funktion), anpassungErstellen (controller-Funktion), suchstringBearbeiten (controller-Funktion), thesaurusBearbeiten (controller-Funktion), anpassungBearbeiten (controller-Funktion), suchstringLöschen (controller-Funktion), thesaurusLöschen (controller-Funktion), anpassungLöschen (controller-Funktion). Es gibt auch Import-Abhängigkeiten für die Verwendung von Livewire und der Verbindung zu einer Datenbank.

---

### tests/Feature/Recherche/PhaseP5Test.php
Die Datei enthält Tests für eine Livewire-Komponente namens `Recherche.PhaseP5`, die zum Bearbeiten und Verwalten von Prisma-Zahlen, Screening-Kriterien und Tool-Entscheidungen verwendet wird. Die Komponenten nutzt Datenbankmodelle wie `P5PrismaZahlen`, `P5ScreeningKriterium`, `P5ToolEntscheidung` und `P5Treffer`.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Recherche.PhaseP5`: Die Livewire-Komponente für das Bearbeiten und Verwalten von Prisma-Zahlen, Screening-Kriterien und Tool-Entscheidungen.
* `P5PrismaZahlen`: Das Datenbankmodell für Prisma-Zahlen.
* `P5ScreeningKriterium`: Das Datenbankmodell für Screening-Kriterien.
* `P5ToolEntscheidung`: Das Datenbankmodell für Tool-Entscheidungen.
* `P5Treffer`: Das Datenbankmodell für Treffer (Records).
* `User`: Das Datenbankmodell für Benutzer.
* `Projekt`: Das Datenbankmodell für Projekte.

Die externe Abhängigkeit ist `Livewire\Volt`, das verwendet wird, um die Livewire-Komponente zu testen. Es gibt auch einen Import von `Illuminate\Support\Facades\DB`, der verwendet wird, um Datenbankabfragen auszuführen.

---

### tests/Feature/Recherche/PhaseP6Test.php
Die Datei `tests/Feature/Recherche/PhaseP6Test.php` enthält Tests für die Funktionen der Recherche-Phase P6, die sich auf die Verarbeitung von Qualitätsbewertungen und Lückenanalysen beschränken. Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `P5Treffer`: eine Modellklasse für das Treffer-Objekt, das in der P5-Phase generiert wurde
* `P6Qualitaetsbewertung`: eine Modellklasse für die Qualitätsbewertung, die in dieser Phase erstellt wird
* `P6Luckenanalyse`: eine Modellklasse für die Lückenanalyse, die in dieser Phase erstellt wird
* `User`: eine Modellklasse für den Benutzer, der die Recherche durchführt
* `Projekt`: eine Modellklasse für das Projekt, das in der P5-Phase generiert wurde
* `RechercheController`: ein Controller für die Recherche-Phase P6, der die Funktionen für die Qualitätsbewertung und Lückenanalyse verwaltet

Die Datei enthält auch Tests für die Authentifizierung des Benutzers, um sicherzustellen, dass nur derjenige, der das Projekt durchgeführt hat, Zugriff auf die Recherche-Funktionen hat.

---

### tests/Feature/Recherche/PhaseP7Test.php
Diese Datei enthält Tests für die Funktionalität der Phase P7 einer Recherche in einer Webanwendung. Sie testet die Erstellung, Bearbeitung und Löschung von Synthese-Methode, Datenextraktion, Mustern und Konsistenz sowie der Einschätzung von Graden. Die Tests verwenden das Laravel Framework und die Livewire-Komponente für die Anwendung.

Die wichtigsten Klassen in dieser Datei sind:

* `App\Models\Recherche\Projekt`: Model für ein Projekt in der Recherchebehörde
* `App\Models\Recherche\P7SyntheseMethode`: Model für die Synthese-Methode einer Phase P7
* `App\Models\Recherche\P7Datenextraktion`: Model für die Datenextraktion einer Phase P7
* `App\Models\Recherche\P7MusterKonsistenz`: Model für die Muster und Konsistenz einer Phase P7
* `App\Models\Recherche\P7GradeEinschaetzung`: Model für die Einschätzung von Graden einer Phase P7

Die wichtigsten Funktionen in dieser Datei sind:

* `newSm()`: Erstellt eine neue Synthese-Methode und zeigt das Formular an.
* `editSm(id)`: Bearbeitet eine bestehende Synthese-Methode und zeigt das Formular an.
* `saveSm()`: Speichert die Änderungen an einer Synthese-Methode und aktualisiert die Datenbank.
* `deleteSm(id)`: Löscht eine Synthese-Methode aus der Datenbank.
* `newDe()`: Erstellt eine neue Datenextraktion und zeigt das Formular an.
* `editDe(id)`: Bearbeitet eine bestehende Datenextraktion und zeigt das Formular an.
* `saveDe()`: Speichert die Änderungen an einer Datenextraktion und aktualisiert die Datenbank.
* `deleteDe(id)`: Löscht eine Datenextraktion aus der Datenbank.
* `newMk()`: Erstellt ein neues Muster und zeigt das Formular an.
* `editMk(id)`: Bearbeitet ein bestehendes Muster und zeigt das Formular an.
* `saveMk()`: Speichert die Änderungen an einem Muster und aktualisiert die Datenbank.
* `deleteMk(id)`: Löscht ein Muster aus der Datenbank.
* `newGr()`: Erstellt eine neue Einschätzung von Graden und zeigt das Formular an.
* `editGr(id)`: Bearbeitet eine bestehende Einschätzung von Graden und zeigt das Formular an.
* `saveGr()`: Speichert die Änderungen an einer Einschätzung von Graden und aktualisiert die Datenbank.
* `deleteGr(id)`: Löscht eine Einschätzung von Graden aus der Datenbank.

---

### tests/Feature/Recherche/PhaseP8Test.php
The file tests/Feature/Recherche/PhaseP8Test.php contains unit tests for the P8 phase of the Recherche module in an application. It uses the Livewire Volt test utility to simulate user interactions with the application and verify that the expected results are returned.

Here's a summary of the functions in the file:

1. `use App\Models\Recherche\{Projekt, P4Suchstring, P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung, P8UpdatePlan};` : This line imports the necessary models for the tests.
2. `use App\Models\User;` : This line imports the User model, which is used in the tests to create and authenticate users.
3. `use Livewire\Volt\Volt;` : This line imports the Livewire Volt test utility, which allows for simulating user interactions with the application and verifying that the expected results are returned.
4. `test('P8: kann suchprotokoll erstellen', function () { ... });` : This test case creates a new Suchprotokoll (search protocol) in P8 phase using Livewire Volt and verifies that it is saved in the database with the correct values.
5. `test('P8: suchprotokoll validiert pflichtfelder', function () { ... });` : This test case validates that the Suchprotokoll form (create/edit) requires all mandatory fields and prevents saving if they are not filled in correctly.
6. `test('P8: kann suchprotokoll bearbeiten', function () { ... });` : This test case edits an existing Suchprotokoll and verifies that the changes are saved in the database with the correct values.
7. `test('P8: kann suchprotokoll löschen', function () { ... });` : This test case deletes an existing Suchprotokoll and verifies that it is removed from the database.
8. `test('P8: kann limitation erstellen', function () { ... });` : This test case creates a new Limitation (constraint) in P8 phase using Livewire Volt and verifies that it is saved in the database with the correct values.
9. `test('P8: limitation validiert typ', function () { ... });` : This test case validates that the Limitation form (create/edit) requires all mandatory fields and prevents saving if they are not filled in correctly.
10. `test('P8: kann limitation löschen', function () { ... });` : This test case deletes an existing Limitation and verifies that it is removed from the database.
11. `test('P8: kann reproduzierbarkeitsprüfung erstellen', function () { ... });` : This test case creates a new Reproduzierbarkeitsprüfung (reproducibility check) in P8 phase using Livewire Volt and verifies that it is saved in the database with the correct values.
12. `test('P8: reproduzierbarkeit validiert pruefpunkt', function () { ... });` : This test case validates that the Reproduzierbarkeitsprüfung form (create/edit) requires all mandatory fields and prevents saving if they are not filled in correctly.
13. `test('P8: kann reproduzierbarkeitsprüfung löschen', function () { ... });` : This test case deletes an existing Reproduzierbarkeitsprüfung and verifies that it is removed from the database.
14. `test('P8: kann update-plan erstellen', function () { ... });` : This test case creates a new Update-Plan (update plan) in P8 phase using Livewire Volt and verifies that it is saved in the database with the correct values.
15. `test('P8: kann update-plan löschen', function () { ... });` : This test case deletes an existing Update-Plan and verifies that it is removed from the database.
16. `test('P8: fremder user bekommt 403', function () { ... });` : This test case tests that a user who is not the owner of the project cannot access the P8 phase.

---

### tests/Feature/Recherche/ProjektDetailTest.php
tests/Feature/Recherche/ProjektDetailTest.php enthält Tests für die Recherche-Projekt-Detail-Seite. Es werden Klassen und Funktionen von Livewire und dem Modell Projekte getestet, sowie die Tab-Wechsel-Funktion und das Zurück-Link.

---

### tests/Feature/Settings/AppearanceTest.php
The file "tests/Feature/Settings/AppearanceTest.php" contains tests for the appearance settings page of a web application. It is written in PHP and uses the Livewire framework for testing purposes. The tests are defined using the `test` function provided by the `Livewire\Volt\Volt` class, which allows to write test cases that can be executed against a specific Volt template or a controller action.

The file contains several test cases:

1. The first test case checks that the appearance settings page renders all three buttons with the "data-mode" attribute, which is used to set the user's preferred appearance mode. It also checks that the container element for the appearance toggle has the correct ID attribute.
2. The second test case checks that the appearance settings page renders a client-side script that can be used to save the user's preferred appearance mode in local storage.
3. The third test case checks that the appearance settings page is only accessible for authenticated users, and redirects unauthenticated users to the login page when trying to access it.
4. The fourth test case checks that the appearance settings page can be accessed successfully and displays the correct page content.

Overall, this file provides a comprehensive set of tests that ensure the functionality and behavior of the appearance settings page is working as expected in a real-world application.

---

### tests/Feature/Settings/PasswordUpdateTest.php
This is a PHP file containing several test cases for the `settings.password` Livewire component. The tests are written using the `test()` function from the `Livewire\Testing\TestableLivewire::class` trait, which allows to write test cases as if they were methods on the component class.

The first test case (`test('password can be updated')`) verifies that a user's password can be updated successfully when providing the correct current password and new password. The test sets up a user model with a password hashed using the `Hash::make()` method, creates an instance of the component using the `actingAs()` method to log in as the user, sets the current password and new password on the component, and calls the `updatePassword` method. The test then asserts that there are no errors and verifies that the updated password matches the new password by checking if it is hashed properly using the `Hash::check()` method.

The second test case (`test('correct password must be provided to update password')`) verifies that an error is thrown when trying to update a user's password with an incorrect current password. The test sets up the same user model as before, creates an instance of the component, and sets the current password and new password on the component. However, it provides an incorrect current password in the `current_password` field, which causes the `updatePassword` method to throw an error. The test then asserts that there is an error on the `current_password` field by checking if it has been added to the list of errors.

The file also imports several external dependencies:

* `App\Models\User`: A user model class used for testing.
* `Illuminate\Support\Facades\Hash`: A facade for the `Hash` helper function, which is used to hash and verify passwords.
* `Livewire\Volt\Volt`: The Volt component, which is used to render the `settings.password` Livewire component.

---

### tests/Feature/Settings/ProfileUpdateTest.php
This is a set of tests for the user profile page in a Laravel application. The tests are written using the Livewire framework and the Volt testing API.

The file contains four tests:

1. `profile page is displayed`: This test ensures that the profile page is displayed correctly when accessed by an authenticated user.
2. `profile information can be updated`: This test updates the user's profile information and checks that the changes have been saved successfully.
3. `email verification status is unchanged when email address is unchanged`: This test ensures that the email verification status of the user remains unchanged even if their email address is not changed.
4. `user can delete their account`: This test allows the user to delete their own account by providing the correct password.

The tests use the `actingAs` method provided by Livewire to simulate an authenticated user, and the `set`, `call`, and `assertHasNoErrors` methods to set and call the appropriate actions on the form, and check for any errors that may occur during the test. The `expect` function is used to make assertions about the state of the application after the tests have run.

The file also imports the `User` model and uses the `factory` method provided by Laravel to create a new user for each test. This allows each test to be run independently, without interfering with any other tests that may be running at the same time.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
The file `tests/Feature/Settings/TwoFactorAuthenticationTest.php` appears to be a test suite for the two-factor authentication feature in a Laravel application. The tests included in this file cover various scenarios related to enabling and disabling two-factor authentication, as well as ensuring that the feature works correctly when certain conditions are met.

The first few lines of the file import various dependencies and define a `beforeEach` callback function that is run before each test. The callback checks if the two-factor authentication feature is enabled in the application and marks the test as skipped if it is not.

The next several tests check different aspects of the two-factor authentication feature, such as rendering the settings page, requiring password confirmation when enabling two-factor authentication, and disabling two-factor authentication when it is disabled. The final test checks that when a user abandons the confirmation process between requests, the two-factor authentication is disabled.

The tests use Laravel's built-in testing features, such as `actingAs()` to simulate a logged-in user and `withSession()` to set up a session for the test. They also use the `Volt::test()` function to render the component that represents the settings page for two-factor authentication.

---

### tests/Feature/Settings/WebhooksTest.php
The file `tests/Feature/Settings/WebhooksTest.php` contains a set of tests for the webhooks feature in a Laravel application. The test class uses the `uses()` method to import the `RefreshDatabase` trait, which allows it to refresh the database between each test.

In the `beforeEach()` method, a new user is created using the `User::factory()->withoutTwoFactor()->create()` method, and the test is set up to use the current authenticated user by calling the `actingAs()` method with the created user as its argument.

The test class then defines several test methods for different scenarios involving webhooks:

1. Tests that saving a new webhook creates a new record in the database and sets the correct state variables.
2. Tests that updating an existing webhook updates the corresponding record in the database and sets the correct state variables.
3. Tests that validating the URL is required for saving a webhook.
4. Tests that validating the URL format is enforced when saving a webhook.
5. Tests that validating the URL length is enforced when saving a webhook.
6. Tests that deleting a webhook removes the corresponding record from the database and sets the correct state variables.
7. Tests that loading existing webhooks sets the correct state variables for the dashboard and recherche webhooks.
8. Tests that no access is granted to other users' webhooks when clearing a webhook.

---

### tests/Unit/ExampleTest.php
This file contains a unit test for the 'true' value using the PHPUnit testing framework. The test checks that true is indeed true.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
This is a PHP class file that defines a custom page for the `ContactResource` in Filament, specifically the list of contacts. The class inherits from the `ListRecords` class provided by Filament and overrides some of its methods to customize the behavior.

The file defines two classes:

1. `App\Filament\Resources\ContactResource\Pages\ListContacts`, which is a subclass of `ListRecords`. This class is responsible for rendering the list of contacts in the Filament UI.
2. `App\Filament\Resources\ContactResource`, which is the resource that contains the contact data.

The file imports the `ContactResource` class using the `use` keyword, which allows it to use the fully qualified name of the class within its own code. This is necessary because the `ListRecords` class is not defined in the same namespace as the custom page class.

The `ListContacts` class defines a static property called `$resource` that refers to the `ContactResource` class. This property is used by Filament to determine which resource to use when displaying the list of contacts.

Overall, this file provides a way to customize the behavior of the Filament list page for the `ContactResource`. It allows developers to define custom functionality and layout for the list page, while still leveraging the existing infrastructure provided by Filament.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
This file is a PHP class that defines a page for viewing a record of the `ContactResource` resource in the Filament admin panel. The page extends the `Filament\Resources\Pages\ViewRecord` class, which provides a basic implementation of the view page for a resource.

The important parts of this file are:

* The namespace declaration: This defines the namespace for the file, which is `App\Filament\Resources\ContactResource\Pages`.
* The use statements: These import the necessary classes and namespaces to use them in the file. In this case, they import the `Filament\Resources\Pages\ViewRecord` class, which provides a basic implementation of the view page for a resource, and the `App\Filament\Resources\ContactResource` class, which defines the resource being viewed.
* The class declaration: This declares the class that defines the view page for the contact resource. It extends the `ViewRecord` class from Filament and overrides some of its methods to customize the behavior of the page.
* The `$resource` property: This is a public property that specifies the name of the resource being viewed. In this case, it is set to `App\Filament\Resources\ContactResource`.

Overall, this file defines a simple view page for a contact resource in Filament. It extends the basic functionality provided by the `ViewRecord` class and customizes it slightly to provide a more tailored view experience for the user.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
This file is a PHP class that extends the `EditRecord` page provided by the Filament PHP framework. It is used to edit an existing user record in the application.

The class is named `EditUser` and it belongs to the `Pages` namespace. It uses the `UserResource` resource to interact with the user data.

This file does not import any external dependencies or classes, but it uses the `Filament\Resources\Pages\EditRecord` base class that provides a basic implementation for editing records in Filament.

The only important method in this class is the `__construct()` method, which initializes the page by setting the `$resource` property to the `UserResource` class. This is used to specify which resource should be used to manage the user data.

In summary, this file provides a simple way to edit an existing user record in the application using the Filament framework.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
File Summary:

* This file is part of the Filament framework for building web applications.
* It contains a class named `ListUsers` that extends `Filament\Resources\Pages\ListRecords`, which is a pre-built page in the Filament framework that allows users to list and search through records in a resource.
* The class is namespaced under `App\Filament\Resources\UserResource\Pages` and uses the `App\Filament\Resources\UserResource` class as its `$resource`.
* This page can be accessed by visiting `/filament/users` in the URL.

---

### resources/views/dashboard.blade.php
The file resources/views/dashboard.blade.php appears to be a Laravel Blade template that renders the dashboard page for a user. The main content of the page is displayed in the left column, while the right column contains a livewire component named BigResearchChat.

Here are some key functions and classes in this file:

* `x-layouts.app`: This is a Blade layout component that defines the overall structure and layout of the dashboard page. It includes the header, navigation bar, and footer sections, as well as the content area where the main content is displayed.
* `auth()->user()`: This is a Laravel auth() method call that retrieves the current user object based on their authentication state. The user object contains information such as their name, email, and other details.
* `\App\Models\Contact`: This is a model class representing the contact table in the database. It provides methods for fetching, creating, updating, and deleting contacts from the database.
* `can('viewAny', \App\Models\Contact::class)`: This is a Laravel authorization check that checks whether the current user has the necessary permissions to view any contacts. The `viewAny` permission is defined in the `contact` model file.
* `\App\Models\Recherche\Projekt`: This is a model class representing the projekt table in the database. It provides methods for fetching, creating, updating, and deleting projects from the database.
* `str()->limit($projekt->titel, 80)`: This is a string helper method provided by Laravel that truncates a string to a specified length (in this case, 80 characters). The `titel` property of the `$projekt` object contains the project title.
* `route('recherche.projekt', $projekt)`: This is a Laravel route() method call that generates a URL for accessing the project details page. The `recherche.projekt` route name is defined in the `web.php` file.
* `Livewire`: This is a Laravel Livewire component that provides real-time updates to the dashboard page, such as updating the list of recent projects or displaying new chat messages.

Overall, this Blade template appears to be responsible for rendering the dashboard page for a user and providing a variety of UI elements, including contact management and chat functionality.

---

### resources/views/welcome.blade.php
This file appears to be a Blade template for the landing page of a website. The file includes HTML, CSS, and JavaScript code, as well as Vite imports for the CSS files. The file also contains some PHP code that includes authentication logic and links to other parts of the site.

The main function of this file is to display a hero section with a title and subtitle, along with a call-to-action button for visitors to contact you or visit your website. It also includes a tagline about the focus of your services, which is on providing real-time telemetry data to your customers.

The file also contains some basic JavaScript code that uses Vite to import CSS files and creates a menu toggle button that allows visitors to open and close the menu. The file also contains some PHP code for authentication logic and links to other parts of the site.

---

### resources/views/components/action-message.blade.php
This is a Laravel Blade template for displaying an action message to the user. The template has several key features:

1. It uses the `x-data` directive to initialize some variables and set up an event listener that listens for the specified `$on` event.
2. When the event is triggered, it clears any existing timeout and sets a new one to hide the message after 2 seconds.
3. The template uses the `x-show.transition.out.opacity.duration.1500ms` directive to show the message with an opacity transition over a period of 1.5 seconds.
4. It also uses the `x-transition:leave.opacity.duration.1500ms` directive to hide the message with an opacity transition over the same period of time.
5. The template accepts several attributes, including a class attribute that is merged with the default `'text-sm'` value.
6. It has a slot for customizing the message content. If no slot is provided, it will display the default "Saved." message.

Overall, this is a simple and concise template that allows developers to easily display action messages to their users in a Laravel application.

---

### resources/views/components/app-logo-icon.blade.php
This is a Blade template for an SVG icon that displays the logo of a company. The template uses the `viewBox` attribute to define the dimensions of the icon and the `fill` attribute to set the color of the icon. The template also includes some nested tags, such as `path` and `g`, which are used to define the shape of the icon.

The template takes an `$attributes` variable as a parameter, which is used to pass any additional attributes that may be needed for the SVG element. For example, if the template is being used in a context where the width or height of the icon needs to be set, the `$attributes` variable can be used to set those properties.

The template does not include any external dependencies or APIs, but it does import the `Illuminate\Support\Facades\View` facade, which is necessary for using Blade templates in Laravel applications.

---

### resources/views/components/app-logo.blade.php
Die Datei resources/views/components/app-logo.blade.php enthält eine Laravel-View, die ein Logo der Applikation darstellt. Die View verwendet das Blade-Templating-System von Laravel und definiert einen flexiblen Container mit einem runden Hintergrund, der das Logo des Kernels und eine Beschriftung enthält. Der Container hat eine Größe von 8 Einheiten und ist in einer Spalte unterteilt, sodass die Beschriftung nach rechts ausgerichtet wird.

---

### resources/views/components/auth-header.blade.php
File Summary: This file contains a reusable UI component for an authentication header. It takes in two props, title and description, and displays them as an h1 and p tag respectively. The component is written using the Blade templating engine and utilizes Tailwind CSS classes to style the elements. There are no external dependencies or APIs used in this file.

---

### resources/views/components/auth-session-status.blade.php
This is a Blade component that displays an authentication session status message. It takes in a single prop named 'status' and conditionally renders it if it exists, using the font-medium text-sm text-green-600 CSS classes.

---

### resources/views/components/placeholder-pattern.blade.php
This is a Blade component file for creating a placeholder pattern in SVG format. It contains the following functions:

* `props`: A function that defines properties for the component, including an `id` property which is used to generate a unique ID for the pattern.
* `<svg>`: An SVG element that defines the pattern as a whole.
* `<defs>`: An SVG element that defines definitions for the pattern.
* `<pattern>`: An SVG element that defines a pattern within the `<defs>` element.
* `<path>`: An SVG element that defines a path within the `<pattern>` element.
* `{{ $attributes }}`: A Blade expression that allows the component to accept any attributes passed from the parent component.
* `width` and `height`: Attributes that define the width and height of the pattern.
* `stroke-width`: An attribute that defines the stroke width of the path.

This file is part of the UI category, as it deals with the visual representation of a placeholder pattern in SVG format. It uses the Blade template engine to generate the SVG code dynamically based on the properties passed from the parent component.

---

### resources/views/components/layouts/app.blade.php
This is a Blade template file for a layout component that includes a sidebar and main content section.

---

### resources/views/components/layouts/auth.blade.php
* Die Datei `resources/views/components/layouts/auth.blade.php` ist eine Blade-View, die als Authentifizierungs-Layout verwendet wird und es ermöglicht Benutzern die Anmeldung bei der Anwendung.
* Die View verwendet den Namespace `x-layouts.auth.simple`, der auf dem Laravel-Framework basiert.
* Die View enthält einen Slot, der durch den Wert von `$slot` ersetzt wird und die Hauptinhalte der Authentifizierungs-Seite darstellt.
* Die Variable `$title` wird verwendet, um eine Titelzeile für die Authentifizierungs-Seite anzugeben. Falls nicht definiert, wird der Wert null verwendet.
* Es gibt keine externen Abhängigkeiten in dieser View, da sie direkt auf das Laravel-Framework basiert und keine APIs oder Datenbank-Zugriffe benötigt.

---

### resources/views/components/layouts/app/header.blade.php
The code you provided is a Blade template for the header section of a web application. It includes several components such as a logo, navigation menu, and user information. The `x-data` directive creates a new component with a reactive context, which allows you to use Vue directives in your HTML.

Here's a breakdown of the code:

1. `<header>` element: This is the root element of the header section. It has a fixed position (`position:fixed`), a top margin of 0px, and a left margin of 0px to make sure it covers the entire width of the page.
2. `<div>` element with `class="flex justify-between px-4 py-6"`: This is a flexbox container that holds the logo, navigation menu, and user information. The `justify-between` class ensures that these elements are evenly spaced across the available width of the header section.
3. `<div>` element with `class="flex items-center" x-data="{ mobileNav: false }"`: This is a flexbox container that holds the navigation menu and user information. The `x-data` directive creates a new component with a reactive context, which allows you to use Vue directives in your HTML. The `mobileNav` variable is used to control the visibility of the mobile sidebar.
4. `<div>` element with `class="flex items-center" x-show="mobileNav"`: This is a flexbox container that holds the navigation menu and user information for small screens (less than 768px wide). The `x-show` directive is used to conditionally render the contents of this element based on the value of the `mobileNav` variable.
5. `<button>` element with `@click="mobileNav = false"`: This button is used to close the mobile sidebar when a user clicks on it.
6. `<a>` element with `href="#" class="flex items-center py-2 px-3 hover:text-gray-600 dark:hover:text-gray-300"`: This is a link that takes users to the home page of the web application when they click on it.
7. `<span>` element with `class="text-xl text-zinc-900 font-bold tracking-tight"`: This element displays the name of the web application as a heading.
8. `<nav>` element with `class="flex items-center px-3 space-x-2 lg:space-x-4"`: This is a flexbox container that holds the navigation menu for small screens (less than 768px wide). The `px-3` class adds left and right padding to the element, while the `lg:space-x-4` class ensures that the elements in this container are evenly spaced across the available width of the header section.
9. `<a>` element with `href="#" class="hover:text-gray-600 dark:hover:text-gray-300"`: This is a link that takes users to the home page of the web application when they click on it. The `hover` class is used to change the color of the text when users hover over it with their mouse.
10. `<a>` element with `href="#" class="hover:text-gray-600 dark:hover:text-gray-300"`: This is another link that takes users to the home page of the web application when they click on it. The `hover` class is used to change the color of the text when users hover over it with their mouse.
11. `<a>` element with `href="#" class="hover:text-gray-600 dark:hover:text-gray-300"`: This is yet another link that takes users to the home page of the web application when they click on it. The `hover` class is used to change the color of the text when users hover over it with their mouse.
12. `<div>` element with `class="flex items-center py-4 lg:py-0"`: This is a flexbox container that holds the user information for small screens (less than 768px wide). The `py-4` class adds top and bottom padding to the element, while the `lg:py-0` class ensures that there is no padding on large screens.
13. `<div>` element with `class="text-sm text-zinc-500 dark:text-zinc-400"`: This element displays the user's email address as a secondary heading.
14. `<button>` element with `@click="mobileNav = false"`: This button is used to close the mobile sidebar when a user clicks on it.
15. `<a>` element with `href="#" class="flex items-center py-2 px-3 hover:text-gray-600 dark:hover:text-gray-300"`: This is a link that takes users to the home page of the web application when they click on it. The `hover` class is used to change the color of the text when users hover over it with their mouse.
16. `<span>` element with `class="text-xl text-zinc-900 font-bold tracking-tight"`: This element displays the name of the web application as a heading.

---

### resources/views/components/layouts/app/sidebar.blade.php
This is a Blade template that generates the HTML markup for the sidebar of the Livewire Starter Kit. The sidebar contains several links to different parts of the application, including the homepage, the repository, and the documentation.

The template uses the `@auth` directive to check if the user is authenticated, and if so, it displays a link to the profile edit page. If the user is not authenticated, it displays a message indicating that the user must log in to access the application.

The `initials` method used in the template is a custom helper function defined in the `App\Helpers\UserHelper` class, which returns the initials of the user's name based on their ID. This function is called inside the `auth()->user()` call to display the initials of the user's name next to their profile picture in the sidebar menu.

---

### resources/views/components/layouts/auth/card.blade.php
The file `resources/views/components/layouts/auth/card.blade.php` is a Blade template that creates a card-like layout for the authentication page. It includes several important classes, functions, and endpoints:

* `resources`: This directory contains all the resources needed by the application, including templates, images, stylesheets, and JavaScript files.
* `views`: This directory contains all the views used in the application, which are HTML templates that define the layout and structure of the pages.
* `components`: This directory contains all the components used in the application, which are reusable pieces of UI code that can be included in multiple views.
* `layouts`: This directory contains all the layouts used in the application, which define the overall structure and layout of each page.
* `auth`: This directory contains all the authentication-related views and components, including the `card` component used here.
* `app()->getLocale()`: This function is used to get the current locale (language) of the application, which is stored in the session or cookie.
* `str_replace('_', '-', app()->getLocale())`: This function is used to replace underscores with hyphens in the current locale string, which is necessary for some languages that use hyphens instead of underscores as word separators.
* `config('app.name', 'Laravel')`: This function is used to get the name of the application from the configuration file, which is stored in the `.env` file. If no value is set for the `APP_NAME` environment variable, it defaults to "Laravel".
* `@include('partials.head')`: This includes a Blade partial named `head`, which defines the `<head>` section of the HTML page.
* `@livewireScriptConfig`: This is a Livewire directive that configures the Livewire script for the current page.
* `max-w-md`: This is a Tailwind CSS class that sets the maximum width of the element to 1200 pixels, which is the default value for the `md` breakpoint in Tailwind CSS.
* `gap-6 md:gap-8`: These are Tailwind CSS classes that set the gap (padding) between elements to 6 pixels on small screens and 8 pixels on medium screens.
* `dark:bg-linear-to-b dark:from-neutral-950 dark:to-neutral-900`: These are Tailwind CSS classes that define a gradient background color for the element, with a dark color scheme. The `dark` class is used to enable the dark mode theme, and the `bg-linear-to-b` class is used to set the background color to a linear gradient that transitions from the `from` color to the `to` color on the bottom edge of the element.
* `font-medium`: This is a Tailwind CSS class that sets the font size and style to medium, which is a good balance between legibility and readability.
* `wire:navigate`: This is a Livewire directive that navigates to a new page when clicked, passing along any necessary data or parameters as needed.
* `wire:livewireScriptConfig`: This is a Livewire directive that configures the Livewire script for the current page, which includes the necessary JavaScript code for Livewire to work properly.

---

### resources/views/components/layouts/auth/simple.blade.php
This is a Blade template file for an authentication layout in a Laravel application. The file includes the following elements:

* `<!DOCTYPE html>` - Defines the document type of the HTML document.
* `<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" class="dark">` - Defines the root element of the HTML document, including the language and theme classes (dark).
* `<head>` - The head section of the HTML document, which includes meta tags and other information.
	+ `@include('partials.head')` - Includes a partial Blade file named "head" from the "partials" directory.
* `<body class="min-h-screen bg-white antialiased dark:bg-linear-to-b dark:from-neutral-950 dark:to-neutral-900">` - Defines the body section of the HTML document, including the background color and theme classes.
	+ `min-h-svh flex min-h-svh flex-col items-center justify-center gap-6 p-6 md:p-10` - Sets the minimum height of the body to "screen" (the full screen), sets the background color to white, and adds theme classes for a dark theme.
	+ `bg-background` - Defines the background color as "background".
	+ `<div class="bg-background flex min-h-svh flex-col items-center justify-center gap-6 p-6 md:p-10">` - Defines a div element with the background and theme classes.
* `<div class="flex w-full max-w-sm flex-col gap-2">` - Defines a div element with flex layout, maximum width of "sm", and column gap of "2".
	+ `<a href="{{ route('home') }}" class="flex flex-col items-center gap-2 font-medium" wire:navigate>` - Defines an anchor tag with the href attribute set to the "home" route, the flex layout classes, and a click event that navigates to the home page.
	+ `size-9` - Sets the size of the app logo icon to "9".
	+ `fill-current text-black dark:text-white` - Sets the color of the app logo icon to black (or white for a dark theme).
	+ `<x-app-logo-icon class="size-9 fill-current text-black dark:text-white" />` - Renders the app logo icon with the size and color classes.
* `{{ $slot }}` - Defines a slot element that renders any content passed to it (e.g., the auth form).
* `@include('cookie-consent::index')` - Includes a partial Blade file named "index" from the "cookie-consent" directory.
* `@livewireScripts` - Renders the Livewire scripts.

Overall, this layout provides a simple and minimalistic authentication form for the application. The layout includes a header with an app logo icon, a centered auth form, and a footer with a cookie consent message. The theme classes are used to define the appearance of the elements in a dark or light mode.

---

### resources/views/components/layouts/auth/split.blade.php
This is a Laravel Blade template file for an authentication layout. The template uses the `str` helper function from the `Illuminate\Support\Str` class to generate random inspiring quotes and display them in a blockquote element on the page. The template also includes several other elements, such as a logo icon, a navigation link to the homepage, and a cookie consent message using the `cookie-consent` package.

The file defines two main sections:

1. The `<head>` section, which contains the HTML head element and includes the necessary meta tags for the page, such as the charset, viewport, and title. It also includes the `partials/head` partial, which is a separate Blade template that defines the `<meta>` and `<link>` elements used in the app.
2. The `<body>` section, which contains the main content of the page, including the logo icon, navigation link to the homepage, and the inspiring quote blockquote element. It also includes the `cookie-consent` package's index file, which displays a message informing users that the app uses cookies to improve their experience.

The template makes use of several external dependencies, including the `str` helper function from the `Illuminate\Support\Str` class, the `wire:navigate` directive for livewire, and the `cookie-consent` package.

---

### resources/views/components/settings/layout.blade.php
This file is a Blade component that displays the settings layout for an application. It uses Laravel's built-in navigation system to provide links to different settings pages. The component is using the `wire:navigate` directive, which allows users to navigate between settings pages without refreshing the page.

The component is structured in a way that it takes up all available space on the screen and divides it into two parts: a navigation menu and the main content area. The navigation menu displays links to different settings pages, while the main content area displays the actual settings form for each page.

Inside the component, there are several classes and functions used to handle the navigation between settings pages. For example, `route('profile.edit')` returns a URL for the profile settings page, `request()->routeIs('profile.edit')` checks if the current route is the profile settings page, and `{{ __('Profile') }}` displays the translated text for the "Profile" label.

The component also uses some external dependencies, such as Laravel's Fortify package to handle two-factor authentication and Laravel's Jetstream package to handle webhooks. These dependencies are imported using the `@php` directive at the top of the file.

Overall, this component provides a user-friendly interface for managing settings in an application, while also allowing users to navigate between different settings pages without refreshing the page.

---

### resources/views/emails/contact-confirmation.blade.php
Die Datei `resources/views/emails/contact-confirmation.blade.php` ist eine HTML-Seite, die als E-Mail-Vorlage verwendet wird, um Anfragen von Kunden an das Linn Games Team zu senden. Die Datei enthält eine HTML-Seite mit einem einfachen Layout und einer kurzen Nachricht für den Empfänger.

Hier sind die wichtigsten Funktionen der Datei:

* Es wird eine HTML-Seite erstellt, die als E-Mail-Vorlage verwendet werden kann.
* Die Seite enthält eine kurze Nachricht an das Linn Games Team und eine Zusammenfassung der Anfrage des Benutzers.
* Die Seite verwendet die `@if`-Anweisung, um die Darstellung des Zeitrahmen-Feldes basierend auf dem Wert des Feldes `timeline` abzuleiten.
* Es gibt einen Import von `sans-serif`, was eine Schriftart für die Anzeige der Texte in der Seite angibt.
* Die Seite verwendet die `@`-Syntax, um Variablen wie `$contact->name` und `$contact->message` zu definieren und darin einzubinden.

Die Datei hat keine externen Abhängigkeiten, da sie nur HTML-Tags und -Attribut verwendet.

---

### resources/views/emails/contact-inquiry.blade.php
This file is a Blade template for an HTML email that is used to display contact inquiries on a website. The template uses Laravel's Blade syntax and includes several variables that are passed in from the controller, such as `$contact->name`, `$contact->company`, and `$contact->message`.

The table in the email displays the following information:

* Name
* Company (if provided)
* E-Mail address
* Project type
* Timeline (if provided)
* Message
* Date and time the inquiry was received

The email is sent to a recipient who has been designated as the contact person for handling inquiries on the website. The purpose of this file is to provide a standardized way of displaying contact inquiries in an easily readable format, allowing the recipient to quickly identify and respond to each inquiry individually.

The file includes several external dependencies, such as Laravel's Blade engine and CSS styles for formatting the email. However, it does not include any database queries or other complex logic, making it a relatively simple and straightforward piece of code that can be easily modified and extended as needed.

---

### resources/views/emails/deploy-notification.blade.php
Diese Datei enthält eine E-Mail-Benachrichtigung, die automatisch gesendet wird, wenn ein neues Release von <strong>{{ config('app.name') }}</strong> deployed wurde. Die Benachrichtigung enthält Details zum Deployment, wie den Zeitpunkt, die Umgebung und die URL des neuen Releases.

---

### resources/views/layouts/legal.blade.php
The file resources/views/layouts/legal.blade.php is a Blade template that serves as the layout for legal documents on the Linn Games website. It includes the following features:

1. A header with a navigation menu, including links to the start page, data protection policy, terms of use, and impressum.
2. A main content area where the actual legal document will be displayed using the @yield('content') directive.
3. A footer with copyright information and links to additional pages on the website.
4. Some CSS styles are defined in the <style> tag, including variables for colors, font sizes, and borders.
5. A script element includes a simple JavaScript function that updates the year displayed in the footer using the new Date().getFullYear() method.

The file is written in HTML with some Blade syntax used for template inheritance and content display. It uses the Laravel framework's blade engine to process PHP code and display dynamic content.

---

### resources/views/legal/agb.blade.php
This file contains the legal information and terms of use for the Linn.Games website and its associated online game, including rules for the Multiplayer and communication features, digital purchases and returns, haftung and garantie, sanctions for violations, freistellung by the user, run time, cancellation, and lastly, closing provisions.

---

### resources/views/legal/dsgvo.blade.php
Dies ist eine Blade-Datei für Laravel, die eine Datenschutzerklärung enthält. Hier sind die wichtigsten Punkte zusammengefasst:

* Die Datei dient als Teil der Website und des Spiels "linn.games".
* Die Verarbeitung von personenbezogenen Daten erfolgt auf der Basis der Datenschutzgesetze (insbesondere DSGVO), insbesondere für den Vertragserfüllungs-Zweck und zur Sicherstellung des technischen Betriebs.
* Es werden verschiedene Arten von personenbezogenen Daten verarbeitet, darunter Registrierungs- und Kontodaten, Authentifizierungsdaten, Spieldaten und Kommunikationsinhalte sowie Nutzungs- und Verbindungsdaten.
* Die Datenverarbeitung erfolgt zum Zweck der Bereitstellung von Website und Spiel (Vertragserfüllung), Kommunikation und Support, Sicherheit und technische Protokollierung, Keine Werbung oder Profiling ohne Einwilligung und für die Kontrolle des Datenschutzrechtes.
* Die Daten werden gespeichert und gelöscht, je nachdem ob es sich um Registrierungs- und Profildaten handelt oder ob es sich um Nutzungs- und Log-Daten handelt.
* Betroffene können sich an uns wenden, um Rechte auszuüben, die im Datenschutzgesetz (insbesondere DSGVO) vorgesehen sind.
* Wir nehmen den Schutz Ihrer personenbezogenen Daten ernst und treffen umfassende technische und organisatorische Maßnahmen, um ein angemessenes Schutzniveau zu gewährleisten.
* Unsere Website verwendet Cookies ausschließlich in technisch notwendigem Umfang. Wir verwenden keine Tracking- oder Werbe-Cookies.
* Unser Hosting wird auf einem selbst betriebenen Server im Inland durchgeführt, ohne dass externe Hosting-Provider eingesetzt werden. Einige Drittdienste werden verwendet, darunter Langdock für die KI-gestützte Recherche-Funktion.

---

### resources/views/legal/impressum.blade.php
This file is a view template for the impressum page, it extends the layouts.legal blade and sets the title to 'Impressum'.

---

### resources/views/livewire/auth/confirm-password.blade.php
This file is a part of the Laravel framework's authentication system and is used to display the confirm password screen for users. The file contains a single Blade component, `x-layouts.auth`, which serves as the layout for the page. The layout includes a header with a title and description, a form to enter the user's password, and a submit button.

The important classes in this file are:

* `x-auth-header`: This is a custom Blade component that renders the header section of the authentication screen. It displays the title and description of the page, as well as any error messages or status updates.
* `x-auth-session-status`: This is another custom Blade component that renders the session status message for the current user. If there are any errors or warnings related to the user's login session, this component will display them.
* `form`: This is a standard HTML form element that allows users to enter their password and submit it to the server. The form has several input fields, including a password field and a submit button.
* `button`: This is a standard HTML button element that is used to submit the form. It displays the "Confirm" text and is styled with a dark theme using CSS classes like `bg-zinc-900` and `text-white`.

The external dependencies in this file include:

* The Laravel framework's routing system, which allows users to navigate between pages and perform various actions.
* The Laravel validation system, which is used to validate user input and ensure that the password field is not empty.
* The Laravel authentication system, which handles user login and logout processes and provides a way for users to access protected resources.

---

### resources/views/livewire/auth/forgot-password.blade.php
Die Datei `resources/views/livewire/auth/forgot-password.blade.php` enthält eine Livewire-Komponente für das Passwort-Zurücksetzen-Formular.

Die wichtigsten Funktionen dieser Datei sind:

* Das Formular zum Zurücksetzen des Passworts, das über die Route `route('password.email')` zugänglich ist und eine E-Mail mit einem Link zum Zurücksetzen des Passworts an den Benutzer sendet.
* Die Validierung der Benutzereingaben und die Anzeige von Fehlern bei Bedarf.
* Eine Verknüpfung mit der Authentifizierungs-Komponente (`x-auth-header`) für die Anzeige einer Kopfzeile und des Status der Sitzung.

Die externe Abhängigkeit ist die Livewire-Bibliothek, die das Formular darstellt und es mithilfe von JavaScript-Events und -Bindungen aktualisiert. Es gibt auch eine Verknüpfung mit der Authentifizierungs-Komponente (`x-auth-header`) für die Anzeige einer Kopfzeile und des Status der Sitzung.

Die Datei verwendet einige PHP-Funktionen wie `route()`, `__()` (für die Lokalisierung) und `@error` (um Fehler zu behandeln). Es gibt auch eine Verknüpfung mit der Authentifizierungs-Komponente (`x-auth-header`) für die Anzeige einer Kopfzeile und des Status der Sitzung.

---

### resources/views/livewire/auth/login.blade.php
This file is a part of a Livewire application, specifically in the `login` page. It defines a form for users to log in to their account using email and password. The file imports several components from Livewire, including `Layouts\Auth`, which provides the basic layout for the login page, and `auth-session-status` and `auth-header`, which display session status and header information respectively.

The form itself consists of several input fields for email address, password, and remember me option. The submit button has a data test attribute to identify it in tests. There is also an error message displayed if there are any validation errors.

The file also includes a check for the "password.request" route, which displays a forgot password link when available.

Overall, this file provides a basic login form with email and password inputs, remember me option, and a submit button to log in to an existing user account.

---

### resources/views/livewire/auth/register.blade.php
Die Datei `resources/views/livewire/auth/register.blade.php` enthält eine Registrierungs-Seite für Benutzer. Hier sind die wichtigsten Funktionen und Klassen der Datei:

* Die `<x-layouts.auth>`-Komponente wird importiert, um die Seite aufzubereiten und eine standardmäßige Authentifizierungs-Seite zu erstellen.
* Die Registrierungs-Formular wird mit dem Formular-Tag `<form method="POST" action="{{ route('register.store') }}">` erstellt. Hierbei wird der POST-Request an die Route `register.store` gesendet und die Daten werden in der Datenbank gespeichert.
* Die Benutzerdaten (Name, E-Mail, Passwort) werden über Eingabefelder eingegeben und mit den Attributen `name`, `email`, und `password` in der Formular-Abfrage definiert.
* Die Konfirmation des Passworts wird mit dem Eingabefeld `password_confirmation` erstellt.
* DerSubmit-Button zum Abschicken der Registrierungsanfrage wird mit dem Attribut `data-test="register-user-button"` markiert, damit er von den Tests ausgegeben werden kann.
* Die Authentifizierungs-Seite wird mit dem Tag `<x-layouts.auth>` aufgerüstet und enthält die Registrierungs-Formular-Abfrage.
* Die Registrierungs-Seite enthält auch einen Link zum Abmelden, der mit dem Attribut `wire:navigate` markiert ist, damit er von den Livewire-Komponenten ausgegeben werden kann.

---

### resources/views/livewire/auth/reset-password.blade.php
The given file is a Blade template for the reset password page of an application. It uses Livewire components and includes forms to handle the user input. The file imports external dependencies such as the `Auth` class from Laravel, which provides authentication functionality.

Here's a summary of the functions of this file:

* Displays the reset password page with a form for the user to enter their new password.
* Handles the submission of the form and updates the user's password using the `Auth` class from Laravel.
* Provides error handling and validation for the password fields.
* Uses Livewire components for the layout and styling of the page.
* Includes external dependencies such as the `Auth` class from Laravel.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
This file appears to be a view for a two-factor authentication (2FA) challenge form in a Livewire application. The main purpose of this file is to allow the user to input their 2FA code or recovery code and submit it for verification.

Here are the main functionalities that this file provides:

1. A dynamic OTP input field with a maximum of 6 digits, which allows the user to enter their 2FA code in a specific format.
2. A button to submit the form for verification.
3. An error message to display if the user's 2FA code or recovery code is incorrect.
4. The ability to switch between inputting an authentication code and a recovery code, using two different forms with different fields.
5. Customized styling and layout using Tailwind CSS.
6. Integration with Livewire, a PHP full-stack framework that allows for real-time updates and event handling.

The file also imports various dependencies such as `Illuminate\Http\Request` to handle form submission, `Livewire\Component` to provide the base class for Livewire components, `App\Models\User` for user authentication, and `Laravel\Fortify\Features` for additional Fortify features.

---

### resources/views/livewire/auth/verify-email.blade.php
The file `resources/views/livewire/auth/verify-email.blade.php` appears to be a Blade template for a page that allows the user to verify their email address.

Here is a summary of the main components of this file:

* The layout used in this file is `resources/views/layouts/auth.blade.php`. This layout is likely used throughout the application to provide a consistent UI for authentication-related pages.
* The content of the page is displayed within a `<div>` element with the class `mt-4 flex flex-col gap-6`. This layout allows for a 12-column grid system and helps to create a responsive design.
* There are several pieces of text on this page, including a description of what to do, a notice if verification has already been sent, and a link to send another verification email. The `<p>` element is used to display these paragraphs of text.
* Within the form for sending a new verification email, there are two input fields: one for the email address and one for the password. These inputs use the `wire:model` directive to bind them to the corresponding properties in the Livewire component. The `<button>` element is used to submit the form.
* The form for logging out the user uses a similar layout, with two input fields and a button.

Overall, this file appears to be a simple UI that allows the user to verify their email address and log out of the application. It may also include additional features or functionality depending on the specific requirements of the project.

---

### resources/views/livewire/chat/big-research-chat.blade.php
**File Summary:**

This is a Livewire component for the chat feature in a web application. It uses Laravel's built-in Livewire framework to manage the client-side state and interact with the server. The component has a few main functions:

* Get a Webhook object from the database based on user ID and the name of the chat channel (e.g., "dashboard_chat").
* Load and display the chat messages for the current user.
* Allow the user to send new messages by submitting a form with a text input field and a button.
* Clear the chat history when requested by the user.

The component also uses some external dependencies, including:

* The `ChatMessage` model from the Laravel framework, which represents a single chat message in the database.
* The `Webhook` model from the Laravel framework, which represents a single webhook in the database.
* The `Http` facade from the Laravel framework, which provides an HTTP client for making requests to external APIs or webhooks.
* The `Auth` facade from the Laravel framework, which provides information about the current user and can be used to retrieve the user ID.
* The `Volt` component from the Livewire framework, which provides a way to render dynamic templates on the client-side.

The component is written in PHP and uses Laravel's Blade template syntax for generating HTML markup. It also uses some Livewire specific directives such as `@php`, `@endphp`, `@forelse`, etc.

---

### resources/views/livewire/recherche/phase-p1.blade.php
This PHP Blade template file provides a UI for editing and managing the project's P1 structure model, including adding new structure models, components, criteria, and warning signals. It also allows the user to edit and delete existing entries. The file uses Livewire, Volt, and Laravel's Auth Facade for authentication purposes.

---

### resources/views/livewire/recherche/phase-p2.blade.php
Maximal 10-zeilige Beschreibung der Datei-Funktionen

Datei: resources/views/livewire/recherche/phase-p2.blade.php
Kategorie: ui

Die Datei enthält Livewire-Komponenten, die für die Recherche-Phase von Projekten im P2-Prozess verantwortlich sind.

Klassen:

* Projekt (Modell)
* P2ReviewTypEntscheidung (Modell)
* P2Cluster (Modell)
* P2MappingSuchstringKomponente (Modell)
* P2Trefferliste (Modell)

Funktionen:

* Mount: Initialisiert die Komponenten-Daten und bindet sie an das Projekt.
* NewRev: Öffnet das Formular für eine neue ReviewTypEntscheidung.
* SaveRev: Speichert eine neue oder aktualisierte ReviewTypEntscheidung in der Datenbank.
* EditRev: Öffnet das Formular für die Bearbeitung einer vorhandenen ReviewTypEntscheidung.
* DeleteRev: Löscht eine vorhandene ReviewTypEntscheidung aus der Datenbank.
* CancelRev: Schließt das Formular für die Bearbeitung einer ReviewTypEntscheidung und setzt alle Felder zurück.

* NewClu: Öffnet das Formular für einen neuen Cluster.
* SaveClu: Speichert eine neue oder aktualisierte Cluster-Eintrag in der Datenbank.
* EditClu: Öffnet das Formular für die Bearbeitung eines vorhandenen Clusters.
* DeleteClu: Löscht einen vorhandenen Cluster aus der Datenbank.
* CancelClu: Schließt das Formular für die Bearbeitung eines Clusters und setzt alle Felder zurück.

* NewMap: Öffnet das Formular für eine neue Suchstring-Komponente im Mapping.
* SaveMap: Speichert eine neue oder aktualisierte Suchstring-Komponente in der Datenbank.
* EditMap: Öffnet das Formular für die Bearbeitung einer vorhandenen Suchstring-Komponente.
* DeleteMap: Löscht eine vorhandene Suchstring-Komponente aus der Datenbank.
* CancelMap: Schließt das Formular für die Bearbeitung einer Suchstring-Komponente und setzt alle Felder zurück.

* NewTref: Öffnet das Formular für einen neuen Treffer-Eintrag in der Trefferliste.
* SaveTref: Speichert eine neue oder aktualisierte Treffer-Eintrag in der Datenbank.
* EditTref: Öffnet das Formular für die Bearbeitung eines vorhandenen Treffer-Eintrags.
* DeleteTref: Löscht einen vorhandenen Treffer-Eintrag aus der Datenbank.
* CancelTref: Schließt das Formular für die Bearbeitung eines Treffer-Eintrags und setzt alle Felder zurück.

Externe Abhängigkeiten:

* Livewire (Framework)
* Illuminate\Support (Facades)

---

### resources/views/livewire/recherche/phase-p3.blade.php
Die Datei `resources/views/livewire/recherche/phase-p3.blade.php` enthält eine Livewire-Komponente für die Phase P3 der Recherche im Projektmanagement-Tool. Die Komponente verwendet Modelle von `App\Models\Recherche\{Projekt, P3Datenbankmatrix, P3Disziplin, P3GeografischerFilter, P3GraueLiteratur}`, um die Datenbankmatrix, Disziplinen und GraueLiteratur des Projekts zu verwalten.

Die Komponente enthält mehrere Methoden für das Erstellen, Bearbeiten und Löschen von Datensätzen in den verschiedenen Tabellen. Die Methoden `mount` und `render` werden verwendet, um die Komponente zu initialisieren und die Benutzeroberfläche anzuzeigen.

Die `newDb`, `saveDb`, `editDb`, `deleteDb`, `cancelDb`-Methoden sind für das Erstellen, Speichern, Bearbeiten und Löschen von Datensätzen in der Datenbankmatrix. Die `newDis`, `saveDis`, `editDis`, `deleteDis`, `cancelDis`-Methoden sind ähnlich und dienen zum Verwalten von Disziplinen.

Die `newGeo`, `saveGeo`, `editGeo`, `deleteGeo`, `cancelGeo`-Methoden sind für das Erstellen, Speichern, Bearbeiten und Löschen von Datensätzen im GeografischenFilter-Tabelleneintrag. Die `newGrau`, `saveGrau`, `editGrau`, `deleteGrau`, `cancelGrau`-Methoden sind ähnlich und dienen zum Verwalten von GraueLiteratur-Einträgen.

Die Komponente enthält auch eine `data`-Variable, die verwendet wird, um die Daten zu initialisieren und in der Benutzeroberfläche anzuzeigen. Diese Variable ist jedoch nicht in der vorliegenden Beschreibung enthalten, da sie 38.856 Zeichen lang ist und nicht zur vollständigen Beschreibung notwendig ist.

---

### resources/views/livewire/recherche/phase-p4.blade.php
The file `resources/views/livewire/recherche/phase-p4.blade.php` appears to be a Livewire component for managing and displaying data related to the "Phase P4" of a research project. The component contains several methods and properties that allow users to manipulate and view different types of data, such as search strings, thesaurus mappings, and anpassungsprotokoll (customization protocol) records.

The component uses Laravel Livewire to handle client-side state management and communication with the server. It also imports several classes from Laravel's Illuminate\Support\Facades namespace, such as Auth and Volt\Component, which are used for user authentication and rendering the component's HTML markup.

The main purpose of this file seems to be to provide a centralized place for managing and displaying research project data related to Phase P4. It includes various methods for creating, editing, deleting, and viewing different types of data related to this phase, as well as properties that allow users to manipulate the state of the component.

Overall, it appears that this file is an important part of a larger Laravel application, and plays a critical role in managing and displaying research project data related to Phase P4.

---

### resources/views/livewire/recherche/phase-p5.blade.php
The file contains a Livewire component that defines several properties and methods for managing data related to the project's screening process. The component is named "PhaseP5" and uses the following classes:

* `App\Models\Recherche\Projekt`: represents a project in the database.
* `App\Models\Recherche\P5Treffer`: represents a reference to a paper in the database.
* `App\Models\Recherche\P5ScreeningKriterium`: represents a screening criterion in the database.
* `App\Models\Recherche\P5ScreeningEntscheidung`: represents a screening decision in the database.
* `App\Models\Recherche\P5ToolEntscheidung`: represents a tool decision in the database.
* `App\Models\Recherche\P5PrismaZahlen`: represents a PRISMA data summary in the database.

The component defines several methods for managing these classes:

* `mount`: sets up the component by fetching the project from the database and setting up any necessary properties or dependencies.
* `newPrisma`: creates a new instance of the `P5PrismaZahlen` class and adds it to the database.
* `savePrisma`: saves changes made to an existing `P5PrismaZahlen` instance in the database.
* `editPrisma`: retrieves an existing `P5PrismaZahlen` instance from the database and allows users to edit its properties.
* `deletePrisma`: deletes an existing `P5PrismaZahlen` instance from the database.
* `cancelPrisma`: discards any changes made to a PRISMA data summary and returns the component to its previous state.
* `newSk`: creates a new instance of the `P5ScreeningKriterium` class and adds it to the database.
* `saveSk`: saves changes made to an existing `P5ScreeningKriterium` instance in the database.
* `editSk`: retrieves an existing `P5ScreeningKriterium` instance from the database and allows users to edit its properties.
* `deleteSk`: deletes an existing `P5ScreeningKriterium` instance from the database.
* `cancelSk`: discards any changes made to a screening criterion and returns the component to its previous state.
* `newTool`: creates a new instance of the `P5ToolEntscheidung` class and adds it to the database.
* `saveTool`: saves changes made to an existing `P5ToolEntscheidung` instance in the database.
* `editTool`: retrieves an existing `P5ToolEntscheidung` instance from the database and allows users to edit its properties.
* `deleteTool`: deletes an existing `P5ToolEntscheidung` instance from the database.
* `cancelTool`: discards any changes made to a tool decision and returns the component to its previous state.
* `openScreen`: retrieves a reference to a paper in the database based on the provided treffer ID and allows users to edit its screening decisions.
* `saveScreen`: saves changes made to an existing `P5ScreeningEntscheidung` instance in the database.
* `cancelScreen`: discards any changes made to a screening decision and returns the component to its previous state.
* `deleteScreen`: deletes an existing `P5ScreeningEntscheidung` instance from the database.

---

### resources/views/livewire/recherche/phase-p6.blade.php
Diese Datei ist eine Blade-Datei, die für die Anzeige der Daten einer Projektphase (Phase P6) auf der Livewire-Seite zuständig ist. Sie enthält eine Klasse namens `Recherche\PhaseP6` und umfasst verschiedene Methoden und Eigenschaften zur Verarbeitung und Anzeige der Daten.

Die wichtigsten Funktionen dieser Datei sind:

* Die `mount()`-Methode, die den Projektparameter überträgt und eine Prüfung auf die Berechtigung durchführt.
* Die `with()`-Methode, die die erforderlichen Daten für die Anzeige der Treffer, Bewertungen und Lückenanalyse bereitstellt.
* Die `newQb()`, `saveQb()`, `editQb()`, `deleteQb()`, `cancelQb()`-Methoden, die die Funktionen zur Verarbeitung von Qualitätsbewertungen implementieren.
* Die `newLa()`, `saveLa()`, `editLa()`, `deleteLa()`, `cancelLa()`-Methoden, die die Funktionen zur Verarbeitung von Lückenanalysen implementieren.

Die Datei enthält auch eine Blade-Template-Datei mit HTML-Code für die Anzeige der Daten auf der Livewire-Seite. Hierbei werden verschiedene Komponenten und Formulare verwendet, um die Benutzerinteraktion zu ermöglichen und die Daten zu verarbeiten.

---

### resources/views/livewire/recherche/phase-p7.blade.php
Die Datei "resources/views/livewire/recherche/phase-p7.blade.php" enthält eine Livewire-Komponente zur Bearbeitung und Anzeige der Daten extrahiert aus der Recherchenphasen 5 und 7. Die Komponente beinhaltet several functionalities for creating, editing, and deleting records in the following models:

* P7SyntheseMethode (synthesis method)
* P7Datenextraktion (data extraction)
* P7MusterKonsistenz (pattern consistency)
* P7GradeEinschaetzung (grade evaluation)

The component also handles the CRUD operations for these models, including validation and error handling. The file also imports several external dependencies:

* App\Models\Recherche\Projekt (project model)
* Illuminate\Support\Facades\Auth (authentication facade)
* Livewire\Volt\Component (Livewire component class)

The component uses the mount method to check if the current user is authorized to access the project and assigns it to the $projekt property. It also defines several public properties for managing the forms and editing records, as well as methods for creating, editing, deleting, and canceling the forms. The save method validates the form data and updates or creates a record in the corresponding model, while the delete method deletes a record from the database. The file ends with a closing Livewire component tag.

---

### resources/views/livewire/recherche/phase-p8.blade.php
File: resources/views/livewire/recherche/phase-p8.blade.php
Category: ui

This file contains a Livewire component for the project phase P8 of the Recherche application. It defines several properties and methods that are used to handle the CRUD operations for the following entities:

* Suchprotokoll (P8Suchprotokoll)
* Limitation (P8Limitation)
* Reproduzierbarkeit (P8Reproduzierbarkeitspruefung)
* Update-Plan (P8UpdatePlan)

The component also includes a few methods for handling the data, such as fetching the suchstrings associated with the project and filtering the results based on the user's permissions.

The component is mounted using the `mount` method, which takes a `Projekt` object as an argument and validates that the current user has permission to view the project. It then sets the `projekt` property of the component to the passed-in project object.

The component also includes several methods for handling the CRUD operations for each entity. For example, the `newSp` method shows a form for creating a new suchprotocol entry, while the `saveSp` method saves the entered data and updates or creates a new suchprotocol entry in the database. Similarly, the `editSp`, `deleteSp`, and `cancelSp` methods handle editing and deleting existing suchprotocol entries.

The component also includes methods for handling the CRUD operations for limitation (such as creating, updating, and deleting), reproduzierbarkeit (such as creating, updating, and deleting), and update-plan (such as creating, updating, and deleting). These methods use similar logic to handle the suchprotocol entries.

The component also includes a few methods for handling the data, such as fetching the suchstrings associated with the project and filtering the results based on the user's permissions. These methods are used to display the suchprotocol entries in a table, and to filter the list of available suchstrings based on the current user's permissions.

Overall, this file defines a Livewire component that provides a UI for managing the various entities associated with the project phase P8 of the Recherche application. The component uses a combination of properties, methods, and events to handle CRUD operations and data filtering, and it is mounted using the `mount` method to validate user permissions before allowing access to the component.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
This is a Livewire component for displaying the details of a project in the "recherche" section. It uses the `Projekt` model from the app, and displays various information about the project, such as its title, creation date, and research question. It also provides buttons to switch between different tabs representing different phases of the project, and displays the status of each phase (e.g. "abgeschlossen" or "in Bearbeitung").

The component uses the `switchTab` method to update the active tab, and the `getPhaseStatus` method to determine the status of each phase based on its corresponding phase number. The component also includes a Volt template that displays the header section, tab bar, and phase content.

External dependencies include the `Projekt` model, `Auth` facade for authentication, and the `Url` attribute for generating URLs.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
This file is a Livewire component for displaying a list of projects in the "Recherche" section of the website. It uses the `Recherche\Projekt` model to retrieve the list of projects and displays them in a card format. The user can delete each project by clicking on the trash icon button.

Here is a summary of the main functionalities of this file:

* Retrieves the list of projects for the current user using the `Recherche\Projekt` model and the `where()` method to filter by the `user_id` column.
* Displays each project in a card format with the title, review type (if available), and creation date.
* Uses the `with()` method to pass the list of projects to the component as a property, so that it can be used in the view.
* Provides an action for deleting each project by clicking on the trash icon button. The `deleteProjekt` function is called with the project ID as an argument, and it uses the `findOrFail()` method to retrieve the project and then calls the `delete()` method to delete it from the database.
* Uses the `authorize()` method to check if the current user has permission to delete the project. If not, an error message is displayed.
* Provides a fallback for when there are no projects available to display by displaying a message.

---

### resources/views/livewire/recherche/research-input.blade.php
Die Datei `resources/views/livewire/recherche/research-input.blade.php` enthält eine Livewire-Komponente, die für die Eingabe von Forschungsfragen oder Themen verwendet wird. Die Komponente erwartet einen Wert für das `eingabe`-Attribut und validiert den eingegebenen Text mit einer maximalen Länge von 10.000 Zeichen.

Wenn der Benutzer auf die Schaltfläche "Recherche starten" klickt, wird der Wert des `eingabe`-Attributs an die `starteRecherche()`-Methode übergeben und im Backend durch den Job `TriggerLangdockAgent` verarbeitet. Der Job lädt die Benutzer-ID und die Forschungsfrage in ein Projekt-Modell, speichert diese Informationen in der Datenbank und startet einen neuen Recherche-Auftrag mit der entsprechenden Forschungsfrage.

Die Komponente rendert auch eine Formular-Elemente für den Benutzer, um seine Forschungsfrage einzugeben. Wenn die Eingabe ungültig ist (z.B. weil sie leer ist), wird ein Fehler angezeigt. Andernfalls wird der Benutzer auf die Projekt-Seite weitergeleitet, auf der er die Recherche verfolgen kann.

Die Datei enthält auch eine `<section>`-Element, das die Formular-Elemente und den Button für den Recherche-Start enthält.

Externe Abhängigkeiten:

* `App\Jobs\TriggerLangdockAgent` - ein Job, der in der backend-Anwendung verwendet wird, um einen neuen Auftrag zu starten.
* `App\Models\Recherche\Projekt` - ein Modell, das die Datenbanktabelle für die Projekte enthält, in denen die Forschungsfragen gespeichert werden.

---

### resources/views/livewire/settings/appearance.blade.php
Diese Datei enthält eine Livewire-Komponente, die die Darstellungseinstellungen des Benutzers verwaltet. Die Komponente enthält ein Formular zum Ändern der Darstellungsmodus des Benutzers (Light/Dark/System), das über JS-Event-Handler interagiert und den Status in der lokalen Speicherung speichert.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* `Livewire\Volt\Component`: Die Livewire-Komponente, die die Darstellungsmodus-Änderungen verarbeitet und in der lokalen Speicherung speichert.
* `x-settings.layout`: Eine Layout-Komponente, die das Formular für den Ändern des Darstellungsmodus enthält.
* `script`: Ein Skriptblock, der die JS-Event-Handler und die Initialisierung der lokalen Speicherung implementiert.

---

### resources/views/livewire/settings/delete-user-form.blade.php
This file is a Livewire component for deleting a user's account. It contains several functions and classes, including:

* `App\Livewire\Actions\Logout`: a class that handles the logout action.
* `Illuminate\Support\Facades\Auth`: a facade for the Auth service provider, which provides methods for authenticating users.
* `Livewire\Volt\Component`: a class that extends Livewire's base component class and provides methods for rendering views.
* `App\Models\User`: a model for the user entity, which is used to delete the account.

The component has several functions:

* `openModal()`: opens a modal dialogue that prompts the user to enter their password before deleting their account.
* `closeModal()`: closes the modal dialogue and resets the form fields.
* `deleteUser()`: deletes the user's account using the `tap` method, which first retrieves the current user from the Auth service provider and then calls the `logout` method on it. The method also redirects the user to the homepage after deleting the account.

The component also has a section that displays the delete account form, which includes several fields:

* `password`: a password field for entering the user's current password before deleting their account.
* `showDeleteModal`: a boolean variable that indicates whether the modal dialogue is currently open or not.

The component also includes some CSS classes and inline JavaScript to handle the modal dialogue and form submission.

---

### resources/views/livewire/settings/password.blade.php
This file is a Livewire component that allows the user to update their password for an authenticated account. The following are the key functions and external dependencies of this file:

1. `updatePassword()` function: This function is called when the form is submitted and it updates the password for the currently authenticated user using the `Auth` facade. It validates the current password, new password, and confirmation password fields using the Livewire validation feature. If there are any errors, it resets the form fields and throws a validation exception.
2. `Password::defaults()` method: This method is used to validate the password field. It uses the `Password` facade to check if the password meets the default requirements (i.e., at least 8 characters long, contains at least one letter, one number, and one special character). If the password does not meet these requirements, it throws a validation exception.
3. `@error('current_password')` directive: This directive is used to display an error message if there are any errors in the current password field. The error message is defined in the Livewire validation rules for this field.
4. `Auth` facade: This facade is used to access the currently authenticated user's information and update their password.
5. `Password::defaults()` method: This method is used to validate the password field. It uses the `Password` facade to check if the password meets the default requirements (i.e., at least 8 characters long, contains at least one letter, one number, and one special character). If the password does not meet these requirements, it throws a validation exception.
6. `@error('password')` directive: This directive is used to display an error message if there are any errors in the new password field. The error message is defined in the Livewire validation rules for this field.
7. `Password::defaults()` method: This method is used to validate the confirmation password field. It uses the `Password` facade to check if the password matches the one entered in the new password field. If the passwords do not match, it throws a validation exception.
8. `@error('password_confirmation')` directive: This directive is used to display an error message if there are any errors in the confirmation password field. The error message is defined in the Livewire validation rules for this field.
9. `Auth::user()->update()` method: This method is used to update the currently authenticated user's password in the database. It takes an array of attributes as input and updates the user's information accordingly.
10. `@include('partials.settings-heading')` directive: This directive includes a reusable heading component for the settings section.

Overall, this Livewire component allows users to update their password securely using a strong, unique password that meets the default requirements.

---

### resources/views/livewire/settings/profile.blade.php
Die Datei `resources/views/livewire/settings/profile.blade.php` enthält eine Livewire-Komponente zur Verwaltung von Benutzereinstellungen und -informationen. Die Komponente umfasst folgende Funktionen:

* Verarbeitung der Benutzerdaten (Name und E-Mail-Adresse) in einem Formular und Überprüfung der Daten auf Gültigkeit.
* Aktualisierung der Benutzereinstellungen und des Namen und der E-Mail-Adresse für den aktuellen Benutzer.
* Senden einer E-Mail-Überprüfungsanfrage an den Benutzer, falls seine E-Mail-Adresse noch nicht verifiziert wurde.
* Anzeige von Fehlern oder Erfolgsmeldungen durch die Livewire-Komponente `x-action-message`.

Die Datei importiert folgende Abhängigkeiten:

* `App\Models\User`: Die Benutzerdatenbankmodelle.
* `Illuminate\Support\Facades\Auth`: Authentifizierungs-Middleware.
* `Illuminate\Support\Facades\Session`: PHP-Sitzung.
* `Illuminate\Validation\Rule`: Überprüfungsregeln für das Formular.
* `Livewire\Volt\Component`: Livewire-Komponente für die Benutzereinstellungen.

Die Komponente verwendet die `mount`-Methode, um den Namen und die E-Mail-Adresse des aktuellen Benutzers zu laden. Die Methode `updateProfileInformation` wird aufgerufen, wenn der Benutzer das Formular absendet, um seine Benutzereinstellungen zu aktualisieren. Die Methode verwendet die `validate`-Methode, um die übermittelten Daten zu validieren und dann die Änderungen an den Benutzerdatenbankmodellen vorzunehmen.

Die Komponente enthält auch eine `resendVerificationNotification`-Methode, die einen Link zur E-Mail-Überprüfung des Benutzers sendet, falls seine E-Mail-Adresse noch nicht verifiziert wurde. Die Methode verwendet das `sendEmailVerificationNotification`-Metod der `Illuminate\Contracts\Auth\MustVerifyEmail`-Schnittstelle des Benutzers, um eine neue Überprüfungsanfrage zu senden.

Die Komponente enthält auch eine `<livewire:settings.delete-user-form />` Tags, die ein Formular für das Löschen des aktuellen Benutzers anzeigt.

---

### resources/views/livewire/settings/two-factor.blade.php
Die Datei enthält eine Livewire-Komponente, die die Konfiguration von two-factor Authentication für einen Benutzer ermöglicht und zeigt den Fortschritt des Prozesses an. Sie importiert Klassen aus Laravel und Symfony, um die Authentifizierung und die API-Aufrufe zu verwalten.

---

### resources/views/livewire/settings/webhooks.blade.php
Die Datei `resources/views/livewire/settings/webhooks.blade.php` ist ein Livewire-Komponenten-Template für die Einstellungen des Benutzers. Die Komponente verwendet die Laravel-Funktion "Livewire" um die User-Einstellungen zu ändern und diese anschließend in der Datenbank zu speichern.

Die Datei enthält folgende Funktionen:

1. Das Mount-Methode, die beim Laden der Komponente ausgeführt wird, um den Benutzer und seine Webhook-Einstellungen abzurufen.
2. Die SaveDashboard- und SaveRecherche-Methoden, die ausgeführt werden, wenn der Benutzer auf den Speichern-Button klickt. Sie überprüfen die URL-Eingabe des Benutzers und speichern sie in der Datenbank, falls dies erfolgreich ist.
3. Die clearDashboard- und clearRecherche-Methoden, die ausgeführt werden, wenn der Benutzer auf den Entfernen-Button klickt. Sie entfernen die Webhook-Einstellungen des Benutzers aus der Datenbank.

Die Datei verwendet auch einige external dependencies:

* Illuminate\Support\Facades\Auth: für die Authentifizierung des Benutzers.
* App\Models\Webhook: um auf die Webhook-Einstellungen des Benutzers zuzugreifen und diese zu speichern.
* Illuminate\Support\Str: zum Generieren von zufälligen Slugs für die Webhook-Einstellungen.
* Livewire\Volt\Component: um die Komponenten-Template-Datei zu erstellen.

Die Datei gibt eine Benutzeroberfläche mit zwei Formularen zurück, um den Benutzer zu ermöglichen, seine Webhook-Einstellungen zu ändern und diese zu speichern. Die Benutzeroberfläche besteht aus einem Dashboard Chat Slot und einem Recherche starten Slot. Jeder Slot hat ein Formular, das den Benutzer darbietet, eine URL für den Webhook einzugeben und diese dann zu speichern oder zu entfernen. Es gibt auch einige Status-Meldungen, die angezeigt werden, wenn der Benutzer seine Einstellungen geändert hat.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
This file is a Blade component that displays recovery codes for two-factor authentication (2FA) in Laravel Jetstream. The component uses the `Livewire` framework and includes several classes and functions from the `Laravel\Fortify` namespace, such as `GenerateNewRecoveryCodes`.

The component has a button to regenerate recovery codes and another button to hide or show the recovery code section. It also includes an error message if there is an issue with loading the recovery codes.

The component uses the `x-show` attribute from Livewire to toggle the visibility of the recovery code section based on the `showRecoveryCodes` variable, which is set to false by default. The `x-bind:aria-hidden="!showRecoveryCodes"` attribute ensures that the recovery code section is not accessible when it is hidden.

The component also includes a transition effect using the `x-transition` attribute from Livewire to animate the hiding and showing of the recovery code section.

Overall, this file provides a user interface for managing two-factor authentication recovery codes in Laravel Jetstream.

---

### resources/views/partials/head.blade.php
Die Datei `head.blade.php` ist eine Blade-Template-Datei, die das `<head>`-Element einer HTML-Seite generiert. Die Datei enthält verschiedene Metadaten und Links zu verschiedenen Ressourcen wie CSS-Dateien, JavaScript-Dateien und Iconen.

Hier sind einige wichtige Funktionen der Datei:

* Es wird eine UTF-8-Kodierung verwendet, um die Darstellung von Zeichen in verschiedenen Sprachen zu unterstützen.
* Die Viewport-Breite wird auf `device-width` eingestellt, um sicherzustellen, dass die Seite auf jeder Gerätegröße angepasst wird.
* Der Titel der Seite wird aus einer Variablen `$title` gelesen oder als Fallback verwendet, wenn keine Variable definiert ist.
* Es werden verschiedene Icon-Dateien für die verschiedenen Größen und Formate hinzugefügt, um die Verwendung von Icons auf verschiedenen Geräten zu unterstützen.
* Es wird eine Dark-Mode-Init-Skript verwendet, um die Darstellung der Seite in einem Dark-Mode anzuzeigen, wenn das Gerät dies unterstützt und die Benutzervorlieben den Dark-Mode vorziehen.
* Es werden CSS- und JavaScript-Dateien aus dem Ordner `resources` geladen, was möglicherweise zu einer Verkettung von Skript- und Stylesheet-Ressourcen führt, falls diese Dateien kompiliert wurden.

---

### resources/views/partials/settings-heading.blade.php
This Blade template file is part of a web application's user interface (UI) components. It contains the HTML and CSS code for displaying the heading element with a subtitle and horizontal line under it.

The main class used in this file is `relative`, which sets the position of the element relative to its normal position. The `mb-6` class adds a margin bottom of 6 pixels, while the `w-full` class sets the width of the element to the full available width of its parent container.

The `text-xl font-semibold text-zinc-900 dark:text-zinc-100` class styles the heading text with a font size of 24 pixels, semibold weight, and the default dark mode color scheme for the text and its shadow. The `mt-1` class adds a margin top of 1 pixel, while the `mb-6` class adds a margin bottom of 6 pixels.

The `text-base text-zinc-500 dark:text-zinc-400 mb-6` class styles the subtitle text with a font size of 16 pixels, regular weight, and the default dark mode color scheme for the text and its shadow. The `mt-1` class adds a margin top of 1 pixel, while the `mb-6` class adds a margin bottom of 6 pixels.

The `<hr>` element with the `border-zinc-200 dark:border-zinc-700` class styles the horizontal line with a zinc-200 or zinc-700 color, depending on the current theme (dark mode or light mode). The `mb-6` class adds a margin bottom of 6 pixels.

The file does not import any external libraries or APIs, but it uses Laravel's built-in __() function for translating text.

---

### resources/views/recherche/index.blade.php
This is a Laravel Blade template file for the "Recherche" page, which is used to display a list of projects that match certain criteria.

The file imports the `recherche.research-input` component and the `recherche.projekt-liste` component from Livewire. These components are responsible for rendering the input form and the project list respectively.

The layout is based on the `app` layout, which is defined in the `layouts` directory. The title of the page is set to "Recherche" using the `__()` function, which allows for internationalization and localization of the text.

The file uses a mix of HTML and Blade directives to render the page. It includes an h1 tag with the title of the page, a form element for entering search criteria, a horizontal rule for separating the input form from the project list, and finally a div element that contains the `recherche.projekt-liste` component.

The file does not contain any external dependencies or API calls, as it relies on the Livewire components to handle the data retrieval and display.

---

### resources/views/recherche/show.blade.php
Die Datei "resources/views/recherche/show.blade.php" ist eine Blade-Datei, die Teil einer Laravel-Anwendung ist. Sie definiert die Benutzeroberfläche für das Anzeigen eines Projekts in der Recherche-Seite der Applikation.

Die Datei enthält folgende Funktionen:

* Ein Layout mit einem Titel, der aus dem Projekt-Objekt extrahiert wird.
* Eine Livewire-Komponente "recherche.projekt-detail", die das Detail-Template für ein bestimmtes Projekt anzeigt.
* Ein div-Element mit einer breiten Maximalbreite von 4xl und einer mittleren Positionierung.

Die Datei benutzt folgende Klassen:

* X-Layouts-App: Eine Layout-Klasse, die für die Benutzeroberfläche der Anwendung verwendet wird.
* Livewire: Ein Framework zur Erstellung von reaktionsfähigen und dynamischen Benutzeroberflächen.
* Projekte: Eine Klasse, die das Projekt-Objekt repräsentiert und die Eigenschaften wie Titel, Beschreibung und andere Metadaten enthält.

Die Datei verwendet folgende Endpunkte:

* "recherche/show": Der Endpunkt für das Anzeigen des Detail-Templates eines bestimmten Projekts in der Recherche-Seite.

Die Datei benutzt keine externen Abhängigkeiten oder APIs.

---

## uncategorized

### Readme.md
{
  "file_summary": "This is a summary of the functions of this file, including the main classes, methods, and external dependencies if any."
}

Here's an example output for the provided Readme.md file:

{
  "file_summary": "Maximal 10-zeilige Beschreibung der Datei-Funktionen",
  "stack": [
    {
      "component": "Laravel",
      "version": "12"
    },
    {
      "component": "PHP",
      "version": "8.2+"
    },
    {
      "component": "Livewire / Volt",
      "version": "1.7 (Inline-Komponenten)"
    },
    {
      "component": "Filament",
      "version": "4.9 (Admin-Panel)"
    },
    {
      "component": "Fortify",
      "version": "1.30 (Auth + 2FA)"
    },
    {
      "component": "Tailwind CSS",
      "version": "4 (via Vite)"
    },
    {
      "component": "PostgreSQL",
      "version": "16 (UUID-Primärschlüssel)"
    },
    {
      "component": "Redis",
      "version": "Alpine (Cache, Session, Queue)"
    }
  ],
  "architecture": [
    "Nginx (Port 6481)",
    "PHP-FPM",
    "Postgres (UUID-Primärschlüssel)",
    "Redis",
    "Postgres-MCP /mcp/sse",
    "Ollama /ollama/",
    "Paper-Search /paper-mcp/",
    "Langdock-KI-Agent via Queue"
  ],
  "features": [
    "Authentifizierung & Benutzerverwaltung",
    "Systematische Literaturrecherche",
    "Admin-Panel (Filament)",
    "Kontaktformular",
    "CI/CD & Deployment",
    "MCP PostgreSQL Endpoint",
    "Ollama Embedding Endpoint",
    "Paper-Search MCP"
  ],
  "routes": [
    {
      "method": "GET",
      "path": "/"
    },
    {
      "method": "POST",
      "path": "/contact"
    },
    {
      "method": "GET",
      "path": "/Impressum.html"
    },
    {
      "method": "GET",
      "path": "/AGB.html"
    },
    {
      "method": "GET",
      "path": "/dashboard"
    },
    {
      "method": "GET",
      "path": "/settings/*"
    },
    {
      "method": "GET",
      "path": "/recherche"
    },
    {
      "method": "GET",
      "path": "/recherche/{projekt}"
    },
    {
      "method": "GET",
      "path": "/dsgvo/export"
    },
    {
      "method": "DELETE",
      "path": "/dsgvo/delete-account"
    }
  ],
  "database": [
    "21 Migrationen (Users, Cache, Jobs, 2FA, Contacts, PageViews, Permissions, Consents, Recherche P1–P8, Indices)",
    "36+ Models: Core: User, Contact, PageView, Consent, Webhook, ChatMessage",
    "Recherche: Projekt, Phase, P5Treffer + 29 Phasen-Models (P1–P8)"
  ],
  "local_development": [
    "Composer-Scripts: setup (Install, Key, Migrate, NPM Build), dev (Server + Queue + Vite (parallel)), test (PHPUnit/Pest Tests)",
    "Docker Compose"
  ],
  "deployment": [
    "Full Deployment: Pull Images → Build → Start Postgres/Redis → Migrate → Cache Config/Routes/Views → Start All → Deploy-Mail",
    "Skip Build: ./deploy.sh (Ohne Docker-Rebuild)",
    "Skip Migrations: ./deploy.sh --skip-migrate (Ohne Migrationen)"
  ],
  "configuration": [
    "Environment Variables in .env: APP_URL, DB_CONNECTION, QUEUE_CONNECTION, CACHE_STORE, SESSION_DRIVER, MAIL_HOST, MCP_AUTH_TOKEN",
    "License: Proprietary — All Rights Reserved"
  ]
}

---

### artisan
This file is an Artisan script that handles the command line interface (CLI) for a Laravel application. It uses the Symfony Console component to parse and handle commands, and it requires the Composer autoloader to load the necessary classes for Laravel. The main function of this file is to define a constant `LARAVEL_START` to track the start time of the script, require the Composer autoloader, bootstrap the Laravel application, and then handle any incoming commands using the `handleCommand` method of the `$app` object. Finally, it exits with an optional status code returned by the `handleCommand` method.

The file imports the following classes:

* `Illuminate\Foundation\Application`: The main entry point for a Laravel application.
* `Symfony\Component\Console\Input\ArgvInput`: A class that provides an interface to parse command line arguments and options.

It also uses the following external dependencies:

* `vendor/autoload.php`: The Composer autoloader, which is used to load the necessary classes for Laravel.
* `__DIR__.'/bootstrap/app.php'`: The bootstrap file for the Laravel application, which is used to initialize the application and handle any incoming commands.

---

### basemodel.md
Die Datei enthält ein PostgreSQL-Datenbankmodell für eine Systematische Literaturrecherche. Es gibt verschiedene Tabellen und Spalten, die zum Speichern von Informationen über Projekte, Phasen, Strukturmodelle und Suchstrings verwendet werden.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* Die Tabelle `projekte` enthält Informationen zu einzelnen Projekten, darunter den Titel, die Forschungsfrage, den Review-Typ, den Verantwortlichen, den Start- und Enddatum sowie Notizen.
* Die Tabelle `phasen` speichert Informationen über die verschiedenen Phasen einer Systematischen Literaturrecherche, darunter die Nummer der Phase, den Titel, den Status (offen, in Bearbeitung oder abgeschlossen) und eventuelle Notizen.
* Die Tabelle `p1_strukturmodell_wahl` enthält Informationen zu den gewählten Strukturmodellen PICO/SPIDER/PICOS für eine bestimmte Phase.
* Die Tabelle `p1_komponenten` speichert Informationen zu den Komponenten eines Strukturmodells, darunter die Kurzel und den Label, Inhaltliche Begriffe (optional), Synonyme, englische Entsprechungen und MESH- oder Thesaurus-Term.
* Die Tabelle `p1_kriterien` enthält Informationen zu den Kriterien für eine bestimmte Phase, darunter die Beschreibung, die Begründung und mögliche Quellbezüge.
* Die Tabelle `p1_warnsignale` speichert Informationen zu Warnsignalen für eine bestimmte Phase, darunter den Warnzeichen-Code, die moegliche Auswirkung und Handlungsempfehlungen.
* Die Tabelle `p2_review_typ_entscheidung` enthält Informationen zu den gewählten Review-Typen für eine bestimmte Phase, darunter den Review-Typ und ob er passt oder nicht passt.
* Die Tabelle `p2_mapping_suchstring_komponenten` speichert Informationen zu den Suchstrings und den Komponenten für eine bestimmte Phase, darunter die Suchstrings (OR-Gruppe), die Sprache, die Trunkierung und Anmerkungen.
* Die Tabelle `p2_trefferlisten` enthält Informationen zu den Treffern für eine bestimmte Phase, darunter die Datenbank, den Suchstring, die Gesamtzahl der Treffer, die Einschätzung (zu viel, passend oder zu wenig) und Anpassungen.
* Die Tabelle `p2_cluster` enthält Informationen zu den Clustern für eine bestimmte Phase, darunter den Cluster-ID, den Cluster-Label, die Beschreibung, die Schätzung der Trefferzahl und die Relevanz (hoch, mittel oder gering).
* Die Tabelle `p3_disziplinen` enthält Informationen zu den Disziplinen für eine bestimmte Phase, darunter die Disziplin, den Art (kerndisziplin oder angrenzend) und eine relevanz-Bewertung.
* Die Tabelle `p3_datenbankmatrix` speichert Informationen zu den Datenbanken für eine bestimmte Phase, darunter die Datenbank, die Disziplin (optional), die Abdeckung, besonderheiten und Zugangsvoraussetzungen.
* Die Tabelle `p3_geografische_filter` enthält Informationen zu den geografischen Filtern für eine bestimmte Phase, darunter die Region (Land) und einen validierten Filter vorhanden oder nicht.
* Die Tabelle `p3_graue_literatur` speichert Informationen zu der grauen Literatur für eine bestimmte Phase, darunter die Quelle, den Typ (optional), die URL (optional) und eine relevanz-Bewertung.
* Die Tabelle `p4_suchstrings` enthält Informationen zu den Suchstrings für eine bestimmte Phase, darunter den Suchstring, die Sprache, die Feldeinschränkungen (optional), gesetzte Filter (optional) und Anmerkungen.
* Die Tabelle `p4_thesaurus_mapping` speichert Informationen zu den Thesaurus-Mapping für eine bestimmte Phase, darunter den Suchstring, die Sprache, die Thesaurus-ID (optional) und eine relevanz-Bewertung.

---

### CLAUDE.md
CLAUDE.md provides guidance on using Claude Code (claude.ai/code) for working with the app.linn.games repository, which is a Laravel 12 research management platform for AI-assisted systematic literature reviews.

---

### composer.json
This is a `composer.json` file for a Laravel application, which defines the dependencies and configuration for the project. Here's a summary of the key information:

* Name: nileneb/app-linn-games
* Type: Project
* Description: Research-Management-Plattform mit KI-gestützter systematischer Literaturrecherche.
* Keywords: laravel, framework
* License: proprietary
* Require:
	+ PHP version 8.2 or higher
	+ Laravel Framework version 12.0
	+ Laravel Sanctum version 4.3
	+ Laravel Fortify version 1.30
	+ Livewire Volt version 1.7.0
	+ Predis library version 3.4
	+ Spatie Laravel Activity Log version 5.0
	+ Spatie Laravel Cookie Consent version 3.4
	+ Spatie Laravel Permission version 7.2
* Require-dev:
	+ FakerPHP library version 1.23 for testing
	+ Laravel Pail library version 1.2.2 for development
	+ Laravel Pint library version 1.24 for development
	+ Laravel Sail library version 1.41 for development
	+ Mockery library version 1.6 for development
	+ NunoMaduro Collision library version 8.6 for development
	+ PestPHP library version 4.2 for testing
	+ PestPHP plugin for Laravel version 4.0 for testing
* Autoload:
	+ PSR-4 autoloading for the App namespace
	+ PSR-4 autoloading for the Database Factories and Seeders namespaces
* Autoload-dev:
	+ PSR-4 autoloading for the Tests namespace
* Scripts:
	+ Setup script to install dependencies, copy environment file, generate app key, run migrations, install NPM packages, and build the frontend using Vite
	+ Dev script to concurrently run the server, queue listener, and NPM dev script
	+ Test script to clear config cache, run Laravel test suite
	+ Post-autoload-dump script to discover Laravel packages
	+ Post-update-cmd script to publish Laravel assets
	+ Post-root-package-install script to copy the environment file if it doesn't exist
	+ Post-create-project-cmd script to generate app key, touch database file if necessary, and migrate the database
	+ Pre-package-uninstall script to clean up Composer cache before uninstalling packages.

---

### CONTRIBUTING.md
This file provides guidelines for contributing to the app.linn.games project, including branch conventions, merge flow, and local development instructions.

---

### deploy.sh
This file is a Bash script that automates the deployment of an application to a production environment. It uses Docker Compose to orchestrate the various components of the application, including PostgreSQL and Redis.

The script starts by checking if Docker is installed on the system and if the `.env` file exists. If either condition is not met, the script exits with an error message.

Next, the script pulls the latest images for the `postgres`, `redis`, and `php-fpm` services using `docker compose pull`. It then builds the production images for the `web`, `queue-worker`, and `php-cli` services using `docker compose build --no-cache`.

The script then starts the PostgreSQL and Redis services using `docker compose up -d`. It then checks the health of the PostgreSQL service using `docker compose exec postgres pg_isready`, and creates any missing extensions for Vector, Hypopg, and PG Statements using `docker compose exec -T postgres sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "CREATE EXTENSION IF NOT EXISTS vector; CREATE EXTENSION IF NOT EXISTS hypopg; CREATE EXTENSION IF NOT EXISTS pg_stat_statements;"'`.

The script then creates a backup of the PostgreSQL database using `docker compose exec -T postgres sh -lc 'pg_dump -U "$POSTGRES_USER" -d "$POSTGRES_DB" --no-owner --no-acl'` and saves it to the `./backups` directory. It then runs any necessary migrations for the application using `docker compose run --rm php-cli php artisan migrate`.

The script then caches the configuration, routes, and views for the application using `docker compose run --rm php-cli php artisan config:cache`, `docker compose run --rm php-cli php artisan route:cache`, and `docker compose run --rm php-cli php artisan view:cache`.

Finally, the script starts all services using `docker compose up -d` and verifies that they are running properly using `docker compose ps`. The script then sends a notification email to the development team indicating that a new deployment has been completed.

---

### package.json
package.json file for a Vite project that uses Tailwind CSS, Axios, and Laravel Vite plugin. It contains information about the project's dependencies, scripts, and optional dependencies.

---

### phpunit.xml
This is a PHPUnit configuration file, which defines the settings and options for running tests. The file includes several sections:

* `<phpunit>` - This section contains the overall configuration for PHPUnit, including the `bootstrap` attribute that specifies the location of the autoloader script to be used for loading the test classes.
* `<testsuites>` - This section defines the different sets of tests to run. In this case, there are two test suites defined: "Unit" and "Feature". Each suite consists of a set of directories that contain the test classes.
* `<source>` - This section specifies the location of the source code to be tested. The `include` directory specifies the location of the app directory.
* `<php>` - This section contains environment variables that are used by PHPUnit. These variables include the database connection settings, the cache store setting, and the email mailer settings.

In summary, this file is used to define the configuration for running tests with PHPUnit, including the location of the test classes, the source code to be tested, and the environment variables that are used by PHPUnit.

---

### vite.config.js
This file is a configuration file for the Vite build tool. It defines several plugins and options for the Vite server.

Here are the main functions of this file:

* Importing required modules from other files, such as `laravel-vite-plugin` and `@tailwindcss/vite`.
* Defining a configuration object with some properties:
	+ `plugins`: an array of plugins to use in the Vite build process. This includes the `laravel` plugin for Laravel-specific functionality and the `tailwindcss` plugin for Tailwind CSS support.
	+ `server`: an object containing options for the Vite server, including:
		- `cors`: enables Cross-Origin Resource Sharing (CORS) for the server.
		- `watch`: an object containing options for the watch mode, including the `ignored` property to specify files and directories that should be ignored by the watcher.

Overall, this file sets up a Vite build process with some Laravel-specific features enabled, such as Tailwind CSS support.

---

### .dockerignore
Die Datei ".dockerignore" dient als Ignorierliste für Docker. Sie listet Dateien und Ordner auf, die von Docker ignoriert werden sollen. Dies bedeutet, dass sie nicht in den Build-Prozess von Docker einbezogen werden und somit nicht Teil des Containerimages sind.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* ".git": Der Ordner ".git" enthält alle Versionskontrolldateien für das Projekt. Diese sollten nicht in den Containerimage aufgenommen werden, da sie nur von Git verwendet werden sollen und nicht Teil des tatsächlichen Produkts sind.
* ".github": Der Ordner ".github" enthält die GitHub-Dateien und -Ordner. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* ".gitignore": Die Datei ".gitignore" dient als Ignorierliste für Git. Sie listet Dateien und Ordner auf, die von Git ignoriert werden sollen.
* ".env": Die Datei ".env" enthält die Umgebungsvariablen des Projekts. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "node_modules/": Der Ordner "node_modules/" enthält alle Node-Modules des Projekts. Diese sollten nicht in den Containerimage aufgenommen werden, da sie nur von der Entwicklungsumgebung benötigt werden und nicht Teil des tatsächlichen Produkts sind.
* "vendor/": Der Ordner "vendor/" enthält alle Abhängigkeiten des Projekts. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "storage/framework/cache/": Der Ordner "storage/framework/cache/" enthält den Cache der Anwendung. Dieser sollten nicht in den Containerimage aufgenommen werden, da er nur von der Development-Umgebung verwendet wird und nicht Teil des tatsächlichen Produkts ist.
* "storage/framework/sessions/": Der Ordner "storage/framework/sessions/" enthält die Session-Dateien der Anwendung. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "storage/framework/views/": Der Ordner "storage/framework/views/" enthält die View-Dateien der Anwendung. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "storage/logs/": Der Ordner "storage/logs/" enthält die Log-Dateien der Anwendung. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "public/build/": Der Ordner "public/build/" enthält die kompilierten Ressourcen des Projekts. Diese sollten nicht in den Containerimage aufgenommen werden, da sie nur von der Development-Umgebung benötigt werden und nicht Teil des tatsächlichen Produkts sind.
* "public/hot": Der Ordner "public/hot" enthält die Hot-Code-Push-Dateien des Projekts. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "tests/": Der Ordner "tests/" enthält alle Testdateien des Projekts. Diese sollten nicht in den Containerimage aufgenommen werden, da sie nur von der Development-Umgebung benötigt werden und nicht Teil des tatsächlichen Produkts sind.
* ".phpunit.cache/": Die Datei ".phpunit.cache/" dient als Cache für PHPUnit-Tests. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "coverage/": Der Ordner "coverage/" enthält die Code-Coverage-Dateien der Anwendung. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "*.log": Die Datei "*.log" dient als Log-Datei für alle Protokolle des Projekts. Diese sollte nicht in den Containerimage aufgenommen werden, da sie nur von der Development-Umgebung benötigt werden und nicht Teil des tatsächlichen Produkts ist.
* "*.sqlite": Die Datei "*.sqlite" dient als Datenbank für die Anwendung. Diese sollten ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "*.sqlite-shm": Die Datei "*.sqlite-shm" dient als temporäre Datei für SQLite. Diese sollte ebenfalls ignoriert werden, um den Containerimage zu minimieren.
* "*.sqlite-wal": Die Datei "*.sqlite-wal" dient als temporäre Datei für SQLite. Diese sollte ebenfalls ignoriert werden, um den Containerimage zu minimieren.

Die Datei ".dockerignore" listet auch einige andere Dateien und Ordner auf, die von Docker ignoriert werden sollten, wie zum Beispiel "docker-compose*.yml" und "docker-compose*.yaml". Diese dienen dazu, Docker-Dateien in der Development-Umgebung zu speichern.

Die Datei ".env.example" sollte auch ignoriert werden, da sie nur von Git verwendet wird und nicht Teil des tatsächlichen Produkts ist.

Insgesamt bietet diese Datei eine Liste von Dateien und Ordner, die von Docker ignoriert werden sollen, um den Containerimage zu minimieren und die Größe des Images zu reduzieren.

---

### .editorconfig
This is an `.editorconfig` file that specifies the settings for a project using the `EditorConfig` format. The file contains several sections with different options and values, which are applied to different types of files. Here's a breakdown of each section:

* `[*]` - This section applies to all files in the project. It sets the character encoding to `utf-8`, the line ending to `lf` (i.e., `\n`), and the indent size to 4 spaces. It also specifies that the final newline should be trimmed and that trailing whitespace should be trimmed from all files.
* `[*.md]` - This section applies to Markdown files. It sets the indent size to 2 spaces, which is the default value for Markdown files. However, it disables trimming of trailing whitespace for these files.
* `[*.{yml,yaml}]` - This section applies to YAML files. It sets the indent size to 4 spaces, which is the standard indent size used in YAML files.
* `[compose.yaml]` - This section applies to `docker-compose.yaml` files. It sets the indent size to 4 spaces, which is the standard indent size used in these files.

Overall, this `.editorconfig` file specifies a set of consistent coding standards for all files in the project, including the character encoding, line ending, and indentation style.

---

### app/Actions/Fortify/CreateNewUser.php
This file is part of the Laravel Fortify package, which provides a set of pre-configured authentication and registration features for Laravel. The `CreateNewUser` class in this file is a custom implementation of the `CreatesNewUsers` interface, which defines the behavior of creating a new user.

Here's a summary of the important classes and functions in this file:

* `App\Actions\Fortify\CreateNewUser`: This is the main class that defines the `create()` method, which is responsible for validating and creating a new user. The `Validator` facade is used to validate the input data, and the `PasswordValidationRules` trait is used to define password validation rules.
* `App\Models\User`: This is the model class that represents the user entity. It has properties like `name`, `email`, and `password`.
* `Illuminate\Support\Facades\Validator`: This facade provides a convenient way to validate data, including the password input.
* `Laravel\Fortify\Contracts\CreatesNewUsers`: This is an interface that defines the contract for creating new users. The `CreateNewUser` class implements this interface and provides its own implementation of the `create()` method.

---

### app/Actions/Fortify/PasswordValidationRules.php
Die Datei `app/Actions/Fortify/PasswordValidationRules.php` enthält eine Klasse `PasswordValidationRules`, die den Validierungsregeln für Passwörter von Laravel Fortify implementiert. Die Regeln sind in der Methode `passwordRules()` definiert und bestehen aus den folgenden Regeln:

* `required`: Das Feld muss ausgefüllt sein (d.h., es muss ein Wert vorhanden sein).
* `string`: Der Wert muss eine Zeichenkette sein.
* `Password::default()`: Der Wert muss ein gültiges Passwort sein, das den Standard-Anforderungen von Laravel Fortify entspricht.
* `confirmed`: Der Wert muss für die Bestätigung des Passworts verwendet werden.

Dies bedeutet, dass der Benutzer das Passwort und seine Wiederholung eingeben muss, um eine gültige Anmeldung durchzuführen. Die Regeln sind in der Methode `passwordRules()` definiert und können von anderen Klassen in Laravel verwendet werden, um Passwörter zu validieren.

Es gibt keine externen Abhängigkeiten in dieser Datei, da sie Teil des Laravel-Frameworks ist und auf die vom Framework bereitgestellten Hilfsklassen und Methoden angewiesen ist.

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei `app/Actions/Fortify/ResetUserPassword.php` implementiert das Verhalten des Passwort-Resets in der Laravel-Applikation, indem sie die Funktion `reset()` implementiert, die den Benutzer mit einem neuen Passwort aktualisiert.

Die Klasse `ResetUserPassword` erweitert die Schnittstelle `ResetsUserPasswords`, um den Verlauf des Passwort-Resets zu speichern und die Sicherheitsanforderungen der Applikation zu erfüllen. Die Klasse verwendet auch eine Reihe von Validierungsregeln, um das neue Passwort zu überprüfen und sicherzustellen, dass es den Anforderungen des Projekts entspricht.

Die wichtigsten Funktionen dieser Datei sind:

* Die Implementierung der `reset()`-Methode, die den Benutzer mit einem neuen Passwort aktualisiert.
* Die Verwendung von `Validator`-Klasse zur Überprüfung des neuen Passworts auf Gültigkeit.
* Die Verwendung von `forceFill()`-Methode um das neue Passwort in der Benutzerdatenbank zu speichern.

Die Datei enthält auch externe Abhängigkeiten, darunter die `Illuminate\Support\Facades\Validator`-Klasse, um Validierungsregeln für das neue Passwort zu erstellen und die `App\Models\User`-Klasse, um den Benutzer zu aktualisieren.

Insgesamt ist diese Datei ein wichtiger Bestandteil der Laravel-Applikation, da sie es ermöglicht, dass Benutzer ihre Passwörter zurücksetzen und die Sicherheitsanforderungen des Projekts erfüllen.

---

### app/Filament/Resources/ContactResource.php
This file contains a PHP class named `ContactResource` that extends the Filament resource class. It is used to manage the `Contact` model, which represents a contact request from a visitor on the website.

The class defines several static methods and properties:

* `model`: Specifies the name of the Eloquent model that is associated with this resource (`App\Models\Contact`).
* `navigationIcon`: Defines the icon that should be displayed in the navigation menu for this resource (`heroicon-o-envelope`).
* `navigationLabel`: Defines the label that should be displayed in the navigation menu for this resource (`Kontaktanfragen`).
* `form`: Defines a schema that is used to generate the form for creating and editing contacts. The schema consists of four text fields: `name`, `company`, `email`, and `project_type`. The `message` field is a textarea component that takes up the full width of the form.
* `table`: Defines a table that is used to display a list of contacts. The table has four columns: `name`, `email`, `project_type`, and `created_at`. The `created_at` column displays the timestamp in the format of `d.m.Y H:i`.
* `getPages`: Returns an array of page classes that should be available for this resource. In this case, there are two pages: `ListContacts` and `ViewContact`.

The `ListContacts` page displays a list of all contacts, along with their names, emails, project types, and creation timestamps. The page also includes actions for viewing and deleting contacts.

The `ViewContact` page displays the details of a single contact, including its name, company, email, project type, message, and creation timestamp. The page also includes an action for deleting the contact.

---

### app/Filament/Resources/UserResource.php
This is a PHP file that defines a `UserResource` class, which extends the `Resource` class provided by the Filament framework. The resource defines a set of operations that can be performed on a user entity.

The `UserResource` class has the following members:

* `$model`: A string representing the Eloquent model class used to interact with the user data. In this case, it is set to `App\Models\User::class`.
* `$navigationIcon`: A string representing the icon used in the navigation menu for this resource. In this case, it is set to `'heroicon-o-users'`.
* `$navigationLabel`: A string representing the label displayed in the navigation menu for this resource. In this case, it is set to `'Nutzer'`.
* `form()`: A method that returns a form schema used for creating and editing user records. The method takes a `Schema` object as an argument and returns a new `Schema` object with the specified fields added. In this case, the form includes three text input fields: `name`, `email`, and `status`.
* `table()`: A method that returns a table schema used for displaying user records in a list. The method takes a `Table` object as an argument and returns a new `Table` object with the specified columns added. In this case, the table includes four columns: `name`, `email`, `status`, and `roles.name`.
* `getPages()`: A method that returns an array of page classes associated with this resource. The method takes no arguments and returns an array of page class names as strings. In this case, the array contains two elements: `'index'` and `'edit'`.

The `UserResource` class also defines a set of static methods for performing various operations on user records, such as creating new users, editing existing users, and deleting users. These methods are not shown in the code snippet provided.

---

### app/Http/Middleware/TrackPageView.php
This file is a PHP class named `TrackPageView` that is part of the Laravel framework. It is used to track page views in a website, and to store this information in a database. The class uses the `Closure` interface for handling requests, which means it can be easily integrated into the Laravel routing system.

The class has two main methods:

* `handle()`: This method is called every time a request comes in, and it checks if the request is a GET request and if the user agent of the request is not a known bot (based on a list of patterns). If both conditions are true, then it creates or increments a page view record in the database. The method returns the response after handling the request.
* `anonymizeIp()`: This method takes an IP address as input and returns an anonymous version of the IP address. It does this by zeroing out the last 80 bits for IPv6 addresses, and zeroing out the last octet for IPv4 addresses. This is done to prevent identifying individual users based on their IP address.
* `isBot()`: This method takes a user agent string as input and checks if it contains any known bot patterns. If it does, then it returns true, otherwise false.

The class also has two private constants: `BOT_PATTERNS` which is an array of regular expressions that match known bot user agents, and `PAGE_VIEWS_TABLE` which is the name of the table in the database where page views are stored.

Overall, this class is used to track page views in a Laravel application, and to store this information in a database for later analysis or reporting.

---

### app/Http/Middleware/VerifyLangdockSignature.php
This file is a middleware for Laravel that verifies the signature of incoming requests from the Langdock service. It is an important part of the security mechanism of the project, as it ensures that only authorized requests are processed and that no unauthorized changes can be made to the system.

The middleware does the following:

1. Checks if a secret key has been configured for the Langdock service in the configuration file. If not, it aborts with an error message.
2. Verifies the signature of the incoming request by comparing it with the expected signature based on the timestamp and the secret key. If the signatures do not match, it aborts with a 403 Forbidden response.
3. Implements replay protection by storing nonces in the cache for a certain amount of time (24 hours). If an incoming request has already been seen before, it is rejected with a 403 Forbidden response.
4. Passes the request to the next middleware in the chain if everything is correct.

The middleware is implemented as a class called `VerifyLangdockSignature`, which extends the `Closure` class and implements the `handle` method. The `handle` method receives the incoming request and the next middleware in the chain as arguments, and it performs the signature verification and replay protection logic before passing the request to the next middleware.

The middleware is registered in the `kernel.php` file of the Laravel project, where it is specified which routes should be protected by this middleware. In this case, all incoming requests to the `/webhook` route are passed through this middleware.

---

### app/Http/Middleware/VerifyMcpToken.php
Die Datei `app/Http/Middleware/VerifyMcpToken.php` enthält eine PHP-Klasse namens `VerifyMcpToken`, die ein Middleware ist und eine bestimmte Anforderung überprüft, um sicherzustellen, dass es eine gültige Authentifizierungs-Token in der Headerzeile gibt.

Die Klasse verwendet die folgenden Funktionen:

* `handle`: Diese Methode nimmt eine Anforderung und einen Closure als Argumente und überprüft den Autorisationsheader der Anforderung, um sicherzustellen, dass es ein gültiger Bearer-Token gibt. Wenn dies nicht der Fall ist, wird eine HTTP-Antwort mit dem Statuscode 401 (Unauthorized) zurückgegeben.
* `config`: Diese Funktion wird verwendet, um den Wert des Konfigurationsparameters `services.mcp.auth_token` zu lesen und sicherzustellen, dass er konfiguriert ist.
* `hash_equals`: Diese Funktion wird verwendet, um den Wert des Autorisationsheaders mit dem konfigurierten Token zu vergleichen. Wenn dies nicht der Fall ist, wird eine HTTP-Antwort mit dem Statuscode 401 (Unauthorized) zurückgegeben.
* `substr`: Diese Funktion wird verwendet, um das Bearer-Token aus dem Autorisationsheader zu extrahieren und es mit dem konfigurierten Token zu vergleichen. Wenn dies nicht der Fall ist, wird eine HTTP-Antwort mit dem Statuscode 401 (Unauthorized) zurückgegeben.
* `abort`: Diese Funktion wird verwendet, um eine HTTP-Antwort mit einem bestimmten Statuscode und einer bestimmten Nachricht zu generieren.

Dieser Middleware wird verwendet, um sicherzustellen, dass jede Anforderung, die an den Endpunkt der API gerichtet ist, über einen gültigen Authentifizierungs-Token verfügt. Wenn ein ungültiger Token angegeben wird oder kein Token konfiguriert ist, wird eine HTTP-Antwort mit dem Statuscode 401 (Unauthorized) zurückgegeben.

---

### app/Jobs/IngestPaperJob.php
This is a PHP file that defines a job class for ingesting papers into the system. The job is defined using the `Illuminate\Bus\Queueable` trait, which provides functionality for queueing and managing jobs in a Laravel application.

The job class contains several public properties and methods:

* `__construct()`: The constructor method takes several parameters as input: `$paperId`, `$source`, `$title`, `$text`, and `$metadata`. These parameters are used to initialize the job instance with the necessary data.
* `handle()`: This is the main entry point of the job class. It contains the logic for handling the ingestion of a paper into the system, including calling the Ollama API to embed the text content and storing the embedding in the database. The method uses the `Http` facade to make a POST request to the Ollama API with the appropriate parameters, and it handles errors by logging them using the `Log` facade.
* `chunkText()`: This is a private method that splits the text content of a paper into chunks of 500 words or less. The method uses regular expressions to split the text into an array of strings, and it returns the resulting chunks as a two-dimensional array.
* `$tries` and `$backoff`: These are public properties that define the maximum number of retries for the job and the backoff time between retries, respectively.

The `IngestPaperJob` class also uses several external dependencies:

* The `Illuminate\Contracts\Queue\ShouldQueue` interface is used to indicate that the job can be queued.
* The `Illuminate\Foundation\Bus\Dispatchable` trait is used to provide a mechanism for dispatching jobs.
* The `Illuminate\Queue\InteractsWithQueue` trait is used to provide access to the queue system.
* The `Illuminate\Support\Facades\DB` facade is used to interact with the database.
* The `Illuminate\Support\Facades\Http` facade is used to make HTTP requests to external APIs, such as the Ollama API.
* The `Illuminate\Support\Facades\Log` facade is used for logging purposes.

---

### app/Jobs/TriggerLangdockAgent.php
This is a PHP class file for the `App\Jobs\TriggerLangdockAgent` job. The job handles sending a webhook request to the Langdock API to trigger an agent for a specific project and user.

The class imports several dependencies from Laravel's framework, including `Illuminate\Bus\Queueable`, `Illuminate\Contracts\Queue\ShouldBeUnique`, `Illuminate\Contracts\Queue\ShouldQueue`, and `Illuminate\Foundation\Bus\Dispatchable`. It also uses the `App\Models\Webhook` model and the `App\Models\User` model.

The class defines a constructor that takes three parameters: `userId`, `projektId`, and `eingabe`. These parameters are used to construct the webhook request.

The `uniqueId()` method returns the `projektId` parameter, which is used to ensure that only one job instance for each project is created.

The `handle()` method sends a POST request to the Langdock API with the following headers:
```php
Authorization: Bearer <api_key>
Content-Type: application/json
```
The request body contains the following data:
```json
{
    "user_id": <userId>,
    "user_status": "trial", // default status for new users
    "projekt_id": <projektId>,
    "eingabe": <eingabe>
}
```
The `Log::error()` method is used to log any errors that occur during the request. If the response from the Langdock API has a non-successful status code (i.e., anything other than 200), the job will fail with an exception indicating the error message and status code.

The class also defines the following properties:
```php
public int $tries = 3; // number of times to retry the job if it fails
public int $backoff = 30; // backoff delay in seconds for retries
public int $uniqueFor = 300; // time window in seconds within which only one job instance per project is allowed
```
These properties are used to control the retry behavior of the job.

---

### app/Livewire/Actions/Logout.php
Die Datei `app/Livewire/Actions/Logout.php` enthält eine Klasse namens `Logout`, die es ermöglicht, den aktuellen Benutzer aus der Anwendung zu loggen.

Die wichtigsten Funktionen dieser Datei sind:

* Die `__invoke()`-Methode, die den Benutzer aus dem Web-Guard ausloggt und das Session-Token invalidiert.
* Die `redirect()`-Methode, die den Benutzer an die Startseite der Anwendung weiterleitet.

Diese Klasse verwendet externe Abhängigkeiten:

* `Illuminate\Support\Facades\Auth` für den Zugriff auf die Authentifizierungs- und Sicherheitsfunktionen von Laravel.
* `Illuminate\Support\Facades\Session` für den Zugriff auf die Session-Funktionen von Laravel.

Diese Klasse ist Teil des Livewire-Frameworks von Laravel, das es ermöglicht, serverseitige Logik in Echtzeit anzuwenden und dynamische Benutzeroberflächen zu erstellen.

---

### app/Mail/ContactConfirmationMail.php
This is a PHP file that defines a class named `ContactConfirmationMail` within the namespace `App\Mail`. The class is derived from the `Mailable` class and implements the `ShouldQueue` interface. It contains two methods: `__construct()` and `envelope()`.

The `__construct()` method takes a single argument of type `Contact`, which is an instance of the `Contact` model. This object is used to pass data to the mailable object.

The `envelope()` method returns an instance of the `Envelope` class, which defines the properties of the email that is being sent. In this case, it sets the subject line of the email to "Ihre Anfrage bei Linn Games".

The `content()` method returns an instance of the `Content` class, which defines the content of the email. In this case, it uses a view named "emails.contact-confirmation" as the body of the email.

This file is likely used to send a confirmation email to users after they submit a contact form on the Linn Games website. The `Contact` model represents the data submitted in the contact form, and the `ContactConfirmationMail` class defines how that data is used to generate the email that is sent to the user.

---

### app/Mail/ContactInquiryMail.php
Die Datei `app/Mail/ContactInquiryMail.php` enthält eine Klasse, die den Eintrag `App\Mail\ContactInquiryMail` darstellt und von der `Illuminate\Mail\Mailable`-Klasse erbt. Die Klasse hat folgende Funktionen:

* `__construct`: Erstellt ein neues Objekt des Typs `ContactInquiryMail`. Der Konstruktor enthält einen Parameter, der den `Contact`-Eintrag darstellt, über den die Nachricht gesendet werden soll.
* `envelope`: Gibt eine Envelope-Instanz zurück, die die Eigenschaften der Nachricht beschreibt, wie z.B. Betreff und Antwortadresse. In diesem Fall ist der Betreff "Neue Kontaktanfrage von {$this->contact->name}" und die Antwortadresse entspricht dem Wert des `email`-Attributs des `Contact`-Eintrags.
* `content`: Gibt eine Content-Instanz zurück, die die Inhalte der Nachricht beschreibt, wie z.B. den View-Dateinamen. In diesem Fall ist der Dateiname "emails.contact-inquiry".

Die Klasse verwendet die folgenden Traits:

* `Illuminate\Bus\Queueable`: Ermöglicht das Senden von E-Mails in der Warteschlange, um die Leistung zu verbessern.
* `Illuminate\Mail\SerializesModels`: Ermöglicht das Serialisieren von Modellinstanzen und das Einbetten von Daten in die Nachricht.

Die Klasse implementiert auch die `ShouldQueue`-Schnittstelle, die es ermöglicht, die Verarbeitung der E-Mails in der Warteschlange zu aktivieren.

Es gibt außerdem eine externe Abhängigkeit vom `Contact`-Modell, das verwendet wird, um den Eintrag des Kontakts zu erhalten, über den die Nachricht gesendet werden soll.

---

### app/Mail/DeployNotificationMail.php
This is a PHP file that defines a class called `DeployNotificationMail` which extends the `Mailable` class from Laravel framework. The class has two public properties: `deployedAt` and `appUrl`. The constructor initializes these properties with the given values.

The `envelope()` method of the class returns an instance of the `Envelope` class, which is used to define the email envelope (i.e., the recipient, sender, subject, etc.). In this case, the subject of the email is set to "Linn.Games — Deployment erfolgreich".

The `content()` method returns an instance of the `Content` class, which is used to define the content of the email (i.e., the body and any attachments). In this case, the content is defined using a view called `emails.deploy-notification`.

Overall, this file defines a custom Laravel mailable class that can be used to send deployment notification emails.

---

### app/Policies/ProjektPolicy.php
This file contains the logic for managing access control for projects. The ProjektPolicy class defines methods for viewing, creating, updating, and deleting projects based on a user's ID.

---

### app/Providers/AppServiceProvider.php
The provided code defines a class `AppServiceProvider` that extends Laravel's built-in service provider. The class contains two methods: `register` and `boot`.

In the `register` method, the code registers the `ProjektPolicy` class as a policy for the `Projekt` model using the `Gate::policy()` method. This means that the `ProjektPolicy` class will be used to determine which actions are allowed on instances of the `Projekt` model.

In the `boot` method, the code uses Laravel's rate limiting feature to set up two rate limits: one for MCP (minute-based) and one for webhooks (minute-based). The `RateLimiter::for()` method is used to define a new rate limit. The first argument is the name of the rate limit, and the second argument is a callable that returns the limit object.

The code also checks if the application is in production mode by checking the value of `$this->app->isProduction()`. If it is, the `URL::forceScheme()` method is used to force all URLs to use the "https" scheme. This ensures that any requests made to the server will be secure.

Overall, this code sets up some basic rate limiting for MCP and webhooks, as well as forces all URLs to use HTTPS when the application is in production mode.

---

### app/Providers/FortifyServiceProvider.php
This is a Laravel service provider class for the Fortify package, which provides a set of functions for authenticating and registering users. The class is part of the `app/Providers` directory in a Laravel project.

The class defines several methods that are called during the bootstrapping process:

* `register`: This method registers any application services that Fortify needs, such as creating new users or resetting user passwords.
* `boot`: This method configures various aspects of Fortify's behavior, including setting up views for the login and registration pages, setting up rate limiting for login attempts, and defining actions to be taken when a user requests a password reset link or verifies their email address.

In addition to these methods, the class also defines several private methods that are used to configure Fortify's behavior in more detail:

* `configureActions`: This method sets up the actions to be taken when a user creates a new account or requests a password reset link.
* `configureViews`: This method sets up the views for the login, registration, and password reset pages.
* `configureRateLimiting`: This method sets up rate limiting for login attempts.

Overall, this class provides a way to customize and extend the functionality of Fortify's authentication system in a Laravel project.

---

### app/Providers/VoltServiceProvider.php
The file "app/Providers/VoltServiceProvider.php" is a service provider for the Livewire framework. It provides a mounting point for the Volt template engine, which allows developers to use PHP in their templates.

Here's a summary of the main functionalities of the file:

* The `register()` method does not register any services.
* The `boot()` method mounts the Volt template engine on the Livewire view path and also mounts the views folder in the root directory. This allows developers to use PHP in their templates.

The external dependencies of this file are:

* The `Illuminate\Support\ServiceProvider` class, which provides a base class for creating service providers.
* The `Livewire\Volt\Volt` class, which is the Volt template engine that is being mounted in this file.
* The `config()` function, which is used to retrieve the value of the `livewire.view_path` configuration option and pass it as an argument to the `mount()` method.
* The `resource_path()` function, which is used to get the path to the views directory in the root directory.

---

### app/Providers/Filament/AdminPanelProvider.php
Diese Datei `app/Providers/Filament/AdminPanelProvider.php` ist Teil des Filament-Frameworks für Laravel. Es handelt sich um eine Klasse, die einen Panel definiert und es für die Verwendung im Framework konfiguriert.

Die wichtigsten Funktionen dieser Datei sind:

* Die Definition eines Panel-Objekts mit einem Standard-ID, Pfad und Login-Seiten.
* Die Konfiguration von Middleware-Funktionen für den Zugriff auf die Panel-Seite, darunter:
	+ `EncryptCookies`: Verschlüsselt Cookies.
	+ `AddQueuedCookiesToResponse`: Fügt queued Cookies zu der Response hinzu.
	+ `StartSession`: Startet eine neue Session.
	+ `AuthenticateSession`: Authentifiziert eine aktive Session.
	+ `ShareErrorsFromSession`: Teilt Fehler von der Session mit.
	+ `VerifyCsrfToken`: Überprüft den CSRF-Token.
	+ `SubstituteBindings`: Ersetzt Bindungen.
	+ `DisableBladeIconComponents`: Deaktiviert Blade-Ikonen-Komponenten.
	+ `DispatchServingFilamentEvent`: Sendet das `serving` Filament-Ereignis.
* Die Konfiguration von Authentifizierungs-Middleware für den Zugriff auf die Panel-Seite, darunter:
	+ `Authenticate`: Authentifiziert einen Benutzer.

---

### bootstrap/app.php
This file is the entry point of a Laravel application. It sets up the application's configuration, routing, middleware, and exceptions. Here is a summary of its main components:

1. `use App\Http\Middleware\TrackPageView;` - This line imports a custom middleware class called TrackPageView.
2. `use Illuminate\Foundation\Application;` - This line imports the Application class from the Laravel framework.
3. `use Illuminate\Foundation\Configuration\Exceptions;` - This line imports the Exceptions class from the Laravel framework, which is used to configure the application's exceptions.
4. `use Illuminate\Foundation\Configuration\Middleware;` - This line imports the Middleware class from the Laravel framework, which is used to configure the application's middleware.
5. `use Illuminate\Http\Request;` - This line imports the Request class from the Laravel framework, which is used to handle HTTP requests.
6. `return Application::configure(basePath: dirname(__DIR__))` - This line creates a new instance of the Application class and sets its base path to the current directory.
7. `.withRouting(web: __DIR__.'/../routes/web.php', api: __DIR__.'/../routes/api.php', commands: __DIR__.'/../routes/console.php', health: '/up')` - This line configures the application's routing. It defines the paths for the web, API, and console routes, as well as a health check route at `/up`.
8. `.withMiddleware(function (Middleware $middleware): void {` - This line configures the application's middleware. It defines a callback function that takes a Middleware instance as an argument and appends the TrackPageView middleware to the 'web' group.
9. `.trustProxies(at: '*', headers: Request::HEADER_X_FORWARDED_FOR | Request::HEADER_X_FORWARDED_PROTO)` - This line configures the application's trusted proxies. It specifies that all incoming requests should be trusted, and that the `X-Forwarded-For` and `X-Forwarded-Proto` headers should be used to determine the client's IP address and protocol.
10. `.create();` - This line creates a new instance of the Application class and returns it.

In summary, this file sets up the basic configuration for a Laravel application, including routing, middleware, and exceptions.

---

### bootstrap/providers.php
Die Datei "bootstrap/providers.php" registriert einige Provider-Klassen, die als Dependencies für das Laravel-Framework geladen werden. Hier sind die wichtigsten Punkte:

* App\Providers\AppServiceProvider::class: Ein Provider, der die grundlegenden Dienste und Funktionen des Frameworks bereitstellt.
* App\Providers\Filament\AdminPanelProvider::class: Ein Provider, der den Admin-Panel-Bereich des Frameworks registriert.
* App\Providers\FortifyServiceProvider::class: Ein Provider, der die Authentifizierung und Autorisierung des Frameworks bereitstellt.
* App\Providers\VoltServiceProvider::class: Ein Provider, der das Volt-Templating-System des Frameworks registriert.

Es gibt keine external dependencies in dieser Datei.

---

### bootstrap/cache/.gitignore
Die Datei '.gitignore' in der Bootstrap-Cache-Verzeichnis ist ein Git-Ignorierplikt, der festlegt, welche Dateien und Ordner von Git ignoriert werden sollen. Diese Datei wird verwendet, um die Cache-Datenbank zu aktualisieren und zu verhindern, dass die Cache-Dateien in das Git-Repository eingebunden werden.

---

### docker/common/nginx/default.conf
This is an Nginx configuration file for a web application. The file sets up several server blocks, each with its own set of directives. Here are the main functions and external dependencies of this file:

* `server { ... }`: This is a server block that listens on port 80 and serves files from the `/var/www/public` directory. The `root` directive sets the document root for this server block to this path. The `index` directive specifies the default index file for this server block, which in this case is `index.php`.
* `resolver 127.0.0.11 valid=10s ipv6=off;`: This line sets up a DNS resolver that uses the local loopback address (127.0.0.11) to resolve hostnames for this server block. The `valid` directive specifies how often Nginx should check the DNS cache for new records, and the `ipv6=off` directive disables IPv6 lookups.
* `location ~ ^/vendor/livewire/(.*)$ { ... }`: This is a location block that matches any URL starting with `/vendor/livewire/` and passes it through to a PHP-FPM backend server for processing. The `fastcgi_pass` directive specifies the IP address and port of the PHP-FPM server, and the `include fastcgi_params` line includes the default set of FastCGI parameters.
* `location ~ ^/livewire/(.*)$ { ... }`: This is another location block that matches any URL starting with `/livewire/` and passes it through to a PHP-FPM backend server for processing.
* `location ~* \.(jpg|jpeg|png|gif|ico|css|svg|woff|woff2|ttf|eot)$ { ... }`: This is a location block that matches any URL ending with one of the extensions listed and sets caching headers to expire in one year. The `expires` directive specifies the expiration time, and the `add_header` directive adds a cache-control header to prevent caching.
* `location ~* ^/(?!livewire/|vendor/livewire/).*\.js$ { ... }`: This is a location block that matches any URL ending with `.js`, but not starting with `/livewire/` or `/vendor/livewire/`. The `expires` directive specifies the expiration time, and the `add_header` directive adds a cache-control header to prevent caching.
* `location /build/ { ... }`: This is a location block that matches any URL starting with `/build/`, sets caching headers to expire in one year, and disables caching.
* `location /images/ { ... }`: This is a location block that matches any URL starting with `/images/` and sets caching headers to expire in one year. The `add_header` directive adds a cache-control header to prevent caching.
* `location = /phpinfo.php { ... }`: This is a location block that matches the exact URL `/phpinfo.php`, passes it through to PHP-FPM for processing, and includes the default set of FastCGI parameters.
* `resolver 127.0.0.11 valid=10s ipv6=off;`: This line is similar to the one in the topmost server block, but sets up a DNS resolver for the MCP PostgreSQL endpoint (SSE).
* `location /mcp/ { ... }`: This is a location block that matches any URL starting with `/mcp/` and uses a proxy to pass requests through to a PostgreSQL backend server. The `set` directive sets up variables for authentication, the `rewrite` directive rewrites URLs to strip off the leading `/mcp/` path component, and the `proxy_pass` directive specifies the IP address and port of the PostgreSQL server.
* `location /messages/ { ... }`: This is another location block that matches any URL starting with `/messages/` and uses a proxy to pass requests through to the same PostgreSQL backend server as in the previous block.
* `resolver 127.0.0.11 valid=10s ipv6=off;`: This line is similar to the one in the topmost server block, but sets up a DNS resolver for the Ollama embedding endpoint (Langdock Embedding Model).
* `location /ollama/ { ... }`: This is a location block that matches any URL starting with `/ollama/` and uses a proxy to pass requests through to an Ollama backend server. The `set` directive sets up variables for authentication, the `rewrite` directive rewrites URLs to strip off the leading `/ollama/` path component, and the `proxy_pass` directive specifies the IP address and port of the Ollama server.
* `location / { ... }`: This is a location block that matches any URL not matched by other blocks, and uses try_files to pass requests through to PHP-FPM for processing. The `try_files` directive includes the default set of FastCGI parameters.

---

### docker/common/nginx/docker-entrypoint.sh
Die Datei docker/common/nginx/docker-entrypoint.sh ist ein Bash-Skript, das vom Docker-Container nginx als Einstiegspunkt verwendet wird. Es enthält eine Reihe von Funktionen und Klassen, die es ermöglichen, den Container zu starten und ihn mit einer bestimmten Konfiguration auszustatten.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `set -e`: Setzt den Exit-Code auf 1, wenn eine Fehlerbedingung auftritt.
* `TEMPLATE="/etc/nginx/templates/default.conf.template"`: Gibt den Pfad zum Nginx-Konfigurationsvorlagen-Template an.
* `TARGET="/etc/nginx/conf.d/default.conf"`: Gibt den Pfad zum generierten Nginx-Konfigurationsdatei an.
* `if [ -f "$TEMPLATE" ]; then`: Überprüft, ob das Template-File existiert und führt dann die nächsten Anweisungen aus.
* `cp "$TEMPLATE" "$TARGET"`: Kopiert das Template-File in die generierte Nginx-Konfigurationsdatei.
* `sed -i "s|__MCP_AUTH_TOKEN__|${MCP_AUTH_TOKEN}|g" "$TARGET"`: Ersetzt den Platzhalter `__MCP_AUTH_TOKEN__` in der generierten Nginx-Konfigurationsdatei durch den Wert der Umgebungsvariablen `MCP_AUTH_TOKEN`.
* `exec "$@"`: Führt das angegebene Kommando als Einstiegspunkt aus und beendet die Ausführung des Skripts.

Dieses Skript verwendet die Umgebungsvariable `MCP_AUTH_TOKEN` zum Ersetzen eines Platzhalters in der Nginx-Konfigurationsdatei, um eine Verbindung zur MCP-API herzustellen.

Die Datei enthält auch einen Import des Bash-Skripts `set -e`, das es ermöglicht, die Exit-Code-Überprüfung zu aktivieren und Fehler in der Skriptausführung zu behandeln.

---

### docker/common/nginx/dockerfile
Die Datei `docker/common/nginx/dockerfile` ist ein Docker-Build-Skript für eine Nginx-Container-Implementierung. Hier sind die wichtigsten Funktionen und Klassen:

1. `FROM nginx:alpine`: Dies bedeutet, dass das Docker-Image basierend auf dem Alpine-Nginx-Image erstellt wird.
2. `COPY ./docker/common/nginx/nginx.conf /etc/nginx/nginx.conf`: Hier werden die benutzerdefinierten Nginx-Konfigurationen kopiert, die sich im Verzeichnis `./docker/common/nginx` befinden.
3. `COPY ./docker/common/nginx/default.conf /etc/nginx/conf.d/default.conf`: Hier werden die site-spezifischen Nginx-Konfigurationen kopiert, die sich im Verzeichnis `./docker/common/nginx` befinden.
4. `COPY ./docker/common/nginx/docker-entrypoint.sh /docker-entrypoint.d/30-inject-secrets.sh`: Hier wird ein Bash-Skript kopiert, das verwendet wird, um die secrets-Dateien während des Build-Prozesses zu injizieren.
5. `COPY ./public /var/www/public`: Hier werden die statischen Dateien kopiert, die sich im Verzeichnis `./public` befinden.
6. `WORKDIR /var/www/public`: Hier wird der Arbeitsverzeichnis für den Container definiert.
7. `EXPOSE 80`: Hier wird angegeben, dass der Container Port 80 auslöst.
8. `CMD ["nginx", "-g", "daemon off;"]`: Hier wird das Befehlssystem für den Container festgelegt.

Das Skript importiert keine externen Abhängigkeiten und verwendet nur die Docker-Engine zum Erstellen des Containers.

---

### docker/common/nginx/nginx.conf
This is an Nginx configuration file used for a Laravel application. The file sets various configuration options and includes other configuration files using the `include` directive.

Here are the main functions of this file:

* Set the maximum number of worker connections to 1024.
* Enable gzip compression with various types of files that can be compressed.
* Set the keepalive timeout to 65 seconds.
* Include other configuration files located in the `/etc/nginx/conf.d` directory using the `include` directive.
* Trust X-Forwarded headers from Docker and upstream proxies using the `set_real_ip_from` directives.
* Set the default mime type to application/octet-stream.
* Enable file upload limits of 100 MB.

This file is included in the Docker image for the Laravel application, which means that it will be used when the container runs. The `include` directive allows the file to include other configuration files located in the `/etc/nginx/conf.d` directory, which can be used to customize the Nginx configuration for the specific needs of the application.

---

### docker/common/php-fpm/dockerfile
This Dockerfile is used to build a container image for a PHP application that runs on the FPM (FastCGI Process Manager) server. It includes multiple stages, each of which has its own set of instructions for building the final image. Here's a breakdown of each stage and what it does:

1. **Stage 1: Build environment and Composer + npm dependencies**
	* This stage is based on the `php:8.4-fpm` image, which means it includes PHP 8.4 and FPM server.
	* It installs the necessary system packages for PHP extensions, including `libpq-dev`, `libicu-dev`, `libzip-dev`, and `nodejs`.
	* It also installs Composer and npm dependencies using `composer install` and `npm ci`.
	* This stage copies the entire Laravel application code into the container.
2. **Stage 2: Production environment**
	* This stage is based on the same `php:8.4-fpm` image as the first stage.
	* It installs runtime libraries required for PHP extensions, including `libpq-dev`, `libicu-dev`, and `libzip-dev`.
	* It copies the dependencies from the build stage into this stage using `COPY --from=builder /usr/local/lib/php/extensions/ /usr/local/lib/php/extensions/` and `COPY --from=builder /usr/local/etc/php/conf.d/ /usr/local/etc/php/conf.d/`.
	* It uses the default production PHP configuration, which is stored in `$PHP_INI_DIR/php.ini-production`.
	* This stage copies the application code and dependencies from the build stage into this stage using `COPY --from=builder /var/www /var/www`.
	* It copies built frontend assets from the build stage to this stage using `COPY --from=builder /var/www/public/build /var/www/public/build`.
3. **Stage 3: Testing environment (includes dev dependencies)**
	* This stage is based on the same `php:8.4-fpm` image as the second stage.
	* It sets the user to root using `USER root`.
	* It installs Composer using `curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer`.
	* It copies the full source including tests and dev config into this stage using `COPY --chown=www-data:www-data . /var/www`.
	* It sets the owner of the copied files to www-data using `&& chown -R www-data:www-data /var/www`.
	* It installs ALL dependencies, including dev/test, using `composer install --optimize-autoloader --no-interaction --no-progress --prefer-dist`.
	* This stage sets the user back to www-data using `USER www-data`.

---

### docker/common/php-fpm/entrypoint.sh
Entrypoint script for PHP-FPM container. Performs migrations, caches configuration and routes, and runs the main command.

---

### docker/common/postgres/dockerfile
Dies ist ein Dockerfile, das auf der Basis des `postgres:16`-Images erstellt wird. Das Dockerfile enthält zwei Zeilen, die es ermöglichen, Hypopg und PG-Vector für PostgreSQL 16 zu installieren. Es verwendet auch den Befehl `apt-get update` um sicherzustellen, dass die Paketlisten aktuell sind und dann verwendet den Befehl `apt-get install` um die erforderlichen Pakete zu installieren. Schließlich wird der Inhalt der `/var/lib/apt/lists`-Verzeichnis mit dem Befehl `rm -rf` gelöscht, um Speicherplatz auf dem Docker-Image zu sparen.

---

### docker/common/postgres/init-extensions.sql
Initialisiert die PostgreSQL-Erweiterungen 'hypopg', 'pg_stat_statements' und 'vector'.

---

### docs/overview-2026-04-02_0617.md
⚠ FEHLER: Timeout (120s)

---

### public/index.php
This file is part of a Laravel application, specifically the `index.php` file in the `public` directory. It contains the entry point for the application and sets up the Composer autoloader and the Laravel framework.

Here are the important classes, functions, and endpoints in this file:

* `Illuminate\Foundation\Application`: The core class of the Laravel framework that provides the necessary functionality to run the application.
* `Illuminate\Http\Request`: A class used to handle HTTP requests and provide information about the current request.
* `__DIR__`: A special variable in PHP that represents the directory where the file is located. In this case, it is the `public` directory of the Laravel application.
* `microtime(true)`: A function that returns the current time as a float. It is used to calculate the execution time of the application.
* `file_exists($maintenance = __DIR__.'/../storage/framework/maintenance.php'))`: A function that checks if the `maintenance.php` file exists in the `storage/framework` directory. If it does, the file is required and sets the application into maintenance mode.
* `require_once __DIR__.'/../vendor/autoload.php'`: This line requires the Composer autoloader for Laravel. It allows the framework to automatically load classes when they are needed.
* `$app = require_once __DIR__.'/../bootstrap/app.php';`: This line requires the `app.php` file in the `bootstrap` directory and sets the application up using the Composer autoloader. The `$app` variable is then used to handle the request.
* `$app->handleRequest(Request::capture());`: This line handles the HTTP request by capturing it using the `Request` class and passing it to the app's handling method.

---

### public/robots.txt
This file specifies the robot protocol and prohibits all access to the website.

---

### public/site.webmanifest
This file is a web manifest file, which specifies metadata about the current website and its offline functionality. It contains information such as the name of the website, its icons, theme color, background color, and display mode. The file also specifies that the website should be launched in standalone mode, meaning that it can be used without an internet connection.

The important classes and functions in this file are:

* "name" and "short_name": These specify the name of the website and its shortened version (e.g., "MyWebSite" and "MySite").
* "icons": This specifies a list of icons for the website, including their sizes and types (e.g., "192x192" and "image/png"). The "purpose" attribute is used to indicate that the icon should be used as a maskable image.
* "theme_color": This specifies the theme color of the website, which is used for the browser's address bar and other UI elements.
* "background_color": This specifies the background color of the website, which is used for the browser's tab background.
* "display": This specifies how the website should be launched, with a value of "standalone" indicating that it should be launched in standalone mode.

There are no external dependencies in this file, as it only contains metadata about the website and its offline functionality.

---

### public/.htaccess
This is an .htaccess file, which is a configuration file used by the Apache web server to rewrite URLs and handle other tasks. The file contains several directives that are used to handle requests and modify the behavior of the server.

Here's a breakdown of the directives in this file:

* `<IfModule mod_rewrite.c>`: This directive checks if the mod_rewrite module is loaded, which allows for URL rewriting.
* `<IfModule mod_negotiation.c>`: This directive checks if the mod_negotiation module is loaded, which allows for handling of HTTP headers.
* `Options -MultiViews -Indexes`: This directive disables MultiViews and Indexes options for the current directory.
* `RewriteEngine On`: This directive turns on URL rewriting for the current directory.
* `Handle Authorization Header`: This directive handles the authorization header, which is used to authenticate requests.
* `Handle X-XSRF-Token Header`: This directive handles the X-XSRF-Token header, which is used to prevent CSRF attacks.
* `Redirect Trailing Slashes If Not A Folder...`: This directive redirects trailing slashes if the request is not a folder.
* `Send Requests To Front Controller...`: This directive sends requests to the front controller, which is responsible for handling requests and routing them to appropriate handlers.

In summary, this .htaccess file contains directives that are used to handle URL rewriting, HTTP headers, and CSRF attacks. It also enables mod_rewrite and disables MultiViews and Indexes options.

---

### resources/css/app.css
This file is a CSS stylesheet for the Laravel framework. It defines the color palette and font styles used in the application. The file imports the TailwindCSS library and defines some custom variants and themes. It also includes some base styles that affect all elements in the application.

---

### resources/css/welcome.css
This is a CSS file that contains styles for the website's header and navigation. The file uses variables to define colors, borders, and other styles that are used throughout the site. It also includes media queries to adjust the layout of the navigation based on the viewport size.

The file begins with some basic reset styles to ensure consistency across different browsers and devices. It then defines a few variables for the color scheme, border styles, and other elements. These variables are used throughout the file to make it easier to change the site's appearance in one place.

Next, the file includes a few utility classes that can be applied to any element to add margin, padding, or other styles. It also includes a few helper classes for creating horizontal and vertical centering, as well as hiding elements at different breakpoints.

The main header section of the file defines the layout and styling for the navigation menu. The menu is centered on desktop screens using flexbox, but collapses into a hamburger menu icon on smaller screens. The menu includes links to various pages on the site, as well as a search bar and a login/signup button.

The file also includes media queries to adjust the layout of the navigation based on the viewport size. On larger screens, the navigation menu is displayed horizontally with each item taking up equal space. On smaller screens, the menu collapses into a single row with the hamburger menu icon.

Overall, this file provides a basic structure for defining the layout and styling of the website's header and navigation. It includes variables, utility classes, and media queries to make it easier to customize the site's appearance in different ways.

---

### resources/js/welcome.js
This is a JavaScript file that contains various functions and events for a web application. The file uses the `()` function to enclose its contents, which makes it self-executing when the page loads.

The main purpose of this file seems to be to handle user interactions and animate elements on the page. It uses a variety of techniques to achieve this, such as event listeners, timers, and animation frames.

Here's a summary of the important functions and events in this file:

* `use strict` - This is a directive that enforces strict mode for the entire script. Strict mode helps to catch common programming errors, such as using undeclared variables or assigning to read-only properties.
* `window.loadSnake3DIframe()` - This function loads an iframe containing a game into the page. It is called when the user clicks on a button to expand a card that contains a game preview.
* `window.addEventListener("scroll", ...)` - This event listener listens for scroll events on the window object and executes a callback function whenever the user scrolls the page. The callback function updates the CSS class of a header element based on the current scroll position.
* `document.querySelectorAll(...).forEach(function (el) { ... })` - This code selects all elements with specific classes and loops through them using `forEach()`. It sets up an intersection observer that observes each element for visibility changes, and adds or removes a CSS class based on the current visibility state of the element.
* `document.addEventListener("click", function (e) { ... })` - This event listener listens for clicks on any element in the document and executes a callback function whenever the user clicks on an anchor link. The callback function prevents the default behavior of the click event (which would be to navigate to a new page), and instead uses JavaScript to navigate to the desired location using the `scrollIntoView()` method.
* `setInterval(updateTelemetry, 3000)` - This sets up an interval timer that calls the `updateTelemetry()` function repeatedly every 3 seconds. The `updateTelemetry()` function simulates live data for various values and updates their display in the page.

---

### storage/logs/.gitignore
Diese Datei speichert Ignorierungen für das .git-Verzeichnis und sorgt dafür, dass bestimmte Verzeichnisse oder Dateien nicht im Git-Repository gespeichert werden.

---

### .claude/agents/code-quality-architect.md
Maximal 10-zeilige Beschreibung der Datei-Funktionen

---

### .github/copilot-instructions.md
This file contains instructions for the app.linn.games project. It covers language conventions, the most important rules, and the development workflow.

---

### .github/instructions/app_linn_games_custom.instructions.md
This file contains the instructions for the app.linn.games project, which is a research management platform that uses natural language processing (NLP) and machine learning (ML) to assist with systematic literature review. The file includes information about the architecture, conventions, and critical details of the project, and it provides a detailed overview of the project's structure and functionality.

The instructions in this file are written for experienced software architects, so they assume that the reader has some knowledge of NLP and ML concepts. The instructions cover several topics related to the project, including its architecture, development process, testing, deployment, and maintenance. The instructions also provide detailed information about the project's database schema, file structure, and external dependencies.

The file is written in a technical style, with clear and concise language that is easy for developers to understand. The instructions are organized into sections, each of which covers a specific aspect of the project. The sections include:

1. Introduction: This section provides an overview of the app.linn.games project and its goals. It also explains how the project relates to other research management platforms.
2. Architecture: This section describes the architecture of the app.linn.games project, including its components and their relationships. It also discusses the project's development process, including the coding standards, testing strategies, and deployment processes.
3. Development Process: This section provides a detailed overview of the app.linn.games development process, including the development workflow, source control systems, and versioning practices.
4. Testing: This section discusses the testing approaches and techniques used in the app.linn.games project, including unit tests, integration tests, and end-to-end tests. It also provides information about how to run tests locally and in production environments.
5. Deployment: This section describes the deployment process for the app.linn.games project, including the deployment strategies, configuration management practices, and monitoring and logging tools.
6. Maintenance: This section discusses the maintenance practices used in the app.linn.games project, including backups, version control, and release management. It also provides information about how to troubleshoot issues and perform maintenance tasks.
7. Database Schema: This section describes the database schema for the app.linn.games project, including the tables, columns, and relationships between them. It also discusses the indexing and querying strategies used in the project.
8. File Structure: This section describes the file structure for the app.linn.games project, including the folders and files, and their purposes. It also discusses how to organize and maintain the codebase.
9. External Dependencies: This section discusses the external dependencies used in the app.linn.games project, including the libraries, frameworks, and tools used in development. It also provides information about how to manage these dependencies and keep them up-to-date.
10. Conclusion: This section summarizes the key points covered in the instructions and provides a final overview of the app.linn.games project. It also encourages developers to ask questions, share their experiences, and contribute to the project's growth and development.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker Dev-Workflow — app.linn.games

---
