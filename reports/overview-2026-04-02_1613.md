---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T16:13:37.160337
model: mistral:7b-instruct
run_id: mistral:7b-instruct
mode: overview
files_total: 251
errors: 0
run_time_s: 678.6
---

# Funktions-Übersicht — 2026-04-02_1613

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 251
- **Fehler:** 0
- **Laufzeit:** 678.6s

## api

### app/Http/Controllers/ContactController.php
Diese Datei stellt eine API-Endpunkt-Implementierung für die Übergabe von Kontaktanfragen dar. Sie nutzt die Klassen Contact, Request, JsonResponse und Mail aus dem Framework Laravel. Es werden zwei Mails (ContactInquiryMail und ContactConfirmationMail) gesendet.

---

### app/Http/Controllers/Controller.php
Abstrakte Basisklasse für alle REST-API Kontrollerklassen

---

### app/Http/Controllers/DsgvoController.php
Die Datei DsgvoController.php definiert zwei Funktionen: export und deleteAccount.
export(Request $request) generiert eine JSON-Exportdatei des aktuellen Nutzers, einschließlich seiner Kontakte und Einwilligungen.
deleteAccount(Request $request) löscht den aktuellen Nutzerkonto sowie alle dazugehörigen Daten.

---

### app/Http/Controllers/LangdockChatCallbackController.php
Verarbeitet Chat-Nachrichten von der Langdock API und speichert sie in einer Datenbank (ChatMessage). Wichtigste Klasse: App\Http\Controllers\LangdockChatCallbackController. Externe Abhängigkeit: Illuminate Framework.

---

### app/Http/Controllers/LangdockWebhookController.php
Handler-Klasse für Langdock-Webhooks, validiert eingehende Daten und dispatcht eine Job-Aufgabe mit TriggerLangdockAgent. Benötigt die Request-Klasse von Laravel, die Jobs-Funktionalität und existierende Tabellen 'users' und 'projekte' in der Datenbank.

---

### app/Http/Controllers/PaperRagController.php
This file defines the PaperRagController in an API context. It contains two main functions: ingest and search. The ingest function accepts a request to queue a new paper for processing, while the search function retrieves similar papers based on user input using an embedding service and database queries.

---

### routes/api.php
```json
{
  "file_summary": "Defines API routes for handling webhooks from Langdock and PaperRag. It includes middleware for signature verification (Langdock) and token verification (MCP). Imports App\Http\Controllers\LangdockWebhookController, App\Http\Controllers\PaperRagController, App\Http\Middleware\VerifyLangdockSignature, App\Http\Middleware\VerifyMcpToken. Also uses Illuminate\Support\Facades\Route and middleware 'auth:sanctum'."
}
```

---

### routes/console.php
Defines custom Artisan commands for 'inspire' and 'deploy:notify'. Imports App\Mail\DeployNotificationMail, uses Illuminate\Foundation\Inspiring, Illuminate\Support\Facades\Artisan, Illuminate\Support\Facades\Mail.

---

### routes/web.php
Defines various routes for web application, including main page, contact form, legal pages, user dashboard and settings, DSGVO-related actions, and research section.

---

## config

### docker-compose.dev.yml
Aktualisiert den Port für nginx im Docker Compose um 6480 statt 6481 für die Entwicklungsumgebung

---

### docker-compose.yml
Docker Compose configuration for a multi-service application, including web server (nginx), PHP FPM, database (PostgreSQL), Redis, and additional services such as queue worker, MCP paper search, and Ollama.

---

### .env.example
Datei für Konfiguration von Laravel-Anwendung. Stellt u.a. DB-Konfiguration, Cache-, Session- und Queue-Einstellungen sowie Mail- und externe API-Verbindungen bereit.

---

### config/app.php
This file sets key configurations for the Laravel application, including environment, debug mode, URL, timezone, locale, encryption key, maintenance mode driver and storage.

---

### config/auth.php
Configures Laravel's authentication defaults, guards, user providers, password reset options and password confirmation timeout.

---

### config/cache.php
Defines cache configurations for multiple stores including array, database, file, memcached, redis, dynamodb, octane, failover. The default store is defined and a prefix for cache keys can be set.

---

### config/cookie-consent.php
Die Datei konfiguriert das Cookie-Einverständnis für die Laravel-Anwendung. Es werden Konfigurationen für den Status, den Namen des Cookies und dessen Gültigkeitsdauer festgelegt.

---

### config/database.php
This PHP file defines database configurations for an Laravel application. It includes default connection, various connections like SQLite, MySQL, MariaDB, Pgsql, and Sqlsrv with their respective settings, a migrations table to keep track of all run migrations, and Redis database options.

---

### config/filesystems.php
Configures different file system disks (local, public, s3) and their properties. Also sets up symbolic links for storage.

---

### config/fortify.php
This file configures various settings for Laravel Fortify, a package that provides authentication and account management features. The file includes settings for the authentication guard, password broker, username field, lowercase usernames, home path, routes prefix/subdomain, middleware, rate limiting, view routes and enabled features.

---

### config/logging.php
This file configures the logging system for Laravel application, defining various channels and handlers for storing log messages in different formats and locations, including local files, Slack, Papertrail, syslog, errorlog, and null handler.

---

### config/mail.php
Configures email settings for Laravel application including default mailer, mailer configurations (smtp, ses, postmark, resend, sendmail, log, array, failover, roundrobin), global 'From' address and other related settings.

---

### config/queue.php
Configures the default queue connection and various settings for queue management in Laravel, including database, beanstalkd, SQS (Amazon Web Services), Redis, deferred, background, failover and batching options. It also defines failed job logging behavior.

---

### config/services.php
Stores credentials for third-party services such as Postmark, Resend, AWS SES, Slack, Langdock, MCP, and Ollama. Uses environment variables for secure storage.

---

### config/session.php
This file configures various settings for session handling in Laravel. It includes the default driver, lifetime, encryption, storage location, database connection, table, cache store, lottery, cookie name, path, domain, HTTPS only, HTTP access only, same-site cookies and partitioned cookies.

---

### docker/common/php-fpm/php-dev.ini
Die Datei docker/common/php-fpm/php-dev.ini ist eine PHP-Konfigurationsdatei, die zur Aktivierung von OPcache Timestamp-Check und Fehleranzeige für Entwicklungszwecke in PHP-FPM konfiguriert ist.

---

### docs/api.yaml
Definitur der API-Endpunkte der app.linn.games Research-Management-Plattform, umfasst Authentifizierung, Webhooks, Paper-Ingestion, Vektorsuche und User-Info, sowie externe Abhängigkeiten wie Laravel Sanctum, MCP Bearer Token und Ollama (nomic-embed-text).

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Konfiguration von GitHub-Issue-Vorlage für Bug-Reports, um Fehler zu melden. Es enthält Felder für Beschreibung des Problems, Schritte zur Reproduktion, Schweregrad, betroffene Bereiche und Akzeptanzkriterien.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Diese YAML-Datei enthält ein GitHub Issues Template für Featureanträge, das eine strukturierte Beschreibung der Vorgabe erfordert.

---

### .github/ISSUE_TEMPLATE/refactor.yml
Dieses Datei-Template definiert einen Refactoring-Issue im GitHub-Repository. Es enthält Felder für Beschreibung, betroffene Dateien und Akzeptanzkriterien.

---

### .github/workflows/lint.yml
Configures a GitHub Action workflow for linting code on push or pull request events to the 'develop', 'main', 'feature/' and 'fix/' branches using PHP's Pint tool. The workflow sets up the PHP environment with version 8.4, adds Flux credentials loaded from ENV variables, installs dependencies with Composer and npm, runs Pint for linting, and does not automatically commit changes.

---

### .github/workflows/tests.yml
GitHub Actions workflow file for running tests on push or pull request events, with PHP 8.4 and Node.js 22 setup. It installs dependencies, generates an application key, builds assets, and runs tests using pest.

---

## data_access

### app/Models/ChatMessage.php
```json
{
  "file_summary": "Defines the ChatMessage model, handling database interactions for chat messages. Implements UUIDs and timestamps, has fillable fields such as user\_id, webhook\_id, role, langdock\_execution\_id, content and casts created\_at as a datetime. BelongsTo relationships with User and Webhook models."
}
```

---

### app/Models/Consent.php
Defines the Consent model for managing user consents in a database, including their categories, statuses, and association with User model. It uses Laravel's Eloquent ORM for database access.

---

### app/Models/Contact.php
Defines the Contact model for storing and managing contact information in a database with Eloquent ORM of Laravel framework. This model includes fields for name, company, email, project type, message, and timeline.

---

### app/Models/PageView.php
Modell für die Speicherung von Seitenaufrufen mit den Attributen 'path', 'ip_anonym', 'user_agent' und 'visits'. Verwendet Laravel Eloquent-Framework und hat Abhängigkeit zur Datenbank.

---

### app/Models/User.php
This file defines the User model, extending Laravel's Authenticatable class. It manages user data access and uses various traits for features like role management (HasRoles), activity logging (LogsActivity), notifications (Notifiable), two-factor authentication (TwoFactorAuthenticatable) and factory creation (HasFactory). The User model has fields for name, email, password, status and handles serialization and casting of these fields. It also provides methods to retrieve the user's initials, check if a user is active or in trial mode.

---

### app/Models/Webhook.php
The Webhook.php file defines a model for storing webhook configurations (URL, user ID, frontend object) in the database. It provides methods to fetch a specific webhook for a user and a frontend object, call the URL associated with a webhook, and retrieve related chat messages. Imports Laravel's Eloquent ORM, Timestamps, and Uuids traits.

---

### app/Models/Recherche/P1Komponente.php
Defines the P1Komponente model for data access in Laravel's Eloquent ORM. The model handles interactions with the 'p1_komponenten' database table, allows foreign key relationship (BelongsTo) with Projekt model.

---

### app/Models/Recherche/P1Kriterium.php
Defines the P1Kriterium model for managing project criteria in the database, it uses Laravel's Eloquent ORM and has an association with the Projekt model.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Defines the P1Strukturmodellwahl Eloquent model, represents a relationship between projects and selected structure models. Imports the Illuminate namespace for database operations.

---

### app/Models/Recherche/P1Warnsignal.php
Defines the P1Warnsignal model for database access, which represents a record in the p1_warnsignale table. It has fillable fields like projekt_id, lfd_nr, warnsignal, moegliche_auswirkung and handlungsempfehlung. BelongsTo relationship with Projekt model is defined.

---

### app/Models/Recherche/P2Cluster.php
Defines the P2Cluster model for handling database interactions with the p2_cluster table, including its attributes, relationships (BelongsTo for Projekt), and validation rules in Laravel's Eloquent ORM.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Defines the P2MappingSuchstringKomponente model which handles database operations for storing and managing project components' search terms in a many-to-many relationship with projects. External dependencies include Laravel Eloquent ORM, its Concerns (HasUuids), and custom classes (Projekt).

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Defines the P2ReviewTypEntscheidung model for managing decisions on whether a review type fits a project in the database. Imports Laravel Eloquent and uses HasUuids concern.

---

### app/Models/Recherche/P2Trefferliste.php
Defines a model (P2Trefferliste) for database interaction with table 'p2_trefferlisten'. It has attributes like projekt_id, datenbank, suchstring etc. and relationships with Projekt class.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Defines the P3Datenbankmatrix model for handling database interactions related to project-specific data bank matrices. Import from Illuminate framework's Eloquent and Concerns, BelongsTo relationship with Projekt model.

---

### app/Models/Recherche/P3Disziplin.php
Defines a model for the 'p3_disziplinen' table. It represents the disciplines within a project and has relationships with the Projekt model. Uses the Eloquent ORM by Laravel, as indicated by the import of Illuminate\Database\Eloquent\Model.

---

### app/Models/Recherche/P3GeografischerFilter.php
Defines the P3GeografischerFilter model that represents geographical filters for projects and handles database interactions such as CRUD operations related to this table. It also includes relationships with the Projekt model, ensuring a project has one associated geographical filter.

---

### app/Models/Recherche/P3GraueLiteratur.php
```json
{
  "file_summary": "Defines P3GraueLiteratur model that represents a record for grey literature within a project. Imports Illuminate's Eloquent and Concerns\HasUuids, manages database table 'p3_graue_literatur', supports timestamps false, fillable fields include 'projekt_id', 'quelle', 'typ', 'url', 'suchpfad', 'relevanz', 'anmerkung'. Establishes a one-to-many relationship with Projekt model."
}
```

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Defines the Eloquent model for P4Anpassungsprotokoll table, representing change logs for search string modifications. It has associations with P4Suchstring model and includes timestamps and casting properties.

---

### app/Models/Recherche/P4Suchstring.php
Defines the P4Suchstring model for managing search strings in a project. It handles database interactions and has relationships with Projekt, P4Anpassungsprotokoll, and P8Suchprotokoll models.

---

### app/Models/Recherche/P4ThesaurusMapping.php
```json
{
  "file_summary": "Defines the P4ThesaurusMapping model, which maps free text terms in different languages to MESH, EMTREE, and PsycINFO terms for a specific project. Imports 'App\Models\Recherche\Projekt' model using BelongsTo relationship."
}
```

---

### app/Models/Recherche/P5PrismaZahlen.php
Defines the P5PrismaZahlen model for database access, representing a record in the 'p5_prisma_zahlen' table with relationships to the Projekt model. The class manages properties related to the prismatic data for a project, including totals, deduplications, exclusions and inclusions.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Defines P5ScreeningEntscheidung model which represents the database table 'p5_screening_entscheidungen' for storing decisions and their details, including a foreign key reference to the corresponding Treffer record. The Laravel Eloquent ORM is used as the primary interface for database interactions.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Defines the P5ScreeningKriterium model for managing project-related screening criteria in a database. Imports Laravel's Eloquent and uses its HasUuids concern. BelongsTo Projekt model through 'projekt_id'.

---

### app/Models/Recherche/P5ToolEntscheidung.php
```json
{
  "file_summary": "Defines a model for the P5ToolEntscheidung table in database. Handles CRUD operations for project-tool decisions with associated FK to Projekt. Imports Laravel's Eloquent Model and Concerns\HasUuids."
}
```

---

### app/Models/Recherche/P5Treffer.php
Klassendatei für das Modell P5Treffer, die die Verbindung zur Datenbank herstellt und die Methoden zum Zugriff auf verwandte Modelle (Projekt, Duplikat, ScreeningEntscheidungen, Qualitaetsbewertung, Datenextraktion) definiert. Es handelt sich um eine Laravel Eloquent-Model-Klasse mit externe Abhängigkeit zur Laravel-Framework-Library.

---

### app/Models/Recherche/P6Luckenanalyse.php
Defines the P6Luckenanalyse model for data management in the database table 'p6_luckenanalyse'. The model has relationships with Projekt. It contains fillable fields: projekt_id, fehlender_aspekt, fehlender_studientyp, moegliche_konsequenz, empfehlung.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Defines the P6Qualitaetsbewertung model for managing quality evaluations of search results in a database. It belongs to the Treffer model and has attributes like treffer_id, studientyp, rob_tool, gesamturteil, hauptproblem, im_review_behalten, anmerkung, bewertet_von, bewertet_am. Uses the HasUuids trait for UUID generation.

---

### app/Models/Recherche/P7Datenextraktion.php
Die Datei definiert die Klasse P7Datenextraktion, die ein Modell für die Tabelle 'p7_datenextraktion' in der Datenbank darstellt. Die Klasse besitzt eine primäre Schlüssel-Beziehung zu P5Treffer und hat Attributtypen wie Treffer_id, Land, stichprobe_kontext usw.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Die Datei definiert den Modell `P7GradeEinschaetzung`, welches eine Tabelle namens `p7_grade_einschaetzung` im Datenbank-Schema vertritt. Es handelt sich um ein Modell, das mit dem Laravel-Framework (Illuminate) erstellt wurde und verschiedene Eigenschaften wie `projekt_id`, `outcome`, `studienanzahl` etc. enthält. Außerdem besteht eine Verbindung zu dem Model `Projekt` durch die Methode `projekt()`. Dieses Modell ist Teil der Unterkategorie `Recherche` in `app/Models/`.

---

### app/Models/Recherche/P7MusterKonsistenz.php
Defines the P7MusterKonsistenz model for managing data related to pattern consistency within a project. The class uses Eloquent ORM and has relationships with Projekt (App\Models\Recherche\Projekt) via 'projekt_id'.

---

### app/Models/Recherche/P7SyntheseMethode.php
Modelliert eine Tabelle für Synthese-Methode in einem Projekt. Hält die Beziehung zu dem entsprechenden Projekt und verfügt über das Attribut 'gewaehlt' (boolean). Importiert die Eloquent-Klasse.

---

### app/Models/Recherche/P8Limitation.php
Modell für P8-Beschränkungen in einer Datenbanktabelle (p8_limitationen) mit Funktionen zum Verbinden mit dem Projekt-Modell

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Defines a model for the P8Reproduzierbarkeitspruefung table. This model manages data related to a project's reproducibility test, handling attributes like 'id', 'projekt_id', 'pruefpunkt', 'erfuellt', and 'anmerkung'. It also has a one-to-many relationship with the Projekt model.

---

### app/Models/Recherche/P8Suchprotokoll.php
Diese Datei definiert den Model 'P8Suchprotokoll' für die Tabelle 'p8_suchprotokoll'. Es stellt eine Verbindung zur Datenbank her, besitzt Füllbare Felder, Gibt Casts für spezielle Datentypen, und hat eine BelongsTo-Relation zu dem Model 'P4Suchstring'.

---

### app/Models/Recherche/P8UpdatePlan.php
Defines the P8UpdatePlan model for database interactions. Implements UUIDs for unique identifiers. The model has relationships with Projekt and manages attributes like projekt_id, update_typ, intervall, verantwortlich, tool, and naechstes_update (a date). It casts 'naechstes_update' as a date type.

---

### app/Models/Recherche/PaperEmbedding.php
Defines the PaperEmbedding model for managing paper-related embeddings in a project. This model uses Eloquent ORM and has relationships with the Projekt model. It handles table mapping, filling, casting, and timestamps management.

---

### app/Models/Recherche/Phase.php
Defines the 'Phase' model for data access, representing a project phase. Provides relationship with Projekt model (BelongsTo). Uses Laravel Eloquent ORM and timestamps false.

---

### app/Models/Recherche/Projekt.php
This file is a PHP model for the 'Projekt' entity representing a research project in a database using Laravel Eloquent. It includes relations to multiple other entities like User, Phase, P1Strukturmodellwahl, P1Komponenten, and many others, each representing a different aspect of the project. The file also uses Spatie Activitylog for logging changes to the 'titel', 'forschungsfrage' and 'review_typ'. It does not have any external API or database dependencies declared in this file.

---

### database/factories/UserFactory.php
```json
{
  "file_summary": "Die Datei UserFactory.php definiert Factories für den Benutzer (User) in der Anwendung. Es werden Standardwerte und Eigenschaften für neue Benutzer erstellt, einschließlich Passwörtern über die Hash-Funktion. Die Datei importiert die Klassen fake(), Illuminate\Database\Eloquent\Factories\Factory und Illuminate\Support\Str aus dem Framework."
}
```

---

### database/factories/Recherche/ProjektFactory.php
```json
{
  "file_summary": "Erstellt Fake-Instanzen für Projekte (App\Models\Recherche\Projekt) mit Hilfe von Eloquent Factories, verwendet zusätzlich User-Instanzen (App\Models\User) und Randomer-Generatoren."
}
```

---

### database/migrations/0001_01_01_000000_create_users_table.php
Diese PHP-Datei enthält Laravel Migrations, um Tabellen für die Benutzer, Passwortzurücksetzungstokens und Sessions im Datenbankschema zu erstellen.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Creates cache and cache_locks tables in the database using Laravel's migration functionality.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
This file creates three database tables (jobs, job_batches, failed_jobs) for a Laravel application using the Eloquent ORM. It extends Migration and overrides up() and down() methods to define schema for each table.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Adds two-factor columns to the users table: 'two_factor_secret', 'two_factor_recovery_codes' and 'two_factor_confirmed_at'. Uses Laravel's Eloquent ORM for database operations.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Die Datei erstellt eine MySQL-Tabelle namens 'contacts' mit den Spalten: id, name, company (optional), email, project_type, message und timeline (optional). Es wird die Laravel-Framework-Klasse Migration verwendet.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Diese Datei erstellt oder löscht eine Tabelle 'page_views' im Datenbank-Schema, wenn sie ausgeführt wird. Es sind die Klassen Migration, Blueprint und Timestamps benötigt.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Migration-Datei zum Erstellen von Tabelle für Berechtigungen und Rollen, die mit Laravel verbunden sind. Es werden die Tabellen `permissions`, `roles`, `model_has_permissions`, `model_has_roles`, sowie `role_has_permissions` erstellt. Darüber hinaus wird eine Cache-Entfernung durchgeführt.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Erweitert die Tabelle 'page_views' um Spalten für den Pfad, IP-Anonymisierung und User Agent

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Diese Datei erstellt einen Tabellen-Eintrag 'consents' in der Datenbank, um Benutzer-Konsenten für die Verarbeitung ihrer persönlichen Daten zu speichern. Es werden die Klassen Migration, Blueprint und Schema von Laravel verwendet.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Creates tables for the 'recherche' database in SQLite. The main classes are Migration and Blueprint from Laravel. This migration file is used to create or modify tables for project management, phase tracking, reference records, and various data related to different phases of a research project.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Creates database extensions and custom enumeration types (enums) in a PostgreSQL database. The file imports necessary classes for database migrations, defines the up and down functions to create and drop the enums, and checks for existing enums before creating them.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
```json
{
  "file_summary": "Erstellt und initialisiert die Tabellen 'projekte' und 'phasen' in der Datenbank. Die Datei importiert die Klassen `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\DB`, `Illuminate\Support\Facades\Schema`."
}
```

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
Diese Datei enthält eine Migration, welche Tabellen für ein Projekt namens 'p1' erstellt. Die Tabelle p1_strukturmodell_wahl, p1_komponenten, p1_kriterien und p1_warnsignale werden initialisiert und enthalten Spalten wie id, projekt_id, ... Es wird auf die Datenbanktreiber 'pgsql' spezifisch geprüft, ob die Tabellen bereits vorhanden sind. Falls nicht, werden sie erstellt.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
```json
{
  "file_summary": "Diese PHP-Datei definiert Migrationsschritte zur Erstellung und Veränderung von Tabellen für die Datenbank des Projekts. Sie enthält Schritte zur Erstellung der Tabelle 'p2_review_typ_entscheidung', 'p2_mapping_suchstring_komponenten', 'p2_trefferlisten' und 'p2_cluster'. Es wird auf die Laravel-Funktionen Illuminate\Database\Migrations\Migration, Illuminate\Support\Facades\DB und weitere Abhängigkeiten zugegriffen."
}
```

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Diese Datei enthält eine Migration für die Erstellung von fünf Tabelen in einer Datenbank: p3_disziplinen, p3_datenbankmatrix, p3_geografische_filter, p3_graue_literatur. Diese Tabelle sind Teil der Projekt-Datenstruktur. Importiert die Laravel-Packages Illuminate/Database/Migrations und Illuminate/Support/Facades.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
Die Datei definiert die Tabellen für das Datenbank-Schema von drei Entitäten (p4_suchstrings, p4_thesaurus_mapping und p4_anpassungsprotokoll) im Zusammenhang mit der Verwaltung von Suchbegriffen innerhalb einer Anwendung.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Creates and manages various tables for a project in the database (p5_prisma_zahlen, p5_screening_kriterien, p5_treffer, p5_screening_entscheidungen, p5_tool_entscheidung). Imports include Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\DB, and Illuminate\Support\Facades\Schema.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Kreiert die Tabelle 'p6_qualitaetsbewertung' und 'p6_luckenanalyse' im Datenbank-Schema. Die Klassen verwenden Laravel Facades wie `DB`, `Schema`, und `Blueprint`.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Diese PHP-Datei enthält eine Migration, die in der Datenbank die Tabelle 'p7_grade_einschaetzung', sowie drei weitere Tabellen namens 'p7_synthese_methode', 'p7_datenextraktion' und 'p7_muster_konsistenz' erstellt, wenn sie nicht vorhanden sind. Die Migration beinhaltet auch eine Funktion um die Tabellen wieder zu löschen.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Diese PHP-Datei enthält eine Migration mit der Funktion, die bei Bedarf Tabelle für die Datenbank 'p8' erstellt und wenn notwendig gelöscht werden kann. Die wichtigsten Tabelleinheiten sind: p8_suchprotokoll, p8_limitationen, p8_reproduzierbarkeitspruefung, p8_update_plan. Es wird auf die Laravel-Funktionen Schema und DB zur Interaktion mit der Datenbank zurückgegriffen.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Die Datei erstellt und verwaltet verschiedene Indizes in der PostgreSQL-Datenbank für die Tabellen phasen, p1_komponenten, p5_treffer, p5_screening_entscheidungen, p6_qualitaetsbewertung, p7_datenextraktion sowie für Fülltextsuche in Titeln und Abstracts der Tabelle p5_treffer. Es wird die Laravel-Migration-Klasse verwendet.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Creates a PostgreSQL function 'berechne_prisma_zahlen(UUID)' that calculates PRISMA-2020 statistics for a research project directly on the DB level. The function counts records in p5_treffer and p5_screening_entscheidungen to generate numbers for the PRISMA flow diagram: Identified → Duplicates → L1-Exclusion → Full-Text → L2-Exclusion → Final. It is called via SELECT * FROM berechne_prisma_zahlen('projekt-uuid'). The function uses Laravel's DB facade for the migration.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Migrates the database schema to create a 'chat_messages' table for chat messages, stores user ID, role ('user' or 'assistant'), content and creation timestamp. Requires Laravel's Eloquent ORM and database abstraction layer.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
```json
{
  "file_summary": "Diese Datei definiert eine Migration zur Erstellung und Veränderung der Tabelle 'webhooks' und die 'chat_messages'-Tabelle um das Feld 'webhook_id' zu ergänzen. Die wichtigsten Klassen sind: `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\DB`, `Illuminate\Support\Facades\Schema`. Es wird auf die PostgreSQL-Datenbank zugegriffen."
}
```

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Erstellt Tabelle 'paper_embeddings' mit einer Vector-Spalte (768 Dimensionen) für die Speicherung von Text-Embeddings und Metadaten. Externer Abhängigkeit: Illuminate Framework

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Kreatiert Tabelle 'activity_log' in der Datenbank für den Speicherung von Aktivitätsprotokollen. Verwendet die Laravel-Migrationsbibliothek und die Schema-Blueprint-Schreibweise.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
Adds a 'status' column of type ENUM ('trial', 'active', 'suspended', 'cancelled') to the users table in PostgreSQL and SQLite databases

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Führt die Migration aus, um eine Spalte 'frontend_object' für das Modell 'webhooks' hinzuzufügen und ein neues Enumeration-Objekt 'webhook_frontend_object' zu erstellen.

---

### database/migrations/2026_04_02_160000_make_chat_messages_content_nullable.php
```json
{
  "file_summary": "Erlaubt die Null-Verarbeitung des Felds 'content' in der Tabelle 'chat_messages', sowie den Zusatzfeld 'langdock_execution_id'. Dieser Code nutzt die Klassen Migration, Blueprint und die Funktionen getDriverName(), schema() und table() von der DB-Abhängigkeit Laravel, die über die Facades Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\DB, sowie Illuminate\Support\Facades\Schema bereitgestellt wird."
}
```

---

### database/seeders/DatabaseSeeder.php
Initialisiert und füllt die Datenbank mit Standarddaten (User, Roles) sowie einem Demobestand der Recherche. Benutzt User-Model, Role-Model, ohne ModelEvents, und RechercheDemoSeeder.

---

### database/seeders/RechercheDemoSeeder.php
Seed-Datei für die Datenbankbefüllung einer Recherche-Anwendung. Initialisiert Projekte, Phasen, Suchstrategien und Trefferlisten mit entsprechenden Klassen und Funktionen. Verwendet die Laravel-Fassade DB um Daten in verschiedenen Tabellen einzufügen.

---

### database/seeders/RoleSeeder.php
Initialisiert die Rollen 'admin' und 'user' für den Guard 'web' in der Datenbank, verwendet Spatie-Permission-Modell Role

---

### database/sql/create_langdock_agent_user.sql
SQL-Skript zum Erstellen des PostgreSQL-Benutzers 'langdock_agent' mit eingeschränkten Rechten auf Recherche-Tabellen. Gibt die Rolle, die Konnektionsrechte und Zugriffsberechtigungen auf spezifische Tabellen fest.

---

## temp_dummy

### .github/pull_request_template.md
Diese Datei dient als Vorlage für GitHub-Pull Requests. Es wird eine Beschreibung des geplanten Änderungen gefordert sowie eine Checkliste zur Überprüfung von Codeschmeln, Tests, Datenbank-Migration und Deploy-Hinweisen.

---

## tests

### tests/Pest.php
Diese Datei importiert die PHPUnit-Testcase Klasse und erweitert sie mit eigenen Funktionen und Erweiterungen. Es werden Tests erstellt, indem Sie die 'pest()' und 'expect()->extend()' Funktionen verwenden. Außerdem können globale Testfunktionen definiert werden.

---

### tests/TestCase.php
```json
{
  "file_summary": "Eine Abstract-Klasse für Tests, die auf der Basis von 'Illuminate\Foundation\Testing\TestCase' erweitert wird."
}
```

---

### tests/Feature/ContactTest.php
```json
{
  "file_summary": "Diese Datei enthält Laravel-Tests für die Kontaktformularfunktion, die Validierungen und Datenbankspeicherung in der Contact-Klasse von App\Models überprüft."
}
```

---

### tests/Feature/DashboardTest.php
```json
{
  "file_summary": "Diese Datei enthält PHP-Tests für den Login- und Dashboard-Endpunkt. Die wichtigsten Klassen sind 'App\Models\User', der Test-Context wird mit 'use Laravel\Foundation\Testing\RefreshDatabase' initialisiert."
}
```

---

### tests/Feature/ExampleTest.php
Contain unit tests for checking the success status (200) of GET request to home route.

---

### tests/Feature/IngestPaperJobTest.php
Contains PHPUnit tests for the IngestPaperJob class, which handles the ingestion of research papers into a database system. It deals with dispatching jobs to the queue, validating parameters, testing retry mechanisms and text chunking. The external dependencies include Ollama API (for embedding) and SQLite (database).

---

### tests/Feature/LegalPagesTest.php
Tests die Rendering von Seiten für Impressum, Datenschutz und AGB überprüft. Es werden Laravel-Routes verwendet um die Seiten zu laden und Asserts verwendet um das Status-Code und Inhalt der Seite zu prüfen.

---

### tests/Feature/PaperRagTest.php
Test file for checking the ingest and RAG search functionality of a paper in Laravel application. Contains tests for validating inputs, checking for token authentication, dispatching jobs for ingest, interacting with external APIs (Ollama) and handling errors.

---

### tests/Feature/ProjektPolicyTest.php
Test-Datei für die Autorisierungspolicy für Projekte, die Funktionalität überprüft, ob der Besitzer eines Projekts lesen, schreiben und löschen kann oder nicht. Außerdem prüft sie, ob jeder authentifizierte Benutzer ein neues Projekt erstellen kann.

---

### tests/Feature/RechercheTest.php
Contient des tests pour le fonctionnement de la page Recherche et ses fonctions connexes, testant les créations de projets, l'envoi de jobs en arrière-plan (TriggerLangdockAgent), et l'affichage de liste et de détails de projets.

---

### tests/Feature/WebhookTest.php
Diese Datei enthält Laravel-Tests für die Verarbeitung von Webhooks von Langdock. Die Testfälle prüfen, ob eine Signatur und ein Zeitstempel bereitgestellt wurden sowie ob diese gültig sind. Es wird auch überprüft, dass Eingabe und Projekt-IDs korrekt sind.

---

### tests/Feature/WelcomeTest.php
Tests the welcome page functionality, including checking for successful response, visit counter increment, and user authentication.

---

### tests/Feature/Auth/AuthenticationTest.php
Test-Datei, die verschiedene Authentifizierungsfunktionen für Laravel Anwendungen testet.
	Es werden Funktionen zur Überprüfung der Rendering des Login-Screens, der Authentifizierung mit korrektem und fehlerhaftem Passwort sowie der Möglichkeit der Logout verwendet.
	Importiert Modelle von User und Abhängigkeiten von Laravel Fortify.

---

### tests/Feature/Auth/EmailVerificationTest.php
Test file that verifies the functionality of email verification for users, including rendering the email verification screen, verifying emails with valid/invalid hashes, and handling already verified users.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
```json
{
  "file_summary": "Testet die Rendering-Funktionalität des Bestätigungsscreens für Passwörter (renderPasswordConfirmationScreen). Es benutzt die User-Klasse aus der App-Kategorie (App\Models\User)."
}
```

---

### tests/Feature/Auth/PasswordResetTest.php
Die Datei testet die Funktionalität des Passwortzurücksetzens für Benutzer in der Anwendung. Es werden wichtige Tests für die Rendering von verschiedenen Seiten, das Senden eines Reset-Password-Links und den tatsächlichen Zurücksetzen des Passworts durchgeführt. Die Datei benötigt eine interne Abhängigkeit zu den Klassen User und Notification sowie einigen Facades von Illuminate.

---

### tests/Feature/Auth/RegistrationTest.php
Contain PHPUnit tests for registration functionality.
Tests include rendering the registration screen and creating new user accounts.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
```json
{
  "file_summary": "Test-Datei für die Authentifizierungsfunktionen von Two-Factor-Authentifizierung, benutzt Laravel\Fortify-Modell User, überprüft Login- und Zwei-Faktor-Login-Routen."
}
```

---

### tests/Feature/Chat/BigResearchChatTest.php
```json
{
  "file_summary": "Testet die Funktionalität der Chat-Komponente, einschließlich Nachrichtensenden und Löschen, und prüft die korrekte Speicherung in der Datenbank (ChatMessage). Die Datei importiert auch HTTP-Funktionen über Illuminate\Support\Facades\Http."
}
```

---

### tests/Feature/Recherche/PhaseP1Test.php
```json
{
  "file_summary": "Die Datei tests/Feature/Recherche/PhaseP1Test.php enthält PHP-Tests für die Funktionalität der Phase 1 des Suchsystems in Form von Methoden, die die Erstellung, Bearbeitung und Löschung von Strukturmodellwahl, Komponente und Kriterium überprüfen.\n\nDie wichtigsten Klassen sind App\\Models\\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal\}, User und Livewire\\Volt\\Volt. Es wird zudem auf das Modell App\\Models\\User sowie die Eloquent-Methoden für die Datenbankzugriffe zurückgegriffen."
}
```

---

### tests/Feature/Recherche/PhaseP2Test.php
Test-Datei für die Phase P2 in der Recherche. Testet die Funktionen zum Erstellen, Bearbeiten und Löschen von ReviewTypEntscheidungen, Clustern, MappingSuchstringKomponenten und Trefferlisten. Benutzt Modelle wie Projekt, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, P2Trefferliste und User.

---

### tests/Feature/Recherche/PhaseP3Test.php
Test Datei für die Phase P3 in der Recherche-Kategorie. Diese Datei umfasst Tests für die Funktionalität zur Erstellung, Bearbeitung und Löschung von Datenbankmatrix, Disziplin, geografischem Filter und Graue Literatur innerhalb eines Projekts.

---

### tests/Feature/Recherche/PhaseP4Test.php
```json
{
  "file_summary": "Die Datei testet verschiedene Funktionen des Recherche-Moduls in Phase P4. Sie enthält Tests zur Erstellung, Bearbeitung und Löschung von Suchstrings, Thesaurus-Mapping und Anpassungsprotokollen. Die wichtigsten Klassen sind App\\Models\\Recherche\{Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll} sowie Livewire\\Volt\\Volt. Es gibt keine externe Abhängigkeiten."
}
```

---

### tests/Feature/Recherche/PhaseP5Test.php
Diese Datei stellt Tests für die Phasen 5 des Prisma- und Screening-Prozesses in einer Anwendung bereit, verwendet Livewire als Framework und importiert verschiedene Modelle.

---

### tests/Feature/Recherche/PhaseP6Test.php
Test Phases P6 of Recherche for creating, editing and deleting Quality Evaluation and Gap Analysis records in a project. The test suite also includes authentication checks.

---

### tests/Feature/Recherche/PhaseP7Test.php
Test-Datei für die Phase P7 im Recherche-Modul. Testet die Erstellung, Bearbeiten und Löschen von Synthese-Methoden, Datenextraktionen, Mustern & Konsistenz sowie Grade-Einschätzungen in einem Projekt. Authentifizierter Benutzer muss Besitzer des Projekts sein.

---

### tests/Feature/Recherche/PhaseP8Test.php
Diese PHP-Datei enthält Tests für die Phase P8 des Recherchesystems, um zu überprüfen, ob es funktioniert, Suchprotokolle, Einschränkungen, Reproduzierbarkeitsprüfungen und Updatepläne erstellen, bearbeiten oder löschen können.

---

### tests/Feature/Recherche/ProjektDetailTest.php
PhpUnit-Test-Datei für das Livewire-Component 'recherche.projekt-detail', die Testfälle für die Interaktion mit dem Projektdetail der Recherche überprüft, wie Tabwechsel, Zugriffsberechtigungen und Anzeige von Phasen.

---

### tests/Feature/Settings/AppearanceTest.php
Test-Datei, die sich mit der Überprüfung der Darstellungseinstellungen (Appearance) von Seiten auseinandersetzt. Die Datei nutzt Livewire und Laravel's User-Modell. Es werden verschiedene Tests durchgeführt um zu überprüfen, ob alle drei Appearance-Buttons angezeigt, der appearance-toggle Container und client-seitiges JavaScript vorhanden sind. Außerdem wird überprüft, dass die Seite nur für eingeloggte User erreichbar ist.

---

### tests/Feature/Settings/PasswordUpdateTest.php
Diese Datei enthält Tests für die Passwortänderung in der App. Es werden zwei Testfälle durchgeführt: Zum einen, dass ein Passwort geändert werden kann (mit korrekter Eingabe), und zum anderen, dass eine falsche Passwortänderung abgefangen wird (mit falschen Daten). Die wichtigsten Klassen sind User und Volt. Es gibt keine externe Abhängigkeiten.

---

### tests/Feature/Settings/ProfileUpdateTest.php
Contains tests for verifying and updating user's profile information, email verification status, and account deletion functionality in the application.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
Test-Datei für die Two-Factor Authentifizierung in Laravel. Testet das Rendern der Two-Factor-Einstellungsseite und deren Funktionalität, einschließlich des Passwortbestätigungsfensters.

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei testet Webhooks für die Seite Einstellungen. Es werden Funktionen bereitgestellt um Webhooks für Dashboard-Chat und Recherche-Start zu speichern, aktualisieren, löschen und eine Liste aller bestehenden Webhooks für den Mount aufzuladen.

---

### tests/Unit/ExampleTest.php
Contient un test phpunit pour vérifier si une valeur est vraie.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Defines the 'ListContacts' page for managing lists of 'Contact' resources in Filament UI. Imports required Filament classes and sets the associated resource to be 'ContactResource'.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Defines a page for viewing a specific 'Contact' record within the Filament UI. It uses the ContactResource and extends the ViewRecord class from Filament.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Die Datei definiert eine Seite zum Bearbeiten eines Benutzerprofils innerhalb der Anwendung Filament. Sie erbt von EditRecord und setzt den zu bearbeitenden Ressourcentyp auf UserResource.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Defines a Filament page for listing all users in the application. The class extends `ListRecords` and sets the associated resource as `UserResource`. No external dependencies are explicitly stated.

---

### resources/views/dashboard.blade.php
Das Blade-Template dashboard.blade.php ist eine UI-Datei für die Darstellung des Benutzer-Dashboards im Forschungsportal.

Es zeigt den Willkommenstext, schnellzugängliche Links und aktuelle Projekte an.

Die wichtigsten Klassen sind x-layouts.app, App\Models\Recherche\Projekt und livewire:chat.big-research-chat.

Es gibt eine Abhängigkeit zum Laravel-Model Recherche\Projekt und Livewire.

---

### resources/views/welcome.blade.php
```json
{
  "file_summary": "Blade template for the welcome page, containing the HTML structure and CSS imports for styling, as well as JavaScript components (Vite) and Laravel helper functions (csrf\_token())"
}
```

---

### resources/views/components/action-message.blade.php
Diese Blade-Datei implementiert einen dynamischen, zeitlich begrenzten Hinweiskomponenten mit dem Namen 'action-message'. Es benutzt Laravel Livewire und erwartet eine Eigenschaft '$on' zur Aktivierung des Hinweises. Wenn die Slot ist leer, wird der Text 'Saved.' angezeigt.

---

### resources/views/components/app-logo-icon.blade.php
Das File 'app-logo-icon.blade.php' enthält ein SVG-Element mit einem Vektor-Diagramm, das einen Logo-Icon darstellt.

---

### resources/views/components/app-logo.blade.php
Displays the application logo in a square container with the text 'Laravel Starter Kit' underneath. This file uses Blade template engine and includes X-App-Logo-Icon component.

---

### resources/views/components/auth-header.blade.php
Eine Blade-Datei zur Darstellung eines Auth-Headers, der mit zwei Eigenschaften ('title', 'description') besetzt werden kann. Die Komponente verarbeitet HTML-Strukturen um den angegebenen Titel und Beschreibung zu zeigen.

---

### resources/views/components/auth-session-status.blade.php
Blade-Komponente zur Anzeige eines Benachrichtigungstextes bei Authentifizierung, falls der Status angemeldet ist

---

### resources/views/components/placeholder-pattern.blade.php
Das Blade-Template definiert einen SVG-Placeholder mit einem benutzerdefinierten Muster, das über eine Pattern-Definition in Defs-Abschnitt erstellt wird. Es nimmt einen optionalen Parameter 'id' entgegen.

---

### resources/views/components/layouts/app.blade.php
Layout-Blade-Template für Seitenstruktur mit Seiteinhalt (via $slot) und Seitenspalte (via x-layouts.app.sidebar)

---

### resources/views/components/layouts/auth.blade.php
Blade template for a basic layout used across Laravel's authentication views, consisting primarily of the 'x-layouts.auth.simple' component and an optional title variable passed to it.

---

### resources/views/components/layouts/app/header.blade.php
This appears to be a piece of HTML code for a web application's header and mobile sidebar. The code includes a header that contains navigation links, a profile dropdown, and a logout button. The header also includes a mobile navigation toggle that reveals a sidebar on smaller screens.

The header and sidebar both use Tailwind CSS, which is a utility-first CSS framework for rapidly building custom user interfaces. The classes used in the code (e.g., `flex`, `text-start`, `border-e`) are part of Tailwind's utility classes.

The profile dropdown allows users to view their account settings and log out of the application. The mobile navigation toggle reveals a sidebar that contains links to various pages in the application, such as "Dashboard", "Calendars", "Events", etc.

Overall, it appears this code is part of a web application's front-end user interface.

---

### resources/views/components/layouts/app/sidebar.blade.php
This code is a part of a Laravel Livewire starter kit sidebar. It contains navigation links, user menu, and other elements for a web application's sidebar. Here is an explanation of some parts:

1. The `<nav>` tag contains the main navigation links, including links to the GitHub repository, documentation, and bottom links (which are not visible in this snippet).

2. The `<div class="relative mt-4 hidden lg:block">` block is for the desktop user menu, which displays a dropdown menu when clicked on the user's avatar with their name and initials.

3. The user menu includes an avatar image (`<span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-neutral-200 text-sm font-medium text-black dark:bg-neutral-700 dark:text-white">`) with the user's initials, their name (`<span class="truncate font-semibold text-zinc-900 dark:text-zinc-100">{{ auth()->user()->name }}</span>`), and email (`<span class="truncate text-xs text-zinc-500">{{ auth()->user()->email }}</span>`).

4. The user menu also contains links for editing the profile, signing out, and viewing account settings:
   - `<a href="{{ route('profile.edit') }}" wire:navigate class="flex w-full items-center gap-2 px-3 py-2 text-sm text-zinc-700 hover:bg-zinc-100 dark:text-zinc-300 dark:hover:bg-zinc-700">` (Edit profile)
   - `<form method="POST" action="{{ route('logout') }}" class="flex w-full items-center gap-2 px-3 py-2 text-sm text-zinc-700 hover:bg-zinc-100 dark:text-zinc-300 dark:hover:bg-zinc-700">` (Logout)
   - `<a href="{{ route('account.settings') }}" class="flex w-full items-center gap-2 px-3 py-2 text-sm text-zinc-700 hover:bg-zinc-100 dark:text-zinc-300 dark:hover:bg-zinc-700">` (Account settings)

5. The user menu also includes a "Sign out" button, which is represented by an `<form>` tag with the `method="POST"` attribute and the `route('logout')` for handling the logout request.

6. At the end of the code snippet, there's an ellipsis (`[...]`) that indicates parts of the code have been omitted due to its length.

---

### resources/views/components/layouts/auth/card.blade.php
Eine UI-Komponente für Authentifizierungs-Seiten, die einen Rahmen mit einem Logo und einem Hauptinhalt bietet. Die Komponente verwendet Laravel Livewire für interaktive Elemente und erfordert die Cookie-Consent-Package.

---

### resources/views/components/layouts/auth/simple.blade.php
Eine HTML-Datei, die einen Standardlayout mit einer Verwendung von Livewire und Laravel's Cookie Consent-Package für Authentifizierungsseiten bereitstellt.

---

### resources/views/components/layouts/auth/split.blade.php
Die Datei definiert eine HTML Seite mit einem Layout für Authentifizierungsbereiche, um verschiedene Elemente wie Header, Footer und Content zu organisieren. Es wird auf die Import-Datei 'partials.head' zurückgegriffen. Verwendet werden die Livewire-Pakete.

---

### resources/views/components/settings/layout.blade.php
Die Datei 'layout.blade.php' enthält eine UI-Komponente, die eine Navigationsleiste für verschiedene Benutzerbereiche wie Profile bearbeiten, Passwort ändern, Zweifaktorauthentisierung und Äußeren Erscheinung bietet. Zusätzlich wird ein Platzhalter für dynamische Inhalte bereitgestellt.

---

### resources/views/emails/contact-confirmation.blade.php
Blade-Template für die Kontaktaufirmage bestätigende E-Mail mit Platzhaltern für Kontaktinformationen

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei enthält ein HTML-Layout für eine E-Mail mit Informationen einer Kontaktanfrage an linn.games: Name, Firma, E-Mail, Projekttyp, Zeitrahmen und Nachricht. Es wird ein Objekt $contact verwendet, das Daten aus einer PHP-Variable enthält.

---

### resources/views/emails/deploy-notification.blade.php
Blade-Template für eine Deployment-Bestätigungs-Email mit Informationen zur Zeit, URL und Umgebung des deployten Release

---

### resources/views/layouts/legal.blade.php
Layouter für die allgemeinen Nutzungsbedingungen, Datenschutzerklärung und Impressum von Linn Games. Enthält HTML-Markup für Layout, CSS-Styling und JavaScript zum Aktualisieren des Copyright-Jahres.

---

### resources/views/legal/agb.blade.php
Die Datei stellt die Allgemeinen Geschäftsbedingungen (AGB) des Angebots von Linn.Games dar, die Nutzungsregeln, Haftungsbestimmungen sowie die Rechte und Pflichten der Nutzer und des Anbieters enthalten.

---

### resources/views/legal/dsgvo.blade.php
Die Datei enthält eine Seite für die Darstellung der Datenschutzerklärung, die Angaben zum Verantwortlichen und den Artikeln der datenverarbeiteten Kategorien macht. Sie erwähnt auch die Rechte der Betroffenen, technische Maßnahmen und Cookie-Informationen. Es werden externe Dienstleister wie Langdock eingesetzt.

---

### resources/views/legal/impressum.blade.php
Die Datei enthält die Darstellung der Seite 'Impressum' mit Informationen über den Verantwortlichen, Kontakt und zusätzliche rechtliche Informationen.

---

### resources/views/livewire/auth/confirm-password.blade.php
HTML Template for Confirm Password page using Laravel Livewire and Tailwind CSS, includes a form for password confirmation and error handling.

---

### resources/views/livewire/auth/forgot-password.blade.php
HTML-Blade-Template für die Seite zur Passwort-Erinnerung. Enthält einen Loginformular, bei dessen Absenden ein Passwort-Zurücksetzungs-Link versendet wird. Importiert andere Layouts (x-layouts.auth). Keine APIs oder externe Abhängigkeiten.

---

### resources/views/livewire/auth/login.blade.php
Laravel Livewire login UI for authentication

---

### resources/views/livewire/auth/register.blade.php
Die Datei stellt ein Registrierungsformular dar, um eine neue Benutzerkonto zu erstellen. Die wichtigsten Klassen sind `x-layouts.auth`, benötigt APIs für die Sitzung und zum Routing. Es gibt keine externen Abhängigkeiten.

---

### resources/views/livewire/auth/reset-password.blade.php
Die Datei enthält eine Livewire-Komponente für das Zurücksetzen des Passworts mit Formularfeldern für Email, Passwort und Bestätigung des Passworts sowie Anzeige von Sessionnachrichten.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Displays a form for user authentication via either an authentication code or recovery code, provided by a two-factor authenticator application. The form processes POST requests to `/two-factor/login/store`. It uses Livewire, Laravel's real-time JavaScript framework.

---

### resources/views/livewire/auth/verify-email.blade.php
UI template for email verification and resend link, along with logout functionality. Imports 'x-layouts.auth' from resources/views/components.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Die Datei 'big-research-chat.blade.php' ist eine Livewire-Komponente für die Darstellung eines Chat-Interfaces innerhalb der Dashboard-Anwendung. Die Hauptfunktionen sind: Verarbeitung von Nutzer-Nachrichten, Abfrage und Ausgabe von vorherigen Nachrichten, Senden von Nachrichten an eine externe API (Langdock), Abfragen eines Webhooks sowie das Löschen des Chat-Verlaufs.

---

### resources/views/livewire/recherche/phase-p1.blade.php
Die Datei definiert eine Livewire-Komponente zur Verwaltung von Projekten in der Phase P1. Es werden Strukturmodellwahl, Komponente, Kriterium und Warnsignal verwaltet. Die Klassen P1Strukturmodellwahl, P1Komponente, P1Kriterium und P1Warnsignal werden importiert sowie die Auth-Fassade von Laravel.

---

### resources/views/livewire/recherche/phase-p2.blade.php
This file is a Livewire component for managing project research phases P2: it handles CRUD operations for ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, and P2Trefferliste entities. The component allows the user to create, edit, delete, and view instances of these objects. Additionally, it uses Auth facade for authorization checks.

---

### resources/views/livewire/recherche/phase-p3.blade.php
Das Blade-Component stellt eine Benutzeroberfläche für das Datenbankmanagement im Projektbereich bereit. Es handelt sich um eine UI-Datei, die u.a. Klassen aus dem Projektmodell (Projekt, P3Datenbankmatrix, P3Disziplin, P3GeografischerFilter, P3GraueLiteratur) verwendet und auf Livewire basiert.

---

### resources/views/livewire/recherche/phase-p4.blade.php
Containes UI component for managing and displaying research project's search strings, thesaurus mappings, and annotations. Uses App\Models\Recherche classes for data management and Livewire Volt for building the Livewire component.

---

### resources/views/livewire/recherche/phase-p5.blade.php
```json
{
  "file_summary": "Livewire component for managing research phase P5, including PRISMA data, screening criteria, screening tool, and treffer screening. Imports App\Models\Recherche* and uses Illuminate\Support\Facades\Auth. Includes CRUD functions for PRISMA data, screening criteria, screening tool, and treffer screening."
}
```

---

### resources/views/livewire/recherche/phase-p6.blade.php
```json
{
  "file_summary": "Die Datei definiert eine Livewire-Komponente für die Bearbeitung von Qualitätsbewertungen (Risk of Bias) und Lückenanalyse im Zusammenhang mit einem Projekt. Die wichtigsten Klassen sind App\Models\Recherche\{Projekt, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse}. Es gibt Funktionen zur CRUD-Verwaltung von Qualitätsbewertungen und Lückenanalyse sowie eine Methode 'with' für das Abrufen der Daten."
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
Livewire component for the Phases-P7 section of the research project UI. It handles CRUD operations for SyntheseMethode, P7Datenextraktion, P7MusterKonsistenz, and P7GradeEinschaetzung models. Also includes user authentication and various forms for data entry.

---

### resources/views/livewire/recherche/phase-p8.blade.php
Die Datei enthält ein Livewire-Komponenten mit den Funktionen für die Erstellung, Bearbeitung und Löschung von Suchprotokollen, Limitationen, Reproduzierbarkeitsprüfungen und Updateplänen innerhalb eines Projektes. Es werden Modelle `Projekt`, `P4Suchstring`, `P8Suchprotokoll`, `P8Limitation`, `P8Reproduzierbarkeitspruefung` und `P8UpdatePlan` benutzt.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Die Datei enthält eine Livewire-Komponente für die Darstellung eines Projektes in einer Recherche. Die wichtigsten Klassen sind App\Models\Recherche\Projekt, Illuminate\Support\Facades\Auth und Livewire. Es gibt Zugriffe auf die Datenbank über Eloquent-Modelle.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Blade-Template für die Darstellung einer Liste von Benutzer-Projekten in der Livewire-Anwendung. Es wird auf die Klasse Projekt zugegriffen, um Daten abzufragen und zu löschen.

---

### resources/views/livewire/recherche/research-input.blade.php
Der Code definiert ein Livewire-Component namens 'research-input', das eine Form zur Anfrage von Recherchen darstellt. Es überprüft die Eingabe, erstellt einen neuen Projekt-Eintrag und aktiviert die LangdockAgent, um die Recherche zu starten. Die Datei importiert den Namespace 'App' sowie die Klassen 'TriggerLangdockAgent', 'Projekt', 'Auth'. Es gibt eine interne Abhängigkeit zur Verarbeitung von Formdaten über Livewire.

---

### resources/views/livewire/settings/appearance.blade.php
Eine Livewire-Komponente, die eine Benutzeroberfläche zur Änderung der Erscheinungseinstellungen für ein Konto darstellt. Es werden drei Themen (Light, Dark und System) angeboten, wobei das Systemthema den aktuellen System-Voreinstellungen entspricht. Die Komponente verwendet die Livewire-Runtime, die 'partials', 'x-settings.layout' und 'appearance-btn' Komponenten und benötigt eine Funktion zum Ändern der DOM-Klassen.

---

### resources/views/livewire/settings/delete-user-form.blade.php
Blade template for a Livewire component (delete-user-form) displaying a delete account modal. It uses `Auth`, `Logout`, and custom validation to confirm password, and `wire:click`, `wire:submit`, and x-data directives for event handling.

---

### resources/views/livewire/settings/password.blade.php
Die Datei definiert ein Livewire-Komponent für das Ändern des Passworts des authentifizierten Nutzers. Es nutzt Authentifizierungsfunktionen von Laravel und eine gültigkeitsprüfung mit Password-Validierungsregeln.

---

### resources/views/livewire/settings/profile.blade.php
Die Datei definiert ein Livewire-Component mit dem Namen 'profile', das die Nutzer-Profilseite erstellt und die Funktionalität bietet, den Benutzernamen und Email-Adresse zu aktualisieren. Es verwendet die Externe Abhängigkeit 'Livewire', 'Illuminate/Support/Facades' und 'App/Models/User'.

---

### resources/views/livewire/settings/two-factor.blade.php
This file is a Livewire component (two-factor.blade.php) for enabling and managing two-factor authentication in a Laravel application using Fortify package. It manages the user's two-factor authentication state, displays QR code, manual setup key and confirmation codes if required. External dependencies include Symfony's HttpFoundation package, Laravel's Fortify, Livewire and Volt components.

---

### resources/views/livewire/settings/webhooks.blade.php
A Livewire component for managing webhooks (external API connections) in the settings of a dashboard application. It allows setting and clearing URLs for 'Dashboard Chat' and 'Recherche starten', with validation, database interaction using Laravel Eloquent (Webhook model), and dispatching custom events.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
UI component for displaying and managing 2FA recovery codes in a Laravel application using Livewire. It allows users to view, regenerate, and hide recovery codes with potential API calls for user authentication.

---

### resources/views/partials/head.blade.php
Die Datei definiert die Grundstruktur des HTML-Dokuments mit Meta-Tags, Titel, Favicon, Apple-Touch-Icon, Manifest, Darkmode-Init und den Verbindungen zu CSS- und JavaScript-Dateien mithilfe von Vite.

---

### resources/views/partials/settings-heading.blade.php
HTML-Kode für eine Division mit einem Kopfzeile und Untertitel, die den Text 'Settings' und eine Erklärung enthält. Dieser Teil ist eine reine UI-Komponente für eine Anwendungsseite, die Konfigurationsoptionen (Profile und Kontokonto) anzeigt.

---

### resources/views/recherche/index.blade.php
Die Datei definiert die Layoutstruktur einer Suchseite mit Titel, Eingabefeld für die Recherche und Liste der Projekte. Die Klassen verwendet die Laravel Livewire-Library (livewire:recherche.research-input und livewire:recherche.projekt-liste).

---

### resources/views/recherche/show.blade.php
Diese Datei renderisiert eine Darstellung des Projekts mit dem Titel aus der Variablen $projekt->titel, indem sie die Komponente livewire:recherche.projekt-detail nutzt, welche über diese Variable parametriert ist.

---

## uncategorized

### Readme.md
Datei bietet eine Beschreibung der Linn.Games Laravel-Anwendung, enthält Informationen zum Stack, Architektur und Features sowie zu wichtigen Klassen, Funktionen und Endpunkten. Die Anwendung dient einer systematischen Literaturrecherche und ist mit KI-gestützter Suche ausgerüstet. Es werden Docker-Dienste, Authentifizierung & Benutzerverwaltung, Kontaktformular, Admin-Panel, CI/CD & Deployment, MCP PostgreSQL Endpoint, Ollama Embedding Endpoint und Paper-Search MCP sowie Webhook-Sicherheit unterstützt.

---

### artisan
Artisan File initialisiert Laravel-Anwendung und führt angegebene Artisan-Befehle aus. Es werden folgende Komponenten verwendet: Illuminate-Foundation, Symfony-Component-Console.

---

### basemodel.md
PostgreSQL database model for Systematic Literature Research, covering all 8 phases (P1–P8). Contains ENUM types for various entities like phase status, review type, and screening level. Imports extensions for UUID generation, full-text search, and others. Key tables include projekte, phasen, p1_strukturmodell_wahl, p2_review_typ_entscheidung, p4_suchstrings, etc.

---

### CLAUDE.md
This Markdown file provides development and deployment commands for a Laravel project named 'app.linn.games'. It details the project's architecture, including backend (Laravel 12, PHP 8.2+, PostgreSQL 16), frontend (Vite, Livewire 3, Tailwind CSS 4), admin interface (Filament 4.9), and AI integration (Langdock Agent, Ollama embeddings). The file also outlines key architectural patterns, security layers, and conventions.

---

### composer.json
Composer configuration file for a Laravel project, managing dependencies and scripts for development environment setup and execution.

---

### CONTRIBUTING.md
Das Dokument beschreibt die Arbeitsablauf-Regeln für Beiträge zum Projekt 'app.linn.games', darunter die Konventionen für Branch-Namen, Merging und lokale Entwicklung. Es beinhaltet zudem Coding-Standards, Anweisungen zur Testung und zu Datenbankänderungen.

---

### deploy.sh
Script for building, deploying and managing a Docker-compose application. It handles building, pulling images, running database migrations, caching configuration, routes and views, optimizing cache, starting all services, sending deployment notifications, and verifying service status.

---

### package.json
This file (package.json) manages dependencies and scripts for a project using Vite, Tailwind CSS, Axios, Concurrently and Laravel-Vite-Plugin. Optional dependencies include Linux binaries for LightningCSS and oxide-Linux-x64-gnu.

---

### phpunit.xml
Configures PHPUnit testing environment for the project. Defines test suites, includes source files for testing, sets environment variables for the test run and specifies that SQLite should be used as the database connection.

---

### vite.config.js
Vite-Konfigurationsdatei für Laravel-Projekte, um CSS und JS-Dateien zu verarbeiten. Stützt die Nutzung von TailwindCSS und bietet CORS für den Server an.

---

### .dockerignore
This .dockerignore file is used for Docker to exclude specific files and directories from the build process.

---

### .editorconfig
Defines coding style conventions for multiple file types using EditorConfig.

Important items:
- root
- [*]
- [*.md]
- [*.{yml,yaml}]
- [compose.yaml]

---

### app/Actions/Fortify/CreateNewUser.php
Defines the CreateNewUser class that validates and creates a new user in the application, leverages the Laravel Validator for input validation and User model for database creation.

---

### app/Actions/Fortify/PasswordValidationRules.php
```json
{
  "file_summary": "Defines custom password validation rules for the Fortify authentication system, using Laravel's built-in validation system. Imports Illuminate\Validation\Rules\Password."
}
```

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei 'app/Actions/Fortify/ResetUserPassword.php' implementiert die Funktion, um ein vergessenes Passwort eines Benutzers zu validieren und zu aktualisieren. Sie benötigt die Klasse User aus dem Modell sowie Laravel's Validator-Fassade und das Fortify-Kontrakt ResetUserPasswords.

---

### app/Filament/Resources/ContactResource.php
Defines Filament resource for handling contact queries. Implements a form and table for managing contacts, with navigation icon and label specified. External dependencies include Filament (for forms, tables, schemas, actions) and App/Models/Contact model.

---

### app/Filament/Resources/UserResource.php
Defines the User resource for Filament, handling forms and tables for managing users in a Laravel application. Imported modules include Filament's Forms, Schemas, Resource, Tables, and Backend Resources.

---

### app/Http/Middleware/TrackPageView.php
Middleware for tracking page views. Records visits in the PageView model when a GET request is made and user agent doesn't match a bot pattern. Anonymizes IP addresses.

---

### app/Http/Middleware/VerifyLangdockSignature.php
This middleware verifies the authenticity of requests from Langdock by validating the signature and timestamp provided in the X-Langdock-Signature and X-Langdock-Timestamp headers. The middleware also prevents replay attacks by using a nonce that expires after 24 hours.

---

### app/Http/Middleware/VerifyMcpToken.php
A middleware that verifies the MCP (Merchant Center Platform) token in HTTP requests. It checks if the Authorization header starts with 'Bearer ', and if the token matches the configured one. If not, it returns an appropriate error response.

---

### app/Jobs/IngestPaperJob.php
Implements a job for ingesting paper data, including title, text, and metadata, into the database using Ollama API embedding. The class handles queuing and retry logic when requests fail.

---

### app/Jobs/TriggerLangdockAgent.php
The TriggerLangdockAgent.php file is a job that handles interactions with the Langdock API for language processing tasks. It uses user and project data to send requests via HTTP, handling retries and errors, and logging failures if necessary.

---

### app/Livewire/Actions/Logout.php
Diese Datei enthält die Logout-Funktionalität für Livewire-Anwendungen innerhalb des Projekts. Es werden die Authentifizierung und die Session verwaltet, indem Auth und Session-Facades aus der Illuminate-Library genutzt werden.

---

### app/Mail/ContactConfirmationMail.php
```json
{
  "file_summary": "Diese Datei enthält eine Klasse für die Versendung einer Bestätigungsmail an Kontaktanfragen bei Linn Games. Sie erbt von Mailable und implementiert ShouldQueue. Es werden die Klassen Contact, Envelope und Content aus dem Namespace App\Mail verwendet."
}
```

---

### app/Mail/ContactInquiryMail.php
```json
{
  "file_summary": "Die Datei definiert eine E-Mail Klasse namens ContactInquiryMail, die auf der Basis von Illuminate\Mail\Mailable erstellt wird. Sie enthält eine Funktion envelope und eine Funktion content. Die envelope-Funktion setzt einen E-Mail-Thema und replyTo, während die content-Funktion den Inhalt der E-Mail durch eine View namens 'emails.contact-inquiry' definiert. Es gibt keine externe Abhängigkeiten in dieser Datei."
}
```

---

### app/Mail/DeployNotificationMail.php
```json
{
  "file_summary": "Die Datei `app/Mail/DeployNotificationMail.php` ist eine Klasse, die eine E-Mail mit dem Titel "Linn.Games — Deployment erfolgreich" sendet, wenn eine Bereitstellung erfolgreich abgeschlossen wurde. Die wichtigsten Komponenten sind die Klasse `DeployNotificationMail` sowie die Funktionen __construct, envelope und content. Es gibt keine externen Abhängigkeiten."
}
```

---

### app/Policies/ProjektPolicy.php
Defines authorization policies for user actions on projects. Contains methods for viewAny, view, create, update, and delete operations on projects for authenticated users.

---

### app/Providers/AppServiceProvider.php
Initialisiert Policy für Projekt-Model und RateLimiters 'mcp' und 'webhooks'. Erzwingt HTTPS im Produktionsmodus.

---

### app/Providers/FortifyServiceProvider.php
This file extends Laravel's Fortify Service Provider to configure authentication actions, views, and rate limiting. It uses custom classes for creating new users and resetting user passwords, as well as Livewire views for various authentication pages.

---

### app/Providers/VoltServiceProvider.php
This file (VoltServiceProvider.php) sets up the Livewire Volt templating engine for the Laravel application, mounting view paths to specific directories in app/views/livewire and app/views/pages.

---

### app/Providers/Filament/AdminPanelProvider.php
Provides configuration for the admin panel using Filament. Includes middleware setup, login functionality, and resource discovery.

---

### bootstrap/app.php
Configures Laravel application instance, sets routing paths and middlewares for web, api, console, health check. Imports 'App\Http\Middleware\TrackPageView', 'Illuminate\Foundation\Application', 'Illuminate\Foundation\Configuration\Exceptions' and 'Illuminate\Foundation\Configuration\Middleware'.

---

### bootstrap/providers.php
Die Datei providers.php initialisiert und registriert Providers für die Anwendung, einschließlich AppServiceProvider, AdminPanelProvider, FortifyServiceProvider und VoltServiceProvider.

---

### bootstrap/cache/.gitignore
Ignoriere alle Dateien und Ordner in diesem Cache-Verzeichnis von Git, außer .gitignore selbst

---

### docker/common/nginx/default.conf
Nginx configuration file for the web application at app.linn.games. It handles static file serving, PHP processing, and proxying to various services including PostgreSQL, Paper Search MCP, and Ollama. Access is controlled through Authorization headers or query parameters.

---

### docker/common/nginx/docker-entrypoint.sh
Bash-Skript, das beim Start von Docker-Container initialisiert. Es kopiert ein Template-Nginx-Konfigurationsdatei und ersetzt einen Platzhalter mit dem Wert von MCP_AUTH_TOKEN (falls definiert). Anschließend führt es das angegebene Kommando weiter („$@“).

---

### docker/common/nginx/dockerfile
Diese Dockerdatei konfiguriert eine Nginx-Umgebung auf Basis der Alpine-Linux-Version, kopiert eine benutzerdefinierte Konfigurationsdatei und die Startskripts, übernimmt eine Sitekonfiguration und stellt den Zugriff auf Port 80 bereit.

---

### docker/common/nginx/nginx.conf
Configures a basic Nginx setup for Laravel applications, including worker connections, real IP handling, gzip compression, sendfile and keepalive settings, file upload limits, and includes additional configuration files.

---

### docker/common/php-fpm/dockerfile
Diese Datei definiert einen Docker-Container für ein PHP-Projekt mit Php 8.4-FPM. Es werden zwei Stagen (Build und Produktion) definiert, wobei die Build-Stage Abhängigkeiten installiert, das Projekt kopiert, Frontend Assets gebaut und Composer verwendet um die Abhängigkeiten zu installieren. In der Produktions-Stage werden PHP Erweiterungen aus der Build-Stage kopiert und ein konfigurationsfreies php.ini-Production-File genutzt. Eine Testumgebung wird als dritte Stagen definiert, die auch alle Abhängigkeiten installiert (einschließlich dev/test).

---

### docker/common/php-fpm/entrypoint.sh
Bash Skript zum Synchronisieren von Frontend Assets, Verzögern des Starts bis auf Anmeldung der Datenbank und Ausführung von Migrations oder Cacheing in Produktion/Staging-Umgebungen. Danach wird der Hauptbefehl (php-fpm or queue:work) ausgeführt.

---

### docker/common/postgres/dockerfile
Dockerfile for a PostgreSQL image with additional packages (postgresql-16-hypopg and postgresql-16-pgvector) installed, updating the package list and cleaning up apt-get cache.

---

### docker/common/postgres/init-extensions.sql
Erstellt erweiterungen 'hypopg', 'pg_stat_statements' und 'vector' für eine PostgreSQL-Datenbank, wenn sie noch nicht vorhanden sind.

---

### docs/overview-2026-04-02_0617.md
Konfiguriert API-Endpunkte und Datenbank-Modelle für die Anwendung. Enthält Klassen wie ContactController, Controller abstrakte Basisklasse für alle API-Controller, DsgvoController, LangdockWebhookController, PaperRagController, Routes für Frontend und Backend sowie Konfigurationsdateien wie .env.example, config/app.php und config/database.php.

---

### public/index.php
Starts a Laravel application and handles the incoming request by initializing the framework, requiring necessary files, checking for maintenance mode, and executing app.php to handle the request

---

### public/robots.txt
Die Datei 'public/robots.txt' ist ein Robots-Exclusion-Standard-Dokument, das bestimmt, welche Seiten eines Websites von Suchmaschinenrobotern zugänglich sind. Es wird allen User-Agents (d.h. alle Webcrawlern) empfohlen, nicht auf die gesamte Website zuzugreifen.

---

### public/site.webmanifest
Web App Manifest defining the site's name, short name, theme color, background color, display mode, and provides two icon images for use in standalone and maskable contexts.

---

### public/.htaccess
Apache .htaccess file for routing and handling various HTTP headers such as Authorization, X-XSRF-Token, managing trailing slashes and redirecting non-existing files to the front controller index.php.

---

### resources/css/app.css
CSS file (app.css) that imports Tailwind CSS and defines custom variants and theming. It also sets base styles for borders and imports views from specified directories.

---

### resources/css/welcome.css
This is a CSS and HTML code for a website's structure, including the design of the header (navbar), logo, navigation links, and buttons. The site also has a mobile navigation menu that appears when clicking on a hamburger icon. Additionally, the site includes some animation effects, transitions, and gradients for visual appeal.

Here's a brief explanation of the main parts:

1. Header (`site-header`) with a fixed background color, box shadow, and transition effect when scrolling down.
2. Navigation inner container (`nav-inner`) with logo and navigation links. The logo has two parts: mark and text. Navigation links have hover effects to change their color and show a subtle background gradient.
3. Mobile navigation menu (`nav-mobile`) that appears when clicking on the hamburger icon (`menu-toggle`). The mobile nav includes the same navigation links as the desktop version but organized differently for better usability on smaller screens.
4. Buttons (`.btn-small`) with different styles (primary and regular) and hover effects for user interaction.
5. CSS variables (e.g., `--border-subtle`, `--accent`, etc.) for easily modifying colors, borders, transitions, and other style properties throughout the code.

---

### resources/js/welcome.js
```json
{
  "file_summary": "JavaScript Datei mit verschiedenen Funktionen:
  1. Automatisches Setzen des Jahres im Footer
  2. Snake3D Karten-Interaktion und -Visualisierung
  3. mobile Navigation toggle
  4. Header scroll effect
  5. Cursor Glow Effect
  6. Scroll Reveal Animations
  7. Smooth scroll für Ankerlinks
  8. Telemetriewerte Animation (simuliert lebendige Daten)
  9. Contact-Form-Abfrage und -Versand
  10. Console Easter Egg"
}
```

---

### .claude/agents/code-quality-architect.md
This file describes the code-quality-architect agent that specializes in redundancy reduction, best practice enforcement, environment-aligned configuration validation, and large-scale task planning. Key responsibilities include identifying duplicate code, checking for best practice violations, planning tasks with phased implementation approaches, analyzing systemic bugs, and validating configurations against environments. External dependencies may exist for web research related to current best practices.

---

### .github/copilot-instructions.md
Diese Datei enthält die wichtigsten Architekturen, Konventionen und Entwicklungsvorschriften für die Anwendung 'app.linn.games'. Es werden verwendet: Laravel 12, Livewire 3/Volt, Filament 4.9, PostgreSQL 16 mit pgvector und Enums, Redis, Pest. Es wird kein Alpine.js verwendet. Die CI/CD-Deployment erfolgt nur manuell via './deploy.sh'. Tests werden mit Pest-Syntax und SQLite in-memory durchgeführt.

---

### .github/instructions/app_linn_games_custom.instructions.md
Diese Datei beschreibt die Architektur und Arbeitsgrundlagen für die proprietäre Research-Management-Plattform 'app.linn.games'. Die Plattform ist für KI-gestützte systematische Literaturrecherche gedacht. Die Hauptfunktionen sind die Erstellung von Rechercheprojekten, Einfügen einer Forschungsfrage und das Durchlaufen von 8 definierten Phasen (P1–P8). Der Code basiert auf PHP, Laravel, Livewire, Filament, Fortify, Tailwind CSS, PostgreSQL, Redis und Ollama. Die Datei enthält eine detaillierte Zusammenfassung des Datenmodells, der Verzeichnisstruktur und der Authentifizierung.

---

### .github/instructions/docker-dev-workflow.instructions.md
Gibt eine Anleitung zum Aufbau und Deployment eines Docker-basierten PHP-Anwendungsentwicklungs-Workflows (Dev) an, einschließlich Konfiguration von Containern, Bind-Mounts, Volumes sowie Lokalen Tests. Stützt sich auf docker-compose.yml für das Basis-Setup und wird durch docker-compose.override.yml erweitert, um die OPcache-Einstellungen zu ändern und Code-Änderungen ohne Rebuild sofort wirksam zu machen.

---
