---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T12:20:02.896846
model: codellama:latest
run_id: codellama:latest
mode: overview
files_total: 248
errors: 0
run_time_s: 1706.0
---

# Funktions-Übersicht — 2026-04-02_1220

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 248
- **Fehler:** 0
- **Laufzeit:** 1706.0s

## api

### app/Http/Controllers/ContactController.php
Die Datei `app/Http/Controllers/ContactController.php` handelt sich um eine PHP-Datei, die die Kontaktanfragen der Benutzer verwaltet und auf der Contact-Seite von Linn Games.

Diese Klasse enthält die folgende Funktion:

* `store`: Die store()-Methode ist verantwortlich für das Speichern der Kontaktanfrage in der Datenbank. Sie überprüft, ob alle benötigten Parameter vorhanden sind und verwendet Laravel's Validierungskomponente, um sicherzustellen, dass die Daten gültig sind.
* `Mail::to('info@linn.games')->queue(new ContactInquiryMail($contact));`: Diese Zeile sendet eine E-Mail-Nachricht an das Linn Games-Supportteam mit Details über die Kontaktanfrage. Die Nachricht wird in der Klasse `ContactInquiryMail` erstellt und per Queue-Funktion gesendet, um die Anforderungen von Laravel zu erfüllen.
* `Mail::to($contact->email)->queue(new ContactConfirmationMail($contact));`: Diese Zeile sendet eine E-Mail-Nachricht an den Benutzer mit Details über die Kontaktanfrage und eine Bestätigung, dass der Vorgang abgeschlossen wurde. Die Nachricht wird in der Klasse `ContactConfirmationMail` erstellt und per Queue-Funktion gesendet, um die Anforderungen von Laravel zu erfüllen.

Die Datei verwendet die folgenden Klassen:

* `App\Http\Controllers\Controller`: Die Basisklasse für alle Controller in Laravel.
* `Illuminate\Http\Request`: Die Request-Klasse von Laravel, die verwendet wird, um Daten aus einem HTTP-Request zu extrahieren und zu überprüfen.
* `Illuminate\Support\Facades\Mail`: Die Mail-Fassade von Laravel, die verwendet wird, um E-Mails zu senden.
* `App\Models\Contact`: Die Kontakt-Modell-Klasse von Linn Games, die verwendet wird, um Kontaktanfragen in der Datenbank zu speichern.

---

### app/Http/Controllers/Controller.php
Here is a summary of the file's functionality in 10 lines or less:

This file contains an abstract controller class for API endpoints. The class provides a basic structure for all API controllers to extend, including methods for handling HTTP requests and responses. It also includes a few utility functions for working with JSON data.

The file imports the necessary namespaces for the class to function properly, such as `Illuminate\Http\Request` and `Illuminate\Support\Facades\Response`. It also defines a few constants for use throughout the class.

In addition to the abstract controller class, the file also contains a few utility functions for working with JSON data, including a function for encoding data as JSON and another for decoding JSON data. These functions are useful for handling data between the API and the client.

---

### app/Http/Controllers/DsgvoController.php
This is a controller class that handles API requests related to data protection and privacy (DSGVO). It includes two methods: export, which allows users to download their personal data in JSON format, and deleteAccount, which deletes the user's account and all associated data.

---

### app/Http/Controllers/LangdockWebhookController.php
The file `app/Http/Controllers/LangdockWebhookController.php` appears to be a controller class in a Laravel application. The controller is responsible for handling HTTP requests and performing actions based on the request data.

The controller defines a single method, `handle`, which takes a `Request` object as input and returns a `JsonResponse` object. The `validate` method of the `Request` object is used to validate the request data against a set of rules defined in an array. If the validation fails, the method throws an exception.

The controller then dispatches a job to the `TriggerLangdockAgent` class using the `dispatch` method provided by Laravel's job queue system. The job is responsible for performing the actual work of handling the webhook request and calling the `trigger_agent` function defined in the `langdock_webhooks` table.

The controller then returns a JSON response with a status code indicating that the request has been successfully queued for processing.

In summary, this file is responsible for receiving and processing webhook requests from the Langdock service, and forwarding them to the appropriate handler function for further processing.

---

### app/Http/Controllers/PaperRagController.php
The provided code is a PHP file containing the implementation of two endpoints: `ingest` and `search`.

The `ingest` endpoint accepts a POST request with JSON data and triggers an asynchronous job to ingest the paper into the system. It validates the input data, creates a job instance using the `IngestPaperJob` class, and returns a JSON response indicating that the job was queued successfully.

The `search` endpoint accepts a GET request with query parameters and performs a search on the paper embeddings database. It validates the input data, sends an HTTP POST request to an external service, retrieves the embedding vector from the response, and queries the paper embeddings database for papers similar to the input query. The results are returned as a JSON response with the found papers' IDs, project IDs, sources, paper IDs, titles, chunk indices, text chunks, and metadata (if any).

External dependencies:

* `Http` class from the Laravel framework for sending HTTP requests to an external service.
* `DB` facade for querying the paper embeddings database.

---

### routes/api.php
{
"file_summary": "This file defines the API routes for the application. It includes endpoints for user authentication, webhook handling, and paper ingestion and search."
}

The file `routes/api.php` defines the API routes for the application. It includes several important functions:

* User authentication: The file includes a route to get the current authenticated user using `Route::get('/user', function () { return request()->user(); });`.
* Webhook handling: The file includes a route to handle Langdock webhooks using `Route::post('/webhooks/langdock', [LangdockWebhookController::class, 'handle'])->middleware([VerifyLangdockSignature::class, 'throttle:webhooks']);`.
* Paper ingestion and search: The file includes routes for ingesting and searching papers using `Route::post('/papers/ingest', [PaperRagController::class, 'ingest'])` and `Route::get('/papers/rag-search', [PaperRagController::class, 'search'])`, respectively. These endpoints are protected by middleware that verifies the MCP token and limits the number of requests to prevent abuse.

The file also imports several classes and facades from the Laravel framework: `App\Http\Controllers\LangdockWebhookController` and `App\Http\Controllers\PaperRagController`, as well as `Illuminate\Support\Facades\Route`. Additionally, it includes two middleware classes: `VerifyLangdockSignature::class` and `VerifyMcpToken::class`. These middleware classes are used to verify the signatures of incoming webhook requests and authenticate requests from the MCP platform.

---

### routes/console.php
Die Datei "console.php" in der routes-Ordner ist eine Art "Kommentar-Datei", die es dem Entwickler ermöglicht, einfache Artisan-Befehle zu definieren und auszuführen. Diese Datei enthält zwei Funktionen:

1. Die `inspire`-Funktion zeigt einem inspirierende Zitat an, das den Benutzer motivieren soll, um sein Bestreben weiterzuverfolgen. Der Befehl lautet: "php artisan inspire".
2. Die `deploy:notify`-Funktion sendet eine Deploy-Benachrichtigung per E-Mail an eine bestimmte Adresse (in diesem Fall "bene@linn.games"). Der Befehl lautet: "php artisan deploy:notify".

Die Funktionen werden über die Artisan-Klasse in Laravel implementiert und nutzen verschiedene Facades, um ihre Funktionalität zu erreichen. Die `inspire`-Funktion verwendet den Inspiring-Facade, um ein Zitat auszuwählen und anzuzeigen, während die `deploy:notify`-Funktion den Mail-Facade nutzt, um eine E-Mail zu senden.

Die Datei enthält keine direkte Verwendung von Datenbanken oder externen APIs, da sie sich auf die Artisan-Klasse beschränkt, um die Funktionalität der Befehle zu implementieren.

---

### routes/web.php
This file defines the routes for a Laravel application. It contains several route definitions, each of which maps to a specific controller method or view. Here are the main features and their corresponding descriptions:

* `Route::get('/', function () { return view('welcome'); })`: This is the default route that loads the welcome page.
* `Route::post('/contact', [ContactController::class, 'store'])->name('contact.store')`: This route sends an HTTP POST request to the `store()` method of the `ContactController` class, which handles the contact form submission. The `name()` method sets a name for this route, which can be used in URLs.
* `Route::view('Impressum.html', 'legal.impressum')->name('impressum')`: This route maps the URL `/Impressum.html` to the view file `legal/impressum.blade.php`.
* `Route::view('dsgvo.html', 'legal.dsgvo')->name('dsgvo')`: This route maps the URL `/dsgvo.html` to the view file `legal/dsgvo.blade.php`.
* `Route::view('AGB.html', 'legal.agb')->name('agb')`: This route maps the URL `/AGB.html` to the view file `legal/agb.blade.php`.
* `Route::view('dashboard', 'dashboard')->middleware(['auth', 'verified'])->name('dashboard')`: This route loads the dashboard view, which requires authentication and verification of the user's email address.
* `Volt::route('settings/profile', 'settings.profile')->name('profile.edit')`: This route maps the URL `/settings/profile` to the `ProfileController@edit()` method. The `name()` method sets a name for this route, which can be used in URLs.
* `Volt::route('settings/password', 'settings.password')->name('user-password.edit')`: This route maps the URL `/settings/password` to the `UserPasswordController@edit()` method. The `name()` method sets a name for this route, which can be used in URLs.
* `Volt::route('settings/appearance', 'settings.appearance')->name('appearance.edit')`: This route maps the URL `/settings/appearance` to the `AppearanceController@edit()` method. The `name()` method sets a name for this route, which can be used in URLs.
* `Volt::route('settings/webhooks', 'settings.webhooks')->name('webhooks.edit')`: This route maps the URL `/settings/webhooks` to the `WebhooksController@edit()` method. The `name()` method sets a name for this route, which can be used in URLs.
* `Volt::route('settings/two-factor', 'settings.two-factor') ->middleware(when(Features::canManageTwoFactorAuthentication() && Features::optionEnabled(Features::twoFactorAuthentication(), 'confirmPassword'), ['password.confirm'], []), )->name('two-factor.show')`: This route maps the URL `/settings/two-factor` to the `TwoFactorController@edit()` method, which requires authentication and verification of the user's email address. The `middleware()` method sets a middleware for this route, which checks if the current user has the ability to manage two-factor authentication and if the confirm password feature is enabled.
* `Route::get('dsgvo/export', [DsgvoController::class, 'export'])->name('dsgvo.export')`: This route maps the URL `/dsgvo/export` to the `export()` method of the `DsgvoController` class, which handles the DSGVO export feature.
* `Route::delete('dsgvo/delete-account', [DsgvoController::class, 'deleteAccount'])->name('dsgvo.delete-account')`: This route maps the URL `/dsgvo/delete-account` to the `deleteAccount()` method of the `DsgvoController` class, which handles the DSGVO delete account feature.
* `Route::get('recherche', fn () => view('recherche.index'))->name('recherche')`: This route maps the URL `/recherche` to the `recherche.index` view file.
* `Route::get('recherche/{projekt}', fn (Projekt $projekt) => view('recherche.show', ['projekt' => $projekt]))->name('recherche.projekt')`: This route maps the URL `/recherche` with a specific project ID to the `recherche.show` view file, which displays information about a specific project.

Overall, this file defines the routes and views for the application's functionality related to user settings, contact form submission, DSGVO export/delete account feature, and project research.

---

## config

### docker-compose.dev.yml
Die Datei `docker-compose.dev.yml` dient als Dev-Override-Datei für die Docker Compose-Konfiguration und ändert den nginx-Port von 6481 auf 6480. Diese Änderung ist notwendig, weil der Port 6481 durch Visual Studio Code belegt wird.

Die Datei enthält nur eine Klasse namens `web`, die einen Port definiert. Der Port `6480` wird auf den Host-Port `80` des Container-Webservers übersetzt.

Dies ist eine wichtige Funktion der Datei, da sie es ermöglicht, das Verhalten der Docker Compose-Konfiguration für Entwicklungszwecke zu ändern.

---

### docker-compose.yml
This is a `docker-compose.yml` file for a Docker Compose project. It defines several services, each of which represents a container running a specific application or process. The file also specifies the networks and volumes that the containers will use, as well as the dependencies between them.

Here are some key points about this file:

* The `services` section lists all the services defined in the file, with their configuration options and any other relevant information.
* Each service has a unique name, which is used to refer to it in other parts of the file. For example, the `web` service has a `build` block that specifies how to build the container image for this service.
* The `ports`, `environment`, and `depends_on` sections specify the network configuration and dependencies between services. For example, the `php-fpm` service depends on the `postgres` and `redis` services, which means that they must be started before the `php-fpm` service can start.
* The `volumes` section specifies the directories that should be mounted as volumes in each container. In this case, there are several volumes defined, including one for the Papers data directory and another for the Ollama data directory.
* The `networks` section defines a network named `linn-network`, which is used to allow containers to communicate with each other.

Overall, this file sets up a Docker Compose project that can be used to manage multiple containers and their dependencies in a convenient way.

---

### .env.example
This is a configuration file for the Laravel framework, specifically the `.env.example` file. It contains environment variables that can be used to customize the behavior of the application.

The most important sections in this file are:

* `APP_NAME`: The name of the application.
* `APP_ENV`: The environment the application is running in (e.g. local, production).
* `APP_KEY`: A unique key used to encrypt sensitive data.
* `APP_DEBUG`: Whether or not debug mode is enabled.
* `APP_URL`: The URL of the application.
* `APP_LOCALE`, `APP_FALLBACK_LOCALE`, `APP_FAKER_LOCALE`: Locale settings for the application.
* `APP_MAINTENANCE_DRIVER`, `APP_MAINTENANCE_STORE`: Maintenance mode configuration.
* `BCRYPT_ROUNDS`: The number of rounds to use for Bcrypt hashing.
* `LOG_CHANNEL`, `LOG_STACK`, `LOG_DEPRECATIONS_CHANNEL`, `LOG_LEVEL`: Logging settings.
* `DB_CONNECTION`, `DB_HOST`, `DB_PORT`, `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD`: Database connection details.
* `SESSION_DRIVER`, `SESSION_LIFETIME`, `SESSION_ENCRYPT`, `SESSION_PATH`, `SESSION_DOMAIN`: Session configuration.
* `BROADCAST_CONNECTION`, `FILESYSTEM_DISK`, `QUEUE_CONNECTION`: Broadcasting, file system, and queue settings.
* `CACHE_STORE`: Cache store type (e.g. redis).
* `MEMCACHED_HOST`: Memcached host.
* `REDIS_CLIENT`, `REDIS_HOST`, `REDIS_PASSWORD`, `REDIS_PORT`: Redis settings.
* `MAIL_MAILER`, `MAIL_SCHEME`, `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`: Mail settings.
* `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_DEFAULT_REGION`, `AWS_BUCKET`: AWS settings (for file uploads).
* `LANGDOCK_API_KEY`, `LANGDOCK_WEBHOOK_URL`, `LANGDOCK_WEBHOOK_SECRET`: Langdock AI settings.
* `POSTGRES_DATABASE`, `POSTGRES_USERNAME`, `POSTGRES_PASSWORD`: PostgreSQL connection details (for docker-compose).
* `MCP_AUTH_TOKEN`: MCP authentication token for accessing the database.
* `LANGDOCK_DB_USERNAME`, `LANGDOCK_DB_PASSWORD`: Langdock DB user credentials (restricted access for MCP).
* `VITE_APP_NAME`: The name of the application (for use with Vite).

---

### config/app.php
This file is part of a Laravel application and contains the configuration for the entire application. It sets several important parameters that determine how the application functions, such as the name of the application, the environment in which it runs, and various security settings. The file also imports external dependencies such as the `.env` file, which defines environmental variables used by the application.

The main sections of this file are:

* `name`: This is the name of the application, which is displayed in notifications and other UI elements. It is set to `Laravel` by default but can be changed in the `.env` file.
* `env`: This sets the environment in which the application runs. The available values are `production`, `staging`, and `local`. Each environment has a different set of configuration options.
* `debug`: This determines whether detailed error messages with stack traces will be displayed when errors occur within the application. It is disabled by default, but can be enabled in the `.env` file.
* `url`: This sets the URL of the application's root directory, which is used by the console to generate URLs for Artisan commands. It defaults to `http://localhost`.
* `timezone`: This sets the timezone that will be used throughout the application. It is set to `UTC` by default.
* `locale`, `fallback_locale`, and `faker_locale`: These determine the locale used by Laravel's translation / localization methods. They are set to `en` by default but can be changed in the `.env` file.
* `cipher`: This sets the encryption algorithm used by Laravel for encrypting sensitive data. It is set to `AES-256-CBC`.
* `key`: This sets the key used by Laravel's encryption services to ensure that all encrypted values are secure. It is set to a random, 32 character string and must be set prior to deploying the application.
* `previous_keys`: This sets an array of previous keys used by Laravel for encrypting sensitive data. It defaults to an empty array but can be populated in the `.env` file.
* `maintenance`: This section determines the driver used to determine and manage Laravel's "maintenance mode" status. The "cache" driver will allow maintenance mode to be controlled across multiple machines. Supported drivers are "file" and "cache".

---

### config/auth.php
Here is a possible summary of the `config/auth.php` file:

* The file contains configuration options for Laravel's authentication system, including default settings for guards and providers, as well as password reset functionality.
* The `defaults` section defines the default guard (`web`) and password broker (`users`) to be used by the application. These values can be changed as needed.
* The `guards` section specifies how authentication is handled for each type of user (in this case, only web users are supported). The `driver` value indicates that the `session` guard is being used, which stores session data in a database table.
* The `providers` section defines how users are retrieved from the database or other storage system. In this case, the `users` provider is using Eloquent to retrieve user data.
* The `passwords` section specifies password reset functionality, including the token table (`password_reset_tokens`) and the expiry time for tokens (60 minutes). This security feature helps keep tokens short-lived, making it more difficult for attackers to guess them.
* The `password_timeout` setting defines the number of seconds before a password confirmation window expires. By default, this is set to three hours (10800 seconds).

Overall, this file contains configuration options for the authentication system in Laravel, which helps manage user authentication and authorization.

---

### config/cache.php
This file contains the configuration for the cache in a Laravel application. The cache is used to store data that can be retrieved quickly, such as user sessions or dynamic calculations.

The `default` key in the configuration specifies the default cache store to use. In this case, it's set to `'database'`. This means that by default, the application will use the database as its cache store.

The `stores` array contains a list of available cache stores and their configurations. Each store has a unique name, which is used to identify it in the configuration file. The available drivers are: `array`, `database`, `file`, `memcached`, `redis`, `dynamodb`, `octane`, `failover`, and `null`.

The `database` store uses the database as its cache backend. It requires a connection to be specified using the `connection` key, which is set to `'env('DB_CACHE_CONNECTION')'`. The `table` key specifies the name of the table to use for storing cached data, and it's set to `'env('DB_CACHE_TABLE', 'cache')'` by default.

The `file` store uses the file system as its cache backend. It requires a path to be specified using the `path` key, which is set to `storage_path('framework/cache/data')` by default. The `lock_path` key specifies the location of the lock file for the cache, and it's set to `storage_path('framework/cache/data')` by default.

The `memcached` store uses the memcached cache backend. It requires a persistent ID to be specified using the `persistent_id` key, which is set to `'env('MEMCACHED_PERSISTENT_ID')'` by default. The `sasl` key specifies the credentials to use for authenticating with the memcached server, and it's set to `['env('MEMCACHED_USERNAME'), 'env('MEMCACHED_PASSWORD')']` by default. The `options` key can be used to specify additional options for the memcached client, such as connection timeouts.

The `redis` store uses the Redis cache backend. It requires a connection name to be specified using the `connection` key, which is set to `'env('REDIS_CACHE_CONNECTION', 'cache')'` by default. The `lock_connection` key specifies the name of the connection to use for locking, and it's set to `'env('REDIS_CACHE_LOCK_CONNECTION', 'default')'` by default.

The `dynamodb` store uses the DynamoDB cache backend. It requires an access key ID and a secret access key to be specified using the `key` and `secret` keys, respectively. The `region` key specifies the AWS region to use for the DynamoDB table, and it's set to `'env('AWS_DEFAULT_REGION', 'us-east-1')'` by default. The `table` key specifies the name of the DynamoDB table to use for caching, and it's set to `'env('DYNAMODB_CACHE_TABLE', 'cache')'` by default.

The `octane` store uses the Octane cache backend. It doesn't require any additional configuration.

The `failover` store is a failover cache store that can be used to provide a backup cache when the primary cache store fails. It requires a list of stores to use for failover, which is specified using the `stores` key. In this case, it's set to `['database', 'array']`.

Finally, the `prefix` key specifies a prefix to be added to every cache key. It's set to `'env('CACHE_PREFIX', Str::slug((string) env('APP_NAME', 'laravel')).'-cache-')` by default, which generates a unique cache key for each application instance based on the `APP_NAME` environment variable and adds the string `'-cache-'` to it.

---

### config/cookie-consent.php
Die Datei `config/cookie-consent.php` enthält die Einstellungen für das Cookie-Zustimmungs-Dialogfeld in Laravel.

Das Objekt zurückgegeben wird eine PHP-Konfigurationsdatei mit folgenden Schlüsseln:

* `enabled`: Wird verwendet, um zu aktivieren oder deaktivieren das Cookie-Zustimmungs-Dialogfeld. Der Standardwert ist `true`.
* `cookie_name`: Gibt den Namen des Cookies an, in dem wir speichern, ob der Benutzer die Bedingungen akzeptiert hat. Der Standardwert ist `'laravel_cookie_consent'`.
* `cookie_lifetime`: Gibt das Cookie-Lebensdauer in Tagen an. Der Standardwert ist `365` (12 Monate / DSGVO).

---

### config/database.php
This is a configuration file for the database connection in a Laravel application. The file defines several database connections, including SQLite, MySQL, MariaDB, PostgreSQL, and SQL Server. Each connection has its own settings, such as the driver, hostname, username, password, and database name.

The `default` connection is set to `sqlite`, which means that if no specific database connection is specified in the configuration file or in the environment variables, Laravel will use the SQLite database by default.

The `migrations` table is used to keep track of all migrations that have been run for the application. The `update_date_on_publish` setting indicates whether the migration date should be updated when a new migration is published.

The `redis` configuration defines settings for the Redis cache database. The `options` array sets the cluster to `redis`, which means that the connection will use the Redis cluster instead of the default Redis instance. The `prefix` setting specifies a prefix for all keys in the cache, which can be useful for preventing conflicts with other applications or caching systems.

Overall, this configuration file sets up the database connections and Redis cache for the Laravel application.

---

### config/filesystems.php
This is a configuration file for Laravel's filesystem management. It sets up the default filesystem disk and defines two disks: `local` and `public`. The `local` disk stores files in the `storage/app/private` directory, while the `public` disk stores files in the `storage/app/public` directory and is accessible via a URL. The file also defines symbolic links for the public disk.

The file uses the Laravel's environment variables to set up the disks. It sets the default filesystem disk as `local`, which can be overridden by setting the `FILESYSTEM_DISK` environment variable. The `disks` array defines the two disks: `local` and `public`. Each disk has a driver, root path, URL (for the `public` disk), visibility, and various other options.

The file also includes an array of symbolic links for the public disk. These links are created when the `storage:link` Artisan command is executed.

---

### config/fortify.php
This file is part of the Laravel Fortify package, which provides authentication and registration features for Laravel applications. The file configures various aspects of Fortify, including the guard, password broker, username field, lowercasing usernames, home path, routes prefix / subdomain, middleware, rate limiting, register view routes, and features.

The guard specifies which authentication guard Fortify will use for user authentication. The password broker defines which password broker Fortify can use when a user is resetting their password. The username field specifies the model attribute that should be considered as the application's "username" field.

The lowercase usernames configuration option determines whether usernames should be lowercased before saving them in the database. If your application uses a case-sensitive string field, you may want to disable this setting.

The home path specifies the path where users will get redirected during authentication or password reset when the operations are successful and the user is authenticated. The prefix and domain options specify the prefix and subdomain under which all of the Fortify routes will be available.

The middleware option specifies which middleware Fortify will assign to the routes it registers with the application.

The limiters configuration option defines the rate limiter that Fortify will use for login attempts. You can specify a custom rate limiter or disable the feature altogether.

The views option specifies whether the routes returning views should be disabled. This may be especially true if you're building a custom single-page application.

Finally, the features configuration option defines which Fortify features are enabled and which are disabled. You can remove certain features or even remove all of them depending on your needs.

---

### config/logging.php
This is the logging configuration file for a Laravel application. It defines the log channels and their configurations, such as the driver, path, level, and other settings.

The file includes several channels:

* `stack`: This channel is used to aggregate multiple log messages into a single log message.
* `single`: This channel logs all messages to a single file located in the `storage/logs` directory. The default log level is set to `debug`.
* `daily`: This channel logs all messages to a daily rotating log file located in the `storage/logs` directory. The default log level is set to `debug`, and the number of days to keep the logs is set to 14.
* `slack`: This channel sends all log messages to a Slack webhook URL, which can be used for notifications or monitoring. The default log level is set to `critical`.
* `papertrail`: This channel logs all messages to a syslog server using the Papertrail protocol. The hostname and port of the syslog server are specified in the `PAPERTRAIL_URL` and `PAPERTRAIL_PORT` environment variables, respectively.
* `stderr`: This channel logs all messages to the standard error stream of the PHP process.
* `syslog`: This channel logs all messages to the syslog facility specified by the `LOG_SYSLOG_FACILITY` environment variable (default: `LOG_USER`).
* `errorlog`: This channel logs all messages to the error log of the PHP process.
* `null`: This channel discards all log messages.
* `emergency`: This channel logs all messages to a single file located in the `storage/logs` directory, and is used for emergency situations when no other logging channels are available.

The file also includes several drivers:

* `single`: The `Single` driver writes log messages to a single file.
* `daily`: The `Daily` driver writes log messages to daily rotating files.
* `slack`: The `Slack` driver sends log messages to a Slack webhook URL.
* `papertrail`: The `Papertrail` driver writes log messages to a syslog server using the Papertrail protocol.
* `stderr`: The `Stderr` driver writes log messages to the standard error stream of the PHP process.
* `syslog`: The `Syslog` driver writes log messages to the syslog facility specified by the `LOG_SYSLOG_FACILITY` environment variable (default: `LOG_USER`).
* `errorlog`: The `Errorlog` driver writes log messages to the error log of the PHP process.
* `monolog`: The `Monolog` driver uses a custom log handler that can be configured using the `handler` and `handler_with` settings.

The file also includes several processors:

* `PsrLogMessageProcessor`: This processor adds context information to each log message, such as the level, channel, and extra data.

Overall, this logging configuration file allows you to customize the logging behavior of your Laravel application by configuring the various channels and drivers that handle log messages.

---

### config/mail.php
This is a configuration file for the email system in a Laravel application. It contains several settings that control how emails are sent and received, including the default mailer used to send all emails, the available mailers, and the global "from" address.

The main function of this file is to provide a centralized location where you can define and manage your email settings. You can specify which mailer you want to use as the default, configure the different mailers that are available, and set the global "from" address for all emails sent by your application.

The file includes several options for configuring the email system in Laravel, including:

* The `default` option, which specifies the default mailer to use when sending emails.
* The `mailers` option, which defines a list of available mailers and their respective settings.
* The `from` option, which sets the global "from" address for all emails sent by your application.

This file also includes several environment variables that can be used to configure the email system further, such as `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, and `MAIL_PASSWORD`. These variables are used to specify the hostname, port number, username, and password for sending emails, respectively.

Overall, this file provides a simple way to manage your email settings in Laravel and can be useful when working with different mailers or configuring the global "from" address.

---

### config/queue.php
This is a configuration file for the Laravel framework's queue system. It defines various settings and options that control how jobs are processed, stored, and failed. Here's a brief summary of what each section does:

* `default`: This specifies the default queue connection name to use when dispatching jobs.
* `connections`: This is an array of available queue connections. Each entry in this array defines a different queue connection, which can be used by Laravel to process jobs.
	+ The `driver` key specifies the type of queue driver to use (e.g., "sync", "database", "beanstalkd", etc.).
	+ The `connection`, `queue`, and `retry_after` keys specify the database connection, queue name, and retry interval for failed jobs, respectively.
	+ The `block_for` and `after_commit` keys are used to control how job processing works with respect to transactions.
* `batching`: This section defines the database and table that store batching information. Batching allows you to group a number of jobs together and process them as a single unit.
* `failed`: This section controls the behavior of failed queue jobs. You can specify different drivers for storing failed jobs (e.g., "database-uuids", "dynamodb", "file", etc.).

---

### config/services.php
The file `config/services.php` is a configuration file for Laravel, which contains credentials for third-party services such as Mailgun, Postmark, AWS, Slack, and Langdock.

Here are the main components of this file:

1. The `postmark` section contains the API key for sending emails using Postmark.
2. The `resend` section contains the API key for resending emails using Resend.
3. The `ses` section contains the credentials for interacting with Amazon SES (Simple Email Service).
4. The `slack` section contains the configuration for Slack notifications, including the bot user OAuth token and default channel.
5. The `langdock` section contains the API key and webhook URL for using Langdock, a tool for testing and validating language translation.
6. The `mcp` section contains the authorization token for interacting with the MCP (Mail Checker Pro) service.

Overall, this file provides a convenient way to store credentials for third-party services used by the Laravel framework.

---

### config/session.php
This file is part of the Laravel framework, specifically the session configuration settings. It defines several options for configuring the behavior of the session system in the application. Here are the main points:

* `driver`: The default driver used to store sessions. The available drivers include "file", "cookie", "database", "memcached", "redis", "dynamodb", and "array". The default is "database".
* `lifetime`: The number of minutes that a session should be allowed to remain idle before it expires. This option can also be set in the `.env` file using the `SESSION_LIFETIME` variable. The default is 120 minutes (2 hours).
* `expire_on_close`: Whether or not sessions should expire when the browser window is closed. When this option is enabled, a session will expire immediately when the user closes their browser. This option can also be set in the `.env` file using the `SESSION_EXPIRE_ON_CLOSE` variable. The default is `false`.
* `encrypt`: Whether or not to encrypt sessions. If enabled, all session data will be encrypted before it's stored. This option can also be set in the `.env` file using the `SESSION_ENCRYPT` variable. The default is `false`.
* `files`: The location where session files are stored when using the "file" driver. By default, this value is set to `storage/framework/sessions`. This option can also be set in the `.env` file using the `SESSION_FILES` variable.
* `connection`: The database connection used by the session driver when using the "database", "memcached", or "redis" drivers. This option can also be set in the `.env` file using the `SESSION_CONNECTION` variable. The default is the value of the `DB_CONNECTION` environment variable.
* `table`: The name of the table used to store sessions when using the "database" driver. This option can also be set in the `.env` file using the `SESSION_TABLE` variable. The default is `sessions`.
* `store`: The cache store used by the session driver when using the "memcached", "redis", or "dynamodb" drivers. This option can also be set in the `.env` file using the `SESSION_STORE` variable. The default is the value of the `CACHE_DRIVER` environment variable.
* `lottery`: An array containing two elements that represent the odds of a session being cleaned up. By default, this value is set to [2, 100], which means that there is a 2% chance that the session will be cleaned up on every request. This option can also be set in the `.env` file using the `SESSION_LOTTERY` variable.
* `cookie`: The name of the cookie used to store the session ID. By default, this value is set to a generated string based on the `APP_NAME` and `SESSION_COOKIE` environment variables. This option can also be set in the `.env` file using the `SESSION_COOKIE` variable.
* `path`: The path for which the session cookie is available. By default, this value is set to `/`. This option can also be set in the `.env` file using the `SESSION_PATH` variable.
* `domain`: The domain and subdomains that the session cookie is available to. By default, this value is set to the root domain without subdomains. This option can also be set in the `.env` file using the `SESSION_DOMAIN` variable.
* `secure`: Whether or not to send the session cookie only over a secure connection (HTTPS). When enabled, the cookie will only be sent to the server when a HTTPS connection is used. This option can also be set in the `.env` file using the `SESSION_SECURE_COOKIE` variable. The default is `false`.
* `http_only`: Whether or not to prevent JavaScript from accessing the session cookie and only allow it to be accessed through the HTTP protocol. When enabled, this option will prevent JavaScript from reading the contents of the cookie. This option can also be set in the `.env` file using the `SESSION_HTTP_ONLY` variable. The default is `true`.
* `same_site`: The Same-Site attribute for the session cookie. By default, this value is set to "lax" to permit secure cross-site requests. This option can also be set in the `.env` file using the `SESSION_SAME_SITE` variable. The supported values are "lax", "strict", "none", and null.
* `partitioned`: Whether or not to tie the session cookie to the top-level site for a cross-site context when using the "file" driver. When enabled, the cookie will be accepted by the browser when flagged "secure" and the Same-Site attribute is set to "none". This option can also be set in the `.env` file using the `SESSION_PARTITIONED_COOKIE` variable. The default is `false`.

In summary, this file allows developers to configure the behavior of the session system in their Laravel application by setting various options that control how sessions are stored, accessed, and managed.

---

### docker/common/php-fpm/php-dev.ini
Die Datei `docker/common/php-fpm/php-dev.ini` ist eine PHP-Konfigurationsdatei für die Verwendung in einem Docker-Container. Sie enthält einige Überrides für die OPcache-Einstellungen und aktiviert die Anzeige von Fehlern und -Berichten.

Hier sind die wichtigsten Klassen, Funktionen oder Endpunkte, die in dieser Datei definiert sind:

* `[opcache]` - Abschnitt für OPcache-Einstellungen.
	+ `opcache.validate_timestamps` = 1 - Aktiviert die Überprüfung der Timestamps bei der Caching-Validierung.
	+ `opcache.revalidate_freq` = 0 - Deaktiviert die automatische Validierung des OPcache nach einer bestimmten Zeitspanne.
* `[PHP]` - Abschnitt für PHP-Einstellungen.
	+ `display_errors` = On - Aktiviert die Anzeige von Fehlern und -Berichten.
	+ `error_reporting` = E_ALL - Gibt an, welche Fehlerarten und -ebenen angezeigt werden sollen.

---

### docs/api.yaml
Die Datei `api.yaml` definiert eine API-Spezifikation für die Research-Management-Plattform `app.linn.games`. Es gibt folgende Funktionen und Endpunkte:

* **User Endpoints**
	+ `GET /api/user`: Gibt den authentifizierten Benutzer zurück.
* **Webhooks Endpoints**
	+ `POST /api/webhooks/langdock`: Empfängt einen Webhook vom Langdock-KI-Agent und startet einen TriggerLangdockAgent-Job in der Redis-Queue.
* **Papers Endpoints**
	+ `POST /api/papers/ingest`: Nimmt einen Paper-Text entgegen, teilt ihn in Chunks auf und dispatcht einen IngestPaperJob.
	+ `GET /api/papers/rag-search`: Sucht in den paper_embeddings via Cosine Distance.

Es gibt folgende Sicherheitsmechanismen:

* `sanctum` für die Authentifizierung von Benutzern mit Laravel Sanctum Bearer-Token.
* `mcpBearer` für die Authentifizierung von Benutzern mit MCP Bearer-Token (MCP_AUTH_TOKEN in .env).
* `langdockHmac` für die Überprüfung der HMAC-SHA256 Signatur.

Es gibt auch folgende Schemas:

* `User`: Das Schema definiert die Eigenschaften des Benutzerobjekts, das von der Authentifizierung zurückgegeben wird.
* `PaperChunk`: Das Schema definiert die Eigenschaften eines Paper-Chunks, der in der Suche verwendet wird.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
This is a template for creating an issue on GitHub, specifically for reporting bugs. The template includes several fields that users can fill out to provide detailed information about the bug they are experiencing.

Here's a breakdown of the different sections in the template:

* **name**: This field displays the name of the template, which is "Bug Report" in this case.
* **description**: This field provides a brief description of the purpose of the template, which is to allow users to report bugs in a structured way.
* **labels**: This field specifies the labels that should be automatically added to the issue when it is created using this template. In this case, the label "bug" is specified.
* **body**: This section contains the actual form fields that users will fill out to report a bug. The first field is a markdown block that provides some guidance on how to use the template.
	+ The next field is a textarea for describing the problem in detail. This field has a required validation, so it must be filled out before the issue can be created.
	+ The third field is also a textarea, but this one is used for providing steps to reproduce the bug. Like the previous field, this field also has a required validation.
	+ The fourth field is a dropdown menu that allows users to select the severity of the bug. This field also has a required validation.
	+ The fifth field is another textarea, but this one is used for listing the affected areas of the codebase.
	+ The sixth and final field is also a textarea, but this one is used for specifying the acceptance criteria for fixing the bug. This field also has a required validation.

Overall, this template provides a structured way for users to report bugs in a clear and concise manner, which can help developers identify and fix issues more efficiently.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
This is a GitHub issue template for feature requests. It includes the following sections:

* Summary: A brief description of the proposed feature.
* Motivation / Problem: The problem that this feature is intended to solve.
* Vorgeschlagene Lösung: A suggested solution for implementing the feature.
* Betroffene Bereiche: The affected files, routes, services, or models.
* Breaking Change?: Whether the feature involves a breaking change (e.g., migration required) or not. If yes, then what kind of migration is necessary?
* Akzeptanzkriterien: Acceptance criteria for when the feature is considered complete.

---

### .github/ISSUE_TEMPLATE/refactor.yml
This file is a template for creating issues related to code refactoring. It includes a description of the proposed refactoring, affected files or areas, and acceptance criteria.

---

### .github/workflows/lint.yml
This is a GitHub Actions workflow file for a PHP project that uses the `pint` package to lint and fix code style issues. The file sets up an environment with PHP 8.4 installed, installs the project's dependencies using Composer, and then runs the `pint` command to lint and fix code style issues.

Here are the main functions of this file:

1. Checkout the code from the repository.
2. Set up a PHP environment with version 8.4 installed.
3. Add Flux credentials loaded from ENV variables.
4. Install dependencies using Composer.
5. Install Node.js and run `npm install` to install any additional dependencies.
6. Run the `pint` command to lint and fix code style issues.
7. Commit any changes made by `pint`. (This step is currently commented out.)

The file specifies the branches on which this workflow should be triggered, using the `on` keyword. It also sets the `contents` permission for the workflow so that it can write to the repository's codebase. The `jobs` section defines a single job named `quality`, which runs on an Ubuntu environment.

Overall, this file is used to set up and automate the linting process for a PHP project using GitHub Actions.

---

### .github/workflows/tests.yml
This file is a GitHub Actions workflow configuration file for running tests. It sets up a test environment using the `shivammathur/setup-php` and `actions/setup-node` actions, installs dependencies using Composer and NPM, adds Flux credentials to the Composer configuration, installs dependencies, copies an example environment file, generates an application key, builds assets, and runs tests.

Here is a summary of the functions in 10 lines or less:

* Sets up a test environment using PHP 8.4 and Node 22.
* Installs dependencies using Composer and NPM.
* Adds Flux credentials to the Composer configuration.
* Installs dependencies.
* Copies an example environment file.
* Generates an application key.
* Builds assets.
* Runs tests using Pest.

---

## data_access

### app/Models/ChatMessage.php
Die Klasse 'ChatMessage' in der App-Namespace ist eine Eloquent-Modellklasse, die eine Tabelle in der Datenbank darstellt. Sie enthält Felder und Beziehungen zu anderen Modellen und kann verwendet werden, um Datensätze in der 'chat_messages'-Tabelle abzufragen oder zu aktualisieren.

---

### app/Models/Consent.php
This file contains the Consent model class, which is used to represent a consent given by a user for data collection. The model has fields such as 'user_id', 'ip_anonymous', 'consent_categories', 'consented_at', and 'revoked_at'. It also has methods such as 'casts' and 'user'.

---

### app/Models/Contact.php
Die Datei `app/Models/Contact.php` enthält eine Klasse namens `Contact`, die es ermöglicht, Datensätze in einer Datenbank zu speichern und abzufragen. Die Klasse erbt von der Eloquent-Klasse in Laravel und definiert ein Modell für den Kontaktformular, das eine Reihe von Feldern enthält, darunter Name, Unternehmen, E-Mail-Adresse, Projekt-Typ und Nachricht.

Die wichtigsten Funktionen dieser Datei sind die Definition der `Contact`-Klasse und die Verwendung des Datenbank-Managers in Laravel, um Datensätze in der Datenbank zu speichern und abzufragen. Die Klasse verwendet auch die Eloquent-Methode `$fillable`, um festzulegen, welche Felder in der Datenbank gespeichert werden können.

Die Datei enthält keine externe Abhängigkeit, da sie die integrierten Funktionen von Laravel und PHP verwendet, um Datensätze in der Datenbank zu speichern und abzufragen.

---

### app/Models/PageView.php
Die Datei `app/Models/PageView.php` enthält eine Klasse namens `PageView`, die eine Eloquent-Model-Klasse ist und es sich um eine Tabelle in der Datenbank handelt, in der Informationen über Webseitenbesuche gespeichert werden.

Die wichtigsten Funktionen dieser Datei sind:

* Die Klasse `PageView` erbt von der Basisklasse `Illuminate\Database\Eloquent\Model`, was bedeutet, dass sie die Fähigkeit hat, Datenbankqueries auszuführen und Objekte daraus zu erstellen.
* Die Klasse enthält eine Variable namens `$fillable`, die eine Liste der Spalten in der Datenbank ist, die gespeichert werden können. In diesem Fall sind dies `path`, `ip_anonymous`, `user_agent` und `visits`.
* Die Klasse hat auch Methoden zum Erstellen, Aktualisieren und Löschen von Objekten in der Datenbank.

---

### app/Models/User.php
Die Datei `app/Models/User.php` enthält eine Klasse namens `User`, die von der Basisklasse `Authenticatable` in Laravel abgeleitet wird. Sie ist für die Verwaltung von Benutzerdaten zuständig und implementiert die folgenden Funktionen:

* Eine Beziehung zwischen `User` und `Role` über das Trait `HasRoles`.
* Eine Beziehung zwischen `User` und `Permission` über das Trait `HasPermissions`.
* Eine Beziehung zwischen `User` und `Activitylog` über das Trait `LogsActivity`.
* Eine Methode `initials()` zum Erhalten der Benutzer-Initialen aus dem Namen.
* Eine Methode `isActive()` zum Überprüfen, ob ein Benutzer aktiv ist.
* Eine Methode `isTrial()` zum Überprüfen, ob ein Benutzer im Testmodus ist.

Die Klasse verwendet auch das Trait `HasFactory` von Laravel für die Erstellung von Benutzerdaten und das Trait `Notifiable` für die Benachrichtigung über Änderungen am Benutzer-Profil.

Die wichtigsten Klassen in dieser Datei sind:

* `User`: Die Hauptklasse, die für die Verwaltung von Benutzerdaten zuständig ist.
* `HasRoles`: Ein Trait, das es ermöglicht, eine Beziehung zwischen `User` und `Role` herzustellen.
* `HasPermissions`: Ein Trait, das es ermöglicht, eine Beziehung zwischen `User` und `Permission` herzustellen.
* `LogsActivity`: Ein Trait, das es ermöglicht, die Aktivität des Benutzers zu protokollieren.

Die wichtigsten Funktionen in dieser Datei sind:

* Die Attribute `$fillable` enthalten eine Liste von Spalten, die für Massen-Einfügevorgänge verfügbar sind.
* Die Attribute `$hidden` enthalten eine Liste von Spalten, die vor der Serialisierung versteckt werden sollen.
* Die Methode `casts()` enthält eine Liste von Spalten und Werten, die als geheim oder gespeichert markiert sind.
* Die Methode `initials()` erzeugt Initialen aus dem Benutzernamen.
* Die Methode `isActive()` überprüft, ob ein Benutzer aktiv ist.
* Die Methode `isTrial()` überprüft, ob ein Benutzer im Testmodus ist.

Die wichtigsten External-Abhängigkeiten in dieser Datei sind:

* `Illuminate\Database\Eloquent\Factories\HasFactory`: Ein Trait, das es ermöglicht, eine Beziehung zwischen `User` und `Factory` herzustellen.
* `Laravel\Fortify\TwoFactorAuthenticatable`: Ein Trait, das es ermöglicht, die Zwei-Faktor-Authentifizierung für den Benutzer zu aktivieren.
* `Illuminate\Notifications\Notifiable`: Ein Trait, das es ermöglicht, Benachrichtigungen über Änderungen am Benutzer-Profil zu senden.
* `Spatie\Activitylog\Models\Concerns\LogsActivity`: Ein Trait, das es ermöglicht, die Aktivität des Benutzers zu protokollieren.

---

### app/Models/Webhook.php
Here is the summary of the file in 10 lines or less:

* This file defines a `Webhook` class that extends the `Model` class from Laravel.
* The `Webhook` class has a `user_id`, `name`, `slug`, `url`, and `frontend_object` fields, as well as a `created_at` timestamp.
* The `callUrl()` method returns the value of the `url` field.
* The `forUser()` static method finds a webhook for a given user and frontend object, if it exists.
* The `user()` method returns a belongs-to relationship to the `User` model.
* The `chatMessages()` method returns a has-many relationship to the `ChatMessage` model.

---

### app/Models/Recherche/P1Komponente.php
The file `app/Models/Recherche/P1Komponente.php` in the Laravel framework contains a PHP class that represents a specific model used for data access within the application. The class is called `P1Komponente`, and it extends the `Model` class provided by Laravel.

The class defines several properties, including `$table`, which specifies the name of the database table used to store instances of this class, and `$timestamps`, which determines whether the model should have timestamps added to its records. The `fillable` property is also defined, which specifies which fields are mass-assignable for creating or updating records.

The class also defines a `projekt()` method that returns an instance of the `BelongsTo` relationship, which allows us to query related models. In this case, it specifies that the `P1Komponente` model belongs to a single `Projekt`.

Finally, the class uses the `HasUuids` trait provided by Laravel to enable UUID support for the model. This means that instances of this class will have UUIDs generated automatically when they are created or updated, and these UUIDs can be used to identify records in the database.

---

### app/Models/Recherche/P1Kriterium.php
This file defines the P1Kriterium model class for the projects table in the database. It uses the HasUuids trait to generate UUIDs for each instance and has timestamps disabled. The model is related to the Projekt model through a belongsTo relationship.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Die Datei `app/Models/Recherche/P1Strukturmodellwahl.php` ist eine Klasse in einem Laravel-basierten Webanwendungssystem, die es ermöglicht, Strukturmodelle für das Projekt `P1` (Projekt 1) zu verwalten.

Die wichtigsten Funktionen dieser Datei sind:

* Die Verwendung von Laravel Eloquent und der Tabelle `p1_strukturmodell_wahl`, um die Strukturmodelle für das Projekt `P1` zu speichern und abzufragen.
* Die Verwendung des Traits `HasUuids` von Illuminate\Database\Eloquent, um die Verwendung von UUIDs (Universally Unique Identifiers) in der Tabelle zu ermöglichen.
* Die Definition von Eigenschaften und Methoden für das Modell, wie z.B. die `projekt`-Methode, die es ermöglicht, das Modell mit einem Projekt zu verknüpfen.
* Die Verwendung von Laravel-Casts, um die Werte der Eigenschaften in einer bestimmten Weise zu formatieren und zu validieren. In diesem Fall wird das Feld `gewaehlt` als Boolean-Wert interpretiert und muss auf `true` oder `false` gesetzt werden.

Die Datei enthält auch eine Reihe von Import-Anweisungen, um externe Abhängigkeiten wie Laravel Eloquent und die Tabelle `p1_strukturmodell_wahl` zu importieren. Es gibt keinen direkten Aufruf der Datei oder ihrer Methoden in der Konsolen-Applikation, da sie nur verwendet wird, um das Modell zu definieren und es in der Datenbank zu speichern und abzufragen.

Die Datei `app/Models/Recherche/P1Strukturmodellwahl.php` ist ein wichtiger Bestandteil einer Laravel-basierten Webanwendung, da sie es ermöglicht, Strukturmodelle für das Projekt `P1` zu verwalten und zu speichern.

---

### app/Models/Recherche/P1Warnsignal.php
{
"file_summary": "This file defines a P1Warnsignal class in the Recherche namespace of the App\Models\Recherche directory. The class extends the Model class and is used to represent warnsignals for a specific project."
}

The file contains the following classes, functions, or endpoints:

* `P1Warnsignal` class: This is a model that represents a warnsignal for a specific project. It extends the `Model` class and has several properties that are defined in the `$fillable` property, including `projekt_id`, `lfd_nr`, `warnsignal`, `moegliche_auswirkung`, and `handlungsempfehlung`. The class also has a `$casts` property that defines how to cast certain properties to specific types.
* `belongsTo` relationship: This is a one-to-many relationship between the `P1Warnsignal` model and the `Projekt` model, where each warnsignal belongs to a specific project. The `belongsTo` method is used to define this relationship.

---

### app/Models/Recherche/P2Cluster.php
This file is a PHP class for a model called `P2Cluster`, which represents a specific type of cluster in the database. The class extends the `Illuminate\Database\Eloquent\Model` class and uses the `HasUuids` trait to generate UUIDs for the model.

The `$table` property is set to `'p2_cluster'`, which indicates that the model should interact with the `p2_cluster` table in the database. The `$timestamps` property is set to `false`, which disables the automatic creation of timestamps for the model.

The `$fillable` property is an array of fields that are mass assignable, meaning they can be filled using the `create()` or `update()` methods. The `projekt_id`, `cluster_id`, `cluster_label`, `beschreibung`, `treffer_schaetzung`, and `relevanz` fields are all fillable.

The `$casts` property is an array of field casts, which determines how the values of certain fields should be interpreted when they are retrieved from the database. In this case, the `treffer_schaetzung` field is cast to an integer value.

Finally, the class defines a relationship with another model called `Projekt`. The `projekt()` method returns a `BelongsTo` object that represents the relationship between the two models.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Die Datei `app/Models/Recherche/P2MappingSuchstringKomponente.php` enthält eine Klasse namens `P2MappingSuchstringKomponente`, die als Eloquent-Model fungiert und die Datenbanktabelle `p2_mapping_suchstring_komponenten` repräsentiert.

Die Klasse erbt von der `Illuminate\Database\Eloquent\Model`-Klasse und nutzt das Trait `HasUuids`, um die Verwendung von UUIDs in der Tabelle zu unterstützen.

Die Klasse definiert folgende Eigenschaften:

* `$table`: Die Datenbanktabelle, die von der Klasse repräsentiert wird. Hier ist dies `p2_mapping_suchstring_komponenten`.
* `$timestamps`: Wenn auf `false` gesetzt, werden die Felder `created_at` und `updated_at` in der Tabelle nicht automatisch gefüllt. Hier ist dies abgelehnt.
* `$fillable`: Eine Liste von Eigenschaften, die im Modell erlaubt sind. Hier sind dies: `projekt_id`, `komponente_label`, `suchbegriffe`, `sprache`, `trunkierung_genutzt` und `anmerkung`.
* `$casts`: Eine Liste von Eigenschaften, die im Modell als bestimmte Datentypen interpretiert werden sollen. Hier sind dies: `suchbegriffe` (als Array), `trunkierung_genutzt` (als Boolean) und `anmerkung` (als String).

Die Klasse definiert auch eine Beziehung zwischen der Tabelle `p2_mapping_suchstring_komponenten` und der Tabelle `projekt`. Hier ist dies durch die Methode `projekt()` implementiert.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Die Datei `app/Models/Recherche/P2ReviewTypEntscheidung.php` enthält eine Klasse für das Speichern und Verwalten von Entscheidungen zu Review-Typen in einer Projektmanagement-Anwendung. Die Klasse erweitert die Illuminate\Database\Eloquent\Model-Klasse und implementiert das HasUuids-Trait, um UUIDs (Universally Unique Identifiers) für die Datensätze zu verwenden.

Die Klasse definiert folgende Funktionen:

* Die `$table` Eigenschaft legt den Namen der Datenbanktabelle fest, in der die Datensätze gespeichert werden.
* Die `$timestamps` Eigenschaft deaktiviert das automatische Erstellen von Zeitstempel-Feldern für das Erstellen und Aktualisieren von Datensätzen.
* Die `$fillable` Eigenschaft legt die Felder fest, die beim Speichern von Datensätzen automatisch ausgefüllt werden sollen.
* Die `$casts` Eigenschaft definiert eine Konvertierung für das `passt`-Feld, um es als booleschen Wert zu interpretieren.
* Die `projekt()`-Funktion definiert eine Beziehung zwischen der Klasse und der `Projekt`-Klasse, indem sie die `belongsTo`-Methode aufruft. Diese Methode definiert eine Beziehung zwischen dem aktuellen Datensatz und einem anderen Datensatz in einer anderen Tabelle.

Die Klasse ist Teil der Datenzugriffsschicht (data access layer) der Anwendung und wird verwendet, um Review-Typen zu speichern und zu verwalten.

---

### app/Models/Recherche/P2Trefferliste.php
* Datei: app/Models/Recherche/P2Trefferliste.php
* Kategorie: data_access
* Inhalt:
	+ Eine Eloquent-Modellklasse für die Tabelle "p2_trefferlisten" in der Datenbank.
	+ Die Klasse verwendet die Trait "HasUuids", um UUIDs zu generieren und die Spalte "id" als UUID zu interpretieren.
	+ Die Klasse definiert eine Beziehung zwischen den Objekten dieser Klasse und den Objekten der Klasse "Projekt" über die Spalte "projekt_id".
	+ Die Klasse enthält Felder für die Spalten "datenbank", "suchstring", "treffer_gesamt", "einschaetzung", "anpassung_notwendig", "suchdatum".
	+ Die Klasse definiert auch Casts für die Felder "treffer_gesamt" und "anpassung_notwendig" als Integer und Boolean.
	+ Die Klasse definiert eine Funktion "projekt()" zur Beziehung zwischen Objekten dieser Klasse und Objekten der Klasse "Projekt".

---

### app/Models/Recherche/P3Datenbankmatrix.php
Die Datei `app/Models/Recherche/P3Datenbankmatrix.php` definiert ein Modell für die Datenbanktabelle `p3_datenbankmatrix`. Das Modell verwendet die Eloquent-ORM von Laravel und ist Teil des Projekts zur Datenrecherche im Bereich der digitalen Bildungsmedien.

Die wichtigsten Funktionen dieser Datei sind:

* Definiert eine Klasse `P3Datenbankmatrix` als Erweiterung der `Model`-Klasse von Laravel
* Implementiert die Verwendung von UUIDs (Universally Unique Identifiers) durch die Nutzung der `HasUuids`-Trait
* Legt den Tabellennamen in der Datenbank auf `p3_datenbankmatrix` fest
* Definiert ein Array von feldenen Attributen für die Modell-Klasse, die mit den Spalten in der Tabelle übereinstimmen
* Definiert eine Beziehung zwischen dem Modell und der `Projekt`-Tabelle als `BelongsTo` Relation

Die wichtigsten Klassen und Funktionen dieser Datei sind:

* `P3Datenbankmatrix`: Die Klasse, die das Modell für die Datenbanktabelle `p3_datenbankmatrix` definiert.
* `HasUuids`: Eine Trait-Klasse, die die Verwendung von UUIDs in Laravel unterstützt.
* `BelongsTo`: Eine Relation-Klasse, die eine Beziehung zwischen einem Modell und einer anderen Tabelle definiert. In diesem Fall ist es eine Beziehung zwischen dem Modell `P3Datenbankmatrix` und der `Projekt`-Tabelle.

---

### app/Models/Recherche/P3Disziplin.php
Die Datei `app/Models/Recherche/P3Disziplin.php` enthält eine Klasse, die eine Eloquent-Model-Klasse für die Tabelle "p3_disziplinen" in der Datenbank ist. Die Klasse verwendet das Trait `HasUuids`, um UUIDs (Universally Unique Identifiers) zu verwenden und ermöglicht es, die ID-Spalte als UUID zu betrachten.

Die Klasse definiert auch eine Reihe von Eigenschaften und Methoden, die für die Interaktion mit der Datenbank erforderlich sind. Die wichtigsten sind:

* `projekt_id`: Eine Eigenschaft, die die ID des zugehörigen Projekts speichert.
* `disziplin`: Eine Eigenschaft, die die Disziplin speichert.
* `art`: Eine Eigenschaft, die den Art der Disziplin speichert.
* `relevanz`: Eine Eigenschaft, die die Relevanz der Disziplin speichert.
* `anmerkung`: Eine Eigenschaft, die eine Anmerkung zu der Disziplin enthält.
* `projekt(): BelongsTo`: Eine Methode, die das Verknüpfen des Modells mit dem Projekt ermöglicht.

Die Klasse verwendet auch das Trait `HasUuids`, um UUIDs zu verwenden und ermöglicht es, die ID-Spalte als UUID zu betrachten.

Das Modell kann zum Speichern von Disziplinen verwendet werden, die im Kontext des Projekts relevant sind. Es können auch Abfragen für das Modell durchgeführt werden, um Daten aus der Tabelle abzurufen oder zu aktualisieren.

---

### app/Models/Recherche/P3GeografischerFilter.php
File Summary: This file defines a model for the P3GeografischerFilter class in the Laravel framework. The class is used to represent geographic filters in the database and provides access to the fields of the filter using Eloquent ORM.

* Name of the class: P3GeografischerFilter
* Location of the file: app/Models/Recherche
* Category: data_access
* Imports:
	+ Illuminate\Database\Eloquent\Concerns\HasUuids
	+ Illuminate\Database\Eloquent\Model
	+ Illuminate\Database\Eloquent\Relations\BelongsTo
* External dependencies:
	+ The class uses the Eloquent ORM to interact with the database.
	+ The class has a belongsTo relationship with the Projekt model, which is defined in the same directory.
* Fields:
	+ id (uuid) - unique identifier of the filter
	+ projekt_id (uuid) - foreign key referencing the Project model
	+ region_land (string) - name of the region in German
	+ validierter_filter_vorhanden (boolean) - indicates whether a validated filter is available for this region
	+ filtername_quelle (string) - source name of the filter
	+ sensitivitaet_prozent (decimal:2) - sensitivity of the filter in percentage
* Methods:
	+ belongsTo() - defines the relationship between the P3GeografischerFilter and Projekt models.

---

### app/Models/Recherche/P3GraueLiteratur.php
Die Klasse P3GraueLiteratur enthält die Datenmodelle für die graue Literatur in einem Projekt. Sie verwendet den Eloquent-ORM von Laravel und hat eine Beziehung zu einer Projekt-Klasse über eine BelongsTo-Beziehung.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Die Datei `app/Models/Recherche/P4Anpassungsprotokoll.php` ist ein Model in der Laravel-Applikation, das die Tabelle "p4_anpassungsprotokoll" in der Datenbank repräsentiert. Das Model enthält eine Reihe von Eigenschaften und Methoden, um mit den Daten der Tabelle zu interagieren.

Die wichtigsten Funktionen der Datei sind:

* Die Definition der Klasse `P4Anpassungsprotokoll`, die von `Model` erbt.
* Die Deklaration des Attributs `$table`, das die Verbindung zur Tabelle "p4_anpassungsprotokoll" definiert.
* Die Definition des Attributs `$fillable`, das angibt, welche Felder der Tabelle in der Datenbank bearbeitet werden dürfen.
* Die Definition des Attributs `$casts`, das angibt, wie die Daten im Modell gespeichert und geladen werden sollen.
* Die Definition der Beziehung zwischen dem Model und der Tabelle "p4_suchstrings" durch den Methodenaufruf `belongsTo(P4Suchstring::class, 'suchstring_id')`.

---

### app/Models/Recherche/P4Suchstring.php
Die Datei `app/Models/Recherche/P4Suchstring.php` enthält eine Klasse namens `P4Suchstring`, die in Laravel verwendet wird, um Suchstrings zu verwalten. Die Klasse erweitert das Modell `Illuminate\Database\Eloquent\Model`, was bedeutet, dass sie eine Datenbank-Tabelle im Modell-Schema von Laravel darstellt.

In dieser Datei sind folgende Funktionen definiert:

* `fillable` - Die Eigenschaften des Objekts, die gefüllt werden können (Suchstring, Projekt-ID, Suchdatum, usw.).
* `casts` - Die Datentypen der Eigenschaften des Objekts, die in bestimmten Formaten dargestellt werden sollen (z.B. `gesetzte_filter` als Array).
* `projekt()` - Eine Beziehung zwischen einem Suchstring und einem Projekt, bei dem ein Suchstring zu einem bestimmten Projekt gehört.
* `anpassungsprotokoll()` - Eine Beziehung zwischen einem Suchstring und einer Anpassung protokolliert, die an diesem Suchstring vorgenommen wurde.
* `suchprotokolle()` - Eine Beziehung zwischen einem Suchstring und mehreren Suchprotokollen, bei denen ein Suchstring als Quelle für die Suche verwendet wird.

Dies sind die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei. Es gibt keine externen Abhängigkeiten, da die Datei nur den Modell-Code von Laravel enthält und keine APIs oder Datenbank-Zugriffe verwendet.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Die Datei `app/Models/Recherche/P4ThesaurusMapping.php` enthält eine PHP-Klasse, die als Modell für die Tabelle `p4_thesaurus_mapping` in einer relationalen Datenbank dient. Die Klasse erweitert die Basisklasse `Model` von Laravel und verwendet das Trait `HasUuids`, um UUIDs (Universally Unique Identifiers) zu verwenden, anstatt die Standard-IDs der Tabelle zu verwenden.

Die Klasse hat folgende Eigenschaften und Methoden:

* Die Eigenschaft `$table` definiert den Namen der Datenbanktabelle, auf die die Klasse zugreift.
* Die Eigenschaft `$timestamps` legt fest, dass Datumsangaben für das Erstellen und Aktualisieren von Datensätzen generiert werden sollen.
* Die Eigenschaft `$fillable` enthält eine Liste der Felder in der Tabelle, die für Massen-Assignment geeignet sind.
* Die Methode `projekt()` definiert eine Beziehung zwischen der Klasse und der Klasse `Projekt`, indem sie den Namen der Fremdschlüsselspalte `projekt_id` verwendet.

Die Datei benötigt keine Extern-Abhängigkeiten, da sie die Laravel-Standard-Klassen und -Traits verwendet.

---

### app/Models/Recherche/P5PrismaZahlen.php
The file `app/Models/Recherche/P5PrismaZahlen.php` is a PHP class that represents a model for the `p5_prisma_zahlen` table in an Eloquent ORM (Object-Relational Mapping) framework. It extends the `Model` class and uses the `HasUuids` trait to generate UUIDs for the ID column of the table.

The model defines a number of attributes, including:

* `projekt_id`: A foreign key referencing the ID of a project in the `projects` table.
* `identifiziert_gesamt`: The total number of identified literature records.
* `davon_datenbank_treffer`: The number of literature records that have been found in the database.
* `davon_graue_literatur`: The number of grey literature records that have been identified.
* `nach_deduplizierung`: The number of literature records after deduplication.
* `ausgeschlossen_l1`: The number of literature records that have been excluded from the final list due to various reasons.
* `volltext_geprueft`: Whether or not the full-text search has been performed on the literature records.
* `ausgeschlossen_l2`: The number of literature records that have been excluded from the final list after the full-text search has been performed.
* `eingeschlossen_final`: The final number of literature records to be included in the project's literature analysis.

The model also defines a relationship between the `P5PrismaZahlen` model and the `Projekt` model, which is represented by the `projekt()` method. This method returns a `BelongsTo` relationship object that allows you to access the associated `Projekt` model instance.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Die Datei `app/Models/Recherche/P5ScreeningEntscheidung.php` definiert eine Eloquent-Model-Klasse, die eine Tabelle in der Datenbank darstellt. Die Klasse verwendet den `HasUuids`-Trait, um UUIDs zu generieren und verwendet die `Illuminate\Database\Eloquent\Relations\BelongsTo` Relation, um die Beziehung zwischen dieser Tabelle und der `P5Treffer`-Tabelle (die von der Klasse `App\Models\Recherche\P5Treffer` repräsentiert wird) herzustellen.

Die Klasse definiert auch einige Attribute, wie z.B. `$table`, `$timestamps`, `$fillable` und `$casts`, die es ermöglichen, die Datenstruktur der Tabelle zu definieren.

Das Modell kann auch Methoden zur Interaktion mit der Datenbank definieren, wie z.B. `treffer()` für die Beziehung zwischen den Datensätzen dieser Tabelle und der `P5Treffer`-Tabelle.

Insgesamt handelt es sich bei dieser Datei um eine Model-Klasse, die eine Tabelle in der Datenbank darstellt und Methoden zur Interaktion mit ihr definiert.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Die Datei `app/Models/Recherche/P5ScreeningKriterium.php` definiert eine Klasse für ein Modell in einer Laravel-Anwendung, das P5-Screening-Kriterien darstellt.

Die Klasse extends von der Model-Klasse und verwendet das Trait `HasUuids`, um die Verwendung von UUIDs (Universally Unique Identifiers) zu ermöglichen.

Es gibt einige wichtige Funktionen und Eigenschaften in dieser Datei:

* Die Tabelle des Modells wird mit dem Namen `p5_screening_kriterien` initialisiert.
* Der Zeitstempel für das Modell wird deaktiviert, da die Tabelle keine Timestamps enthält.
* Die Eigenschaften `projekt_id`, `level`, `kriterium_typ`, `beschreibung` und `beispiel` können festgelegt werden.
* Die Funktion `projekt()` gibt eine Beziehung zwischen dem Modell und der `Projekt`-Tabelle zurück.

Dieses Modell wird wahrscheinlich für die Speicherung von P5-Screening-Kriterien in einer Laravel-Anwendung verwendet, wie z.B. in einem Datenbankmodell oder als Teil einer API-Endpunkt-Implementierung.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Die Datei "app/Models/Recherche/P5ToolEntscheidung.php" enthält eine Eloquent-Modellklasse für die Tabelle "p5_tool_entscheidung" in der Datenbank. Die Klasse definiert die Beziehungen zwischen den Objekten dieser Tabelle und anderen Objekten, die über andere Tabellen in der Datenbank verbunden sind.

Hier sind die wichtigsten Funktionen der Datei:

* Das Modell "P5ToolEntscheidung" verwendet die Tabelle "p5_tool_entscheidung", die Informationen zu den Entscheidungen eines Projekts in einem P5-Prozess speichert.
* Die Klasse definiert eine Beziehung zwischen dem Objekt und dem Projekt, das es repräsentiert (über die "belongsTo"-Methode).
* Das Modell enthält auch Attribute, die für die Speicherung von Informationen über die Entscheidung relevant sind, wie zum Beispiel den Namen des Tools, das verwendet wurde, ob die Entscheidung gewählte wurde und eine Begrundung für die Entscheidung.
* Die Klasse implementiert auch die Methode "hasUuids", um die Verwendung von UUIDs in der Datenbank zu ermöglichen.

---

### app/Models/Recherche/P5Treffer.php
Die Datei `app/Models/Recherche/P5Treffer.php` ist ein Model-Klassendatei in Laravel, die die Treffer-Entität in der Datenbank darstellt.

Die Klasse `P5Treffer` extends von `Illuminate\Database\Eloquent\Model`, was bedeutet, dass sie die Fähigkeiten von Eloquent im Laravel Framework verwendet. Sie enthält folgende Funktionen:

* `use HasUuids;` - eine Trait-Klasse, die UUIDs (Universally Unique Identifiers) für die Treffer-Entität generiert.
* `protected $table = 'p5_treffer';` - definiert den Tabellennamen in der Datenbank, in dem die Treffer-Entitäten gespeichert sind.
* `public $timestamps = false;` - gibt an, dass das Model keine Timestamp-Felder (Erstellungs- und Aktualisierungszeitstempel) hat.
* `protected $fillable = [...];` - definiert die Felder der Treffer-Entität, die während des Einfügens oder Aktualisierens gefüllt werden dürfen.
* `protected $casts = [...];` - definiert die Typen von den Feldern der Treffer-Entität. Hier sind das `jahr` als Integer und `ist_duplikat` als Boolean.
* `public function projekt(): BelongsTo;` - definiert eine Beziehung zwischen dem Modell `P5Treffer` und dem Modell `Projekt`, bei der ein Treffer zu einem Projekt gehört. Diese Beziehung ist vom Typ "BelongsTo".
* `public function duplikatOriginal(): BelongsTo;` - definiert eine Beziehung zwischen einem Treffer und dem Original-Treffer, falls es sich um einen Duplikat handelt.
* `public function duplikate(): HasMany;` - definiert eine Beziehung zwischen einem Original-Treffer und allen anderen Duplikate, die diesem Original-Treffer zugeordnet sind. Diese Beziehung ist vom Typ "HasMany".
* `public function screeningEntscheidungen(): HasMany;` - definiert eine Beziehung zwischen einem Treffer und den Screening-Entscheidungen, die diesem Treffer zugeordnet sind. Diese Beziehung ist vom Typ "HasMany".
* `public function qualitaetsbewertung(): HasMany;` - definiert eine Beziehung zwischen einem Treffer und den Qualitätsbewertungen, die diesem Treffer zugeordnet sind. Diese Beziehung ist vom Typ "HasMany".
* `public function datenextraktion(): HasMany;` - definiert eine Beziehung zwischen einem Treffer und den Datenextraktionen, die diesem Treffer zugeordnet sind. Diese Beziehung ist vom Typ "HasMany".

Diese Model-Klasse enthält auch eine Trait-Klasse namens `HasUuids`, die UUIDs für die Treffer-Entität generiert.

Die Klasse `P5Treffer` definiert außerdem verschiedene Beziehungen zwischen verschiedenen Modellen in der Datenbank, wie zum Beispiel zwischen einem Projekt und seinen Treffern, oder zwischen einem Original-Treffer und allen Duplikaten.

---

### app/Models/Recherche/P6Luckenanalyse.php
This file contains the definition of a model class for accessing data related to the P6Luckenanalyse table in the database. The model is based on the Eloquent ORM provided by Laravel, and it defines relationships with other models through the use of the Illuminate\Database\Eloquent\Relations\BelongsTo trait.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Die Datei `app/Models/Recherche/P6Qualitaetsbewertung.php` enthält eine Klasse für die Model-Klasse von P6Qualitaetsbewertung in der Laravel-Anwendung.

* Die Klasse verwendet die Trait `HasUuids`, das in Laravel integriert ist, um UUIDs zu verwenden anstatt die Standard-IDs.
* Die Klasse erbt von `Illuminate\Database\Eloquent\Model` und hat die Tabelle `p6_qualitaetsbewertung`.
* Die Eigenschaften `$timestamps` und `$fillable` werden verwendet, um das Timestamp-Feld und die Spalten `treffer_id`, `studientyp`, `rob_tool`, `gesamturteil`, `hauptproblem`, `im_review_behalten`, `anmerkung`, `bewertet_von`, und `bewertet_am` als gefüllte Felder zu kennzeichnen.
* Die Eigenschaft `$casts` wird verwendet, um den Wert von `im_review_behalten` auf ein boolesches Objekt und den Wert von `bewertet_am` auf eine Datums-Zeit-Objekt zu konvertieren.
* Die Methode `treffer()` gibt die Beziehung zwischen der Klasse `P6Qualitaetsbewertung` und der Klasse `P5Treffer`.

---

### app/Models/Recherche/P7Datenextraktion.php
Die Datei `app/Models/Recherche/P7Datenextraktion.php` enthält eine Eloquent-Model-Klasse für die Tabelle `p7_datenextraktion` in der Datenbank. Diese Klasse definiert eine Beziehung zwischen dem Modell und einer `treffer`-Tabelle (durch die Verwendung der `BelongsTo`-Relation) und erlaubt das Speichern von Daten in der `p7_datenextraktion`-Tabelle.

Die Klasse verwendet die `HasUuids`-Trait, was bedeutet, dass sie UUIDs (Universally Unique Identifiers) für die Eigenschaften des Modells verwendet. Die Tabelle `p7_datenextraktion` hat auch einen UUID-Index.

Die Klasse definiert eine Reihe von Eigenschaften (`treffer_id`, `land`, `stichprobe_kontext`, `phaenomen_intervention`, `outcome_ergebnis`, `hauptbefund`, `qualitaetsurteil` und `anmerkung`), die den Datensatz darstellen. Die Eigenschaften können gespeichert werden, indem man das Modell erstellen und die entsprechenden Werte für die Eigenschaften setzen.

Die Klasse definiert auch eine Funktion `treffer()`, die eine Beziehung zwischen dem Modell und einer anderen Tabelle (durch die Verwendung der `BelongsTo`-Relation) herstellt. Diese Funktion gibt den zugehörigen Treffer zurück, der durch die ID des Modells identifiziert wird.

Die Datei importiert auch die `Illuminate\Database\Eloquent\Concerns\HasUuids` Trait und die `P5Treffer`-Klasse (die eine Beziehung zwischen dem Modell und der `treffer`-Tabelle definiert).

Die Datei enthält keine Prosa oder Kommentare vor oder nach dem JSON-Block.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Here is a summary of the `P7GradeEinschaetzung` class in JSON format:

{
"file_summary": "This file defines a P7GradeEinschaetzung model for the Recherche category. It extends the Illuminate\Database\Eloquent\Model class and uses the HasUuids trait to generate UUIDs for the model's ID. The model is defined for the p7_grade_einschaetzung table in the database, and it has timestamps disabled."
"classes": [
{
"name": "P7GradeEinschaetzung",
"namespace": "App\Models\Recherche",
"extends": "Illuminate\Database\Eloquent\Model",
"uses": ["HasUuids"],
"properties": [
{
"name": "table",
"value": "p7_grade_einschaetzung"
},
{
"name": "timestamps",
"value": false
},
{
"name": "fillable",
"value": ["projekt_id", "outcome", "studienanzahl", "rob_gesamt", "inkonsistenz", "indirektheit", "impraezision", "grade_urteil", "begruendung"]
},
{
"name": "casts",
"value": ["studienanzahl": "integer"]
}
],
"methods": [
{
"name": "projekt",
"return_type": "Illuminate\Database\Eloquent\Relations\BelongsTo",
"description": "Defines a belongs-to relationship with the projekt model."
}
]
},
{
"name": "Projekt",
"namespace": "App\Models",
"extends": "Illuminate\Database\Eloquent\Model",
"uses": ["HasUuids"],
"properties": [
{
"name": "table",
"value": "projekte"
},
{
"name": "timestamps",
"value": false
}
],
"methods": [
{
"name": "belongsTo",
"return_type": "Illuminate\Database\Eloquent\Relations\BelongsTo",
"description": "Defines a belongs-to relationship with the p7_grade_einschaetzung model."
}
]
}
]
}

---

### app/Models/Recherche/P7MusterKonsistenz.php
Diese PHP-Datei definiert eine Klasse namens P7MusterKonsistenz, die ein Modell für eine Tabelle in der Datenbank darstellt. Sie enthält mehrere wichtige Funktionen und Eigenschaften, wie z.B. die Verwendung von Eloquent-Relationen und Arrays für Unterstützende und Widersprechende Quellen.

---

### app/Models/Recherche/P7SyntheseMethode.php
This file contains a class for the P7SyntheseMethode model in the Laravel framework. It extends the Model class and defines a table name and timestamps. It also specifies that the project_id field is fillable and casts it as a boolean.

---

### app/Models/Recherche/P8Limitation.php
Die Datei `app/Models/Recherche/P8Limitation.php` enthält eine Klasse `P8Limitation`, die eine Eloquent-Model-Klasse für den Zugriff auf die Datenbanktabelle `p8_limitationen` ist.

Die wichtigsten Funktionen der Klasse sind:

* Die Konfiguration der Tabellen- und Spaltennamen durch die Variablen `$table` und `$fillable`.
* Die Definition von Relationen zu anderen Models, insbesondere zur `Projekt`-Klasse.
* Das Setzen von Timestamps für die Model-Klasse.

Es gibt keine externe Abhängigkeit in dieser Datei.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Die Datei `app/Models/Recherche/P8Reproduzierbarkeitspruefung.php` enthält einen Eloquent-Modellklassen für die Tabelle `p8_reproduzierbarkeitspruefung` in der Datenbank. Die Klasse erweitert die Basisklasse `Model` aus Laravel und verwendet den `HasUuids`-Trait, um die Verwendung von UUIDs (Universally Unique Identifiers) für die Primärschlüssel zu ermöglichen.

Die Klasse hat eine Beziehung zur Tabelle `projekte` über die Spalte `projekt_id`, indem sie den `belongsTo`-Typ verwendet. Sie definiert auch eine Spalte `pruefpunkt` als fillable, was bedeutet, dass der Wert dieser Spalte vor dem Speichern in der Datenbank festgelegt werden muss.

Die Klasse hat auch eine Eigenschaft `casts`, die den Typ von Spalten definiert, die als Booleans interpretiert werden sollen. Hier ist der Wert für die Spalte `erfuellt` auf `boolean` gesetzt.

Insgesamt stellt diese Datei einen Eloquent-Modellklassen dar, der für das Speichern und Abrufen von Daten in der Tabelle `p8_reproduzierbarkeitspruefung` verwendet wird.

---

### app/Models/Recherche/P8Suchprotokoll.php
This file defines a PHP class named `App\Models\Recherche\P8Suchprotokoll` that represents a search protocol in the P8 system. The class extends the `Illuminate\Database\Eloquent\Model` class and uses the `HasUuids` trait to generate UUIDs for each instance.

The table name is set to 'p8_suchprotokoll' and the timestamps are disabled. The fillable fields are:

* `suchstring_id`: a foreign key that references the `App\Models\Recherche\P4Suchstring` class.
* `datenbank`: a string field representing the database used for the search.
* `suchdatum`: a date field representing the date of the search.
* `db_version`: an integer field representing the version number of the database used for the search.
* `suchstring_final`: a string field representing the final search string used for the search.
* `gesetzte_filter`: an array field representing any filters that were applied to the search.
* `treffer_gesamt`: an integer field representing the total number of hits returned by the search.
* `treffer_eindeutig`: an integer field representing the number of unique hits returned by the search.

The class also defines a relationship between instances of this class and instances of the `App\Models\Recherche\P4Suchstring` class, using the `BelongsTo` method.

---

### app/Models/Recherche/P8UpdatePlan.php
This file defines a P8UpdatePlan model that stores information about the P8 update plan, including the project ID, update type, interval, responsible person, tool, and next update date.

---

### app/Models/Recherche/PaperEmbedding.php
Die Datei "app/Models/Recherche/PaperEmbedding.php" enthält eine Klasse "PaperEmbedding", die in Eloquent-Terminologie ein Modell ist. Dieses Modell stellt eine Tabelle in der Datenbank dar und erlaubt den Zugriff auf diese Tabelle mithilfe von Eloquents Funktionen.

Die Klasse "PaperEmbedding" hat folgende Funktionen:

* Es definiert die Verwendung von UUIDs als Primärschlüssel, indem es die "HasUuids"-Klasse importiert und diese als Trait verwendet.
* Es definiert den Namen der Tabelle, in der die Daten gespeichert werden sollen, indem es den Wert "paper_embeddings" für das Attribut "$table" festlegt.
* Es definiert, dass das Modell keine Timestamps verwenden soll, indem es den Wert "false" für das Attribut "$timestamps" festlegt.
* Es definiert die Spalten, die im Modell gespeichert werden sollen, indem es den Array "$fillable" mit einer Liste von Spaltennamen füllt.
* Es definiert die Art der Felder in den Spalten, indem es den Array "$casts" mit einer Liste von Cast-Definitionen füllt. In diesem Fall wird das Feld "metadata" als Array interpretiert und das Feld "erstellt_am" als Datum.
* Es definiert eine Beziehung zwischen dem Modell "PaperEmbedding" und dem Modell "Projekt" über die Spalte "projekt_id". Diese Beziehung wird durch die Funktion "projekt()" implementiert, die den Eloquent-Query-Builder verwendet, um die Beziehung zu definieren.

---

### app/Models/Recherche/Phase.php
This file defines the Phase model in the Recherche directory. It extends the Illuminate\Database\Eloquent\Model class and specifies the table name as 'phasen'. The model has a timestamps property set to false, indicating that it does not have any timestamp columns. The fillable property is an array of strings specifying which attributes are mass assignable, while the casts property defines how certain attributes should be cast before being saved to the database.

---

### app/Models/Recherche/Projekt.php
Die Datei `app/Models/Recherche/Projekt.php` definiert eine Klasse namens `Projekt`, die ein Eloquent-Modell für die Tabelle `projekte` in der Datenbank ist. Die Klasse verwendet several traits, including `HasFactory`, `HasUuids`, and `LogsActivity`.

The class has several methods that define the relationships between the `Projekt` model and other models, such as `user()`, `phasen()`, `p1Strukturmodellwahl()`, etc. The `getActivitylogOptions()` method is used to configure the activity logging options for the `Projekt` model.

The class also has several attributes that define the columns in the `projekte` table, such as `user_id`, `titel`, `forschungsfrage`, and `review_typ`. The `$casts` property is used to cast certain attributes to specific types, such as `startdatum` and `letztes_update` to `datetime` and `erstellt_am` to `date`.

The class has a total of 28 methods, including the ones mentioned above, which define the relationships between the `Projekt` model and other models. The class also uses several traits, such as `HasFactory`, `HasUuids`, and `LogsActivity`, which provide additional functionality for the model.

---

### database/factories/UserFactory.php
This is a factory for the `User` model in Laravel. The factory is used to create new instances of the `User` model with randomized data.

The factory uses the `Illuminate\Database\Eloquent\Factories\Factory` class and extends it to add custom behavior. It defines a `definition()` method that returns an array of values for the model's attributes, as well as other methods like `unverified()` and `withoutTwoFactor()` to customize the data generated by the factory.

The `definition()` method in this case generates randomized data for the `name`, `email`, `password`, and `remember_token` attributes of the model, using the `fake()` and `Hash::make()` functions from Laravel's built-in faker library. It also sets some default values for other attributes like `two_factor_secret`, `two_factor_recovery_codes`, and `two_factor_confirmed_at`.

The `unverified()` method is used to generate a new instance of the model where the email address is not verified, while the `withoutTwoFactor()` method generates a new instance where two-factor authentication is not enabled.

---

### database/factories/Recherche/ProjektFactory.php
Die Datei `database/factories/Recherche/ProjektFactory.php` definiert einen Eloquent-Model-Fabrik für die Projekt-Klasse in der Applikation. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Factories\Factory` und implementiert die `definition()`-Methode, die eine Array-Konfiguration für die Erstellung von neuen Projekten enthält.

Die Fabrik definiert ein Objekt der Klasse `App\Models\Recherche\Projekt`, das über eine Beziehung zur Klasse `App\Models\User` verfügt. Die `user_id`-Eigenschaft wird mit einem Fake-Objekt der Klasse `App\Models\User` initialisiert, indem die `User::factory()`-Methode aufgerufen wird.

Die Fabrik definiert auch eine Eigenschaft `titel`, die einen zufälligen Satz aus dem Satz von Wörtern generiert, der durch die `fake()->sentence()`-Methode bereitgestellt wird.

Die Fabrik definiert auch eine Eigenschaft `forschungsfrage`, die einen zufälligen Absatz aus dem Satz von Wörtern generiert, der durch die `fake()->paragraph()`-Methode bereitgestellt wird.

Darüber hinaus importiert die Datei die Klasse `Illuminate\Database\Eloquent\Factories\Factory` und definiert eine Beziehung zwischen der Fabrik und der Klasse `App\Models\Recherche\Projekt`.

---

### database/migrations/0001_01_01_000000_create_users_table.php
The file is a migration for creating tables in the database. It defines three tables: `users`, `password_reset_tokens`, and `sessions`.

Here's a summary of each table:

* `users`: This table stores information about users, including their name, email address, password, and other details. The primary key is the id column, which is automatically incremented by Laravel.
* `password_reset_tokens`: This table stores tokens for resetting passwords. Each user can have one token at a time, which is used to verify the user's identity when they request a password reset.
* `sessions`: This table stores information about active user sessions. Each session has a unique id, and can optionally store additional data such as the user's IP address and user agent. The last activity timestamp is also stored, which indicates when the session was last accessed or created.

The file imports several classes from Laravel: `Migration`, `Blueprint`, and `Schema`. These classes are used to define the structure of the tables and the relationships between them.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Die Datei `database/migrations/0001_01_01_000001_create_cache_table.php` ist eine Migration, die eine Tabelle namens `cache` und eine Tabelle namens `cache_locks` in der Datenbank erstellt.

Die Migration verwendet den Framework `Illuminate\Database\Migrations\Migration`, um die Tabelle zu erstellen und zu verwalten.

Die Tabelle `cache` hat die folgenden Spalten:

* `key`: eine primäre Schlüsselspalte, die den Schlüssel der Cachecache speichert.
* `value`: eine Spalte zum Speichern des Wertes der Cachecache.
* `expiration`: eine Spalte zum Speichern der Ablaufzeit der Cachecache.

Die Tabelle `cache_locks` hat die folgenden Spalten:

* `key`: eine primäre Schlüsselspalte, die den Schlüssel der Sperre speichert.
* `owner`: eine Spalte zum Speichern des Besitzers der Sperre.
* `expiration`: eine Spalte zum Speichern der Ablaufzeit der Sperre.

Die Migration verwendet das Framework `Illuminate\Database\Schema\Blueprint`, um die Tabelle zu erstellen und zu verwalten. Das Framework `Illuminate\Support\Facades\Schema` wird verwendet, um die Schema-Migrationen zu verwalten.

Die Datei importiert auch das Framework `Illuminate\Database\Migrations\Migration`, das für die Erstellung und Verwaltung von Tabellen in der Datenbank verwendet wird.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
The file is a migration for the Laravel framework that creates several tables in the database, including `jobs`, `job_batches`, and `failed_jobs`.

The `up` method of the migration creates these tables using the `Schema::create()` method. It first creates the `jobs` table with columns for `id`, `queue`, `payload`, `attempts`, `reserved_at`, `available_at`, and `created_at`. The `job_batches` table has columns for `id`, `name`, `total_jobs`, `pending_jobs`, `failed_jobs`, `failed_job_ids`, `options`, `cancelled_at`, and `created_at`. The `failed_jobs` table has columns for `id`, `uuid`, `connection`, `queue`, `payload`, `exception`, and `failed_at`.

The `down` method of the migration drops these tables if they exist using the `Schema::dropIfExists()` method.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Datei: database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Kategorie: data_access

* Diese Datei fügt der Tabelle "users" die Spalten 'two_factor_secret', 'two_factor_recovery_codes' und 'two_factor_confirmed_at' hinzu.
* Die Spalte 'two_factor_secret' enthält den zwei-Faktor-Authentifizierungs-Schlüssel des Benutzers, der für die Überprüfung des Passworts verwendet wird.
* Die Spalte 'two_factor_recovery_codes' enthält einen Satz von Recovery-Codes, die verwendet werden können, um den Zugriff auf das Konto wiederherzustellen, wenn der Benutzer sein Passwort vergessen hat.
* Die Spalte 'two_factor_confirmed_at' wird verwendet, um zu bestimmen, ob der Benutzer die zwei-Faktor-Authentifizierung erfolgreich abgeschlossen hat und den Zugriff auf das Konto autorisiert hat.
* Diese Migration verwendet Laravel-Komponenten wie Blueprint und Schema, um die Spalten hinzuzufügen und zu entfernen.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Die Datei `database/migrations/2026_03_30_100000_create_contacts_table.php` enthält eine Migration für die Erstellung einer Tabelle namens "Contacts". Die Migration verwendet die Laravel-Klasse `Migration` und implementiert das Verhalten des Up- und Down-Vorgangs durch die Methoden `up()` und `down()`, die für die Ausführung der Migration verantwortlich sind.

In der Methode `up()` wird die Tabelle "Contacts" erstellt, indem ein Schema mit dem Namen "contacts" definiert wird. Dieses Schema enthält die Spalten "id", "name", "company", "email", "project_type", "message", "timeline" und "created_at" sowie "updated_at". Die Spalte "id" ist die Primärschlüsselspalte, die alle Zeilen in der Tabelle eindeutig identifiziert.

In der Methode `down()` wird die Tabelle "Contacts" gelöscht. Dies dient als Sicherheitsmaßnahme und hilft dabei, den Zustand des Datenbankschemas zu korrigieren, wenn eine Migration fehlschlägt oder nicht ordnungsgemäß angewendet wird.

Die Datei importiert auch die Laravel-Klasse `Blueprint`, um das Schema der Tabelle "Contacts" definieren zu können. Die Klasse `Schema` wird von Laravel bereitgestellt und dient als Abstraktion für die verschiedenen Datenbanken, die von Laravel unterstützt werden.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Die Datei database/migrations/2026_03_30_100001_create_page_views_table.php ist eine Migration, die eine neue Tabelle mit dem Namen "page_views" erstellt. Die Tabelle hat eine ID-Spalte, eine visits-Spalte und Zeitstempel-Spalten für die Erstellung und Aktualisierung der Tabelle.

Die Migration verwendet die Klasse `Illuminate\Database\Migrations\Migration` von Laravel und implementiert die Methoden `up()` und `down()`, um die Tabelle zu erstellen und zu löschen, wenn sie erforderlich ist.

Die Tabelle wird mit einer ID-Spalte versehen, die eine eindeutige Identifikationsnummer für jede Zeile in der Tabelle enthält. Die visits-Spalte speichert die Anzahl der Besuche pro Seite und die Zeitstempel-Spalten werden verwendet, um den Zeitpunkt der Erstellung und Aktualisierung der Tabelle zu verfolgen.

Die Migration importiert das `Illuminate\Support\Facades\Schema` Facade, um Schemata in der Datenbank zu erstellen und zu löschen. Es importiert auch das `Illuminate\Database\Schema\Blueprint` Objekt, um die Definition der Tabelle zu erstellen und zu bearbeiten.

Die Migration ist Teil der Laravel-Umgebung und wird verwendet, um die Datenbankstruktur zu verwalten und zu ändern. Es ist wichtig, dass die Migration ordnungsgemäß ausgeführt wird, um sicherzustellen, dass die Tabelle korrekt erstellt wird und dass die Daten richtig gespeichert werden.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Die Datei `database/migrations/2026_03_31_000001_create_permission_tables.php` enthält eine Laravel-Migration, die die Tabelle für Berechtigungen und Rollen erstellt.

Die Klasse, die diese Migration implementiert, ist `Illuminate\Database\Migrations\Migration`, die von der Klasse `Illuminate\Support\Facades\Schema` geerbt wird.

Im Methodenrumpf der Klasse werden verschiedene Schema-Methoden aufgerufen, um die Tabelle für Berechtigungen und Rollen zu erstellen:

* `create()`: Hier wird die Tabelle für Berechtigungen erstellt. Die Tabelle hat eine ID-Spalte (`id`), einen Namen (`name`) und einen Guard-Namen (`guard_name`). Es gibt auch ein eindeutiges Index für den Namen und den Guard-Namen, sowie Timestamps für die Erstellung und Aktualisierung der Datensätze.
* `create()`: Hier wird die Tabelle für Rollen erstellt. Die Tabelle hat eine ID-Spalte (`id`), einen Namen (`name`) und einen Guard-Namen (`guard_name`). Es gibt auch ein eindeutiges Index für den Namen und den Guard-Namen, sowie Timestamps für die Erstellung und Aktualisierung der Datensätze.
* `create()`: Hier wird die Tabelle für Modelle mit Berechtigungen erstellt. Die Tabelle hat eine ID-Spalte (`id`), einen Model-Typ (`model_type`), eine Morph-Schlüsselspalte (`model_morph_key`) und ein Index für die Modell-ID und den Model-Typ. Es gibt auch eine Fremdschlüsse für die Berechtigung-Tabelle und ein Primary Key für den Datensatz.
* `create()`: Hier wird die Tabelle für Modelle mit Rollen erstellt. Die Tabelle hat eine ID-Spalte (`id`), einen Model-Typ (`model_type`), eine Morph-Schlüsselspalte (`model_morph_key`) und ein Index für die Modell-ID und den Model-Typ. Es gibt auch eine Fremdschlüsse für die Rolle-Tabelle und ein Primary Key für den Datensatz.
* `create()`: Hier wird die Tabelle für Beziehungen zwischen Berechtigungen und Rollen erstellt. Die Tabelle hat eine ID-Spalte (`id`), eine Berechtigung-ID (`permission_id`) und eine Rolle-ID (`role_id`). Es gibt auch Fremdschlüsse für die Berechtigung-Tabelle und die Rolle-Tabelle, sowie ein Primary Key für den Datensatz.

Die Methode `down()` wird verwendet, um die Tabelle zu entfernen, wenn die Migration rückgängig gemacht werden muss. Sie ruft `dropIfExists()` auf, um die Tabelle zu entfernen, wenn sie existiert.

Es ist wichtig zu beachten, dass diese Migration den Inhalt der Konfigurationsdatei `config/permission.php` verwendet, um die Tabellen-Namen und -Spaltennamen anzupassen. Daher muss die Konfigurationsdatei vorher geladen werden, bevor diese Migration ausgeführt wird.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
File: database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Category: data_access

This file is a migration class that adds three new columns to the `page_views` table in the database. The columns are `path`, `ip_anonymous`, and `user_agent`. These columns are added to the `page_views` table to store additional information about the page views, such as the path of the viewed page, the IP address of the anonymous user, and the user agent string (browser type and version).

The migration class extends the `Migration` class provided by Laravel's built-in migration system. The `up()` method is called when running the migration to add the new columns to the table, while the `down()` method is called when rolling back the migration to remove the added columns.

The migration uses the `Schema` facade to interact with the database schema and adds the new columns to the `page_views` table using the `table()` method. The `after()` method is used to specify the position of the new columns in the table, and the `default()` and `nullable()` methods are used to set default values and make the columns nullable, respectively.

This migration file does not import any external dependencies or use any specific Laravel features, it's a basic migration class that adds new columns to an existing database table.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Die Datei `database/migrations/2026_03_31_000003_create_consents_table.php` enthält eine Datenbankmigration, die eine neue Tabelle namens `consents` erstellt. Die Tabelle hat folgende Spalten:

* `id`: Eine eindeutige ID für jede Einwilligung.
* `user_id`: Die ID des Benutzers, der die Einwilligung erteilt hat (optional).
* `ip_anonymous`: Der anonyme IP-Adresse des Benutzers (optional).
* `consent_categories`: Eine JSON-Zeichenfolge mit den Kategorien der Einwilligung.
* `consented_at`: Das Datum und die Uhrzeit, zu denen die Einwilligung erteilt wurde.
* `revoked_at`: Das Datum und die Uhrzeit, zu denen die Einwilligung widerrufen wurde (optional).
* `created_at`: Das Datum und die Uhrzeit, zu der die Einwilligung erstellt wurde.
* `updated_at`: Das Datum und die Uhrzeit, zu dem die Einwilligung aktualisiert wurde.

Die Migration verwendet die Laravel-Migration-Klasse `Migration` und hat einen `up`- und einen `down`-Methodenblock, um die Tabelle zu erstellen und zu löschen. Der `up`-Block erstellt die Tabelle mit den obigen Spalten und Indizes, während der `down`-Block die Tabelle wieder löscht.

Die Migration importiert auch die Laravel-Klasse `Blueprint`, um eine schreibgeschützte Definition der Tabelle zu erstellen, und verwendet sie in der `create`-Methode, um die Tabelle zu erstellen. Die Migration verwendete auch die Facade `Schema` um die Schema-Definition der Tabelle zu erstellen und zu bearbeiten.

Die Migration hat keine externen Abhängigkeiten und ist nicht von anderen Dateien oder Bibliotheken abhängig.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Die Datei `database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php` enthält eine Reihe von Klassennamen und Funktionen für die Datenbanksicht "Recherche".

Die wichtigsten Klassen in dieser Datei sind:

* `Migration`: Eine Klasse zur Verarbeitung von Datenbankmigrationen.
* `Illuminate\Database\Schema\Blueprint`: Eine Klasse, um Tabellendefinitionen zu erstellen und zu ändern.
* `Illuminate\Support\Facades\DB`: Eine Klasse, um auf die Datenbank-Konfiguration zuzugreifen.
* `Illuminate\Support\Facades\Schema`: Eine Klasse, um auf die Schema-Definitionen der Tabelle zuzugreifen.

Die wichtigsten Funktionen in dieser Datei sind:

* `up()`: Eine Methode, um die Migration auszuführen und die Recherche-Tabellen in der Datenbank zu erstellen oder zu ändern.
* `down()`: Eine Methode, um die Rollback-Operation für die Migration durchzuführen und die Recherche-Tabellen in der Datenbank zu entfernen.

Die Datei enthält auch eine Reihe von Tabellendefinitionen für die Recherche-Tabellen, darunter `users`, `projekte`, `phasen`, `p5_treffer`, `p1_strukturmodell_wahl`, `p1_komponenten`, `p1_kriterien`, `p1_warnsignale`, `p2_review_typ_entscheidung`, `p2_cluster`, `p2_mapping_suchstring_komponenten`, `p2_trefferlisten`, `p3_disziplinen` und `p3_datenbankmatrix`.

Die Datei enthält auch einen Import von verschiedenen Klassen und Funktionen aus der Laravel-Bibliothek, darunter `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\DB` und `Illuminate\Support\Facades\Schema`.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei `2026_03_31_100000_create_recherche_extensions_and_enums.php` enthält eine Migration für eine PostgreSQL-Datenbank, die einige Erweiterungen und Enumerationen hinzufügt.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `Illuminate\Database\Migrations\Migration`: Die Migrationsklasse ist von der Laravel-Bibliothek importiert und wird verwendet, um die Erweiterungen und Enumerationen zu erstellen.
* `Illuminate\Support\Facades\DB`: Die DB-Klasse ist ebenfalls von der Laravel-Bibliothek importiert und wird verwendet, um SQL-Anweisungen auszuführen.
* `return new class extends Migration`: Diese Zeile erstellt eine neue Klasse, die von der `Migration`-Klasse abgeleitet wird.
* `if (DB::getDriverName() !== 'pgsql') { return; }`: Diese Bedingung überprüft, ob die verwendete Datenbank-Engine PostgreSQL ist. Wenn nicht, wird das Skript beendet.
* `DB::statement('CREATE EXTENSION IF NOT EXISTS "uuid-ossp"');`: Diese Anweisung erstellt eine Erweiterung namens "uuid-ossp", falls sie noch nicht existiert.
* `DB::statement('CREATE EXTENSION IF NOT EXISTS "pg_trgm"');`: Diese Anweisung erstellt eine Erweiterung namens "pg_trgm", falls sie noch nicht existiert.
* `$enums = [...];`: Hier werden die Enumerationen definiert, die hinzugefügt werden sollen.
* `foreach ($enums as $name => $values) { ... }`: Diese Schleife durchläuft die Enumerationen und fügt sie ein.
* `if (! $exists) { DB::statement("CREATE TYPE {$name} AS ENUM ({$values})"); }`: Wenn die Enumeration noch nicht existiert, wird sie erstellt.
* `foreach ($types as $type) { ... }`: Diese Schleife durchläuft die Types und fügt sie ein.
* `DB::statement("DROP TYPE IF EXISTS {$type} CASCADE");`: Diese Anweisung löscht die Types, falls sie noch vorhanden sind.

Insgesamt erstellen diese beiden Funktionen Enumerationen und Erweiterungen in der PostgreSQL-Datenbank, um einige benötigte Felder zu unterstützen.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
This file contains a migration for the Laravel framework, which creates two tables in the database: `projekte` and `phasen`. The `projekte` table stores information about projects, while the `phasen` table stores information about project phases.

The migration uses the `Illuminate\Database\Migrations\Migration` class to create a new migration instance. The `up` method creates two tables in the database, and the `down` method drops them if they exist.

In the `up` method, the first conditional statement checks whether the driver used by Laravel is PostgreSQL. If it is not, then the rest of the code in the method is skipped. The next block of code creates the `projekte` table using a Blueprint instance. It defines the columns for the table, including a primary key, foreign key, and unique index.

The second conditional statement checks whether the database already has a table named `phasen`. If it does not, then the method creates that table using another Blueprint instance. The table has similar column definitions as the `projekte` table, but with an additional column for the project phase status.

Finally, the two `ALTER TABLE` statements in the `up` method add a new column to both tables: `review_typ` in the `projekte` table and `phase_status` in the `phasen` table. These columns are used to store additional information about the projects and phases.

In the `down` method, the code checks whether the driver used by Laravel is PostgreSQL again. If it is not, then the rest of the code in the method is skipped. The next block of code drops both tables from the database if they exist.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
This is a Laravel migration file that creates several tables for a project management application. The tables are named `p1_warnsignale`, `p1_kriterien`, `p1_komponenten`, and `p1_strukturmodell_wahl`. The `projekte` table already exists in the database, so the migration only creates new tables if they do not exist.

The file imports several classes from Laravel's `Schema` namespace, including `Blueprint` to define the structure of each table, and `Migration` to create the up/down methods for the migration. The migration also uses several database functions, such as `uuid_generate_v4()`, to generate unique IDs for each row in the tables.

The `up()` method creates the new tables if they do not exist, and adds foreign keys between the tables. The `down()` method drops the tables if they exist.

The migration does not include any external dependencies or imports from other files. It is intended to be run as part of a Laravel project's database migration process.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
The file database/migrations/2026_03_31_100003_create_p2_tables.php is a migration script for Laravel that creates several tables in the database for the P2 system. The script uses the Illuminate\Database\Migrations\Migration class and its up() and down() methods to perform the table creation and dropping operations respectively.

The important classes, functions, or endpoints are:

* The Migration class: This is a Laravel base class that provides a structure for creating and managing database migrations. It contains the up() and down() methods, which are used to perform the migration operations respectively.
* The Blueprint class: This is a Laravel class that is used to define the structure of a table in the database. It is used in the migration script to create the tables with the desired columns and constraints.
* The DB facade: This is a Laravel facade that provides an interface for interacting with the underlying database. It is used in the migration script to perform various operations such as creating, dropping, and modifying tables.
* The Schema class: This is a Laravel class that provides an interface for working with the database schema. It is used in the migration script to create and modify tables, as well as to perform other schema-related operations.
* The Projekt class: This is a custom class that represents a P2 project. It contains properties such as the project ID, name, and description, which are used in the migration script to create and populate tables related to projects.
* The Trefferliste class: This is a custom class that represents a P2 search result list. It contains properties such as the search string, total hits, and estimation, which are used in the migration script to create and populate tables related to search results.
* The Cluster class: This is a custom class that represents a P2 cluster. It contains properties such as the cluster ID, label, description, and relevance, which are used in the migration script to create and populate tables related to clusters.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
The code in this file is a Laravel migration for creating the following tables:

* `p3_disziplinen`
* `p3_datenbankmatrix`
* `p3_geografische_filter`
* `p3_graue_literatur`

These tables are used to store data related to the project "P3" (Planetary Planning III) and its components. The migration creates the tables if they do not already exist, and it also adds foreign key constraints to ensure referential integrity between the tables.

The `up()` method of the migration class is responsible for creating the tables and their columns. It uses Laravel's schema builder to create the tables with the appropriate column definitions. The `down()` method is used to drop the tables if they are already created, which is necessary because this migration can be run multiple times as part of a development cycle.

The code also includes some checks for database drivers and schema compatibility, which are not strictly necessary but are included to ensure that the migration can be run on different databases and versions of Laravel without issues.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
The code in this file creates three tables for the "Projekt 4" project: `p4_suchstrings`, `p4_thesaurus_mapping`, and `p4_anpassungsprotokoll`.

`p4_suchstrings` is a table that stores information about search strings for the Projekt 4 project. It has columns for an ID, a reference to the associated project ID, the database name, the search string itself, and other metadata such as the number of hits, the field constraints, and the version.

`p4_thesaurus_mapping` is another table that stores information about thesauri used in the Projekt 4 project. It has columns for an ID, a reference to the associated project ID, and fields for free text, MESH terms, EMTREE terms, PsychInfo terms, and an annotation field.

`p4_anpassungsprotokoll` is a table that stores information about changes made to search strings in the Projekt 4 project. It has columns for an ID, a reference to the associated search string ID, the version of the search string, a date, a description of the change, the reason for the change, the number of hits before and after the change, and a decision on the change.

This code also includes foreign key constraints that ensure referential integrity between these tables.

Overall, this file creates three tables to support the data access needs of the Projekt 4 project and provides a way to manage search strings and thesauri.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
The provided file is a migration for a Laravel project, specifically for the `p5` database. It creates several tables:

* `p5_prisma_zahlen`: This table contains various numbers related to the P5 screening process.
* `p5_screening_kriterien`: This table contains criteria used in the P5 screening process, including the level and type of the criterion (e.g., "graue Literatur" or "Datenbank-Treffer").
* `p5_treffer`: This table contains central hits from various sources, such as literature databases or online journals.
* `p5_screening_entscheidungen`: This table contains screening decisions made by the reviewers, including the level of the decision (e.g., "accept" or "reject") and the reason for the decision (if any).
* `p5_tool_entscheidung`: This table contains tool recommendations made by the reviewer, including the tool name and a brief justification.

The migration also adds foreign keys to ensure data consistency between tables. Additionally, it uses UUIDs as primary keys throughout the schema.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
This is a PHP file that contains a migration for the Laravel framework. The migration creates two new tables in the database: `p6_qualitaetsbewertung` and `p6_luckenanalyse`.

The `p6_qualitaetsbewertung` table has columns for an ID, a reference to the `p5_treffer` table, and several other columns that describe the quality assessment of a project's deliverables. The `studientyp` column is a PostgreSQL-specific data type called an "ENUM" that represents a set of predefined values (in this case, the student types).

The `p6_luckenanalyse` table has columns for an ID, a reference to the `projekte` table, and several other columns that describe the analysis of a project's quality. The `fehlender_aspekt`, `fehlender_studientyp`, and `moegliche_konsequenz` columns contain text data, while the `empfehlung` column can contain null or text data.

The migration also includes a statement to create foreign key constraints between the two tables. The `p6_qualitaetsbewertung` table has a foreign key constraint on the `treffer_id` column that references the `id` column of the `p5_treffer` table, and the `p6_luckenanalyse` table has a foreign key constraint on the `projekt_id` column that references the `id` column of the `projekte` table.

The migration also includes a statement to drop the tables if they already exist in the database.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
This file contains the migration for creating new tables in the database. It uses the Laravel framework's schema builder to create the tables, and it includes foreign key constraints to ensure data consistency.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
This migration file contains the creation of several tables: p8_suchprotokoll, p8_limitationen, p8_reproduzierbarkeitspruefung, and p8_update_plan. These tables are used to store information about search protocols, limitations, reproducibility checks, and update plans for the P4 project.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Die Datei `2026_03_31_100010_create_recherche_indices.php` enthält eine Migration für eine PostgreSQL-Datenbank, die verschiedene Indizes erstellt, um die Suche und Verarbeitung von Daten in der Datenbank zu verbessern.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Migration`: Diese Klasse ist ein generisches PHP-Objekt, das für die Ausführung von SQL-Statements auf einer Datenbank verwendet wird. In diesem Fall wird sie verwendet, um Indizes zu erstellen und zu löschen.
* `up()`: This function is called when the migration is executed and is responsible for creating the indices in the database. It checks if the driver used by the application is PostgreSQL, and if it is, it creates the necessary indices for the search functionality.
* `down()`: This function is called when the migration is rolled back and is responsible for dropping the indices that were created in the `up()` function.

The external dependencies in this file are:

* `Illuminate\Database\Migrations\Migration`: This is a Laravel package that provides a way to create and manage migrations in a database.
* `Illuminate\Support\Facades\DB`: This is a Laravel facade that provides an easy-to-use interface for interacting with the database. It is used in this file to execute SQL statements and to create the necessary indices for the search functionality.

Overall, this file contains code that is responsible for creating and managing indices in a PostgreSQL database, which is necessary for the search functionality of the application.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
This is a PHP migration file for Laravel that creates a server-side SQL function called `berechne_prisma_zahlen` in PostgreSQL. The function takes a UUID as an input parameter and returns several PRISMA-related statistics about the number of identified hits, duplicates, and excluded hits based on the input project ID.

Here is a breakdown of the main parts of the code:

* `use Illuminate\Database\Migrations\Migration;` This line imports the `Migration` class from Laravel's `Illuminate\Database\Migrations` namespace, which is needed to define database migrations.
* `use Illuminate\Support\Facades\DB;` This line imports the `DB` facade from Laravel, which provides an easy-to-use interface for interacting with the database in a migration.
* `return new class extends Migration {` This line defines a new class that extends the `Migration` class and returns an instance of it.
* `public function up(): void {` This method is called when running the migration in the "up" direction, which means it will create the new database object if it does not already exist.
* `if (DB::getDriverName() !== 'pgsql') { return; }` This line checks whether the current driver name is PostgreSQL (`'pgsql'`). If it is not, the function returns immediately without doing anything further.
* `DB::statement("CREATE OR REPLACE FUNCTION berechne_prisma_zahlen(p_projekt_id UUID) RETURNS TABLE (...);");` This line creates a new SQL function called `berechne_prisma_zahlen` that takes a UUID as an input parameter and returns several statistics about the number of identified hits, duplicates, and excluded hits based on the input project ID. The function is defined in a multi-line string literal using the `\$\$` syntax.
* `public function down(): void {` This method is called when running the migration in the "down" direction, which means it will drop the new database object if it already exists.
* `if (DB::getDriverName() !== 'pgsql') { return; }` This line checks whether the current driver name is PostgreSQL (`'pgsql'`). If it is not, the function returns immediately without doing anything further.
* `DB::statement('DROP FUNCTION IF EXISTS berechne_prisma_zahlen(UUID);');` This line drops the `berechne_prisma_zahlen` SQL function if it already exists. The `IF EXISTS` clause is used to prevent an error from being thrown if the function does not exist.

In summary, this migration file creates a server-side SQL function called `berechne_prisma_zahlen` in PostgreSQL that returns several PRISMA-related statistics about the number of identified hits, duplicates, and excluded hits based on an input project ID.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Die oben angegebene Datei `database/migrations/2026_03_31_200000_create_chat_messages_table.php` enthält eine Laravel-Migration, die einen neuen Endpunkt namens `chat_messages` in der Datenbank erstellt.

Die Migration importiert die Klasse `Illuminate\Database\Migrations\Migration`, die das Basisklassenelement für alle Laravel-Migrationen ist. Die Klasse `Illuminate\Support\Facades\DB` wird verwendet, um den aktuellen Datenbanktreiber und die Methoden zur Erstelung von Tabellen und Einschränkungen zu erkennen.

Die Migration beginnt mit der Definition eines neuen Endpunkts namens `chat_messages`, der eine UUID-Spalte (`id`) als Primärschlüssel hat, die durch die `uuid_generate_v4()`-Funktion generiert wird. Sie enthält auch eine Fremdschlüssel-Einschränkung auf den `users`-Endpunkt (`user_id`), die einen cascading delete verursacht, wenn der zugehörige Benutzer gelöscht wird.

Die Migration erstelle auch eine `content`-Spalte vom Typ "Text" und eine `created_at`-Spalte vom Typ "Timestamp with time zone", die den Zeitpunkt der Erstellung des Datensatzes speichert.

Die Migration verwendet außerdem die `DB::getDriverName()`-Methode, um zu überprüfen, ob der aktuelle Datenbanktreiber `pgsql` ist. Wenn dies der Fall ist, wird eine zusätzliche Einschränkung namens `chat_messages_role_check` hinzugefügt, die sicherstellt, dass die Werte in der `role`-Spalte nur aus dem Set {'user', 'assistant'} stammen.

Insgesamt erstellt die Migration einen neuen Endpunkt namens `chat_messages`, der eine UUID-Spalte und eine Fremdschlüssel-Einschränkung auf den `users`-Endpunkt enthält, sowie einige weitere Spalten für die Speicherung von Textdaten und Erstellungszeit.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Die Datei `database/migrations/2026_03_31_200001_create_webhooks_table.php` enthält eine PHP-Klasse, die eine Datenbankmigration für die Erstellung einer Tabelle mit dem Namen "Webhooks" in der PostgreSQL-Datenbank durchführt.

Die Klasse erbt von `Illuminate\Database\Migrations\Migration`, was bedeutet, dass sie sich an das Laravel-Framework bindet und somit die Möglichkeit bietet, Datenbankmigrationen über eine einfache Schnittstelle auszuführen.

Im Methodenkörper der Klasse werden folgende Aktionen durchgeführt:

1. Überprüfung der Datenbanktreiber-Name auf "pgsql"
2. Erstellen einer neuen Tabelle namens "Webhooks" mit einem eindeutigen Identifikator (UUID) als Primärschlüssel, einem Fremdschlüssel zu den Benutzerdaten (Foreign Key), dem Namen und der URL des Webhooks, einer Text-Spalte für die Frontend-Objektbezeichnung sowie einer Timestamp-Spalte für das Erstellungsdatum.
3. Hinzufügen eines Fremdschlüssels zu der Tabelle "Chat_Messages", um eine Beziehung zwischen den beiden Tabellen aufzubauen (eine Webhook-ID kann in der Tabelle "Chat_Messages" verwendet werden).
4. Wenn die Migration rückgängig gemacht wird, wird der Fremdschlüssel und die Spalte mit der Webhook-ID aus der Tabelle "Chat_Messages" gelöscht sowie die Tabelle "Webhooks" gelöscht.

Insgesamt hat diese Migration das Ziel, eine neue Tabelle namens "Webhooks" in der PostgreSQL-Datenbank zu erstellen und dieselbe mit einer Beziehung zwischen den Tabellen "Webhooks" und "Chat_Messages" aufzubauen.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
This file is a migration for the Laravel framework, specifically for creating a new table called "paper_embeddings" in the database. The table will have the following columns:

* `id`: A UUID primary key.
* `projekt_id`: A UUID foreign key referencing the id column of the projects table. This field is nullable, meaning that it can be empty or have a value.
* `source`: A string representing the source of the paper embedding (e.g., "arXiv").
* `paper_id`: A string representing the unique identifier of the paper in the source system (e.g., "123456789").
* `title`: A text field containing the title of the paper.
* `chunk_index`: An integer representing the index of the text chunk in the paper (e.g., 0, 1, 2, etc.).
* `text_chunk`: A text field containing the actual text of the paper chunk.
* `metadata`: A JSONB field containing metadata about the paper, such as its authors, publication date, and subject area. This field is nullable.
* `erstellt_am`: A timestamp indicating when the paper embedding was created.

The migration also includes foreign key constraints for the projekt_id field, as well as indexes on the source and paper_id fields.

This migration is specifically designed for a PostgreSQL database, which requires the use of vector data types and certain query optimizations to work efficiently with the "vector" extension. The "CREATE INDEX paper_embeddings_embedding_idx ON paper_embeddings USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100)" statement creates an index on the "embedding" column of the paper_embeddings table, which is a vector data type. This index allows for efficient search and retrieval of paper embeddings based on their similarity to other vectors in the database.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Die Datei `database/migrations/2026_04_01_090000_create_activity_log_table.php` in Laravel ist eine Datenbankmigration, die ein neues Tabellenmodell namens "activity_log" erstellt.

Die Klasse `return new class extends Migration` erbt von der Klasse `Migration` und implementiert die Methoden `up()` und `down()`, um die Tabelle zu erstellen und wieder zu entfernen.

In der Methode `up()` wird das Tabellenschema definiert, indem es einen Blueprint-Builder verwendet, um eine neue Tabelle namens "activity_log" zu erstellen. Der Blueprint-Builder enthält die folgenden Spalten:

* `id`: Eine ID für jede Zeile in der Tabelle (immer erforderlich)
* `log_name`: Ein Name für den Log, der verwendet werden kann, um unterschiedliche Logs zu unterscheiden (optional)
* `description`: Eine kurze Beschreibung des Ereignisses (optional)
* `subject_id`: Die ID des Objektes, das das Ereignis ausgelöst hat (optional)
* `subject_type`: Der Typ des Objektes, das das Ereignis ausgelöst hat (optional)
* `event`: Der Name des Ereignisses (optional)
* `causer_id`: Die ID des Benutzers, der das Ereignis ausgelöst hat (optional)
* `causer_type`: Der Typ des Benutzers, der das Ereignis ausgelöst hat (optional)
* `attribute_changes`: Eine JSON-Darstellung der Änderungen an den Attributen (optional)
* `properties`: Eine JSON-Darstellung von zusätzlichen Eigenschaften (optional)
* `created_at` und `updated_at`: Zeitstempel für die Erstellungs- und Aktualisierungszeiten der Zeilen (immer erforderlich)

In der Methode `down()` wird die Tabelle gelöscht, wenn sie existiert.

Diese Datei definiert ein Tabellenmodell namens "activity_log", das es ermöglicht, Aktivitäten und Ereignisse in einer Datenbank zu protokollieren. Es unterstützt verschiedene Funktionen wie die Möglichkeit, mehrere Benutzer oder Objekte zu verknüpfen, um den Kontext eines Ereignisses zu definieren.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
This file is a migration script for Laravel that adds a new column to the `users` table in the database. The column will be called `status` and will have an enum data type with three possible values: `trial`, `active`, and `suspended`. The default value for this column will be `trial`.

The migration script uses the `Illuminate\Database\Migrations\Migration` class to define a new class that extends the `Migration` class. This class contains two methods: `up()` and `down()`. The `up()` method is called when running the migration, and it will add the new column to the `users` table. The `down()` method is called when rolling back the migration, and it will remove the new column from the `users` table.

The script also uses the `Illuminate\Database\Schema\Blueprint` class to define a blueprint for the `users` table. This allows us to create the new column with the desired data type and default value.

In addition to the `up()` and `down()` methods, this migration script also includes some PostgreSQL-specific code that is only executed if we are using PostgreSQL as our database driver. This code creates a new enum type called `user_status` and sets its possible values to `trial`, `active`, and `suspended`. It then adds the new column to the `users` table, with an ENUM data type that uses this newly created type.

Finally, the script includes some SQLite-specific code that is only executed if we are using SQLite as our database driver. This code creates a new enum type called `user_status` and sets its possible values to `trial`, `active`, and `suspended`. It then adds the new column to the `users` table, with an ENUM data type that uses this newly created type.

In summary, this migration script will add a new column called `status` to the `users` table in the database, with an enum data type and default value of `trial`. The script also includes some PostgreSQL-specific code to create a new enum type called `user_status`, as well as some SQLite-specific code to create a similar enum type.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
This file is a migration for the Laravel framework, which adds a new column to the `webhooks` table in the database. The column is called `frontend_object`, and it is of type `webhook_frontend_object`. This type is defined as an enumeration (ENUM) with two values: `'dashboard_chat'` and `'recherche_start'`.

The migration also creates a unique constraint on the columns `user_id` and `frontend_object` in the `webhooks` table, which ensures that there can only be one webhook for each user and object type.

The migration is written in PHP using the Laravel framework's syntax and functions. It uses the `DB::statement()` function to execute SQL queries directly on the database, and the `Schema::hasColumn()` function to check if a column already exists before attempting to add it.

This migration is specific to the PostgreSQL database, as it uses PostgreSQL-specific syntax for creating an enumerated type (`CREATE TYPE`) and adding a unique constraint (`ADD CONSTRAINT`). The `IF` statements are used to prevent errors when running the migration on other databases that do not support these features.

---

### database/seeders/DatabaseSeeder.php
This file is part of a Laravel application, specifically the `DatabaseSeeder` class. This class is responsible for seeding the database with data during the development process.

The main function of this class is to call other seeders (such as the `RoleSeeder`) and create an initial user with the role of "admin". The user's email address, name, password, and email verification date are all set to a default value in the code snippet you provided.

The file also imports the `User` model from the `App\Models` namespace, which is used to interact with the users table in the database. The `WithoutModelEvents` trait is not used in this file, as it is only necessary when using Eloquent models for events.

Overall, this file is responsible for setting up the initial state of the application's data and creating an admin user to interact with the system during development.

---

### database/seeders/RechercheDemoSeeder.php
Diese Datei enthält eine Klasse `RechercheDemoSeeder`, die mit der Datenbankseedung und -ausführung für ein Projekt der klinischen Recherche beschäftigt. Sie ruft verschiedene Methode auf, um den Prozess von Datenimport bis Trefferauswertung abzuschließen.

---

### database/seeders/RoleSeeder.php
Die Datei `database/seeders/RoleSeeder.php` ist ein Seeder-Modul für das Laravel-Framework, das die Rolle "admin" und die Rolle "user" in der Web-Guard für das Permissions-System von Spatie erstellt.

Diese Datei importiert die Klasse `Spatie\Permission\Models\Role`, um die Rollenmodelle in der Web-Guard zu erstellen und zu aktualisieren. Die Funktion `run()` ruft die Methoden `firstOrCreate()` auf, um die Rollen zu erstellen oder zu aktualisieren, wenn sie noch nicht existieren.

Die Datei benötigt keinerlei externe Abhängigkeiten, da sie nur das Laravel-Framework und das Spatie-Permissions-System verwendet.

---

### database/sql/create_langdock_agent_user.sql
This SQL file creates a new PostgreSQL user named "langdock_agent" with restricted read/write access to Recherche tables only. The user is created as part of the application database and will be used by the MCP-Postgres Docker container to perform data access tasks.

The file contains several GRANT and REVOKE statements, which are used to grant or revoke privileges on specific PostgreSQL objects. Here's a breakdown of each statement:

* `CREATE ROLE`: This statement creates a new PostgreSQL role named "langdock_agent". The `LOGIN` parameter specifies that the user should be able to log in, and the `PASSWORD` parameter sets the password for the user.
* `GRANT CONNECT ON DATABASE`: This statement grants the "langdock_agent" role permission to connect to the "linn_games" database.
* `GRANT USAGE ON SCHEMA`: This statement grants the "langdock_agent" role permission to use the "public" schema.
* `GRANT SELECT, INSERT, UPDATE, DELETE ON`: These statements grant the "langdock_agent" role permission to perform various operations (SELECT, INSERT, UPDATE, DELETE) on a list of Recherche tables.
* `GRANT EXECUTE ON FUNCTION`: This statement grants the "langdock_agent" role permission to execute the `berechne_prisma_zahlen` function.
* `REVOKE ALL ON`: These statements revoke all permissions for a list of sensitive tables from the "langdock_agent" role.

Overall, this SQL file creates a new PostgreSQL user with restricted access to Recherche tables and grants permission to perform various data access tasks. The user is used by the MCP-Postgres Docker container to perform data access tasks.

---

## temp_dummy

### .github/pull_request_template.md
This file is a pull request template for the '.github' directory. It provides a framework for developers to create and manage their pull requests.

---

## tests

### tests/Pest.php
The provided code is a PestPHP test suite, specifically a `Pest.php` file that contains test cases and expectations for testing Laravel applications using the PHPUnit framework. The file is part of the PestPHP package and can be used to write and run tests for Laravel projects.

Here are the 10 most important functions in this file:

1. `pest()` function: This function is used to extend the `Tests\TestCase` class with additional functionality, such as loading the RefreshDatabase trait.
2. `expect()` function: This function provides access to a set of "expectations" methods that can be used to assert different conditions in tests.
3. `something()` function: This is a global function that can be used to reduce the number of lines of code in test files by exposing helper functions.
4. The `TestCase` class: This is the base class for all PestPHP test cases and provides a lot of functionality out-of-the-box, including support for mocking, spying, and stubbing.
5. The `RefreshDatabase` trait: This trait is used to refresh the database after each test, which can be useful for testing Laravel applications that use Eloquent models and migrations.
6. The `Expectation` class: This class provides a set of methods that can be used to assert different conditions in tests, such as `toBe()` and `toEqual()`.
7. The `TestResult` class: This class is used to represent the result of a test run, including information about any failed or passed tests.
8. The `Test` interface: This interface provides a set of methods that can be implemented by classes that want to participate in the PestPHP testing framework, such as `run()` and `getResult()`.
9. The `TestCollection` class: This class represents a collection of test cases that can be run together using the `run()` method.
10. The `TestRunner` class: This class is responsible for running tests and providing information about the results, including any failed or passed tests.

---

### tests/TestCase.php
This file is a PHP class named `TestCase`, which is an abstract class that extends the `BaseTestCase` class provided by Laravel. The `TestCase` class is used as a base class for creating test cases in the application, which allows us to write more concise and DRY tests.

The `TestCase` class does not contain any code itself, but it provides a set of methods that can be used by child classes to perform common tasks such as setting up the test environment, creating test data, and asserting the results of tests.

This file is part of the Laravel testing framework, which allows us to write automated tests for our application. By extending this class, we can create more concise and DRY tests that are easier to maintain and debug.

---

### tests/Feature/ContactTest.php
Die Datei `tests/Feature/ContactTest.php` enthält eine Reihe von Tests für die Funktionalität der Kontaktformular-Route in einer Laravel-Anwendung. Die wichtigsten Klassen und Funktionen sind:

* `App\Models\Contact`: Die Model-Klasse, die die Kontaktdaten speichert und aus der die Tests lesen.
* `use App\Models\Contact;`: Importiert die Model-Klasse, damit sie von den Tests verwendet werden kann.
* `test('contact form accepts valid submission', function () { ... })`: Der erste Test im File. Er simuliert eine gültige Kontaktanfrage und überprüft, ob der Statuscode 200 und die JSON-Daten korrekt sind.
* `->assertStatus(200)->assertJson(['success' => true, 'message' => 'Anfrage erfolgreich gesendet.']);` Überprüft den Statuscode und die JSON-Nutzlast der Antwort.
* `->postJson(route('contact.store'), [ ... ]);` Simuliert eine POST-Anfrage an die Route `/contact/store` mit den angegebenen Daten.
* `test('contact form requires name', function () { ... })`: Der zweite Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn keine Name-Daten übermittelt werden.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['name']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit leeren Namen-Daten und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.
* `test('contact form requires email', function () { ... })`: Der dritte Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn keine E-Mail-Daten übermittelt werden.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['email']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit leeren E-Mail-Daten und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.
* `test('contact form requires valid email', function () { ... })`: Der vierte Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn eine ungültige E-Mail-Adresse übermittelt wird.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['email']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit einer ungültigen E-Mail-Adresse und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.
* `test('contact form requires project_type', function () { ... })`: Der fünfte Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn keine Projekt-Typ-Daten übermittelt werden.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['project_type']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit leeren Projekt-Typ-Daten und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.
* `test('contact form requires message', function () { ... })`: Der sechste Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn keine Nachrichten-Daten übermittelt werden.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['message']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit leeren Nachrichten-Daten und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.
* `test('contact form rejects message exceeding 5000 characters', function () { ... })`: Der siebte Test im File. Überprüft, ob die Kontaktformular-Route einen Fehler gibt, wenn eine Nachrichten-Daten mit mehr als 5000 Zeichen übermittelt werden.
* `->postJson(route('contact.store'), [ ... ])->assertStatus(422)->assertJsonValidationErrors(['message']););` Simuliert eine POST-Anfrage an die Route `/contact/store` mit einer Nachrichten-Daten mit mehr als 5000 Zeichen und überprüft, ob der Statuscode 422 und die JSON-Nutzlast korrekt sind.

---

### tests/Feature/DashboardTest.php
This file contains two tests for the dashboard page. The first test checks that guests are redirected to the login page when trying to access the dashboard without being authenticated. The second test checks that authenticated users can visit the dashboard and receive a 200 status code.

---

### tests/Feature/ExampleTest.php
Tests for example features in the application. Uses the Laravel testing framework.

---

### tests/Feature/IngestPaperJobTest.php
The file `tests/Feature/IngestPaperJobTest.php` contains a set of tests for the `IngestPaperJob` class, which is responsible for ingesting paper data into the database. The test file defines several tests that verify different aspects of the job's functionality.

Here are the key functions and classes mentioned in the summary:

* `IngestPaperJob`: This is the main class under test. It contains the logic to ingest paper data into the database, including the embedding step using Ollama API.
* `Queue::fake()`: This is a method from the Laravel framework that allows you to fake the queue system during testing. By calling this method, you can avoid actually dispatching jobs and instead test the job's behavior without sending them to the queue.
* `IngestPaperJob::dispatch()`: This method is used to create an instance of the `IngestPaperJob` class and dispatch it to the queue for processing. It takes several parameters, including the paper ID, source, title, and text.
* `Queue::assertPushed()`: This method is used to verify that a job has been pushed onto the queue. You can pass a closure that will be executed with the job instance as an argument. In this test file, it is used to verify that the correct parameters were passed to the job.
* `Http::fake()`: This method is used to fake HTTP requests and responses during testing. It allows you to intercept requests made by the `IngestPaperJob` class and return pre-defined responses. In this test file, it is used to fake the Ollama API request for embedding the paper text.
* `Http::response()`: This method is used to create a response object that can be returned by the fake HTTP request handler. It takes several parameters, including the response body and status code. In this test file, it is used to return a pre-defined response from the Ollama API.
* `Log::spy()`: This method is used to intercept log messages generated during testing. By calling this method, you can inspect the log messages that would normally be written to the console and verify that they contain the expected information. In this test file, it is used to verify that an error message was logged when the Ollama API request failed.
* `expect(fn () => $job->handle())->toThrow(\Exception::class)`: This method is used to expect a thrown exception during testing. It takes a closure as an argument, which is executed and expected to throw an exception of the specified class (in this case, `\Exception`). In this test file, it is used to verify that an exception was thrown when the Ollama API request failed.
* `Http::assertSentCount()`: This method is used to verify that a certain number of HTTP requests were sent during testing. It takes two arguments: the expected count and the optional URL matcher. In this test file, it is used to verify that the correct number of Ollama API requests were sent for each test case.

---

### tests/Feature/LegalPagesTest.php
Die Datei `tests/Feature/LegalPagesTest.php` enthält Tests für die Legal-Seiten der Webanwendung. Die Tests verifizieren, ob die Seiten ordnungsgemäß gerendert werden können und ob sie den erwarteten Inhalt enthalten.

Die wichtigsten Funktionen in dieser Datei sind:

* `test('impressum page can be rendered', function () { ... })`: Testet, ob die Impressum-Seite korrekt gerendert werden kann und ob sie den Namen und die Adresse des Betreibers enthält.
* `test('dsgvo page can be rendered', function () { ... })`: Testet, ob die Datenschutz-Grundverordnung (DSGVO) kann gerendert werden und ob sie den Inhalt der DSGVO enthält.
* `test('agb page can be rendered', function () { ... })`: Testet, ob die Allgemeine Geschäftsbedingungen (AGB) können gerendert werden und ob sie den Inhalt der AGB enthalten.

Die Tests sind in PHP geschrieben und verwenden Laravel's Test-Facility. Sie importieren die Route-Funktion, um die Routen für die Legal-Seiten zu definieren. Die Tests nutzen auch Laravel's HTTP-Client, um GET-Anfragen an die Routen zu senden und das Ergebnis zu überprüfen.

---

### tests/Feature/PaperRagTest.php
Die Datei enthält Tests für eine Webanwendung, die einen API-Endpunkt für den Papier-Ingest und einen anderen Endpunkt für die RAG-Suche (Risk Assessment Grid) bereitstellt.

* Die erste Kategorie der Tests ist `tests/Feature`, die die Feature-Tests enthält. Hierbei handelt es sich um Tests, die sich auf das Verhalten des Endpunktes für den Papier-Ingest konzentrieren und die Funktionalität von der Anwendung überprüfen.
* Die nächste Kategorie ist `tests/Unit`, die die Einheitentests enthält. Hierbei handelt es sich um Tests, die sich auf die Implementierung der Klassen und Methoden der Anwendung konzentrieren und das Verhalten von einzelnen Komponenten überprüfen.
* Die dritte Kategorie ist `tests/Integration`, die die IntegrationsTests enthält. Hierbei handelt es sich um Tests, die sich auf das Zusammenwirken von verschiedenen Komponenten der Anwendung konzentrieren und das Verhalten des Systems als Ganzes überprüfen.

Die wichtigsten Klassen in dieser Datei sind `PaperRagTest`, `IngestPaperJob`, `RagSearch`, `EmbeddingService` und `Ollama`. Die letztere ist eine Abhängigkeit, die von der Anwendung verwendet wird, um die Embeddings für den RAG-Suchalgorithmus zu erhalten.

Die wichtigsten Funktionen in dieser Datei sind:

* Tests für den Ingest-Endpunkt, um sicherzustellen, dass die Anfrage mit einem gültigen MCP-Token und Daten übermittelt wird.
* Tests für die RAG-Suche, um sicherzustellen, dass die Anfrage mit einem gültigen MCP-Token und einer Abfragezeichenkette übermittelt wird, sowie dass der Suchalgorithmus ordnungsgemäß funktioniert.
* Hilfsfunktionen, wie `mcpHeaders()`, um den Bearer-Token für den MCP-Service zu erstellen und die Anfrage mit diesem Token zu versehen.

Die wichtigsten Endpunkte in dieser Datei sind:

* `/api/papers/ingest`: Der Ingest-Endpunkt, der verwendet wird, um Papierdaten von einer externen Quelle einzusammeln und in die Anwendung aufzunehmen.
* `/api/papers/rag-search`: Der RAG-Suche-Endpunkt, der verwendet wird, um Papierdaten mithilfe des RAG-Suchalgorithmus zu durchsuchen.

---

### tests/Feature/ProjektPolicyTest.php
This is a set of tests for the `ProjektPolicy` class in Laravel. The policy allows certain actions to be taken on a project based on the user's ownership and permissions. The tests cover the following scenarios:

* A user can view their own project.
* A non-owner cannot view another user's project.
* An owner can update their own project.
* A non-owner cannot update another user's project.
* An owner can delete their own project.
* A non-owner cannot delete another user's project.
* Any authenticated user can create a new project.

The tests use the `Projekt` and `User` models to represent projects and users, respectively. The `ProjektPolicy` class is tested using the `expect()` method provided by Laravel's testing framework. The tests assert that certain conditions are met for each scenario.

---

### tests/Feature/RechercheTest.php
This file contains a set of unit tests for the `Recherche` feature in an app that allows users to create and manage research projects. The tests are written using Laravel's built-in testing tools, which provide a simple and expressive API for defining test cases.

The file defines five tests:

1. The first test checks that an authenticated user can create a new research project by filling in the required fields and clicking the "Create" button. This test uses the `actingAs()` method to simulate a logged-in user and verifies that the project is created successfully, as well as checking that the database has been updated with the correct values.
2. The second test checks that the research project creation process dispatches a job (using Laravel's queuing system) to trigger the Langdock agent (a third-party service). This test uses the `fake()` method to mock the queuing system and verifies that the job has been pushed successfully.
3. The third test checks that the research input form requires the "eingabe" field to be filled in before submitting the form. This test uses the `hasErrors()` method to check that the form validation failed, as well as checking that the correct error message is displayed.
4. The fourth test checks that the project list page only shows projects owned by the current user, while hiding those owned by other users. This test creates two users (owner and intruder) and two projects (one for each user), and then verifies that the owner sees both of their own projects on the project list page, while the intruder only sees one project.
5. The fifth test checks that a guest cannot access the recherche page, as it requires authentication to view. This test uses the `get()` method to make a request to the page and verifies that the response is a redirect to the login page.

---

### tests/Feature/WebhookTest.php
This file contains tests for the webhook functionality in a Laravel application. The tests are written using PHPUnit and are located in the `tests/Feature/WebhookTest.php` file.

The file defines several test methods, each of which tests a different aspect of the webhook functionality. These tests cover scenarios such as:

* Webhook request without signature (403 error)
* Webhook request with invalid signature (403 error)
* Webhook request without timestamp (403 error)
* Webhook request with expired timestamp (403 error)
* Valid webhook request with valid signature and timestamp (200 response)
* Webhook request with missing `eingabe` field (422 error)
* Webhook request with non-existent `projekt_id` (422 error)
* Webhook request with non-existent `user_id` (422 error)
* Webhook request with `eingabe` exceeding max length (422 error)

The tests use the Laravel testing framework to simulate HTTP requests and test the responses. They also use the `Config` facade to set the `services.langdock.secret` configuration value and the `Queue` facade to fake the queueing of jobs.

---

### tests/Feature/WelcomeTest.php
This is a PHPUnit test file for the "welcome" page of an application. The tests are written using the `test` method provided by PHPUnit, which allows you to write test cases as if they were functions. The tests include several assertions that verify different aspects of the welcome page:

* The response status code is 200 (OK).
* The visit counter for the "welcome" page is incremented correctly.
* A new "page_view" record is created if none exists, and the visit counter is set to 1.
* The response includes links to login and register pages for guests.
* The response includes a link to the dashboard page for authenticated users.

---

### tests/Feature/Auth/AuthenticationTest.php
This is a set of test cases for the authentication system in a Laravel application. The tests are written using PHPUnit, and they cover various scenarios related to logging in, authenticating, and logging out.

Here's a breakdown of each test case:

1. `test('login screen can be rendered', function () { ... })`: This test checks that the login screen can be rendered successfully. It does this by making a GET request to the `/login` route and asserting that the response has a 200 status code.
2. `test('users can authenticate using the login screen', function () { ... })`: This test checks that users can authenticate using the login screen. It creates a new user, then makes a POST request to the `/login` route with the user's email and password. It asserts that the response redirects to the dashboard page and that the user is authenticated.
3. `test('users can not authenticate with invalid password', function () { ... })`: This test checks that users cannot authenticate using an invalid password. It creates a new user, then makes a POST request to the `/login` route with the user's email and an incorrect password. It asserts that the response has errors in the email field and that the user is still not authenticated.
4. `test('users with two factor enabled are redirected to two factor challenge', function () { ... })`: This test checks that users who have two-factor authentication enabled are redirected to a two-factor challenge page when they try to log in. It first checks that the feature is enabled, then creates a new user with two-factor authentication enabled and makes a POST request to the `/login` route with the user's email and password. It asserts that the response redirects to the two-factor challenge page.
5. `test('users can logout', function () { ... })`: This test checks that users can log out successfully. It creates a new user, then makes an actingAs() request with the user's email and password. It asserts that the response redirects to the homepage and that the user is no longer authenticated.

Overall, these tests cover various scenarios related to logging in and authentication, and they provide a good starting point for testing the functionality of the authentication system in a Laravel application.

---

### tests/Feature/Auth/EmailVerificationTest.php
This is a test file for verifying the email of a user in a Laravel application. The test file contains four tests:

1. Tests whether the email verification screen can be rendered successfully.
2. Tests whether the email can be verified successfully.
3. Tests whether the email cannot be verified with an invalid hash.
4. Tests whether an already verified user visiting the verification link does not fire the event again.

The test file imports the following classes and functions:

* `App\Models\User` to represent the user model.
* `Illuminate\Auth\Events\Verified` to represent the "verified" event.
* `Illuminate\Support\Facades\Event` to interact with events in the application.
* `Illuminate\Support\Facades\URL` to generate signed URLs for the verification link.
* `Illuminate\Foundation\Testing\WithoutMiddleware` to disable middleware for the test.
* `Illuminate\Foundation\Testing\DatabaseMigrations` to reset the database after each test.
* `Illuminate\Foundation\Testing\DatabaseTransactions` to roll back the database transactions after each test.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
This file contains a test case for the password confirmation screen, which checks if it can be rendered correctly and returns HTTP status code 200.

---

### tests/Feature/Auth/PasswordResetTest.php
This file contains four test cases for the password reset functionality in a Laravel application. The tests are written using PHPUnit and the Laravel Testing Framework.

The first test case, `reset password link screen can be rendered`, verifies that the password reset link screen can be displayed to the user. This is done by sending a GET request to the route `route('password.request')` and checking the response status code.

The second test case, `reset password link can be requested`, sends a POST request to the route `route('password.request')`, which simulates the user clicking on the password reset link. The test checks that an email is sent to the user with a reset password token in it using the `Notification::fake()` method.

The third test case, `reset password screen can be rendered`, sends a GET request to the route `route('password.reset', $notification->token)`, where `$notification` is the notification object returned by the previous test case. The test checks that the reset password screen can be displayed correctly, which involves checking the response status code and the presence of certain elements in the HTML response.

The fourth test case, `password can be reset with valid token`, sends a POST request to the route `route('password.update')`, which simulates the user clicking on the "Reset password" button after entering their email address, password, and confirmation password. The test checks that the password is updated correctly by checking the response status code and the absence of certain elements in the HTML response.

The file also imports several classes and uses the `Notification::fake()` method to mock notifications for testing purposes.

---

### tests/Feature/Auth/RegistrationTest.php
Die Datei `tests/Feature/Auth/RegistrationTest.php` enthält Unit-Tests für die Registrierung von Benutzern in der Anwendung.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `FeatureTestCase`: Die Klasse, aus der die Tests ausgeführt werden. Sie importiert die `Illuminate\Foundation\Testing\TestCase`-Klasse und erweitert sie mit den benötigten Traits für die Testausführung.
* `get`: Die Funktion zum Ausführen eines GET-Requests an eine Route.
* `post`: Die Funktion zum Ausführen eines POST-Requests an eine Route.
* `assertStatus`: Die Funktion, um den HTTP-Statuscode des Requests zu überprüfen.
* `assertSessionHasNoErrors`: Die Funktion, um sicherzustellen, dass die Session keine Fehler enthält.
* `assertRedirect`: Die Funktion, um sicherzustellen, dass der Request eine Redirect-Antwort liefert.
* `assertAuthenticated`: Die Funktion, um sicherzustellen, dass der Benutzer authentifiziert wurde.

Die externe Abhängigkeit in dieser Datei ist die Verwendung von Routen und Endpunkten, um die Anfragen an die Server-Seite zu senden.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
This file contains two tests for the "Two Factor Challenge" functionality in Laravel. The first test checks that a user is redirected to the login page when attempting to access the two factor challenge endpoint without being authenticated. The second test checks that the two factor challenge view can be rendered correctly.

The file imports several classes and functions from the `App\Models` namespace, including the `User` model. It also uses the `Features` facade to check if two factor authentication is enabled on the current installation of Laravel.

Overall, this file is part of a testing suite for the "Two Factor Challenge" functionality in Laravel, and it contains tests that cover various scenarios related to accessing and using this feature.

---

### tests/Feature/Chat/BigResearchChatTest.php
Diese Datei enthält eine Testklasse für die Komponenten "Chat" in der Livewire-Anwendung. Die Klasse testet verschiedene Funktionalität von Chat, einschließlich das Senden und Empfangen von Nachrichten, das Validieren von Nachrichten als Pflichtfeld, das Löschen von Nachrichten und das Anzeigen einer leeren Liste ohne Webhook.

Die wichtigsten Klassen in dieser Datei sind:

* `User`: Die Benutzerklasse wird verwendet, um Benutzer zu erstellen und zu authentifizieren.
* `Webhook`: Die Webhook-Klasse wird verwendet, um die Webhooks für den Chat zu erstellen und zu verwalten.
* `ChatMessage`: Die Nachrichtenklasse wird verwendet, um die Nachrichten im Chat zu speichern und zu verwalten.
* `Http`: Die Http-Klasse wird verwendet, um HTTP-Anfragen für die Verbindung mit dem Server durchzuführen.
* `Volt`: Die Volt-Klasse wird verwendet, um die Livewire-Komponenten in der Testumgebung zu initialisieren und zu rendern.

Die wichtigsten Funktionen in dieser Datei sind:

* `test('chat: nachricht senden erstellt user- und assistant-message')` : Diese Methode testet die Fähigkeit des Benutzers, Nachrichten zu senden und diese für den Assistenten und den Benutzer zu speichern.
* `test('chat: validiert nachricht als pflichtfeld')` : Diese Methode testet, ob der Benutzer die Meldung als Pflichtfeld überprüft.
* `test('chat: ohne webhook wird nichts gespeichert')` : Diese Methode testet, ob der Benutzer keine Nachrichten speichert, wenn kein Webhook vorhanden ist.
* `test('chat: http-fehler erzeugt fehlermeldung')` : Diese Methode testet, ob die Komponenten eine Fehlermeldung anzeigt, wenn ein HTTP-Fehler auftritt.
* `test('chat: connection exception erzeugt fehlermeldung')` : Diese Methode testet, ob die Komponenten eine Fehlermeldung anzeigt, wenn eine Verbindungsausnahme auftritt.
* `test('chat: clearHistory löscht nur eigene nachrichten')` : Diese Methode testet, ob die Komponenten nur eigene Nachrichten löschen, wenn der Benutzer den "Clear History"-Button drückt.
* `test('chat: getChatMessages gibt nachrichten chronologisch zurueck')` : Diese Methode testet, ob die Komponenten eine Liste von Nachrichten in der richtigen Reihenfolge rendern, wenn der Benutzer den "Get Chat Messages"-Button drückt.
* `test('chat: ohne webhook zeigt leere liste')` : Diese Methode testet, ob die Komponenten eine leere Liste anzeigen, wenn kein Webhook vorhanden ist.
* `test('chat: nachricht max 10000 zeichen')` : Diese Methode testet, ob der Benutzer keine Nachrichten mit mehr als 10000 Zeichen sendet, um einen Fehler zu vermeiden.

---

### tests/Feature/Recherche/PhaseP1Test.php
This file contains tests for the P1 research phase of a project. It uses Livewire and Volt to render the components and test the functionality.

---

### tests/Feature/Recherche/PhaseP2Test.php
Diese Datei enthält Tests für die P2-Phase der Recherche. Es werden verschiedene Klassen und Endpunkte getestet, um sicherzustellen, dass sie ordnungsgemäß funktionieren.

---

### tests/Feature/Recherche/PhaseP3Test.php
This file contains tests for the 'Recherche' component in the P3 phase of a project. It uses Livewire to test the functionality of the component and asserts that the expected values are set correctly.

---

### tests/Feature/Recherche/PhaseP4Test.php
Diese Datei enthält Funktionen und Tests für die Suche in einer Datenbank (PubMed) mittels eines Suchstrings. Sie beschreibt auch eine Mapping-Funktion für Thesaurus-Termini und ein Anpassungsprotokoll für den Suchstring. Es werden verschiedene Aspekte der Suche und des Anpassungsvorgangs getestet, wie z.B. die Validierung von Eingaben, die Verwendung eines fremden Suchstrings oder die Bearbeitung und Löschung von Such- und Thesaurus-Mapping.

Die wichtigsten Klassen in dieser Datei sind:

* P4Suchstring: Modell für den Suchstring mit Informationen zur Datenbank, Version und Trefferanzahl.
* P4ThesaurusMapping: Modell für die Mapping-Funktion für Thesaurus-Termini.
* P4Anpassungsprotokoll: Modell für das Anpassungsprotokoll mit Informationen zu Änderungen am Suchstring und den daraus resultierenden Treffern.

Die wichtigsten Funktionen in dieser Datei sind:

* newSs(): Öffnet ein Formular zum Erstellen eines neuen Suchstrings.
* saveSs(): Speichert einen neu erstellten oder geänderten Suchstring.
* editSs(): Öffnet ein Formular zur Bearbeitung eines Suchstrings.
* deleteSs(): Löscht einen Suchstring.
* newTh(): Öffnet ein Formular zum Erstellen eines neuen Thesaurus-Mappings.
* saveTh(): Speichert ein neues oder geändertes Thesaurus-Mapping.
* editTh(): Öffnet ein Formular zur Bearbeitung eines Thesaurus-Mappings.
* deleteTh(): Löscht ein Thesaurus-Mapping.
* newAp(): Öffnet ein Formular zum Erstellen eines neuen Anpassungsprotokolls.
* saveAp(): Speichert ein neues oder geändertes Anpassungsprotokoll.
* editAp(): Öffnet ein Formular zur Bearbeitung eines Anpassungsprotokolls.
* deleteAp(): Löscht ein Anpassungsprotokoll.

---

### tests/Feature/Recherche/PhaseP5Test.php
Die Datei `tests/Feature/Recherche/PhaseP5Test.php` enthält PHPUnit-Tests für die Recherche-Phase 5 (P5) einer Forschungsprojektplanung im DARIAH-DE-Format. Die Klasse `App\Models\Recherche\Projekt` stellt die Projekt-Daten dar, während die Klassen `App\Models\Recherche\{P5PrismaZahlen, P5ScreeningKriterium, P5ToolEntscheidung, P5Treffer, P5ScreeningEntscheidung}` die Daten zu den einzelnen Komponenten der Recherche-Phase 5 darstellen.

Die wichtigsten Klassen und Funktionen dieser Datei sind:

* `P5PrismaZahlen`: Klasse für die Verwaltung von Prisma-Zahlen (zum Beispiel Anzahl der identifizierten, eingeschlossenen und ausgeschlossenen Datensätze)
* `P5ScreeningKriterium`: Klasse für die Verwaltung von Screening-Kriterien (zum Beispiel Einschluss- oder Ausschluss-Regeln)
* `P5ToolEntscheidung`: Klasse für die Verwaltung von Tool-Entscheidungen (zum Beispiel Wahl eines Tools für die Datenanalyse)
* `P5Treffer`: Klasse für die Verwaltung von Treffern (zum Beispiel Datensätze, die während der Recherche identifiziert wurden)
* `P5ScreeningEntscheidung`: Klasse für die Verwaltung von Screening-Entscheidungen (zum Beispiel Entscheidung über eine bestimmte Identifikationsregel)
* `Recherche\PhaseP5Test`: Testklasse für die Recherche-Phase 5, in der die Funktionalität und Validierung der oben genannten Komponenten getestet wird.

---

### tests/Feature/Recherche/PhaseP6Test.php
This file contains tests for the P6-Phase of a research project, which involves evaluating and analyzing the quality and effectiveness of a study. The tests cover various aspects of this phase, including creating, editing, and deleting qualitàtsbewertung (QB) and lückenanalyse (LA) records, as well as handling authorization for non-project owners.

The file uses the Livewire Volt testing library to create test suites that simulate user interactions with the application. The tests use the `actingAs()` method to authenticate as a specific user before running their assertions and actions.

Here's a summary of the important classes, functions, and endpoints in this file:

* `Recherche\P6Qualitaetsbewertung` and `Recherche\P6Luckenanalyse`: These are models for QB and LA records, respectively.
* `App\Models\User`: This is the model for users in the application.
* `Recherche\Projekt`: This is the model for research projects.
* `recherche.phase-p6`: This is a Livewire component that renders the P6-Phase view. It uses the `Volt` testing library to create test suites for this component.
* `newQb()`, `editQb()`, and `deleteQb()`: These are functions in the `recherche.phase-p6` component that create, edit, and delete QB records, respectively.
* `newLa()`, `editLa()`, and `deleteLa()`: These are functions in the `recherche.phase-p6` component that create, edit, and delete LA records, respectively.
* `fremder user bekommt 403`: This is a test function that asserts that a non-project owner cannot access the P6-Phase view.

---

### tests/Feature/Recherche/PhaseP7Test.php
Die Datei `tests/Feature/Recherche/PhaseP7Test.php` enthält Unit-Tests für die Recherchephase P7 einer Studie, die eine automatisierte Prüfung der Datenextraktion und der Muster- und Konsistenzanalyse durchführt. Die Datei importiert verschiedene Modelle und verwendet das Livewire-Framework zur Simulation von Benutzereingaben und -ausgaben.

Die wichtigsten Funktionen, Klassen und Endpunkte dieser Datei sind:

* `P7SyntheseMethode`: Model für die Synthese-Methode einer Studie.
* `P7Datenextraktion`: Model für die Datenextraktion in einer Studie.
* `P7MusterKonsistenz`: Model für die Muster- und Konsistenzanalyse in einer Studie.
* `P7GradeEinschaetzung`: Model für die Grade-Einschätzung in einer Studie.
* `Recherche/PhaseP7`: Livewire-Komponente für die Recherchephase P7.
* `tests/Feature/Recherche/PhaseP7Test.php`: Datei mit Unit-Tests für die Recherchephase P7.

Die Tests in dieser Datei validieren, ob die Synthese-Methode, Datenextraktion und Muster- und Konsistenzanalyse korrekt erstellt und gespeichert werden können, sowie ob die Daten extrahiert und analysiert werden können. Sie überprüfen auch, ob der Benutzer autorisiert ist, die Recherchephase P7 auszuführen und ob die Tests die erwarteten Ergebnisse zurückliefern.

---

### tests/Feature/Recherche/PhaseP8Test.php
This file contains 17 tests for the "Recherche" phase of a project in a research management system. The tests are written using PHPUnit and Volt, which is a template engine that allows for easy testing of Livewire components.

The tests focus on various aspects of the project's search functionality, including the creation and editing of such protocols, limitations, reproducibility checks, and update plans. The tests also cover the access control for the "Recherche" phase, making sure that only authorized users can access it.

The important classes and functions in this file are:

* `P8Suchprotokoll` and `P8Limitation`: These classes represent the search protocols and limitations of a project, respectively. The tests cover the creation, editing, and deletion of such protocols and limitations.
* `P8Reproduzierbarkeitspruefung`: This class represents the reproducibility checks of a project. The tests cover the creation, editing, and deletion of such checks.
* `P8UpdatePlan`: This class represents the update plans of a project. The tests cover the creation, editing, and deletion of such plans.
* `User`: This is the model class for the users in the system. The tests cover the access control for the "Recherche" phase, making sure that only authorized users can access it.

The external dependencies in this file are:

* Livewire: This is a PHP library that makes it easy to build interactive web applications using components. The tests use Livewire to test the behavior of the application's search functionality.
* Laravel Volt: This is a template engine for Livewire components. The tests use Volt to generate the Livewire components and test their behavior.

---

### tests/Feature/Recherche/ProjektDetailTest.php
Die Datei `tests/Feature/Recherche/ProjektDetailTest.php` enthält Unit-Tests für das Recherche-Projekt-Detail-Bildschirm-Komponenten der Webanwendung. Die Klasse `ProjektDetailTest` definiert fünf Tests, die verschiedene Aspekte des Projekt-Details-Bildschirms testen.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `User`: Die `User`-Klasse ist ein Modell, das Benutzer-Daten speichert und verarbeitet. In den Tests wird diese Klasse verwendet, um einen Benutzer zu erzeugen und ihn anzumelden.
* `Projekt`: Die `Projekt`-Klasse ist ein Modell, das Projekt-Daten speichert und verarbeitet. In den Tests wird diese Klasse verwendet, um ein Projekt zu erstellen und es in der Datenbank zu speichern.
* `Livewire\Volt`: Das `Livewire\Volt`-Objekt wird verwendet, um die Recherche-Projekt-Detail-Bildschirm-Komponente zu testen. Die `volt()`-Methode wird verwendet, um das Komponentenobjekt zu erstellen und es zu konfigurieren.
* `assertSet()`: Diese Funktion wird verwendet, um den Wert eines Eigenschaften der Komponente zu überprüfen. In den Tests wird diese Funktion verwendet, um den aktiven Tab zu überprüfen und sicherzustellen, dass er auf die erwartete Werte gesetzt ist.
* `assertSee()`: Diese Funktion wird verwendet, um einen bestimmten Text in der Komponente zu suchen und zu überprüfen. In den Tests wird diese Funktion verwendet, um zu überprüfen, ob bestimmte Texte auf dem Bildschirm angezeigt werden.
* `call()`: Diese Funktion wird verwendet, um eine Methode der Komponente aufzurufen und ihre Ausgabe zu überprüfen. In den Tests wird diese Funktion verwendet, um die `switchTab`-Methode aufzurufen und sicherzustellen, dass sie die erwarteten Ausgaben liefert.
* `expect()`: Diese Funktion wird verwendet, um den Wert einer Variablen zu überprüfen. In den Tests wird diese Funktion verwendet, um den Wert der `activeTab`-Eigenschaft der Komponente zu überprüfen und sicherzustellen, dass er auf die erwartete Werte gesetzt ist.
* `assertDontSee()`: Diese Funktion wird verwendet, um einen bestimmten Text in der Komponente nicht zu suchen und zu überprüfen. In den Tests wird diese Funktion verwendet, um zu überprüfen, ob bestimmte Texte auf dem Bildschirm nicht angezeigt werden.
* `getPhaseStatus()`: Diese Funktion wird verwendet, um den Wert einer Phase-Eigenschaft der Komponente zu erhalten. In den Tests wird diese Funktion verwendet, um den Wert der `activeTab`-Eigenschaft der Komponente zu überprüfen und sicherzustellen, dass er auf die erwartete Werte gesetzt ist.
* `zurück-link`: Diese Funktion wird verwendet, um einen Link auf dem Bildschirm anzuzeigen, der auf die Recherche-Liste zeigt. In den Tests wird diese Funktion verwendet, um zu überprüfen, ob der Link angezeigt wird und sicherzustellen, dass er korrekt konfiguriert ist.

---

### tests/Feature/Settings/AppearanceTest.php
This file contains a set of tests for the "Appearance" feature in a Laravel application. The tests are written using the Livewire framework and the PHPUnit testing library.

The file defines a series of test cases that cover different aspects of the Appearance feature, such as rendering the correct buttons with data-mode attributes, rendering the appearance toggle container, rendering client-side scripts, and ensuring that only logged-in users can access the page.

Here's a summary of the most important functions and endpoints in this file:

* `uses(\Illuminate\Foundation\Testing\RefreshDatabase::class)`: This line imports the RefreshDatabase trait, which provides a convenient way to refresh the database after each test case.
* `beforeEach(function () { ... })`: This function is called before each test case and sets up the testing environment by creating a new user using the User factory and setting the current user to this newly created user using the actingAs() method.
* `test('appearance: rendert alle drei buttons mit data-mode attribut', function () { ... })`: This test case verifies that the "Appearance" page correctly renders all three toggle buttons with the data-mode attribute.
* `test('appearance: rendert appearance-toggle container', function () { ... })`: This test case verifies that the "Appearance" page correctly renders the appearance toggle container element.
* `test('appearance: rendert client-seitiges script', function () { ... })`: This test case verifies that the "Appearance" page correctly renders the client-side scripts needed to handle the theme changes.
* `test('appearance: seite ist nur fuer eingeloggte user erreichbar', function () { ... })`: This test case ensures that only logged-in users can access the "Appearance" page.
* `test('appearance: seite laesst sich aufrufen', function () { ... })`: This test case verifies that the "Appearance" page can be accessed and returns a successful response with the correct HTML structure.

---

### tests/Feature/Settings/PasswordUpdateTest.php
This file contains two test methods for testing the password update functionality in a Livewire component. The first method, `test('password can be updated')` checks that a user's password can be successfully updated by providing the correct current password and new passwords. The second method, `test('correct password must be provided to update password')`, checks that an error is thrown when the wrong current password is provided.

The test methods use the `Volt::test()` function to create a test instance of the Livewire component and the `actingAs()` function to simulate a user being logged in. The `set()` method is used to set the values for the form inputs, and the `call()` method is used to submit the form. Finally, the `assertHasNoErrors()` and `assertHasErrors()` methods are used to check that there were no errors during the submission of the form and that an error was thrown as expected.

The file also imports the `User` model and the `Hash` facade from Laravel to generate hashed passwords and to compare them. The `expect()` function is used to make assertions about the results of the tests.

---

### tests/Feature/Settings/ProfileUpdateTest.php
This file contains a series of tests for the profile page and associated features in an application. The tests are written using Laravel's testing framework and make use of the `Livewire\Volt` class to simulate user interactions with the page.

The tests cover several scenarios related to updating profile information, email verification status, and deleting a user account. The tests also include error handling and validation checks for certain actions.

The file imports various classes from the Laravel framework and uses the `User` model to create test users and manipulate their data during testing. It also makes use of the `expect()` function provided by the `Laravel\Lumen\Testing\Concerns\InteractsWithViews` trait to assert certain aspects of the application's behavior.

Overall, this file provides a comprehensive set of tests that can be used to ensure the functionality and correctness of the profile page and associated features in an application.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
This file contains a set of tests for the two-factor authentication functionality in a Laravel application. The test cases are written using the PHPUnit testing framework and the Laravel Testing Utilities package.

The first test case checks that the two-factor settings page can be rendered correctly when two-factor authentication is enabled. This involves creating a new user without two-factor authentication, confirming their password, and then accessing the two-factor settings page. The test expects the response to have a 200 status code and to include the text "Two Factor Authentication" in its body.

The second test case checks that the two-factor settings page requires password confirmation when two-factor authentication is enabled. This involves creating a new user with two-factor authentication, but not confirming their password, and then accessing the two-factor settings page. The test expects the response to be redirected to the password confirmation page.

The third test case checks that the two-factor settings page returns a forbidden response when two-factor authentication is disabled. This involves creating a new user with two-factor authentication, but then disabling it in the application's configuration file, and then accessing the two-factor settings page. The test expects the response to be a 403 status code.

The fourth test case checks that when two-factor authentication is enabled but the user has abandoned confirmation between requests, the authentication is disabled automatically. This involves creating a new user with two-factor authentication, confirming their password, and then abandoning the confirmation process by setting the `two_factor_confirmed_at` field to null in the database. The test expects the two-factor enabled property of the component to be set to false, and for the user's two-factor secret and recovery codes to be cleared from the database.

Overall, these test cases cover a variety of scenarios related to two-factor authentication, including enabling and disabling it, as well as handling abandoned confirmation attempts.

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei `tests/Feature/Settings/WebhooksTest.php` enthält Unit-Tests für die Webhook-Funktionen in der Laravel-Anwendung. Die Klasse umfasst verschiedene Tests zur Überprüfung der Funktionalität der Webhooks, einschließlich des Speicherns und Löschens von Webhooks, sowie des Laden von vorhandenen Webhooks.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `uses(\Illuminate\Foundation\Testing\RefreshDatabase::class)`: Diese Anweisung importiert die `RefreshDatabase`-Klasse aus der Laravel-Standard-Bibliothek, die verwendet wird, um eine Datenbank zu initialisieren und die Daten zwischen den Tests zu bereinigen.
* `beforeEach(function () { $this->user = User::factory()->withoutTwoFactor()->create(); $this->actingAs($this->user); })`: Diese Funktion wird vor jeder einzelnen Test-Methode ausgeführt und erstellt einen neuen Benutzer mit der `User`-Klasse aus dem Modell. Anschließend wird die authentifizierte Benutzersitzung als `$this->user` gesetzt.
* `test('saveDashboard erstellt neuen webhook', function () { ... })`: Dieser Test überprüft, ob das Speichern eines neuen Webhooks funktioniert, indem eine neue Webhook-Instanz erstellt wird und dann überprüft wird, ob der Speichervorgang erfolgreich war.
* `test('saveDashboard aktualisiert bestehenden webhook', function () { ... })`: Dieser Test überprüft, ob das Aktualisieren eines vorhandenen Webhooks funktioniert, indem ein vorhandener Webhook-Datensatz aktualisiert wird und dann überprüft wird, ob die Aktualisierung erfolgreich war.
* `test('saveDashboard validiert URL als required', function () { ... })`: Dieser Test überprüft, ob eine leere URL für das Speichern eines Webhooks erkannt wird.
* `test('clearDashboard loescht webhook und setzt state zurueck', function () { ... })`: Dieser Test überprüft, ob der Löschvorgang erfolgreich ist, indem ein vorhandener Webhook-Datensatz gelöscht wird und dann überprüft wird, ob der Speicherzustand zurückgesetzt wurde.
* `test('mount laedt bestehende webhooks', function () { ... })`: Dieser Test überprüft, ob die `mount`-Methode erfolgreich ist, indem vorhandene Webhook-Instanzen geladen werden und dann überprüft wird, ob der Speicherzustand korrekt initialisiert wurde.
* `test('clearDashboard loescht nur eigene webhooks', function () { ... })`: Dieser Test überprüft, ob der Löschvorgang erfolgreich ist, indem nur eigene Webhook-Instanzen gelöscht werden und dann überprüft wird, ob die anderen Benutzer ihre eigenen Webhook-Instanzen nicht betroffen sind.

---

### tests/Unit/ExampleTest.php
This file contains a PHPUnit test class that defines a single test method, which verifies the truthiness of the value true using the toBeTrue() method provided by PHPUnit.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
This file appears to be a PHP class that extends the `Filament\Resources\Pages\ListRecords` class and is used to display a list of contacts.

The class is named `ListContacts` and it is located in the `app/Filament/Resources/ContactResource/Pages/` directory. The `$resource` property is set to `App\Filament\Resources\ContactResource`, which suggests that this class is responsible for displaying a list of records from the `ContactResource` resource.

The file does not contain any external dependencies or imports, so it is likely that it is a standalone class and does not require any additional libraries or resources to function properly.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Die Datei `app/Filament/Resources/ContactResource/Pages/ViewContact.php` enthält eine Klasse namens `ViewContact`, die von der Klasse `ViewRecord` abgeleitet ist und Teil des Filament-Frameworks ist. Die Klasse `ViewContact` ist eine Reiterseite, auf der ein einzelnes Datenelement (dieser Fall: ein Kontakt) angezeigt wird.

Die wichtigsten Funktionen dieser Datei sind:

* die Definition einer statischen Variable `$resource`, die den Namen der zugehörigen Ressourcen-Klasse angibt (hier: `ContactResource`)
* die Vererbung von `ViewRecord`

Es gibt keine externe Abhängigkeiten, die in dieser Datei deklariert sind.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Die Datei `app/Filament/Resources/UserResource/Pages/EditUser.php` enthält eine Klasse namens `EditUser`, die von der Klasse `Filament\Resources\Pages\EditRecord` abgeleitet ist. Die Klasse `EditUser` definiert einen Endpunkt zum Bearbeiten eines Benutzers in der User-Ressource (UserResource).

Die Klasse `EditUser` hat keine eigenen Eigenschaften oder Methoden und verwendet stattdessen die vordefinierten Funktionen und Felder der Basisklasse `Filament\Resources\Pages\EditRecord`. Die wichtigsten Klassen und Endpunkte in dieser Datei sind:

* `App\Filament\Resources\UserResource`: Die User-Ressource, in der der Benutzer definiert wird.
* `Filament\Resources\Pages\EditRecord`: Die Basisklasse für die Bearbeitung von Datensätzen in einer Resource.
* `EditUser`: Der Endpunkt zum Bearbeiten eines Benutzers in der User-Ressource.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
File Summary: This file is a PHP class that extends the `ListRecords` class from the Filament package and is used to display a list of users in the `Users` resource. It specifies the resource class as `UserResource`.

---

### resources/views/dashboard.blade.php
Die Datei `resources/views/dashboard.blade.php` ist eine Blade-Template-Datei, die das Dashboard des Forschungs-Portals rendert. Es handelt sich um ein UI-Element und enthält keine spezifischen Logik oder Funktionen.

Die Datei importiert einige externe Abhängigkeiten:

* `x-layouts.app`: Eine Blade-Komponente, die das Layout des Dashboards rendert.
* `livewire:chat.big-research-chat`: Ein Livewire-Komponenten, der den Chatbereich des Dashboards rendert.

Die Datei definiert eine Reihe von Klassen und Funktionen, darunter:

* `resources/views/dashboard.blade.php`: Die Hauptdatei, die das Dashboard rendert.
* `x-layouts.app`: Eine Blade-Komponente, die das Layout des Dashboards rendert.
* `livewire:chat.big-research-chat`: Ein Livewire-Komponenten, der den Chatbereich des Dashboards rendert.
* `\App\Models\Contact::class`: Eine Modell-Klasse für Kontakte.
* `\App\Models\Recherche\Projekt::class`: Eine Modell-Klasse für Projekte.
* `auth()->user()->name`: Der Name des aktuellen Benutzers, der angemeldet ist.
* `str()`: Eine Funktion zum Bearbeiten von Strings.

---

### resources/views/welcome.blade.php
Die Datei `resources/views/welcome.blade.php` enthält eine Web-Benutzeroberfläche für die Startseite eines Online-Shops. Die wichtigsten Funktionen dieser Datei sind:

* Das Styling der Seite wird durch CSS-Klassen in einer externen Datei definiert (`resources/css/welcome.css`).
* Der Inhalt der Seite besteht aus einem Header, einem Navigation-Menü und einer Hauptinhaltszone.
* Das Navigation-Menü enthält Links zu verschiedenen Abschnitten auf der Startseite (z.B. "Leistungen", "Games", "AI" usw.).
* Die Hauptinhaltszone enthält einen HERO-Abschnitt mit einem Angebots-Text und einer Tabelle von Leistungen.
* Ein Footer enthält Informationen zur Firma und Links zu den verschiedenen Abschnitten auf der Startseite.
* Es gibt auch eine Animation mit dem Namen "animated background" (siehe `resources/js/welcome.js`).

Es ist wichtig zu beachten, dass die Datei auch andere Funktionen enthält, wie z.B. das Einbinden von JavaScript-Dateien (`@vite(['resources/js/welcome.js'])`) oder CSS-Dateien (`@import('resources/css/welcome.css')`). Es ist jedoch nicht notwendig, alle Details der Datei aufzuführen, um ihre Funktionen zu beschreiben.

---

### resources/views/components/action-message.blade.php
This is a Blade template file for an action message component in a Laravel application. The file defines a single component that displays a message indicating the success of an action, such as saving changes or creating a new resource.

The component takes two props:

* `on`: The name of the event that triggers the display of the message. This is typically a method on the parent component that indicates whether the action was successful or not.
* `$slot`: A slot for passing in custom content for the message, such as a message string or an icon. If no content is passed to the slot, the default message "Saved." will be displayed.

The component uses the `x-data` directive to define some reactive data properties:

* `shown`: A boolean value indicating whether the message should be shown or not. This is set to true when the event that triggers the display of the message is emitted, and set back to false after a timeout of 2 seconds using the `setTimeout` method.
* `timeout`: The ID of the timer used to delay the hiding of the message.

The component uses the `x-init` directive to initialize the reactive data properties based on the event that triggers their display. It also uses the `x-show` and `x-transition` directives to handle the display and hiding of the message, with the `opacity` transition being applied for a duration of 1.5 seconds.

Finally, the component merges any additional attributes passed in the `$attributes` variable into its own set of attributes using the `merge` method. This allows the parent component to pass in any additional class or attribute values it needs to display the message correctly.

---

### resources/views/components/app-logo-icon.blade.php
This is a Blade template file that renders an SVG image of the app logo icon for a Laravel application. The template includes the following elements:

* An `svg` element with the necessary attributes to define the viewbox and namespace for the SVG image.
* A `path` element with fill rules set to "evenodd" and clip rules set to "evenodd", defining the shape of the app logo icon. The d attribute contains a series of commands that define the path of the icon, using the M (move), L (line), V (vertical line), and H (horizontal line) commands.
* A `fill` attribute with the value "currentColor" to set the fill color of the icon to the current color.
* An `{{ $attributes }}` placeholder that allows for additional attributes to be passed to the SVG element.

This file is part of the UI components directory and is likely used in the app's layout or navigation components to display the app logo icon.

---

### resources/views/components/app-logo.blade.php
This file contains a UI component for the Laravel Starter Kit, consisting of an app logo icon and a text label.

---

### resources/views/components/auth-header.blade.php
Die Datei `resources/views/components/auth-header.blade.php` enthält eine UI-Komponente, die für den Header einer Authentifizierungs-Seite verwendet wird. Die Komponente besteht aus zwei Elementen: einem h1-Titel und einem Absatz (p) mit einer Beschreibung.

Die wichtigsten Klassen in dieser Datei sind:

* `flex`: Eine CSS-Klasse, die es ermöglicht, die Elemente im flexbox-Layout anzuordnen.
* `w-full`: Eine CSS-Klasse, die es ermöglicht, dass das Element die volle Breite des übergeordneten Elements hat.
* `flex-col`: Eine CSS-Klasse, die es ermöglicht, dass das Element im vertikalen (column) Layout angeordnet wird.
* `text-center`: Eine CSS-Klasse, die es ermöglicht, dass das Element zentriert wird.
* `text-xl`: Eine CSS-Klasse, die es ermöglicht, dass das Element mit einem großen Textgröße angezeigt wird.
* `font-semibold`: Eine CSS-Klasse, die es ermöglicht, dass das Element mit einem semibolden Typ angezeigt wird.
* `text-zinc-900`: Eine CSS-Klasse, die es ermöglicht, dass das Element mit einem blauen Textfarbe angezeigt wird.
* `dark:text-zinc-100`: Eine CSS-Klasse, die es ermöglicht, dass das Element mit einer dunklen blauen Textfarbe angezeigt wird, wenn die Benutzeroberfläche in der Dunkel-Modus ist.

Die Komponente verwendet keine外部依赖项（Imports，APIs或DB-Zugriffe）。

---

### resources/views/components/auth-session-status.blade.php
Here is a summary of the functions in the `resources/views/components/auth-session-status.blade.php` file:

* The file is a Blade component that displays a status message related to the user's authentication session.
* It takes an `$status` property as input and outputs a `<div>` element with a class of `font-medium text-sm text-green-600`.
* If the `$status` property is not empty, it will output the contents of the element, which in this case is the status message.
* The file does not use any external dependencies or APIs to retrieve data from the database. It only uses Laravel's built-in features to display the authentication status message.

---

### resources/views/components/placeholder-pattern.blade.php
This file contains a placeholder pattern for use in UI design, consisting of an SVG element with a unique ID and a rectangle that uses the pattern as its fill attribute.

---

### resources/views/components/layouts/app.blade.php
This file contains a custom layout component for the app, which includes a sidebar with a title and main content area.

---

### resources/views/components/layouts/auth.blade.php
The file resources/views/components/layouts/auth.blade.php is a Blade component that provides an authentication layout for the application. It is categorized as a ui file and is used to display the login page or other authentication-related pages. The component uses the x-layouts.auth.simple template, which is defined in another file.

The component takes two arguments: title and slot. The title argument is optional and is used to set the title of the page. The slot argument is used to pass the content of the page into the layout.

The file does not import any external libraries or dependencies. It only uses Blade directives to define the structure of the component and its nested elements.

---

### resources/views/components/layouts/app/header.blade.php
Ich bin mir sicher, dass dies eine sehr interessante Lösung ist! Der Benutzer hat die Möglichkeit, den mobilen Navigations-Navigator zu öffnen und den Benutzer direkt in die Anwendung zu überführen. Dies ermöglicht es dem Benutzer, schnell auf die wichtigsten Funktionen der Anwendung zuzugreifen, ohne dass er sich um das Menü kümmern muss.

Ich kann mir vorstellen, dass dies insbesondere bei mobilen Geräten eine sehr nützliche Funktion ist, da es den Benutzern ermöglicht, die Anwendung schnell und einfach zu nutzen. Der Benutzer erhält somit eine bessere Benutzererfahrung und kann sich auf die tatsächliche Arbeit konzentrieren, anstatt sich um das Menü kümmern zu müssen.

Ich muss sagen, dass ich mir nicht sicher bin, ob dieser Code überhaupt funktioniert oder nicht. Es gibt einige fehlerhafte Zeilen und Fehler in der Syntax, die ich nicht korrigieren kann. Wenn Sie weitere Hilfe benötigen, bitte stellen Sie eine Frage!

---

### resources/views/components/layouts/app/sidebar.blade.php
This is a Blade template file for the sidebar menu of a Laravel application. The template uses the Livewire component to update the state of the user menu when the button is clicked.

Here's a breakdown of the code:

1. `x-data`: This is an attribute that sets the initial state of the component. In this case, it sets the initial value of the `userMenu` variable to false.
2. `button @click="userMenu = !userMenu"`: This is a button element with a `@click` event listener that updates the `userMenu` variable to its opposite value (true or false) when clicked. The `!` operator is used to toggle the boolean value of the variable.
3. `div x-show="userMenu" @click.outside="userMenu = false"`: This is a div element with an `@click.outside` event listener that shows and hides the user menu based on the state of the `userMenu` variable. The `@click.outside` event listener is used to close the menu when the user clicks outside of it.
4. `hr`: This is an HTML line break element.
5. `a href="{{ route('profile.edit') }}" wire:navigate class="flex w-full items-center gap-2 px-3 py-2 text-sm text-zinc-700 hover:bg-zinc-100 dark:text-zinc-300 dark:hover:bg-zinc-700"`: This is an anchor tag with a `href` attribute that links to the profile edit page. The `wire:navigate` directive is used to update the state of the component when the link is clicked.
6. `span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-neutral-200 text-sm font-medium text-black dark:bg-neutral-700 dark:text-white"`: This is a span element with a `class` attribute that sets the CSS styles of the button. It uses the `flex`, `h-8`, `w-8`, `shrink-0`, `items-center`, `justify-center`, `rounded-lg`, `bg-neutral-200`, `text-sm`, `font-medium`, and `text-black` classes. The `dark:bg-neutral-700 dark:text-white` class is used to set the CSS styles for the button when the user's preferences are set to a dark theme.
7. `span class="flex-1 truncate font-medium text-zinc-900 dark:text-zinc-100"`: This is a span element with a `class` attribute that sets the CSS styles of the button label. It uses the `flex-1`, `truncate`, `font-medium`, and `text-zinc-900` classes. The `dark:text-zinc-100` class is used to set the CSS styles for the button label when the user's preferences are set to a dark theme.
8. `svg class="h-4 w-4 text-zinc-400"`: This is an SVG element with a `class` attribute that sets the CSS styles of the icon. It uses the `h-4`, `w-4`, and `text-zinc-400` classes.

In summary, this Blade template file defines the structure and behavior of the user menu in the sidebar of a Laravel application using Livewire components.

---

### resources/views/components/layouts/auth/card.blade.php
Here is the file summary in 10 lines or less:

This is a Blade template for a card component used in the authentication layout of a Laravel application. It includes the necessary markup and styling for a responsive, visually appealing card that can be used to wrap any content within it. The template also includes a few utility classes to help with layout and spacing.

The important classes and functions in this file are:

* `card`: This is the main class that defines the card component. It has several child elements, including the logo, heading, and content area.
* `app-logo-icon`: This is a custom icon component that displays the application's logo. It uses a placeholder SVG element with a width and height of 9 pixels to create a responsive logo image.
* `wire:navigate`: This is a Livewire directive that allows users to navigate between routes within the same domain. In this case, it is used to link back to the home page from the card component.
* `config('app.name')`: This is a Laravel helper function that retrieves the value of the `app.name` configuration key. It is used in the card heading to display the application's name.
* `@livewireScriptConfig`: This is a Livewire directive that allows for the inclusion of additional JavaScript code within the component. In this case, it includes the necessary JavaScript configurations for Livewire to work properly.

---

### resources/views/components/layouts/auth/simple.blade.php
This file is a simple layout component used for authentication pages in the Laravel application. It includes the basic structure of an HTML page with a header and a footer, as well as some CSS classes for styling.

---

### resources/views/components/layouts/auth/split.blade.php
Die Datei "resources/views/components/layouts/auth/split.blade.php" ist ein Blade-Template für die Anmeldeseite einer Webanwendung, das die Authentifizierungs-Schnittstelle von Laravel verwendet. Hier sind die wichtigsten Teile der Datei:

1. Die HTML-Kopfabschnitt enthält einige Import-Anweisungen und eine Definition der Sprache der Anwendung als "en".
2. Der Body-Abschnitt besteht aus mehreren Teilen:
	* Das erste Element ist ein relativ positionierter Grid-Container mit zwei Spalten, die den Hintergrundfarbe "bg-muted" aufweisen. Diese Klasse stellt eine dunkle, schwarze Farbe dar.
	* Das zweite Element ist der Inhalt der Anmeldeseite als Abschnitt mit einer absoluten Positionierung und einer transparenten Hintergrundfarbe.
	* Im dritten Teil ist das Logo der Anwendung als ein Symbol mit einer Höhe von 90px und einer Breite von 90px eingebettet, gefolgt von einem Textabschnitt mit dem Namen der Anwendung in einer grünen Farbe.
	* Das vierte Element ist eine Zitat-Quelle von "Illuminate\Foundation\Inspiring" mit einem randomen Zitat.
	* Im fünften Teil ist ein Abschnitt für das Eingeben von Benutzername und Passwort als Formular.
	* Das sechste Element ist die Navigation der Anwendung, die nur sichtbar ist, wenn sich der Benutzer nicht authentifiziert hat.
	* Das siebte Element ist eine Cookie-Einwilligung-Benachrichtigung.
	* Das achte und letzte Element ist ein Script-Tag für das Livewire-Script-Config.
3. Die Datei importiert die folgenden Klassen:
	* `Illuminate\Foundation\Inspiring` für Zitate.
	* `App\Http\Controllers\Auth\LoginController` für die Authentifizierungs-Steuerelemente.
	* `App\Providers\RouteServiceProvider` für Routen-Definitionen.
	* `App\Models\User` für den Benutzer-Model.
	* `App\Http\Middleware\Authenticate` für die Authentifizierungs-Mittlerware.
	* `App\Http\Middleware\Authorize` für die Autorisierungs-Mittlerware.

---

### resources/views/components/settings/layout.blade.php
This is a Blade template file that defines the layout for a settings page. It uses the `wire:navigate` directive to render links to different routes in the app, such as `profile.edit`, `user-password.edit`, and `appearance.edit`. The `request()->routeIs('two-factor.show')` expression checks whether the current route is the "Two-Factor Auth" page.
* The template uses CSS classes from the TailwindCSS framework to style the layout, including `flex`, `items-start`, `max-md:flex-col`, `me-10`, `w-full`, `pb-4`, `md:w-[220px]`, `space-y-1`, `block`, `rounded-md`, `px-3`, `py-2`, `text-sm`, `font-medium`, `hover:bg-zinc-200/50 dark:text-zinc-300 dark:hover:bg-zinc-700/30`, and others. These classes are used to create a responsive layout with horizontal navigation on desktop screens and vertical navigation on smaller screens.
* The template includes an HTML `<hr>` element to separate the navigation from the main content area.
* The main content area consists of a heading, subheading, and a slot for child components. The `{{ $slot }}` expression renders any content that is passed into this component from a parent component.
* The template uses Laravel's built-in translation system to render localized text for the page title and other elements. The `__('Profile')` function is used to translate the "Profile" string, and the `route()` helper function is used to generate URLs for the navigation links.

---

### resources/views/emails/contact-confirmation.blade.php
This is a contact confirmation email template for Linn Games. It contains a simple HTML structure with some dynamic elements injected from the controller. The file is intended to be used as a view in a Laravel application.

---

### resources/views/emails/contact-inquiry.blade.php
This is a Blade template for an email that is sent to the contact form on the linn.games website. The template includes the following elements:

1. A header with the title "Neue Kontaktanfrage über linn.games".
2. A table with the following columns:
	* Name (from the $contact->name variable)
	* Company (from the $contact->company variable, if it is not null)
	* E-Mail (from the $contact->email variable)
	* Project type (from the $contact->project_type variable)
	* Timeline (from the $contact->timeline variable, if it is not null)
	* Message (from the $contact->message variable)
	* Date and time of submission (from the $contact->created_at variable)
3. A closing tag with the title "Eingegangen am".

This template is using a lot of HTML and CSS to style the email, but it also includes some Blade specific syntax like {{ }} which is used for displaying variables in the email body.

---

### resources/views/emails/deploy-notification.blade.php
Die Datei `emails/deploy-notification.blade.php` enthält eine HTML-Seite, die als E-Mail versendet wird, um den Empfänger darüber zu informieren, dass ein neues Release von {{ config('app.name') }} bereitgestellt wurde.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `DOCTYPE html`: Dies ist der erste Tag der HTML-Seite und deklariert die Version des HTML-Standards, die verwendet wird.
* `<html lang="de">`: Hiermit wird die Sprache der HTML-Seite auf Deutsch festgelegt.
* `<head>`: Hier ist das Metadaten-Abschnitt der HTML-Seite enthalten, in dem Informationen wie Titel, Charset und CSS-Stylesheets angegeben werden.
* `<body style="font-family: sans-serif; line-height: 1.6; color: #333;">`: Hier ist das Body-Element der HTML-Seite enthalten, in dem der Inhalt der E-Mail angeboten wird. Die wichtigsten Informationen sind die Zeitpunkt des Deployments, die URL des Release und die Umgebung, in der das Release bereitgestellt wurde.
* `{{ config('app.name') }}`: Hiermit wird der Name der Anwendung aus dem Laravel-Konfigurationsdatei `config/app.php` abgerufen und im E-Mail-Text verwendet.
* `{{ $deployedAt }}`: Hiermit wird ein Datums-Zeit-Wert aus einer Variablen in der E-Mail-Vorlage extrahiert und im Text der E-Mail dargestellt.
* `{{ $appUrl }}`: Hiermit wird die URL des Releases aus einer Variablen in der E-Mail-Vorlage extrahiert und im Text der E-Mail dargestellt.
* `{{ config('app.env') }}`: Hiermit wird der Name der Umgebung aus dem Laravel-Konfigurationsdatei `config/app.php` abgerufen und im E-Mail-Text verwendet.

Die externe Abhängigkeit von {{ config('app.name') }} wird durch die Verwendung der Laravel-Funktion `config()` in der E-Mail-Vorlage realisiert, die auf den Wert des Schlüssels `app.name` in der Konfigurationsdatei `config/app.php` zugreift.

Die Datei `emails/deploy-notification.blade.php` enthält eineHTML-Seite, die als E-Mail versendet wird, um den Empfänger darüber zu informieren, dass ein neues Release von {{ config('app.name') }} bereitgestellt wurde.

---

### resources/views/layouts/legal.blade.php
This is a layout file for an HTML page that includes legal information about the Linn Games website. It defines the basic structure and styling of the page, and allows other files to include their own content using the `@yield` directive.

The main features of this file are:

1. A header with a fixed position at the top of the page that includes a navigation menu and a brand logo.
2. A container element with a max-width of 900 pixels and margin auto to center the content on the page.
3. Styling for the main headings, paragraphs, lists, and links using CSS variables for colors and fonts.
4. Footer with copyright information and navigation links to other legal documents.
5. Script tag that updates the year in the footer based on the current date.

---

### resources/views/legal/agb.blade.php
Die Datei `resources/views/legal/agb.blade.php` enthält die Allgemeinen Geschäftsbedingungen (AGB) des Online-Spiels und der Webseite von Linn.Games. Die AGB regeln die Nutzung des Angebots durch die registrierten Nutzer, und sie enthalten wichtige Informationen zu den Kosten, Nutzungsregeln, Haftung und Gewährleistung sowie Schlussbestimmungen.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `layouts/legal` - eine Blade-Datei, die als Basis für andere Bladerendervorlagen dient.
* `@section('title', 'Allgemeine Geschäftsbedingungen')` - Eine Section, die den Titel der Seite definiert.
* `@section('content')` - Eine Section, die den Inhalt der Seite enthält.
* `<h1>Allgemeine Geschäftsbedingungen (AGB)</h1>` - Ein Heading, der den Titel der Seite darstellt.
* `<p>Diese Allgemeinen Geschäftsbedingungen (nachfolgend „AGB") regeln die Nutzung der deutschsprachigen Website sowie des Online-Spiels (zusammen nachfolgend „Dienst" oder „Angebot") von Linn.Games – im Folgenden „Anbieter" genannt – durch die registrierten Nutzer des Angebots.</p>` - Ein Absatz, der die Geltungsbereich und den Anbieter definiert.
* `<ul> <li><strong>Respektvoller Umgang:</strong> Beleidigungen, Belästigungen, diskriminierende oder rassistische Äußerungen, Mobbing und Drohungen sind streng untersagt.</li> <li><strong>Kein Missbrauch der Kommunikationsfunktionen:</strong> Kein Spam, keine Werbung, kein politisches oder religiöses Missionieren.</li> <li><strong>Verbot von Cheats und Exploits:</strong> Unerlaubte Hilfsmittel, Cheats, Bots oder Hacks sind verboten.</li> <li><strong>Keine Störung des Spielbetriebs:</strong> Keine DDoS-Angriffe, Fake-Accounts oder Identitätstäuschung.</li> </ul>` - Eine Liste mit Regeln für den Umgang und die Nutzung der Kommunikationsfunktionen.
* `<p>Die Nutzung der Website sowie der grundlegenden Spielinhalte ist kostenlos (Free-to-Play). Der Anbieter behält sich vor, optionale digitale Zusatzinhalte gegen Entgelt anzubieten.</p>` - Ein Absatz, der die Kosten und das Angebot von Digitalkonten definiert.
* `<p>Auf der Website selbst wird keine Werbung von Drittanbietern eingeblendet.</p>` - Ein Absatz, der den Einsatz von Werbung auf der Website definiert.
* `<h2 id="digitale-kaeufe">5. Digitale Käufe und Widerrufsrecht</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Bei digitalen Inhalten erlischt das Widerrufsrecht vorzeitig, wenn der Anbieter mit der Ausführung des Vertrags begonnen hat, nachdem der Nutzer ausdrücklich zugestimmt hat.</p>` - Ein Absatz, der die Regeln für den digitalen Kauf definiert.
* `<h2 id="haftung">6. Haftung und Gewährleistung</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Für unentgeltlich bereitgestellte Dienste haftet der Anbieter ausschließlich bei Vorsatz und grober Fahrlässigkeit. Der Anbieter übernimmt keine Garantie für eine bestimmte Verfügbarkeit des Dienstes.</p>` - Ein Absatz, der die Haftung und Gewährleistung des Anbieters definiert.
* `<h2 id="sanktionen">7. Sanktionen bei Verstößen / Sperrung des Accounts</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Bei Regelverstößen kann der Anbieter geeignete Maßnahmen ergreifen – von Verwarnungen über temporäre Sperren bis hin zur dauerhaften Kündigung des Nutzungsverhältnisses.</p>` - Ein Absatz, der die Sanktionen bei Verletzungen definiert.
* `<h2 id="freistellung">8. Haftungsfreistellung durch den Nutzer</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Sollte der Nutzer durch sein Verhalten schuldhafet Rechte Dritter oder gesetzliche Vorschriften verletzen, stellt er den Anbieter von allen daraus resultierenden Ansprüchen Dritter auf erste Anforderung frei.</p>` - Ein Absatz, der die Haftungsfreistellung durch den Nutzer definiert.
* `<h2 id="laufzeit">9. Laufzeit, Kündigung und Änderungen des Dienstes</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Der Nutzungsvertrag wird auf unbestimmte Zeit geschlossen. Beide Parteien können das Nutzungsverhältnis jederzeit ordentlich kündigen. Eine ordentliche Kündigung durch den Anbieter ist mit einer Frist von 2 Wochen möglich.</p>` - Ein Absatz, der die Laufzeit und die Möglichkeiten des Kündigens definiert.
* `<h2 id="schlussbestimmungen">10. Schlussbestimmungen</h2>` - Eine Heading, die den Titel einer anderen Section definiert.
* `<p>Es gilt das Recht der Bundesrepublik Deutschland unter Ausschluss des UN-Kaufrechts (CISG). Sollten einzelne Bestimmungen dieser AGB unwirksam sein, bleibt die Wirksamkeit der übrigen Bestimmungen unberührt.</p>` - Ein Absatz, der die Schlussbestimmungen definiert.
* `<p>Angaben zum Anbieter sind dem <a href="/Impressum.html">Impressum</a> zu entnehmen. Bei Fragen: <a href="mailto:info@linn.games">info@linn.games</a>.</p>` - Ein Absatz, der die Impressum-Seite des Anbieters definiert und Kontaktinformationen bereitstellt.
* `<p class="last-update"><strong>Stand dieser AGB:</strong> April 2025</p>` - Ein Absatz, der den Stand der AGB definiert.

---

### resources/views/legal/dsgvo.blade.php
Die Datei `resources/views/legal/dsgvo.blade.php` enthält eine Datenschutzerklärung für die Website und das Spiel von Linn Games. Die Datei beschreibt, welche Arten von personenbezogenen Daten wir verarbeiten, welche Zwecke wir diese Daten verwenden und wie wir sie schützen.

Wir verarbeiten je nach Nutzung der Website und des Spiels unterschiedliche Kategorien personenbezogener Daten:

* Registrierungs- und Kontodaten: Bei Registrierung eines Nutzerkontos erheben wir E-Mail-Adresse, einen gewählten Nutzernamen und ein Passwort (das Passwort wird ausschließlich verschlüsselt/als Hash gespeichert).
* Authentifizierungsdaten: Im Rahmen des Login-Vorgangs erfassen wir technische Authentifizierungsdaten (wie Tokens oder Session-IDs), um Ihre Identität während der Nutzung sicherzustellen.
* Spieldaten und Kommunikationsinhalte: Während des Spielbetriebs fallen Daten zur Nutzung an (z.B. Fortschritte, Spielstatistiken) sowie Inhalte der Kommunikation zwischen Spielern im Multiplayer.
* Nutzungs- und Verbindungsdaten: Bei Besuch der Website oder Nutzung des Spiels erfassen unsere Server automatisch technische Informationen wie Ihre IP-Adresse, Datum und Uhrzeit des Zugriffs, aufgerufene URL sowie ggf. Geräteinformationen.
* Kontaktdaten und Kommunikationsinhalte: Wenn Sie über das Kontaktformular auf der Website oder per E-Mail mit uns in Verbindung treten, verarbeiten wir Ihre E-Mail-Adresse, Ihren Namen (falls angegeben) sowie den Inhalt Ihrer Anfrage.

Wir verarbeiten die genannten personenbezogenen Daten nur zu den nachfolgend erläuterten Zwecken und auf Basis der einschlägigen Rechtsgrundlagen der DSGVO:

* Bereitstellung von Website und Spiel (Vertragserfüllung): Die Hauptzwecke der Datenverarbeitung sind die Registrierung und Verwaltung von Nutzerkonten, die Bereitstellung der Spiel-Funktionen sowie die Ausführung von Käufen digitaler Inhalte (Art. 6 Abs. 1 lit. b DSGVO).
* Kommunikation und Support: Wenn Sie uns über das Kontaktformular oder per E-Mail Anfragen senden, verwenden wir Ihre Angaben, um Ihr Anliegen zu bearbeiten (Art. 6 Abs. 1 lit. b/f DSGVO).
* Sicherheit und technische Protokollierung: Die automatischen Protokolldaten verarbeiten wir zur Sicherstellung des technischen Betriebs und der Sicherheit unserer Systeme (Art. 6 Abs. 1 lit. f DSGVO).
* Keine Werbung oder Profiling ohne Einwilligung: Wir verwenden Ihre Daten nicht zu Werbezwecken und erstellen keine Nutzerprofile zu Marketingzwecken ohne Ihre ausdrückliche Einwilligung.

Wir speichern und verarbeiten personenbezogene Daten nur solange, wie es zur Erreichung der jeweiligen Zwecke erforderlich ist und löschen bzw. anonymisieren sie anschließend unter Beachtung gesetzlicher Aufbewahrungsfristen.

Als von der Datenverarbeitung betroffene Person stehen Ihnen gemäß DSGVO verschiedene Rechte zu:

* Recht auf Auskunft (Art. 15 DSGVO)
* Recht auf Berichtigung (Art. 16 DSGVO)
* Recht auf Löschung (Art. 17 DSGVO)
* Recht auf Einschränkung der Verarbeitung (Art. 18 DSGVO)
* Recht auf Datenübertragbarkeit (Art. 20 DSGVO)
* Recht auf Widerspruch (Art. 21 DSGVO)
* Recht auf Widerruf einer Einwilligung (Art. 7 Abs. 3 DSGVO)
* Recht auf Beschwerde bei einer Aufsichtsbehörde (Art. 77 DSGVO)

Um eines der genannten Rechte auszuüben, können Sie sich jederzeit formlos an uns wenden (z.B. per E-Mail an info@linn.games).

Wir nehmen den Schutz Ihrer personenbezogenen Daten ernst und treffen umfassende technische und organisatorische Maßnahmen, um ein angemessenes Schutzniveau zu gewährleisten:

* Verschlüsselung: Die Übertragung von Daten zwischen Ihrem Endgerät und unseren Servern erfolgt stets verschlüsselt über das SSL/TLS-Protokoll.
* Zugriffskontrolle: Unsere Server sind durch Firewalls und Zugangsbeschränkungen abgesichert.
* Datenminimierung: Wir erheben nur erforderliche Daten und speichern Passwörter ausschließlich in gehashter Form.

Unsere Website verwendet Cookies ausschließlich in technisch notwendigem Umfang. Wir verwenden keine Tracking- oder Werbe-Cookies.

Eingesetzte Cookies:

* `laravel_session`: Session-Verwaltung (technisch notwendig)
* `XSRF-TOKEN`: CSRF-Schutz (technisch notwendig)
* `laravel_cookie_consent`: Speicherung Ihrer Cookie-Einwilligung

Diese Website wird auf einem selbst betriebenen Server (Synology NAS) im Inland gehostet. Es werden keine externen Hosting-Provider eingesetzt.

Wir verwenden eingebundene Drittdienste, um bestimmte Funktionen wie z.B. die KI-gestützte Recherche für eingeloggte Nutzer zu ermöglichen:

* Langdock (KI-Recherche-Agent): Für eingeloggte Nutzer wird ein KI-gestützter Recherche-Dienst über Langdock bereitgestellt. Dabei werden Projekt- und Nutzerdaten via Webhook an Langdock übermittelt. Rechtsgrundlage: Art. 6 Abs. 1 lit. b DSGVO (Vertragserfüllung).

Eingeloggte Nutzer können jederzeit eine vollständige Kopie ihrer personenbezogenen Daten herunterladen oder ihr Konto samt allen verknüpften Daten löschen:

* Datenexport: Über den Menüpunkt „DSGVO-Export" im eingeloggten Bereich können Sie alle Ihre Daten als JSON-Datei herunterladen.
* Kontolöschung: Über „Konto löschen" werden Ihr Konto und alle damit verknüpften Daten unwiderruflich gelöscht.

Stand dieser Datenschutzerklärung: März 2026

---

### resources/views/legal/impressum.blade.php
Die Datei `resources/views/legal/impressum.blade.php` ist eine Blade-Vorlage, die für den Inhalt der Impressumsseite auf unserer Website verantwortlich ist. Diese Datei definiert einen Layout-Block namens "legal", der als Basis für die Impressumsseite dient.

In dieser Datei werden folgende Funktionen implementiert:

* Die Verwendung des `extends`-Schlüssels wird verwendet, um den `layouts/legal`-Layout-Block zu erben und eine neue Impressumsseite zu erstellen.
* Der `@section`-Schlüssel wird verwendet, um die Inhalte der Impressumsseite zu definieren. Hier werden die wichtigsten Informationen wie Name des Verantwortlichen, Kontakt und Berufsbezeichnung sowie die Umsatzsteuer-ID angegeben.
* Die `@endsection`-Anweisung endet die Definition der `@section`-Anweisung.

Darüber hinaus enthält diese Datei auch eine `<a href="/dsgvo.html">Datenschutzerklärung</a>`-Verknüpfung und eine `<a href="/AGB.html">Allgemeine Geschäftsbedingungen</a>`, die auf andere Webseiten im gleichen Ordner verweisen.

Dieser Code verwendet keine externe Abhängigkeiten, da er Teil unseres eigenen Web-Applikations-Backends ist und alle benötigten Funktionen bereits implementiert sind.

---

### resources/views/livewire/auth/confirm-password.blade.php
This file is a Blade template for the "Confirm Password" page in an application. It uses the `livewire` namespace to define the component and its dependencies. The component consists of a form with a password input field, a submit button, and some error handling using Laravel's built-in validation.

The file also imports several other components from the `laravel/ui` package, including `x-layouts.auth`, `x-auth-header`, and `x-auth-session-status`. These components are likely used to define the layout and user interface for the entire authentication system in the application.

The file also defines a route named "password.confirm.store" that is associated with the form's submit button. When this route is accessed, it will handle the form submission and validate the password input field. If the validation passes, it will attempt to confirm the user's password and redirect them to the appropriate page.

Overall, this file appears to be a part of an authentication system in Laravel, with the purpose of allowing users to confirm their passwords before accessing certain parts of the application.

---

### resources/views/livewire/auth/forgot-password.blade.php
This is a Livewire component for the "Forgot Password" page of a web application. The component uses the `x-auth-header` and `x-auth-session-status` components from the `x-layouts.auth` file, which are both part of the Livewire framework.

The component renders an email input field and a submit button for the user to enter their email address and request a password reset link. The email address is validated using the `@error('email')` directive, and if there are any errors, they are displayed next to the input field.

After submitting the form, the component will send an email with a password reset link to the user's registered email address. If the email is successfully sent, the component will display a success message using the `@if($success)` directive, and if there are any errors, they will be displayed next to the submit button.

The component also includes a "Return to" link that takes the user back to the login page after requesting a password reset link. The link is rendered using the `wire:navigate` directive, which is part of Livewire's routing features.

---

### resources/views/livewire/auth/login.blade.php
Die Datei `resources/views/livewire/auth/login.blade.php` enthält eine Laravel Livewire-Komponente für die Anmeldung von Benutzern. Die Komponente verwendet die `x-layouts.auth`-Wrapper-Klasse, um eine authentifizierte Sitzung zu starten und eine `form`-Elemente mit verschiedenen Feldern zu erstellen, um die Anmeldeinformationen des Benutzers einzugeben.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* Die `x-layouts.auth`-Wrapper-Klasse wird verwendet, um eine authentifizierte Sitzung zu starten und eine `form`-Elemente mit verschiedenen Feldern zu erstellen.
* Die `Route::has('register')` -Funktion wird verwendet, um festzustellen, ob ein Benutzer registriert wurde oder nicht. Wenn dies der Fall ist, wird ein Link zum Registrieren des Benutzers angezeigt.
* Die `Route::has('password.request')` -Funktion wird verwendet, um festzustellen, ob ein Benutzer sein Passwort vergessen hat oder nicht. Wenn dies der Fall ist, wird ein Link zum Zurücksetzen des Passworts angezeigt.
* Die `old()`-Funktion wird verwendet, um den Wert der Felder "email" und "password" aus dem vorherigen Formular-Bereitstellungsversuch zurückzusetzen.

---

### resources/views/livewire/auth/register.blade.php
Die Datei `resources/views/livewire/auth/register.blade.php` enthält eine Blade-Template-Datei, die für die Registrierung von Benutzern verwendet wird. Die wichtigsten Funktionen der Datei sind:

* Es gibt eine `<x-layouts.auth>` -Komponente, die ein Layout für die Authentifizierung bereitstellt.
* Es gibt einen `<form>` -Tag, der für die Registrierung verwendet wird.
* Es gibt Input-Felder für Name, E-Mail und Passwort.
* Es gibt eine Schaltfläche zum Abschließen des Formulars.
* Es gibt einen Link zur Anmeldeseite.

Die Datei importiert auch die `<x-auth-header>`, `<x-auth-session-status>` und `<x-layouts.auth>` -Komponenten, die für die Authentifizierung verwendet werden.

---

### resources/views/livewire/auth/reset-password.blade.php
The provided file is a Blade template for a reset password page in a Laravel application. It uses the Livewire component framework to render the form.

Here are the main components of the file:

1. `<x-layouts.auth>`: This is the root element of the file, which represents the parent layout component for this page. It includes a header, a form, and a footer.
2. `<div class="flex flex-col gap-6">`: This div element contains all the other components in the file. It is used to style the form as a column with a 6px gap between each child element.
3. `<x-auth-header>`: This component renders the header section of the page, which includes the title and description.
4. `@csrf`: This is a Blade directive that adds a hidden CSRF token field to the form. It is used to prevent cross-site request forgery attacks on the reset password endpoint.
5. `<input type="hidden" name="token" value="{{ request()->route('token') }}">`: This input element stores the reset password token in the form, which is passed as a parameter in the route. It is used to verify the validity of the token and prevent unauthorized access to the reset password endpoint.
6. `<div>...</div>`: These div elements contain the email, password, and confirm password input fields. They are styled with the `block` class from Tailwind CSS, which creates a full-width container for each field.
7. `<label for="email" class="block text-sm font-medium text-zinc-700 dark:text-zinc-300">{{ __('Email') }}</label>`: This label element renders the "Email" text in a medium font size, with a zinc-700 color for light mode and a zinc-300 color for dark mode.
8. `<input id="email" name="email" type="email" value="{{ request('email') }}" required autocomplete="email" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500" />`: This input element renders the email field in a rounded rectangle shape, with a zinc-300 border color for light mode and a zinc-700 border color for dark mode. It has a placeholder text, a required attribute, and an autocomplete="email" attribute to enable email autocompletion.
9. `<label for="password" class="block text-sm font-medium text-zinc-700 dark:text-zinc-300">{{ __('Password') }}</label>`: This label element renders the "Password" text in a medium font size, with a zinc-700 color for light mode and a zinc-300 color for dark mode.
10. `<input id="password" name="password" type="password" required autocomplete="new-password" placeholder="{{ __('Password') }}" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500" />`: This input element renders the password field in a rounded rectangle shape, with a zinc-300 border color for light mode and a zinc-700 border color for dark mode. It has a placeholder text, a required attribute, and an autocomplete="new-password" attribute to enable password autocompletion.
11. `<label for="password_confirmation" class="block text-sm font-medium text-zinc-700 dark:text-zinc-300">{{ __('Confirm password') }}</label>`: This label element renders the "Confirm password" text in a medium font size, with a zinc-700 color for light mode and a zinc-300 color for dark mode.
12. `<input id="password_confirmation" name="password_confirmation" type="password" required autocomplete="new-password" placeholder="{{ __('Confirm password') }}" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500" />`: This input element renders the password confirmation field in a rounded rectangle shape, with a zinc-300 border color for light mode and a zinc-700 border color for dark mode. It has a placeholder text, a required attribute, and an autocomplete="new-password" attribute to enable password confirmation autocompletion.
13. `<div class="flex items-center justify-end">`: This div element contains the submit button in a flexbox container with the "items-center" and "justify-end" classes from Tailwind CSS, which aligns the button to the right side of its parent container.
14. `<button type="submit" data-test="reset-password-button" class="inline-flex w-full items-center justify-center rounded-md bg-zinc-900 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-zinc-500 focus:ring-offset-2 dark:bg-white dark:text-zinc-900 dark:hover:bg-zinc-200">{{ __('Reset password') }}</button>`: This button element renders the "Reset password" text in a medium font size, with a zinc-900 background color for light mode and a zinc-700 background color for dark mode. It has a rounded rectangle shape, padding of 4px, and a shadow effect to give it depth and visual interest. The button is also aligned to the right side of its parent container with the "inline-flex" class from Tailwind CSS.

Overall, this file defines a reset password page layout for a Laravel application using Livewire components and Tailwind CSS styles.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
This is a Livewire component for two-factor authentication challenge. The component displays an input field for the user to enter their 2FA code, and it also includes a recovery code input field for users who have lost access to their primary 2FA method.

The component uses Laravel's built-in form handling features and Livewire's event system to handle user input and validation. The `x-data` directive is used to define the component's state, including the 2FA code and recovery code. The `x-bind` directive is used to bind the `required` attribute of the recovery code input field to the `showRecoveryInput` variable, which controls whether the input field is required or not.

The component also includes a custom directive called `@clear-2fa-auth-code.window` that clears the 2FA code and focuses the first input field when the window loads. The `@focus-2fa-auth-code.window` directive is used to focus the first input field when the component is initialized.

The component also includes a custom event called `toggleInput()` that is triggered by the "login using a recovery code" and "login using an authentication code" links. This event toggles the visibility of the recovery code input field and clears any entered data.

Overall, this component provides a simple and secure way for users to authenticate with two-factor authentication.

---

### resources/views/livewire/auth/verify-email.blade.php
This file is a Livewire component for verifying an email address in a Laravel application. It uses the `x-layouts.auth` layout and displays a form to request another verification email or log out of the account. The component also displays a message if the user has already verified their email address.

The important classes, functions, or endpoints in this file are:

* `AuthenticatesUsers`: This is a trait that provides some basic authentication functionality for the application.
* `VerifiesEmails`: This is another trait that provides verification-related functionality for the application.
* `logout`: This is a route that logs out of the current user's account.
* `verification.send`: This is a route that sends another verification email to the user's email address.
* `Auth::user()`: This is a method that retrieves the currently authenticated user object.
* `Auth::logout()`: This is a method that logs out of the current user's account.

The external dependencies in this file are:

* `Livewire`: This is a library that provides a way to create reactive components for the application.
* `Laravel`: This is a web framework that provides many features for building web applications, including authentication and verification.

---

### resources/views/livewire/chat/big-research-chat.blade.php
This file is a Livewire component for the big research chat feature in a web application. It defines a class that extends `Component` and contains several methods to handle user input, send messages, and display chat messages.

The component uses the `Illuminate\Support\Facades\Auth` facade to access the current logged-in user's ID, and the `App\Models\ChatMessage` model to store and retrieve chat messages. It also uses the `App\Models\Webhook` model to handle webhooks and the `Illuminate\Support\Facades\Http` facade to make API requests.

The component defines a public property `$message` of type string, which is used to store the user's input message. It also has a public boolean property `$loading` that indicates whether a message is being sent or not.

The `getWebhook()` method returns the current logged-in user's webhook object, or null if no webhook is configured. The `getChatMessages()` method returns a collection of chat messages for the current user and webhook, sorted by creation date.

The `sendMessage()` method validates the input message, sets `$loading` to true, creates a new chat message with the current user ID, webhook ID, and content, and then sends an API request to the configured call URL with the `prompt` parameter set to the user's input. The response is then processed to retrieve the assistant's content, which is stored in a new chat message. Finally, `$loading` is set back to false and the component dispatches an event to auto-scroll to the bottom of the chat history.

The `clearHistory()` method deletes all chat messages for the current user and webhook.

The template HTML code uses Livewire's `@php` directive to execute PHP code, and the `x-data` directive to initialize a new Livewire component instance. It also includes a few utility classes for styling and animations. The main content of the component is a flexbox container that displays the chat history, the input form, and a loading indicator when necessary.

Overall, this code provides a solid foundation for implementing a big research chat feature in a web application using Livewire.

---

### resources/views/livewire/recherche/phase-p1.blade.php
This file contains a Livewire component for the project phase P1. It includes forms to create and edit various types of entities (Strukturmodellwahl, Komponente, Kriterium, Warnsignal), as well as methods to delete and cancel the editing of these entities. The component also includes a mount method to validate the user's access to the project before rendering the form.

---

### resources/views/livewire/recherche/phase-p2.blade.php
{
  "file_summary": "Maximal 10-zeilige Beschreibung der Datei-Funktionen",
  "classes": {
    "App\\Models\\Recherche\\Projekt": "Eine Instanz dieser Klasse stellt ein Projekt dar.",
    "App\\Models\\Recherche\\P2ReviewTypEntscheidung": "Eine Instanz dieser Klasse stellt eine Entscheidung über einen Reviewtyp dar.",
    "App\\Models\\Recherche\\P2Cluster": "Eine Instanz dieser Klasse stellt ein Cluster dar.",
    "App\\Models\\Recherche\\P2MappingSuchstringKomponente": "Eine Instanz dieser Klasse stellt eine Mapping-Komponente dar.",
    "App\\Models\\Recherche\\P2Trefferliste": "Eine Instanz dieser Klasse stellt eine Trefferliste dar."
  },
  "functions": {
    "mount": "Initialisiert die Komponenten für das Projekt und lädt die Daten.",
    "newRev": "Zeigt ein neues Formular für eine Reviewtyp-Entscheidung an.",
    "saveRev": "Speichert eine neue oder vorhandene Reviewtyp-Entscheidung.",
    "editRev": "Öffnet das Formular für die Bearbeitung einer Reviewtyp-Entscheidung.",
    "deleteRev": "Löscht eine Reviewtyp-Entscheidung.",
    "cancelRev": "Schließt das Formular für die Bearbeitung einer Reviewtyp-Entscheidung und resettet die Werte.",
    "newClu": "Zeigt ein neues Formular für einen Cluster an.",
    "saveClu": "Speichert eine neue oder vorhandene Cluster.",
    "editClu": "Öffnet das Formular für die Bearbeitung eines Clusters.",
    "deleteClu": "Löscht einen Cluster.",
    "cancelClu": "Schließt das Formular für die Bearbeitung eines Clusters und resettet die Werte.",
    "newMap": "Zeigt ein neues Formular für eine Mapping-Komponente an.",
    "saveMap": "Speichert eine neue oder vorhandene Mapping-Komponente.",
    "editMap": "Öffnet das Formular für die Bearbeitung einer Mapping-Komponente.",
    "deleteMap": "Löscht eine Mapping-Komponente.",
    "cancelMap": "Schließt das Formular für die Bearbeitung einer Mapping-Komponente und resettet die Werte.",
    "newTref": "Zeigt ein neues Formular für eine Trefferliste an.",
    "saveTref": "Speichert eine neue oder vorhandene Trefferliste.",
    "editTref": "Öffnet das Formular für die Bearbeitung einer Trefferliste.",
    "deleteTref": "Löscht eine Trefferliste.",
    "cancelTref": "Schließt das Formular für die Bearbeitung einer Trefferliste und resettet die Werte."
  },
  "imports": [
    "Illuminate\\Support\\Facades\\Auth",
    "App\\Models\\Recherche\\Projekt",
    "App\\Models\\Recherche\\P2ReviewTypEntscheidung",
    "App\\Models\\Recherche\\P2Cluster",
    "App\\Models\\Recherche\\P2MappingSuchstringKomponente",
    "App\\Models\\Recherche\\P2Trefferliste"
  ],
  "variables": {
    "pid": "Die ID des aktuellen Projekts."
  },
  "with": [
    "reviewTypen": "Lädt alle Reviewtyp-Entscheidungen für das Projekt.",
    "clusters": "Lädt alle Cluster für das Projekt.",
    "mappingKomponenten": "Lädt alle Mapping-Komponenten für das Projekt.",
    "trefferlisten": "Lädt alle Trefferlisten für das Projekt."
  ]
}

---

### resources/views/livewire/recherche/phase-p3.blade.php
Die Datei `resources/views/livewire/recherche/phase-p3.blade.php` enthält einen Livewire-Komponenten-Code für die Recherche-Phase P3. Die Klasse erweitert die `Component`-Klasse von Livewire und definiert verschiedene Funktionen, um die Datenbankmatrix, Disziplin, GeografischerFilter und GraueLiteratur zu bearbeiten.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* Die `P3Datenbankmatrix`-Klasse für die Datenbankmatrix-Funktionen.
* Die `P3Disziplin`-Klasse für die Disziplin-Funktionen.
* Die `P3GeografischerFilter`-Klasse für den Geografischen Filter.
* Die `P3GraueLiteratur`-Klasse für die Graue Literatur-Funktionen.
* Die `mount()`-Methode, um die Komponenten zu initialisieren.
* Die `newDb()`, `saveDb()`, `editDb()`, `deleteDb()`, `cancelDb()`-Methoden für die Datenbankmatrix.
* Die `newDis()`, `saveDis()`, `editDis()`, `deleteDis()`, `cancelDis()`-Methoden für die Disziplin.
* Die `newGeo()`, `saveGeo()`, `editGeo()`, `deleteGeo()`, `cancelGeo()`-Methoden für den Geografischen Filter.
* Die `newGrau()`, `saveGrau()`, `editGrau()`, `deleteGrau()`, `cancelGrau()`-Methoden für die Graue Literatur.

Die Datei importiert auch die notwendigen Abhängigkeiten, wie z.B. die Modelle für die Datenbankmatrix, Disziplin, Geografischer Filter und Graue Literatur, sowie die Auth-Facade von Laravel.

---

### resources/views/livewire/recherche/phase-p4.blade.php
This file is a Livewire component for a project management system. It uses the Laravel framework and includes functionality related to three main categories of data: search strings (P4Suchstring), thesaurus mappings (P4ThesaurusMapping), and anpassungsprotokolls (P4Anpassungsprotokoll).

The component uses the `Illuminate\Support\Facades\Auth` class to check if the user is authorized to access the project. If the user is not authorized, it will abort with a 403 status code.

The component also includes functions for creating, editing, and deleting search strings and thesaurus mappings. It uses the `P4Suchstring` and `P4ThesaurusMapping` models to perform these actions. The component also has a function for retrieving data related to these categories of data using the `with()` method.

The component has a form for creating, editing, and deleting anpassungsprotokolls, which uses the `P4Anpassungsprotokoll` model to perform these actions. It also includes functions for saving and canceling changes made to these forms.

---

### resources/views/livewire/recherche/phase-p5.blade.php
Die Datei `resources/views/livewire/recherche/phase-p5.blade.php` enthält eine Livewire-Komponente zur Verwaltung von Daten zu einer Forschungsprojekt-Phase 5 (P5). Die Komponente ist für die Anzeige und Verwaltung der folgenden Informationen zuständig:

* PRISMA-Zahlen (Identifiziert Gesamt, Davon Datenbank Treffer, Graue Literatur, Nach Deduplikation, Ausgeschlossen L1, Volltext geprüft, Ausgeschlossen L2, Eingeschlossen Final)
* Screening-Kriterien (Level, Kriterium Typ, Beschreibung, Beispiel)
* Screening-Tool (Name, Gewählt, Begründung)
* Treffer Screening (Treffer ID, Level, Entscheidung, Ausschlussgrund, Reviewer, Anmerkung)

Die Komponenten hat folgende Funktionen:

* Mount: Führt eine Überprüfung durch, ob der angemeldete Benutzer der Besitzer des Projekts ist und initialisiert die Komponente.
* New Prisma: Öffnet das Formular zum Erstellen einer neuen PRISMA-Zahl.
* Save Prisma: Speichert die aktuellen Werte des Formulars in der Datenbank als neue PRISMA-Zahl.
* Edit Prisma: Öffnet das Formular zum Bearbeiten einer existierenden PRISMA-Zahl.
* Delete Prisma: Löscht eine ausgewählte PRISMA-Zahl aus der Datenbank.
* Cancel Prisma: Schließt das Formular und setzt die Werte zurück, falls Änderungen vorgenommen wurden.
* New Sk: Öffnet das Formular zum Erstellen eines neuen Screening-Kriteriums.
* Save Sk: Speichert die aktuellen Werte des Formulars in der Datenbank als neues Screening-Kriterium.
* Edit Sk: Öffnet das Formular zum Bearbeiten eines existierenden Screening-Kriteriums.
* Delete Sk: Löscht ein ausgewähltes Screening-Kriterium aus der Datenbank.
* Cancel Sk: Schließt das Formular und setzt die Werte zurück, falls Änderungen vorgenommen wurden.
* New Tool: Öffnet das Formular zum Erstellen eines neuen Screening-Tools.
* Save Tool: Speichert die aktuellen Werte des Formulars in der Datenbank als neues Screening-Tool.
* Edit Tool: Öffnet das Formular zum Bearbeiten eines existierenden Screening-Tools.
* Delete Tool: Löscht ein ausgewähltes Screening-Tool aus der Datenbank.
* Cancel Tool: Schließt das Formular und setzt die Werte zurück, falls Änderungen vorgenommen wurden.
* Open Screen: Öffnet das Formular zum Bearbeiten eines existierenden Treffer Screenings.
* Save Screen: Speichert die aktuellen Werte des Formulars in der Datenbank als neues Treffer Screening.
* Cancel Screen: Schließt das Formular und setzt die Werte zurück, falls Änderungen vorgenommen wurden.
* Delete Screen: Löscht ein ausgewähltes Treffer Screening aus der Datenbank.

Die Komponenten importiert folgende externen Abhängigkeiten:

* `App\Models\Recherche\{Projekt, P5Treffer, P5ScreeningKriterium, P5ScreeningEntscheidung, P5ToolEntscheidung, P5PrismaZahlen}` für die Verarbeitung von Modellen und Datenbank-Zugriffe.
* `with()`-Methode für das Hinzufügen von Eigenschaften zur Komponente.

---

### resources/views/livewire/recherche/phase-p6.blade.php
Dies ist eine Livewire-Komponente für die Recherche-Seite (Projekt P6), die es ermöglicht, Qualitätsbewertungen und Lückenanalysen für ein bestimmtes Projekt zu verwalten. Es bietet CRUD-Operationen (Create, Read, Update, Delete) für Qualitätsbewertungen und Lückenanalysen und stellt auch Visualisierungen bereit, um die Ergebnisse der Bewertungen und Analysen darzustellen.

---

### resources/views/livewire/recherche/phase-p7.blade.php
Diese Datei ist ein Livewire-Komponenten-File für die Projekt-Seite der Webanwendung. Sie definiert eine Klasse 'Projekt' als Root-Klasse und enthält verschiedene Funktionen zur Verwaltung von Projekten, darunter CRUD-Operationen für Synthese-Methode, Datenextraktion, Muster & Konsistenz und GRADE.

---

### resources/views/livewire/recherche/phase-p8.blade.php
The file resources/views/livewire/recherche/phase-p8.blade.php is a Livewire component for managing the Phase P8 of a project in the Recherche application. It contains several classes and functions that are responsible for managing different aspects of Phase P8, such as searching, limitations, reproducibility, and update plans.

The file imports several external dependencies, including Livewire, Volt, and Illuminate\Support\Facades\Auth. It also defines several classes, such as Projekt, P4Suchstring, P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung, and P8UpdatePlan.

The main functionality of the file is to provide a form for managing Phase P8 data, including search protocols, limitations, reproducibility assessments, and update plans. The file also includes several functions for creating, editing, deleting, and canceling these forms, as well as retrieving data from the database and updating the project's Phase P8 data.

Overall, this file is an important part of the Recherche application's functionality, as it allows users to manage the various aspects of Phase P8 in a structured and organized way.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
This file is a Blade template for the project detail page in the livewire framework. It uses the `Projekt` model and the `App\Models\Recherche\Phase` model to render the project's details, such as the title, research question, and phases.

The template includes several Livewire components that are used to display the different parts of the project detail page, such as the phase headers, the phase content, and the back button. The `mount()` method is used to set the initial state of the component, including loading the project data and setting the active tab. The `switchTab()` method is used to switch between the different tabs in the tab bar.

The template also includes a few utility functions, such as the `getPhaseStatus()` function that returns the status of a specific phase (either "abgeschlossen" or "in_bearbeitung"). These functions are used to display the appropriate color and icon for each phase in the tab bar.

Overall, this template is designed to provide a flexible and easy-to-use interface for viewing and managing project details. It uses Livewire's reactive programming model to update the user interface as the user interacts with it, and it includes a few helpful utility functions to make it easier to display complex data.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Die Datei `resources/views/livewire/recherche/projekt-liste.blade.php` enthält eine Livewire-Komponente, die Recherche-Projekte anzeigt und erlaubt den Benutzer, einzelne Projekte zu löschen.

Die Komponente verwendet die folgenden Funktionen und Endpunkte:

* `projekte()`: Diese Funktion ruft alle Recherche-Projekte ab, die dem aktuellen Benutzer zugeordnet sind, und sortiert sie nach Erstellungsdatum in absteigender Reihenfolge. Die Funktion gibt eine Liste von Projekten zurück.
* `deleteProjekt($id)`: Diese Funktion löscht ein bestimmtes Recherche-Projekt, indem es die ID des Projekts überprüft und dann das Projekt löscht. Der Benutzer muss über die Berechtigung zum Löschen des Projekts verfügen.
* `with()`: Diese Funktion gibt eine Liste von Variablen zurück, die für die Anzeige der Komponente verwendet werden. Die Variable `projekte` enthält alle Recherche-Projekte, die dem aktuellen Benutzer zugeordnet sind.

Die Komponente verwendet auch einige externe Abhängigkeiten:

* `App\Models\Recherche\Projekt`: Diese Klasse definiert das Modell für Recherche-Projekte und enthält Informationen wie den Titel, die Erstellungsdatum usw.
* `Illuminate\Support\Facades\Auth`: Diese Klasse bietet einen Weg, um auf die Authentifizierungsinformationen des Benutzers zuzugreifen.
* `Livewire\Volt\Component`: Diese Klasse definiert eine Livewire-Komponente und enthält Funktionen wie `render()` und `with()`.

---

### resources/views/livewire/recherche/research-input.blade.php
This is a Livewire component for creating a new research project. The component has a textarea input field where the user can enter the research question or topic, and a submit button to start the research process.

The component uses the `Auth` facade to get the current user's ID, and the `Projekt::create()` method to create a new research project in the database. The `TriggerLangdockAgent` job is dispatched with the user ID, the new project ID, and the research question or topic entered by the user.

After creating the project, the component redirects the user to the project overview page using the `redirect()` method.

The component also uses the `Volt` template engine to render the HTML form and the `wire:submit` directive to handle the form submission. The `wire:model` directive is used to bind the value of the input field to a property in the component, which allows the user to enter text into the field and have it automatically reflected in the component's state.

The component also uses the `wire:loading` directive to display a loading indicator while the project is being created. The `wire:loading.remove` directive is used to hide the loading indicator when the form submission is successful, and the `wire:loading` directive is used to show the loading indicator while the form submission is in progress.

Overall, this component allows users to easily start a new research project by entering their research question or topic into an input field and clicking the submit button. The component handles the creation of the new project in the background and redirects the user to the project overview page after completion.

---

### resources/views/livewire/settings/appearance.blade.php
Diese Datei ist ein Blade-Template für eine Livewire-Komponente, die die Darstellungseinstellungen eines Benutzers anzeigt und ermöglicht, zwischen verschiedenen Themen zu wechseln. Die Komponente verwendet Livewire und stellt die folgenden Funktionen zur Verfügung:

* Einen Switcher, der dem Benutzer erlaubt, zwischen "Light" (Hell), "Dark" (Dunkel) und "System" (automatisch je nach Systemeinstellungen) zu wechseln.
* Eine lokale Speicherung für die gewählte Option, um den Zustand der Komponente zwischen Aufrufen zu halten.
* Ein Skript, das das Thema der Website basierend auf dem gewählten Modus anwendet und den Zustand des Switchers aktualisiert, wenn der Benutzer eine andere Option auswählt.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* `Livewire\Volt\Component`: Die Komponente wird mit dem Livewire-Framework erstellt und verwendet Volt für die Darstellung von HTML-Markup.
* `x-settings.layout`: Die Komponente enthält eine Vorlage, die das Layout der Einstellungen anzeigt.
* `script`: Das Skript in der Datei ruft den Switcher hervor und setzt das Thema der Website basierend auf dem gewählten Modus.

Die Datei importiert auch einige externe Abhängigkeiten, darunter Volt und das Livewire-Framework, um die Funktionalität der Komponente zu ermöglichen.

---

### resources/views/livewire/settings/delete-user-form.blade.php
This is a Livewire component for deleting a user's account. The component has three main functions:

1. `openModal()`: Opens the modal to ask for confirmation to delete the user's account.
2. `closeModal()`: Closes the modal and resets the form data.
3. `deleteUser()`: Deletes the user's account and redirects the user to the homepage. The function first validates the password, then calls the `Logout` action to delete the user's account.

The component also has a few dependencies:

* `App\Livewire\Actions\Logout`: A Livewire action for logging out the user.
* `Illuminate\Support\Facades\Auth`: The Laravel auth facade, used to access the current user and log them out.
* `Livewire\Volt\Component`: The Livewire component class, which is extended by this component.

The component uses Volta, a template engine for Livewire, to define its UI. It includes a few HTML elements:

* A button with the text "Delete account" that calls the `openModal()` function when clicked.
* An `<input>` element with the type "password" and a label that asks for the user's password to confirm they want to delete their account.
* A form with three submit buttons:
	+ A button with the text "Cancel" that closes the modal and resets the form data.
	+ A button with the text "Delete account" that calls the `deleteUser()` function when clicked, which deletes the user's account and redirects them to the homepage.

The component also includes some CSS classes for styling the UI, such as "bg-red-600", "px-4", "py-2", and "text-white".

---

### resources/views/livewire/settings/password.blade.php
This Livewire component is for updating the password of the currently authenticated user. It defines three properties: `current_password`, `password`, and `password_confirmation`. These properties are used to validate the current password, the new password, and the confirmation of the new password. The `updatePassword` method is called when the form is submitted. It validates the input using Laravel's built-in validation rules, and if successful, updates the user's password in the database. After updating the password, it resets the properties and dispatches a custom event to display a success message.

The component also includes an error message for each of the three properties. The `x-action-message` tag is used to display a success message after updating the password.

Overall, this component provides a convenient way for users to update their passwords securely and easily.

---

### resources/views/livewire/settings/profile.blade.php
This file contains a Livewire component that is used to update the user's profile information in a Laravel application. The component is defined in the `Profile.php` file and is used to display a form for the user to enter their name and email address, as well as the option to resend a verification email if the user has not yet verified their email address.

Here are the key features of this Livewire component:

1. The `mount()` method is called when the component is first mounted and is used to set the initial values for the `name` and `email` properties based on the current authenticated user's information.
2. The `updateProfileInformation()` method is called when the form is submitted and is responsible for updating the user's profile information in the database. This method validates the input data using Laravel's built-in validation functionality, updates the user's record in the database, and dispatches an event to notify any other components that may be listening for this update.
3. The `resendVerificationNotification()` method is called when the user clicks on the "Resend Verification Email" link and is responsible for sending a new verification email to the user's email address. This method uses Laravel's built-in email functionality to send the verification email.
4. The component also includes a `deleteUserForm` component that allows the user to delete their own account. This component is not shown in this code snippet, but it would be included if the `auth()->user()` instance has the `CanDeleteAccount` trait applied to it.
5. The component uses Laravel's built-in Livewire functionality to manage the state of the form and display any errors that may occur during validation.
6. The component also includes a `settings.layout` component that is used to display the heading and subheading for the settings section, as well as the delete user form if applicable.

Overall, this Livewire component provides a simple way for users to update their profile information in an application using Laravel's built-in authentication and validation functionality.

---

### resources/views/livewire/settings/two-factor.blade.php
Diese Datei ist eine Livewire-Komponente für die Einstellungen des Two Factor Authentication (2FA) im Laravel-Framework. Sie ermöglicht dem Benutzer das Aktivieren und Deaktivieren von 2FA für sein Konto, sowie das Anzeigen der QR-Code und des Setup-Schlüssels zur Nutzung in einem Authentifizierungs-App.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Component`: Die Basisklasse für alle Livewire-Komponenten im Laravel-Framework. Sie enthält die Methoden für das Aktualisieren von Komponenten und das Verarbeiten von Events.
* `DisableTwoFactorAuthentication`: Die Klasse für das Deaktivieren von 2FA für ein Benutzerkonto.
* `EnableTwoFactorAuthentication`: Die Klasse für das Aktivieren von 2FA für ein Benutzerkonto.
* `ConfirmTwoFactorAuthentication`: Die Klasse für das Bestätigen des Setup-Schlüssels für eine Authentifizierungs-App.
* `Features`: Die Klasse für die Prüfung der verfügbaren Funktionen im Laravel-Framework.
* `Fortify`: Die Klasse für die Implementierung von 2FA in Laravel-Apps.
* `Locked`: Das Attribut für das Sperren von Eigenschaften in einer Livewire-Komponente, um zu verhindern, dass sie vom Benutzer geändert werden.
* `Validate`: Das Attribut für die Validierung von Formularfeldern in einer Livewire-Komponente.
* `Livewire\Volt\Component`: Die Klasse für die Erstellung von Volt-Templates für Livewire-Komponenten.

Diese Datei enthält auch eine Reihe von Eigenschaften und Methoden, um das Two Factor Authentication-System zu verwalten.

---

### resources/views/livewire/settings/webhooks.blade.php
This is a Livewire component that allows the user to configure webhooks for various frontend objects in Langdock's chat system. The component renders two slots, each with a form to input a webhook URL for a specific frontend object. The user can save or remove the configured webhook URLs.

The component uses the `Webhook` model from Laravel to interact with the database and handle the CRUD operations for webhooks. It also uses the `Illuminate\Support\Facades\Auth` facade to get the currently authenticated user's ID, which is used to retrieve or create webhooks for that user.

The component has the following methods:

* `mount`: This method is called when the component is first mounted. It retrieves the current user's webhook URLs and sets the corresponding properties on the component.
* `saveDashboard`: This method is called when the user submits the form for configuring the dashboard chat webhook URL. It validates the input, updates or creates the webhook in the database, and then dispatches an event to indicate that the dashboard webhook has been saved.
* `saveRecherche`: This method is similar to `saveDashboard`, but for configuring the recherche start webhook URL.
* `clearDashboard`: This method is called when the user clicks the "Remove" button for the dashboard chat webhook URL. It deletes the corresponding webhook from the database and updates the component's state accordingly.
* `clearRecherche`: Similar to `clearDashboard`, but for removing the recherche start webhook.

The component also uses the `@error` directive to display error messages if there are any issues with the input, and the `@if` directive to conditionally render certain parts of the form based on whether a webhook URL has been configured or not.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
This file contains a Livewire component that displays the recovery codes for a user's two-factor authentication (2FA) setup. The component uses Laravel Fortify and is part of the Laravel Jetstream application starter kit.

The component has several responsibilities:

1. Loading the recovery codes from the database and displaying them in a list.
2. Regenerating the recovery codes when the user clicks the "Regenerate Codes" button.
3. Displaying an error message if there is an issue loading or regenerating the recovery codes.
4. Validating that the user has enabled 2FA before allowing them to view or regenerate the recovery codes.
5. Updating the user's 2FA recovery codes in the database when they are regenerated.

The component uses several external dependencies, including Laravel Fortify, Livewire, and Volt. These libraries provide the functionality necessary for the component to function correctly.

In terms of its overall functionality, the component is responsible for displaying the user's 2FA recovery codes and allowing them to regenerate them if needed. It also validates that the user has enabled 2FA before allowing them to view or regenerate their recovery codes. Overall, this component helps to ensure that users can securely access their account even if they lose access to their 2FA device.

---

### resources/views/partials/head.blade.php
This is a partial view file for the head tag of an HTML document. It contains meta tags, link elements for CSS and JS resources, and a script element for initializing dark mode based on localStorage.

---

### resources/views/partials/settings-heading.blade.php
Here is a possible summary of the file `resources/views/partials/settings-heading.blade.php` in 10 lines or less:

* This file defines a heading component for the settings page.
* It includes an `<h1>` element with the text "Settings" and a `<p>` element with a subtitle.
* The heading is styled with a horizontal rule to separate it from the rest of the content.
* The text in the heading and subtitle are defined using the `__()` function, which allows for localization of the text.
* This file does not import any external dependencies or access any APIs.
* It is part of the "ui" category in the Laravel project.

---

### resources/views/recherche/index.blade.php
Die Datei `resources/views/recherche/index.blade.php` ist eine UI-Datei, die für die Recherche-Funktion in der Anwendung verwendet wird. Sie enthält eine X-Layout-Komponente, die von Livewire bereitgestellt wird, um die verschiedenen Komponenten der Recherche-Seite einzubinden und zu organisieren.

Die Datei definiert auch die CSS-Klassen und HTML-Attribute für die verschiedenen Komponenten auf der Seite, wie z.B. das Eingabefeld für den Suchbegriff (`<livewire:recherche.research-input />`), den Trennstrich (`<hr class="border-zinc-200 dark:border-zinc-700" />`) und die Projektliste (`<livewire:recherche.projekt-liste />`).

Die Datei verwendet externe Abhängigkeiten, wie z.B. die Livewire-Komponente, um die verschiedenen Komponenten der Recherche-Seite einzubinden und zu organisieren. Es gibt auch eine Beziehung zwischen den verschiedenen Komponenten auf der Seite, wie z.B. das Eingabefeld für den Suchbegriff und der Trennstrich.

Insgesamt ist die Datei `resources/views/recherche/index.blade.php` Teil der Recherche-Funktion in der Anwendung und enthält eine Vielzahl von Komponenten, die zusammenarbeiteten, um die Benutzerfreundlichkeit und Funktionalität der Recherche-Seite zu optimieren.

---

### resources/views/recherche/show.blade.php
This Blade file represents the UI for displaying a single project. It uses the <livewire:recherche.projekt-detail /> component to render the project details.

---

## uncategorized

### Readme.md
This is a readme file for the Linn.Games application. It includes information about the stack, architecture, features, and deployment process.

---

### artisan
This is a PHP script that serves as an executable for the Laravel framework. It does the following:

1. Defines the `LARAVEL_START` constant, which stores the current time in microseconds.
2. Registers the Composer autoloader by requiring the file `__DIR__.'/vendor/autoload.php'`. This allows Laravel to automatically load classes and functions from the vendor directory.
3. Requires the `bootstrap/app.php` file, which sets up the Laravel application.
4. Instantiates an instance of the `Application` class using the `$app` variable.
5. Handles the command using the `$app->handleCommand(new ArgvInput)` method. This allows the user to run commands on the Laravel framework, such as `php artisan migrate` or `php artisan serve`.
6. Exits with the status code returned by the `$app->handleCommand()` method.

In summary, this script serves as a convenient way for users to interact with the Laravel framework from the command line using the `php artisan` command.

---

### basemodel.md
Die Datei basemodel.md enthält ein Modell für eine Datenbank zur Erfassung von Systematischen Reviews in der Medizininformatik. Es beinhaltet die wichtigsten Klassen und Funktionen, die in der Datenbank verwendet werden, sowie einige externe Abhängigkeiten wie die Imports und APIs.

Die wichtigsten Klassen sind:

* Projekte: Diese Klasse enthält Informationen zu den Projekten, die in der Datenbank erfasst werden, wie z.B. Titel, Forschungsfrage, Verantwortlicher und Startdatum.
* Phasen: Diese Klasse beinhaltet Informationen zu den verschiedenen Phasen einer Systematischen Review, wie z.B. die Nummer, das Titel und der Status jeder Phase.
* p1_strukturmodell_wahl: Diese Klasse enthält Informationen zu den Wahlmöglichkeiten für das Strukturmodell in der ersten Phase, wie z.B. welches Modell gewählt wurde und warum.
* p1_komponenten: Diese Klasse beinhaltet Informationen zu den Komponenten des Strukturmodells, wie z.B. die Kürzel und die Beschriftungen der einzelnen Komponenten.
* p1_kriterien: Diese Klasse enthält Informationen zu den Kriterien für die Einschluss-/Ausschlussregeln in der ersten Phase, wie z.B. welches Kriterium verwendet wurde und warum.
* p1_warnsignale: Diese Klasse beinhaltet Informationen zu den Warnzeichen, die in der ersten Phase erkannt wurden, wie z.B. das Warnzeichen, die mögliche Auswirkung und die Handlungsempfehlung.
* p2_review_typ_entscheidung: Diese Klasse enthält Informationen zu den Entscheidungen über den Review-Typ in der zweiten Phase, wie z.B. welcher Typ ausgewählt wurde und warum.
* p2_mapping_suchstring_komponenten: Diese Klasse beinhaltet Informationen zu den Suchstrings und den Komponenten des Mapping-Prozesses in der zweiten Phase, wie z.B. den Suchbegriff und die entsprechende Komponente.
* p2_trefferlisten: Diese Klasse enthält Informationen zu den Trefferlisten in der zweiten Phase, wie z.B. die Anzahl der Treffer und die Einschätzung des Entscheiders.
* p2_cluster: Diese Klasse beinhaltet Informationen zu den Clustern in der zweiten Phase, wie z.B. die ID des Clusters, das Label und die Beschreibung.
* p3_disziplinen: Diese Klasse enthält Informationen zu den Disziplinen in der dritten Phase, wie z.B. die ID der Disziplin und der Art.
* p3_datenbankmatrix: Diese Klasse beinhaltet Informationen zu den Datenbanken und ihren Abdeckungen in der dritten Phase, wie z.B. die ID der Datenbank und die Anzahl der Treffer.
* p3_geografische_filter: Diese Klasse enthält Informationen zu den geografischen Filtern in der dritten Phase, wie z.B. die Region oder das Land und die Sensitivität des Filters.
* p3_graue_literatur: Diese Klasse beinhaltet Informationen zu der grauen Literatur in der dritten Phase, wie z.B. die Quelle, der Typ und die URL.
* p4_suchstrings: Diese Klasse enthält Informationen zu den Suchstrings in der vierten Phase, wie z.B. den Suchbegriff, das Feld und die Anzahl der Treffer.
* p4_thesaurus_mapping: Diese Klasse beinhaltet Informationen zu den Thesaurus-Mapping in der vierten Phase, wie z.B. die ID des Thesaurus und die Beschreibung.

Die wichtigsten Funktionen sind:

* create extension: Diese Funktion erstellt eine neue Erweiterung für PostgreSQL, die notwendig ist um die Datenbank zu erstellen.
* create table: Diese Funktion erstellt eine neue Tabelle in der Datenbank und definiert die Spalten und Datentypen.
* insert into: Diese Funktion fügt neue Daten in die Datenbank ein.
* update: Diese Funktion aktualisiert vorhandene Daten in der Datenbank.
* delete: Diese Funktion löscht Daten aus der Datenbank.
* select: Diese Funktion ruft Daten aus der Datenbank ab.

---

### composer.json
This is a `composer.json` file for a Laravel project. Here's a summary of its contents in 10 lines or less:

* The file defines the basic information and dependencies for a Laravel project, including the version of PHP required and the versions of other packages used by the project.
* It also includes some scripts for running various commands during development, such as `composer install`, `php artisan key:generate`, and `npm run build`.
* The file uses the JSON format and has a strict structure, with keys like `name`, `description`, `keywords`, `license`, `require`, `require-dev`, `autoload`, `scripts`, `extra`, `config`, and `minimum-stability` for organizing its content.
* Some of the packages mentioned in the file, such as Laravel Fortify, Laravel Sanctum, and Livewire Volt, are external dependencies that need to be installed separately using Composer.
* The file also includes a `post-install-cmd` script that runs some additional commands after installing the project.
* Overall, this file is used to define the structure and dependencies of a Laravel project, making it easier to manage and maintain the project over time.

---

### CONTRIBUTING.md
Dies ist eine CONTRIBUTING.md-Datei für das app.linn.games-Projekt, die die Branch-Konvention, den Merge-Fluss und die Arbeitsablauf für Issue besitzt.

---

### deploy.sh
This script is a deployment tool for the `app.linn.games` application, which deploys the code to production and sets up the infrastructure needed for the app to run. It does the following tasks:

1. Pulls the latest images from Docker Hub for PostgreSQL, Redis, and PHP-FPM.
2. Builds the production images for the application using Docker Compose.
3. Starts the required infrastructure services (PostgreSQL, Redis) in detached mode (-d).
4. Waits until PostgreSQL becomes ready before continuing.
5. Ensures that the required Postgres extensions are installed.
6. Creates a database backup and saves it to a file in the `backups` directory.
7. Runs database migrations using Laravel's migration tool.
8. Caches config, routes & views for better performance.
9. Starts all services in detached mode (-d).
10. Verifies that all services are up and running correctly.
11. Sends a deploy notification email using Laravel's mail tool.

The script uses Docker Compose to manage the deployment process, and it requires Docker to be installed on the system where it is run. It also assumes that the `.env` file exists in the same directory as the script, which should contain environment variables for the application.

---

### package.json
Diese Datei ist eine `package.json` Datei, die Informationen über das Projekt enthält. Hier sind die wichtigsten Elemente:

* `private`: Die Datei ist privat und nicht für den Offenlegungsschutz bestimmt.
* `type`: Der Typ der Datei ist `module`.
* `scripts`: Die Datei enthält Skripte, die verwendet werden können, um das Projekt zu erstellen und auszuführen. Hier sind zwei Skripte definiert: `build` und `dev`.
* `dependencies`: Die Datei enthält Abhängigkeiten von anderen Paketen, die für das Projekt benötigt werden. Hier sind einige der wichtigsten Abhängigkeiten aufgeführt:
	+ `@tailwindcss/vite`: Eine Tailwind CSS-kompatible Vite-Version (Version 4.1.11).
	+ `autoprefixer`: Ein Autoprefixer-Paket, das zum Hinzufügen von Präfixen für verschiedene Browser verwendet wird.
	+ `axios`: Ein Axios-Paket, das für die Ausführung von HTTP-Anfragen verwendet wird.
	+ `concurrently`: Ein Concurrently-Paket, das verwendet werden kann, um mehrere Befehle gleichzeitig auszuführen.
	+ `laravel-vite-plugin`: Ein Laravel Vite Plugin-Paket, das verwendet wird, um den Laravel-Vite-Compiler zu erstellen.
	+ `tailwindcss`: Eine Tailwind CSS-Version (4.0.7).
	+ `vite`: Eine Vite-Version (7.0.4).
* `optionalDependencies`: Die Datei enthält optionale Abhängigkeiten, die nicht für das Projekt erforderlich sind, aber installiert werden können, um bestimmte Funktionen zu aktivieren oder zu verbessern. Hier sind einige der wichtigsten optionalen Abhängigkeiten aufgeführt:
	+ `@rollup/rollup-linux-x64-gnu`: Ein Rollup Linux x64 GNU-Paket (Version 4.9.5).
	+ `@tailwindcss/oxide-linux-x64-gnu`: Eine Tailwind CSS Oxide Linux x64 GNU-Version (Version 4.0.1).
	+ `lightningcss-linux-x64-gnu`: Ein LightningCSS Linux x64 GNU-Paket (Version 1.29.1).

Insgesamt handelt es sich um eine `package.json`-Datei, die Informationen über das Projekt und seine Abhängigkeiten enthält.

---

### phpunit.xml
This is a PHPUnit configuration file (`phpunit.xml`). It defines the settings and parameters for running unit tests on a Laravel application. The file specifies the test suites, the source code directories to be tested, the environment variables, and other options.

Here are the main components of the `phpunit.xml` file:

* `<testsuites>`: This element defines the test suites that will be run during testing. In this case, there are two test suites defined: "Unit" and "Feature".
* `<directory>tests/Unit</directory>`: This element specifies the directory where the unit tests are located. The `tests/Unit` directory is a subdirectory of the project root.
* `<directory>tests/Feature</directory>`: This element specifies the directory where the feature tests are located. The `tests/Feature` directory is also a subdirectory of the project root.
* `<source>`: This element defines the source code directories to be tested. In this case, only the `app` directory is specified.
* `<php>`: This element specifies the PHP settings and environment variables for testing. Some of the important settings defined in this section are:
	+ `env name="APP_ENV" value="testing"`: This sets the application environment to "testing".
	+ `env name="APP_KEY" value="base64:6ycWHMJ0B4cldr4ymRVm8vdkSAlH1LKZp2o9P5+uHhU="`: This sets the application key to a random, base64-encoded string.
	+ `env name="APP_MAINTENANCE_DRIVER" value="file"`: This sets the maintenance driver to "file".
	+ `env name="BCRYPT_ROUNDS" value="4"`: This sets the bcrypt round to 4.
	+ `env name="BROADCAST_CONNECTION" value="null"`: This sets the broadcast connection to "null".
	+ `env name="CACHE_STORE" value="array"`: This sets the cache store to "array".
	+ `env name="DB_CONNECTION" value="sqlite" force="true"`: This sets the database connection to "sqlite" and forces it to be used.
	+ `env name="DB_DATABASE" value=":memory:" force="true"`: This sets the database name to ":memory:" and forces it to be used.
	+ `env name="MAIL_MAILER" value="array"`: This sets the mail mailer to "array".
	+ `env name="QUEUE_CONNECTION" value="sync"`: This sets the queue connection to "sync".
	+ `env name="SESSION_DRIVER" value="array"`: This sets the session driver to "array".
	+ `env name="PULSE_ENABLED" value="false"`: This disables pulse.
	+ `env name="TELESCOPE_ENABLED" value="false"`: This disables telescope.
	+ `env name="NIGHTWATCH_ENABLED" value="false"`: This disables nightwatch.

Overall, this file sets up the necessary environment for running unit tests on a Laravel application.

---

### vite.config.js
This file is a Vite configuration file for a Laravel project. It defines the plugins and settings to be used during the development of the project.

Here are the main functions of this file:

1. `import { defineConfig } from 'vite';`: This line imports the `defineConfig` function from the Vite library, which is used to define the configuration for the Vite project.
2. `import laravel from 'laravel-vite-plugin';`: This line imports the `laravel-vite-plugin` library, which provides a set of plugins for Laravel that can be used with Vite.
3. `import tailwindcss from "@tailwindcss/vite";`: This line imports the Tailwind CSS plugin for Vite, which allows to use Tailwind CSS in the project.
4. `export default defineConfig({ ... })`: This line exports the defined configuration as the default export of the file.
5. `plugins: [ ... ]`: This section defines a list of plugins that will be used during the development of the project. In this case, we have two plugins defined: `laravel` and `tailwindcss`.
6. `server: { ... }`: This section defines the server configuration for the Vite project. It includes settings such as CORS (cross-origin resource sharing) and file watcher.
7. `watch: { ignored: ['**/storage/framework/views/**'] }`: This line ignores any changes to files in the `storage/framework/views` directory, which is used by Laravel for storing compiled view templates.

Overall, this file defines the configuration for a Vite project that uses the `laravel-vite-plugin` and Tailwind CSS plugins, and has server settings to enable CORS and ignore changes to files in the `storage/framework/views` directory.

---

### .dockerignore
This is a `.dockerignore` file, which specifies files and directories that should be ignored by the Docker build process. The file lists various types of files and directories that should not be included in the Docker image, such as source code, configuration files, cache files, log files, and other temporary files. The list includes common conventions for ignoring files and directories in a Dockerized application.

The file is typically placed in the root directory of the project and is used by the `docker build` command to exclude unnecessary files from the image build process. It helps reduce the size of the image and improve build times by avoiding the inclusion of large or unnecessary files.

Here are the 10 most important lines in the file:

1. `.git`: Ignore Git source control directory.
2. `.github`: Ignore GitHub specific files.
3. `.gitignore`: Ignore this file itself.
4. `.env`: Ignore environment variable files.
5. `.env.*`: Ignore environment variable files with different extensions.
6. `!env.example`: Exclude the `env.example` file from being ignored.
7. `node_modules/`: Ignore Node.js module directory.
8. `vendor/`: Ignore vendor directory.
9. `storage/framework/cache/`: Ignore cache files generated by Laravel framework.
10. `storage/framework/sessions/`: Ignore session files generated by Laravel framework.

The file also includes several other directories and files that should be ignored, such as the `public/build/` directory, the `public/hot` directory, the `tests/` directory, and several other temporary files.

---

### .editorconfig
This file is an `.editorconfig` file, which is a configuration file for various text editors and IDEs that helps developers maintain consistent coding styles. The file contains several settings that specify the desired coding style for different programming languages and file types.

Here are the key points about this file:

* `root = true`: This line specifies that this is the root configuration file for all sub-directories.
* `[*]`: This section defines the default settings for all files, regardless of their language or type. The settings specified in this section will be applied to all files unless they are overridden by more specific settings later in the file.
* `charset = utf-8`: This setting specifies that all files should use the UTF-8 character encoding.
* `end_of_line = lf`: This setting specifies that all files should use Unix-style line endings (i.e., linefeed characters only).
* `indent_size = 4`: This setting specifies that all files should use a four-space indentation when writing code.
* `indent_style = space`: This setting specifies that all files should use spaces for indentation, rather than tabs.
* `insert_final_newline = true`: This setting specifies that all files should have a final newline character at the end of the file.
* `trim_trailing_whitespace = true`: This setting specifies that all files should have any trailing whitespace removed before saving.
* `[*.md]`: This section defines specific settings for Markdown files. The `trim_trailing_whitespace = false` setting overrides the general setting and prevents trailing whitespace from being removed in Markdown files.
* `[*.{yml,yaml}]`: This section defines specific settings for YAML files (specifically, both `.yml` and `.yaml` file extensions are included). The `indent_size = 2` setting specifies that all YAML files should use a two-space indentation when writing code.
* `[compose.yaml]`: This section defines specific settings for the `compose.yaml` file. The `indent_size = 4` setting overrides the general setting and specifies that the `compose.yaml` file should use a four-space indentation when writing code.

---

### app/Actions/Fortify/CreateNewUser.php
This is a PHP class that implements the `CreatesNewUsers` interface from Laravel Fortify. It is used to validate and create new users for the application.

The class uses the following dependencies:

* `Validator`: A helper class for validating input data.
* `User`: The User model that represents the users in the application.
* `PasswordValidationRules`: A trait that provides password validation rules.

The `create()` method is responsible for creating a new user in the application. It takes an array of input data as its argument, and it validates the input using Laravel's built-in validation system. The validation rules are defined using the `Validator` class, which checks that the `name`, `email`, and `password` fields are present and meet certain criteria (e.g., the password must be at least 8 characters long).

If the validation passes, the method creates a new user instance using the input data and saves it to the database. If the validation fails, an exception is thrown and the error message is displayed to the user.

---

### app/Actions/Fortify/PasswordValidationRules.php
This file contains a trait called 'PasswordValidationRules' that defines a method called 'passwordRules()' which returns an array of validation rules used to validate passwords. The 'Password::default()' method is used to create the password validation rules.

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei `app/Actions/Fortify/ResetUserPassword.php` enthält eine Klasse `ResetUserPassword`, die den Prozess des Zurücksetzens von Passwörtern für Benutzer in der Laravel-Applikation implementiert. Die Klasse implementiert das Contract `ResetsUserPasswords`, was bedeutet, dass sie die Methode `reset()` verwendet, um das Passwort eines Benutzers zurückzusetzen.

Die Klasse enthält auch eine Funktion `passwordRules()`, die die Regeln für das Passwort definiert, bevor es gespeichert wird. Diese Regeln werden mithilfe von Laravel's Validator-Komponente implementiert und beinhalten die Überprüfung der Passwortlänge und den Schutz vor Brute-Force-Angriffen.

Die Klasse `ResetUserPassword` hängt auch von anderen Komponenten ab, wie z.B. `App\Models\User`, um das Passwort des Benutzers zu ändern. Es gibt außerdem einen Import von `Illuminate\Support\Facades\Validator`, der verwendet wird, um die Validierung der Eingaben durchzuführen.

Insgesamt ist diese Datei eine wichtige Komponente der Laravel-Applikation und stellt sicher, dass Benutzer ihre Passwörter zurücksetzen können, indem sie einen gültigen Benutzernamen und das neue Passwort übergeben.

---

### app/Filament/Resources/ContactResource.php
Die Datei `app/Filament/Resources/ContactResource.php` ist ein PHP-Code-File, das eine Klasse für die Interaktion mit dem Modell "Contact" in der Laravel-Anwendung definiert. Die Klasse erweitert die Klasse `Filament\Resources\Resource`, die wiederum von anderen Filament-Komponenten wie Formularen und Tabellen verwendet wird.

Die wichtigsten Funktionen dieser Datei sind:

* Das Definieren des Modells, das von der Klasse `ContactResource` verwendet wird (`protected static ?string $model = Contact::class;`). Dies ist ein Verweis auf die `Contact`-Klasse in der Laravel-Anwendung.
* Die Definition des Navigations-Icons und -Labels für die Resource (`protected static ?string $navigationIcon = 'heroicon-o-envelope';`, `protected static ?string $navigationLabel = 'Kontaktanfragen';`). Dies sind Symbole und Texte, die in der Navigation verwendet werden.
* Die Definition des Formulars für das Erstellen/Bearbeiten von Kontakten (`public static function form(Schema $schema): Schema`) und die Tabelle für die Darstellung einer Liste von Kontakten (`public static function table(Table $table): Table`). Diese Funktionen definieren, wie die Resource-Komponenten auf die Datenbank zugreifen und welche Informationen angezeigt werden sollen.
* Die Definition der Pages für die Interaktion mit den Daten (zum Beispiel "ListContacts" und "ViewContact"). Diese Pages sind Teil des Filament-Frameworks und definieren, wie die Resource-Komponenten auf die Datenbank zugreifen und welche Informationen angezeigt werden sollen.

Die Datei importiert auch verschiedene Filament-Komponenten (`Filament\Forms`, `Filament\Schemas\Schema`, `Filament\Resources\Resource`, `Filament\Tables\Table`) und die Laravel-Modellklasse `Contact`, um die Funktionalität der Resource zu implementieren.

---

### app/Filament/Resources/UserResource.php
This is a PHP file that defines a Filament resource for managing users. The resource provides functionality for listing, creating, editing, and deleting users.

The resource uses the `App\Models\User` model to interact with the database. It also includes several methods for customizing the user interface, such as setting up forms and tables for editing users.

Here are the main features of the resource:

* The `form` method defines a form schema that is used to create and edit users. It contains three fields: `name`, `email`, and `status`. The `status` field allows the user to select one of four options: "trial", "active", "suspended", or "cancelled".
* The `table` method defines a table schema that is used to display a list of users. It contains four columns: `name`, `email`, `status`, and `roles`. The `roles` column displays the name of each user's role, which is determined by the `roles.name` field in the database.
* The `getPages` method returns an array of page definitions for the resource. It includes two pages: `index` and `edit`. The `index` page lists all users and allows filtering and searching based on the status and role fields. The `edit` page allows editing a specific user's information.
* The `navigationIcon` property is set to `'heroicon-o-users'` to use a heroicon icon for the resource in the navigation menu.
* The `navigationLabel` property is set to `'Nutzer'` to use a custom label for the resource in the navigation menu.

---

### app/Http/Middleware/TrackPageView.php
This is a PHP class file for the `TrackPageView` middleware. It defines a middleware class that tracks page views in an application and logs them to the database. The middleware is attached to the HTTP request lifecycle using Laravel's middleware system, and it intercepts incoming requests before they reach the controller.

Here are the main features of this file:

1. It uses the `Closure` class from the PHP standard library to define a closure that represents the next middleware in the chain.
2. It uses Laravel's `Request` class to access the current HTTP request object, and it checks if the request method is GET using the `isMethod()` method.
3. If the request method is GET and the user agent does not match any of the known bot patterns (e.g., 'bot', 'crawl', etc.), then it creates a new page view record in the database using the `firstOrCreate()` method from the Eloquent ORM. It anonymizes the IP address before saving it to the database, and it saves the user agent as a substring of up to 500 characters.
4. The middleware then increments the `visits` counter for the page view record in the database using the `increment()` method.
5. Finally, it returns the response from the next middleware in the chain using the `$next($request)` method.

This middleware is useful for tracking page views and analytics in a Laravel application. It allows you to track how many times a particular page or route is visited by users, which can be useful for optimizing the user experience or identifying popular content on your website.

---

### app/Http/Middleware/VerifyLangdockSignature.php
This file is part of a Laravel application and defines a middleware class called `VerifyLangdockSignature`. The class provides functionality to validate the signature of incoming webhook requests from the Langdock service.

The `handle` method takes two arguments: an instance of the `Request` class and an anonymous function called `$next`. It first retrieves the secret key for verifying signatures from the `config()` helper function, which is defined in the `Illuminate\Support\Facades\Config` class. If the secret key is not configured, it aborts with a 500 error code and an error message.

The method then retrieves the signature and timestamp from the incoming request headers using the `Request::header()` method and checks if they are present. It also checks if the timestamp is within the allowed tolerance (five minutes) and that it is a valid integer. If any of these conditions are not met, it aborts with an error code and message.

The next step is to calculate the expected signature using the `hash_hmac()` function from PHP's `hash` extension. The `hash_hmac()` function takes three arguments: the hashing algorithm (in this case, SHA256), the data to be hashed (a concatenation of the timestamp and the request body), and the secret key.

The method then compares the expected signature with the one received in the `X-Langdock-Signature` header using the `hash_equals()` function, which is a secure way to compare two strings without leaking timing information. If the signatures do not match, it aborts with an error code and message.

The method then checks if the signature has already been seen by checking the cache for a nonce key (a unique identifier) that is based on the received signature. If the nonce is found in the cache, it aborts with a duplicate request error code and message. Otherwise, it puts the nonce in the cache and passes the request to the `$next` anonymous function.

Overall, this middleware class provides a way to verify the integrity of incoming webhook requests from Langdock using the signature header and a secret key. It prevents replay attacks by checking if the signature has already been seen and by using a cache nonce to prevent duplicate requests.

---

### app/Http/Middleware/VerifyMcpToken.php
Die Datei "VerifyMcpToken.php" enthält eine Klasse namens "VerifyMcpToken", die als Middleware-Komponente verwendet wird, um den Bearer-Token des Benutzers zu überprüfen und zu validieren. Die Klasse importiert den Laravel-Container (Closure) und die Symfony HTTP Foundation Response-Klasse.

Die `handle`-Methode der Klasse ist der Einstiegspunkt für das Middleware, bei dem die Anforderung verarbeitet wird. Hier gibt es folgende Schritte:

1. Der Autorisierungs-Header des Benutzers wird aus der Anfrage gelesen und in eine Variable gespeichert.
2. Wenn der Authorization-Header nicht vorhanden ist oder nicht mit "Bearer " beginnt, wird ein 401 Unauthorized-Fehler zurückgegeben.
3. Der Token-Wert aus der Konfigurationsdatei wird gegen den Wert des Authorization-Headers in der Anfrage gecheckt. Wenn das Muster nicht übereinstimmt, wird ein 401 Unauthorized-Fehler zurückgegeben.
4. Wenn der Token-Wert korrekt ist, wird die Anfrage an den nächsten Middleware-Komponente weitergeleitet.

Die Klasse importiert auch die `config`-Funktion aus dem Laravel-Container, um auf die Konfigurationseinstellungen zuzugreifen. Diese Einstellungen enthalten den Authorization-Token für den Zugriff auf das MCP-System (Maximum Confidence Points). Wenn der Token nicht konfiguriert ist, wird ein 500 Internal Server Error-Fehler zurückgegeben.

Insgesamt stellt die Klasse eine einfache Möglichkeit dar, den Bearer-Token des Benutzers zu überprüfen und zu validieren, bevor er auf das MCP-System zugreift.

---

### app/Jobs/IngestPaperJob.php
This is a PHP file that defines a job for processing text data using the Ollama API. The job class, `IngestPaperJob`, implements the `ShouldQueue` interface and has several public properties, including `paperId`, `source`, `title`, `text`, `projektId`, and `metadata`.

The `handle()` method is where the magic happens. It takes the given text data and splits it into chunks using the `chunkText()` method. The `chunkText()` method first trims the input string, then splits it into an array of words using a regular expression. Finally, it slices the array into smaller chunks with size `CHUNK_SIZE` and overlaps each chunk by `CHUNK_OVERLAP`.

Once the chunks are created, the job sends each chunk to the Ollama API for embedding using the `Http::post()` method. The response from the API is then parsed and the resulting embedding vector is inserted into a database using an SQL INSERT statement with a prepared value for the embedding.

The `fail()` method is used to handle errors that may occur during the job processing. It logs an error message and marks the job as failed.

This file defines several external dependencies, including `Illuminate\Support\Facades\DB`, `Illuminate\Support\Facades\Http`, and `Illuminate\Support\Str`. These classes are part of the Laravel framework's facade system and provide a convenient way to interact with various components of the application without having to instantiate them directly.

---

### app/Jobs/TriggerLangdockAgent.php
Die Datei `TriggerLangdockAgent.php` in der Laravel-Applikation stellt eine Klasse dar, die den Job `TriggerLangdockAgent` implementiert. Diese Klasse ist für das Senden von Anforderungen an den Langdock API Server zuständig.

Die wichtigsten Funktionen dieser Datei sind:

1. Die Definition der Klasse `TriggerLangdockAgent`, die eine Implementierung des Jobs ist, der von Laravel unterstützt wird.
2. Das Definieren der Eigenschaften und Methoden für die Klasse, wie z.B. `tries`, `backoff` und `uniqueFor`, die Bestimmungen für das Versenden der Anforderung an den Langdock API Server festlegen.
3. Die Definition des Konstruktors, der die Parameter `userId`, `projektId` und `eingabe` annimmt und diese in der Klasse speichert.
4. Die Definition der Methode `uniqueId`, die den Projekt-ID-Parameter als eindeutige ID für den Job festlegt.
5. Die Definition der Methode `handle`, die die Anforderung an den Langdock API Server sendet und die Response verarbeitet. Hierbei wird auf Fehler vom Typ `RuntimeException` geachtet und eine Log-Meldung erzeugt, wenn die Anforderung fehlschlägt.
6. Die Verwendung von Laravel-Facades wie `Http`, `Log` und `Config` zum Senden der Anforderung an den Langdock API Server und dem Verarbeiten der Response.

---

### app/Livewire/Actions/Logout.php
The provided code is a Livewire action for logging out the current user of an application. The class `Logout` defines a method `__invoke()` that performs the logout operation.

Here's a summary of the file in 10 lines or less:

* This file contains a Livewire action for logging out the current user of an application.
* The class `Logout` defines a method `__invoke()` that performs the logout operation.
* The method first logs the user out of the authentication guard using `Auth::guard('web')->logout()`.
* It then invalidates the session using `Session::invalidate()`, regenerates the token using `Session::regenerateToken()`, and redirects to the homepage using `redirect('/')`.
* The file imports the necessary classes from Laravel's support package, specifically `Illuminate\Support\Facades\Auth` and `Illuminate\Support\Facades\Session`.

---

### app/Mail/ContactConfirmationMail.php
Die Datei `app/Mail/ContactConfirmationMail.php` enthält eine Klasse, die eine E-Mail mit einem Bestätigungslink für die Kontaktanfrage eines Benutzers sendet. Die Klasse implementiert die `Mailable`-Schnittstelle und verwendet die Funktionen von Laravel's Mail-System.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `ContactConfirmationMail`: Eine Klasse, die eine E-Mail mit einem Bestätigungslink für die Kontaktanfrage eines Benutzers sendet. Sie verwendet die Funktionen von Laravel's Mail-System und implementiert die `Mailable`-Schnittstelle.
* `__construct()`: Die Konstruktorfunktion der Klasse, die den `Contact`-Modell-Objekt als Parameter erhält und es im Objekt speichert.
* `envelope()`: Eine Funktion, die ein neues `Envelope`-Objekt erstellt und das Subject der E-Mail festlegt.
* `content()`: Eine Funktion, die ein neues `Content`-Objekt erstellt und die View-Datei `emails.contact-confirmation` als Inhalt verwendet.
* `view`: Die View-Datei, die den Inhalt der E-Mail darstellt. Hier wird eine E-Mail mit einem Bestätigungslink für die Kontaktanfrage eines Benutzers erstellt.

Diese Datei hat keine externes Abhängigkeiten, da sie nur auf Laravel's Mail-System und das `Contact`-Modell basiert.

---

### app/Mail/ContactInquiryMail.php
Die Datei `app/Mail/ContactInquiryMail.php` enthält eine Klasse, die von der Basisklasse `Mailable` in Laravel abgeleitet ist und als E-Mail-Klasse fungiert. Die Klasse wird verwendet, um eine neue E-Mail-Nachricht zu erstellen, wenn ein neuer Kontaktanfrage in der Anwendung erstellt wird.

Die Klasse hat folgende Funktionen:

* `__construct`: Der Konstruktor initialisiert die Objekteigenschaften und legt den übergebenen `Contact`-Parameter fest.
* `envelope`: Diese Methode gibt einen E-Mail-Envelope mit dem Betreff "Neue Kontaktanfrage von {Name}" zurück, wobei Name durch den Wert der `$contact->name`-Eigenschaft ersetzt wird. Der Envelope enthält auch die Antwortadresse, die durch den Wert der `$contact->email`-Eigenschaft definiert ist.
* `content`: Diese Methode gibt einen E-Mail-Inhalt zurück, der die Ansicht "emails.contact-inquiry" rendert. Die View ist eine Vorlage für die E-Mail-Nachricht und enthält Informationen wie den Namen und die E-Mail-Adresse des Kontaktanfragers, die Anfrage und den Namen der Seite, von der die Anfrage stammt.

Die Klasse benutzt die `Queueable`-Trait und implementiert die `ShouldQueue`-Schnittstelle, um die Erstellung und Sende von E-Mails in einer Warteschlange zu ermöglichen. Dadurch kann die Anwendung die E-Mail-Nachrichten asynchron und parallel zur restlichen Anwendungsarbeit senden, was die Performance verbessert und Fehler reduziert.

Die Klasse benutzt auch die `SerializesModels`-Trait, um Objekte zu serialisieren und in der Warteschlange zu speichern, wenn sie in eine Warteschlange gesendet werden. Dadurch kann die Anwendung sicherstellen, dass die E-Mail-Nachrichten korrekt und effektiv gespeichert und verarbeitet werden, auch wenn die Anwendung beendet oder heruntergefahren wird.

Die Klasse benutzt auch den `Contact`-Modell-Klassen und den `Envelope` und `Content`-Objekten, um die E-Mail-Nachricht zu erstellen und zu senden.

---

### app/Mail/DeployNotificationMail.php
The file `app/Mail/DeployNotificationMail.php` appears to be a Laravel mailable class that sends an email notification when a deployment is successful. The class extends the `Mailable` class, which provides a convenient way to send emails in a Laravel application.

Here are the main features of the class:

1. It has a constructor that takes two parameters: `deployedAt`, which represents the date and time of the deployment, and `appUrl`, which is the URL of the application.
2. It defines an `envelope` method that returns an instance of the `Envelope` class, which provides information about the recipient of the email. In this case, the subject of the email is set to "Linn.Games — Deployment erfolgreich".
3. It defines a `content` method that returns an instance of the `Content` class, which provides information about the content of the email. In this case, the view is set to "emails.deploy-notification".
4. The class also uses the `Illuminate\Mail\Mailable` trait, which provides additional functionality for sending emails in Laravel.

Overall, this class appears to be used to send an email notification when a deployment is successful, using the `envelope` and `content` methods to define the recipient and content of the email.

---

### app/Policies/ProjektPolicy.php
The given file is a PHP class for a policy that defines access control rules for a project model. The class name `ProjektPolicy` suggests that it is used to handle authorization and access control for projects.

The methods defined in the class are:

* `viewAny(User $user)`: Determines whether a user can view any projects or not.
* `view(User $user, Projekt $projekt)`: Determines whether a user can view a specific project or not.
* `create(User $user)`: Determines whether a user can create a new project or not.
* `update(User $user, Projekt $projekt)`: Determines whether a user can update a specific project or not.
* `delete(User $user, Projekt $projekt)`: Determines whether a user can delete a specific project or not.

The class uses the `App\Models\Recherche\Projekt` and `App\Models\User` models to handle the access control rules. It appears that the policy is used to enforce authorization and access control for projects in a Laravel application.

---

### app/Providers/AppServiceProvider.php
Die Datei `AppServiceProvider.php` ist eine Klasse, die von Laravel bereitgestellt wird und es ermöglicht, dass die App Services registriert werden und booten können. Die Klasse implementiert die `Illuminate\Support\ServiceProvider`-Schnittstelle und enthält Methoden zum Registrieren und Booten von Anwendungsservices.

In der Methode `register()` wird die Gate-Politik für das Modell `Projekt` registriert, indem die Klasse `ProjektPolicy` als Richtlinie für das Modell verwendet wird.

In der Methode `boot()` werden verschiedene Dinge ausgeführt:

* Die Rate-Limitierung für Anfragen zum Endpunkt `/mcp` wird konfiguriert, indem eine Limit-Methode aufgerufen wird, die 60 Anfragen pro Minute für einen bestimmten Bearer-Token oder IP-Adresse zulässt.
* Die Rate-Limitierung für Anfragen zum Endpunkt `/webhooks` wird konfiguriert, indem eine Limit-Methode aufgerufen wird, die 30 Anfragen pro Minute für eine bestimmte IP-Adresse zulässt.
* Wenn die App in der Produktion läuft, wird die URL mit `https` erzwungen.

Insgesamt bietet diese Datei eine Möglichkeit, dass Anwendungsservices registriert und booten können, indem sie von Laravel bereitgestellt wird.

---

### app/Providers/FortifyServiceProvider.php
This is a Laravel service provider that provides the functionality for resetting user passwords using the Fortify authentication system. The provider is registered in the `config/app.php` file and is responsible for configuring the actions, views, and rate limiting of the Fortify authentication process.

The `configureActions()` method sets the actions to be used for creating new users (`CreateNewUser`) and resetting user passwords (`ResetUserPassword`). These actions are classes that implement the `Laravel\Fortify\Contracts\UpdatesUserPasswords` interface.

The `configureViews()` method sets the views to be used for different Fortify authentication stages, such as login, verification, two-factor challenge, confirm password, register, reset password, and forgot password. These views are livewire components that render the respective forms and UI elements.

The `configureRateLimiting()` method sets rate limiting for the login and two-factor authentication stages. The `RateLimiter` class is used to define the rate limits for each stage, based on the IP address of the client making the request. The `Limit` class is used to define the maximum number of requests that can be made within a given time period (minute in this case).

Overall, this service provider is responsible for configuring and customizing the Fortify authentication system, allowing users to reset their passwords securely and efficiently.

---

### app/Providers/VoltServiceProvider.php
Die Datei `app/Providers/VoltServiceProvider.php` ist ein Service-Provider-Klasse für Livewire-Templates in der Laravel-Anwendung.

Sie enthält folgende Funktionen:

* Registriert die Klasse `Livewire\Volt\Volt` als Dienst, indem sie die Methode `register()` überschreibt und die Klasse einbindet.
* Bootstrap-Methode, die die Methode `boot()` überschreibt. Hier wird die Mount-Methode von Livewire Volt verwendet, um die Views-Ordner zu mounten, indem man die Methoden `config('livewire.view_path', resource_path('views/livewire'))` und `resource_path('views/pages')` aufruft.

Diese Datei ist Teil der Laravel-Anwendung und dient zur Unterstützung von Livewire Volt Templates.

---

### app/Providers/Filament/AdminPanelProvider.php
This file is part of the Filament administration panel for Laravel. It defines a custom `PanelProvider` class called `AdminPanelProvider`, which extends the `Filament\PanelProvider` class and provides the configuration for the admin panel.

The `panel()` method returns an instance of the `Panel` class, which represents the admin panel, and sets its default configuration using the `default()` method. The `id` parameter is set to "admin", indicating that this is the admin panel. The `path` parameter is set to "admin", indicating that the URL path for accessing the admin panel is "/admin".

The `login()` method is used to specify the login page for the admin panel. It returns an instance of the `LoginPage` class, which represents the login page, and sets its default configuration using the `default()` method. The `id` parameter is set to "login", indicating that this is the login page.

The `discoverResources()` method is used to automatically discover resources in a given namespace. In this case, it is looking for resources in the "app_path('Filament/Resources')" directory and registering them under the "App\\Filament\\Resources" namespace.

The `middleware()` method is used to specify the middleware that should be applied to requests to the admin panel. It takes an array of middleware classes as its argument, which are executed in the order they are specified. The `EncryptCookies`, `AddQueuedCookiesToResponse`, `StartSession`, `AuthenticateSession`, `ShareErrorsFromSession`, and `VerifyCsrfToken` middleware classes are used to perform various security-related tasks, such as encrypting cookies, starting a session, authenticating requests, and verifying the CSRF token. The `SubstituteBindings` middleware class is used to substitute route bindings with eagerly resolved ones.

The `authMiddleware()` method is used to specify the authentication middleware that should be applied to requests to the admin panel. It takes an array of middleware classes as its argument, which are executed in the order they are specified. In this case, it is using the `Authenticate` middleware class to authenticate requests.

In summary, this file defines a custom `PanelProvider` class called `AdminPanelProvider`, which provides the configuration for the admin panel, including the login page, resources, and middleware.

---

### bootstrap/app.php
bootstrap/app.php: This file is the entry point for an application built using the Laravel PHP framework. It sets up the application's configuration, middleware, and routes.

---

### bootstrap/providers.php
This file defines the providers that will be loaded by the application. It returns an array of class names for the service providers.

---

### docker/common/nginx/default.conf
This is a configuration file for an Nginx web server, which serves as a reverse proxy for the Laravel application running on PHP-FPM. The following are the main features and functionalities of this file:

1. Server block: This section defines the parameters for the Nginx server block, including the listen address and port (80), the server name (app.linn.games), and the DNS resolver used to resolve service names lazily (127.0.0.11).
2. Root directory: The root directory of the website is set to /var/www/public.
3. Index files: The index files that Nginx should look for are specified as index.php and index.html.
4. Logging: Nginx logs access and error logs to the files /var/log/nginx/access.log and /var/log/nginx/error.log, respectively.
5. Scheme preservation: The scheme of the original request is preserved by setting $real_scheme to $scheme or $http_x_forwarded_proto if it exists.
6. Livewire v3 assets: Nginx acts as a reverse proxy for requests to /vendor/livewire/ and /livewire/, rewriting the URI and passing it to PHP-FPM.
7. Static files with caching: Nginx caches static files (such as images, CSS, SVGs) for up to one year if their extensions are jpg, jpeg, png, gif, ico, css, svg, woff, woff2, ttf, or eot.
8. Build assets: Nginx caches build assets (such as JS files) for up to one year if they do not start with /livewire/ or /vendor/livewire/.
9. Images: Nginx caches images (such as avatars) for up to one year.
10. PHPInfo endpoint: Nginx proxies requests to the phpinfo.php file in the document root, which displays information about the PHP installation.
11. MCP PostgreSQL Endpoint (SSE): Nginx acts as a reverse proxy for SSE connections to the MCP PostgreSQL database. The authorization header is checked using environment variables ($http_authorization or $http_x_api_key), and if unauthorized, a 401 response with an error message is returned.
12. Paper Search MCP Endpoint (SSE): Nginx acts as a reverse proxy for SSE connections to the paper-search MCP database. The authorization header is checked using environment variables ($http_authorization or $http_x_api_key), and if unauthorized, a 401 response with an error message is returned.
13. Message endpoint for paper-search MCP sessions: Nginx proxies messages to the paper-search MCP database using environment variables ($http_authorization or $http_x_api_key) as authorization headers.
14. Ollama Embedding Endpoint (Langdock): Nginx acts as a reverse proxy for requests to the ollama embedding model. The authorization header is checked using environment variables ($http_authorization or $http_x_api_key), and if unauthorized, a 401 response with an error message is returned.
15. Main location block: Nginx uses the main location block to serve static files and proxy requests for dynamic URLs to PHP-FPM.
16. PHP processing: Nginx uses the PHP processing block to process PHP scripts using FastCGI. The PHP-FPM server is specified as php-fpm:9000, and fastcgi_params includes additional parameters that control the request and response headers.

---

### docker/common/nginx/docker-entrypoint.sh
This script is the entry point for the Nginx container, which runs in a Docker environment. It sets up some basic settings and then executes the command provided to it as arguments. The main tasks of this script are:

1. Checking if a template file exists at `/etc/nginx/templates/default.conf.template`. If it does, it copies that file to `/etc/nginx/conf.d/default.conf`. This is useful for generating the Nginx configuration file from a template.
2. Replacing the placeholder `MCP_AUTH_TOKEN` in the generated Nginx configuration file with the value of the `$MCP_AUTH_TOKEN` environment variable, if it is set. This is necessary to authenticate requests to the MCP (Monitoring and Configuration Protocol) endpoint.
3. Executing the command provided as arguments to the script. This is the entry point for the Nginx container, which typically runs in a loop, monitoring the HTTP traffic and serving responses as needed.

The script uses several external dependencies:

1. `/etc/nginx/templates/default.conf.template`: The template file used to generate the Nginx configuration file. This file is mounted from a volume at container startup.
2. `/etc/nginx/conf.d/default.conf`: The generated Nginx configuration file, which is generated by replacing the `MCP_AUTH_TOKEN` placeholder with the value of `$MCP_AUTH_TOKEN`. This file is also mounted from a volume at container startup.
3. `sed`: A command-line text editor used to replace the `MCP_AUTH_TOKEN` placeholder in the Nginx configuration file.
4. `exec`: A shell built-in command that executes a command and replaces the current process with it. This is used to run the Nginx server as the main process of the container.

---

### docker/common/nginx/dockerfile
This file is a Dockerfile for building an Nginx image. It uses the official Alpine Linux Nginx image as its base and copies several configuration files to customize the behavior of the container.

The following are some of the key features and functions of this file:

* `FROM nginx:alpine`: This line specifies that the Dockerfile should build an image based on the official Alpine Linux Nginx image.
* `COPY ./docker/common/nginx/nginx.conf /etc/nginx/nginx.conf`: This line copies a custom Nginx configuration file to the `/etc/nginx/nginx.conf` directory in the container.
* `COPY ./docker/common/nginx/default.conf /etc/nginx/conf.d/default.conf`: This line copies a site configuration file (`./docker/common/nginx/default.conf`) to the `/etc/nginx/conf.d/` directory in the container, where Nginx looks for custom configuration files.
* `COPY ./docker/common/nginx/docker-entrypoint.sh /docker-entrypoint.d/30-inject-secrets.sh`: This line copies a shell script (`./docker/common/nginx/docker-entrypoint.sh`) to the `/docker-entrypoint.d/` directory in the container, which is used to inject secrets at runtime.
* `COPY ./public /var/www/public`: This line copies the contents of the `./public` directory (which contains static public files such as robots.txt, favicon, index.php, and images) to the `/var/www/public` directory in the container.
* `WORKDIR /var/www/public`: This line sets the working directory of the container to `/var/www/public`.
* `EXPOSE 80`: This line exposes port 80 on the container, which is necessary for Nginx to listen and serve traffic.
* `CMD ["nginx", "-g", "daemon off;"]`: This line sets the default command for the container to run Nginx with the `-g` flag set to `daemon off`, which tells Nginx to run in the foreground and not detach from the terminal.

---

### docker/common/nginx/nginx.conf
This is a basic Nginx configuration file for Laravel applications. The main purpose of this configuration is to provide a standardized way of configuring the Nginx server for Laravel applications, with some customization options for specific use cases.

Here are the main features and functionalities of this configuration:

1. **Events**: This section sets up the worker connections to 1024, which is the default value for most web servers. However, it's important to note that this value can be adjusted based on your server's resources.
2. **Http**: This section includes the mime.types file, which defines the MIME types of different file formats. It also sets the default type to application/octet-stream, which is suitable for most web servers.
3. **Trust X-Forwarded headers from Docker and upstream proxies**: This section sets up Nginx to trust the X-Forwarded-For header from Docker and upstream proxies. This is useful when using Nginx as a reverse proxy in a containerized environment, such as Docker.
4. **Enable gzip compression**: This section enables gzip compression for text files, which can help reduce the size of HTTP responses and improve performance.
5. **Sendfile**: This section sets up Nginx to use the sendfile feature, which allows it to transfer file data directly from the server to the client without having to read the entire file into memory first.
6. **Keepalive timeout**: This section sets the keepalive timeout to 65 seconds, which is a reasonable value for most web servers.
7. **File upload limits**: This section sets the maximum size of uploaded files to 100MB, which is a reasonable limit for most websites.
8. **Include**: This section includes all configuration files from the /etc/nginx/conf.d directory, which allows you to easily add or modify configurations as needed.

Overall, this configuration file provides a good starting point for configuring an Nginx server for Laravel applications, with some customization options for specific use cases. It's important to note that this is just a basic configuration and may need to be modified based on your specific requirements.

---

### docker/common/php-fpm/dockerfile
This Dockerfile sets up a PHP 8.4 FPM environment for running Laravel applications. It consists of three stages:

1. The first stage builds the development environment and installs Composer and npm dependencies. This stage is based on the `php:8.4-fpm` image and uses the `builder` stage name.
2. The second stage creates the production environment by copying the PHP extensions and libraries from the builder stage, as well as the application code and built frontend assets. This stage is also based on the `php:8.4-fpm` image and uses the `production` stage name.
3. The third stage creates a testing environment that includes development dependencies. This stage is based on the `testing` stage name and is used for running tests.

The Dockerfile includes the following instructions:

1. `FROM php:8.4-fpm AS builder`: This instruction sets up the first stage as a build environment based on the `php:8.4-fpm` image. The `AS` keyword allows us to specify a custom name for the stage, which is used later in the Dockerfile.
2. `RUN apt-get update && apt-get install -y --no-install-recommends curl unzip libpq-dev libonig-dev libssl-dev libxml2-dev libcurl4-openssl-dev libicu-dev libzip-dev nodejs npm && docker-php-ext-install -j$(nproc) pdo_mysql pdo_pgsql pgsql opcache intl zip bcmath soap`: This instruction installs system dependencies and PHP extensions required for running Laravel applications.
3. `WORKDIR /var/www`: This instruction sets the working directory to `/var/www`, which is the default directory used by Laravel applications.
4. `COPY . /var/www`: This instruction copies the entire Laravel application code into the container at `/var/www`.
5. `RUN mkdir -p storage/framework/cache storage/framework/sessions storage/framework/views storage/logs bootstrap/cache && chmod -R 775 storage bootstrap/cache`: This instruction creates the necessary directories and sets the correct permissions for Laravel applications to function correctly.
6. `RUN curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer && composer install --no-dev --optimize-autoloader --no-interaction --no-progress --prefer-dist`: This instruction downloads and installs Composer, which is used for managing PHP dependencies. It also installs the production dependencies using the `--no-dev` flag to prevent installing development dependencies.
7. `RUN npm ci && npm run build`: This instruction runs the `npm ci` command to install all necessary packages and dependencies, and then runs the `npm run build` command to build frontend assets for production.
8. `FROM php:8.4-fpm AS production`: This instruction sets up the second stage as a production environment based on the `php:8.4-fpm` image.
9. `COPY --from=builder /usr/local/lib/php/extensions/ /usr/local/lib/php/extensions/ && COPY --from=builder /usr/local/etc/php/conf.d/ /usr/local/etc/php/conf.d/`: This instruction copies the PHP extensions and configuration files from the builder stage into the production environment.
10. `RUN mv "$PHP_INI_DIR/php.ini-production" "$PHP_INI_DIR/php.ini"`: This instruction renames the default PHP configuration file to be used in the production environment.
11. `COPY --from=builder /var/www /var/www`: This instruction copies the application code and built frontend assets from the builder stage into the production environment.
12. `RUN chown -R www-data:www-data /var/www`: This instruction sets the correct ownership for the Laravel application files to be used in the production environment.
13. `COPY ./docker/common/php-fpm/entrypoint.sh /entrypoint.sh`: This instruction copies the entrypoint script from the current directory into the container at `/entrypoint.sh`.
14. `RUN chmod +x /entrypoint.sh`: This instruction makes the entrypoint script executable.
15. `ENTRYPOINT ["/entrypoint.sh"]`: This instruction sets the entrypoint of the container to be the entrypoint script at `/entrypoint.sh`.
16. `CMD ["php-fpm"]`: This instruction sets the default command for the container to be running PHP FPM.

---

### docker/common/php-fpm/entrypoint.sh
This file is an entrypoint script for PHP-FPM, which sets up a few things before executing the main command. Here's a breakdown of what it does:

1. It checks if the built frontend assets exist and copies them to the shared volume so that Nginx can serve them.
2. It waits for the database to be ready by repeatedly trying to connect to the database until it succeeds.
3. It runs migrations in production or staging environments using `php artisan migrate --force`.
4. It caches configuration, routes, and views for production environments using `php artisan config:cache`, `php artisan route:cache`, and `php artisan view:cache`.
5. It executes the main command (either PHP-FPM or queue:work).

---

### docker/common/postgres/dockerfile
Dockerfile for PostgreSQL 16 with hypopg and pgvector extensions

---

### docker/common/postgres/init-extensions.sql
This SQL file is used to initialize the PostgreSQL extensions required for the application. The following extensions are created:

* `hypopg`: This extension provides support for "hypo-hazard" queries, which allow for more efficient execution of certain types of queries.
* `pg_stat_statements`: This extension provides detailed information about the performance of SQL statements executed on the database.
* `vector`: This extension provides support for vectorized query execution, which can improve the performance of certain types of queries.

These extensions are required by the application and must be created in order to ensure that the application functions correctly.

---

### docs/overview-2026-04-02_0617.md
Definiert eine Eloquent-Model-Klasse für die Tabelle p4_anpa. Stellt Zugriff auf Daten der Anpassungsvorschläge in Verbindung mit Projekten. Erbt von Illuminate\Database\Eloquent\Model und verwendet HasUuids Trait.

---

### public/index.php
Die Datei `public/index.php` ist der Einstiegspunkt für eine Laravel-Anwendung und enthält die wichtigsten Funktionen zur Initialisierung und Behandlung von Anfragen. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte beschrieben:

* `Illuminate\Foundation\Application`: Die Klasse `Application` ist die Hauptklasse der Laravel-Anwendung und enthält eine Reihe von Funktionen und Eigenschaften für die Behandlung von Anfragen.
* `Illuminate\Http\Request`: Die Klasse `Request` wird verwendet, um HTTP-Anfragen zu behandeln und Informationen darüber zu sammeln.
* `__DIR__.'/../vendor/autoload.php'`: Der Autoloader von Composer wird verwendet, um die notwendigen Klassen zur Laufzeit zu laden.
* `__DIR__.'/../bootstrap/app.php'`: Die Bootstrap-Datei wird verwendet, um die Laravel-Anwendung zu initialisieren und die Behandlung von Anfragen zu starten.
* `$app->handleRequest(Request::capture())`: Diese Funktion behandelt die HTTP-Anfrage und ruft die entsprechenden Aktionen in der Anwendung auf.

Die Datei enthält auch eine Reihe von Variablen, wie z.B. `LARAVEL_START`, die verwendet werden, um die Zeitmarkierung für den Start der Anwendung zu speichern.

Es gibt auch einen externen Abhängigkeits-Import (`vendor/autoload.php`) und eine externe Datei (`storage/framework/maintenance.php`), die verwendet wird, um die Wartungsmodus-Funktion von Laravel zu aktivieren.

---

### public/robots.txt
This file contains instructions for web robots (such as search engine crawlers) on how to interact with the website.

---

### public/site.webmanifest
Die Datei `public/site.webmanifest` ist ein Web App Manifest, das definiert die Eigenschaften und Funktionen einer Web-App für den Browsers. Hier sind die wichtigsten Informationen in der Datei:

* `name`: Der Name der Web-App.
* `short_name`: Ein kurzer Name der Web-App.
* `icons`: Eine Liste von Bildern, die als Symbole verwendet werden können. In dieser Datei gibt es zwei Symbole mit verschiedenen Größen.
* `theme_color`: Die Hintergrundfarbe der Web-App.
* `background_color`: Die Hintergrundfarbe der Web-App, wenn sie im Fokus ist.
* `display`: Gibt an, wie die Web-App angezeigt werden soll. In diesem Fall ist dies "standalone", was bedeutet, dass die Web-App unabhängig von einer anderen Website funktionieren kann.

Es gibt keine externen Abhängigkeiten in dieser Datei, da sie nur definiert, welche Eigenschaften und Funktionen eine Web-App hat, um auf einem Browser installiert zu werden.

---

### public/.htaccess
This .htaccess file configures the URL rewriting module in Apache to redirect all requests to the front controller of a Laravel application.

---

### resources/css/app.css
This file is a CSS stylesheet for the Tailwind CSS framework. It defines several custom variants and themes, as well as some base styles that are applied to all elements in the document.

Here's a summary of the main features of this file:

* Import statements for Tailwind CSS and other dependencies.
* Custom variants for dark mode and a theme with a few colors and font families defined.
* Base styles for various HTML elements, including borders and text colors.

The file does not contain any external dependencies or APIs, but it does import the `tailwindcss` library, which is a popular CSS framework for building responsive websites. The file also imports some views from the `../views` directory and some components from the `../../vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php` directory.

---

### resources/css/welcome.css
This is a CSS file that styles the layout and appearance of a website. It uses various CSS properties such as `margin`, `padding`, `background-color`, `border`, `box-shadow`, `transform`, etc to achieve the desired design.

Here's a breakdown of the code:

1. The first section is the reset rule, which sets all HTML elements to their default styles using the `*` selector. This resets the browser's defaults and ensures that our custom styles are applied consistently across all browsers.
2. Next, we have the global style rules for the entire website. We set the font family, size, weight, line height, and color for the body element, as well as the background color and image for the `body` selector.
3. The `html`, `body`, and `a` selectors are used to define the styles for the HTML document, the `<body>` element, and all links in the document, respectively. We set the font size, line height, and color for these elements.
4. The `h1`, `h2`, `h3`, `h4`, `h5`, and `h6` selectors are used to define the styles for headings of different levels (h1-h6). We set the font size, line height, and color for these elements.
5. The `.btn` selector is used to define the styles for buttons in the website. We set the background color, border radius, padding, margin, and font weight for this element.
6. The `.nav-list` selector is used to define the styles for navigation lists in the website. We set the list style type, padding, margin, and background color for this element.
7. The `.page-wrapper` selector is used to define the styles for the page wrapper element in the website. We set the max width, margin, and padding for this element.
8. The `.hero` selector is used to define the styles for hero sections in the website. We set the background color, padding, margin, and font size for this element.
9. The `.header` selector is used to define the styles for the header section of the website. We set the background color, padding, margin, and font size for this element.
10. The `.menu-toggle` selector is used to define the styles for the menu toggle button in the website. We set the border radius, padding, margin, and font weight for this element.

Overall, this CSS file defines a consistent design language for the entire website, including colors, fonts, margins, paddings, and other styling elements that contribute to the overall look and feel of the site.

---

### resources/js/welcome.js
This is a JavaScript file that contains various functions and event listeners for the Linn Games website. Here are the main functionalities of this file in roughly 10 lines of text:

* The year in the footer is automatically set to the current year.
* The Snake3D card click handler toggles the display of the Snake3D preview element.
* The window loadSnake3DIframe function loads an iframe with the Snake3D game.
* The mobile navigation toggles between showing and hiding the mobile navigation menu.
* The header scroll effect adds a "scrolled" class to the header when scrolling down past 50px, and removes it when scrolling back up.
* The cursor glow effect animates the position of a small glowing cursor on the page.
* Scroll Reveal Animations are used to add a "visible" class to elements as they come into view.
* Smooth scroll for anchor links is implemented, using the IntersectionObserver API to detect when an element comes into view and scrolling to it smoothly.
* Telemetry values animation (simulating live data) updates the text content of telemetry value elements every 3 seconds with random values between a min and max range.
* The contact form submission handler sends an HTTP POST request to "/contact" with the form data as JSON, using async/await to handle the response and errors.

---

### storage/app/private/.gitignore
This file specifies which files or directories should be excluded from the Git repository. It is a global ignore file, meaning it applies to all commits and branches. The file consists of a single line that specifies the pattern '*', which matches any file or directory. Additionally, the second line includes an exception for the '.gitignore' file itself.

---

### storage/app/public/.gitignore
Diese Datei ist ein GitIgnore-File, das festlegt, welche Verzeichnisse und Dateien von der Versionsverwaltung ignoriert werden.

---

### .github/copilot-instructions.md
Diese Datei enthält Informationen zur Software-Architektur der Linn Games-App. Sie beschreibt die Sprache und die wichtigsten Regeln für die Entwicklung, die Verwendung von Tools wie Laravel, Livewire, Filament und Pest. Es gibt auch Anweisungen für den Docker-Workflow und den Git-Workflow.

---

### .github/instructions/app_linn_games_custom.instructions.md
Diese Datei enthält eine Einführung in die wichtigsten Funktionen und Klassen der Anwendung `app.linn.games`. Die Datei beschreibt die Architektur, Konventionen und Kritischen Details des Projekts, die für alle Code-Agenten und Entwickler von Bedeutung sind.

Die wichtigsten Funktionen der Anwendung sind:

* Ein Nutzer kann ein Recherche-Projekt erstellen, indem er eine Forschungsfrage eingibt und durchläuft acht definierte Phasen (P1 bis P8).
* Ein KI-Agent wird asynchron über eine Queue ausgelöst und arbeitet mit der Datenbank via MCP-Protokoll.
* Fachartikel werden heruntergeladen, in Chunks aufgeteilt, via Ollama als Vektoren eingebettet und in pgvector gespeichert.

Die wichtigsten Klassen sind:

* `User`: Das Modell für Benutzer des Projekts.
* `Projekt`: Das Modell für Recherche-Projekte.
* `Phase`: Das Modell für die Phasen einer Recherche.
* `PaperEmbedding`: Das Modell für die pgvector-Einbindungen der Fachartikel.

Die wichtigsten Funktionen sind:

* `CreateContact`: Die Funktion erstellt einen neuen Kontakt und speichert ihn in der Datenbank.
* `UpdateProjekt`: Die Funktion aktualisiert ein Recherche-Projekt und speichert es in der Datenbank.
* `TriggerLangdockAgent`: Die Funktion löst den Langdock-Agent asynchron aus und fügt ihn zur Warteschlange hinzu.
* `IngestPaperJob`: Die Funktion erstellt einen neuen Job zum Einbinden eines Fachartikels in die Datenbank und fügt ihn zur Warteschlange hinzu.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker Dev Workflow for Linn Games — PHP, Nginx, Postgres, Redis

---
