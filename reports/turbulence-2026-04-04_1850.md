---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-04T18:50:25.041658
model: mistral:7b-instruct
mode: turbulence
files_total: 309
run_time_s: 1248.9
---

# Turbulenz-Analyse — 2026-04-04 18:50

## Zusammenfassung

| Metrik | Wert |
|--------|------|
| Dateien gesamt | 309 |
| 🔴 Kritisch (>50%) | 10 |
| 🟡 Mittel (20-50%) | 76 |
| ⬛ Stabil (<20%) | 223 |
| Findings | 3 |
| Redundanzen | 3731 |

## 🔴 Kritische Dateien

### `tests/Feature/Controllers/PaperRagControllerTest.php` — 54% Turbulenz

- Hot-Zone Zeile 31–45 (Peak: 67%)
- Hot-Zone Zeile 74–86 (Peak: 67%)

**[MEDIUM]** Es ist vermischt, da die Funktionalität der API-Anfrage und das Validieren der Antwort in einer Klasse zusammengefasst sind.
> Empfehlung: Die API-Anfragen könnten in eine separate Klasse ausgelagert werden, um die Validierung der Antwort zu erleichtern. Die neue Klasse könnte dann von der Testklasse aufgerufen werden.

**[MEDIUM]** Die Funktionalität der Abfrage an den Ollama-Service ist in diesem Test vermischt, da die Überprüfung des HTTP-Status und die Ausgabe der Fehlermeldung innerhalb des Tests stattfindet.
> Empfehlung: Es wäre sinnvoll, die Abfrage an den Ollama-Service in eine separate Funktion zu extrahieren und dieselbe dann im Test aufzurufen. Die Überprüfung des HTTP-Status und der Fehlermeldung könnten dann innerhalb dieser Funktion implementiert werden.

### `app/Services/UnpaywallService.php` — 53% Turbulenz

- Hot-Zone Zeile 27–55 (Peak: 67%)

**[MEDIUM]** Die Funktionalität zur Abfrage der Unpaywall API ist vermischt mit der Logik für die Verarbeitung des Ergebnisses.
> Empfehlung: Es wäre sinnvoll, die API-Abfrage in eine separate Funktion zu extrahieren und die Verarbeitung des Ergebnisses in einer anderen Funktion. Die Logik für das Schreiben der Logs könnte in einer weiteren Funktion zusammengefasst werden.

### `app/Actions/Fortify/ResetUserPassword.php` — 50% Turbulenz


### `app/Http/Controllers/DsgvoController.php` — 50% Turbulenz


### `app/Mail/ContactInquiryMail.php` — 50% Turbulenz


### `app/Providers/Filament/AdminPanelProvider.php` — 50% Turbulenz


### `database/factories/WorkspaceUserFactory.php` — 50% Turbulenz


### `database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php` — 50% Turbulenz


### `resources/views/dashboard.blade.php` — 50% Turbulenz


### `tests/Feature/Auth/TwoFactorChallengeTest.php` — 50% Turbulenz


## 🔄 Mögliche Redundanzen

- `send_agent_message` ≈ `send_agent_message` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
