---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T11:04:06.743745
model: llama3.1:8b
run_id: llama3.1:8b
mode: overview
files_total: 251
errors: 0
run_time_s: 669.6
---

# Funktions-Übersicht — 2026-04-02_1104

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 251
- **Fehler:** 0
- **Laufzeit:** 669.6s

## api

### app/Http/Controllers/ContactController.php
```json
{
  "file_summary": "Der ContactController verarbeitet Anfragen per POST. Die Validierung der Eingaben erfolgt durch Laravel's Validation-Mechanismus. Nach erfolgreicher Validierung wird eine neue Kontakt-Eintrag im Datenbank-Modell Contact erstellt und zwei Emails versendet: Eine Bestätigung an den Absender und eine Neuigkeitsmail an 'info@linn.games'. Die Funktion gibt ein erfolgreiches Senden der Anfrage als JSON zurück.",
  "important_classes": ["ContactController", "Contact"],
  "important_functions": ["store"],
  "external_dependencies": ["App\Mail\ContactConfirmationMail", "App\Mail\ContactInquiryMail"]
}
```

---

### app/Http/Controllers/Controller.php
Basis-Klassen für die API-Controller. Definiert abstracte Controller-Klasse, die durch spezifische Controller-Klassen erweitert wird.

---

### app/Http/Controllers/DsgvoController.php
```json
{
  "file_summary": "DsgvoController verantwortet die Exportierung von Nutzerdaten und die Löschung des Nutzerkontos. Es werden externe Abhängigkeiten zu Authentifizierungs- (Auth) und Sitzungsverwaltungsfunktionen verwendet.",
  "key_classes": ["DsgvoController", "Request"],
  "important_functions": ["export", "deleteAccount"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth", "Illuminate\Http\Request"]
}
```

---

### app/Http/Controllers/LangdockWebhookController.php
```
{
  "file_summary": "Langdock-Webhook-Controller zur Auslösung von Langdock-Agenten. 
                    Überprüft die Validität der Webhook-Eingabe und dispatcht den Agenten.
                    Wichtige Klassen: TriggerLangdockAgent, wichtiger Endpunkt: handle()
                    Externe Abhängigkeit: App\Jobs\TriggerLangdockAgent"
}
```

---

### app/Http/Controllers/PaperRagController.php
API-Kontroller für Paper-RAID-Funktionen. Zwei Endpunkte: ingest und search.

- ingest: Ein Papier wird in das System eingefügt, indem ein Job erstellt wird.
- search: Eine Suchanfrage wird verarbeitet, indem die Embedding-Suche aufgerufen wird.

Hauptsächliche Klassen/Funktionen: PaperRagController, IngestPaperJob.

Externe Abhängigkeiten: DB-Zugriffe, HTTP-Anfragen an Embedding-Service.

---

### routes/api.php
API-Routen für Langdock-Webhooks und Papierinhaltsverwaltung. Externe Abhängigkeiten: auth:sanctum, VerifyLangdockSignature, VerifyMcpToken.

---

### routes/console.php
```
{
  "file_summary": "Das Skript definiert drei Artisan-Kommandos zur Ausführung von bestimmten Aktionen:\n\
Artisan::command('inspire') - Zeigt ein inspirierendes Zitat an.\n\
Artisan::command('deploy:notify') - Sendet eine Benachrichtigungsmail beim Deploy des Projekts.\n\
Die Kommandos nutzen die Facades von Laravel, um ihre Funktionalität bereitzustellen.",
  "klasse": ["App\Mail\DeployNotificationMail"],
  "funktionen/endpunkte": ["inspire", "deploy:notify"],
  "extern": ["config('app.url')"]
}
```

---

### routes/web.php
Diese Datei definiert Routen für die Web-Applikation, darunter Eingabefelder, Datenexport und Dashboards. Es werden auch externe Abhängigkeiten wie Livewire und Laravel-Fortify verwendet.

---

## config

### docker-compose.dev.yml
Das Docker-Compose-Skript für die lokale Entwicklungsumgebung. Der Port des Nginx-Dienstes wird auf 6480 geändert, sodass er nicht mit dem VS Code-Port überschneidet.

---

### docker-compose.yml
Docker-Konfiguration für eine Anwendung mit mehreren Diensten, darunter ein Webserver, PHP-FPM, PostgreSQL und Redis. Die Services sind mithilfe von Docker Compose konfiguriert und können über Port-Forwarding erreicht werden.

---

### .env.example
Diese Datei enthält Konfigurationswerte für eine Laravel-Anwendung. Sie umfasst Einstellungen wie App-Name, Umgebung, Schlüssel und URL, sowie Datenbank- und Cache-Konfigurationen. Darüber hinaus werden Einstellungen für Sitzungen, Broadcasts, Filesystem und Queue definiert. Zudem sind die Verbindungsdaten zu verschiedenen externen Diensten wie AWS, Redis, Postgres und Mail erhältlich.

---

### config/app.php
```json
{
  "file_summary": "Diese Datei enthält Konfigurationseinstellungen für ein Laravel-Projekt. 
                    Sie umfasst Einstellungen wie Anwendungsname, Umgebung, Debug-Modus, URL und Zeitzone.
                    Die wichtigsten Klassen sind nicht direkt relevant, da dies eine Konfigurationsdatei ist,
                    aber es werden externe Abhängigkeiten verwendet, wie z.B. .env-Dateien.
                    Die Datei enthält mehrere Funktionen für die Anzeige von Einstellungen und die Generierung von URLs."
}
```

---

### config/auth.php
```json
{
  "file_summary": "Konfiguration für Authentifizierung und Passwortschutz mit Einstellungen für Guards, User-Provider und Passwort-Zuweisung",
  "important_classes_functions_endpoints": [
    "defaults", 
    "guards", 
    "providers", 
    "passwords"
  ],
  "external_dependencies": [
    "env() - Umgebungsvariablen-Einträge",
    "App\Models\User::class - User-Modell"
  ]
}
```

---

### config/cache.php
Diese Datei definiert Konfigurationen für das Caching in einer Laravel-Anwendung. Es werden verschiedene Cache-Stores wie Array, Database, File, Memcached und Redis konfiguriert. Die Standard-Cache-Konfiguration kann über Umgebungsvariablen angepasst werden.

---

### config/cookie-consent.php
Konfiguration für die Zustimmungsmaske zum Cookie-Einsatz
- Aktivierung der Zustimmungsmaske über Umgebungsvariable
- Namensangabe des Cookies, in dem die Zustimmung gespeichert wird
- Dauer des Cookies in Tagen

---

### config/database.php
```json
{
  "file_summary": "Konfiguration von Datenbank-Verbindungen und -Einstellungen. Es werden Verbindungseinstellungen für verschiedene Datenbanktypen (z.B. MySQL, PostgreSQL, SQLite) definiert. Die Einstellungen können durch Umgebungsvariablen überschrieben werden.",
  "important_classes": ["Illuminate\Database\DatabaseManager", "Illuminate\Support\Str"],
  "key_functions": ["default", "connections", "migrations", "redis"],
  "external_dependencies": ["env() Funktion für Umgebungsvariable-Auswahl", "database_path() Funktion zur Datenbank-Pfad-Ermittlung"]
}
```

---

### config/filesystems.php
Konfiguration für Dateisysteme, darunter Default-Disk und verschiedene Driver (lokales, FTP, SFTP, S3)

---

### config/fortify.php
Dieser Konfigurationsdatei (config/fortify.php) enthält Einstellungen für das Authentifizierungsframework Fortify. Die Datei definiert die zu verwendende Wache, den Password-Broker und weitere Funktionalitäten wie Zwei-Faktor-Authentifizierung, Passwort-Zurücksetzung und Registrierung. Externe Abhängigkeiten sind Laravel\Fortify\Features und Auth-Konfiguration.

---

### config/logging.php
```json
{
  "file_summary": "Dieser Konfigurationsdatei ist für die Log-Funktionen von Laravel verantwortlich. Sie definiert die Default-Log-Kanal, Deprecations-Log-Kanal und Log-Channels für verschiedene Anwendungen. Die wichtigsten Klassen sind Monolog\Handler\NullHandler, Monolog\Handler\StreamHandler und Monolog\Processor\PsrLogMessageProcessor. Externe Abhängigkeiten sind Laravel's Environment-Variable, Storage-Path und PAPERTRAIL-URL."
}
```

---

### config/mail.php
Diese Datei konfiguriert die E-Mail-Funktionen von Laravel. Es werden E-Mail-Träger wie SMTP, SES und Sendmail unterstützt. Die wichtigsten Klassen sind keine explizit genannten, aber es gibt verschiedene Funktionen für die verschiedenen E-Mail-Träger. Externe Abhängigkeiten sind z.B. das Umgebungsvariable-System (env()).

---

### config/queue.php
Diese Datei konfiguriert die Funktionen von Laravel zur Verwaltung von Aufträgen und Warteschlangen. Die wichtigsten Klassen und Funktionen sind: 'default', 'connections' und 'failed'. Externe Abhängigkeiten sind: .env-Dateien für Umgebungsvariablen.

---

### config/services.php
Diese Datei enthält Konfigurationen für Drittanbieterdienste wie Postmark, AWS, Slack und mehr. Sie definiert Schlüssel und Geheimnisse für diese Dienste und nutzt Umgebungsvariablen für die Speicherung von sensiblen Informationen.

---

### config/session.php
```
{
  "file_summary": "Diese Datei enthält die Konfiguration für Sitzungsverwaltung. Sie definiert den Session-Beitritt, das Sitzungslaufzeitfenster, Verschlüsselung und Speicherort der Sitzungen.",
  "important_classes_functions_endpoints": [
    "env", "storage_path",
    "Str::slug"
  ],
  "external_dependencies": [
    "Illuminate\Support\Str",
    "database"
  ]
}
```

---

### docker/common/php-fpm/php-dev.ini
Die Datei php-dev.ini ist ein Konfigurationsdokument für PHP-FPM. Sie enthält Einstellungen, die bei der Entwicklung von Anwendungen verwendet werden sollen. Die wichtigsten Funktionen umfassen den aktivierten OPcache-Validierungstest und die Anzeige aller Fehler.

---

### docs/api.yaml
API-Dokumentation für die Research-Management-Plattform app.linn.games. Enthält Endpunkte für Webhooks, Paper-Ingestion, Vektorsuche und User-Info. Nutzt Sicherheitsmechanismen wie Bearer-Tokens und HMAC-Signaturen.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Dokumentiert ein Muster für Bug-Reports mit bestimmten Feldern wie Beschreibung, Schritten zur Reproduktion, Schweregrad und Akzeptanzkriterien.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Dieses Template ermöglicht das Einreichen von Feature-Anfragen, die als GitHub Issues erstellt werden können. Es besteht aus mehreren Feldern, darunter Beschreibung, Zusammenfassung, Motivation/Problem, Vorgeschlagene Lösung, Betroffene Bereiche und Akzeptanzkriterien. Die wichtigsten Funktionen sind das Einreichen von Formularen und die Überprüfung der notwendigen Informationen.

---

### .github/ISSUE_TEMPLATE/refactor.yml
Dokumentations-Template für Refactoring-Anträge in GitHub Issues. Definiert Felder wie Beschreibung, betroffene Dateien und Akzeptanzkriterien.

---

### .github/workflows/lint.yml
Diese Datei definiert ein Workflow für die automatische Durchführung von Linting-Analysen auf dem Git-Repository. Dabei werden vor allem PHP-Code und NPM-Abhängigkeiten überprüft, um sicherzustellen, dass der Code an den aktuellsten Standards entspricht. Die wichtigsten Klassen/Funktionen sind "pint" (für PHP-Linting) und "npm install" (für NPM-Abhängigkeiten). Externe Abhängigkeiten werden über die "actions/checkout@v5", "shivammathur/setup-php@v2" und "stefanzweifel/git-auto-commit-action@v7" verwaltet.

---

### .github/workflows/tests.yml
Dieses YAML-File definiert eine GitHub-Automatisierungsworkflow, die vor allem zum Ausführen von Tests verwendet wird. Die wichtigsten Klassen oder Funktionen sind "ci" und das Setup von PHP und Node.js. Externe Abhängigkeiten sind Actions, Composer, npm und Pest.

---

## data_access

### app/Models/ChatMessage.php
Definiert die ChatMessage-Entität, um Nachrichten in einer Chatsystem zu speichern und verwalten. Enthält Eloquent-Methode zum Beziehungen zu User und Webhook aufgebaut.

---

### app/Models/Consent.php
```json
{
  "file_summary": "Die Consent-Datei (app/Models/Consent.php) stellt ein Eloquent-Modell für den Einwilligungsbefehl dar.\n\
Es enthält Eigenschaften wie \"fillable\" und \"casts\" zur Validierung und Datenformatierung sowie Beziehungen zum User-Modell.\n\
Die Datei importiert die \"Illuminate\Database\Eloquent\Model\" und \"Illuminate\Database\Eloquent\Relations\BelongsTo\" Klassen."
}
```

---

### app/Models/Contact.php
Definiert eine Eloquent-Model-Klasse für die Tabelle "contacts". Fügt Daten in die Tabelle ein und ermöglicht das Hinzufügen, Bearbeiten und Löschen von Einträgen.

---

### app/Models/PageView.php
Definiert Eloquent-Model für Datenbanktabelle PageViews, die Informationen über Seitenaufrufe speichert. Importiert Model von Laravel's Eloquent.

---

### app/Models/User.php
Datenmodell für Benutzer, beinhaltet Attribute wie Name, Email und Status. Enthält Funktionen für Initialisierungen und Aktivitätsprotokolle.

---

### app/Models/Webhook.php
Definiert den Webhook-Modell, mit Funktionen zur Einholung der Webhook-URL und zum Finden von Webhooks für einen bestimmten User und Frontend-Objekt. Verwendet Eloquent und hat Beziehungen zu Benutzer und Chat-Meldungen.

---

### app/Models/Recherche/P1Komponente.php
Die Datei P1Komponente.php stellt eine Eloquent-Model-Darstellung für die Datenbank-Tabelle 'p1_komponenten' dar. Sie verfügt über Beziehungen zu der Projekt-Einheit und ermöglicht die Abfrage von Komponentendaten.

---

### app/Models/Recherche/P1Kriterium.php
Eloquent-Modell für Datenbanktabelle 'p1_kriterien'. Definiert Beziehung zu Projekt-Modell über projekt_id. Fügt UUID-Funktionen hinzu.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Die Datei definiert das Model P1Strukturmodellwahl, welches die Beziehungen zwischen Projekten und Strukturmodellen darstellt. Es verwaltet Daten in der Tabelle p1_strukturmodell_wahl und bietet Zugriff auf ein Projekt über die Beziehung mit dem Projekt-Modell.

---

### app/Models/Recherche/P1Warnsignal.php
```
{
  "file_summary": "P1Warnsignal ist ein Eloquent-Model für die Datenbanktabelle p1_warnsignale. Es definiert Beziehungen zu Projekt und ermöglicht den Zugriff auf Warnsignale und ihre Eigenschaften.",
  "key_classes": ["P1Warnsignal"],
  "important_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P2Cluster.php
```
{
  "file_summary": "Datenzugriff-Modell für P2-Cluster-Tabellen. Es umfasst die Beziehung zwischen einem Projekt und einem Cluster.",
  "key_classes_functions_endpoints": ["P2Cluster", "projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
```
{
  "file_summary": "Definiert das Datenmodell für die Tabelle p2_mapping_suchstring_komponenten und beschreibt deren Beziehungen zu anderen Tabellen. Die Datei definiert eine Eloquent-Model-Klasse mit UUID-Support, die sich auf die Tabelle 'p2_mapping_suchstring_komponenten' bezieht und einige attributabhängige Funktionen wie das Projekt-Beziehung bietet.",
  "important_classes": ["P2MappingSuchstringKomponente"],
  "notable_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Datenmodell für die Entscheidung zum Review-Typ in einem Projekt. Definiert Beziehungen zu Projekt-Entitäten und ermöglicht Zugriffe auf Datenbank-Tabelle "p2_review_typ_entscheidung".

---

### app/Models/Recherche/P2Trefferliste.php
Die Datei definiert eine Eloquent-Model-Klasse für Datenbanktabelle p2_trefferlisten. Es handelt sich um ein Model für die Recherche und enthält Beziehungen zu einer Projekt-Tabelle.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Das Modell P3Datenbankmatrix stellt die Beziehung zwischen Projekten und Datenbanken dar. Es bietet Zugriff auf die Projekt-Datenbank-Beziehungen in der Datenbank 'p3_datenbankmatrix'. Es verwendet die Eloquent-Relations, um die Beziehung zu einem zugehörigen Projekt herzustellen.

---

### app/Models/Recherche/P3Disziplin.php
Diese Datei stellt eine Datenbank-Abstraktion für die Tabelle "p3_disziplinen" bereit. Sie ermöglicht den Zugriff auf Projekt-Dissiplinen und enthält Beziehungen zu Projekten.

---

### app/Models/Recherche/P3GeografischerFilter.php
Modell für die geografische Filter-Sammlung in der Datenbank. Es verfügt über Beziehungen zum Projekt und ermöglicht das Casten von Werten.

---

### app/Models/Recherche/P3GraueLiteratur.php
Eloquent-Modell für "p3_graue_literatur" Tabelle
Bezieht sich auf Projekt durch BelongsTo Relation
Erweitert das Model mit HasUuids Konzept
Benutzt Eloquent Relations zur Kommunikation zwischen Modellen

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Datenmodell für P4-Anpassungsprotokolle. Fügt ein Eloquent-Modell hinzu, das Zugriff auf die Tabelle "p4_anpassungsprotokoll" ermöglicht. Inklusive Beziehung zu P4-Suchstrings und Datenkonvertierung.

---

### app/Models/Recherche/P4Suchstring.php
```
{
  "file_summary": "Die Datei P4Suchstring.php definiert eine Eloquent-Model-Klasse für die Datenbanktabelle 'p4_suchstrings'. Sie ermöglicht die Abfrage und Manipulation von Suchstrings und ihren zugehörigen Daten. Die Klasse verfügt über Beziehungen zu Projekt, Anpassungsprotokoll und Suchprotokolle.",
  "klasse": "P4Suchstring",
  "bezogene_klassen": ["Projekt", "P4Anpassungsprotokoll", "P8Suchprotokoll"],
  "externer_zugriff": ["Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P4ThesaurusMapping.php
Einbindung von P4ThesaurusMapping-Modell zur Datenabfrage aus Tabelle p4_thesaurus_mapping. Zugehörige Beziehung zur Projekt-Tabelle definiert.

---

### app/Models/Recherche/P5PrismaZahlen.php
Diese Datei stellt eine Datenbank-Tabelle "p5_prisma_zahlen" dar, die Informationen über Zahlen in einem Prisma-Projekt enthält. Sie umfasst Beziehungen zu einer "Projekt"-Tabelle und ermöglicht den Zugriff auf diese Daten mithilfe von Eloquent.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Die Datei definiert ein Eloquent-Modell für die Datenbank-Tabelle p5_screening_entscheidungen. Sie ermöglicht den Zugriff auf Screening-Entscheidungen und enthält Beziehungen zu Treffern.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Model für Datenzugriff auf Tabelle p5_screening_kriterien. Beinhaltet Beziehungen zu Projekt-Modell.

---

### app/Models/Recherche/P5ToolEntscheidung.php
```
{
  "file_summary": "Die Datei P5ToolEntscheidung.php stellt ein Datenmodell für Entscheidungen im Zusammenhang mit Werkzeugen in einem Projekt dar. Sie enthält eine Tabelle p5_tool_entscheidung, die Felder für das Projekt-Id, das verwendete Tool, die Auswahl und die Begründung beinhaltet.",
  "key_classes": ["P5ToolEntscheidung"],
  "key_methods": ["projekt", "Projekt"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P5Treffer.php
Eloquent-Model für die Datenbank-Tabelle 'p5_treffer'. Es handelt sich um eine Treffer-Entität im Rahmen einer Recherche. Die Klasse verfügt über Beziehungen zu anderen Modellen, wie zum Beispiel dem Projekt und den Duplikaten.

---

### app/Models/Recherche/P6Luckenanalyse.php
```json
{
  "file_summary": "Definiert die Eloquent-Model-Klasse P6Luckenanalyse für die Datenbank-Tabelle p6_luckenanalyse. Die Klasse ermöglicht das Laden und Speichern von Datensätzen. Die wichtigsten Funktionen sind: - Einbindung des projekts, welches ein Beziehung zu Projekt enthält.",
  "main_classes": ["P6Luckenanalyse"],
  "main_functions": ["projekt", "save", "create", "find"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
```json
{
  "file_summary": "Datenmodell für die Tabelle \"p6_qualitaetsbewertung\" mit Beziehung zu \"p5_treffer\"",
  "key_classes": ["P6Qualitaetsbewertung"],
  "important_methods": ["treffer"],
  "external_dependencies": [
    {"name": "Illuminate\Database\Eloquent\Model"},
    {"name": "Illuminate\Database\Eloquent\Relations\BelongsTo"}
  ]
}
```

---

### app/Models/Recherche/P7Datenextraktion.php
Datei definiert Model für Datenextraktion aus der Tabelle p7_datenextraktion.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
```json
{
  "file_summary": "Datenmodell für P7-Grade-Einschätzungen: Entwedererstellt eine Tabelle zur Verknüpfung von Projekten mit Grade-Einschätzungen oder als Teil eines größeren Datenmodells. In dieser Implementierung ist die Verbindung zu einem Projekt (Projekt) hergestellt und die Tabelle wird im Rahmen der Recherche verwendet.",
  "key_elements": {
    "Klassen": ["P7GradeEinschaetzung"],
    "Funktionen": ["projekt()"],
    "Endpunkte": [],
    "externe Abhängigkeiten": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model", "Projekt"]
  }
}
```

---

### app/Models/Recherche/P7MusterKonsistenz.php
Definiert Eloquent-Modell für die Datenbanktabelle "p7_muster_konsistenz". Die Tabelle enthält Informationen zur Konsistenz von Mustern in Projekten.

---

### app/Models/Recherche/P7SyntheseMethode.php
```json
{
  "file_summary": "Die Datei definiert die Datenbank-Tabelle p7_synthese_methode für die Methode P7. Sie enthält eine Beziehung zum Projekt und stellt sicher, dass bestimmte Felder boolean-Werte enthalten.",
  "important_classes_functions_endpoints": [
    "P7SyntheseMethode",
    "projekt() (BelongsTo-Beziehung)",
    "HasUuids"
  ],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Concerns\HasUuids",
    "Illuminate\Database\Eloquent\Model",
    "Illuminate\Database\Eloquent\Relations\BelongsTo",
    "Projekt::class (BelongsTo-Beziehung)"
  ]
}
```

---

### app/Models/Recherche/P8Limitation.php
Das Modell P8Limitation stellt eine Tabelle in der Datenbank dar und bietet Zugriffsmethoden auf die zugehörigen Projektinformationen. Die Klasse verwendet den uuid-Mechanismus von Eloquent und ermöglicht das Auslesen von Limitationstypen, Beschreibungen und deren Beziehung zu Projekten.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Diese Datei definiert eine Eloquent Model-Klasse für die Tabelle 'p8_reproduzierbarkeitspruefung'. Sie stellt die Beziehung zu der Projekt-Tabelle dar und ermöglicht den Zugriff auf diese Daten.

---

### app/Models/Recherche/P8Suchprotokoll.php
```json
{
  "file_summary": "Modell für die Datenbanktabelle p8_suchprotokoll. Definiert die Beziehung zu P4Suchstring und castet spezifische Felder.",
  "key_classes": ["P8Suchprotokoll"],
  "important_methods": ["suchstring", "__construct"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P8UpdatePlan.php
```json
{
  "file_summary": "Modell für die Tabelle p8_update_plan mit Eloquent-Relationen zur Projekt-Tabelle. Es wird ein Updateplan für das P8-System verwaltet.",
  "important_classes": ["P8UpdatePlan"],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Concerns\HasUuids",
    "Illuminate\Database\Eloquent\Model",
    "Illuminate\Database\Eloquent\Relations\BelongsTo",
    "Projekt"
  ]
}
```

---

### app/Models/Recherche/PaperEmbedding.php
Model für Paper-Embeddings bezieht sich auf Projekt-Datenbanktabelle. Beinhaltet Verbindung zu Projekt, speichert Embedding-Daten für ein Papier.

---

### app/Models/Recherche/Phase.php
Model für Datenbank-Tabelle "phasen", enthält Beziehungen zu Projekt und UUID-Generierung.

---

### app/Models/Recherche/Projekt.php
Diese Datei definiert die Projekt-Model-Klasse in der Datenbank. Sie verwaltet Beziehungen zu anderen Modellen wie User und speichert Projektdaten wie Titel, Forschungsfrage, Review-Typ und Startdatum. Die Klasse verwendet HasFactory und HasUuids für Eloquent-Methoden und LogsActivity für Aktivitätsprotokoll-Logik.

---

### database/factories/UserFactory.php
Erstellt Fakes für Benutzer mit Hilfe von Faker und Hashing.

---

### database/factories/Recherche/ProjektFactory.php
Die ProjektFactory-Datei definiert eine Fabrik für die Erzeugung von Projekt-Objekten in der Datenbank. Sie verwendet die Factory-Klasse von Laravel und importiert die User-Model-Klasse. Die wichtigste Funktion ist definition(), die ein Array mit Beispielwerten für Projekte zurückgibt, wie z.B. Titel und Forschungsfrage.

---

### database/migrations/0001_01_01_000000_create_users_table.php
Diese Datei erstellt die Tabelle 'users' mit den Feldern id, name, email, email_verified_at, password und rememberToken. Darüber hinaus werden die Tabellen 'password_reset_tokens' und 'sessions' erstellt.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Dieser Migrations-Code erstellt in der Datenbank zwei Tabelle: 'cache' und 'cache_locks'. Die Tabelle 'cache' speichert kachelohe Werte mit Schlüssel, Wert und einer Ablaufzeit. Die Tabelle 'cache_locks' speichert Schlüsseleigentumsinformationen für die Kache.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Migration für die Erstellung von drei Tabellen im Datenbank-Modell: jobs, job_batches und failed_jobs. Diese Tabellen werden zum Verwalten von Job-Ausführungen verwendet.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
```
{
  "file_summary": "Lädt Datenbankmigration, um Spalten für Two-Factor-Authentifizierung in Benutzer-Tabelle hinzuzufügen.",
  " wichtige_Klassen Funktionen Endpunkte": [
    "Migration",
    "Schema",
    "Blueprint"
  ],
  "externe Abhängigkeiten": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Migration für die Erstellung einer Tabelle 'contacts' in der Datenbank. Die Tabelle enthält Felder für Kontaktdaten wie Name, Email und Projekttyp sowie ein Textfeld für Nachrichten. Die Migration bietet Funktionen zur Erstellung und Löschung der Tabelle.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
```
{
  "file_summary": "Migration zur Erstellung der Datenbank-Tabelle page_views für die Aufzeichnung von Seitenaufrufen. Diese Tabelle enthält eine ID, ein Count für Besuche und Timestamps für die Einfügung und Aktualisierung.",
  "wichtige_Klassen": ["Migration", "Blueprint"],
  "externe_Abhängigkeiten": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Dieses Skript erstellt oder löscht Tabelle für die Benutzerberechtigungen. Es verwaltet die Verbindungen zwischen Rollen und Berechtigungen sowie die Zuweisung von Berechtigungen an Modelle.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
```json
{
  "file_summary": "Migration zum Hinzufügen von Details zur 'page_views'-Tabelle. Fügt die Spalten 'path', 'ip_anonymous' und 'user_agent' hinzu. Kann diese Tabelle auch wieder zurückgebaut werden.",
  " wichtigste Klassen/Funktionen": "Illuminate\Database\Migrations\Migration",
  "Externe Abhängigkeiten": ["Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_000003_create_consents_table.php
```
{
  "file_summary": "Migration zur Erstellung einer Consent-Tabelle in der Datenbank. Diese Tabelle enthält Informationen über die Zustimmung von Benutzern zu bestimmten Kategorien. Die wichtigsten Funktionen sind die Erstellung und Löschung der Tabelle. Externe Abhängigkeiten: Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\Schema."
}
```

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Dieses Skript generiert Recherche-Tabelle-Strukturen für das SQLite-Datenbank-System. Es enthält Funktionen zur Erstellung von Benutzer-, Projekt-, Phasen- und Trefferlisten-tabellen, sowie spezifische Tabellen für die Recherche-Stufen P1 bis P3.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei erstellt PostgreSQL-Extensions und -Enums für die Datenbank.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Migration für die Tabelle Projekt und Phase im PostgreSQL-Database, um diese zu erstellen oder zu aktualisieren. Die Migration besteht aus zwei Hauptfunktionen: 'up' und 'down'. Bei der Migration in Richtung 'up' werden die Tabellen 'projekte' und 'phasen' erstellt. Dabei wird die UUID als Primärschlüssel verwendet, um eindeutige IDs für jedes Projekt bzw. jede Phase zu generieren. Die Beziehungen zwischen den Tabellen und die relevanten Datenfelder wie Titel, Beschreibung, Startdatum, Notizen usw., werden ebenfalls erstellt oder aktualisiert. In der Migration in Richtung 'down' werden die Tabellen 'projekte' und 'phasen' gelöscht.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
```json
{
  "file_summary": "Diese Datei erstellt mehrere Tabellen in der Datenbank, die für das Projekt Strukturmodell benötigt werden. Es handelt sich um folgende Tabellen: p1_strukturmodell_wahl, p1_komponenten, p1_kriterien und p1_warnsignale. Die Tabellen enthalten Felder wie projekt_id, gewaehlt, begruendung und kriterium_typ. Die Datei verwendet die Illuminate\Databaseigrations-Migration-Klasse und hängt von den Schema- und DB-Facaden ab."
}
```

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Diese Datei definiert Migrationen für die Datenbank, um vier Tabellen zu erstellen: p2_review_typ_entscheidung, p2_mapping_suchstring_komponenten, p2_trefferlisten und p2_cluster. Die Tabelle p2_cluster enthält eine Check-Konstante zur Einschränkung der Relevanz-Einstellungen auf 'hoch', 'mittel' oder 'gering'.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
```
{
  "file_summary": "Erstellt drei Tabellen in der Datenbank für ein Projekt: p3_disziplinen, p3_datenbankmatrix und p3_geografische_filter. 
Fügt die Tabelle p3_graue_literatur hinzu. Führt diese Tabellen beim 'down' Prozess zurück."
}
```

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
```
{
  "file_summary": "Diese Datei definiert eine Migration für die Datenbank, um drei Tabelle zu erstellen: p4_suchstrings, p4_thesaurus_mapping und p4_anpassungsprotokoll. Diese Tabelle enthalten Informationen über Suchanfragen, Thesauruserweiterungen und Anpassungsvorgänge in der Datenbank.",
  "key_classes": ["Migration"],
  "important_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
```
{
  "file_summary": "Migration für die Datenbank-Tabellen p5_prisma_zahlen, p5_screening_kriterien, p5_treffer, p5_screening_entscheidungen und p5_tool_entscheidung. Diese Migration erstellt und konfiguriert diese Tabellen für das Projekt 'P5'.",
  "key_classes": ["Migration", "Blueprint"],
  "important_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Migration zur Datenbank-Struktur-Update: Erstellung von Tabellen p6_qualitaetsbewertung und p6_luckenanalyse, sowie Einfügen von Spalten und Fremdschlüsseln. Funktioniert nur unter PostgreSQL.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
```
{
  "file_summary": "Dieser Migrations-File erstellt und verwaltet vier Datenbanktabellen (p7_synthese_methode, p7_datenextraktion, p7_muster_konsistenz und p7_grade_einschaetzung) in einer PostgreSQL-Datenbank. Die Tabellen enthalten Felder wie UUIDs, Boolesche Werte, Texte und JSON-Arrays.",
  "key_classes": ["Migration", "Blueprint"],
  "important_functions": ["up", "down", "hasTable", "create", "dropIfExists"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Diese Datei erstellt und löscht Tabellen in der Datenbank für Projekt P8. Es werden vier Tabellen erstellt: p8_suchprotokoll, p8_limitationen, p8_reproduzierbarkeitspruefung und p8_update_plan. Die Tabelle p8_update_plan erhält eine Prüfkonstante für die gültigen Update-Typen.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Erstellt und löscht Datenbank-Indizes für Projekt- und Treffer-Daten in der PostgreSQL-Datenbank. Fügt Indizes zu den Tabellen phasen, p1_komponenten, p5_treffer, p5_screening_entscheidungen, p6_qualitaetsbewertung und p7_datenextraktion hinzu, um Abfragen zu beschleunigen. Löscht die entsprechenden Indizes bei Down-Migration.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Diese Datei erstellt und löscht eine PostgreSQL-Funktion 'berechne_prisma_zahlen' zum Berechnen von PRISMA-Kennzahlen. Die Funktion zählt Werte für ein Recherche-Projekt aus Tabellen wie p5_treffer und p5_screening_entscheidungen.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Migration zur Erstellung einer Tabelle für Chat-Nachrichten, wenn PostgreSQL als Datenbank verwendet wird. Die Tabelle enthält die Spalten 'id', 'user_id', 'role' und 'content'. Ein Check-Konstrukt sorgt dafür, dass der Wert in der Spalte 'role' nur 'user' oder 'assistant' sein kann.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Migration zum Erstellen einer Webhooks-Tabelle und Anpassung der Chat-Messages-Tabelle. Einige Felder werden nur bei PostgreSQL unterstützten Datenbanken erstellt.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
```
{
  "file_summary": "Migration für die Datenbank, um eine Tabelle für Paper-Einblendungen zu erstellen. Die Tabelle enthält Metadaten zu den Papers und ermöglicht die Speicherung von Vektoren mit der Größe 768.",
  "key_classes_functions": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint",
    "DB::getDriverName()",
    "Schema::create('paper_embeddings', ...)"
  ],
  "external_dependencies": [
    "Illuminatedatabase",
    "PostgreSQL"
  ]
}
```

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Diese Datei erstellt und löscht eine Aktivitäts-Log-Tabelle in der Datenbank. Haupt-Klassen: Migration, Blueprint. Externe Abhängigkeit: Illuminate-Datenbank-Framework.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
Diese Datei ist eine Migrationsdatei, die ein Status-Feld in der Benutzer-Tabelle erstellt oder ändert. Sie unterstützt PostgreSQL und SQLite. Die wichtigsten Klassen sind die Migration und Blueprint. Externe Abhängigkeiten sind DB ( Datenbank-Abstraktion) und Schema.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
```
{
  "file_summary": "Migration für PostgreSQL-Datenbank um neue Funktionen zu implementieren: \
Erstellung einer Typdefinition und Hinzufügen einer Spalte an die Tabelle \"webhooks\". \
Idempotenz durch Ausnahmehandling, falls der Typ bereits existiert."
}
```

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
```
{
  "file_summary": "Migrationsdatei für PostgreSQL-Datenbank, die die Spalte 'password' der Tabelle 'webhooks' von nicht null zu nullable macht. Die Datei ist Teil des Datenzugriffs-Katalogs.",
  "important_classes_functions": ["Migration", "DB::getDriverName()", "DB::statement()"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
Migration zur Hinzufügung eines Secrets-Spalten für Webhook-Tabellen in einer PostgreSQL-Datenbank.

---

### database/migrations/2026_04_02_200000_drop_password_from_webhooks.php
```
{
  "file_summary": "Migration für Datenbank, um die Spalte 'password' in der Tabelle 'webhooks' zu entfernen. Ein Rollback dieser Änderung setzt die Spalte wiederher und macht sie nullable.",
  "main_classes": ["Illuminate\Database\Migrations\Migration"],
  "key_functions": ["Schema::table", "DB::getDriverName"],
  "external_dependencies": ["Laravel-DB-Framework (Eloquent, Schema), PostgreSQL-Driver"]
}
```

---

### database/seeders/DatabaseSeeder.php
```json
{
  "file_summary": "Diese Datei ist ein Seeder für die Datenbank, der Rollen und Benutzer anlegt. Es werden Benutzer mit einer Standard-Rolle 'admin' erstellt.",
  "main_classes_functions": ["DatabaseSeeder", "RoleSeeder", "RechercheDemoSeeder"],
  "externe_abhaengigkeiten": ["App\Models\User", "Illuminate\Database\Seeder"]
}
```

---

### database/seeders/RechercheDemoSeeder.php
Die Datei RechercheDemoSeeder.php ist eine Seeder-Datei für das Laravel-Modell. Sie dient zur Bevorratung von Daten in die Datenbank. Die Seed-Methode sät verschiedene Modelle an, wie zum Beispiel Projekt, Phase, P1-Komponenten und Trefferlisten, mit fiktiven Werten. Externe Abhängigkeiten sind u.a. die Laravel-Datenbank-Facade (DB) und Models wie User, Projekt, Phase usw.

---

### database/seeders/RoleSeeder.php
```
{
  "file_summary": "Diese Datei befasst sich mit der Einrichtung von Rollen in einer Datenbank. Sie verwendet die Spatie-Permission-Bibliothek und ist Teil des Seed-Prozesses, um standardmäßige Einstellungen für eine Anwendung vorzudefinieren. Die wichtigsten Klassen sind RoleSeeder, Role (von Spatie\Permission\Models) und Seeder (vom Framework). Externe Abhängigkeiten sind die Datenbank selbst sowie die Spatie-Permission-Bibliothek."
}
```

---

### database/sql/create_langdock_agent_user.sql
Erstellt eine Datenbank-Rolle mit restriktedem Zugriff auf Recherche-Tabellen. Fügt Rollenzugriffe hinzu, entfernt Zugriffe auf sensible Tabellen.

---

## temp_dummy

### .github/pull_request_template.md
Pull-Request-Vorlage für GitHub mit Beschreibung, Änderungstyp, Checkliste und Deploy-Hinweisen

---

## tests

### tests/Pest.php
```
{
  "file_summary": "Konfiguration von Pest, Erweiterung der Testfall-Klasse und Definition von Funktions-Helper",
  " wichtigste_Klassen": ["Pest", "Tests\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"],
  "wichtige_Funktionen": ["extend", "use", "in", "expect", "toBeOne"],
  "externe_Abhängigkeiten": ["PHPUnit\Framework\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/TestCase.php
```json
{
  "file_summary": "Die Datei TestCase.php enthält eine abstrakte Test-Klasse, die von Laravel verwendet wird. Sie erweitert die Basisklasse BaseTestCase und dient als Grundlage für weitere Tests.",
  "important_classes": ["TestCase"],
  "external_dependencies": ["Laravel Framework", "Illuminate\Foundation\Testing\TestCase"]
}
```

---

### tests/Feature/ContactTest.php
```json
{
  "file_summary": "Diese Datei enthält die Tests für die Kontaktformular-Funktionen in einer Web-App. Die Tests überprüfen die Validierung, Erfolgsmeldungen und Datenbankeintragung nach erfolgreichen Formularabgaben.",
  "important_classes_functions_endpoints": ["Contact", "route('contact.store')"],
  "external_dependencies": ["App\Models\Contact"]
}
```

---

### tests/Feature/DashboardTest.php
```
{
  "file_summary": "Dashboard-Tests mit Benutzerauthentifizierung und -autorisation.",
  "main_class": "DashboardTest",
  "key_methods": [
    "test_guests_are_redirected_to_the_login_page",
    "test_authenticated_users_can_visit_the_dashboard"
  ],
  "external_dependencies": [
    "App\Models\User",
    "route('dashboard')",
    "route('login')"
  ]
}
```

---

### tests/Feature/ExampleTest.php
Unit-Test für die Route 'home'. Überprüft, ob der Controller eine erfolgreiche Antwort (HTTP-Statuscode 200) zurückgibt.

---

### tests/Feature/IngestPaperJobTest.php
```
{
  "file_summary": "Dieses Skript enthält Einheitstests für die IngestPaperJob-Klasse. Die Klasse wird getestet, ob sie korrekt Parameter an die Queue sendet und wie sie mit Fehlern umgeht.",
  "main_classes": ["IngestPaperJob"],
  "main_functions": ["dispatch", "handle"],
  "external_dependencies": [
    {"name": "Illuminate\Support\Facades\Http"},
    {"name": "Illuminate\Support\Facades\Log"},
    {"name": "Illuminate\Support\Facades\Queue"}
  ]
}
```

---

### tests/Feature/LegalPagesTest.php
Diese Datei enthält Tests für die Anzeige von Impressum-, DSGVO- und AGB-Seiten. Es werden Assertions für die Statuscodes (200) und Inhalte der Seiten durchgeführt.

---

### tests/Feature/PaperRagTest.php
Diese Datei enthält Einheitstests für das API, insbesondere für die Paper-RAG-Suche und den Ingest-Endpunkt. Die wichtigsten Klassen sind Illuminate\Support\Facades\Config, Illuminate\Support\Facades\Http und Illuminate\Support\Facades\Queue. Externe Abhängigkeiten sind der HTTP-Client und die Datenbank.

---

### tests/Feature/ProjektPolicyTest.php
```
{
  "file_summary": "Diese Datei enthält eine Reihe von Tests für die Projekt-Policy eines Anwendungs-Systems.
Die wichtigsten Klassen, Funktionen oder Endpunkte sind:
- ProjektPolicy: Die Policy für Projekte.
- User: Benutzermodell.
- Projekt: Das Modell für Projekte.
Externe Abhängigkeiten:
- App\Models\Recherche\Projekt
- App\Models\User
- App\Policies\ProjektPolicy
Die Tests überprüfen die Berechtigungen von Benutzern auf Projekte und die Fähigkeit, solche zu erstellen."
}
```

---

### tests/Feature/RechercheTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Recherche-Funktionen. 
  Es werden Funktionen wie Projekt-Erstellung, Job-Auslösung und Projekt-Ansicht getestet.
  Die Tests decken die folgenden Funktionen ab:
  - Projekt-Erstellung als angemeldeter Benutzer
  - Auslösung des TriggerLangdockAgent-Jobs bei Projekt-Erstellung
  - Erforderlichkeit der Eingabe-Felder für Projekt-Erstellung
  - Anzeige eigener Projekte im Projekt-Liste-Ansicht
  - Verhindern von Zugriff auf fremde Projekte als angemeldeter Benutzer
  - Anzeigen der Detailansicht eines eigenen Projekts
  - Verbieten des Zugriffs auf ein anderes Projekt als angemeldeter Benutzer.
  Externe Abhängigkeiten: Laravel Livewire, Queue-System (Fakturen)"
}
```

---

### tests/Feature/WebhookTest.php
Diese Datei enthält Einheitstests für die Webhook-Implementierung. Die Tests überprüfen, ob die Webhooks korrekt autorisiert werden und welche Fehlermeldung ausgegeben wird, wenn die Anforderungen nicht erfüllt sind. Die wichtigsten Klassen sind: User, Projekt, Recherche\Projekt, Queue. Externe Abhängigkeiten sind: Config, Queue.

---

### tests/Feature/WelcomeTest.php
Diese Datei enthält Funktionen für Tests der Willkommensseite eines Web-Anwendungs-Frameworks. Sie überprüft, ob die Seite erfolgreich geladen wird, ob Besuche gezählt werden und ob sich Gäste oder angemeldete Benutzer korrekt sehen.

---

### tests/Feature/Auth/AuthenticationTest.php
Diese Datei enthält Authentifizierungs-Tests, die den Login-Prozess und die Logout-Funktion prüfen. Die wichtigsten Funktionen sind: login screen can be rendered, users can authenticate using the login screen, users can not authenticate with invalid password und users can logout.

---

### tests/Feature/Auth/EmailVerificationTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die E-Mail-Verifizierung im Authentifizierungsprozess. Sie testet, ob der Benutzer nach dem Klicken auf den Verifikationslink erfolgreich verifiziert wird und ob der Link auch bei bereits verifizierten Benutzern funktioniert.",
  " wichtige Klassen": ["User", "Verified"],
  "wichtige Funktionen": ["URL::temporarySignedRoute", "Event::fake", "Event::assertDispatched"],
  "externe Abhängigkeiten": ["Illuminate\Auth\Events\Verified", "Illuminate\Support\Facades\Event", "Illuminate\Support\Facades\URL"]
}
```

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Diese Datei testet die Funktionen der Passwortschutz-Seite. Sie erstellt ein Benutzer-Konto, besucht die Passwort-Bestätigungsseite und prüft auf erfolgreichen Status-Code (200).

---

### tests/Feature/Auth/PasswordResetTest.php
```
{
  "file_summary": "Daten zum Testen von Authentifizierungsfunktionen, insbesondere für das Zurücksetzen eines Passworts. Es werden verschiedene Tests durchgeführt, um sicherzustellen, dass die korrekten Statuscodes und die richtige Ausführung der Anfragen zurückgegeben werden.",
  "important_classes": ["User", "Notification"],
  "important_functions": ["post", "get"],
  "external_dependencies": ["Illuminate\Auth\Notifications\ResetPassword", "Illuminate\Support\Facades\Notification"]
}
```

---

### tests/Feature/Auth/RegistrationTest.php
Diese Datei enthält Tests für die Registrierungsfunktionen einer Anwendung. Es werden zwei Funktionen getestet: Die Renderung der Registrierungsseite und die erfolgreiche Registrierung eines Benutzers.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Diese Datei enthält zwei Tests für die Zwei-Faktor-Authentifizierung.
Die Tests überprüfen, ob die Anwendungskonsole richtig umgeleitet wird, wenn kein Authentifizierungsstatus vorhanden ist,
nachdem das Two-Factor-Challenge-Formular gerendert wurde und der Nutzer erfolgreich angemeldet ist.

---

### tests/Feature/Chat/BigResearchChatTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Funktionen eines Chats. Es werden verschiedene Szenarien getestet, wie z.B. das Senden einer Nachricht, die Validierung von Pflichtfeldern, der Datenbankzugriff und Fehlerbehandlung.",
  "important_classes": ["ChatMessage", "User", "Webhook"],
  "important_functions": ["sendMessage", "clearHistory", "getChatMessages"],
  "external_dependencies": ["Http", "Illuminate\Support\Facades\Http"]
}
```

---

### tests/Feature/Recherche/PhaseP1Test.php
```
{
  "file_summary": "Diese Datei enthält die Testfälle für Phase P1 der Recherche-Funktionen. Es gibt Tests für Strukturmodellwahlen (P1Strukturmodellwahl), Komponenten (P1Komponente) und Kriterien (P1Kriterium). Die Tests überprüfen, ob Benutzer ein Strukturmodell wählen, eine Komponente erstellen oder bearbeiten können. Außerdem wird geprüft, ob Kriterien korrekt erstellt und bearbeitet werden können.",
  "main_classes": ["P1Strukturmodellwahl", "P1Komponente", "P1Kriterium"],
  "functions": ["newSmw", "saveSmw", "editSmw", "deleteSmw", "newKomp", "saveKomp", "editKomp", "deleteKomp", "newKrit", "saveKrit", "editKrit"],
  "endpoints": ["recherche.phase-p1"],
  "imports": ["App\Models\Recherche\", \"App\Models\User\", \"Livewire\Volt\Volt\"],
  "db_zugriffe": ["P1Strukturmodellwahl", "P1Komponente", "P1Kriterium"]
}
```

---

### tests/Feature/Recherche/PhaseP2Test.php
Diese Datei enthält Unit-Tests für die Phase P2 des Recherche-Prozesses. Die Tests umfassen die Erstellung, Bearbeitung und Löschung von Review-Typen, Clustern, Mappings und Trefferlisten. Die wichtigsten Klassen sind Projekt, User, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente und P2Trefferliste. Externe Abhängigkeiten umfassen die Eloquent-Modelle von Laravel sowie den Livewire-Volt-Treiber.

---

### tests/Feature/Recherche/PhaseP3Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Phase P3 der Recherche-Funktion. Es werden Tests für die Datenbankmatrix, Disziplin, geografischen Filter und graue Literatur durchgeführt. Die wichtigsten Klassen sind Projekt, P3Datenbankmatrix, P3Disziplin, P3GeografischerFilter und P3GraueLiteratur.",
  "main_classes": ["Projekt", "P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"],
  "main_functions": ["newDb", "saveDb", "editDb", "deleteDb", "newDis", "saveDis", "editDis", "deleteDis", "newGeo", "saveGeo", "editGeo", "deleteGeo", "newGrau", "saveGrau", "editGrau"],
  "externale_abhaengigkeiten": ["App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP4Test.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für Funktionen zur Suchstring-, Thesaurus-Mapping- und Anpassungsprotokoll-Verwaltung. 
                   Die Tests prüfen die korrekte Erstellung, Bearbeitung und Löschung von Suchstrings, 
                   Thesaurus-Mappings und Anpassungsprotokollen sowie die Validierung von Eingabedaten.",
  "important_classes": ["P4Suchstring", "P4ThesaurusMapping", "P4Anpassungsprotokoll"],
  "external_dependencies": ["Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP5Test.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Recherchefunktion in Phase P5.\nFunktionalitäten:\nP5-Prisma-Zahlen erstellen, bearbeiten und löschen\nP5-Screening-Kriterien erstellen, bearbeiten und löschen\nP5-Tool-Entscheidung erstellen und löschen\nP5-Screening-Entscheidung erstellen und validieren\nAuth-Funktion: Fremder User bekommt 403.\nWichtige Klassen:\nP5PrismaZahlen, P5ScreeningKriterium, P5ToolEntscheidung, P5Treffer, P5ScreeningEntscheidung\nExterne Abhängigkeiten:\nApp\Models\Recherche*, App\Models\User, Livewire\Volt\Volt",
}
```

---

### tests/Feature/Recherche/PhaseP6Test.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Funktionalität der Phase P6 in einer Rechercheanwendung. 
    Es werden Funktionen wie die Erstellung, Bearbeitung und Löschung von Qualitätsbewertungen und Lückenanalysen getestet.
    Die Tests verwenden das Livewire-Volt-Testing-Framework, um die Funktionalität der Anwendung zu überprüfen.
    Die Datei importiert Modelle wie Projekt, P5Treffer, P6Qualitaetsbewertung und P6Luckenanalyse.
    Externe Abhängigkeiten sind Livewire/Volt und die Datenbank.",
  "wichtige Klassen": ["P6Qualitaetsbewertung", "P6Luckenanalyse"],
  "wichtige Funktionen/Endpunkte": ["newQb", "saveQb", "editQb", "deleteQb", "newLa", "saveLa", "editLa", "deleteLa"],
  "externe Abhängigkeiten": ["Livewire/Volt", "Datenbank"]
}
```

---

### tests/Feature/Recherche/PhaseP7Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Funktionalität der Phase P7 in einer Recherche-Anwendung. Die Tests überprüfen die Funktionen zur Erstellung, Bearbeitung und Löschung von Synthese-Methode, Datenextraktion, Muster-Konsistenz und Grade-Einschätzung. Außerdem wird die Authentifizierung für fremde Benutzer überprüft.",
  "key_classes": ["P7SyntheseMethode", "P7Datenextraktion", "P7MusterKonsistenz", "P7GradeEinschaetzung"],
  "key_functions": ["newSm", "saveSm", "editSm", "deleteSm", ...],
  "external_dependencies": ["Livewire\Volt\Volt", "App\Models\User"]
}
```

---

### tests/Feature/Recherche/PhaseP8Test.php
```
{
  "file_summary": "Diese Datei enthält unit tests für die Funktionen von Phase P8 einer Suchanfrage. Es werden die folgenden Funktionen getestet:\n\n- Erstellen und bearbeiten von Suchprotokollen\n- Erstellen und bearbeiten von Limitationen\n- Erstellen und bearbeiten von Reproduzierbarkeitsprüfungen\n- Erstellen und bearbeiten von Update-Plänen\n\nDie wichtigsten Klassen sind P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung und P8UpdatePlan. Externe Abhängigkeiten sind die App\Models\* und Livewire\Volt\Volt-Klassen."
}
```

---

### tests/Feature/Recherche/ProjektDetailTest.php
Diese Datei enthält Tests für die Projekt-Detail-Seite in der Recherche. Die Funktionen umfassen das Rendern von Tabs, die Überprüfung von Zugriffsrechten und die Verarbeitung von Phasen-Status.

---

### tests/Feature/Settings/AppearanceTest.php
Datenbank-Testfälle für die Erscheinungsbild-Konfiguration. Prüft Rendering von Buttons, Container und clientseitigen Skripten. Überprüft Zugriffsberechtigungen für eingeloggte User.

---

### tests/Feature/Settings/PasswordUpdateTest.php
```
{
  "file_summary": "Tests für die Passwort-Update-Funktion in der Settings-Seite. Die Tests prüfen, ob ein User erfolgreich sein Passwort ändern kann und ob eine fehlgeschlagene Änderung mit einem Fehler angezeigt wird.",
  "wichtige_Klassen_Funktionen_Endpunkte": ["User", "Hash", "Volt"],
  "externe_Abhängigkeiten": ["Illuminate\Support\Facades\Hash", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/ProfileUpdateTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für das Profil-Update-Feature. Die wichtigsten Funktionen sind das Aktualisieren der Benutzerinformationen, die Überprüfung des E-Mail-Status und das Löschen eines Benutzerkontos. Die externen Abhängigkeiten umfassen die Klasse 'User' und den Livewire-Volt-Test-Driver.",
  "key_classes": ["User", "Volt"],
  "important_functions": ["updateProfileInformation", "deleteUser", "assertHasNoErrors"],
  "external_dependencies": ["route", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
Die Datei testet Funktionen für die Zwei-Faktor-Authentifizierung im System. Sie beinhaltet Tests für das Anzeigen der Einstellungen, die Erfordernis von Passwortbestätigung bei aktivierter Authentifizierung und die Ausführung von Verboten auf die Einstellungsseite wenn Zwei-Faktor-Authentifizierung deaktiviert ist.

---

### tests/Feature/Settings/WebhooksTest.php
```json
{
  "file_summary": "Diese Datei testet die Funktionen für das Speichern und Löschen von Webhooks im Dashboard Chat- und Recherche-Modus. Die Tests überprüfen auch, ob die URL als erforderlich und korrekt formatiert wird, sowie ob der geheime Schlüssel korrekt maskiert ist.",
  "important_classes": ["User", "Webhook", "Volt"],
  "main_functions": ["saveDashboard", "clearDashboard", "saveRecherche", "clearRecherche", "mount"],
  "external_dependencies": ["App\Models\User", "App\Models\Webhook", "Livewire\Volt\Volt"]
}
```

---

### tests/Unit/ExampleTest.php
Unit-Test-Datei für die Verifizierung von Logik in der Anwendung

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Definiert die Liste von Kontakten für die Ressource 'ContactResource'. Dient als Startpunkt für das Anzeigen aller Kontakte. Hauptklasse: ListContacts, wichtigste Funktion: Keine explizite Funktion definiert, aber erbt von ListRecords.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei implementiert eine View-Page für die Anzeige von Kontaktdaten. Sie basiert auf der Filament-Ressource "ContactResource" und enthält keine eigene Logik, sondern übernimmt diese von der Oberklasse.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
```json
{
  "file_summary": "Diese Datei definiert die Anzeige-Page für das Bearbeiten von Benutzer-Daten in der Filament-UI.",
  "main_class": "EditUser",
  "parent_class": "EditRecord",
  "resource_class": "UserResource",
  "external_dependencies": ["Filament\Resources"]
}
```

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Das Skript definiert eine Seite für die Auflistung von Benutzern in einer Filament-Webanwendung.

---

### resources/views/dashboard.blade.php
```json
{
  "file_summary": "Das Dashboard-Template für die Anwendung, das den Benutzer willkommen heißt und ihm wichtige Funktionen zur Verfügung stellt. 
  - Es enthält eine Übersicht über aktuelle Projekte und gibt direkte Links zu Recherche-Funktionen und Admin-Bereichen an.
  - Die wichtigsten Klassen sind: x-layouts.app, livewire:chat.big-research-chat.
  - Externe Abhängigkeiten: auth(), route(), Livewire."
}
```

---

### resources/views/welcome.blade.php
Dieses Blade-Template (welcome.blade.php) ist für das Hauptlayout der Website von Linn Games verantwortlich. Es enthält alle notwendigen CSS- und JavaScript-Ressourcen sowie die Struktur des Hauptbereichs mit Header, Hauptinhalt und Footer. Es verwendet verschiedene Klassen, Funktionen und Endpunkte aus dem Laravel-Framework und importiert externe Abhängigkeiten wie Google Fonts.

---

### resources/views/components/action-message.blade.php
Komponente für eine auf Popup-ähnliche Nachrichten, die beim laden von Daten oder Änderung angezeigt wird. Wenn sich der Nutzer innerhalb eines Timeout-Intervalls (2s) nicht mehr im Element befindet, schließt es sich automatisch.

---

### resources/views/components/app-logo-icon.blade.php
Darstellung des Logo-Icons als SVG-Datei

---

### resources/views/components/app-logo.blade.php
Dieses Blade-Template dient zur Anzeige des App-Logos und der Anwendungsbezeichnung. Es enthält zwei Hauptkomponenten: 
	* Eine für das Logo, die durch den x-app-logo-icon-Komponenten repräsentiert wird.
	* Eine für die Anwendungsbezeichnung, die als span-Tagelement ausgeführt wird und den Namen 'Laravel Starter Kit' anzeigt.

---

### resources/views/components/auth-header.blade.php
Erstellt ein Auth-Header-Komponenten in einer Bladefile, die Titel und Beschreibung enthält. Hauptklasse: Blade-Komponente mit Eigenschaften 'title' und 'description'.

---

### resources/views/components/auth-session-status.blade.php
Diese Datei zeigt den Status einer Authentifizierungssitzung an. Sie enthält eine Blade-Vorlage, die einen Status anzeigt, falls ein Status vorliegt. Die wichtigsten Funktionen sind: @props und @if.

---

### resources/views/components/placeholder-pattern.blade.php
Generiert ein SVG-Pattern als Platzhalter für UI-Komponenten. Die Funktionen umfassen die Erzeugung eines einzigartigen UUIDs und das Definieren von Mustern für das SVG-Element. Der wichtigste Endpunkt ist der generierte SVG-Code, während externe Abhängigkeiten in Form von PHP-Funktionen wie uniqid() und Twig-Tags wie @props() existieren.

---

### resources/views/components/layouts/app.blade.php
Das Layout für die Anwendung, enthält ein App-Sidebar und ein Hauptbereich für Inhalte.

---

### resources/views/components/layouts/auth.blade.php
Authentifizierung Layout für Benutzeranmeldeseiten. Die Datei bietet ein einfaches authentifiziertes Layout, das von anderen Blade-Vorlagen verwendet werden kann. Die wichtigste Komponente ist der `x-layouts.auth.simple`-Tag mit einem Titel und einem Slot zum Platzieren des Inhalts.

---

### resources/views/components/layouts/app/header.blade.php
Dies ist ein HTML-Templat für die Anwendungshauptseite. Es enthält eine Header-Sektion mit Navigationsmenü und Benutzerprofilinformationen. Die Datei verwendet Livewire, um interaktive Komponenten zu rendern.

---

### resources/views/components/layouts/app/sidebar.blade.php
Diese Datei rendert das Sidebar-Menü einer Anwendung. Es enthält Navigationslinks, ein Benutzerprofil und Bottom-Links. Die Funktionen sind modular aufgebaut und lassen sich je nach Rolle des Benutzers anpassen.

---

### resources/views/components/layouts/auth/card.blade.php
Diese Blade-Datei ist für die Darstellung von Authentifizierungscard-Komponenten zuständig. Sie enthält eine grundlegende HTML-Struktur, ein Logo und einen Link zur Startseite. Die Datei wird in der Authentifizierungskarte verwendet.

---

### resources/views/components/layouts/auth/simple.blade.php
HTML-Templatdatei für Authentifizierungsmuster, einschließlich Logo und Cookie-Bestätigung.

---

### resources/views/components/layouts/auth/split.blade.php
```json
{
  "file_summary": "Diese Datei ist ein Teil einer Benutzeroberfläche und dient als Layout für Authentifizierungs- oder Anmeldungsbereiche. Sie enthält einen Header mit einem Logo, eine Begrüßungsmeldung und Links zum Hauptbereich der Anwendung.",
  " wichtigsten_Klassen": ["x-app-logo-icon", "livewireScriptConfig"],
  "Funktionen/Endpunkte": [route('home'), route('auth.login')],
  "externe_Abhängigkeiten": ["partials.head", "cookie-consent::index"]
}
```

---

### resources/views/components/settings/layout.blade.php
```
{
  "file_summary": "Diese Datei enthält eine Benutzeroberfläche für Einstellungen, darunter Profile, Password, Two-Factor Auth, Appearance und Webhooks. Sie verwendet Laravel-Route-Names und -Funktionen sowie Blade-Templating.",
  "key_classes": ["nav", "a"],
  "important_functions": [route(), request()],
  "external_dependencies": ["Laravel\\Fortify\\Features"]
}
```

---

### resources/views/emails/contact-confirmation.blade.php
E-Mail-Vorlage für Kontakt-Bestätigungen, die einen Kunden über den Status seiner Anfrage informiert.

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei ist ein E-Mail-Vorlagen-Template, das Kontaktanfragen anzeigt. Es enthält Informationen wie Name, E-Mail, Projekttyp, Zeitrahmen und Nachricht des Kontakts. Externe Abhängigkeiten: $contact-Objekt (mit Eigenschaften name, company, email, project_type, timeline und message).

---

### resources/views/emails/deploy-notification.blade.php
Mail-Template für Deploy-Ergebnisse. Zeigt URL, Zeitpunkt und Umgebung an.

---

### resources/views/layouts/legal.blade.php
Dieser Blade-Template definiert ein Layout für rechtliche Inhalte. Es enthält eine Navigation mit einem Zurück-Link und dem Branding des Unternehmens, sowie einen Inhaltsslot, der durch @yield('content') angezeigt wird. Externe Abhängigkeiten sind Google Fonts.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für das Online-Spiel Linn.Games. Die AGB regeln die Nutzung der Website und des Spiels durch registrierte Nutzer. Sie umfassen Themen wie Registrierung, Benutzerkonto, Nutzungsregeln, Kosten, digitale Käufe und Haftung.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei ist Teil der Webanwendung und stellt die Datenschutzerklärung dar. Sie enthält wichtige Informationen über die Verarbeitung personenbezogener Daten, wie z.B. Registrierungsdaten, Authentifizierungsdaten, Spieldaten und Nutzungs- und Log-Daten. Die Datei informiert darüber hinaus über Rechte der Betroffenen, Sicherheitsmaßnahmen und verwendete Cookies. Es werden keine externen Abhängigkeiten oder Datenbank-Zugriffe verwendet.

---

### resources/views/legal/impressum.blade.php
Das Impressum-Blade-Template stellt Informationen über den Inhalt, Kontaktdaten, Berufsbezeichnung und Umsatzsteuer-ID bereit. Es enthält außerdem Links zu weiteren rechtlichen Dokumenten wie der Datenschutzerklärung und Allgemeinen Geschäftsbedingungen.

---

### resources/views/livewire/auth/confirm-password.blade.php
Diese Datei enthält ein Formular zur Bestätigung eines Passworts in einer sicheren Anwendung. Es wird über das Livewire-Framework erstellt und enthält eine Anmelde-Header-Spalte, einen Status-Bereich für Authentifizierungsstatus und ein Bestätigungsformular mit einem Eingabefeld für das Passwort sowie einem Bestätigungsbutton.

---

### resources/views/livewire/auth/forgot-password.blade.php
E-Mail Wiederherstellung für vergessenes Passwort: Formular, E-Mail-Adresse-Eingabe, Zustand-Anzeige und Routen-Weiterleitung.

---

### resources/views/livewire/auth/login.blade.php
Dies ist die Login-Seite für Benutzer, mit Formular für E-Mail und Passwort, sowie Optionen zum Erinnern des Nutzers. Außerdem gibt es Links zur Passwort-Restitution und Registrierung.

---

### resources/views/livewire/auth/register.blade.php
Diese Datei ist ein Register-Formular, das eine Benutzerregistrierung ermöglicht. Es enthält Felder für Vorname, Nachname, E-Mail-Adresse, Passwort und Bestätigung des Passworts. Die Formulareingaben werden mittels POST-Anfrage an den Routenpunkt 'register.store' übermittelt.

---

### resources/views/livewire/auth/reset-password.blade.php
Reset-Passwort-Seite mit Eingabefeldern für neue Passwörter und Bestätigung des Passworts, sowie submit-Button.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Die Datei enthält ein Formular für den zweifaktorigen Authentifizierungsprozess. Benutzer können zwischen einer Code- und einer Recovery-Code-Eingabe entscheiden.

---

### resources/views/livewire/auth/verify-email.blade.php
Verifizierungsemail: Benutzer wird dazu aufgefordert, ihre E-Mail-Adresse zu verifizieren. Zurücksetzen und Logout-Funktionen sind ebenfalls vorhanden.

---

### resources/views/livewire/chat/big-research-chat.blade.php
```
{
  "file_summary": "Livewire-Komponente für eine große Chat-Anwendung.\n\t- Laden Sie die Chat-Messages ab\n\t- Senden Sie Nachrichten über einen Webhook an einen Assistant\n\t- Fügen Sie eine Überprüfung ein, um sicherzustellen, dass der User angemeldet ist und ein Webhook konfiguriert ist.\n\t- Führen Sie eine Validierung durch, um sicherzustellen, dass die Nachricht nicht zu lang ist.\n\t- Wenn die Antwort erfolgreich ist, fügen Sie den Assistant-Chat in die Datenbank hinzu. Wenn die Antwort fehlschlägt, fügen Sie einen Fehler-Chat hinzu.",
  "important_classes": ["Livewire\Volt\Component", "App\Models\ChatMessage", "App\Models\Webhook"],
  "important_functions": ["getWebhook()", "getChatMessages()", "sendMessage()", "clearHistory()"],
  "external_dependencies": ["Http", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p1.blade.php
```
{
  "file_summary": "Dieses Blade-Template ermöglicht die Verwaltung von Projekten über einen Livewire-Komponenten. Es umfasst CRUD-Funktionen für Strukturmodelle (Strukturmodellwahl), Komponenten, Kriterien und Warnsignale. Die Daten werden in verschiedenen Modelleingaben gespeichert.",
  "main_classes": ["Livewire\Volt\Component", "App\Models\Recherche\\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}"],
  "important_functions": ["saveSmw", "editSmw", "deleteSmw", ... (für alle CRUD-Funktionen)],
  "external_dependencies": ["Auth", "Livewire\Volt\Component"]
}
```

---

### resources/views/livewire/recherche/phase-p2.blade.php
```
{
  "file_summary": "Livewire-Komponente für Projekt-Phasen-P2, beinhaltet CRUD-Funktionen für Review-Typen, Cluster, Mapping-Suchstring-Komponenten und Trefferlisten.",
  "important_classes": ["P2ReviewTypEntscheidung", "P2Cluster", "P2MappingSuchstringKomponente", "P2Trefferliste"],
  "external_dependencies": ["App\Models\Recherche", "Livewire\Volt\Component"]
}
```

---

### resources/views/livewire/recherche/phase-p3.blade.php
```json
{
  "file_summary": "Livewire-Komponente für die Projekt-Phase P3: Datenbankmatrix, Disziplin, geografischer Filter und graue Literatur bearbeiten.",
  "key_classes": ["P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"],
  "main_functions": ["newDb", "saveDb", "editDb", "deleteDb", ...],
  "external_dependencies": ["App\Models\Recherche\", \"Illuminate\Support\Facades\Auth\"]
}
```

---

### resources/views/livewire/recherche/phase-p4.blade.php
```
{
  "file_summary": "Livewire-Komponente für Projekt-P4-Anpassungen. CRUD-Funktionen für Suchstrings, Thesaurus-Mappings und Anpassungsprotokolle. Datenbankzugriffe über P4Suchstring, P4ThesaurusMapping und P4Anpassungsprotokoll-Modelle.",
  "key_classes": ["Livewire\Volt\Component", "P4Suchstring", "P4ThesaurusMapping", "P4Anpassungsprotokoll"],
  "key_functions": ["newSs", "saveSs", "editSs", "deleteSs", ...],
  "external_dependencies": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p5.blade.php
```
{
  "file_summary": "Die Datei 'phase-p5.blade.php' ist ein Livewire-Komponenten-Template für das Recherche-Tool. Sie ermöglicht die Verwaltung und Bearbeitung von PRISMA-Zahlen, Screening-Kriterien, Tools und Treffern in einem Projekt. Die Komponente bietet CRUD-Funktionen (Create, Read, Update, Delete) für diese Daten sowie Formulare zur Bearbeitung und Speicherung neuer oder existierender Einträge.",
  "main_classes": ["Livewire\Volt\Component"],
  "important_functions": ["newPrisma", "savePrisma", "editPrisma", "deletePrisma", "newSk", "saveSk", "editSk", "deleteSk", ...],
  "external_dependencies": ["App\Models\Recherche\Projekt", "P5Treffer", "P5ScreeningKriterium", "P5ScreeningEntscheidung", "P5ToolEntscheidung", "P5PrismaZahlen"],
  "database_access": ["P5Treffer", "P5ScreeningEntscheidung"]
}
```

---

### resources/views/livewire/recherche/phase-p6.blade.php
```json
{
  "file_summary": "Recherche-Funktionen für Qualitätsbewertung und Lückenanalyse, integriert in Livewire-Komponente. Externe Abhängigkeiten: App\Models\Recherche\{Projekt, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse}, Auth, Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
```
{
  "file_summary": "Livewire-Komponente für das Management von Projekt-Phasen P7.
    Es gibt CRUD-Funktionen für Synthese-Methode, Datenextraktion, Muster/Konsistenz und GRADE.
    Die Komponente verwendet Modelle wie Projekt, P5Treffer, P7SyntheseMethode, ...
    Die externe Abhängigkeit besteht in der Verwendung von Livewire und den entsprechenden Modellen."
}
```

---

### resources/views/livewire/recherche/phase-p8.blade.php
```
{
  "file_summary": "Die Datei ist Teil einer Livewire-Komponente und bietet CRUD-Funktionen für verschiedene Entitäten wie Suchprotokolle, Limitationen, Reproduzierbarkeitsprüfungen und Update-Pläne. Sie verwendet Eloquent-Modelle wie P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung und P8UpdatePlan.",
  "important_classes": ["P8Suchprotokoll", "P8Limitation", "P8Reproduzierbarkeitspruefung", "P8UpdatePlan"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/projekt-detail.blade.php
```json
{
  "file_summary": "Diese Blade-Datei stellt ein Livewire-Komponenten für das Anzeigen von Projekt-Details dar. Die Komponente lädt Daten aus der Datenbank und zeigt die Details eines Projekts an, einschließlich seiner Phasenstatus und verschiedenen Tab-Inhalten. Externe Abhängigkeiten: App\Models\Recherche\Projekt, Livewire."
}
```

---

### resources/views/livewire/recherche/projekt-liste.blade.php
```
{
  "file_summary": "Die Datei erstellt eine Liste von Projekten, die vom aktuellen Benutzer erstellten wurden. Die Liste enthält den Projekt-Titel, das Review-Typ und den Erstellungs-Zeitpunkt. Es gibt auch die Möglichkeit, ein Projekt zu löschen.",
  "important_components": [
    "projekte() Funktion, um alle Projekte des aktuellen Benutzers zurückzugeben",
    "deleteProjekt($id) Methode, um ein bestimmtes Projekt zu löschen"
  ],
  "external_dependencies": [
    "Illuminate\Support\Facades\Auth", für die Authentifizierung,
    "Livewire\Volt\Component", für das Livewire-Rendering
  ]
}
```

---

### resources/views/livewire/recherche/research-input.blade.php
Die Datei handelt sich um eine Livewire-Komponente, die einen Benutzer dazu auffordert, eine Forschungsfrage oder ein Thema einzugeben. Wenn der Benutzer auf den "Recherche starten"-Button klickt, wird die Eingabe validiert und dann ein neues Projekt in der Datenbank erstellt. Als Nächstes wird ein Job ausgelöst, um einen Agenten zu triggeren, welcher die Recherche durchführt. Anschließend wird der Benutzer auf eine neue Seite umgeleitet.

---

### resources/views/livewire/settings/appearance.blade.php
```json
{
  "file_summary": "Die Datei enthält ein Livewire-Component, das die Einstellungen für die Benutzeransicht (Appearance) anzeigt und verwaltet. Es enthält Buttons zur Auswahl zwischen Licht-, Dunkel- oder System-Thema. Die Auswahl wird in lokalen Storage gespeichert und auf dem Dokumentelement angewendet.",
  "main_classes": ["Livewire\Volt\Component"],
  "important_functions": [],
  "external_dependencies": ["localStorage", "window.matchMedia"],
  "endpoints": []
}
```

---

### resources/views/livewire/settings/delete-user-form.blade.php
```
{
  "file_summary": "Diese Blade-Vorlage erstellt ein Modal-Fenster zum Löschen eines Benutzerkontos. Es enthält Formularelemente, um sicherzustellen, dass der Benutzer seine Passwörter bestätigen muss, bevor sein Konto endgültig gelöscht wird.",
  "important_classes": ["Livewire\Volt\Component"],
  "important_functions": ["openModal", "closeModal", "deleteUser"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/settings/password.blade.php
```
{
  "file_summary": "Die Datei enthält eine Livewire-Komponente für das Ändern des Passworts eines Benutzers. Sie überprüft die aktuellen und neuen Passwörter, validiert sie und aktualisiert das Passwort der Authentifizierung des Benutzers.",
  " wichtige Klassen/Funktionen": ["Livewire\Volt\Component", "Auth"],
  "Externe Abhängigkeiten": ["Illuminate\Support\Facades\Auth", "Illuminate\Validation\Rules\Password"]
}
```

---

### resources/views/livewire/settings/profile.blade.php
Diese Datei stellt das Profil-Einstellungs-Formular bereit, in dem der Benutzer seine persönlichen Informationen wie Name und E-Mail-Adresse aktualisieren kann. Die Formulareingaben werden über Livewire validiert und auf Serverseite verarbeitet.

---

### resources/views/livewire/settings/two-factor.blade.php
```
{
  "file_summary": "Diese Datei enthält einen Livewire-Komponenten zur Verwaltung der Zwei-Faktor-Authentifizierung. Die Komponente ermöglicht es Benutzern, die Zwei-Faktor-Authentifizierung zu aktivieren oder deaktivieren und überprüft den Authentifizierungscode.",
  "important_classes": ["TwoFactorSettings"],
  "key_functions": ["enable", "disable", "confirmTwoFactor", "showVerificationIfNecessary", "loadSetupData"],
  "external_dependencies": [
    {"name": "Laravel\Fortify\Actions\ConfirmTwoFactorAuthentication"},
    {"name": "Laravel\Fortify\Actions\DisableTwoFactorAuthentication"},
    {"name": "Laravel\Fortify\Actions\EnableTwoFactorAuthentication"},
    {"name": "Livewire\\Attributes\\Locked"}
  ]
}
```

---

### resources/views/livewire/settings/webhooks.blade.php
```json
{
  "file_summary": "Diese Datei enthält eine Livewire-Komponente für die Konfiguration von Webhooks in der Settings-Seite des Frontends. Es gibt zwei Slots: Dashboard Chat und Recherche starten. Jeder Slot erlaubt das Einrichten einer Webhook-URL und eines geheimen Codes (optional). Die Konfiguration wird über die saveDashboard bzw. saveRecherche-Funktion gespeichert, wobei die Webhook-Einstellungen aktualisiert oder neu erstellt werden können.",
  "key_classes": ["Livewire\Volt\Component"],
  "important_functions": ["saveDashboard", "saveRecherche", "clearDashboard", "clearRecherche"],
  "extern_abhaengigkeiten": [
    {"name": "Webhook", "type": "Model"},
    {"name": "Auth", "type": "Facade"},
    {"name": "Str", "type": "Support"}
  ]
}
```

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
```json
{
  "file_summary": "Diese Datei stellt ein Livewire-Komponenten-Template für die Anzeige und Verwaltung von Wiederherstellungs-Codes zum zweifaktorischen Authentifizierungsprotokoll (2FA) bereit. Die Komponente ermöglicht es dem Benutzer, seine aktuellen Wiederherstellungs-Codes anzusehen und neue Codes zu generieren.",
  "important_classes": ["Livewire\Volt\Component", "GenerateNewRecoveryCodes"],
  "external_dependencies": ["Laravel\Fortify\Actions\GenerateNewRecoveryCodes"]
}
```

---

### resources/views/partials/head.blade.php
Diese Datei generiert HTML-Head-Seiteninhalte für die Webanwendung. Sie enthält Meta-Tags, Titel, Links zu Favicons und externe CSS/JS-Bibliotheken.

---

### resources/views/partials/settings-heading.blade.php
Die Datei settings-heading.blade.php enthält den HTML-Code für einen Überschriftenbereich in einer Anwendung. Sie stellt eine Überschrift und eine kurze Beschreibung dar, die sich auf Einstellungen bezieht. Die wichtigsten Komponenten sind der H1-Titel, ein Punkt für die Beschreibung und ein Horizontallinien-Element. Es gibt keine externe Abhängigkeiten oder Datenbankzugriffe.

---

### resources/views/recherche/index.blade.php
Template für Recherchefunktionen in einer webbasierten Anwendung
Wichtige Klassen/Endpunkte:
- recherche.research-input
- recherche.projekt-liste
Externe Abhängigkeiten:
- livewire
APIs/DB-Zugriffe: None

---

### resources/views/recherche/show.blade.php
Diese Datei ist für die Darstellung eines Projektdetails-Seiten verantwortlich. Sie enthält eine App-Layout und ein Livewire-Komponenten, um den Projekt-Detail-Inhalt darzustellen.

---

## uncategorized

### Readme.md
Die Datei beschreibt eine Laravel-Anwendung namens Linn.Games, die als Research-Management-Plattform mit KI-gestützter systematischer Literaturrecherche fungiert. Sie enthält Informationen zur Architektur, Features, Routen, Datenbank und Konfiguration.

---

### artisan
Laravel-Anwendung als CLI-Tool starten, Composer Autoloader laden, Laravel Bootstrap ausführen und Befehl bearbeiten

---

### basemodel.md
Datenbankmodell zur systematischen Literaturrecherche mit verschiedenen Phasen und Tabellen für Projekte, Phasen-Tracking, Fragestellung, Explorative Mapping-Suche, Datenbankauswahl und geografische Filterung.

---

### composer.json
Dies ist die composer.json-Datei für das Laravel-Projekt 'nileneb/app-linn-games'. Sie beinhaltet eine Liste der erforderlichen Abhängigkeiten, Autoloading-Konfiguration und Skripte zur Projektierung. Die wichtigsten Klassen sind App, Database\Factories, Database\Seeders und Tests.

---

### CONTRIBUTING.md
Beschreibung der Datei CONTRIBUTING.md

---

### deploy.sh
Dieses Skript deployt eine Applikation, indem es ein Produktions-Image baut und mit Docker Compose hochfährt. Es überprüft die Voraussetzungen, baut die Datenbank um, erstellt einen Backup und lädt die neuesten Images von PostgreSQL und Redis. Schließlich sendet es eine Benachrichtigung per E-Mail.

---

### package.json
```
{
  "file_summary": "Package-Manager-Konfiguration für ein Vite-Projekt\n\
Wichtige Funktionen:\
- Projekt-Setup mit Vite und Tailwind CSS,\
- Abhängigkeiten von Axios, Autoprefixer und Laravel-Vite-Plugin,\
- Scripte zum Build- und Entwicklungsprozess.",
  "main_classes": ["package"],
  "external_dependencies": [
    "@tailwindcss/vite",
    "autoprefixer",
    "axios",
    "concurrently",
    "laravel-vite-plugin",
    "tailwindcss",
    "vite"
  ]
}
```

---

### phpunit.xml
PHPUnit-Konfiguration für Tests in Laravel-Projekten. Konfiguriert die Ausführung von Unit- und Feature-Tests, einschließlich des Bootstraping-Verhaltens, der Test-Suites und Umgebungsvariablen für den Test-Zustand.

---

### vite.config.js
Vite-Konfigurationsdatei für Laravel-Projekt. Konfiguriert Vite-Server und integriert Laravel-Vite-Plugin, Tailwindcss und CORS-Unterstützung.

---

### .dockerignore
Diese Datei definiert, welche Dateien und Verzeichnisse vom Docker-Container aus ignoriert werden sollen.

---

### .editorconfig
Ein Editor-Konfigurationsdatei, die die Einstellungen für verschiedene Dateitypen festlegt. Diese Datei definiert die Codierung, Zeilenendung, Einrückungen und weitere Formatierungsregeln.

---

### app/Actions/Fortify/CreateNewUser.php
```
{
  "file_summary": "Diese Datei implementiert die Funktionalität zur Erstellung eines neuen Benutzers. Sie verwendet Laravel's Fortify-Konfiguration und Validierungsregeln. Die wichtigsten Klassen sind CreateUser, User und Validator.",
  "key_classes_functions_endpoints": [
    "CreateNewUser",
    "Validator",
    "User"
  ],
  "external_dependencies": [
    "Illuminate\Support\Facades\Validator",
    "Laravel\Fortify\Contracts\CreatesNewUsers",
    "App\Models\User"
  ]
}
```

---

### app/Actions/Fortify/PasswordValidationRules.php
Bereitstellt Regeln für die Validierung von Passwörtern. Wichtigste Funktion: passwordRules(), welche einen Array mit Validierungsregeln zurückgibt. Verwendet die Illuminate\Validation\Rules\Password-Klasse.

---

### app/Actions/Fortify/ResetUserPassword.php
Klasse zum Zurücksetzen eines Benutzerpassworts. Implementiert die "ResetsUserPasswords"-Schnittstelle von Laravel Fortify. Validiert das neue Passwort und aktualisiert den Benutzer.

---

### app/Filament/Resources/ContactResource.php
Ressourcen-Definition für Kontakte in Filament, beinhaltet Formulare, Tabellen und Seiten. Importiert Model 'Contact', bietet Formularfelder und Tabelle zur Darstellung von Kontaktdaten.

---

### app/Filament/Resources/UserResource.php
```
{
  "file_summary": "Diese Datei definiert die Ressourcen-Entität für Nutzer in Filament. Sie enthält Formulare, Tabellen und Seiten zur Darstellung von Nutzern. Externe Abhängigkeiten sind Filament's Forms-, Tables- und Schema-Klassen.",
  "important_classes": ["UserResource", "Schema"],
  "important_functions": ["form", "table", "getPages"],
  "external_dependencies": ["Filament\Forms", "Filament\Schemas\Schema", "Filament\Tables"]
}
```

---

### app/Http/Middleware/TrackPageView.php
```
{
  "file_summary": "Diese Datei ist ein Middleware für die Tracking von Seitenaufrufen. Sie überprüft, ob der Aufrufer kein Bot ist und wenn nicht, speichert sie den Seitenaufruf in der Datenbank. Externe Abhängigkeiten: App\Models\PageView, Illuminate\Http\Request.",
  "important_classes": ["TrackPageView"],
  "key_functions": ["handle", "anonymizeIp", "isBot"],
  "external_dependencies": ["App\Models\PageView", "Illuminate\Http\Request"]
}
```

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware zur Überprüfung von Webhook-Signaturen für Langdock. Verifiziert Timestamp und Signatur durch Hashierung. Replikationsprävention mittels Cache.

---

### app/Http/Middleware/VerifyMcpToken.php
Überprüft den Auth-Token im Header und gibt Zugriffsberechtigung basierend auf Eingabe

---

### app/Jobs/IngestPaperJob.php
Diese Datei enthält die Implementierung der IngestPaperJob-Klasse, die papierbasierte Daten in das Projekt-System einstellt. Sie nutzt das Ollama-API für Embeddings und speichert die Ergebnisse in einer Datenbank-Tabelle. Die wichtigsten Funktionen sind: chunkText (das Text in Blöcke zerlegt), handle (die primäre Logik der Job-Ausführung). Externe Abhängigkeiten umfassen das Ollama-API über HTTP-POST-Anfragen und die lokale Datenbank.

---

### app/Jobs/TriggerLangdockAgent.php
```json
{
  "file_summary": "Diese Datei implementiert eine Job-Klasse 'TriggerLangdockAgent', die als Queue-Aufgabe ausgeführt wird. Sie setzt den Langdock-Agent aus, indem sie einen Webhook an Langdock sendet und überprüft, ob die Antwort erfolgreich war. Die Klasse verwendet die Laravel-Queue-Funktionen und die Http-Client-Klasse von Laravel, um eine POST-Anfrage an den Langdock-Webhook zu senden. Wenn die Anfrage fehlschlägt, wird ein Fehler aufgezeichnet und die Job-Aufgabe abgebrochen.",
  "wichtige_Klassen": ["TriggerLangdockAgent"],
  "wichtige_Funktionen": ["uniqueId", "handle"],
  "externe_Abhängigkeiten": ["Illuminate\Http\Client\PendingResponse", "App\Models\Webhook"]
}
```

---

### app/Livewire/Actions/Logout.php
Diese Datei handelt es sich um einen Logout-Aktion in der Anwendung, die den aktuellen Benutzer aus dem System abmeldet. Sie nutzt Auth-Funktionen und Sitzungszugriffe. Die wichtigste Funktion ist __invoke().

---

### app/Mail/ContactConfirmationMail.php
Die Datei sendet eine E-Mail zur Bestätigung von Kontaktanfragen. Sie verwendet die Laravel-Mailing-Funktionen und bindet den Contact-Model ein.

---

### app/Mail/ContactInquiryMail.php
Diese Datei sendet eine E-Mail an den Administrator, wenn ein neuer Kontaktanfrager eine Nachricht hinterlässt. Sie verarbeitet den Anfrager-Daten über die Contact-Model-Klasse und verwendet das Mail-Building-Framework von Laravel.

---

### app/Mail/DeployNotificationMail.php
```
{
  "file_summary": "Diese Datei erstellt eine Benachrichtigungs-E-Mail über die erfolgreiche Bereitstellung eines Apps. Die E-Mail enthält den Zeitpunkt der Bereitstellung und einen Link zur App-URL. 
  Die wichtigsten Klassen sind DeployNotificationMail, Envelope und Content.
  Externe Abhängigkeiten: 
  - Illuminate\Mail\Mailable
  - Illuminate\Mail\Mailables\Content
  - Illuminate\Mail\Mailables\Envelope
  - Datenbankabfrage zur App-URL nicht explizit angegeben, aber vermutlich wird die E-Mail aus einer Konfiguration oder einem anderen Quellen abgeleitet"
}
```

---

### app/Policies/ProjektPolicy.php
```
{
  "file_summary": "Diese Datei definiert eine Policy für Projekte. Sie überprüft Benutzerberechtigungen zum Ansehen, Erstellen, Aktualisieren und Löschen von Projekten. Die wichtigsten Klassen sind ProjektPolicy, User und Projekt.",
  "main_classes": ["ProjektPolicy", "User", "Projekt"],
  "important_functions": ["viewAny", "view", "create", "update", "delete"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "App\Models\User"]
}
```

---

### app/Providers/AppServiceProvider.php
Konfiguriert Application-Gates, Rate-Limits und SSL-Force-Through für die Anwendung in einer Produktionsumgebung.

---

### app/Providers/FortifyServiceProvider.php
```json
{
  "file_summary": "Diese Datei ist der Fortify-Service-Provider und konfiguriert die Sicherheitsfunktionen in einer Laravel-Anwendung. Es handelt sich um eine zentrale Stelle für die Konfiguration von Passwortschutz, Registrierung, Login, Zwei-Faktor-Authentifizierung und mehr. Die wichtigsten Klassen sind Fortify, RateLimiter und das Register und Booten des Service-Providers.",
  "important_classes_functions_endpoints": {
    "Fortify": "",
    "RateLimiter": "",
    "CreateNewUser": "Klasse zum Erstellen neuer Benutzer",
    "ResetUserPassword": "Klasse zum Zurücksetzen von Benutzern-Passwörtern"
  },
  "external_dependencies": [
    "Illuminate\Cache\RateLimiting\Limit",
    "Illuminate\Http\Request",
    "Laravel\Fortify\Fortify"
  ]
}
```

---

### app/Providers/VoltServiceProvider.php
```json
{
  "file_summary": "Diese Datei registriert und konfiguriert die Livewire-Integration mit Laravel.",
  "main_classes": ["VoltServiceProvider", "Volt"],
  "important_functions": ["mount"],
  "external_dependencies": ["Illuminate\Support\ServiceProvider", "Livewire\Volt\Volt"]
}
```

---

### app/Providers/Filament/AdminPanelProvider.php
```
{
  "file_summary": "Dokumentiert die Funktionen der Admin-Panel-Erweiterung für Filament.
  - Definiert ein Panel mit ID 'admin' und Pfad '/admin'.
  - Konfiguriert Middleware zur Authentifizierung, Session-Management, Fehler-Teilung und mehr.
  - Registriert Resource-Entitäten aus der `app/Filament/Resources`-Menge."
}
```

---

### bootstrap/app.php
```
{
  "file_summary": "Diese Datei konfiguriert die grundlegende Funktionalität einer Laravel-Webanwendung. 
Wichtige Funktionen sind die Routings-Konfiguration, Middleware-Einbindung und Ausnahmemanagement.
Die wichtigsten Klassen sind Application, Request und Exceptions.
Externe Abhängigkeiten bestehen zur Laravel-Framework (Illuminate).
Die Datei initialisiert die Anwendungsinstanz und setzt wichtige Einstellungen."
}
```

---

### bootstrap/providers.php
Definiert Anbieter für Framework-Funktionen: Authentifizierung (Fortify), Admin-Panel (Filament) und Volt. Enthält eine Liste von Provider-Klassen.

---

### bootstrap/cache/.gitignore
Git-Ignore-Datei, ignoriert das gesamte Verzeichnis und die Datei .gitignore selbst

---

### docker/common/nginx/default.conf
Dieser Nginx-Config-Datei konfiguriert den Webservice für verschiedene Anwendungen, darunter Livewire, MCP PostgreSQL Endpoint und Ollama Embedding Endpoint. Die wichtigsten Funktionen sind die Weiterleitung von Anfragen an diverse Backends (z.B. PHP-FPM), das Setzen von Headern und dem Cache-Controll-Header. Externe Abhängigkeiten: Docker, Postgres, Livewire, Ollama und PHP-FPM.

---

### docker/common/nginx/docker-entrypoint.sh
```json
{
  "file_summary": "Dieses Skript ist der Docker-Eintrittspunkt für Nginx-Container und dient zum Konfigurieren von Nginx mit einer Template-Konfiguration. 
  Es überprüft, ob eine Vorlagen-Datei existiert und generiert dann die Konfigurationsdatei. Danach wird der MCP-Authorisierungstoken-Placeholder in der Konfiguration ersetzt.
  Die Ausführung des Skripts erfolgt am Ende durch Aufrufen der ersten Zeichenfolge mit 'exec'."
}
```

---

### docker/common/nginx/dockerfile
```
{
  "file_summary": "Dockerfile für Nginx-Bild, einschließlich Konfiguration und Einbindung von Secrets. Hauptfunktionen: 
    - Nginx-Konfiguration in /etc/nginx/nginx.conf
    - Default-Site-Konfiguration in /etc/nginx/conf.d/default.conf
    - Einbindung von Secrets durch Eintrittspunkt-Script docker-entrypoint.sh
    - Kopieren statischer Inhalte aus ./public nach /var/www/public
    Externe Abhängigkeiten: Nginx:alpine-Bild
    Wichtige Klassen/ Funktionen/ Endpunkte: docker-entrypoint.sh"
}
```

---

### docker/common/nginx/nginx.conf
Konfiguration für die Nginx-Instanz zur Verbindung mit einer Laravel-Anwendung. Diese Datei enthält grundlegende Einstellungen wie gzip-Komprimierung, Sicherheitsmaßnahmen und Konfigurationen zum Upload von Dateien.

---

### docker/common/php-fpm/dockerfile
```
{
  "file_summary": "Docker-Datei für PHP-FPM-Umgebungen: 
    - Aufbaut Staging-Umgebung, installiert Abhängigkeiten und Pakete.
    - Kopiert die Anwendungskomponenten in den Container.
    - Installiert Composer und Abhängigkeiten.
    - Baut frontend-Assets (Vite) auf.
    - Setzt Staging-Umgebungen für Produktions- und Testumgebungen vor.
    Wichtige Klassen/ Funktionen: composer, docker-php-ext-install,
    externe Abhängigkeiten: apt-get, curl, npm"
}
```

---

### docker/common/php-fpm/entrypoint.sh
Das Script initialisiert die Umgebung für PHP-FPM und läuft Migrations, Caching und Routen-Caching durch. Es wartet auf eine verfügbare Datenbankverbindung und synchronisiert statische Dateien mit einem gemeinsamen Volume.

---

### docker/common/postgres/dockerfile
Dockerfile für die PostgreSQL-Datenbank. Der Dockerbuild erzeugt einen Postgres Container mit HypoPG und PGVector Extensions.

---

### docker/common/postgres/init-extensions.sql
Initialisiert PostgreSQL-Erweiterungen für Datenbank-Statistiken und Optimierung

---

### docs/overview-2026-04-02_0617.md
Die Datei enthält eine umfassende Dokumentation zur Funktionsweise einer Anwendung. Sie besteht aus verschiedenen Modulen wie API-Endpunkten, Controllers, Routen und Konfigurationsdateien. Die Anwendung unterstützt Funktionen wie Authentifizierung, Webhooks, Papier-Ingestion, Suche nach Textsimilarität und Datenschutz (DSGVO). Es werden externe Abhängigkeiten wie Docker Compose, GitHub Actions und verschiedene Datenbank- und Cache-Speicher verwendet.

---

### public/index.php
```
{
  "file_summary": "Dies ist der Eingangspunkt für das Laravel-Projekt. Die Datei verantwortet das Einrichten des Frameworks und die Verarbeitung von Anfragen.",
  "key_classes_functions": {
    "Application": "Die Anwendungsinstanz",
    "Request": "Ein Objekt zur Abwicklung von HTTP-Anfragen",
    "handleRequest": "Funktion zum Verarbeiten der Anfrage"
  },
  "external_dependencies": [
    "Illuminate/Foundation/Application",
    "Illuminate/Http/Request",
    "vendor/autoload.php" (Composer-Autoloader)
  ]
}
```

---

### public/robots.txt
Diese Datei definiert die Zugriffsrechte für Webcrawlers (z.B. Googlebot) und gibt an, welche Bereiche der Website nicht indexiert werden sollen.

---

### public/site.webmanifest
Diese Datei enthält die Manifest-Daten für das Web-Application-Manifest einer Website. Sie definiert Icons, Farben und Anzeigemodus. Die wichtigsten Klassen oder Funktionen sind keine. Externe Abhängigkeiten sind keine.

---

### public/.htaccess
Apache-Konfiguration zum Routing und zu Sicherheit auf einem Laravel-Projekt, einschließlich der Verarbeitung von Authentifizierungs- und XSRF-Tokens.

---

### resources/css/app.css
Diese Datei enthält eine Sammlung von Stilen und Variablen für ein CSS-Projekt. Es werden externe Abhängigkeiten wie Tailwindcss importiert und eigene Themen konfiguriert.

---

### resources/css/welcome.css
Dies ist ein CSS-Datei, die das Design und die Übersicht einer Website steuert. Die Datei definiert Farben, Schriftarten, Größen und Effekte für die Website. Es werden auch Animations-Effekte für Scroll-Reveal-Anwendungen und andere Interaktionen definiert.

---

### resources/js/welcome.js
```json
{
  "file_summary": "Die Datei 'welcome.js' ist eine Clientseitige Skript-Datei, die verschiedene Funktionen für das Frontend einer Webanwendung enthält. 
    - Automatisches Setzen des Jahres im Footer.
    - Karten-Klick-Handler für die Anzeige von Snake3D-Content.
    - Laden eines Snake3D-Iframe.
    - Mobile Navigation mit Toggle-Button.
    - Header-Scroll-Effekt.
    - Cursor-Glow-Effekt.
    - Scroll-Reveal-Animations für verschiedene Elemente.
    - Glatter Scroll für ankern Links.
    - Animation von Telemetrie-Werten (Simulierte Live-Daten).
    - Kontaktformular-Submission mit AJAX-Anfrage und Fehlerbehandlung.
    - Console-Easter-Egg."
}
```

---

### storage/app/public/.gitignore
Ignoriert alle Daten in dem Verzeichnis, außer der .gitignore Datei selbst.

---

### .github/copilot-instructions.md
Diese Datei enthält Anweisungen für die Entwicklung des Apps.linn.games-Projekts mit Laravel und Livewire. Es werden Sprachkonventionen (Englisch für Code, Deutsch für Kommunikation), Regeln für die Projektstruktur, Docker-Workflow, CI/CD-Deploy-Mechanismus, Test-Syntax und -Datenbank, Port-Freigabe und Git-Arbeit definiert. Die Datei enthält auch wichtige Klassen wie `User::factory()` und externe Abhängigkeiten wie PostgreSQL, Redis, Pest und Filament.

---

### .github/instructions/app_linn_games_custom.instructions.md
Diese Datei beschreibt die Architektur und Konventionen von app.linn.games. Sie enthält Informationen über die Projektübersicht, den Software-Stack, die Verzeichnisstruktur, das Datenmodell, Authentifizierung und Autorisierung sowie die KI-Integration.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker-Entwicklungsumgebung für PHP-Anwendung. Beschreibt die Containerarchitektur, Code- und Assets-Deployments, sowie Sicherheitsregeln für docker-compose.yml.

---
