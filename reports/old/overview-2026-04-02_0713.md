---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T07:13:17.744759
model: codellama
run_id: codellama
mode: overview
files_total: 245
errors: 0
run_time_s: 1620.4
---

# Funktions-Übersicht — 2026-04-02_0713

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 245
- **Fehler:** 0
- **Laufzeit:** 1620.4s

## api

### app/Http/Controllers/ContactController.php
The file `app/Http/Controllers/ContactController.php` contains a controller class for handling contact requests from the website visitors. The controller is using Laravel's built-in validation and mail functionality to validate the input data, create a new contact entry in the database, and send an email confirmation to both the visitor and Linn Games.

The `ContactController` class extends Laravel's `Controller` class and defines a single method called `store`, which is responsible for handling the contact request submission. The method first validates the input data using Laravel's built-in validation functionality, then creates a new contact entry in the database using Eloquent's `create` method. Finally, it sends two emails: one to Linn Games (using the `Mail::to('info@linn.games')->queue(new ContactInquiryMail($contact));` line) and another to the visitor (using the `Mail::to($contact->email)->queue(new ContactConfirmationMail($contact));` line).

The external dependencies of this controller include Laravel's built-in validation, mail functionality, and Eloquent ORM for working with the database.

---

### app/Http/Controllers/Controller.php
This file is a PHP class that defines an abstract controller for the API endpoints in the Laravel framework. The class is named `Controller` and is located in the `App\Http\Controllers` namespace. It is an abstract class, meaning it cannot be instantiated directly but must be extended by other classes to provide specific functionality.

The file imports several external dependencies:

* `Illuminate\Foundation\Bus\DispatchesJobs`: This trait allows the controller to dispatch jobs using Laravel's job queue system.
* `Illuminate\Routing\Controller`: This class provides the base functionality for all controllers in Laravel, including methods for rendering views and responding to requests.
* `Illuminate\Foundation\Validation\ValidatesRequests`: This trait allows the controller to validate incoming request data using Laravel's validation system.
* `Illuminate\Foundation\Auth\Access\AuthorizesRequests`: This trait allows the controller to authorize incoming requests using Laravel's authentication and authorization system.
* `Illuminate\Foundation\Auth\Access\AuthorizesResources`: This trait allows the controller to authorize resources using Laravel's resource-based authorization system.
* `Illuminate\Foundation\Validation\ValidatesRequests`: This trait allows the controller to validate incoming request data using Laravel's validation system.

The class defines several abstract methods that must be implemented by classes that extend it:

* `__construct()`: This method is called when a new instance of the class is created and is used to initialize any necessary resources or dependencies.
* `__invoke()`: This method is called when the controller is invoked and is used to handle incoming requests.
* `middleware()`: This method is called to define any middleware that should be applied to the controller's actions.
* `__destruct()`: This method is called when the class instance is destroyed and is used to clean up any resources or dependencies that were created during its lifetime.

---

### app/Http/Controllers/DsgvoController.php
File Summary:
This file is part of the Laravel framework and contains two methods for exporting and deleting user data according to the German Data Protection Act (DSGVO). The first method, `export`, exports a JSON object containing the user's basic information, their contacts, and their consents. The second method, `deleteAccount`, logs the user out and deletes their account from the database.

---

### app/Http/Controllers/LangdockWebhookController.php
* Die Datei `LangdockWebhookController.php` ist ein Controller in einer Laravel-Anwendung, der eine Webhook-API für das Langdock-Projekt implementiert.
* Die Klasse `LangdockWebhookController` erbt von `Illuminate\Http\Controllers\Controller`, was bedeutet, dass sie die Funktionen und Methoden eines Controllers hat, um HTTP-Anfragen zu verarbeiten.
* Der Controller definiert eine Methode `handle()`, die als Endpunkt für den Webhook fungieren wird. Diese Methode verwendet die Request-Klasse von Laravel, um die ankommenden Anfrage zu validieren und dann die Daten der Anfrage in Variablen zu speichern.
* Der Controller verwendet dann die Dispatcher-Klasse von Laravel, um ein neues Job-Objekt für das TriggerLangdockAgent-Job zu erstellen. Dieses Job ist zuständig für das Ausführen des Agenten und das Verarbeiten der Anfrage.
* Die Methode `handle()` gibt eine JSON-Antwort zurück, die den Status "queued" enthält. Hierbei wird angegeben, dass der Auftrag in die Warteschlange gestellt wurde und wird ausgeführt werden, sobald er seine Ausführung beginnen kann.

Externe Abhängigkeiten:

* Die Datei verwendet die Request-Klasse von Laravel, um die ankommenden Anfrage zu validieren und zu verarbeiten.
* Der Controller verwendet die Dispatcher-Klasse von Laravel, um ein neues Job-Objekt für das TriggerLangdockAgent-Job zu erstellen und es in die Warteschlange zu stellen.
* Die Methode `handle()` verwendet auch die Response-Klasse von Laravel, um eine JSON-Antwort zurückzugeben.

---

### app/Http/Controllers/PaperRagController.php
The given PHP code is a controller for an API that provides two endpoints: `ingest` and `search`.

The `ingest` endpoint takes in a JSON request with the following fields:

* `paper_id`: a unique identifier for the paper (required)
* `source`: the source of the paper (e.g., "arXiv") (required)
* `title`: the title of the paper (required)
* `text`: the text of the paper (required)
* `projekt_id`: an optional ID for a project associated with the paper
* `metadata`: an optional JSON object containing metadata about the paper

The endpoint validates the request data and then dispatches an asynchronous job using Laravel's Jobs system to handle the ingestion of the paper. The job is defined in the `IngestPaperJob` class, which is imported from the `App\Jobs` namespace.

The `search` endpoint takes in a JSON request with the following fields:

* `q`: the query string (required)
* `projekt_id`: an optional ID for a project associated with the search results
* `max_results`: an optional integer representing the maximum number of search results to return (defaults to 5 if not provided)

The endpoint validates the request data and then sends an HTTP POST request to an external service at the URL `http://ollama:11434/api/embeddings` with a JSON body containing the following fields:

* `model`: "nomic-embed-text" (required)
* `prompt`: the query string (required)

The endpoint then retrieves search results from a PostgreSQL database using a SQL SELECT statement that selects the ID, project ID, source, paper ID, title, chunk index, text chunk, and metadata fields for each matching row. The search results are returned as a JSON response to the client.

---

### routes/api.php
This file defines routes for the API of a Laravel application. It includes authentication middleware for Sanctum and VerifyMcpToken middleware for MCP token verification. The file also defines two routes: one for GET /user, which returns the authenticated user information, and another for POST /webhooks/langdock, which handles Langdock webhook events.

---

### routes/console.php
Die Datei `routes/console.php` ist eine Art "Router" für die Laravel-Kommandozeile. Sie enthält zwei Kommandos:

* `inspire`: Der erste Befehl ruft den `Inspiring`-Service auf, der eine Zitat-Funktion bereitstellt, um Inspirationen für Entwickler zu generieren.
* `deploy:notify`: Der zweite Befehl sendet eine E-Mail an einen bestimmten Empfänger (bene@linn.games) mit einer Deploy-Benachrichtigung und gibt eine Meldung aus, dass die E-Mail erfolgreich gesendet wurde.

Die Datei importiert auch zwei Laravel-Facades: `Artisan` und `Mail`, um den Aufruf der Kommandos und das Senden von E-Mails zu ermöglichen.

Insgesamt ist diese Datei eine Möglichkeit, Laravel-Kommandos und -E-Mails auf einem Server auszuführen oder zu planen, ohne dass ein Webinterface erforderlich wäre.

---

### routes/web.php
The `routes/web.php` file defines the routes for the Laravel application. It contains a mix of route definitions using the traditional `Route::` syntax and Livewire Volt syntax.

Here is a summary of the functions of this file in 10 lines or less:

* Defines the home page route, which shows the `welcome` view.
* Defines the contact form route, which uses the `ContactController` class to store the form data.
* Defines routes for legal documents (Impressum, AGB, and DSGVO), which show the corresponding views.
* Defines a dashboard route that requires authentication and verified email address. It redirects to the settings profile page by default.
* Defines a group of routes that require authentication. These include:
	+ Routes for the user profile settings (profile, password, appearance, webhooks).
	+ A route for exporting DSGVO data.
	+ A route for deleting the user account.
	+ A route for searching projects.

The file also imports various classes and uses Laravel's built-in auth and verified email address middleware.

---

## config

### docker-compose.dev.yml
This is a `docker-compose` configuration file for the development environment. It specifies the services that should be run in the development environment, including the web service on port 6481. The file also overrides the default port of the nginx service to 6480, which is needed because port 6481 is already used by Visual Studio Code.

The file includes external dependencies such as `docker-compose` and `nginx`. It also specifies the ports that should be exposed for each service, with the web service exposing port 6480 and nginx exposing port 6481.

Overall, this file is used to set up a development environment for the application, including running the necessary services and exposing them on the correct ports.

---

### docker-compose.yml
This file contains the configuration for a Docker Compose application that includes several services, such as an NGINX web server, a PHP-FPM container, a PostgreSQL database, and a Redis cache. The application also includes a MCP (Multi-Conference Paper) search service that uses Ollama to download papers from multiple sources.

Here is a summary of the services included in the file:

* `web`: An NGINX web server that serves the frontend of the application. It is built using a custom Dockerfile located in the `./docker/common/nginx` directory and includes several volumes, such as the `/var/www/storage` volume for storing user data and the `/var/www/public/build` volume for hosting build assets.
* `php-fpm`: A PHP-FPM container that serves as a backend for the web server. It is built using a custom Dockerfile located in the `./docker/common/php-fpm` directory and includes several volumes, such as the `/var/www/storage` volume for storing user data and the `/var/www/public/build-shared` volume for hosting build assets. The container also has an environment file that specifies some environment variables, such as `APP_ENV`, `DB_CONNECTION`, and `REDIS_HOST`.
* `php-cli`: A PHP CLI container that is used to run commands in the context of the web server. It is built using a custom Dockerfile located in the `./docker/common/php-fpm` directory and includes several volumes, such as the `/var/www/storage` volume for storing user data and the `/var/www/public/build-shared` volume for hosting build assets.
* `php-test`: A PHP container that is used to run tests in the context of the web server. It is built using a custom Dockerfile located in the `./docker/common/php-fpm` directory and includes several volumes, such as the `/var/www/storage` volume for storing user data and the `/var/www/public/build-shared` volume for hosting build assets.
* `queue-worker`: A PHP container that is used to run background jobs in the context of the web server. It is built using a custom Dockerfile located in the `./docker/common/php-fpm` directory and includes several volumes, such as the `/var/www/storage` volume for storing user data and the `/var/www/public/build-shared` volume for hosting build assets.
* `postgres`: A PostgreSQL container that serves as a database for the application. It is built using a custom Dockerfile located in the `./docker/common/postgres` directory and includes several volumes, such as the `/var/lib/postgresql/data` volume for storing data and the `/docker/common/postgres/init-extensions.sql` file for initializing the database.
* `redis`: A Redis cache container that is used to store temporary data. It is built using a standard Dockerfile located in the `redis:alpine` image and includes several volumes, such as the `/var/lib/redis` volume for storing data.
* `postgres-mcp`: A PostgreSQL container that serves as a backend for the MCP search service. It is built using a custom Dockerfile located in the `./paper-search-mcp` directory and includes several volumes, such as the `/paper-messages/` volume for storing messages and the `/data/papers` volume for storing downloaded papers.
* `ollama`: An Ollama container that is used to download papers from multiple sources. It is built using a standard Dockerfile located in the `ollama/ollama` image and includes several volumes, such as the `/root/.ollama` volume for storing configuration files.
* `mcp-paper-search`: A custom MCP search service that uses Ollama to download papers from multiple sources. It is built using a Dockerfile located in the `./paper-search-mcp` directory and includes several volumes, such as the `/data/papers` volume for storing downloaded papers.

The file also defines several networks, such as the `linn-network`, which is used to connect services together. It also defines several volumes, such as the `linn-postgres-data` volume for storing PostgreSQL data and the `linn-papers-data` volume for storing downloaded papers.

---

### .env.example
This is a `.env` file used by the Laravel framework. It contains various environment variables that define the configuration of the application. Here's a breakdown of the main functions of this file:

1. APP_NAME: The name of the application.
2. APP_ENV: The environment in which the application is running. This can be set to `local`, `testing`, or `production`.
3. APP_KEY: A unique key used for encryption and other purposes. If this value is not present, Laravel will generate one automatically when the application starts.
4. APP_DEBUG: Whether debugging is enabled or not. Set to `true` in development environments and `false` in production.
5. APP_URL: The base URL of the application. This should be set to the root domain name or subdomain where the application will be accessed.
6. APP_LOCALE, APP_FALLBACK_LOCALE, APP_FAKER_LOCALE: The locale for the application. These are used for formatting dates, times, and numbers, among other things.
7. APP_MAINTENANCE_DRIVER: The driver used for maintenance mode. This can be set to `file`, `database`, or `redis`.
8. PHP_CLI_SERVER_WORKERS: The number of workers used by the built-in web server in development environments.
9. BCRYPT_ROUNDS: The number of rounds used for bcrypt hashing. Increasing this value will make password hashing more secure, but it will also increase the computational time required to hash passwords.
10. LOG_CHANNEL, LOG_STACK, LOG_DEPRECATIONS_CHANNEL: The logging channel used by Laravel for log messages. `LOG_CHANNEL` can be set to `stack`, `single`, or `daily`. `LOG_STACK` determines whether the logs are stored in memory or on disk. `LOG_DEPRECATIONS_CHANNEL` determines how deprecation notices are handled.
11. DB_CONNECTION, DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD: The database connection details for the application. These can be used to connect to a PostgreSQL or MySQL database.
12. SESSION_DRIVER, SESSION_LIFETIME, SESSION_ENCRYPT, SESSION_PATH, SESSION_DOMAIN: The session driver used by Laravel. This determines how sessions are stored and managed. `SESSION_LIFETIME` specifies the number of minutes that a session is valid for.
13. BROADCAST_CONNECTION, FILESYSTEM_DISK, QUEUE_CONNECTION: The broadcast connection and filesystem disk used by Laravel. These determine how messages are broadcast to clients and how files are stored on the file system.
14. CACHE_STORE: The cache store used by Laravel. This determines which caching mechanism is used for storing data in memory.
15. MEMCACHED_HOST: The hostname or IP address of a memcached server.
16. REDIS_CLIENT, REDIS_HOST, REDIS_PASSWORD, REDIS_PORT: The Redis connection details for the application. These determine how data is stored and retrieved from a Redis cache store.
17. MAIL_MAILER, MAIL_SCHEME, MAIL_HOST, MAIL_PORT, MAIL_USERNAME, MAIL_PASSWORD: The mail configuration details for the application. These determine how emails are sent from the application.
18. AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_DEFAULT_REGION, AWS_BUCKET: The Amazon Web Services (AWS) connection details for the application. These determine how data is stored and retrieved in an S3 bucket.
19. LANGDOCK_API_KEY, LANGDOCK_WEBHOOK_URL, LANGDOCK_WEBHOOK_SECRET: The Langdock API key and webhook URL used for connecting to the Langdock AI platform. These determine how data is retrieved from the AI platform.
20. POSTGRES_DATABASE, POSTGRES_USERNAME, POSTGRES_PASSWORD: The PostgreSQL database connection details for the application. These determine how data is stored and retrieved in a PostgreSQL database.
21. MCP_AUTH_TOKEN: A token used for authenticating with the Langdock AI platform.
22. LANGDOCK_DB_USERNAME, LANGDOCK_DB_PASSWORD: The database connection details for the Langdock AI platform. These determine how data is stored and retrieved in a PostgreSQL database.
23. VITE_APP_NAME: The name of the application used by Vue.js. This determines how the application is referred to in the UI.

---

### config/app.php
This file is the `config/app.php` configuration file for a Laravel application. It contains various settings that configure the behavior of the application, such as the name and environment, debug mode, timezone, locale, encryption key, maintenance mode driver, etc.

Here are the key functions and features of this file:

* The `name`, `env`, `debug`, `url`, `timezone`, `locale`, `fallback_locale`, `faker_locale`, `cipher`, `key`, and `previous_keys` settings define various aspects of the application, such as its name, environment, debug mode, URL, timezone, locale, fallback locale, faker locale, encryption key, and previous keys.
* The `maintenance` setting defines the driver used to manage the application's maintenance mode status, which can be controlled across multiple machines using the "cache" driver.
* The `return [...]` statement at the end of the file returns an array containing the configuration options for the application.

---

### config/auth.php
Die Datei `config/auth.php` enthält Konfigurationen für die Authentifizierung in einer Laravel-Anwendung.

Es gibt folgende wichtige Funktionen in dieser Datei:

* Definition der Standard-Authentifizierungs "Guard" und des Passwort-Zurücksetzungs-Brokerns (Password Reset Broker) für Ihre Anwendung.
* Konfiguration von Authentifizierung-Gardenen, die definieren, wie Benutzer aus der Datenbank oder anderen Speicheranwendungen abgerufen werden.
* Definition von User Providern, die definieren, wie Benutzer tatsächlich in der Datenbank oder anderen Speicheranwendungen abgerufen werden.
* Konfiguration von Passwort-Zurücksetzungs-Optionen, einschließlich des Tabellennamens für Token und dem Benutzerprovider, der verwendet wird, um Benutzer zu actualisieren.
* Definition des Timeouts für die Password Confirmation, der angibt, wie lange eine Passwortbestätigung-Fenster gültig ist und ob Benutzer erneut ihre Passwörter eingeben müssen.

Die Datei `config/auth.php` verwendet eine Reihe von Klassen und Funktionen aus den Laravel-Frameworks, einschließlich:

* Die Authentifizierungs-Guard-Klasse (`Illuminate\Auth\Guard`) für die Authentifizierung von Benutzern.
* Die User Provider-Klasse (`Illuminate\Auth\UserProvider`) für die Abfrage von Benutzerdaten aus der Datenbank oder anderen Speicheranwendungen.
* Die Passwort-Zurücksetzung-Klasse (`Illuminate\Auth\Passwords\PasswordBroker`) für das Zurücksetzen von Passwörtern.

Es ist wichtig zu beachten, dass diese Datei keine External Dependencies wie APIs oder DB-Zugriffe hat, da sie nur Konfigurationen für die Authentifizierung in Ihrer Anwendung enthält.

---

### config/cache.php
This is a configuration file for the cache system in Laravel. It defines the available cache stores, their drivers, and other settings.

The main configuration options are:

* `default`: The default cache store to use if no specific store is specified when running a cache operation.
* `stores`: A map of all the available cache stores with their respective drivers and configurations.
* `prefix`: The prefix to use for every cache key, which can be used to avoid collisions between different applications using the same cache system.

The available cache store drivers are:

* `array`: Stores cache data in an array on the server.
* `database`: Stores cache data in a database table.
* `file`: Stores cache data in files on the server's file system.
* `memcached`: Stores cache data in a memcached instance.
* `redis`: Stores cache data in a Redis instance.
* `dynamodb`: Stores cache data in an Amazon DynamoDB table.
* `octane`: Stores cache data in memory using the PHP Accelerator.
* `failover`: Tries to use a specific cache store, and if it fails, tries another one.

The configuration also defines the connection settings for each of the cache stores, such as the database connection for the `database` driver or the memcached instance for the `memcached` driver.

---

### config/cookie-consent.php
Here is the summary of the file "config/cookie-consent.php":

This file contains configuration settings for the cookie consent dialog in a Laravel application. The settings include:

* `enabled`: A boolean value indicating whether to enable the cookie consent dialog.
* `cookie_name`: The name of the cookie in which we store if the user has agreed to accept the conditions.
* `cookie_lifetime`: The duration of the cookie in days, default is 365 (12 months / DSGVO).

This file is located in the "config" directory of a Laravel project and is used to define the settings for the cookie consent dialog.

---

### config/database.php
This is a configuration file for Laravel's database management system. It contains several settings that determine how the application interacts with the underlying database.

Here are the main components of this file:

* `default`: Specifies the default database connection name, which is used when no other connection is specified. The default value is `sqlite`, but it can be changed to any of the other connections listed below.
* `connections`: Contains all the available database connections for the application. Each connection has a unique name and defines various settings such as the driver, host, port, username, password, and more. The available drivers are: `sqlite`, `mysql`, `mariadb`, `pgsql`, and `sqlsrv`.
* `migrations`: Specifies the table used to store migration information in the database. By default, this is set to `migrations`, but it can be changed if desired.
* `redis`: Defines the Redis settings for the application. Redis is a key-value store that provides additional features such as caching and job queues. This section allows you to configure the connection settings for Redis.

In summary, this file contains all the configuration settings for the database management system in Laravel, including the default connection name, available connections, migration table, and Redis settings.

---

### config/filesystems.php
Die Datei `config/filesystems.php` ist eine Konfigurationsdatei für Laravel, die die Einstellungen für die verschiedenen Filesystem-Diskte verarbeitet. Hier sind die wichtigsten Funktionen und Klassen:

* `default`: Gibt den Standard-Filesystem-Disk an, der von der Anwendung verwendet wird.
* `disks`: Enthält eine Liste aller verfügbaren Filesystem-Disks, einschließlich ihrer Einstellungen.
* `local`: Der lokale Dateisystem-Disk, der für die Speicherung von privaten Daten in einem Verzeichnis namens "app/private" verwendet wird.
* `public`: Der öffentliche Dateisystem-Disk, der für die Speicherung von öffentlichen Daten in einem Verzeichnis namens "app/public" verwendet wird.
* `s3`: Der Amazon S3 Dateisystem-Disk, der für den Zugriff auf AWS-S3-Speicher verwendet wird.
* `links`: Enthält eine Liste von Symbolic Links, die durch den `storage:link`-Befehl erstellt werden. Diese Links verweisen auf das Verzeichnis "app/public" und leiten sie in das Verzeichnis "storage/app/public".

Externe Abhängigkeiten:

* `env`: Die Datei "`.env`" enthält die Umgebungsvariablen für die Anwendung. Diese werden durch den `env`-Befehl ausgelesen und können verwendet werden, um bestimmte Einstellungen anzupassen.

---

### config/fortify.php
Die Datei `config/fortify.php` ist eine Konfigurationsdatei für das Laravel-Paket Fortify. Sie definiert verschiedene Einstellungen und Optionen, die von Fortify verwendet werden, um den Authentifizierungsprozess zu optimieren und anzupassen.

Eine der wichtigsten Klassen in dieser Datei ist `Laravel\Fortify\Features`, die eine Klasse mit verschiedenen statischen Methoden ist, die zum Aktivieren oder Deaktivieren von Features verwendet werden können. In dieser Datei sind einige der Optionen wie Registrierung, Passwort-Wiederherstellung, E-Mail-Bestätigung und mehr definiert.

In dieser Konfigurationsdatei sind auch einige wichtige Einstellungen für den Fortify-Guard, das Password Broker und die Benutzernamen / Email-Adresse definiert. Es ist auch möglich, den Wert von `lowercase_usernames` anzupassen, um festzulegen, ob Benutzernamen in Kleinbuchstaben gespeichert werden sollen oder nicht.

Für die Routen des Fortify-Pakets sind verschiedene Einstellungen wie der Präfix, der Unterdomänenname und die Middleware verfügbar. Es ist auch möglich, die Anzahl der Login-Anfragen pro Minute zu beschränken und die Verwendung von Rate Limitern anzupassen.

Insgesamt enthält diese Datei wichtige Einstellungen für den Fortify-Authentifizierungsprozess und kann je nach Bedarf angepasst werden, um die Authentifizierungskomponente für Ihre Anwendung zu optimieren.

---

### config/logging.php
This is a configuration file for the logging system in Laravel. It defines various log channels and their associated settings, such as the level of logging, the handler used to write logs, and the processors that modify the log messages before they are written.

The main classes and functions in this file are:

* `Monolog\Handler\NullHandler`: A null logger that discards all messages.
* `Monolog\Handler\StreamHandler`: A stream logger that writes to a file or other stream.
* `Monolog\Handler\SyslogUdpHandler`: A syslog handler that sends log messages over UDP.
* `Monolog\Processor\PsrLogMessageProcessor`: A processor that adds the `PSR-3` message and context placeholders to the log message.

The file also imports several classes from Laravel's logging library, including `Illuminate\Contracts\Logging\Log`, which is used to define the contract for log channels.

---

### config/mail.php
This is a configuration file for the mail functionality in Laravel, which allows you to customize the settings for sending emails from your application.

The file contains several sections:

* `default`: This section specifies the default mailer that should be used when sending emails. The default value is `log`, which means that any email sent by your application will be logged in the Laravel log file.
* `mailers`: This section defines multiple mailers that can be used for sending emails. Each mailer has its own configuration settings, such as the transport (e.g., SMTP, Sendmail, Mailgun, etc.), scheme, URL, host, port, username, password, timeout, and local domain. The `ses` and `postmark` mailers are used for sending emails through Amazon SES and Postmark respectively.
* `from`: This section specifies the global "From" address that should be used when sending emails from your application. The default value is `hello@example.com`, but you can change this to any email address that you want to use as the sender of your emails.

In summary, this file allows you to customize the settings for sending emails from your Laravel application, such as the default mailer and the global "From" address.

---

### config/queue.php
Die Datei "queue.php" in der "config"-Verzeichnis ist eine Konfigurationsdatei für die Queue-Funktionalität von Laravel. Sie definiert die Verbindung und das Verhalten der verschiedenen Queue-Backends, die von Laravel unterstützt werden. Hier sind die wichtigsten Klassen, Funktionen oder Endpunkte, die in dieser Datei definiert sind:

* "default" - Der Name der Standard-Queue-Verbindung, die von Laravel verwendet wird.
* "connections" - Eine Liste aller verfügbaren Queue-Backends und ihre Konfigurationsoptionen. Jedes Backend hat einen eindeutigen Namen und kann auf eine bestimmte Datenbank oder externen Service zugreifen.
* "sync" - Der Synchronisierungs-Queue, der direkt in der Anwendung ausgeführt wird.
* "database" - Der Database-Queue, der die Jobs in einer Datenbank speichert und ausführt.
* "beanstalkd" - Der Beanstalkd-Queue, der auf einem separaten Server läuft und über eine API mit Laravel kommuniziert.
* "sqs" - Der SQS-Queue, der auf Amazon Web Services (AWS) basiert und den Jobs in einer externen Warteschlange speichert und ausführt.
* "redis" - Der Redis-Queue, der die Jobs in einem separaten Cache oder Data Store speichert und ausführt.
* "deferred" - Der Deferred-Queue, der den Jobs eine bestimmte Zeitspanne gewährt, um abgeschlossen zu werden.
* "background" - Der Background-Queue, der die Jobs in einem separaten Thread oder Prozess ausführt, während die Anwendung weiterläuft.
* "failover" - Der Failover-Queue, der den Jobs eine bestimmte Zeitspanne gewährt, um abgeschlossen zu werden. Wenn das Timeout abgelaufen ist, wird der Job als fehlgeschlagen markiert.
* "batching" - Die Batching-Verbindung, die für das Speichern von Job-Batches verwendet wird.
* "failed" - Die Verbindung für das Speichern von fehlerhaften Jobs, die nicht erfolgreich abgeschlossen wurden.

---

### config/services.php
This is a PHP file that defines an array of third party services used by the application. It returns an associative array where each key is the name of the service and the value is an object containing the necessary credentials to access the service.

The keys in the returned array are:

* `postmark`: an object containing the Postmark API key
* `resend`: an object containing the Resend API key
* `ses`: an object containing the AWS access key ID, secret access key, and region
* `slack`: an object containing the Slack bot user OAuth token and default channel
* `langdock`: an object containing the Langdock API key and webhook URL and secret
* `mcp`: an object containing the MCP auth token

The file also uses Laravel's built-in helper functions to access environment variables, which are stored in a `.env` file. This allows the application to use different credentials for each environment (e.g. development, staging, production).

---

### config/session.php
This file contains configuration options for the session management functionality of a Laravel application. The main purpose of this file is to define how the framework should handle sessions, including the driver to be used, the lifetime of the session, and various other settings related to session management.

Here's a breakdown of each setting in the file:

* `driver`: Specifies the default session driver that will be utilized for incoming requests. Laravel supports several storage options for sessions, including "file", "cookie", "database", "memcached", "redis", and "dynamodb". This option can also be set to "array" if you prefer to store sessions in memory rather than on disk.
* `lifetime`: Determines the number of minutes that a session is allowed to remain idle before it expires. If you want the session to immediately expire when the browser is closed, you may set this option to `false`.
* `expire_on_close`: Specifies whether or not the session should expire immediately when the browser is closed. This can be useful if you want the session to only exist for as long as the user is actively using your application.
* `encrypt`: Enables or disables session encryption. If set to `true`, all session data will be encrypted before it's stored.
* `files`: Specifies where sessions should be stored when using the "file" driver. The default location for this setting is `storage/framework/sessions`. You can change this value if you prefer to store sessions in a different location on your server.
* `connection`: Specifies the database connection that should be used to manage sessions when using the "database" or "redis" drivers. This option should correspond to a connection defined in your application's database configuration file.
* `table`: Specifies the table that should be used to store sessions when using the "database" driver. The default value for this setting is "sessions". You can change this value if you prefer to use a different table name.
* `store`: When using one of the framework's cache-driven session backends, such as "dynamodb", "memcached", or "redis", you may define the cache store that should be used to store the session data between requests. This option should correspond to a cache store defined in your application's configuration file.
* `lottery`: Specifies the chances that the framework will automatically sweep its storage location for old sessions, assuming a "database" or "redis" driver is being used. The default value for this setting is `[2, 100]`, which means there is a 2% chance that the session data will be swept every time a request is made to the application.
* `cookie`: Specifies the name of the cookie that should be created when a new session is started. The default value for this setting is "laravel_session". You can change this value if you prefer to use a different cookie name.
* `path`: Specifies the path for which the cookie will be regarded as available when using the "cookie" driver. The default value for this setting is "/".
* `domain`: Specifies the domain and subdomains that the session cookie should be available to, when using the "cookie" driver. The default value for this setting is null.
* `secure`: When set to true, the session cookie will only be sent back to the server if the browser has a HTTPS connection. This can help mitigate CSRF attacks.
* `http_only`: Specifies whether or not JavaScript should be able to access the value of the session cookie. By default, this option is set to true, which means that JavaScript will not be able to access the session cookie value.
* `same_site`: Determines how your cookies behave when cross-site requests take place. The default value for this setting is "lax", which will permit secure cross-site requests. You can also set this option to "strict" or "none" if you want more control over the behavior of your cookies.
* `partitioned`: When using a cookie driver, ties the cookie to the top-level site for a cross-site context. This can be useful when working with subdomains.

In summary, this file contains configuration options related to session management in Laravel applications. By adjusting these settings, you can customize how sessions are managed and optimize their performance.

---

### docker/common/php-fpm/php-dev.ini
This file is a PHP configuration override for the FPM (FastCGI Process Manager) container in the `docker-compose` project. It modifies the behavior of the PHP opcode cache and enables error display in development mode.

The most important lines are:

* `[opcache]`: This section defines settings related to the PHP opcode cache.
* `opcache.validate_timestamps = 1`: This setting enables the cache to check for changes in file timestamps, which allows it to invalidate cached files when they change on disk.
* `opcache.revalidate_freq = 0`: This setting disables the opcode cache's automatic revalidation of cached files after a certain amount of time (specified by the `opcache.revalidate_freq` setting). By disabling this feature, we ensure that the cache is always invalidated when necessary.
* `[PHP]`: This section defines settings related to the PHP engine itself.
* `display_errors = On`: This setting enables the display of error messages in development mode.
* `error_reporting = E_ALL`: This setting sets the level of errors that are reported by PHP. The `E_ALL` constant includes all possible error types, including warnings and notices.

The file is loaded as an override configuration file (`zz-dev.ini`) in the `docker-compose` project via a bind mount in `docker-compose.override.yml`. This allows us to enable development mode settings without having to modify the original PHP configuration files.

---

### docs/api.yaml
Here's a summary of the functionality of this file in 10 lines or less:

* The file contains OpenAPI definitions for the Research Management Platform app.linn.games API, including Webhooks, Paper Ingest, Vector Search, and User Information endpoints.
* The file includes definitions for security schemes such as Laravel Sanctum Bearer Token and MCP Bearer Token, as well as schema definitions for user objects and paper chunk objects.
* The file includes operation descriptions and example responses for each endpoint, including error handling for rate limiting and validation errors.
* The file uses a strict JSON format for its output, with no prose before or after the JSON block.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
This is a GitHub issue template for reporting bugs in the project. It includes the following fields:

* Description: A text area where users can describe the bug they are experiencing.
* Steps to reproduce: A text area where users can provide steps on how to reproduce the bug.
* Severity: A dropdown menu with options for "Critical" (Data loss / Security), "High" (Core functionality affected), "Medium" (Workaround available), and "Low" (Cosmetic / Edge case).
* Affected areas: A text area where users can specify the files, routes, or services that are affected by the bug.
* Acceptance criteria: A text area where users can provide acceptance criteria for the fix, such as when the bug is considered fixed.

The issue template includes validation rules to ensure that all fields are filled in before creating a new issue.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
This is a GitHub issue template for requesting a new feature. The template includes the following sections:

* Summary: A brief description of the requested feature.
* Motivation / Problem: An explanation of why this feature is needed and what problem it solves.
* Vorgeschlagene Lösung: A suggested solution to implement the feature.
* Betroffene Bereiche: A list of files, routes, services, or models that will be affected by the new feature.
* Breaking Change?: Whether the change is a breaking change and if so, what kind of migration is required.
* Akzeptanzkriterien: Acceptance criteria for when the feature is considered complete.

The template uses GitHub's issue templates to make it easier for developers to create new issues for requesting features in the repository. It includes a set of pre-filled fields that are commonly used for feature requests, such as the summary, motivation, and solution. The affected sections can be customized based on the specific requirements of the project.

---

### .github/ISSUE_TEMPLATE/refactor.yml
This file is used to describe refactoring suggestions for code improvements without changing the functionality. The file includes a description of the proposed changes, the affected files or areas, and the acceptance criteria.

---

### .github/workflows/lint.yml
This is a GitHub Actions workflow file that defines a job named "quality" which runs on an Ubuntu machine and performs several actions, including installing dependencies using Composer and NPM, running Pint, and committing changes to the repository (which has been disabled by commenting out the step). The job listens for push events on the develop and main branches, as well as pull request events. It also requires write permissions for the contents of the repository.

The file imports several external dependencies:

* `actions/checkout@v5` to check out the repository before running the job
* `shivammathur/setup-php@v2` to install PHP 8.4 on the machine
* `composer config http-basic.composer.fluxui.dev "${{ secrets.FLUX_USERNAME }}" "${{ secrets.FLUX_LICENSE_KEY }}"` to add Flux credentials to Composer
* `vendor/bin/pint` to run Pint (a tool for checking the style of PHP code)

The file also defines an environment named "Testing" which is used by the job.

Overall, this workflow file sets up a job that runs on every push event to the develop and main branches, as well as pull request events. It installs dependencies using Composer and NPM, runs Pint to check the style of PHP code, and commits changes (which has been disabled by commenting out the step).

---

### .github/workflows/tests.yml
This is a GitHub Actions workflow file for testing. The file is located in the `.github/workflows` directory and is named `tests.yml`.

The purpose of this file is to automate the testing process of a PHP application that uses Laravel as its framework. The file sets up a workflow that runs on pushes to certain branches (such as `develop`, `main`, `feature/**`, and `fix/**`) and pull requests.

The workflow consists of several steps:

1. Checkout the code: This step checks out the code from the repository into the environment.
2. Setup PHP: This step sets up a PHP environment using the `shivammathur/setup-php` action. The version of PHP used is 8.4, and the tools included are Composer (version 2) and Xdebug.
3. Setup Node: This step sets up a Node environment using the `actions/setup-node` action. The version of Node used is 22, and caching is enabled for npm.
4. Install Node Dependencies: This step installs the dependencies needed by the application using npm.
5. Add Flux Credentials Loaded From ENV: This step adds the Flux credentials loaded from the environment variables (i.e., `FLUX_USERNAME` and `FLUX_LICENSE_KEY`) to Composer's configuration.
6. Install Dependencies: This step installs the dependencies of the application using Composer. The `--no-interaction` flag is used to avoid prompting for input, and the `--prefer-dist` flag is used to tell Composer to use pre-downloaded packages whenever possible. Finally, the `--optimize-autoloader` flag is used to optimize the autoloader.
7. Copy Environment File: This step copies the `.env.example` file to `.env`.
8. Generate Application Key: This step generates a new application key for the Laravel project using the `php artisan key:generate` command.
9. Build Assets: This step builds the assets of the application using npm.
10. Run Tests: This step runs the tests of the application using the `pest` command.

---

## data_access

### app/Models/ChatMessage.php
This file is a part of the Laravel framework and contains the definition of the `ChatMessage` model, which represents a chat message in the system. The model extends the `Illuminate\Database\Eloquent\Model` class and uses the `HasUuids` trait to generate UUIDs for the model's primary key.

The `ChatMessage` model has the following properties:

* `user_id`: a foreign key referencing the user who sent the message
* `webhook_id`: a foreign key referencing the webhook that received the message
* `role`: the role of the user who sent the message (e.g. "admin", "moderator", etc.)
* `content`: the content of the chat message
* `created_at`: the timestamp when the message was created

The model also defines two relationships:

* `user()`: a belongs-to relationship with the `User` model, which represents the user who sent the message
* `webhook()`: a belongs-to relationship with the `Webhook` model, which represents the webhook that received the message

The file also imports the following external dependencies:

* The `Illuminate\Database\Eloquent\Concerns\HasUuids` trait, which provides functionality for generating UUIDs for the model's primary key.
* The `Illuminate\Database\Eloquent\Model` class, which is a base class for all Eloquent models in Laravel.
* The `User` and `Webhook` classes, which represent the user and webhook models in the system.

---

### app/Models/Consent.php
Die Datei `app/Models/Consent.php` definiert eine Klasse `Consent`, die als Modell für die Tabelle `consents` in der Datenbank fungiert. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model`, um das Speichern und Abrufen von Benutzerzustimmungen zu ermöglichen.

Die Klasse definiert folgende Eigenschaften:

* `user_id`: Die ID des Nutzers, der die Zustimmung erteilt hat (integer)
* `ip_anonymous`: Die anonyme IP-Adresse des Nutzers (string)
* `consent_categories`: Ein Array von Kategorien, für die der Nutzer eine Zustimmung erteilt hat (array)
* `consented_at`: Das Datum und die Uhrzeit der Zustimmung (datetime)
* `revoked_at`: Das Datum und die Uhrzeit der Stornierung der Zustimmung (datetime)

Die Klasse definiert auch die folgenden Methoden:

* `fillable`: Ein Array von Eigenschaften, die als gefüllt markiert werden können (array)
* `casts`: Ein Array von Eigenschaften, die als bestimmte Datentypen interpretiert werden müssen (array)
* `user`: Eine Beziehung zur Klasse `User`, die den Benutzer mit der Zustimmung verknüpft (BelongsTo)

---

### app/Models/Contact.php
This file contains the Contact model for the project, which extends the Eloquent Model class and defines the fillable properties for a contact object.

---

### app/Models/PageView.php
Die Datei `app/Models/PageView.php` definiert eine Klasse `PageView`, die eine Datenbanktabelle repräsentiert. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und enthält eine Reihe von Attributen, die dem Speichern von Daten in der Tabelle dienen.

Die wichtigsten Funktionen dieser Datei sind:

* `PageView`: Eine Klasse, die eine Datenbanktabelle repräsentiert und eine Reihe von Attributen enthält, die zum Speichern von Daten in der Tabelle verwendet werden können.
* `$fillable`: Ein Array von Attributen, das den Spaltennamen der Tabelle zuordnet.

Es gibt auch einige externe Abhängigkeiten in dieser Datei:

* `Illuminate\Database\Eloquent\Model`: Eine Basisklasse, die die Speicherung und Abfrage von Daten in der Datenbank erlaubt.
* `App\Models\PageView`: Die Klasse selbst, die die Funktionalität zur Verarbeitung und Speicherung von Daten in der Tabelle implementiert.

---

### app/Models/User.php
This file defines a `User` model in the Laravel framework. The class extends several traits, including `HasFactory`, `HasRoles`, `LogsActivity`, and `Notifiable`. These traits provide functionality for the user to interact with the database, manage roles and permissions, log activity, and notify users about certain events.

The model defines two properties: `$fillable` and `$hidden`. The `$fillable` property contains a list of fields that can be mass assigned when creating or updating a user record. The `$hidden` property contains a list of fields that should not be included in the model's JSON representation.

The `casts()` method defines the types of the attributes that are stored in the database. In this case, the `email_verified_at` attribute is cast as a datetime object, and the `password` attribute is cast as a hashed string. The `status` attribute is also defined as a string, but it is not included in the `$hidden` property.

The `initials()` method returns the user's initials based on their name. It uses Laravel's `Str` class to explode the name into an array of words, take the first two words, and map them to their first letter. The resulting string is then imploded using the initials as a delimiter.

The `getActivitylogOptions()` method returns a `LogOptions` object that defines the attributes that should be logged for this model. In this case, the `name`, `email`, and `status` attributes are included in the log. The `isActive()` and `isTrial()` methods define additional helper functions to determine whether the user is active or on a trial period.

---

### app/Models/Webhook.php
The `Webhook` class in the `app/Models` namespace is a model for storing and retrieving webhook information in a database. It extends the `Model` class provided by Laravel, which provides a set of functions for interacting with Eloquent ORM (Object-Relational Mapping) and working with databases.

The `Webhook` class has several important features:

* It uses the `HasUuids` trait to automatically generate UUIDs for each webhook instance. This allows for efficient and unique identification of webhooks in the database.
* It defines a `$table` property that specifies the name of the table where the webhook information is stored. In this case, it is `webhooks`.
* It defines a `$fillable` property that specifies which columns are mass assignable for creating and updating webhook instances. In this case, it is `user_id`, `name`, `slug`, `url`, `secret`, and `frontend_object`.
* It defines a `$hidden` property that specifies which columns should be hidden from serialization. In this case, it is `secret`, which is not intended to be exposed in the API responses.
* It defines a `$casts` property that specifies how certain columns should be cast when retrieved from the database. In this case, it is `created_at` which is cast as a `datetime`.
* It has several methods for interacting with the webhook information:
	+ The `callUrl()` method returns the URL of the webhook, optionally including the secret parameter if it exists.
	+ The `forUser()` method finds a webhook instance for a given user ID and frontend object name.
	+ The `user()` method retrieves the associated user instance for a webhook instance.
	+ The `chatMessages()` method retrieves all chat messages associated with a webhook instance.

The `Webhook` class also defines several constants that are used to store commonly used frontend object names. These constants are used in other parts of the application, such as in the controllers and views.

---

### app/Models/Recherche/P1Komponente.php
Die Datei `app/Models/Recherche/P1Komponente.php` enthält eine Klasse namens `P1Komponente`, die eine Implementierung einer Modell-Instanz für das Projekt "Projekt 1" darstellt. Die Klasse erweitert die `Model`-Klasse von Laravel und implementiert die `HasUuids`-Trait, um UUIDs für die Objekte zu generieren.

Die Klasse definiert einen Eloquent-Modellbezeichner `P1Komponente`, der auf einer Tabelle namens `p1_komponenten` basiert. Die Tabelle enthält verschiedene Felder, darunter ein Feld für den Bezeichner des Projekts, an das die Komponente angehört, und ein Feld für den Modellnamen der Komponente.

Die Klasse definiert auch eine Reihe von Attributen als "fillable", das heißt, sie werden automatisch ausgefüllt, wenn ein neues Objekt erstellt wird. Dazu gehören Felder für die Bezeichnung der Komponente, ihre Synonyme und Anmerkungen.

Die Klasse enthält auch eine Methode namens `projekt()`, die eine BelongsTo-Beziehung zwischen dem Modell und einem anderen Modell namens "Projekt" herstellt. Diese Methode gibt an, dass eine Komponente zu einem bestimmten Projekt gehört.

Die Datei enthält auch eine Reihe von Import-Anweisungen für die benötigten Laravel-Klassen und -Funktionen.

---

### app/Models/Recherche/P1Kriterium.php
Diese Datei enthält eine Klasse namens P1Kriterium, die ein Modell für die Tabelle p1_kriterien in der Datenbank darstellt. Die Klasse erweitert den Eloquent-Modell-Typ von Laravel und hat eine Beziehung zu einer anderen Klasse namens Projekt. Die Klasse definiert auch einige Attribute wie kriterium_typ, beschreibung, begruendung und quellbezug, die in der Datenbank gespeichert werden können. Die Datei importiert auch den Trait HasUuids von Laravel, um das Verwenden von UUIDs für die IDs zu ermöglichen.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
This file defines the P1Strukturmodellwahl class that represents a model used for storing data in the p1_strukturmodell_wahl table of the database. The class extends the Model class from Laravel's Eloquent ORM and uses the HasUuids trait to enable UUID generation for the model. The fillable property specifies which attributes are mass assignable, while the casts property defines the types of the 'gewaehlt' attribute as a boolean value.

---

### app/Models/Recherche/P1Warnsignal.php
Die Datei `app/Models/Recherche/P1Warnsignal.php` definiert eine Klasse namens `P1Warnsignal`, die als Model für die Tabelle `p1_warnsignale` fungiert und in der Laravel-Anwendung `P1` verwendet wird. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und implementiert die Traits `HasUuids`, um die Verwendung von UUIDs bei der Speicherung von Objekten zu ermöglichen.

Die Klasse definiert folgende Attribute:

* `$table`: Der Name der Datenbanktabelle, auf die die Klasse zugreift.
* `$timestamps`: Gibt an, ob für das Model Zeitstempel-Spalten erstellt werden sollen.
* `$fillable`: Eine Liste von Spaltennamen, die als massenanwendungsfähig markiert sind.
* `$casts`: Eine Liste von Spaltennamen und ihren Datentypen, die bei der Serialisierung des Models verwendet werden sollen.

Die Klasse definiert auch eine Beziehung zur Klasse `Projekt`, indem sie den Typ `BelongsTo` für die Methodensignatur `projekt()` verwendet. Die Beziehung ermöglicht es, den Projekt-Eintrag für das Warnsignal aus der Datenbank abzurufen.

Die Datei `app/Models/Recherche/P1Warnsignal.php` enthält auch Importe von Laravel-Klassen und -Traits wie `Illuminate\Database\Eloquent\Model`, `HasUuids` oder `BelongsTo`.

---

### app/Models/Recherche/P2Cluster.php
Die Datei `app/Models/Recherche/P2Cluster.php` enthält eine Klasse namens `P2Cluster`, die ein Modell für die Datenbanktabelle `p2_cluster` darstellt. Die Klasse erweitert das Laravel-Modell `Model` und verwendet den Trait `HasUuids` von Eloquent, um UUIDs (Universally Unique Identifiers) zu generieren.

Die Klasse hat folgende Funktionen:

* Es wird eine Verbindung zur Datenbank hergestellt und ein Modell für die Tabelle `p2_cluster` erstellt.
* Die Eigenschaften der Klasse können über `protected $fillable` angegeben werden, um sicherzustellen, dass nur bestimmte Spalten in der Tabelle aktualisiert werden können.
* Es wird eine Methode namens `projekt()` definiert, die eine Beziehung zwischen den Modellen `P2Cluster` und `Projekt` herstellt. Die Methode gibt an, dass ein Objekt des Typs `P2Cluster` einen `belongsTo`-Beziehungs-Typ hat, der auf die Tabelle `projekte` verweist.

Die Klasse benötigt eine Verbindung zur Datenbank und verwendet den Trait `HasUuids` von Eloquent, um UUIDs zu generieren. Die Eigenschaften können über `protected $fillable` angegeben werden, um sicherzustellen, dass nur bestimmte Spalten in der Tabelle aktualisiert werden können. Es wird auch eine Beziehung zwischen den Modellen definiert, sodass ein Objekt des Typs `P2Cluster` einen `belongsTo`-Beziehungs-Typ hat, der auf die Tabelle `projekte` verweist.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
This PHP file defines a model class for the P2MappingSuchstringKomponente table in the projects database. The class provides access to the suchbegriffe and trunkierung_genutzt columns of the table, as well as the BelongsTo relationship with the Projekt model.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
This file defines the P2ReviewTypEntscheidung model for the app, which represents a decision made by a reviewer regarding a specific project and review type. The model includes properties such as 'projekt_id', 'review_typ', 'passt', and 'begruendung', as well as relationships with other models such as the Projekt model.

---

### app/Models/Recherche/P2Trefferliste.php
File Summary: This file defines a model for a "P2Trefferliste" in the Laravel framework. It is a Eloquent model that represents a table called "p2_trefferlisten" in the database, and it has a one-to-many relationship with another Eloquent model called "Projekt".

Key Features:

* Uses the `HasUuids` trait to generate UUIDs for the model.
* Defines a `$table` property to specify the name of the table in the database.
* Defines a `$timestamps` property to disable automatic timestamps for the model.
* Defines a `$fillable` property to specify the attributes that are mass assignable for the model.
* Defines a `$casts` property to cast certain attributes to specific data types (e.g., `treffer_gesamt` is an integer, and `anpassung_notwendig` is a boolean).
* Has a `projekt()` method that defines a one-to-many relationship with the "Projekt" model using the `belongsTo()` method.

---

### app/Models/Recherche/P3Datenbankmatrix.php
This file is a PHP class called `P3Datenbankmatrix` that extends the Laravel Eloquent Model. It is used to represent a specific table in the database that contains information about the databases and their accessibility for projects.

The class has several properties, including:

* `projekt_id`: The ID of the project that this database belongs to
* `datenbank`: The name of the database
* `disziplin`: The discipline (or field) of study that this database is relevant for
* `abdeckung`: The coverage (or scope) of the database
* `besonderheit`: Any special features or requirements of the database
* `zugang`: Whether the database is accessible or not
* `empfohlen`: Whether the database is recommended for use in the project
* `begruendung`: A reason for recommending the database (optional)

The class also has several methods, including:

* `__construct()`: The constructor method that initializes the object
* `getTableName()`: Returns the name of the table that this model is associated with
* `getFillableAttributes()`: Returns an array of attributes that can be mass assigned in the database
* `getCasts()`: Returns an array of casts (i.e., type conversions) for the attributes
* `project(): BelongsTo`: A method that returns a relationship between this model and another model called `Projekt` (which represents a project). This relationship is defined using Laravel's Eloquent ORM.

---

### app/Models/Recherche/P3Disziplin.php
This PHP file defines a model for the P3Disziplin entity in an Eloquent ORM framework. The file is located in the Models/Recherche directory of the app directory.

The model extends the Model class and uses the HasUuids trait to enable UUID support for the entity. The table name is set to 'p3_disziplinen'. Timestamps are not enabled for this entity.

The fillable properties define which attributes can be mass assigned when creating a new instance of this model. The project attribute is defined as a belongsTo relationship with the Projekt entity, which is also defined in this file.

This model provides a way to interact with the p3_disziplinen table in the database, and allows for the creation, reading, updating, and deleting of P3Disziplin entities.

---

### app/Models/Recherche/P3GeografischerFilter.php
File Summary: This file is part of a Laravel application and represents the P3GeografischerFilter model, which extends the Model class from the Eloquent ORM. The model is used to represent geographical filters in the database for a specific project. It contains various properties such as the project ID, region, validity status, filter name, sensitivity percentage, and helper strategy. The model also defines relationships with other models, such as the Projekt model.

Key classes:

* P3GeografischerFilter: This is the main class of this file, which represents a geographical filter for a specific project. It extends the Model class from the Eloquent ORM and uses various properties to represent the different fields in the database table.
* Projekt: This is another class defined in this file, which represents a project in the database. The P3GeografischerFilter model has a relationship with the Projekt model through the "belongsTo" method.

External dependencies:

* Illuminate\Database\Eloquent\Model: This is a base class for all Eloquent models in Laravel, which provides various methods and properties for interacting with the database.
* HasUuids: This is a trait provided by Laravel that adds functionality to automatically generate UUIDs for models.
* Illuminate\Database\Eloquent\Relations\BelongsTo: This is a class that represents a relationship between two models, where one model "belongs" to another. In this case, the P3GeografischerFilter model belongs to a Projekt model instance.

---

### app/Models/Recherche/P3GraueLiteratur.php
Die Datei "P3GraueLiteratur" enthält eine PHP-Klasse für das Modell von "grauer Literatur". Die Klasse erweitert die Eloquent-Model-Klasse und implementiert die HasUuids-Trait, um die Verwendung von UUIDs in der Tabelle zu ermöglichen.

Die wichtigsten Funktionen dieser Datei sind:

* Definieren einer Datenbanktabelle namens "p3_graue_literatur" für das Modell.
* Definieren von Feldern für die Eigenschaften des Modells, wie z.B. projekt_id, quelle, typ, url, suchpfad, relevanz und anmerkung.
* Definieren einer Beziehung zwischen dem Modell und der "Projekt"-Tabelle über eine belongsTo-Beziehung.

Die Klasse importiert auch die erforderlichen PHP-Klassen von Laravel, um die Verwendung von UUIDs und BelongsTo-Beziehungen zu ermöglichen.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
The file `app/Models/Recherche/P4Anpassungsprotokoll.php` is a PHP class that defines a model for the `p4_anpassungsprotokoll` table in the database.

The class uses the `Illuminate\Database\Eloquent\Model` base class and implements the `HasUuids` trait to use UUIDs as primary keys. The table name is set using the `$table` property, and the timestamps are disabled using the `public $timestamps = false;` statement.

The class defines a `fillable` property that lists the attributes of the model that can be mass-assigned when creating or updating a record. In this case, the following attributes can be set:

* `suchstring_id`: The ID of the associated `P4Suchstring` model.
* `version`: A version number for the anpassungsprotokoll.
* `datum`: The date of the anpassungsprotokoll.
* `aenderung`: The type of change made to the search string (e.g., "Added", "Removed").
* `grund`: The reason for making the change.
* `treffer_vorher`: The number of hits before the change was made.
* `treffer_nachher`: The number of hits after the change was made.
* `entscheidung`: The decision made regarding the change (e.g., "Approved", "Rejected").

The class also defines a `casts` property that specifies how certain attributes should be cast to specific types when they are retrieved from the database. In this case, the following attributes are cast to specific types:

* `datum`: Cast to a `date` type.
* `treffer_vorher`: Cast to an `integer` type.
* `treffer_nachher`: Cast to an `integer` type.

Finally, the class defines a `belongsTo` relationship with the `P4Suchstring` model using the `suchstring()` method. This allows you to retrieve the associated search string for a given anpassungsprotokoll record.

---

### app/Models/Recherche/P4Suchstring.php
This is a PHP class for the `App\Models\Recherche\P4Suchstring` model. It extends the `Illuminate\Database\Eloquent\Model` class and uses the `HasUuids` trait to generate UUIDs for the `id` column. The table name is set to `p4_suchstrings`.

The model has 10 fillable columns, including `projekt_id`, `datenbank`, `suchstring`, `feldeinschraenkung`, `gesetzte_filter`, `treffer_anzahl`, `einschaetzung`, `anpassung`, `version`, and `suchdatum`. The `gesetzte_filter` column is an array, the `treffer_anzahl` column is an integer, and the `suchdatum`, `erstellt_am` columns are of type date.

The model has two relations: one to a `Projekt` model using the `belongsTo()` method, and another to a `P4Anpassungsprotokoll` and `P8Suchprotokoll` models using the `hasMany()` method.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Die Datei `app/Models/Recherche/P4ThesaurusMapping.php` enthält eine Klasse namens `P4ThesaurusMapping`, die das Modell für eine Beziehung zwischen einem Projekttyp und einem Thesaurus-Term darstellt. Die Klasse extends von `Illuminate\Database\Eloquent\Model` und verwendet den Trait `HasUuids`, um UUIDs zu generieren.

Die Tabelle, die von der Klasse verwendet wird, heißt `p4_thesaurus_mapping`. Die Klasse hat keine timestamps (Zeitstempel) und kann auf 8 Felder gespeist werden:

* `projekt_id`: Eine ID des zugeordneten Projekts.
* `freitext_de`: Ein freier Text in deutscher Sprache.
* `freitext_en`: Ein freier Text in englischer Sprache.
* `mesh_term`: Ein MESH-Term (Medical Subject Headings).
* `emtree_term`: Ein EMTREE-Term (Enzyme Metabolism Tree).
* `psycinfo_term`: Ein PSYCINFO-Term (Psychopharmacology Information Center).
* `anmerkung`: Eine Anmerkung oder ein Kommentar.

Die Klasse enthält auch eine Beziehung zur Klasse `Projekt`, indem sie die Funktion `projekt()` definiert, die einen BelongsTo-Relationship zu der anderen Klasse herstellt.

---

### app/Models/Recherche/P5PrismaZahlen.php
Die Datei `app/Models/Recherche/P5PrismaZahlen.php` ist ein PHP-Modell für die Verarbeitung und Speicherung von Daten zu P5-Prisma-Zahlen in einer MySQL-Datenbank. Die Klasse erweitert die Basisklasse `Model` der Laravel-Framework und implementiert die Traits `HasUuids`.

Die Klasse definiert eine Tabelle mit dem Namen `p5_prisma_zahlen`, die Felder für die Speicherung von Zahlen zu P5-Prisma in der Datenbank enthält. Die Tabellenspalten sind:

* `projekt_id`: Eine Referenz auf ein Projekt (FK)
* `identifiziert_gesamt`: Gesamtzahl der identifizierten Prisma-Zahlen
* `davon_datenbank_treffer`: Anzahl der von der Datenbank-Suche gefundenen Prisma-Zahlen
* `davon_graue_literatur`: Anzahl der von grauer Literatur gefundenen Prisma-Zahlen
* `nach_deduplizierung`: Anzahl der nach Deduplikation gefundenen Prisma-Zahlen
* `ausgeschlossen_l1`: Anzahl der ausgeschlossenen Prisma-Zahlen, die nicht dem L1-Satz entsprechen
* `volltext_geprueft`: Anzahl der bereits vollständig überprüften Prisma-Zahlen
* `ausgeschlossen_l2`: Anzahl der ausgeschlossenen Prisma-Zahlen, die nicht dem L2-Satz entsprechen
* `eingeschlossen_final`: Anzahl der endgültig eingeschlossenen Prisma-Zahlen

Die Klasse definiert auch einige Eigenschaften und Methoden zur Verarbeitung und Speicherung dieser Daten in der Datenbank.

* Die Eigenschaft `$table` definiert den Namen der Tabelle, in der die Daten gespeichert werden sollen.
* Die Eigenschaft `$timestamps` deaktiviert das automatische Setzen von Zeitstempeln für den Erstellungs- und Aktualisierungszeitpunkt der Datensätze.
* Die Eigenschaft `$fillable` definiert, welche Felder in der Tabelle nicht null sein dürfen.
* Die Eigenschaft `$casts` definiert, dass das Feld `aktualisiert_am` als Datum interpretiert werden soll.
* Die Methode `projekt()` definiert eine Beziehung zwischen den Modellen "P5PrismaZahlen" und "Projekt". Sie gibt an, dass ein Projekt über viele P5-Prisma-Zahlen verfügen kann und eine P5-Prisma-Zahl zu einem bestimmten Projekt gehört.

Insgesamt stellt die Klasse `P5PrismaZahlen` ein Modell für die Verarbeitung, Speicherung und Abfrage von Daten zu P5-Prisma-Zahlen in einer MySQL-Datenbank dar.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
This PHP file is a model for a P5ScreeningEntscheidung object in the Laravel framework. The class extends the Model class and uses the HasUuids trait to automatically generate UUIDs for the record.

The table name is defined as 'p5_screening_entscheidungen' and timestamps are disabled.

The fillable fields of the model are:

* treffer_id
* level
* entscheidung
* ausschlussgrund
* reviewer
* datum
* anmerkung

The casts field is used to convert the 'datum' field to a date object.

The treffer() method defines a relationship between this model and the P5Treffer model, using the belongsTo() method to define the foreign key on the P5Treffer side of the relationship.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Die Datei `app/Models/Recherche/P5ScreeningKriterium.php` enthält eine Klasse namens `P5ScreeningKriterium`, die es ermöglicht, Daten aus einer Datenbanktablle mit dem Namen `p5_screening_kriterien` zu lesen und zu verändern.

Die Klasse erweitert die Eloquent-Klasse `Model`, welche von Laravel bereitgestellt wird, und verwendet den Trait `HasUuids`, um UUIDs für die ID-Felder der Tabelle zu generieren.

Die Klasse hat folgende Eigenschaften:

* `table`: Gibt an, dass die Daten in einer Tabelle mit dem Namen `p5_screening_kriterien` gespeichert sind.
* `timestamps`: Deaktiviert das Speichern von Timestamps für die Datensätze in der Tabelle.
* `fillable`: Gibt an, welche Felder in der Tabelle gefüllt werden dürfen. In diesem Fall sind dies die Felder `projekt_id`, `level`, `kriterium_typ`, `beschreibung` und `beispiel`.
* `projekt`: Eine Beziehung zu einer anderen Klasse namens `Projekt`, die verwendet wird, um den Beziehungen zwischen den Daten in der Tabelle und anderen Teilen des Systems zu ermöglichen.

Die Klasse bietet auch mehrere Methoden zum Lesen und Ändern von Daten in der Tabelle, wie z.B. `find()`, `where()` und `update()`.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Die Datei `app/Models/Recherche/P5ToolEntscheidung.php` enthält eine Klasse, die den Zugriff auf die Tabelle `p5_tool_entscheidung` in der Datenbank ermöglicht. Die Klasse erweitert das Modell `Model` von Laravel und verwendet das Trait `HasUuids`, um die Verwendung von UUIDs für die Primary Key zu aktivieren.

Die Klasse definiert folgende Methoden:

* `__construct()`: Der Konstruktor der Klasse, der ein neues Objekt erstellt und die Parameter initialisiert.
* `table()`: Methode, die den Namen der Tabelle als String zurückgibt, auf der die Klasse zugreift.
* `timestamps()`: Methode, die den Status von Timestamp-Feldern im Modell aktiviert oder deaktiviert. In diesem Fall ist dies `false`, da die Tabelle keine Timestamps enthält.
* `fillable()`: Methode, die die Felder als "füllbar" markiert, das heißt, dass sie in Massen-Assignments und dem Modell-Konstruktor verwendet werden können. In diesem Fall sind dies `projekt_id`, `tool`, `gewaehlt` und `begruendung`.
* `casts()`: Methode, die die Typen von Feldern in der Tabelle definiert. In diesem Fall wird das Feld `gewaehlt` als Boolean-Wert interpretiert.
* `projekt()`: Methode, die eine Beziehung zwischen dieser Klasse und der `Projekt`-Klasse definiert. Diese Methode gibt an, dass es eine Belongsto-Beziehung zwischen den beiden Klassen ist, und gibt auch an, auf welcher Spalte in der `p5_tool_entscheidung`-Tabelle die Beziehung basiert.

Diese Klasse ermöglicht es, direkt mit der Tabelle `p5_tool_entscheidung` in der Datenbank zu interagieren und Objekte dieser Klasse zu erstellen oder abzufragen.

---

### app/Models/Recherche/P5Treffer.php
Die Datei `app/Models/Recherche/P5Treffer.php` enthält eine PHP-Klasse, die den Namen `P5Treffer` hat und sich auf das Modell einer Suchergebnis-Datenbank bezieht. Die Klasse erweitert das Basis-Modell von Laravel (`Illuminate\Database\Eloquent\Model`) und definiert eine Reihe von Eigenschaften, Methoden und Beziehungen zu anderen Modellen.

Hier sind die wichtigsten Funktionen der Klasse in maximal 10 Zeilen dargestellt:

* Die Klasse verwendet das Trait `HasUuids` vom Paket `Illuminate\Database\Eloquent\Concerns`, um UUIDs zu generieren und zu verwenden.
* Die Tabelle für diese Klasse heißt `p5_treffer`.
* Die Eigenschaften `projekt_id`, `record_id`, `titel`, `autoren`, `jahr`, `journal`, `doi`, `abstract`, `datenbank_quelle`, `ist_duplikat` und `erstellt_am` sind aufgefüllt.
* Die Eigenschaften `jahr` und `ist_duplikat` werden als Integer-Werte interpretiert, während die Eigenschaft `erstellt_am` als Datums-Zeit-Objekt interpretiert wird.
* Die Klasse definiert eine Beziehung zwischen sich selbst (`BelongsTo`) und der Klasse `Projekt`, um auf das Projekt zuzugreifen, das dem Treffer zugeordnet ist.
* Die Klasse definiert auch eine Beziehung zwischen sich selbst (`HasMany`) und der Klassen `P5ScreeningEntscheidung`, `P6Qualitaetsbewertung` und `P7Datenextraktion`, um auf die entsprechenden Screening-Entscheidungen, Qualitätsbewertungen und Datenextraktions-Vorgänge zuzugreifen.
* Die Klasse importiert das Paket `Illuminate\Database\Eloquent\Concerns` für das Trait `HasUuids`.
* Die Klasse importiert das Paket `Illuminate\Database\Eloquent\Model` für die Basisklasse von Eloquent.

---

### app/Models/Recherche/P6Luckenanalyse.php
Die Datei stellt eine Modell-Klasse dar, die eine Tabelle in der Datenbank darstellt. Es handelt sich dabei um die P6Luckenanalyse-Tabelle, die Informationen zu den möglichen Lücken im Projekttyp und dem fehlenden Aspekt für ein bestimmtes Projekt speichert. Die Klasse enthält eine Beziehung zu einer anderen Tabelle (Projekt), mit der sie über das Feld 'projekt_id' verbunden ist.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
This file is a PHP class named P6Qualitaetsbewertung, which extends the Illuminate\Database\Eloquent\Model class. It represents a table in the database with the same name. The class has a number of fields that correspond to columns in the table, including 'treffer_id', 'studientyp', 'rob_tool', 'gesamturteil', 'hauptproblem', 'im_review_behalten', 'anmerkung', 'bewertet_von', and 'bewertet_am'. The class also defines some relationships with other tables, such as a BelongsTo relationship to the P5Treffer table.

---

### app/Models/Recherche/P7Datenextraktion.php
File: `app/Models/Recherche/P7Datenextraktion.php`

* Kategorie: data_access
* Importierte Klassen:
	+ `Illuminate\Database\Eloquent\Concerns\HasUuids`: Ermöglicht das Verwenden von UUIDs als Primärschlüssel für die Tabelle.
	+ `Illuminate\Database\Eloquent\Model`: Die Basisklasse für Eloquent-Modelle in Laravel.
	+ `Illuminate\Database\Eloquent\Relations\BelongsTo`: Eine Beziehungsbeziehung zwischen zwei Modellen, bei der das Modell "P7Datenextraktion" ein belongsTo-Verhältnis zu dem Modell "P5Treffer" hat.
* Tabelle: `p7_datenextraktion`
* Primärschlüssel: UUID
* Timestamps: Deaktiviert die automatische Erstellung von Timestamp-Feldern (created_at und updated_at)
* Füllbare Felder:
	+ `treffer_id`: Eine Verknüpfung zu einem anderen Modell "P5Treffer"
	+ `land`: Land des Treffers
	+ `stichprobe_kontext`: Kontext der Stichprobe
	+ `phaenomen_intervention`: Phänomene, die durch eine Intervention verändert wurden
	+ `outcome_ergebnis`: Endergebnis einer Intervention
	+ `hauptbefund`: Hauptfundus einer Stichprobe
	+ `qualitaetsurteil`: Qualitätsurteil für eine Stichprobe
	+ `anmerkung`: Anmerkungen zu einer Stichprobe
* Relationship: BelongsTo: Eine Beziehung zwischen "P7Datenextraktion" und "P5Treffer", bei der "P7Datenextraktion" ein belongsTo-Verhältnis zu dem Modell "P5Treffer" hat.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Die Datei app/Models/Recherche/P7GradeEinschaetzung.php definiert eine Eloquent-Modellklasse für die Tabelle p7_grade_einschaetzung in der Datenbank, die als Beziehung zu einer Projektmodellklasse fungiert.

---

### app/Models/Recherche/P7MusterKonsistenz.php
File: app/Models/Recherche/P7MusterKonsistenz.php
Category: data_access

This file defines a PHP class for the P7MusterKonsistenz model in the Recherche namespace of the App module. The class extends the Illuminate\Database\Eloquent\Model class and uses the HasUuids trait to generate UUIDs for the model instances.

The class is responsible for storing information about the consistency of different muster types in the database, including the id of the related Projekt, the muster type itself, and arrays of supporting and contradicting sources. It also defines a belongsTo relationship with the Projekt class to retrieve the project information associated with each muster konsistenz entry.

The file contains the following functions:

* The constructor function that initializes the model instance.
* The getTable function that returns the name of the database table used by the model.
* The timestamps function that returns false, indicating that the model does not use timestamps.
* The fillable and casts properties that define which attributes are mass assignable and how they should be cast to the appropriate type.
* The projekt method that defines a belongsTo relationship with the Projekt class.

---

### app/Models/Recherche/P7SyntheseMethode.php
This file contains the P7SyntheseMethode model for the data access layer of a Laravel application. It defines a database table for storing synthesis methods used in a project and provides methods for retrieving, creating, updating, and deleting records from this table.

---

### app/Models/Recherche/P8Limitation.php
Die Datei app/Models/Recherche/P8Limitation.php enthält eine Klasse namens P8Limitation, die es ermöglicht, Limitierungen für ein Projekt zu speichern und zu verarbeiten. Sie verwendet den Datenbank-Treiber von Eloquent und verfügt über eine Beziehung zur Projekttabelle über das Project-Modell. Die Klasse erbt von der Model-Klasse und definiert die Füllfähigkeiten für die Projekt-ID, Limitationstyp, Beschreibung, Auswirkung auf Vollständigkeit.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
{ "file_summary" : "Maximal 10-zeilige Beschreibung der Datei-Funktionen"

Diese PHP-Klasse implementiert die Klasse P8Reproduzierbarkeitspruefung, um Recherche-Modell für Prüfpunkte zu speichern. Die Klasse erweitert die Model-Klasse von Laravel und verwendet die HasUuids-Trait-Datei für die Verwendung von UUIDs als Primärschlüssel.
Die Tabelle "p8_reproduzierbarkeitspruefung" in der Datenbank wird durch den Tabellen-Namen "projekt" und die Relation "belongsTo" zugewiesen. Die Klasse verfügt über eine Methode "projekt()", die einen Eloquent-Relationship für das Projekt herstellt, welches der Prüfpunkt gehört.
Zusätzlich zum Speichern von Primärschlüssel-Werten und Timestamps in der Datenbank, erlaubt die Klasse auch die Anmerkung und den Status des Prüfpunkts zu speichern. Der Status wird als Boolean-Wert "erfuellt" gespeichert, der eine bestimmte Anmerkung haben kann, wenn der Prüfpunkt nicht erfüllt ist.
Diese Klasse nutzt auch die casts-Methode von Laravel, um den Wert des Primärschlüssels und des Status zu einem Boolean-Wert in der Datenbank zu konvertieren.
}

---

### app/Models/Recherche/P8Suchprotokoll.php
The file "app/Models/Recherche/P8Suchprotokoll.php" contains a PHP class that represents a record in the database table "p8_suchprotokoll". The class is part of the Laravel framework, and it uses Eloquent ORM to interact with the database.

Here are some key features of the class:

* It extends the `Illuminate\Database\Eloquent\Model` class, which provides a convenient way to interact with the database in Laravel.
* The class uses the `HasUuids` trait, which adds support for generating UUIDs (Universally Unique Identifiers) for the model's primary key. This is useful for creating unique identifiers for each record in the table.
* The class has a `$table` property that defines the name of the database table to use for this model. In this case, it is set to "p8_suchprotokoll".
* The class has a `$fillable` array that specifies which columns of the table can be mass-assigned when creating or updating records. In this case, it includes the ID column and all other columns that are allowed to be updated.
* The class has a `$casts` array that specifies how certain attributes should be casted to specific types when retrieved from the database. For example, the `gesetzte_filter` attribute is cast to an array type.
* The class defines a relationship between this model and another model called "P4Suchstring" using the `belongsTo()` method. This means that each instance of this model has a reference to a related instance of the other model, and can access its attributes through that reference.

Overall, this file defines a PHP class that represents a record in the "p8_suchprotokoll" table of the database, and provides a convenient way to interact with that table using Eloquent ORM.

---

### app/Models/Recherche/P8UpdatePlan.php
* Die Datei `app/Models/Recherche/P8UpdatePlan.php` ist eine Eloquent-Modell-Klasse in Laravel, die die Tabelle `p8_update_plan` in der Datenbank darstellt.
* Die Klasse verwendet die Trait-Methode `HasUuids`, um UUIDs (Universally Unique Identifiers) für die Modell-Instanzen zu generieren.
* Die Tabelle ist durch die Eigenschaften `$table` und `$timestamps` definiert, und es sind einige Attribut-Einstellungen vorgenommen worden, um die Modell-Einträge zu bearbeiten.
* Die Klasse enthält auch eine `belongsTo`-Relation für das Projekt-Modell (`Projekt`), das durch den Fremdschlüssel `projekt_id` verknüpft wird.
* Die Modell-Einträge können über die Eigenschaften `$fillable` und `$casts` gefiltert werden, um bestimmte Spalten zu aktualisieren oder zu lesen.

---

### app/Models/Recherche/PaperEmbedding.php
* Die Datei `app/Models/Recherche/PaperEmbedding.php` enthält eine Klasse namens `PaperEmbedding`, die ein Eloquent-Modell für die Tabelle `paper_embeddings` in der Datenbank darstellt.
* Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und nutzt das `HasUuids`-Trait, um UUIDs zu verwenden, anstatt die standardmäßige Auto-Increment-IDs.
* Die Klasse definiert verschiedene Attribute für die Tabelle, darunter `projekt_id`, `source`, `paper_id`, `title`, `chunk_index`, `text_chunk`, `metadata` und `erstellt_am`.
* Die Klasse hat auch eine Beziehung zu der `Projekt`-Klasse über die Methode `projekt()`, die einen BelongsTo-Typ verwendet.

---

### app/Models/Recherche/Phase.php
This is a PHP class file for the `Phase` model in an Eloquent ORM. The class is located in the `app/Models/Recherche/` directory and uses the `HasUuids` trait to enable UUID support for the model.

The `Phase` model represents a phase in a project, with the following fields:

* `projekt_id`: a foreign key referencing the `Projekt` model
* `phase_nr`: an integer representing the number of the phase
* `titel`: a string representing the title of the phase
* `status`: an enum representing the status of the phase (e.g., "in progress", "completed")
* `notizen`: a text field for notes related to the phase
* `abgeschlossen_am`: a datetime field representing the date when the phase was completed

The `Phase` model also has a relationship with the `Projekt` model through the `projekt()` method, which returns a BelongsTo relationship. This means that each instance of the `Phase` model is associated with exactly one instance of the `Projekt` model.

Overall, this class file defines a PHP class for the `Phase` model in an Eloquent ORM and provides the necessary functionality to interact with the related data stored in the database.

---

### app/Models/Recherche/Projekt.php
Die Datei `app/Models/Recherche/Projekt.php` enthält eine Klasse für das Modell "Projekt" in einer Suchmaschine-Anwendung. Die Klasse ist Teil des Laravel-Frameworks und erweitert die `Illuminate\Database\Eloquent\Model`-Klasse.

Die Klasse definiert verschiedene Beziehungen zu anderen Modellen in der Anwendung, darunter `User`, `Phase`, `P1Strukturmodellwahl`, `P1Komponente`, `P1Kriterium`, `P1Warnsignal`, `P5Treffer`, `P2ReviewTypEntscheidung`, `P2Cluster`, `P2MappingSuchstringKomponenten`, `P2Trefferlisten`, `P3Datenbankmatrix`, `P3Disziplin`, `P3GeografischeFilter`, `P3GraueLiteratur`, `P4Suchstring`, `P4ThesaurusMapping`, `P5PrismaZahlen`, `P5ScreeningKriterium`, `P5ToolEntscheidung`, `P6Luckenanalyse`, `P7SyntheseMethode`, `P7GradeEinschaetzung`, `P7MusterKonsistenz`, `P8Limitationen`, `P8Reproduzierbarkeitspruefung` und `P8UpdatePlan`.

Die Klasse definiert auch mehrere Eigenschaften, darunter `user_id`, `titel`, `forschungsfrage`, `review_typ`, `verantwortlich`, `startdatum`, `notizen` und `erstellt_am`. Die Eigenschaften werden durch die `$fillable`-Eigenschaft definiert.

Die Klasse verwendet auch einige Traits, darunter `HasFactory`, `HasUuids` und `LogsActivity`. Der Trait `HasFactory` ermöglicht die Verwendung von Factory-Methode, um Instanzen der Klasse zu erstellen. Der Trait `HasUuids` ermöglicht die Verwendung von UUIDs als Primärschlüssel für die Klasse. Der Trait `LogsActivity` ermöglicht die Protokollierung von Aktivitäten in der Klasse.

Die Klasse definiert auch eine Methode namens `getActivitylogOptions`, um die Optionen für das Protokollieren von Aktivitäten zu konfigurieren. Die Methode gibt eine Instanz des Traits `LogOptions` zurück, um die Optionen für das Protokollieren von Aktivitäten zu konfigurieren.

Insgesamt ist diese Datei ein wichtiger Bestandteil der Suchmaschine-Anwendung und bietet eine Klasse für das Modell "Projekt", das verschiedenen Funktionen und Beziehungen zu anderen Modellen in der Anwendung entspricht.

---

### database/factories/UserFactory.php
File: database/factories/UserFactory.php
Category: data_access

This file is part of a Laravel project and contains the definition for the User model factory. The factory is used to generate test data for the User model, which is an Eloquent ORM model that represents a user in the application.

The class `UserFactory` extends the `Factory` class provided by Laravel's Eloquent ORM, and it defines a set of methods for generating test data for the User model. The `definition()` method returns an array of attributes that are used to create new User models when the factory is called. The `unverified()` and `withoutTwoFactor()` methods are used to generate test data that does not have an email address or two-factor authentication configured, respectively.

The file also imports several external dependencies:

* `Illuminate\Database\Eloquent\Factories\Factory` is the base class for factories in Laravel's Eloquent ORM. It provides a set of methods for generating test data for models.
* `Hash` is a PHP function that generates hashed strings, which are used to store passwords securely in the database.
* `Str` is a PHP function that provides a set of string manipulation functions. In this case, it is used to generate random strings for the user's two-factor authentication recovery codes and secret.

Overall, this file defines a factory for generating test data for the User model in a Laravel application, which can be useful for testing the application's behavior with different types of users.

---

### database/factories/Recherche/ProjektFactory.php
{
"file_summary": "Die Datei database/factories/Recherche/ProjektFactory.php definiert eine Factory-Klasse, die es ermöglicht, Objekte der Klasse App\Models\Recherche\Projekt in einer Datenbank zu erstellen und zu aktualisieren."
}

---

### database/migrations/0001_01_01_000000_create_users_table.php
Die Datei `database/migrations/0001_01_01_000000_create_users_table.php` enthält eine Klasse, die die Migration einer Datenbank-Tabelle für Benutzer implementiert. Die Klasse erweitert die `Illuminate\Database\Migrations\Migration`-Klasse von Laravel und implementiert die Methoden `up()` und `down()`.

In der Methode `up()` wird die Tabelle `users` erstellt, indem sie mithilfe des `Schema`-Objekt das Layout der Tabelle definiert. Die Tabelle hat eine ID-Spalte (`$table->id()`), einen Namen-Spalte (`$table->string('name')`), eine E-Mail-Adresse-Spalte (`$table->string('email')->unique()`), eine E-Mail-Verifizierungs-Zeitstempel-Spalte (`$table->timestamp('email_verified_at')->nullable()`) und eine Passwort-Spalte (`$table->string('password')`). Die Tabelle hat auch eine Remember-Token-Spalte (`$table->rememberToken()`) und Timestamp-Spalten für die Erstellung und Aktualisierung der Tabelle (`$table->timestamps()`).

Die Methode `down()` löscht die Tabelle, falls sie vorhanden ist. Sie verwendet die `dropIfExists`-Methode des `Schema`-Objektes, um die Tabelle zu entfernen, wenn sie existiert.

Diese Datei ist Teil der Laravel-Anwendung und dient dazu, Benutzer in einer Datenbank zu speichern.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Die Datei `database/migrations/0001_01_01_000001_create_cache_table.php` enthält eine Migration für das Erstellen einer Tabelle mit dem Namen `cache`. Die Migration verwendet die Laravel-Klasse `Illuminate\Database\Migrations\Migration` und die Funktionen `up()` und `down()` zum Erstellen und Löschen der Tabelle.

Die Funktion `up()` erstellt zwei Tabellen: `cache` und `cache_locks`. Die Tabelle `cache` enthält die Spalten `key`, `value` und `expiration`, während die Tabelle `cache_locks` die Spalten `key`, `owner` und `expiration` enthält.

Die Funktion `down()` löscht die Tabellen `cache` und `cache_locks`.

Dieser Code deklariert auch einige Abhängigkeiten, wie z.B. die Verwendung von Laravel-Komponenten wie `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\Schema` und `Illuminate\Database\Query\Builder`.

Die Datei ist Teil des Laravel-Frameworks und dient zur Verwaltung der Daten in der Datenbank.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
The file `database/migrations/0001_01_01_000002_create_jobs_table.php` contains a migration for creating tables in the database to store jobs and job batches.

Here is a summary of the main functionalities of the file:

* The file defines three new tables: `jobs`, `job_batches`, and `failed_jobs`.
* The `jobs` table contains columns for storing information about each job, such as its ID, queue, payload, attempts, reserved time, available time, and created time.
* The `job_batches` table contains columns for storing information about each job batch, such as its ID, name, total jobs, pending jobs, failed jobs, and options.
* The `failed_jobs` table contains columns for storing information about each failed job, such as its UUID, connection, queue, payload, exception, and failed time.

The file also defines two functions: `up()` and `down()`. The `up()` function is called when the migration is executed and creates the new tables in the database. The `down()` function is called when the migration is rolled back and drops the previously created tables.

This file requires the Laravel framework to run, as it uses the `Illuminate\Database\Migrations` namespace and the `use` statement to import the necessary classes. Additionally, this file relies on the database connection to be set up correctly in order to interact with the database.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
This migration adds two additional columns to the users table: 'two_factor_secret' and 'two_factor_recovery_codes'. These columns are used for storing information about two-factor authentication.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
{
"file_summary": "This file creates a 'contacts' table in the database. It uses the Illuminate\Database\Migrations\Migration class and defines two methods: up() and down(). The up() method is called when running migrations, and it creates the 'contacts' table with columns for id, name, company (nullable), email, project_type, message, timeline (nullable), and timestamps. The down() method is called when rolling back migrations, and it drops the 'contacts' table if it exists."
}

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Die Datei ist eine PHP-Klasse, die den Vorgang der Erstellung einer Tabelle in einer Datenbank implementiert. Die Klasse erbt von der Migrationsklasse und enthält zwei Methoden: `up()` und `down()`.

In der `up`-Methode wird die Tabelle "page_views" erstellt, indem eine Blueprint-Instanz verwendet wird, um die Spaltendefinitionen zu definieren. Die Tabelle hat eine ID-Spalte (mit dem Typ `id()`), eine visits-Spalte (mit dem Typ `unsignedBigInteger` und einem Standardwert von 0) und zwei Zeitstempel-Spalten (mit den Typen `timestamps`).

In der `down`-Methode wird die Tabelle "page_views" gelöscht, wenn sie existiert.

Die Klasse importiert die Schema-Klasse von Laravel, um auf das Schema-Objekt zuzugreifen und die Tabelle zu erstellen oder zu löschen. Sie importiert auch die Blueprint-Klasse, um eine Instanz der Blueprint-Klasse zu erstellen, die verwendet wird, um die Spaltendefinitionen zu definieren.

Die Datei ist Teil des Laravel-Frameworks und dient dazu, Datenbankmigrationen durchzuführen.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
This is a Laravel migration file that creates the necessary database tables for the "spatie/laravel-permission" package. The file contains several `Schema::create()` statements, each defining a new table with its own structure and constraints.

The `permissions` table has three columns: an ID column, a `name` column, and a `guard_name` column. These columns correspond to the attributes of a permission object in the package. The table also includes timestamps for created and updated at, as well as a unique index on the name and guard name.

The `roles` table has similar structure, with an ID column, a `name` column, and a `guard_name` column, as well as timestamps for created and updated at. The table also includes a unique index on the name and guard name.

The `model_has_permissions`, `model_has_roles`, and `role_has_permissions` tables are used to establish relationships between permissions, roles, and models in the application. These tables have foreign key constraints that reference the primary keys of the corresponding tables. They also include an index on the model morph key and a unique constraint on the permission ID and role ID.

The `down()` method is responsible for dropping the tables created by this migration file. It uses the `Schema::dropIfExists()` function to drop each table if it exists, based on the table names defined in the configuration file (`config/permission.php`).

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
This file contains a migration for adding three new columns to the `page_views` table in the database: `path`, `ip_anonymous`, and `user_agent`. The migration is written using the Laravel framework's migration system, which provides a way to safely modify the database schema without breaking existing data.

The file starts with an opening PHP tag (`<?php`) and then includes two necessary imports:
```php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
```
These imports allow the file to use the `Migration` and `Blueprint` classes provided by the Laravel framework.

The next section of the code defines a new class that extends the `Migration` class:
```php
return new class extends Migration {
    // ...
};
```
This class has two methods: `up()` and `down()`. The `up()` method is called when the migration is run, and it should perform any necessary actions to add or modify columns in the database. In this case, the `up()` method adds three new columns to the `page_views` table:
```php
Schema::table('page_views', function (Blueprint $table) {
    $table->string('path')->default('/')->after('id');
    $table->string('ip_anonymous')->nullable()->after('path');
    $table->string('user_agent')->nullable()->after('ip_anonymous');
});
```
The `down()` method is called when the migration needs to be rolled back, and it should perform any necessary actions to remove or modify columns in the database. In this case, the `down()` method drops the three new columns added by the `up()` method:
```php
Schema::table('page_views', function (Blueprint $table) {
    $table->dropColumn(['path', 'ip_anonymous', 'user_agent']);
});
```
The migration file is written in a way that allows it to be executed safely, without affecting any existing data. The `Schema::table()` method provides a way to interact with the database schema, and the `Blueprint` class provides a way to create and modify tables in a safe and efficient manner.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Die Datei `2026_03_31_000003_create_consents_table.php` enthält eine Migration für die Erstellung einer Tabelle namens `consents`. Diese Tabelle wird verwendet, um Benutzer-Zustimmungen zu speichern.

Die Datei importiert folgende Klassen und Funktionen:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse für Migrationen in Laravel.
* `Illuminate\Database\Schema\Blueprint`: Eine Hilfs-Klasse, die verwendet wird, um Tabellendefinitionen zu erstellen.
* `Illuminate\Support\Facades\Schema`: Eine Fassade-Klasse, die verwendet wird, um Schema-Migrationen anzulegen und auszuführen.
* `user_id`: Ein Foreign Key, der auf eine Tabelle mit Benutzer-Informationen verweist.
* `consent_categories`: Eine JSON-Spalte, die verwendet wird, um Benutzer-Zustimmungen für verschiedene Kategorien zu speichern.
* `timestamp`: Eine Spalte, die verwendet wird, um Datumsangaben zu speichern.

Die Datei definiert zwei Methoden: `up` und `down`. Die Methode `up` wird verwendet, um die Tabelle `consents` zu erstellen, während die Methode `down` verwendet wird, um die Tabelle zu löschen. Beide Methoden werden bei der Ausführung von Laravel-Migrationen aufgerufen.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Die Datei `database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php` ist eine Migration-Datei für die SQLite-Version des Recherche-Projekts. Die Datei enthält einige Tabelle für die Projektdatenbank und fügt einige Standardwerte hinzu.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `Migration`: Die Klasse stellt eine Migration dar, das heißt ein Prozess, der es ermöglicht, die Datenbankstruktur zu ändern. In dieser Datei wird die Migration verwendet, um neue Tabellen für das Projekt zu erstellen.
* `Blueprint`: Diese Klasse dient zum Erstellen von Tabellen- und Spaltendefinitionen. Sie wird in der Migration verwendet, um die neuen Tabellen für das Projekt zu definieren.
* `Schema`: Die Klasse `Schema` bietet eine API zur Verfügung, um Datenbanktabellen und -spalten zu erstellen, zu ändern und zu löschen. In dieser Datei wird `Schema::hasTable()` verwendet, um zu überprüfen, ob die Tabelle bereits existiert, bevor sie erstellt wird.
* `DB`: Die Klasse `DB` stellt eine API zur Verfügung, um Datenbankzugriffe durchzuführen. In dieser Datei wird `DB::getDriverName()` verwendet, um den aktuellen Datenbanktreiber zu bestimmen und die Tabellen zu erstellen oder zu ändern.
* `return`: Die Funktion `return` wird verwendet, um einen Wert zurückzugeben. In diesem Fall wird ein neuer `class` mit der Migration-Klasse zurückgegeben.

Die Datei enthält auch einige Standardwerte für die Tabellen und Spalten, wie z.B. den Status der Benutzer (`trial`) oder das Datum und die Uhrzeit des letzten Updates.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
This is a PHP migration file for a PostgreSQL database. It creates a number of enum types that are used to define different kinds of metadata in the system. The file uses the Laravel framework's migration syntax, which allows it to be run as part of a larger database migration process.

The file defines 16 enum types, each with a specific set of values. The names and values of these enums are determined by the names and values of the corresponding columns in the system's metadata tables.

Here is a brief overview of the enums defined in this file:

* `phase_status`: Defines the status of a phase in the research process, with possible values such as "offen", "in_bearbeitung", and "abgeschlossen".
* `review_typ`: Defines the type of review being conducted, with possible values such as "systematic_review", "scoping_review", or "evidence_map".
* `strukturmodell`: Defines the structure model used in the research process, with possible values such as "PICO", "SPIDER", or "PICOS".
* `kriterium_typ`: Defines the type of inclusion/exclusion criteria being used in the research process, with possible values such as "einschluss" or "ausschluss".
* `screening_level`: Defines the level of screening being applied to a study, with possible values such as "L1_titel_abstract" or "L2_volltext".
* `screening_entscheidung`: Defines the decision made about a study after it has been screened, with possible values such as "eingeschlossen", "ausgeschlossen", or "unklar".
* `rob_tool`: Defines the type of tool being used for ROBUST evaluation, with possible values such as "RoB2" or "ROBINS-I".
* `rob_urteil`: Defines the level of agreement between the reviewer and the researcher in terms of ROBUST evaluation, with possible values such as "niedrig", "moderat", "hoch", "kritisch", or "nicht_bewertet".
* `synthese_methode`: Defines the type of synthesis method being used for a study, with possible values such as "meta_analyse", "narrative_synthese", "thematische_synthese", or "framework_synthesis".
* `grade_urteil`: Defines the level of agreement between the reviewer and the researcher in terms of synthesis grade, with possible values such as "stark", "moderat", "schwach", or "sehr_schwach".
* `studientyp`: Defines the type of study being analyzed, with possible values such as "RCT", "nicht_randomisiert", "qualitativ", "systematic_review", "guideline_framework", or "konzeptuell".
* `tool_empfehlung`: Defines the type of tool that is recommended for use in a specific context, with possible values such as "Rayyan", "Covidence", "EPPI_Reviewer", "DistillerSR", "ASReview", or "SWIFT_ActiveScreener".

This file also includes two functions for upgrades and downgrades of the enums: `up()` and `down()`. The `up()` function creates the enum types if they do not already exist, while the `down()` function deletes the enum types if they exist.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
This is a PHP file that defines a migration for creating two tables in the `database` schema: `projekte` and `phasen`. The `projekte` table contains information about projects, such as their title, responsible person, start date, and other notes. The `phasen` table contains information about project phases, such as their title, notes, and completion status.

The migration also adds a column to the `projekte` table called `review_typ`, which is not present in the initial table definition. This column is not used in the provided code, but it might be added by other developers or during future migrations.

In addition, this migration creates indexes on the `id` and `phase_nr` columns of the `phasen` table to ensure efficient lookup and search operations. The `projekt_id` column in the `phasen` table is defined as a foreign key that references the `id` column of the `projekte` table, which ensures referential integrity and prevents invalid data from being inserted into the `phasen` table.

Overall, this migration creates two new tables in the `database` schema and adds some columns to existing tables. It also creates indexes on some columns to improve performance.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
This PHP file contains a migration for a Laravel application. It creates several tables in the database, including `p1_strukturmodell_wahl`, `p1_komponenten`, `p1_kriterien`, and `p1_warnsignale`. The `up` method of the migration class is responsible for creating these tables, while the `down` method is responsible for dropping them.

The `use Illuminate\Database\Migrations\Migration;` line at the top of the file imports the `Migration` class from Laravel's database migrations package. The `use Illuminate\Support\Facades\DB;` line imports the `DB` facade, which provides a convenient way to interact with the database in Laravel.

The `use Illuminate\Database\Schema\Blueprint;` line imports the `Blueprint` class from Laravel's database schema builder package. The `Blueprint` class is used to define the structure of a table.

The `use Illuminate\Support\Facades\Schema;` line imports the `Schema` facade, which provides a convenient way to interact with the database schema in Laravel.

The `return new class extends Migration` statement creates an anonymous subclass of the `Migration` class and returns it as a closure. This is necessary because the `Migration` class is not instantiable directly. The `extends Migration` keyword makes this file a migration that can be executed by Laravel's migration system.

The `up` method creates several tables in the database, including `p1_strukturmodell_wahl`, `p1_komponenten`, `p1_kriterien`, and `p1_warnsignale`. The `down` method drops these tables.

The `id` column is a primary key that uses the default Laravel auto-incrementing ID strategy. The `projekt_id` column is a foreign key that references the `id` column of the `projekte` table. The `gewaehlt`, `begruendung`, and `quellbezug` columns are nullable, while the other columns are not.

The `kriterium_typ` column is a foreign key that references the `kriterien_typ` table.

The `synonyme` column is a JSONB (JSON with binary) column that contains an array of synonyms for each component.

The migration also includes some SQL statements that use the PostgreSQL-specific syntax for creating tables and modifying columns. These statements are only executed if the database driver being used is PostgreSQL.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Die Datei `database/migrations/2026_03_31_100003_create_p2_tables.php` enthält eine Klasse, die eine Migration für die Erstellung von Tabellen in der Datenbank durchführt. Die Klasse ist Teil des Laravel-Frameworks und erbt von der `Migration`-Klasse.

In der Datei sind folgende Funktionen definiert:

1. `up`: Diese Funktion wird ausgeführt, wenn die Migration angewendet werden soll. Sie erstellen die Tabellen in der Datenbank, die für das P2-Projekt benötigt werden.
2. `down`: Diese Funktion wird ausgeführt, wenn die Migration zurückgenommen werden soll. Sie löschen die Tabellen aus der Datenbank, die für das P2-Projekt benötigt wurden.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Migration`: Die Basisklasse für alle Laravel-Migrationen.
* `Blueprint`: Eine Hilfsklasse, die verwendet wird, um Tabellen zu definieren.
* `Schema`: Eine statische Klasse, die den Zugriff auf die Datenbank ermöglicht.
* `DB::getDriverName()`: Diese Funktion gibt den Namen des verwendeten Datenbanktreibers zurück.
* `Schema::hasTable('p2_review_typ_entscheidung')`: Diese Funktion überprüft, ob eine bestimmte Tabelle in der Datenbank vorhanden ist.
* `Schema::create('p2_review_typ_entscheidung', function (Blueprint $table) {...})`: Diese Funktion erstellt eine neue Tabelle mit dem Namen 'p2_review_typ_entscheidung' und definiert die Spalten der Tabelle.
* `DB::statement("ALTER TABLE p2_review_typ_entscheidung ADD COLUMN review_typ review_typ NOT NULL")`: Diese Funktion fügt einer vorhandenen Tabelle eine neue Spalte hinzu.
* `Schema::dropIfExists('p2_cluster')`: Diese Funktion löscht die Tabelle 'p2_cluster' aus der Datenbank, wenn sie bereits vorhanden ist.

Die Datei enthält auch External Dependencies, d.h. Abhängigkeiten von anderen Komponenten in der Anwendung. Hierzu zählen:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse für alle Laravel-Migrationen.
* `Illuminate\Database\Schema\Blueprint`: Eine Hilfsklasse, die verwendet wird, um Tabellen zu definieren.
* `Illuminate\Support\Facades\DB`: Eine statische Klasse, die den Zugriff auf die Datenbank ermöglicht.
* `Illuminate\Support\Facades\Schema`: Eine statische Klasse, die den Zugriff auf die Datenbank ermöglicht.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Die Datei `2026_03_31_100004_create_p3_tables.php` enthält eine PHP-Klasse, die eine Migration für die Datenbank darstellt. Die Klasse erweitert die `Migration`-Klasse von Laravel und definiert die Methoden `up()` und `down()`, um die Tabelle `p3_disziplinen`, `p3_datenbankmatrix`, `p3_geografische_filter` und `p3_graue_literatur` zu erstellen und zu löschen.

In der Methode `up()` werden die Tabellen erstellt, indem sie mithilfe der Laravel-Migrationsfunktionen erstellt werden. Die Tabellen enthalten die Spalten `id`, `projekt_id`, `disziplin`, `art`, `relevanz`, `anmerkung` für die Tabelle `p3_disziplinen`. Die Spalte `id` ist eine UUID-Spalte, die als Primärschlüssel fungiert und automatisch generiert wird. Die Spalten `projekt_id`, `disziplin`, `art` und `relevanz` sind Textspalten.

Die Tabelle `p3_datenbankmatrix` enthält Spalten für die Primärschlüssel `id`, `projekt_id`, `datenbank`, `disziplin`, `abdeckung`, `besonderheit`, `zugang`, `empfohlen` und `begruendung`. Die Spalte `id` ist eine UUID-Spalte, die als Primärschlüssel fungiert und automatisch generiert wird. Die Spalten `projekt_id`, `datenbank`, `disziplin`, `abdeckung`, `besonderheit`, `zugang`, `empfohlen` und `begruendung` sind Textspalten.

Die Tabelle `p3_geografische_filter` enthält Spalten für die Primärschlüssel `id`, `projekt_id`, `region_land`, `validierter_filter_vorhanden`, `filtername_quelle`, `sensitivitaet_prozent`, `hilfsstrategie`. Die Spalte `id` ist eine UUID-Spalte, die als Primärschlüssel fungiert und automatisch generiert wird. Die Spalten `projekt_id`, `region_land`, `validierter_filter_vorhanden`, `filtername_quelle`, `sensitivitaet_prozent` und `hilfsstrategie` sind Textspalten.

Die Tabelle `p3_graue_literatur` enthält Spalten für die Primärschlüssel `id`, `projekt_id`, `quelle`, `typ`, `url`, `suchpfad`, `relevanz` und `anmerkung`. Die Spalte `id` ist eine UUID-Spalte, die als Primärschlüssel fungiert und automatisch generiert wird. Die Spalten `projekt_id`, `quelle`, `typ`, `url`, `suchpfad`, `relevanz` und `anmerkung` sind Textspalten.

Die Methode `down()` löscht die Tabellen, indem sie mithilfe der Laravel-Migrationsfunktionen gelöscht werden.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
Die Datei `database/migrations/2026_03_31_100005_create_p4_tables.php` enthält eine Klasse, die eine Datenbankmigration für ein Laravel-Projekt darstellt. Die Klasse erweitert das Basisklassenobjekt `Migration`, das in der Laravel-Kernel-Bibliothek definiert ist.

In dieser Datei werden folgende Tabellen erstellt:

1. `p4_suchstrings`: Diese Tabelle enthält Suchbegriffe und ihre Zuordnung zu bestimmten Projekten. Jede Zeile stellt eine Suche dar, die durch eine eindeutige ID (`id`) identifiziert wird. Die Spalte `projekt_id` enthält eine Referenz zur Projekt-ID, der diese Suche zugeordnet ist. Die Spalte `datenbank` enthält den Namen der Datenbank, in der die Suche ausgeführt wurde. Die Spalte `suchstring` enthält den Suchbegriff selbst. Die Spalten `feldeinschraenkung`, `gesetzte_filter` und `treffer_anzahl` werden nicht verwendet. Die Spalte `einschaetzung`, `anpassung` und `version` enthalten Informationen über die Auswertung der Suche. Die Spalte `suchdatum` enthält das Datum, an dem die Suche durchgeführt wurde. Die Spalte `erstellt_am` enthält das Datum und die Uhrzeit, zu der diese Migration erstellt wurde.
2. `p4_thesaurus_mapping`: Diese Tabelle enthält Zuordnungen zwischen bestimmten Projekten und Thesaurus-Einträgen. Jede Zeile stellt eine Zuordnung dar, die durch eine eindeutige ID (`id`) identifiziert wird. Die Spalte `projekt_id` enthält eine Referenz zur Projekt-ID, der diese Zuordnung zugeordnet ist. Die Spalten `freitext_de`, `freitext_en`, `mesh_term`, `emtree_term` und `psycinfo_term` enthalten die Zuordnung zwischen dem Thesaurus-Eintrag und dem Projekt. Die Spalte `anmerkung` enthält eine Anmerkung zur Zuordnung.
3. `p4_anpassungsprotokoll`: Diese Tabelle enthält ein Protokoll der Anpassungen, die an bestimmten Suchbegriffen vorgenommen wurden. Jede Zeile stellt eine Anpassung dar, die durch eine eindeutige ID (`id`) identifiziert wird. Die Spalte `suchstring_id` enthält eine Referenz zur Suche, für die diese Anpassung durchgeführt wurde. Die Spalten `version`, `datum`, `aenderung`, `grund` und `entscheidung` enthalten Informationen über die Anpassung.

Die Klasse definiert auch Methoden zum Erstellen und Löschen dieser Tabellen (`up()` und `down()`, respectively). Die Methoden `up()` werden beim Start des Laravel-Projekts aufgerufen, um die Tabellen zu erstellen, wenn sie noch nicht vorhanden sind. Die Methoden `down()` werden dagegen beim Update des Laravel-Projekts aufgerufen, um die Tabellen zu löschen und neu zu erstellen, wenn sie geändert wurden.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Diese Datei enthält eine Laravel-Migration, die verschiedene Tabellen und Fremdschlüssel in der Datenbank erstellt. Hier sind die wichtigsten Informationen:

* Die Migration ist für den `pgsql`-Treiber ausgelegt, d.h. sie wird nur auf einer PostgreSQL-Datenbank ausgeführt.
* Es werden mehrere Tabellen erstellt:
	+ `p5_prisma_zahlen`: Eine Tabelle für die PrISMA-Zahlen des Projekts. Die Tabelle enthält einige Felder wie `projekt_id`, `identifiziert_gesamt` und `davon_datenbank_treffer`.
	+ `p5_screening_kriterien`: Eine Tabelle für die Screening-Kriterien des Projekts. Die Tabelle enthält Felder wie `beschreibung`, `beispiel` und `level`.
	+ `p5_treffer`: Eine Tabelle für die centralen Treffer des Projekts. Die Tabelle enthält Felder wie `record_id`, `titel`, `autoren`, `jahr` usw.
	+ `p5_screening_entscheidungen`: Eine Tabelle für die Screening-Entscheidungen der Treffer des Projekts. Die Tabelle enthält Felder wie `ausschlussgrund`, `reviewer` und `datum`.
	+ `p5_tool_entscheidung`: Eine Tabelle für die Tool-Empfehlungen des Projekts. Die Tabelle enthält Felder wie `gewaehlt`, `begruendung` und `tool`.
* Es werden auch einige Fremdschlüssel erstellt:
	+ Ein Fremdschlüssel von der Tabelle `p5_screening_kriterien` zu der Tabelle `p5_prisma_zahlen`, der die Verbindung zwischen den Screening-Kriterien und den PrISMA-Zahlen herstellt.
	+ Ein Fremdschlüssel von der Tabelle `p5_screening_entscheidungen` zu der Tabelle `p5_treffer`, der die Verbindung zwischen den Screening-Entscheidungen und den Treffern herstellt.
	+ Ein Fremdschlüssel von der Tabelle `p5_tool_entscheidung` zu der Tabelle `p5_prisma_zahlen`, der die Verbindung zwischen den Tool-Empfehlungen und den PrISMA-Zahlen herstellt.
* Die Migration behandelt auch das Löschen der Tabellen, wenn sie bereits in der Datenbank vorhanden sind.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
This is a PHP class that creates two database tables: `p6_qualitaetsbewertung` and `p6_luckenanalyse`. The `up()` method creates these tables if they do not already exist, using the Laravel framework. The `down()` method drops these tables if they exist.

The `p6_qualitaetsbewertung` table has columns for a unique identifier (`id`), a foreign key referencing the `projekte` table, and some text fields for storing data. The `p6_luckenanalyse` table also has a foreign key reference to the `projekte` table, but it also has additional columns for storing data.

The `up()` method uses Laravel's migration system to create these tables, which allows for more fine-grained control over the schema and allows for versioning of the database. The `down()` method is used to drop the tables if they exist, so that the migrations can be re-run if necessary.

The `use Illuminate\Database\Migrations\Migration;` line at the top of the file imports the `Migration` class from the Laravel framework, which allows for creating and managing database migrations. The `use Illuminate\Support\Facades\DB;` line imports the `DB` facade from Laravel, which provides a convenient way to interact with the database.

The `if (DB::getDriverName() !== 'pgsql') { return; }` check at the beginning of the file checks if the driver used for the database connection is PostgreSQL, and if it is not, the method returns early without doing anything. This is because this class is intended to be used with a PostgreSQL database, and it would not make sense to use it with any other type of database.

The `Schema::create()` method is used to create the tables, and the `Blueprint` class is used to define the structure of each table. The `uuid('id')->primary()->default(DB::raw('uuid_generate_v4()'))` line in the `p6_qualitaetsbewertung` table creates a unique identifier for each row in the table, using the `uuid` data type and the `uuid_generate_v4()` function to generate a new UUID. The `cascadeOnDelete()` method is used to specify that deleting a record from the `projekte` table should also delete any related records from this table.

The `if (! Schema::hasTable('p6_luckenanalyse')) { Schema::create('p6_luckenanalyse', function (Blueprint $table) { ... }); }` line checks if the `p6_luckenanalyse` table exists, and if it does not, it creates the table using the same structure as the `p6_qualitaetsbewertung` table.

Overall, this class is intended to be used with a PostgreSQL database and provides a convenient way to create and manage the `p6_qualitaetsbewertung` and `p6_luckenanalyse` tables in the database.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
This migration file creates several tables in the `database` schema for a Laravel application:

1. `p7_grade_einschaetzung`: This table has columns for `id`, `projekt_id`, `outcome`, `studienanzahl`, `inkonsistenz`, `indirektheit`, `impraezision`, and `begruendung`. It also has a foreign key reference to the `projekte` table.
2. `p7_muster_konsistenz`: This table has columns for `id`, `projekt_id`, `muster_befund`, `moegliche_erklaerung`, and `unterstuetzende_quellen`. It also has a foreign key reference to the `projekte` table.
3. `p7_datenextraktion`: This table has columns for `id`, `treffer_id`, `land`, `stichprobe_kontext`, `phaenomen_intervention`, `outcome_ergebnis`, `hauptbefund`, and `anmerkung`. It also has a foreign key reference to the `p5_treffer` table.
4. `p7_synthese_methode`: This table has columns for `id`, `projekt_id`, `gewaehlt`, and `begruendung`. It also has a foreign key reference to the `projekte` table.

All of these tables are defined using the Laravel migrations syntax, which allows for creating and modifying database schema in a structured and version-controlled way. The migrations also include foreign key references between the tables, which ensures data consistency and integrity.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
This is a Laravel database migration file that creates several tables related to the P8 process, which is used for project management. The tables are:

* `p8_suchprotokoll`: This table stores information about the search protocols used in the P8 process. It has columns for the ID, the ID of the associated suchstring, the database name, the search date, the database version, the final search string, the set filters, and the total and unique hits.
* `p8_limitationen`: This table stores information about the limitations of a project in the P8 process. It has columns for the ID, the ID of the associated project, the limitation type, the description, the impact on completeness, and the foreign key to the project table.
* `p8_reproduzierbarkeitspruefung`: This table stores information about the reproducibility checks in the P8 process. It has columns for the ID, the ID of the associated project, the check point, whether it is fulfilled or not, an annotation, and the foreign key to the project table.
* `p8_update_plan`: This table stores information about the update plan for a project in the P8 process. It has columns for the ID, the ID of the associated project, the type of update, the interval, the responsible person, the tool, and the next update date.

The migration file also includes foreign key constraints to ensure referential integrity between tables.

Overall, this migration file creates the necessary tables and relationships for the P8 process, allowing for efficient data storage and retrieval of information related to project management.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
This is a PHP file that defines a migration for a Laravel application. The migration creates several indexes on tables in the database to improve query performance.

The file uses the `Illuminate\Database\Migrations\Migration` class, which provides a mechanism for defining database migrations in Laravel. The `up()` method is called when the migration is run in the "up" direction, and the `down()` method is called when the migration is rolled back in the "down" direction.

In this case, the `up()` method creates several indexes on tables in the database:

* An index on the `phasen` table to improve query performance for filtering by project ID.
* An index on the `p1_komponenten` table to improve query performance for filtering by project ID.
* An index on the `p5_treffer` table to improve query performance for filtering by project ID, record ID, and duplicate status.
* An index on the `p5_screening_entscheidungen` table to improve query performance for filtering by treffer ID.
* An index on the `p6_qualitaetsbewertung` table to improve query performance for filtering by treffer ID.
* An index on the `p7_datenextraktion` table to improve query performance for filtering by treffer ID.
* Full-text search (German) indices on the `abstract` and `titel` columns of the `p5_treffer` table to improve query performance for full-text searches.

The `down()` method is used to roll back the migration in case of any issues during the "up" direction, and it drops all of the indexes that were created by the `up()` method.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
The file `database/migrations/2026_03_31_100011_create_prisma_function.php` contains a Laravel database migration that creates a new PostgreSQL function called `berechne_prisma_zahlen(UUID)` for a specific project in the PRISMA-2020 research project.

The function takes one parameter, which is the UUID of the project, and it returns several columns with information about the identified hits in the project:

* `identifiziert_gesamt`: The total number of hits identified for the project.
* `duplikate`: The number of duplicated hits in the project.
* `nach_deduplizierung`: The number of hits after dedupe for the project.
* `ausgeschlossen_l1`: The number of hits that have been excluded from the L1-title/abstract screening stage.
* `volltext_geprueft`: The number of hits that have been checked in the L2-fulltext screening stage.
* `ausgeschlossen_l2`: The number of hits that have been excluded from the L2-fulltext screening stage.
* `eingeschlossen_final`: The number of hits that have been included in the final screening stage.

The function uses several subqueries to filter the information based on the data in the `p5_treffer` and `p5_screening_entscheidungen` tables, which are related to the PRISMA-2020 research project. The function is written in SQL language and is executed directly on the database, allowing for efficient processing of large amounts of data.

The migration file also includes two methods: `up()` and `down()`, which are used to create and drop the function respectively. The `down()` method is not implemented in this case, as the function is not dependent on any specific table or schema, but rather exists solely for its intended purpose of calculating PRISMA-2020 kennzahlen for a specific project.

In summary, the file creates a new PostgreSQL function `berechne_prisma_zahlen(UUID)` that allows for efficient calculation of PRISMA-2020 kennzahlen for a specific research project.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
This file defines a migration for the Laravel framework. It creates a new table called `chat_messages` in the database, with columns for an ID (UUID), user ID (foreign key to the `users` table), role (string, limited to 'user' or 'assistant'), content (text), and created at (timestamp). The migration also adds a check constraint on the role column to ensure that it is either 'user' or 'assistant'.

The file imports several classes from the Laravel framework: `Migration`, `Blueprint`, `DB`, and `Schema`. It also uses the `return` keyword to return an instance of the `class extends Migration` class.

This migration is designed to be run using the Laravel Artisan command line tool, which will execute the `up()` method of the class to create the table in the database. If the driver name of the database is not 'pgsql', the migration will simply return without doing anything.

The file does not include any external dependencies or APIs that need to be called. It only depends on the Laravel framework and its built-in functionality for creating and modifying tables in the database.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
This file is a migration for creating the `webhooks` table in a PostgreSQL database. It uses Laravel's built-in migration system and defines two functions: `up()` and `down()`. The `up()` function creates the `webhooks` table with columns for an ID, user ID, name, slug, URL, password, and created at timestamp. It also adds a foreign key constraint to the `user_id` column, which references the primary key of the `users` table.

The `down()` function is used to remove the `webhooks` table when rolling back the migration. It simply drops the table.

This migration file defines two important classes:

* The `Migration` class from the `Illuminate\Database\Migrations\Migration` namespace, which provides a base class for defining migrations.
* The `Blueprint` class from the `Illuminate\Database\Schema\Blueprint` namespace, which is used to define the structure of the table being created.

The file also uses Laravel's built-in support for PostgreSQL and defines two external dependencies:

* The `DB` facade, which provides a simple way to interact with the database.
* The `Schema` facade, which is used to define the structure of the table being created.

Overall, this migration file creates a new `webhooks` table in the PostgreSQL database and defines the necessary columns for storing information about webhooks.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
This file is a migration for the Laravel framework, specifically for creating a new table called `paper_embeddings` in the database. The table will have columns for an ID, a project ID (which is nullable), a source column, a paper ID column, a title column, a chunk index column, a text chunk column, and a metadata column (which can be null). Additionally, it will also have a timestamp indicating when the record was created.

The migration code uses the `Illuminate\Database\Migrations\Migration` class to create the table, and it makes use of several Laravel-specific features such as the `useCurrent()` method for setting the default value of the `erstellt_am` column to the current timestamp, and the `foreign()` and `references()` methods for creating foreign key constraints between this table and other tables in the database.

The migration also makes use of the `CREATE EXTENSION IF NOT EXISTS vector` statement to create a new extension called "vector" if it does not already exist, which is required for creating a column of type "vector". This extension is specific to PostgreSQL databases and allows for the creation of columns with data types that are optimized for vector operations.

Finally, the migration creates an index on the `embedding` column using the `CREATE INDEX` statement, which can improve the performance of queries that involve filtering or sorting based on the contents of the embedding column. The `WITH (lists = 100)` clause is used to specify the size of the index, in this case set to 100.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
This is a migration file for creating an `activity_log` table in a Laravel application. The table will have the following columns:

* `id`: An incremental ID that serves as a primary key for the table.
* `log_name`: A string column that can store the name of the log, which can be used to filter the logs based on their type. This column is nullable and indexed.
* `description`: A text column that can store a brief description of the log entry.
* `subject_id`: A string column that can store the ID of the subject (e.g., a user) associated with the log entry. This column supports both UUIDs (for Recherche models) and integers (for User PKs). It is nullable and indexed.
* `subject_type`: A string column that can store the type of the subject (e.g., "User" or "Recherche"). It is nullable and indexed.
* `event`: A string column that can store the name of the event (e.g., "created", "updated", etc.) associated with the log entry. This column is nullable.
* `causer_id`: A string column that can store the ID of the causer (e.g., a user) associated with the log entry. This column supports both UUIDs (for Recherche models) and integers (for User PKs). It is nullable.
* `causer_type`: A string column that can store the type of the causer (e.g., "User" or "Recherche"). It is nullable.
* `attribute_changes`: A JSON column that can store any changes made to the subject associated with the log entry. This column is nullable.
* `properties`: A JSON column that can store any additional properties associated with the log entry. This column is nullable.
* `created_at` and `updated_at`: Timestamp columns that track when the log entry was created and last updated, respectively.

The migration file creates the `activity_log` table if it does not already exist, and it also provides methods for dropping the table if necessary.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
The file `database/migrations/2026_04_01_100000_add_status_to_users_table.php` is a migration class for Laravel that adds a new column `status` to the `users` table in the database. The `status` column will have an enum type with four possible values: `trial`, `active`, `suspended`, and `cancelled`.

The migration is written using the Laravel Migration Schema Builder, which provides a convenient way to create and modify database tables. In this case, the migration creates a new column `status` in the `users` table if it does not already exist, and sets its default value to `trial`. It also specifies that the `status` column should be of type `enum`, which is a PostgreSQL-specific data type for representing enumerated values.

The migration is designed to work with both PostgreSQL and SQLite databases, but it uses different approaches for each database type. For PostgreSQL, it creates a new enum type called `user_status` and then alters the `users` table to add a new column `status` of that type. It also sets the default value of the `status` column to `'trial'`.

For SQLite, the migration simply adds a new column `status` to the `users` table if it does not already exist, and sets its default value to `trial`.

Overall, this migration is designed to add a new column `status` to the `users` table in the database, with an enum type for the values of the column. The migration is written in such a way that it can work with both PostgreSQL and SQLite databases, but it uses different approaches for each database type.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
This migration file is part of a Laravel project and adds a new type, `webhook_frontend_object`, to the PostgreSQL database. The file uses Laravel's built-in DB facade to execute SQL statements against the database.

The `up()` method first checks if the driver being used by the application is PostgreSQL, and if so, it creates a new type called `webhook_frontend_object` using the `CREATE TYPE` statement. The `EXCEPTION WHEN duplicate_object THEN NULL;` clause in the statement is used to handle the case where the type already exists in the database, which would result in an error if not caught.

The `down()` method is similar, but it drops the type and any constraints that may have been created during the `up()` method.

This migration file adds a new column to the `webhooks` table called `frontend_object`, which has the type `webhook_frontend_object`. The column allows for storing a specific object from the frontend, such as a chat or a search query, in the webhook data.

The migration also adds a unique constraint to the `webhooks` table that ensures there are no duplicate values for the combination of user ID and frontend object in the database.

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
This file is a migration for the Laravel framework, specifically for PostgreSQL database. It contains two methods: `up` and `down`, which are used to migrate the database schema and roll back any changes made by the `up` method.

In the `up` method, we can see that the column `password` in the `webhooks` table is being altered to make it nullable. The reason for this is that the password field is not required for incoming webhooks, but it is for outgoing ones. The `ALTER TABLE` statement is used to modify the schema of the `webhooks` table.

In the `down` method, we can see that the column `password` in the `webhooks` table is being altered again to make it not nullable. This is done by setting the default value of the password field to an empty string. The `ALTER TABLE` statement is also used to modify the schema of the `webhooks` table.

The file also imports the `Illuminate\Database\Migrations\Migration` class, which provides a way to perform database migrations in Laravel. It also uses the `DB::getDriverName()` method to check if the current driver is PostgreSQL, and if not, it will return without making any changes to the schema.

Overall, this migration makes the `password` field in the `webhooks` table nullable for outgoing webhooks, while keeping it required for incoming webhooks. The purpose of this migration is to optimize the database schema by reducing the amount of data that needs to be stored.

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
This migration file adds a `secret` column to the `webhooks` table if it does not already exist, using the `IF NOT EXISTS` clause. The column is of type `TEXT` and allows for `NULL` values. This migration is specifically designed for use with PostgreSQL databases, as it uses the `pgsql` driver provided by Laravel's DB facade.

The file consists of a single class that extends Laravel's Migration class. The `up()` method adds the `secret` column to the table if it does not already exist, while the `down()` method drops the column if it exists. Both methods are executed within the `if (DB::getDriverName() !== 'pgsql') { ... }` block, which checks whether the current database driver is PostgreSQL. This is necessary because some other databases may not support the `IF NOT EXISTS` clause.

The file also uses Laravel's Schema facade to perform the column addition and removal operations. The `statement()` method of the Schema facade is used to execute SQL queries, which are used to add or drop columns from the `webhooks` table.

---

### database/seeders/DatabaseSeeder.php
File: database/seeders/DatabaseSeeder.php
Category: data_access

This file contains a Seeder class that is used to seed the database with initial data. The `run` method of this class calls other seeders, including the `RoleSeeder`, and creates an admin user with the email "test@example.com" and password "password". It also assigns the role of "admin" to this user. Additionally, it calls the `RechercheDemoSeeder`.

This file is using Laravel's seeding functionality, which allows you to easily populate your database with initial data during development or deployment. The `Seeder` class in this file is extending the `Illuminate\Database\Seeder` class provided by Laravel.

Here are the key points about this file:

* It contains a Seeder class that is used to seed the database with initial data.
* The `run` method of this class calls other seeders, including the `RoleSeeder`.
* It creates an admin user with the email "test@example.com" and password "password".
* It assigns the role of "admin" to this user.
* It also calls the `RechercheDemoSeeder`.
* This file is using Laravel's seeding functionality, which allows you to easily populate your database with initial data during development or deployment.

---

### database/seeders/RechercheDemoSeeder.php
Diese Datei beinhaltet die Klasseneigenschaften und -methoden der RechercheDemoSeeder-Klasse. Sie enthält auch die Definition der Klassen, Funktionen und Endpunkte, mit denen die Suche in verschiedenen Datenquellen durchgeführt wird.

---

### database/seeders/RoleSeeder.php
This file is a seeder for the `Role` model in Laravel, specifically for the `spatie/laravel-permission` package. The seeder class `RoleSeeder` extends the `Seeder` class from Laravel and contains two methods: `run`, which creates an admin role and a user role with the names "admin" and "user", respectively, and the `__construct` method, which is used to initialize the database connection.

The seeder uses the `firstOrCreate` method of the `Role` model to create the roles if they do not already exist in the database. The `guard_name` parameter is set to `'web'` for both roles, indicating that these are web-based roles and not API roles.

---

### database/sql/create_langdock_agent_user.sql
This SQL script creates a new PostgreSQL user named `langdock_agent`. The user is created with login privileges and has a password set to "CHANGE_ME_IN_ENV".

The script grants the following permissions to the `langdock_agent` user:

* Connectivity to the database (the user can connect to the application database).
* Usage of the schema public.
* Read-only access on certain tables in the application database, including users, migrations, chat_messages, webhooks, and some other tables needed for MCP health check.
* Full CRUD access to all Recherche tables.
* Permission to call the PRISMA helper function `berechne_prisma_zahlen`.
* Deny access to sensitive tables, including password_reset_tokens, sessions, cache, cache_locks, jobs, job_batches, failed_jobs, permissions, roles, model_has_permissions, model_has_roles, role_has_permissions, consents, contacts, and page_views.

Note that the script assumes that the user has superuser or DB owner privileges to run correctly.

---

## temp_dummy

### .github/pull_request_template.md
This file is a pull request template for the .github repository. It provides guidance on how to structure and write a pull request.

---

## tests

### tests/Pest.php
The given file is a PestPHP test case file, which contains various declarations and definitions related to testing. Here's a summary of the main functions and classes in this file:

1. `pest()->extend(Tests\TestCase::class)`: This line sets up the default test case class for Pest PHP tests. It is equivalent to `use Tests\TestCase;` in a standard PHPUnit test file.
2. `->use(Illuminate\Foundation\Testing\RefreshDatabase::class)`: This line includes the `Illuminate\Foundation\Testing\RefreshDatabase` trait in the default test case class. The `RefreshDatabase` trait provides helpful methods for testing database interactions, such as `refreshDatabase()` and `seed()`.
3. `->in('Feature')`: This line specifies that the tests in this file are related to features, which is a way of organizing tests in Pest PHP.
4. `expect()->extend('toBeOne', function () { return $this->toBe(1); })`: This line defines an extension for the `expect()` function called "toBeOne". It simply checks that a value is equal to 1.
5. `function something()`: This line defines a global function called "something()". Global functions are not directly related to testing, but they can be useful in reducing code duplication in test files.

Overall, this file is a good example of how to set up and organize tests using Pest PHP. It includes several key components that make it easy to write and run tests for a Laravel application.

---

### tests/TestCase.php
This is a PHP file that defines an abstract test case class for testing Laravel applications. The class is defined in the `Tests` namespace and extends the `BaseTestCase` class from the Laravel framework.

The file does not contain any code or functionality, but it serves as a template for creating new test cases. It imports the `Illuminate\Foundation\Testing\TestCase` class, which provides a set of helpful methods for testing Laravel applications.

There are no external dependencies in this file, as it only defines an abstract class and does not contain any code or functionality.

---

### tests/Feature/ContactTest.php
This file contains a set of test cases for the `Contact` model in a Laravel application. The tests are written using PHPUnit, and they cover various aspects of the contact form submission process, including validation, database interactions, and error handling.

The file consists of multiple test functions, each of which defines a specific scenario or behavior that should be tested. For example, the first test function `test('contact form accepts valid submission')` tests the happy path for submitting a valid contact form, while the second test function `test('contact form works without optional fields')` tests whether the contact form can handle missing values for optional fields.

The file also includes test cases for handling invalid input data, such as missing or duplicate entries, and ensuring that the form submission process is secure and follows best practices.

Overall, this file provides a comprehensive set of tests that can help ensure the functionality and robustness of the contact form in the Laravel application.

---

### tests/Feature/DashboardTest.php
Diese Datei enthält PHP-Tests für die Dashboard-Seite. Die Tests sind als Funktionen definiert und führen zu einer Redirect-Anfrage bei unbefugten Benutzern zur Login-Seite und zu einer Status-200-Antwort bei authentifizierten Benutzern.

---

### tests/Feature/ExampleTest.php
Die Datei `tests/Feature/ExampleTest.php` enthält einen Testfall für eine GET-Anfrage auf die Route `home`. Die Testklasse wird mithilfe des Namespaces `test` importiert und die Methode `get()` wird verwendet, um eine Anfrage auf den Endpunkt `route('home')` zu senden.

Die Methode `assertStatus()` wird verwendet, um zu überprüfen, ob der Statuscode der Antwort ein 200 ist, was bedeutet, dass die Anfrage erfolgreich war und ein gültiger Response-Objekt zurückgegeben wurde.

Die Datei enthält keine externe Abhängigkeiten oder Imports, da sie eine einfache Testklasse ist, die nur für die Überprüfung der Statuscodes verwendet wird.

---

### tests/Feature/LegalPagesTest.php
This file is a set of tests for the legal pages (impressum, dsgvo, and agb) in a Laravel application. The tests are written using PHPUnit and are contained within a `Feature` directory within the `tests` directory. Each test method is defined as a separate function, with the test name being used as the function name.

The first test method, `impressum`, retrieves the impressum page using the `get()` method provided by Laravel's testing framework. The test then asserts that the response has a status code of 200 (OK), and that the page contains certain text.

Similarly, the second test method, `dsgvo`, retrieves the dsgvo page using the `get()` method provided by Laravel's testing framework. The test then asserts that the response has a status code of 200 (OK), and that the page contains certain text.

The third test method, `agb`, retrieves the agb page using the `get()` method provided by Laravel's testing framework. The test then asserts that the response has a status code of 200 (OK), and that the page contains certain text.

---

### tests/Feature/ProjektPolicyTest.php
This file contains tests for the `ProjektPolicy` class, which is responsible for determining whether a given user can perform certain actions on a project. The file defines several test cases that cover different scenarios related to project ownership and access control.

Here's a breakdown of the code:

* `use App\Models\Recherche\Projekt;` imports the `Projekt` model, which is used in the tests.
* `use App\Models\User;` imports the `User` model, which is used to create test users.
* `use App\Policies\ProjektPolicy;` imports the `ProjektPolicy` class, which is used in the tests.
* The first four test cases verify that a user can view, update, delete, and create projects, respectively. These tests use the `view`, `update`, `delete`, and `create` methods of the `ProjektPolicy` class to determine whether a given user has permission for each action.
* The fifth test case verifies that any authenticated user can create a project by calling the `create` method of the `ProjektPolicy` class with an authenticated user as its parameter.

Overall, this file contains tests for the authorization logic implemented in the `ProjektPolicy` class, which is responsible for enforcing access control and ownership-based permissions on projects.

---

### tests/Feature/RechercheTest.php
The file "tests/Feature/RechercheTest.php" contains unit tests for the Recherche feature in a web application. The tests cover various scenarios related to creating and managing research projects, such as authenticating users, checking if the input field is required, and ensuring that only the owner of the project can view it.

The file imports several classes and functions from Laravel, including `User`, `Projekt`, `Queue`, `Volt`, `TriggerLangdockAgent`, and `Route`. It also uses the `test` method provided by the `TestCase` class to write unit tests.

Here is a summary of the functionalities covered in this file:

1. Authenticated users can create research projects (tested in "authenticated user can create a research project" test).
2. Research project creation dispatches a TriggerLangdockAgent job (tested in "research project creation dispatches TriggerLangdockAgent job" test).
3. The input field for creating research projects is required (tested in "research input requires eingabe field" test).
4. Only the owner of a research project can view it (tested in "user can view own project detail" test).
5. Users cannot view another user's research project (tested in "user cannot view another users project" test).

Overall, these tests ensure that the Recherche feature is working correctly and provides a good basis for testing future changes to the feature.

---

### tests/Feature/WebhookTest.php
This test file contains five test cases for a webhook that is used to receive data from the Langdock service. The tests cover different scenarios, such as:

1. Testing whether the webhook rejects requests without a valid signature or timestamp.
2. Testing whether the webhook rejects requests with an invalid signature or timestamp.
3. Testing whether the webhook accepts requests with a valid signature and timestamp.
4. Testing whether the webhook sends the received data to the queue for further processing.
5. Testing whether the webhook returns a success status code (200) when the request is accepted.

The tests use the `tests/Feature` directory as their location, which is where functional tests are typically stored in Laravel projects. The `Illuminate\Support\Facades\Config` and `Illuminate\Support\Facades\Queue` facades are imported at the top of the file, indicating that these classes are being used in the tests.

The `tests/Feature` directory is also where the test case files for the application's features are stored. In this case, the test case files are located under the `tests/Feature/WebhookTest.php` file.

---

### tests/Feature/WelcomeTest.php
Die Datei `tests/Feature/WelcomeTest.php` enthält eine Reihe von Tests zur Überprüfung der Funktionen der Website auf der Startseite.

* Der erste Test (`welcome page returns successful response`) überprüft, ob die Startseite mit einer erfolgreichen Antwort (Status 200) zurückgegeben wird.
* Der zweite Test (`welcome page increments visit counter`) überprüft, ob jede Anfrage an die Startseite eine Erhöhung der Besucherzahl in der Datenbank hervorruft.
* Der dritte Test (`welcome page creates page_view record if none exists`) überprüft, ob eine neue Seiteansicht mit einer Besucherzahl von 1 erstellt wird, wenn keine vorhanden ist.
* Der vierte Test (`guest sees login and register links`) überprüft, ob für Gäste (nicht angemeldete Benutzer) die Links zum Anmelden und Registrieren auf der Startseite sichtbar sind.
* Der fünfte Test (`authenticated user sees dashboard link`) überprüft, ob für angemeldete Benutzer (nicht Gäste) der Link zum Dashboard auf der Startseite sichtbar ist.

Die Datei importiert die Klassen `App\Models\PageView` und `App\Models\User`. Es gibt externe Abhängigkeiten von APIs wie `route('home')` für den Zugriff auf die Startseite und `actingAs($user)` für die Simulation des Benutzer-Logins.

---

### tests/Feature/Auth/AuthenticationTest.php
This file contains tests for the authentication functionality of a Laravel application. The file uses the PHPUnit testing framework and is located in the `tests/Feature/Auth` directory.

The test functions are as follows:

1. `login screen can be rendered`: This test verifies that the login screen can be rendered successfully. It does this by sending a GET request to the `/login` route and checking the response status code.
2. `users can authenticate using the login screen`: This test verifies that users can authenticate using the login screen. It creates a user, sends a POST request to the `/login/store` endpoint with the user's email and password, and checks that the response does not contain any errors and redirects to the dashboard.
3. `users can not authenticate with invalid password`: This test verifies that users cannot authenticate with an invalid password. It creates a user and sends a POST request to the `/login/store` endpoint with the user's email and an invalid password, and checks that the response contains errors related to the email field.
4. `users with two factor enabled are redirected to two factor challenge`: This test verifies that users with two-factor authentication enabled are redirected to the two-factor challenge screen when trying to log in. It creates a user with two-factor authentication enabled and sends a POST request to the `/login/store` endpoint with the user's email and password, and checks that the response redirects to the two-factor login page.
5. `users can logout`: This test verifies that users can log out successfully. It creates a user, logs them in using the `/login/store` endpoint, and sends a POST request to the `/logout` endpoint to log them out. The response is then checked to ensure it redirects to the home page.

The file also imports the `App\Models\User` model and the `Laravel\Fortify\Features` class, which are used in the tests to interact with the application's user authentication functionality.

---

### tests/Feature/Auth/EmailVerificationTest.php
The file `tests/Feature/Auth/EmailVerificationTest.php` contains four test cases for verifying an email address in a Laravel application. The tests use the Laravel testing framework and are located in the "tests" directory under the "Feature" category.

The first test case, "email verification screen can be rendered", creates an unverified user and then attempts to render the email verification notice page. The test checks that the response has a status code of 200 and verifies that the page is displayed correctly.

The second test case, "email can be verified", creates an unverified user and generates a temporary signed URL for the verification process. The test then simulates the verification process by visiting the URL, which should trigger the `Verified` event to be fired. The test checks that the `Verified` event has been dispatched and that the user's email address is marked as verified.

The third test case, "email is not verified with invalid hash", creates an unverified user and generates a temporary signed URL with an invalid hash. The test then simulates the verification process by visiting the URL, which should not trigger the `Verified` event. The test checks that the user's email address is still marked as unverified.

The fourth test case, "already verified user visiting verification link is redirected without firing event again", creates a verified user and generates a temporary signed URL for the verification process. The test then simulates the verification process by visiting the URL, which should not trigger the `Verified` event. The test checks that the user's email address remains marked as verified and that the `Verified` event has not been dispatched again.

Overall, these tests cover various scenarios for verifying an email address in a Laravel application, including successful verification, invalid hashes, and already-verified users.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
* The file contains a test for the password confirmation screen, which is a feature of the application.
* It uses the `User` model to create a user and then fetches the confirmation screen using the `actingAs` method provided by Laravel's testing framework.
* The test asserts that the response status is 200, indicating that the screen was successfully rendered.
* No external dependencies are mentioned in this file.

---

### tests/Feature/Auth/PasswordResetTest.php
This file contains a set of feature tests for the password reset functionality in a Laravel application. The tests are written using PHPUnit, and they cover various aspects of the password reset process, including rendering the password reset link screen, requesting a password reset link, rendering the password reset screen, and resetting the password with a valid token.

Here's a summary of the most important classes, functions, and endpoints in this file:

* `App\Models\User`: This is the model class for the user entity, which represents a user account in the application.
* `Illuminate\Auth\Notifications\ResetPassword`: This is a notification class that is used to send password reset links to users.
* `Notification::fake()`: This function is used to disable the actual sending of notifications during testing. It's important for this test file because we don't want to actually send password reset emails during testing.
* `route('password.request')`: This is a route helper function that generates a URL for the password reset request screen.
* `route('password.reset', $notification->token)`: This is another route helper function that generates a URL for the password reset screen, given a valid token.
* `route('password.update')`: This is a route helper function that generates a URL for updating the user's password after they have received a password reset link.

---

### tests/Feature/Auth/RegistrationTest.php
This file contains two tests for the registration process. The first test checks that the registration screen can be rendered, while the second test checks that new users can register and are authenticated.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
This file contains two test cases for the `TwoFactorChallenge` feature in Laravel Fortify. The first test case checks that when a user is not authenticated, they are redirected to the login page. The second test case verifies that the two-factor authentication challenge can be rendered correctly.

The file imports the `Features` class from the `Laravel\Fortify` namespace and uses it to check whether two-factor authentication is enabled or not. It also imports the `User` model from the `App\Models` namespace for testing purposes.

---

### tests/Feature/Chat/BigResearchChatTest.php
This file contains tests for a Livewire component that allows users to interact with a chat feature. The component is called `chat.big-research-chat` and it uses the `ChatMessage` model to store and retrieve chat messages. The test file includes various scenarios to ensure that the component behaves as expected, such as:

* Testing the send message functionality
* Testing the validation of the message input field
* Testing the scenario where no webhook is set up for the user
* Testing the scenario where there is a HTTP error during message sending
* Testing the scenario where there is a connection exception during message sending
* Testing the clear history functionality to ensure that only the current user's messages are cleared
* Testing the get chat messages functionality to ensure that the messages are returned in chronological order
* Testing the scenario where no webhook is set up for the user and therefore no messages are displayed.

---

### tests/Feature/Recherche/PhaseP1Test.php
Die Datei enthält Tests für die Recherche-Phase P1 von Livewire. Sie testet die Funktionen zur Erstellung und Bearbeitung von Strukturmodellwahl, Komponente und Kriterium.

---

### tests/Feature/Recherche/PhaseP2Test.php
**Die Datei `tests/Feature/Recherche/PhaseP2Test.php` enthält Tests für die Funktionalität der Recherche-Phase P2 in einem Projekt-Management-System.**

* Die wichtigsten Klassen und Funktionen in dieser Datei sind:
	+ `App\Models\Recherche\{Projekt, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, P2Trefferliste}`: Diese Klassen sind Modelle für die Datenbanktabellen "Projekt", "P2ReviewTypEntscheidung", "P2Cluster", "P2MappingSuchstringKomponente" und "P2Trefferliste".
	+ `App\Models\User`: Die Klasse "User" ist ein Modell für die Datenbanktabelle "Users".
	+ `Livewire\Volt\Volt`: Das ist eine Klasse, die das Volt-Framework von Livewire verwendet.
* Die wichtigsten Tests in dieser Datei sind:
	+ `P2: kann review-typ erstellen`: Dieser Test erstellt einen neuen "ReviewTypEntscheidung" und überprüft, ob der Review-Typ erfolgreich gespeichert wurde.
	+ `P2: review-typ validiert review_typ als pflichtfeld`: Dieser Test überprüft, ob das Feld "review_typ" als Pflichtfeld markiert wird, wenn es nicht ausgefüllt wird.
	+ `P2: kann review-typ bearbeiten`: Dieser Test bearbeitet einen vorhandenen "ReviewTypEntscheidung" und überprüft, ob die Änderungen erfolgreich gespeichert wurden.
	+ `P2: kann review-typ löschen`: Dieser Test löscht einen vorhandenen "ReviewTypEntscheidung" und überprüft, ob der Eintrag erfolgreich gelöscht wurde.
	+ `P2: kann cluster erstellen`: Dieser Test erstellt einen neuen "Cluster" und überprüft, ob der Cluster erfolgreich gespeichert wurde.
	+ `P2: cluster validiert label als pflichtfeld`: Dieser Test überprüft, ob das Feld "label" als Pflichtfeld markiert wird, wenn es nicht ausgefüllt wird.
	+ `P2: kann cluster bearbeiten`: Dieser Test bearbeitet einen vorhandenen "Cluster" und überprüft, ob die Änderungen erfolgreich gespeichert wurden.
	+ `P2: kann cluster löschen`: Dieser Test löscht einen vorhandenen "Cluster" und überprüft, ob der Eintrag erfolgreich gelöscht wurde.
	+ `P2: kann mapping erstellen`: Dieser Test erstellt einen neuen "MappingSuchstringKomponente" und überprüft, ob die Zuordnung erfolgreich gespeichert wurde.
	+ `P2: mapping suchbegriffe werden als array gespeichert`: Dieser Test überprüft, ob die Suchbegriffe als Array gespeichert werden, wenn sie als Komma-getrennte Liste angegeben werden.
	+ `P2: mapping validiert label als pflichtfeld`: Dieser Test überprüft, ob das Feld "label" als Pflichtfeld markiert wird, wenn es nicht ausgefüllt wird.
	+ `P2: kann mapping bearbeiten`: Dieser Test bearbeitet einen vorhandenen "MappingSuchstringKomponente" und überprüft, ob die Änderungen erfolgreich gespeichert wurden.
	+ `P2: kann mapping löschen`: Dieser Test löscht einen vorhandenen "MappingSuchstringKomponente" und überprüft, ob der Eintrag erfolgreich gelöscht wurde.
	+ `P2: kann trefferliste erstellen`: Dieser Test erstellt eine neue "Trefferliste" und überprüft, ob die Trefferliste erfolgreich gespeichert wurde.
	+ `P2: kann trefferliste bearbeiten`: Dieser Test bearbeitet eine vorhandene "Trefferliste" und überprüft, ob die Änderungen erfolgreich gespeichert wurden.
	+ `P2: kann trefferliste löschen`: Dieser Test löscht eine vorhandene "Trefferliste" und überprüft, ob der Eintrag erfolgreich gelöscht wurde.

---

### tests/Feature/Recherche/PhaseP3Test.php
This file contains tests for the Recherche/PhaseP3Test.php file, which defines the functionality for the Phase P3 of a research project in the "Recherche" section of the DigiPrototype platform.

---

### tests/Feature/Recherche/PhaseP4Test.php
Die Datei `tests/Feature/Recherche/PhaseP4Test.php` enthält Tests für verschiedene Funktionen der Suche in Phase P4 des Projekthelm. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte:

* `App\Models\Recherche\{Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll}`: Modelle für Suche, Thesaurus-Mapping und Anpassungsprotokoll.
* `App\Models\User`: Modell für Benutzer.
* `Livewire\Volt\Volt`: Klasse für die Verwendung von Livewire-Komponenten in Tests.
* `recherche.phase-p4`: Endpunkt der Suche in Phase P4.
* `newSs`, `saveSs`, `editSs`, `deleteSs`: Funktionen zur Verwaltung von Suchstrings.
* `thFreitextDe`, `mesh_term`, `emtree`: Eigenschaften des Thesaurus-Mapping-Modells.
* `newTh`, `saveTh`, `editTh`, `deleteTh`: Funktionen zur Verwaltung von Thesaurus-Mapping.
* `newAp`, `saveAp`: Funktionen zur Verwaltung von Anpassungsprotokollen.

Diese Tests überprüfen die Funktionalität der Suche in Phase P4, einschließlich der Validierung von Suchstrings und des Thesaurus-Mapping, der Verarbeitung von Anpassungsprotokoll und der Beziehung zwischen den Modellen.

---

### tests/Feature/Recherche/PhaseP5Test.php
Die Datei `tests/Feature/Recherche/PhaseP5Test.php` enthält Unit-Tests für die Phase P5 der Recherche im Projektmanagement-Tool "Budget Tracker". Die Tests erstellen, bearbeiten und löschen verschiedene Objekte des Models `P5PrismaZahlen`, `P5ScreeningKriterium`, `P5ToolEntscheidung` und `P5ScreeningEntscheidung`.

Die wichtigsten Klassen, Funktionen und Endpunkte in dieser Datei sind:

* `App\Models\Recherche`: Die Modelle, die für die Phase P5 verwendet werden.
* `App\Models\User`: Der Benutzer-Modell, der für die Authentifizierung und Autorisierung von Nutzern verwendet wird.
* `Livewire\Volt\Volt`: Der Livewire-Vollverwalter, der für die Anzeige von Komponenten und Formularen verwendet wird.
* `recherche.phase-p5`: Die Livewire-Komponente für die Phase P5 der Recherche.

Die Tests in dieser Datei verwenden die Laravel-Testing-Facade, um das Verhalten von Komponenten und Funktionen zu testen. Sie erstellen und bearbeiten verschiedene Objekte des Modells `P5PrismaZahlen`, `P5ScreeningKriterium`, `P5ToolEntscheidung` und `P5ScreeningEntscheidung`.

Die Tests validieren auch die Eingabe von Pflichtfeldern und überprüfen die Auswirkungen von Änderungen auf die Datenbank. Die Tests fragen auch nach externen Abhängigkeiten, wie z.B. der Authentifizierung und Autorisierung von Nutzern.

---

### tests/Feature/Recherche/PhaseP6Test.php
Diese Datei `tests/Feature/Recherche/PhaseP6Test.php` enthält PHPUnit-Tests für die Recherchephase P6 (Qualitätsbewertung und Lückenanalyse) einer Forschungsprojekt-Applikation. Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `App\Models\Recherche\{Projekt, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse}`: Modell-Klassen für die Recherchephase P6.
* `App\Models\User`: Model-Klasse für Benutzer.
* `Livewire\Volt\Volt`: Klasse für die Verarbeitung von Livewire-Aktionen und -Ereignissen in der Testumgebung.
* `P5Treffer`: Model-Klasse für Treffer (Ergebnis) einer Recherchephase P5 (Meta-Analyse).
* `P6Qualitaetsbewertung`: Model-Klasse für Qualitätsbewertung in Recherchephase P6.
* `P6Luckenanalyse`: Model-Klasse für Lückenanalyse in Recherchephase P6.

Die Tests in dieser Datei testen verschiedene Funktionalitäten und Eigenschaften der Anwendung, wie zum Beispiel das Erstellen und Bearbeiten von Qualitätsbewertungen und Lückenanalysen, die Validierung von Pflichtfeldern und die Authentifizierung von Benutzern.

Die wichtigsten Methoden in dieser Datei sind:

* `test('P6: kann qualitaetsbewertung erstellen', function () {...})`: Testet das Erstellen einer Qualitätsbewertung (P6Qualitaetsbewertung) durch den Benutzer.
* `test('P6: qualitaetsbewertung validiert pflichtfelder', function () {...})`: Testet die Validierung von Pflichtfeldern bei der Erstellung einer Qualitätsbewertung (P6Qualitaetsbewertung).
* `test('P6: kann qualitaetsbewertung bearbeiten', function () {...})`: Testet das Bearbeiten einer vorhandenen Qualitätsbewertung (P6Qualitaetsbewertung) durch den Benutzer.
* `test('P6: kann qualitaetsbewertung löschen', function () {...})`: Testet das Löschen einer vorhandenen Qualitätsbewertung (P6Qualitaetsbewertung) durch den Benutzer.
* `test('P6: kann lückenanalyse erstellen', function () {...})`: Testet das Erstellen einer Lückenanalyse (P6Luckenanalyse) durch den Benutzer.
* `test('P6: lückenanalyse validiert fehlender_aspekt', function () {...})`: Testet die Validierung des "Fehlenden Aspekts" bei der Erstellung einer Lückenanalyse (P6Luckenanalyse).
* `test('P6: kann lückenanalyse bearbeiten', function () {...})`: Testet das Bearbeiten einer vorhandenen Lückenanalyse (P6Luckenanalyse) durch den Benutzer.
* `test('P6: kann lückenanalyse löschen', function () {...})`: Testet das Löschen einer vorhandenen Lückenanalyse (P6Luckenanalyse) durch den Benutzer.
* `test('P6: fremder user bekommt 403', function () {...})`: Testet die Authentifizierung von Benutzern und erwartet einen HTTP-Statuscode 403 (Forbidden) für Benutzer, die nicht berechtigt sind, auf diese Funktionen zuzugreifen.

---

### tests/Feature/Recherche/PhaseP7Test.php
This file contains integration tests for the "Recherche" phase of a research project in a web application. The tests cover various functionality related to the synthesis method, data extraction, pattern and consistency analysis, and grade evaluation.

The file imports several classes and functions from the Laravel framework, including `App\Models\User`, `App\Models\Recherche\{Projekt, P5Treffer, P7SyntheseMethode, P7Datenextraktion, P7MusterKonsistenz, P7GradeEinschaetzung}`, and `Livewire\Volt\Volt`. It also uses the `assertDatabaseHas()` and `assertDatabaseMissing()` methods to verify that specific data has been saved or removed from the database.

The tests use the `actingAs()` method to simulate a user's authentication status, which is necessary for accessing certain routes in the application. The `Volt::test()` method allows us to create and interact with Livewire components within our test suite. We can call methods on these components using chainable syntax, such as `->call('saveSm')`.

The tests cover various scenarios related to the synthesis method, data extraction, pattern and consistency analysis, and grade evaluation. For example, we can create a new synthesis method, edit an existing one, or delete it altogether. Similarly, we can test the validation of required fields for these methods, as well as the creation, editing, and deletion of data extraction results, muster and consistency analysis results, and grade evaluation results.

The tests also cover authorization scenarios, such as ensuring that a user with the appropriate permissions can access certain routes in the application, while preventing unauthorized users from doing so. This is achieved through the use of the `actingAs()` method to simulate a user's authentication status and the `assertStatus()` method to verify that a given route is inaccessible or returns an error response.

---

### tests/Feature/Recherche/PhaseP8Test.php
Diese Datei enthält PHPUnit-Tests für eine Webanwendung, die das Erfassung von Suchprotokoll, Limitationen und Update-Plänen für ein Projekt namens "Recherche" ermöglicht. Die Klasse `P8Suchprotokoll`, `P8Limitation` und `P8UpdatePlan` sind Modelle, die in der Datenbank gespeichert werden und von den Tests verwendet werden.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `Projekt`: Das Modell "Projekt" wird im Konstruktor verwendet, um einen neuen Suchprotokoll-Eintrag zu erstellen.
* `P8Suchprotokoll`: Das Modell "Suchprotokoll" wird für die Erstellung und Bearbeitung von Suchprotokoll-Einträgen verwendet.
* `P8Limitation`: Das Modell "Limitation" wird für die Erstellung und Bearbeitung von Limitations-Einträgen verwendet.
* `P8UpdatePlan`: Das Modell "Update-Plan" wird für die Erstellung und Bearbeitung von Update-Plan-Einträgen verwendet.
* `User`: Das Modell "User" wird für den Zugriff auf das Projekt verwendet.
* `Tests\Feature\Recherche\PhaseP8Test`: Die Klasse enthält PHPUnit-Tests für die Funktionen des Suchprotokolls, Limitations und Update-Plans.

---

### tests/Feature/Recherche/ProjektDetailTest.php
Diese Datei enthält Tests für die `Recherche`-Komponente in der Laravel-Anwendung. Die Komponente ist verantwortlich für die Darstellung von Projektinformationen und ermöglicht es Nutzern, verschiedene Informationen über ein Projekt zu sehen oder zu bearbeiten.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `tests/Feature/Recherche/ProjektDetailTest.php`: Die Datei selbst, die die Tests für die `Recherche`-Komponente enthält.
* `App\Models\Recherche\Projekt`: Die Modell-Klasse für Projekte, die in der Datenbank gespeichert werden.
* `App\Models\User`: Die Modell-Klasse für Nutzer, die in der Datenbank gespeichert werden.
* `Livewire\Volt\Volt`: Die Volt-Komponente ist eine Komponente von Livewire, die verwendet wird, um Komponenten zu erstellen und zu rendern.

Die Datei importiert auch einige externe Abhängigkeiten, darunter:

* `User::factory()`: Eine Factory-Methode, die verwendet wird, um Nutzer in der Datenbank zu erstellen.
* `Projekt::factory()`: Eine Factory-Methode, die verwendet wird, um Projekte in der Datenbank zu erstellen.
* `assertStatus(403)`: Ein Assertion-Test, der verwendet wird, um zu überprüfen, ob eine bestimmte HTTP-Antwortcode (in diesem Fall 403) zurückgegeben wird.

---

### tests/Feature/Settings/AppearanceTest.php
This file contains tests for the "Appearance" feature of a Laravel application. The tests use the Livewire framework to test the functionality of the component.

The following are the important classes, functions, or endpoints mentioned in the file:

* `tests\Feature\Settings\AppearanceTest.php`: This is the test class that contains all the tests for the "Appearance" feature.
* `User`: This is a model class that represents the user entity. It is used to create a test user and log them in before running the tests.
* `Livewire\Volt\Volt`: This is a class that provides a simple way to write Livewire component tests using the Volta framework. The `test()` function is used to run the tests.
* `Illuminate\Foundation\Testing\RefreshDatabase`: This is a trait that provides helper methods for testing database operations in Laravel. It is used in the `beforeEach` method to refresh the database before each test.
* `route('appearance.edit')`: This is an endpoint that returns the "Appearance" page. The tests check if the user can access this page and if it renders correctly.
* `auth()->logout()`: This is a function that logs out the current user. It is used in one of the tests to ensure that only logged-in users can access the "Appearance" page.

---

### tests/Feature/Settings/PasswordUpdateTest.php
This file is a test for the `settings.password` Livewire component, which allows users to update their passwords. The test consists of two tests: one that checks that a user can successfully update their password with the correct current password, and another that checks that an error is thrown when the current password is incorrect.

The test imports several classes and facades:

* `App\Models\User`: A Laravel model for storing users in the database.
* `Illuminate\Support\Facades\Hash`: A Laravel facade for hashing passwords.
* `Livewire\Volt\Volt`: The Livewire test helper class.

The test first creates a user and logs them in using the `actingAs` method. Then, it uses the `test` method to call the `updatePassword` method on the `settings.password` Livewire component, passing in some sample data.

The first test checks that the password is updated successfully when the correct current password is provided. The test uses the `set` method to set the current password, new password, and confirmation password, and then calls the `updatePassword` method using the `call` method. The test then asserts that there are no errors in the response.

The second test checks that an error is thrown when the current password is incorrect. The test sets the current password to a wrong value and attempts to update the password again, expecting an error to be thrown. The test asserts that the `current_password` field has an error.

---

### tests/Feature/Settings/ProfileUpdateTest.php
Die Datei `tests/Feature/Settings/ProfileUpdateTest.php` enthält Tests für die Einstellungen-Seite des Benutzers auf der Plattform. Die Klasse `Volt::test()` wird verwendet, um die Komponenten und Funktionen der Tests zu testen.

Die Tests in dieser Datei führen folgende Aktionen aus:

1. Sie testen, ob die Profil-Seite korrekt angezeigt wird, wenn man sich als authentifizierter Benutzer anmeldet.
2. Sie testen, ob der Benutzer seine Profilinformationen erfolgreich aktualisieren kann, indem sie das Formular ausfüllen und die Funktion `updateProfileInformation` aufrufen.
3. Sie testen, ob der E-Mail-Verifizierungsstatus des Benutzers erhalten bleibt, wenn er seinen E-Mail-Adressen nicht geändert hat.
4. Sie testen, ob der Benutzer sein Konto erfolgreich löschen kann, indem sie das Formular ausfüllen und die Funktion `deleteUser` aufrufen.
5. Sie testen, ob ein korrektes Passwort benötigt wird, um das Benutzerkonto zu löschen.

Die Datei importiert die Klasse `User` aus der Modell-Datei `App\Models\User`, um den authentifizierten Benutzer zu erstellen und zu simulieren. Die Tests verwenden auch die Livewire-Komponente `Volt` für die Komponenten-Tests und die Funktion `assertHasNoErrors()` von Laravel, um sicherzustellen, dass keine Fehler in der Antwort zurückgegeben werden.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
This is a PHPUnit test file for testing the "Two Factor Authentication" feature in a Laravel application. The file contains four tests that verify different aspects of the two-factor authentication functionality:

1. Tests if the two-factor settings page can be rendered successfully and displays the expected content, such as the two-factor authentication status and any available recovery codes.
2. Tests if the two-factor settings page requires password confirmation when enabling two-factor authentication. This test is skipped if two-factor authentication is not enabled in the application's configuration.
3. Tests if the two-factor settings page returns a forbidden response if two-factor authentication is disabled.
4. Tests if two-factor authentication is disabled when the confirmation is abandoned between requests. This test uses a mock component to set the two-factor enabled state and verifies that the database is updated accordingly.

The file imports several classes, including `App\Models\User`, `Laravel\Fortify\Features`, `Livewire\Volt\Volt`, and `Illuminate\Database\Eloquent\Model`. The `beforeEach` function is used to set up the testing environment by creating a new user with two-factor authentication disabled, and the `test` functions are used to run individual tests. The `assertOk`, `assertSee`, `assertRedirect`, `assertForbidden`, and `assertDatabaseHas` methods are used in the tests to verify different aspects of the application's behavior.

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei `tests/Feature/Settings/WebhooksTest.php` enthält Unit-Tests für die Funktionen zur Verwaltung von Webhooks in der Anwendung. Hier sind einige wichtige Punkte:

* Die Datei importiert diverse Klassen und Hilfsfunktionen, darunter `Illuminate\Foundation\Testing\RefreshDatabase`, um Datenbanktests zu unterstützen.
* Jeder Test beginnt mit einer `beforeEach`-Funktion, die einen Benutzer erstellt und sich als diesem anmeldet.
* Es gibt verschiedene Tests für verschiedene Webhook-Endpunkte:
	+ Das erste Stück tests, das sich auf den Dashboard-Chat bezieht, testet die Funktion `saveDashboard`, um einen neuen oder bestehenden Webhook zu speichern. Es überprüft auch, ob der gespeicherte Webhook korrekt ist und ob das `dashboard-saved`-Ereignis ausgelöst wurde.
	+ Das zweite Stück tests, das sich auf die Recherche-Funktion bezieht, testet ähnlich wie das erste, aber speichert oder aktualisiert den Webhook für die Recherche-Startfunktion. Es überprüft auch, ob der gespeicherte Webhook korrekt ist und ob das `recherche-saved`-Ereignis ausgelöst wurde.
	+ Das dritte Stück tests, das sich auf den Mount-Vorgang bezieht, testet die Funktion `mount`, um bestehende Webhooks für den Benutzer zu laden und maskiert das Secret in der UI. Es überprüft auch, ob die gespeicherten Webhooks korrekt sind und ob das `dashboardHasSecret` und `rechercheHasSecret`-State-Objekte auf `true` gesetzt wurden.
* Es gibt auch einige Tests, die sich auf die Isolation des Benutzers in Bezug auf fremde Webhooks beschränken.

---

### tests/Unit/ExampleTest.php
The file contains a single unit test written in PHPUnit format, testing that the value 'true' is indeed true.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
This file contains a custom page for the ContactResource that lists all records. It uses Filament's ListRecords component to display the records, and extends it with additional features such as search and sorting.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
* This file is part of the Contact resource in the Filament administration panel.
* It extends the `Filament\Resources\Pages\ViewRecord` class and overrides the `$resource` property to specify the resource that should be displayed.
* The `ContactResource` class is a custom resource that represents a contact record in the administration panel, and it is defined in a separate file called `app/Filament/Resources/ContactResource.php`.
* This page allows users to view a specific contact record and its details, such as the name, email address, and phone number.
* The external dependencies include the `Filament\Resources` namespace and the `ContactResource` class itself.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Die Datei `app/Filament/Resources/UserResource/Pages/EditUser.php` enthält eine Klasse, die von der Klasse `Filament\Resources\Pages\EditRecord` erbt und für die Bearbeitung von Benutzerdaten in der Filament-Benutzerschnittstelle (UI) verwendet wird.

Die wichtigsten Funktionen der Datei sind:

* Die Definition einer Klasse namens `EditUser`, die sich von der Klasse `Filament\Resources\Pages\EditRecord` ableitet.
* Die Verwendung des Filament-Ressourcen-Managers, um auf das Benutzer-Ressourcenobjekt zuzugreifen.
* Die Definition einer Konstante `$resource`, die den Typ der zu bearbeitenden Ressource definiert.

Es gibt keine externe Abhängigkeit in dieser Datei, da sie nicht auf eine andere Klasse oder Bibliothek verweist.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
This file is a PHP class that represents a page for listing users in the Filament administration interface. It extends the `Filament\Resources\Pages\ListRecords` class and specifies the `UserResource` as the resource to be listed. The class does not have any external dependencies, but it requires the `App\Filament\Resources\UserResource` class to be available in order to work properly.

---

### resources/views/dashboard.blade.php
This is the dashboard page for a researcher, showing a list of recent projects and a chat component. It uses Livewire to manage the chat functionality.

---

### resources/views/welcome.blade.php
Hier sind die wichtigsten Klassen und Funktionen der Datei "welcome.blade.php":

1. **HTML**: Die Datei enthält den HTML-Kode, der die grundlegenden Strukturen für eine Webseite definiert. Hier sind einige wichtige Elemente:
	* `<!DOCTYPE html>`: Das Dokumenttypdeklaration (Doctype) ist das erste Element in einer HTML-Datei. Es gibt einen Standard und mehrere Versionen, aber bei der Verwendung von Laravel ist es nicht erforderlich.
	* `<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">`: Hier wird die Spracheinstellung für das Dokument definiert. Der Wert "app()->getLocale()" stammt aus dem App-Objekt und enthält die aktuelle Lokalisierung.
	* `<head>...</head>`: Das Head-Element definiert den Kopfbereich einer HTML-Datei. Hier sind einige wichtige Elemente:
		+ `<meta charset="utf-8">`: Dieses Metadatenelement definiert die Zeichenkodierung der Seite.
		+ `<title>Linn Games · Interactive Games, KI-Lösungen & Web-Apps</title>`: Hier wird der Titel der Seite definiert.
		+ `<meta name="viewport" content="width=device-width, initial-scale=1">`: Dieses Metadatenelement definiert die Darstellung von Inhalten auf verschiedenen Geräten.
		+ `<meta name="csrf-token" content="{{ csrf_token() }}">`: Hier wird ein Token zur CSRF-Schutzmechanik festgelegt, das für die Übermittlung von Formulardaten verwendet wird.
2. **CSS**: Die Datei enthält einige CSS-Regeln, die die Darstellung der Seite definieren. Hier sind einige wichtige Regeln:
	* `body {...}`: Hier werden die Eigenschaften des Body-Elements definiert, das die größte Element auf einer HTML-Seite ist.
	* `.page-wrap {...}`: Hier werden die Eigenschaften des Elements ".page-wrap" definiert, das den Inhalt der Seite darstellt.
3. **JavaScript**: Die Datei enthält einige JavaScript-Skripte, die die Funktionalität auf der Seite anpassen. Hier sind einige wichtige Skripte:
	* `window.addEventListener('load', function() {...}`: Hier wird ein Event-Listener für das "load"-Ereignis definiert, das ausgelöst wird, wenn alle Ressourcen auf einer Webseite geladen wurden.
	* `document.querySelectorAll('.cursor-glow').forEach(function(el) {...}`: Hier wird ein Event-Listener für das "mouseover"-Ereignis definiert, das ausgelöst wird, wenn der Mauszeiger über ein Element mit der Klasse ".cursor-glow" positioniert wird.
	* `document.querySelectorAll('.nav-links li').forEach(function(el) {...}`: Hier wird ein Event-Listener für das "click"-Ereignis definiert, das ausgelöst wird, wenn der Benutzer auf ein Element mit der Klasse ".nav-links li" klickt.
4. **Laravel**: Die Datei enthält einige Laravel-spezifische Regeln und Funktionen, die die Funktionalität auf der Seite anpassen. Hier sind einige wichtige Regeln und Funktionen:
	* `{{ str_replace('_', '-', app()->getLocale()) }}`: Hier wird die aktuelle Lokalisierung für die Seite festgelegt, indem die Unterstriche durch Bindestriche ersetzt werden.
	* `{{ csrf_field() }}`: Hier wird ein Token zur CSRF-Schutzmechanik festgelegt, das für die Übermittlung von Formulardaten verwendet wird.

---

### resources/views/components/action-message.blade.php
This is a Blade component file for displaying an action message on the screen. The component takes in an `on` prop which specifies the event to listen to, and a slot for customizing the message content.

The component uses Alpine.js to manage its state and displays the message only for a limited time (2 seconds) before hiding it again. The message is displayed using a fade-out transition.

The file requires some external dependencies, including:

* `Alpine\bind`: This library provides an easy way to bind properties to Alpine.js instances.
* `Alpine\transitions`: This library provides a set of predefined transitions that can be used with the `x-transition` directive.
* `Illuminate\Support\Traits\Macroable`: This trait provides a way to define methods and properties on the component instance.

The file defines a single class, which is a custom Alpine.js component that listens for an event specified in the `on` prop and displays the message using the `x-show.transition.out.opacity.duration.1500ms` directive. The `x-init` directive is used to initialize the component and set up the event listener. The `x-transition:leave.opacity.duration.1500ms` directive is used to define a custom transition for when the message is hidden.

---

### resources/views/components/app-logo-icon.blade.php
This is a Blade template file for an SVG icon. It defines a `svg` element with a few attributes, and includes a `path` element that defines the shape of the icon. The `fill` attribute of the path element specifies that the icon should be filled with the current color. The `viewBox` attribute sets the viewport for the SVG, and the `d` attribute specifies the path data for the icon.

The file is likely part of a larger application and is used to generate an SVG icon that can be used in various contexts. The file does not contain any external dependencies or API calls, but it may import Blade components or functions from other files.

---

### resources/views/components/app-logo.blade.php
This Blade file contains a UI component for displaying the app logo, which is a Laravel starter kit. The component includes an image tag and some text that describes the app.

---

### resources/views/components/auth-header.blade.php
Die Datei `resources/views/components/auth-header.blade.php` ist ein Blade-Template-File, das für die Anzeige einer Autorisierungs-Header-Komponente in der Laravel-Anwendung verwendet wird.

Die Komponente besteht aus zwei wichtigen Elementen: einem Titel und einer Beschreibung. Der Titel ist als `<h1>`-Element dargestellt, während die Beschreibung als `<p>`-Element angezeigt wird. Beide Elemente sind durch Klassen definiert, die für die Anzeige in einer Dark Mode-Umgebung relevant sind.

Die Komponente hat keine direkten Abhängigkeiten von anderen Komponenten oder APIs. Sie verwendet jedoch die Laravel-Standard-Klassen `props` und `blade`, um die Verwendung von Eigenschaften zu ermöglichen und das Rendern des Templates zu erleichtern.

---

### resources/views/components/auth-session-status.blade.php
This file is a Blade template for displaying authentication session status messages. It is used to display status messages such as "Logged in" or "Not logged in" to the user.

The file consists of 3 lines of code:

* The first line imports the props functionality from Laravel, which allows us to pass data from the parent component to this one.
* The second line checks if the status variable is set and if it is not empty. If it is, it displays a div element with the class "font-medium text-sm text-green-600", which sets the font size to medium, color to green 600, and adds some padding around the text.
* The third line displays the status message using the {{ }} syntax.

This file does not have any external dependencies or database access. It is a simple UI component that is used to display authentication session status messages.

---

### resources/views/components/placeholder-pattern.blade.php
This is a Blade component for generating a placeholder pattern in an SVG element. It uses the `uniqid()` function to generate a unique ID for the pattern, which is then used as the value of the `id` attribute on the `pattern` element. The component also accepts an `$attributes` parameter, which can be used to pass additional attributes to the SVG element.

The component defines two external dependencies:

* The `uniqid()` function from the PHP `uniqid` extension, which is used to generate a unique ID for the pattern.
* The `pattern` element from the SVG 1.1 standard, which is used to define a repeating pattern in the SVG element.

The component defines two important classes:

* The `PatternPlaceholder` class, which represents the placeholder pattern and provides the necessary properties and methods for rendering it.
* The `PatternPlaceholderFactory` class, which provides a factory method for creating instances of the `PatternPlaceholder` class.

---

### resources/views/components/layouts/app.blade.php
This file contains a layout for the app component, which defines the structure and functionality of the application's main page.

---

### resources/views/components/layouts/auth.blade.php
Die Datei "resources/views/components/layouts/auth.blade.php" ist eine Blade-Datei, die Teil des Laravel-Frameworks ist und für die Anzeige von Authentifizierungsseiten verwendet wird. Sie enthält ein einzelnes x-Layout-Element namens "simple", das einen Titel und einen Slot für den Inhalt der Authentifizierungsseite definiert.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* x-Layout-Klasse (aus dem Laravel-Framework)
* simple-Layout-Element (definiert im x-Layout-Kontext)
* $title-Variable (wird als Argument für das "simple"-Layout übergeben)
* $slot-Variable (enthält den Inhalt der Authentifizierungsseite)

---

### resources/views/components/layouts/app/header.blade.php
This code is using Alpine.js to create a responsive sidebar navigation menu for the header on large screens and a mobile hamburger button for smaller screens.

The main components of this code are:

* `mobileNav`: This is a boolean variable that determines whether the sidebar should be visible or not. It's set to false by default, but it can be toggled by clicking on the hamburger button.
* `toggleMobileNav`: This is a function that toggles the value of `mobileNav` between true and false. It's called when the hamburger button is clicked.
* `mobileSidebar`: This is a variable that represents the sidebar element in the DOM. It's used to apply CSS transitions to the sidebar when it's being shown or hidden.

The code also includes some CSS transitions and animations to make the sidebar slide in and out of view on larger screens, and to show and hide the hamburger button on smaller screens.

---

### resources/views/components/layouts/app/sidebar.blade.php
Dies ist der Code für die Sidebar auf der Laravel Livewire Starter Kit Seite. Die Sidebar ist ein wichtiger Teil einer Anwendung, da sie Benutzern ermöglicht, schnell zwischen verschiedenen Funktionen und Daten zu navigieren. In diesem Code wird die Sidebar mit einem Menü für den Benutzer erstellt, das verschiedene Optionen enthält, wie zum Beispiel das Protokoll anzeigen, das Profil bearbeiten usw.

Das HTML-Element `<div>` wird verwendet, um die Sidebar zu erstellen und eine ID-Attribut mit dem Wert `sidebar` hinzugefügt, um es eindeutig zu machen. Anschließend werden einige CSS-Klassen für die Layout- und Styling-Funktionen der Sidebar verwendet.

Das JavaScript-Element `<script>` wird verwendet, um eine Funktion namens `userMenu()` zu erstellen, die das Menü des Benutzers anzeigt und versteckt, wenn auf den Button klickt. Die Funktion verwendet den HTML-Attribute `classList` der Klasse `hidden` für das Element mit der ID `sidebar`, um es anzuzeigen oder zu verbergen.

Die Klasse `hidden` wird dann über CSS definiert, um die visuelle Ausblendung des Elements zu implementieren. Die Klasse `sidebar` wird ebenfalls verwendet, um die Sidebar auf der Seite anzupassen und die benötigten Styling-Eigenschaften festzulegen.

Die Verwendung von JavaScript und CSS in diesem Code ermöglicht es, eine interaktive Benutzeroberfläche zu erstellen, die es Benutzern ermöglicht, schnell zwischen verschiedenen Funktionen und Daten zu navigieren.

---

### resources/views/components/layouts/auth/card.blade.php
Die Datei `resources/views/components/layouts/auth/card.blade.php` ist eine Blade-Vorlage, die als Auth-Layout für das Laravel-Framework verwendet wird. Die Klasse und Funktion der Datei sind:

* `ui`: Die Datei enthält UI-Komponenten und -Elemente, wie z.B. das Layout für die Authentifizierung.
* `components/layouts/auth/card`: Die Datei ist ein Teil des Auth-Layouts und definiert ein Karten-Layout für die Auth-Seiten.

Die wichtigsten Funktionen der Datei sind:

* `@include('partials.head')`: Diese Anweisung importiert eine andere Blade-Datei namens `head`, die weitere Metadaten wie den HTML-Head und -Title enthält.
* `<div class="bg-muted flex min-h-svh flex-col items-center justify-center gap-6 p-6 md:p-10">`: Diese Klasse definiert ein Background-Farbton als "muted", legt das Layout für die Auth-Seite fest und setzt das Verhältnis der Seitenhöhe (min-h-svh) auf "Screen Height" (SVH).
* `<div class="flex w-full max-w-md flex-col gap-6">`: Diese Klasse definiert ein Layout für die Auth-Seite, das eine horizontale Flexbox-Box ist und eine maximale Breite von 768px (max-w-md) hat.
* `<a href="{{ route('home') }}" class="flex flex-col items-center gap-2 font-medium" wire:navigate>`: Diese Anweisung erstellt einen Link zur Homepage und verwendet eine Klasse, um den Text der Links zu formatieren.
* `<x-app-logo-icon class="size-9 fill-current text-black dark:text-white" />`: Diese Anweisung importiert ein Symbol für die Applikation und definiert seine Größe (size-9) und Füllung (fill-current). Der Text-Farbton wird als "black" und "dark:text-white" definiert, um ihn so zu formatieren, dass er auf verschiedenen Hintergründen sichtbar bleibt.
* `config('app.name', 'Laravel')`: Diese Anweisung importiert die Konfigurationsdatei "app" und ruft das Attribut "name" ab. Wenn der Wert nicht gefunden wird, wird stattdessen der String "Laravel" verwendet.
* `@livewireScriptConfig`: Diese Anweisung importiert ein Skript für die Livewire-Komponente und konfiguriert sie mit einigen Optionen.

Die Datei enthält keine externen Abhängigkeiten oder APIs, da sie nur eine Blade-Vorlage ist.

---

### resources/views/components/layouts/auth/simple.blade.php
Die Datei `resources/views/components/layouts/auth/simple.blade.php` ist eine Blade-Datei, die den Authentifizierungs-Layout für eine einfache Anwendung darstellt. Hier sind die wichtigsten Funktionen dieser Datei:

* Es handelt sich um einen HTML-Code, der in einer Blade-Datei definiert ist.
* Die Datei enthält HTML-Tags wie `<html>`, `<head>`, `<body>`, `<div>` und andere, um den Authentifizierungs-Layout zu erstellen.
* Die Datei verwendet die Laravel-Funktion `str_replace()`, um den Sprachcode in der Applikation zu ersetzen und den Standardwert von `en` durch `de` zu ersetzen.
* Die Datei enthält einen `wire:navigate`-Block, der eine Route für die Anwendung definiert.
* Es gibt auch einen `livewireScripts`-Block, der Livewire-Skripte enthält.
* Die Datei verwendet die Laravel-Funktion `config()`, um den Namen der Applikation aus der Konfigurationsdatei zu lesen und als Wert für den Standardwert zu verwenden.
* Es gibt auch einen `partials.head`-Block, der eine Head-Sektion definiert.
* Die Datei verwendet die Laravel-Funktion `include()`, um eine andere Blade-Datei (in diesem Fall `head`) einzuschließen.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `html`: Eine Klasse, die zum Definieren von HTML-Tags verwendet wird.
* `head`: Eine Klasse, die zum Definieren von Head-Elementen verwendet wird.
* `body`: Eine Klasse, die zum Definieren von Body-Elementen verwendet wird.
* `div`: Eine Klasse, die zum Definieren von Div-Elementen verwendet wird.
* `wire:navigate`: Ein Block, der eine Route für die Anwendung definiert.
* `livewireScripts`: Ein Block, der Livewire-Skripte enthält.
* `config()`: Eine Funktion, die verwendet wird, um Konfigurationswerte aus der Datei zu lesen.
* `include()`: Eine Funktion, die verwendet wird, um eine andere Blade-Datei einzuschließen.

---

### resources/views/components/layouts/auth/split.blade.php
This is a Laravel Blade template for the login page of a web application. The template uses Livewire and includes the `cookie-consent` package, which is used to display a consent dialog box for using cookies on the website.

The template consists of several elements:

1. A `<!DOCTYPE html>` tag that specifies the document type.
2. An `<html>` element that defines the HTML document structure and includes the `lang` attribute with the value `{{ str_replace('_', '-', app()->getLocale()) }}`. This sets the language for the page to the current application locale.
3. A `<head>` element that contains several important elements, including:
* A `<meta charset="UTF-8">` tag that specifies the character encoding used in the document.
* A `<meta name="viewport" content="width=device-width, initial-scale=1">` tag that sets the viewport for responsive design.
* An `<title>` element that includes the application name and a title separator.
* A `<link rel="stylesheet" href="{{ mix('css/app.css') }}">` tag that links to the main CSS file for the application.
4. A `<body>` element that contains several important elements, including:
* A `class="dark"` attribute that sets the page background color to dark.
* A `<div>` element with a class of "relative grid h-dvh flex-col items-center justify-center px-8 sm:px-0 lg:max-w-none lg:grid-cols-2 lg:px-0" that sets the page layout and includes several important elements.
* A `<div>` element with a class of "bg-muted relative hidden h-full flex-col p-10 text-white lg:flex dark:border-e dark:border-neutral-800" that sets the background color to muted and includes several important elements.
* A `<a>` element with a class of "relative z-20 flex items-center text-lg font-medium" that includes the application logo and a link to the home page.
* A `<div>` element with a class of "relative z-20 mt-auto" that includes several important elements, including:
	+ A `<blockquote>` element with a class of "space-y-2" that sets the margin and padding for the blockquote element.
	+ An `<p>` element with a class of "text-lg font-semibold text-white" that includes the quote message.
	+ A `<footer>` element with a class of "text-sm font-medium text-white" that includes the quote author.
* A `<div>` element with a class of "w-full lg:p-8" that sets the page layout and includes several important elements.
* An `<a>` element with a class of "z-20 flex flex-col items-center gap-2 font-medium lg:hidden" that includes the application logo and a link to the home page.
* A `{{ $slot }}` variable that includes the content for the login page.
5. An `@include('cookie-consent::index')` statement that includes the cookie consent dialog box from the `cookie-consent` package.
6. An `@livewireScriptConfig` statement that configures Livewire scripts.

---

### resources/views/components/settings/layout.blade.php
This is a Blade template for displaying settings for a user. It consists of a left navigation menu with links to different pages, and a right content area that displays the current page's contents. The `wire:navigate` attribute on the links is used to wire up the navigation in the livewire component.

The important classes and functions in this file are:

* `wire:navigate`: This is a custom directive for livewire, which allows us to navigate between pages without having to reload the entire page. It takes two parameters: the route name, and an optional parameter object. In this case, we're using it to set the active tab based on the current route.
* `request()->routeIs('profile.edit')`: This is a helper function provided by Laravel that checks if the current route matches a given pattern. We're using it to check if the current route is the profile edit page, and applying some CSS classes accordingly.
* `Laravel\Fortify\Features::canManageTwoFactorAuthentication()`: This is a helper function provided by Laravel Fortify that checks if two factor authentication is enabled for the current user. We're using it to show or hide the "Two-Factor Auth" link based on whether it's available for the current user.
* `route('profile.edit')`, `route('user-password.edit')`, `route('two-factor.show')`, and `route('appearance.edit')` are used to generate URLs for the profile edit, password change, two factor authentication settings, and appearance settings pages, respectively.

Overall, this file is responsible for displaying a list of settings options for the current user, and allowing them to navigate between those options using livewire.

---

### resources/views/emails/contact-confirmation.blade.php
{
"file_summary": "This is a contact confirmation email template for the Linn Games website. It provides a summary of the user's request and sends a thanks message."
}

Important classes, functions, or endpoints:

* `contact` - This is an instance of the Contact class that contains information about the user's request.
* `$contact->name` - The name of the user who made the request.
* `$contact->project_type` - The type of project that the user requested.
* `$contact->timeline` - The time frame for which the user requested the project.
* `$contact->message` - The user's message related to the request.
* `{{ $contact->name }}` - A place holder for the name of the user who made the request.
* `{{ $contact->project_type }}` - A place holder for the type of project that the user requested.
* `{{ $contact->timeline }}` - A place holder for the time frame for which the user requested the project.
* `{{ $contact->message }}` - A place holder for the user's message related to the request.

---

### resources/views/emails/contact-inquiry.blade.php
This is a HTML file that contains an email template for a contact inquiry form on the website linn.games. The file defines a table with several columns that contain information about the contact, including the name, company (if provided), email address, project type, timeline (if provided), and message. Additionally, the file includes the date when the contact inquiry was received in a timestamp format.

The file is written in HTML5 and uses CSS to define the styling for the table. The main elements are the `<!DOCTYPE html>`, `<html lang="de">`, `<head>`, `<title>` (which defines the title of the email), `<body>`, `<table>`, `<tr>`, `<td>`, and `<style>` tags, which define the structure and styling for the table.

The file does not import any external dependencies or use any APIs to retrieve data from a database. It only uses the contact information provided in the `$contact` variable to generate the email template.

---

### resources/views/emails/deploy-notification.blade.php
This file is a Blade template used to send an email notification after a successful deployment of the application. It is located in the `resources/views/emails` directory and is named `deploy-notification.blade.php`.

The file uses the `config()` function to access configuration values such as the app name, environment, and URL. The template also uses the `{{ $var }}` syntax to interpolate variables and display them in the email body.

Here's a summary of the most important elements of this file:

* `DOCTYPE html`: This is the document type declaration for an HTML5 document.
* `html lang="de"`: This sets the language attribute of the HTML tag to German (de).
* `<head>...</head>`: This section contains metadata about the page, such as the character encoding and the title of the page.
* `<body style="font-family: sans-serif; line-height: 1.6; color: #333;">`: This is the main content area of the email. It uses a sans-serif font with a line height of 1.6 and a text color of #333 (a light gray color).
* `<h2>Deployment erfolgreich 🚀</h2>`: This is the heading for the email, which displays a green success icon followed by the message "Deployment successful".
* `<p>Ein neues Release von {{ config('app.name') }} wurde soeben deployed.</p>`: This paragraph displays a message indicating that a new release of the application was just deployed. The `config()` function is used to access the app name from the configuration file.
* `<ul>...</ul>`: This section contains a list of bullet points with information about the deployment, such as the time and URL of the deployment.
* `<p>Diese E-Mail bestätigt, dass der Mailversand korrekt funktioniert.</p>`: This paragraph displays a message indicating that the email notification was sent successfully.
* `<p>— {{ config('app.name') }} Bot</p>`: This is the footer of the email, which displays the name of the application and a message from its bot user.

---

### resources/views/layouts/legal.blade.php
This file is a Blade template for the legal information page of a website. It includes the following functions:

1. Displays the title and content of the legal information page, which are passed as parameters to the `@yield` function.
2. Defines the styles for the layout using CSS variables.
3. Includes a navigation menu with a "Back" button that links back to the homepage.
4. Defines a footer section with copyright information and links to other legal documents.
5. Displays the last update date of the legal document in a separate section.
6. Uses JavaScript to dynamically display the current year in the footer.

The template uses Bootstrap's CSS framework for styling, but it is not necessary to use Bootstrap to create the layout. The HTML structure and CSS styles used in this template are sufficient to create a responsive design that works well on different devices and screen sizes.

---

### resources/views/legal/agb.blade.php
The file resources/views/legal/agb.blade.php contains the legal terms and conditions of using the Angebot (offer) of Linn.Games, which is a multiplayer online game with a website. The file summarizes the key features and important points of the AGB in 10 lines or less.

* The file extends from the layouts.legal view.
* The title of the document is "Allgemeine Geschäftsbedingungen (AGB)".
* The document covers two main categories: (1) Geltungsbereich und Anbieter and (2) Nutzungsregeln für den Multiplayer und die Kommunikation.
* The AGB sets forth rules for the use of the Multiplayer-Spiel and certain features of the website, including the prohibition of cheats and exploits, as well as the requirement to respect others in the communication functions.
* The document also covers digital purchases and the right of withdrawal before delivery.
* Haftung and guarantee are covered in section 6.
* Sanktionen (penalties) and account suspension are discussed in section 7.
* Haftungsfreistellung durch den Nutzer is explained in section 8.
* The AGB includes a section on the duration, cancellation, and amendments of the contract.
* Schlussbestimmungen (conclusion) cover legal rights and the obligations of both parties.

---

### resources/views/legal/dsgvo.blade.php
Die Datei resources/views/legal/dsgvo.blade.php ist eine Blade-Vorlage, die die Datenschutz-Grundverordnung (DSGVO) für die Website und das Spiel von Linn Games beschreibt. Sie enthält wichtige Informationen über die Verarbeitung personenbezogener Daten, die Rechte der Betroffenen, technische und organisatorische Maßnahmen zur Sicherung des Schutzniveaus und Kontroll- und Berichtigungsoptionen.

In dieser Datei finden sich die folgenden Funktionen:

* Eine Blade-Sektion `@extends('layouts.legal')`, mit der eine Layout-Datei namens `legal` importiert wird, die für die Darstellung des Datenschutz-Auskunftsformulars verwendet wird.
* Eine Blade-Sektion `@section('title', 'Datenschutzerklärung')`, mit der der Inhalt im `<title>`-Tag der HTML-Seite definiert wird und den Titel "Datenschutzerklärung" enthält.
* Eine Blade-Sektion `@section('content')`, die den tatsächlichen Inhalt der Datenschutzerklärung enthält. Hierbei werden Informationen über die Verarbeitung personenbezogener Daten, die Rechtsgrundlage der Verarbeitung, die technischen und organisatorischen Maßnahmen zur Sicherung des Schutzniveaus, die Rechte der Betroffenen und weitere Punkte erläutert.
* Eine Blade-Sektion `@endsection`, die den Inhalt der Datenschutzerklärung beendet.

Die Datei importiert keine外部Dependencys, aber es gibt einige Anpassungen für das CSS-Framework Bootstrap.

Der Ausgabeformat ist JSON, und die Antwort enthält eine "file_summary"-Eigenschaft mit einer kurzen Beschreibung der Datei-Funktionen.

---

### resources/views/legal/impressum.blade.php
Blade template for the impressum page, containing contact information and legal disclaimers.

---

### resources/views/livewire/auth/confirm-password.blade.php
This is a Blade template file for a Livewire component that displays a form to confirm the user's password. The form includes an input field for entering the password and a submit button to confirm it.

The file imports several classes from the Laravel framework, including `x-auth-header`, `x-auth-session-status`, and `x-layouts.auth`. These classes are used to display the page header, session status, and layout for the authentication pages.

The form is submitted to the route `password.confirm.store` using the HTTP POST method. The `csrf` middleware is included in the form to prevent cross-site request forgery attacks.

The file also includes a few error messages that are displayed if there are any validation errors when submitting the form.

---

### resources/views/livewire/auth/forgot-password.blade.php
{
"file_summary": "This file is a Livewire component that allows the user to request a password reset link via email. It uses the auth layout and includes several form fields for entering the email address and submitting the request."
}

The following are the important classes, functions, or endpoints in this file:

* `x-layouts.auth`: This is the auth layout used by this Livewire component.
* `form method="POST" action="{{ route('password.email') }}": This form is used to submit the password reset request. The method is POST and the action is set to the password.email route.
* `@csrf`: This is a security token that is required for submitting the form. It ensures that the form submission is coming from a legitimate source.
* `label for="email" class="block text-sm font-medium text-zinc-700 dark:text-zinc-300">{{ __('Email Address') }}</label>`: This label is used to display the email address field in the form. The `for` attribute specifies that the label refers to the `email` input field, and the `class` attribute sets the CSS classes for the label.
* `<input id="email" name="email" type="email" required autofocus placeholder="email@example.com" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500" />`: This input field is used to enter the email address. The `id` attribute specifies that this is the `email` input field, and the `name`, `type`, `required`, `autofocus`, and other attributes are set as appropriate for an email input field.
* `@error('email') <p class="mt-1 text-sm text-red-600">{{ $message }}</p> @enderror`: This is a Blade directive that displays any error messages generated by the `email` input field. The `@error` directive checks if there are any errors related to the `email` field, and if so, it displays the error message using the `mt-1` CSS class.
* `<button type="submit" data-test="email-password-reset-link-button" class="inline-flex w-full items-center justify-center rounded-md bg-zinc-900 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-zinc-500 focus:ring-offset-2 dark:bg-white dark:text-zinc-900 dark:hover:bg-zinc-200">{{ __('Email password reset link') }}</button>`: This button is used to submit the form and request a password reset link. The `type` attribute is set to `submit`, and the `data-test` attribute is used for testing purposes.
* `<div class="space-x-1 rtl:space-x-reverse text-center text-sm text-zinc-400">`: This div element is used to display a message that directs the user back to the login page if they do not have an account or are unable to reset their password. The `class` attribute sets the CSS classes for the div element.
* `<a href="{{ route('login') }}" wire:navigate class="underline text-zinc-400 hover:text-zinc-100">{{ __('log in') }}</a>`: This is a link that allows the user to log in again if they have an account or are unable to reset their password. The `href` attribute specifies the URL for the login page, and the `wire:navigate` directive is used for testing purposes.

---

### resources/views/livewire/auth/login.blade.php
This is a Blade template for a login form in a Livewire application. It uses the `livewire` namespace and includes several other components from the same namespace, such as `x-auth-header`, `x-auth-session-status`, and `x-layouts.auth`. The main purpose of this file is to provide a UI for users to log in to their account using an email address and password.

The template includes several form fields:

* Email address: A text input field where the user can enter their email address.
* Password: A password input field where the user can enter their password.
* Remember me: A checkbox that allows the user to choose whether they want to be remembered (i.e., logged in automatically) for a certain period of time.
* Log in: A submit button that submits the form and logs the user in.

The template also includes some conditional logic, such as displaying an error message if there is an issue with the form submission or displaying a link to reset the password if the user requests it.

Overall, this template provides a simple yet functional UI for logging in to an account using an email address and password, while also including features like remembering the user's login status and allowing them to easily reset their password.

---

### resources/views/livewire/auth/register.blade.php
This file is a Blade component that displays a form for creating a new user account on a website. It uses the Livewire framework to handle user input and validation. The form includes fields for the user's name, email address, password, and confirmation of the password. The component also includes links for logging in or recovering an existing account.

The main class in this file is `X-Layouts.Auth`, which is a custom Blade component that defines the layout for the form. It includes a header section with a title and description, as well as a footer section with links to other pages on the website. The form itself is defined using a series of `<div>` elements with various attributes and classes set to control its appearance and behavior.

The `route` function is used to generate URLs for the form's action attribute, which refers to the "register.store" route. The `wire:navigate` directive is used to prevent navigation away from the page while the form is being submitted.

Overall, this file provides a basic user registration experience on a website using Livewire and Blade components.

---

### resources/views/livewire/auth/reset-password.blade.php
Die Datei `resources/views/livewire/auth/reset-password.blade.php` ist eine Blade-Datei, die als Teil der Livewire-Frameworks verwendet wird. Sie enthält eine Formular-Komponente, mit der Benutzer ihre Passwörter zurücksetzen können.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `x-layouts.auth`: Dies ist ein Layout-Komponente, das als Basis für die Anmeldeseite verwendet wird. Es enthält eine Navbar und einen Footer.
* `form method="POST" action="{{ route('password.update') }}` : Diese Formular-Komponente sendet eine POST-Anfrage an die Route 'password.update', um das Passwort zu ändern.
* `input type="hidden" name="token" value="{{ request()->route('token') }}"`: Dies ist ein Eingabefeld, das den Authentifizierungs-Token enthält, der für die Validierung des Password Reset-Prozesses verwendet wird.
* `@error('email') <p class="mt-1 text-sm text-red-600">{{ $message }}</p> @enderror`: Diese Direktive gibt den Fehler an, wenn der Benutzer eine ungültige Email-Adresse eingegeben hat.
* `input id="password" name="password" type="password" required autocomplete="new-password" placeholder="{{ __('Password') }}" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500"` : Dies ist ein Eingabefeld für das Passwort.
* `@error('password') <p class="mt-1 text-sm text-red-600">{{ $message }}</p> @enderror`: Diese Direktive gibt den Fehler an, wenn der Benutzer ein ungültiges Passwort eingegeben hat.
* `input id="password_confirmation" name="password_confirmation" type="password" required autocomplete="new-password" placeholder="{{ __('Confirm password') }}" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500"` : Dies ist ein Eingabefeld für das Passwort Bestätigung.
* `button type="submit" data-test="reset-password-button" class="inline-flex w-full items-center justify-center rounded-md bg-zinc-900 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-zinc-500 focus:ring-offset-2 dark:bg-white dark:text-zinc-900 dark:hover:bg-zinc-200"` : Dies ist ein Schaltflächen-Komponente, die das Formular absendet und das Passwort zurücksetzt.

Die Datei benutzt auch einige externe Abhängigkeiten, wie z.B. `x-auth-header` und `x-auth-session-status`, die aus der Livewire-Frameworks verwendet werden.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
This is a Laravel Blade template for the "two-factor-challenge" view in the authentication system of a web application. The file contains several HTML elements, including input fields, buttons, and error messages.

The main classes that are used in this file are:

1. `x-layouts`: This is a custom component that is used to define the layout of the page. It includes the header and footer of the page, as well as the navigation bar and sidebar.
2. `form`: This is a HTML form element that is used to collect user input for the authentication code or recovery code. The form is submitted using the "Continue" button.
3. `x-auth-header`: This is a custom component that is used to display the title and description of the page. It includes an `<h1>` element with the title, and a `<p>` element with the description.
4. `div`: This is a HTML div element that is used to group together related elements on the page. In this file, it is used to wrap around the header, form, and recovery code input fields.
5. `input`: This is a HTML input element that is used to collect user input for the authentication code or recovery code. It has several attributes, including `type`, `name`, `x-model`, `autocomplete`, and `class`.
6. `button`: This is a HTML button element that is used to submit the form. It has several attributes, including `type`, `class`, and `hover:bg-zinc-700`.
7. `span`: This is a HTML span element that is used to display error messages for the authentication code or recovery code input fields. It has several attributes, including `class` and `opacity-50`.
8. `div.inline`: This is a HTML div element with an inline style that is used to display the title of the page in a bold font. It has several attributes, including `class` and `font-medium`.
9. `span.underline`: This is a HTML span element with an underline style that is used to display the title of the page in a bold font. It has several attributes, including `class` and `cursor-pointer`.

The main functions that are used in this file are:

1. `toggleInput()`: This function is used to toggle between displaying the authentication code input fields or the recovery code input fields. It sets the `showRecoveryInput` property to true or false, depending on its current value, and updates the `code` and `recovery_code` properties accordingly.
2. `handleInput()`: This function is used to handle user input for the authentication code or recovery code input fields. It updates the corresponding property (`code` or `recovery_code`) with the new value, and focuses on the next input field if it is not empty.
3. `handleKeydown()`: This function is used to handle key press events in the authentication code input fields. It checks for the "Backspace" key press and moves the cursor to the previous input field if it is not empty.
4. `handlePaste()`: This function is used to handle paste events in the authentication code input fields. It updates the corresponding property (`code`) with the pasted text, and focuses on the next input field if it is not empty.

---

### resources/views/livewire/auth/verify-email.blade.php
This is a Laravel Livewire component file for the email verification page. The file imports the `x-layouts.auth` component, which provides the layout for the page.

The component defines three main parts:

1. A paragraph element that displays a message to the user, asking them to verify their email address by clicking on a link sent to them via email.
2. An if statement that checks if the user has just verified their email address and displays a different message in that case.
3. A form that allows the user to resend the verification email or log out of their account.

The component also imports the `route` function, which is used to generate URLs for the forms. The `session` function is also imported, which is used to store and retrieve data in the user's session. Finally, the `__` function is imported, which is used to translate strings using Laravel's localization feature.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Die Datei `big-research-chat.blade.php` ist eine Livewire-Komponente, die für das Anzeigen und Verwalten von Chat-Nachrichten in einem Livewire-Chat-System entwickelt wurde. Die Komponente verwendet die Laravel-Framework-Bibliothek zum Erstellen einer Benutzeroberfläche und interagiert mit einer Datenbank zur Speicherung und Ausgabe der Chat-Nachrichten.

Die wichtigsten Funktionen dieser Datei sind:

1. `use` von Laravel-Bibliotheken wie `Illuminate\Support\Facades\Auth`, `Illuminate\Support\Facades\Http`, `Livewire\Volt\Component`, und `App\Models\ChatMessage`.
2. Definition einer Klasse, die eine Livewire-Komponente darstellt und Funktionen wie `getWebhook()`, `getChatMessages()`, `sendMessage()`, und `clearHistory()` enthält.
3. Verwendung von Blade-Syntax zur Erstellung der Benutzeroberfläche für den Chat, einschließlich der Anzeige des Webhooks-Namens, der Chat-Verlauf und eines Formulars zum Senden einer Nachricht.
4. Verwendung von Livewire-Aktionen wie `@chat-updated` für die automatische Scrolling-Funktion und `@disabled` für das Deaktivieren von Buttons während der Nachrichtenversandung.
5. Verwendung von Laravel-Facades für den Zugriff auf die Authentifizierung (`Auth::id()`) und HTTP-Anfragen (`Http::timeout()`).
6. Verwendung von Eloquent-ORM-Modellen (`App\Models\ChatMessage`) zum Speichern und Abfragen von Chat-Nachrichten in der Datenbank.

Die externe Abhängigkeit für diese Datei ist die Laravel-Framework-Bibliothek, um die Benutzeroberfläche zu erstellen und die Interaktion mit der Datenbank zu ermöglichen.

---

### resources/views/livewire/recherche/phase-p1.blade.php
{
"file_summary": "Diese Datei enthält Livewire-Komponenten und CRUD-Aktionen für die Erstellen, Bearbeiten und Löschen von Strukturmodellwahl, Komponenten, Kriterien und Warnsignalen im Kontext eines Projekts. Sie importiert Klassen und Funktionen aus verschiedenen Namespaces, um diese Funktionalitäten zu ermöglichen."
}
```

In this file, we have a Livewire component that handles the CRUD operations for various objects related to a project: Strukturmodellwahl (model selection), Komponente (component), Kriterium (criterion), and Warnsignal (warning signal). The component is mounted with a project instance and has several methods for creating, editing, deleting, and cancelling these objects.

The file also imports various classes and functions from different namespaces to enable the functionality of the CRUD operations. For example, we have `use App\Models\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal};` which imports the project model and the various objects related to it.

In summary, this file provides a comprehensive set of Livewire components that allow users to manage the different aspects of a project, including its Strukturmodellwahl, Komponenten, Kriterien, and Warnsignale.

---

### resources/views/livewire/recherche/phase-p2.blade.php
{
"file_summary": "Die Datei enthält eine Livewire-Komponente für das Recherche-Projekt mit verschiedenen Funktionen für die Verwaltung von ReviewTypen, Clustern, MappingSuchstringKomponenten und Trefferlisten."
}

In der Datei `resources/views/livewire/recherche/phase-p2.blade.php` werden verschiedene Funktionen für das Recherche-Projekt definiert:

* ReviewTypen: Es können ReviewTypen erstellt, bearbeitet oder gelöscht werden. Die Validierung der Eingaben wird mit dem `validate`-Methoden verwendet.
* Cluster: Cluster können erstellt, bearbeitet oder gelöscht werden. Die Validierung der Eingaben wird mit dem `validate`-Methoden verwendet.
* MappingSuchstringKomponenten: Es können Komponenten für die Suche nach verschiedenen Suchbegriffen hinzugefügt werden. Die Validierung der Eingaben wird mit dem `validate`-Methoden verwendet.
* Trefferliste: Es können Trefferlisten erstellt, bearbeitet oder gelöscht werden. Die Validierung der Eingaben wird mit dem `validate`-Methoden verwendet.

Die Funktionen sind in Form von Methoden definiert und rufen andere Methoden auf, um die Daten zu bearbeiten. Die verschiedenen Klassen und Endpunkte der Datei sind:

* `App\Models\Recherche\Projekt`: Die Modell-Klasse für das Recherche-Projekt, in der die Datenbank-Tabellen definiert werden.
* `App\Models\Recherche\{ReviewTypEntscheidung, Cluster, MappingSuchstringKomponente, Trefferliste}`: Die Modell-Klassen für die verschiedenen Objekte, die in das Recherche-Projekt eingefügt werden können.
* `Illuminate\Support\Facades\Auth`: Die Authentifizierungshilfs-Klasse von Laravel, die verwendet wird, um zu überprüfen, ob der Benutzer für den Zugriff auf das Projekt berechtigt ist.
* `Livewire\Volt\Component`: Die Livewire-Komponente, die die UI für das Recherche-Projekt rendert und die verschiedenen Funktionen verwaltet.

Die Datei importiert verschiedene Klassen und Endpunkte von Laravel und anderen Paketen, um die Funktionalität der Komponenten zu unterstützen.

---

### resources/views/livewire/recherche/phase-p3.blade.php
Die Datei `resources/views/livewire/recherche/phase-p3.blade.php` enthält eine Blade-Template-Datei für die Projektmanagement-Anwendung des Recherchemoduls. In dieser Datei werden verschiedene Komponenten verwendet, um das Formular zum Erstellen und Bearbeiten von Datenbankmatrix-, Disziplin-, GeografischerFilter- und GraueLiteratur-Eintragen anzuzeigen und zu verwalten.

Die wichtigsten Klassen, Funktionen und Endpunkte in dieser Datei sind:

* `Projekt` - eine Modellklasse, die ein Projekt darstellt, das von Nutzern erstellt werden kann.
* `P3Datenbankmatrix` - eine Modellklasse, die eine Datenbankmatrix darstellt, die zu einem Projekt gehört.
* `P3Disziplin` - eine Modellklasse, die eine Disziplin darstellt, die zu einem Projekt gehört.
* `P3GeografischerFilter` - eine Modellklasse, die einen geographischen Filter darstellt, der zu einem Projekt gehört.
* `P3GraueLiteratur` - eine Modellklasse, die eine GraueLiteratur darstellt, die zu einem Projekt gehört.
* `Livewire\Volt\Component` - eine Klasse, die eine Livewire-Komponente darstellt und die Benutzeroberfläche der Anwendung generiert.
* `mount()` - eine Funktion, die ausgeführt wird, wenn das Komponentenobjekt erstellt wird. Sie überprüft, ob der aktuelle Nutzer der Besitzer des Projekts ist und legt den `projekt`-Eigenschaftswert fest.
* `newDb()`, `saveDb()`, `editDb()`, `deleteDb()`, `cancelDb()` - Funktionen zur Verwaltung von Datenbankmatrix-Eintragungen.
* `newDis()`, `saveDis()`, `editDis()`, `deleteDis()`, `cancelDis()` - Funktionen zur Verwaltung von Disziplin-Eintragungen.
* `newGeo()`, `saveGeo()`, `editGeo()`, `deleteGeo()`, `cancelGeo()` - Funktionen zur Verwaltung von GeografischerFilter-Eintragungen.
* `newGrau()`, `saveGrau()`, `editGrau()`, `deleteGrau()`, `cancelGrau()` - Funktionen zur Verwaltung von GraueLiteratur-Eintragungen.

---

### resources/views/livewire/recherche/phase-p4.blade.php
Diese Datei ist ein PHP-Skript, das eine Livewire-Komponente definiert. Die Komponente ist für die Verwaltung von Suchstrings und Thesaurus-Mappingen in einem Projekt mit dem Namen "Recherche" auf einer Webanwendung gedacht.

Die wichtigsten Funktionen dieser Datei sind:

* Mount: Hier wird der Benutzer überprüft, ob er der Besitzer des Projekts ist und die Komponente initialisiert.
* Suchstring-CRUD (Create, Read, Update, Delete): Hier können Suchstrings hinzugefügt, gelesen, geändert oder gelöscht werden. Der Benutzer kann auch Suchstrings bearbeiten oder löschen.
* ThesaurusMapping-CRUD: Hier können Thesaurus-Mappinge hinzugefügt, gelesen, geändert oder gelöscht werden. Der Benutzer kann auch Thesaurus-Mappinge bearbeiten oder löschen.
* Anpassungsprotokoll-CRUD: Hier können Anpassungsprotokolle hinzugefügt, gelesen, geändert oder gelöscht werden. Der Benutzer kann auch Anpassungsprotokolle bearbeiten oder löschen.

Diese Datei importiert die folgenden Klassen und Funktionen:

* Livewire\Volt\Component: Die Klasse für Livewire-Komponenten.
* App\Models\Recherche\{Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll}: Die Modellklassen für die Suchstrings und Thesaurus-Mappinge in diesem Projekt.
* Illuminate\Support\Facades\Auth: Die Authentifizierungsschnittstelle für den Benutzer.

Diese Datei definiert eine Livewire-Komponente, die auf der Basis von PHP-Code geschrieben ist und die Funktionen zum Erstellen, Lesen, Aktualisieren und Löschen von Suchstrings und Thesaurus-Mappingen und Anpassungsprotokollen bereitstellt.

---

### resources/views/livewire/recherche/phase-p5.blade.php
Diese Datei enthält eine Livewire-Komponente für das Recherche-Phase-5-Modul im Projektmanagement-System. Die Komponente verfolgt die Statusänderungen von Projekten und verwaltet die Beziehungen zwischen den verschiedenen Objekten, wie z.B. Prismen-, Screening-Kriterien- und Treffer-Objekten.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Projekt`: Die Klasse repräsentiert ein Projekt und enthält seine Eigenschaften wie Name, Beschreibung, Status usw.
* `P5Treffer`: Die Klasse repräsentiert einen Treffer im Recherche-Phase-5-Modul und enthält Informationen über den Treffer wie den Titel, die Authoren und die Veröffentlichungsdatum.
* `P5ScreeningKriterium`: Die Klasse repräsentiert ein Screening-Kriterium im Recherche-Phase-5-Modul und enthält Informationen über das Kriterium wie den Level, den Typ und die Beschreibung.
* `P5ToolEntscheidung`: Die Klasse repräsentiert eine Entscheidung für ein Tool im Recherche-Phase-5-Modul und enthält Informationen über das Tool wie den Namen und die Begruendung der Entscheidung.
* `P5PrismaZahlen`: Die Klasse repräsentiert Prismen-Zahlen im Recherche-Phase-5-Modul und enthält Informationen über die Anzahl der identifizierten Literaturartikel, die Davon-Datenbank-Treffer, die Graue-Literatur-Treffer usw.
* `P5ScreeningEntscheidung`: Die Klasse repräsentiert eine Entscheidung für einen Treffer im Recherche-Phase-5-Modul und enthält Informationen über den Treffer wie den Level, die Entscheidung und den Ausschlussgrund.

Die wichtigsten Funktionen in dieser Datei sind:

* `mount`: Diese Funktion wird verwendet, um eine Instanz der Komponente zu erstellen und die Abhängigkeiten der Komponente einzurichten. Sie überprüft auch, ob der authentifizierte Benutzer der Besitzer des Projekts ist.
* `newPrisma`: Diese Funktion wird verwendet, um ein neues Prisma-Objekt zu erstellen.
* `savePrisma`: Diese Funktion wird verwendet, um ein Prisma-Objekt zu speichern oder zu aktualisieren. Sie überprüft die Eingaben und aktualisiert das Objekt in der Datenbank.
* `editPrisma`: Diese Funktion wird verwendet, um ein bestehendes Prisma-Objekt zu editieren.
* `deletePrisma`: Diese Funktion wird verwendet, um ein Prisma-Objekt zu löschen.
* `cancelPrisma`: Diese Funktion wird verwendet, um das Erstellen oder Bearbeiten eines Prisma-Objekts abzubrechen und die Formular-Eingaben zurückzusetzen.
* `newSk`: Diese Funktion wird verwendet, um ein neues Screening-Kriterium-Objekt zu erstellen.
* `saveSk`: Diese Funktion wird verwendet, um ein Screening-Kriterium-Objekt zu speichern oder zu aktualisieren. Sie überprüft die Eingaben und aktualisiert das Objekt in der Datenbank.
* `editSk`: Diese Funktion wird verwendet, um ein bestehendes Screening-Kriterium-Objekt zu editieren.
* `deleteSk`: Diese Funktion wird verwendet, um ein Screening-Kriterium-Objekt zu löschen.
* `cancelSk`: Diese Funktion wird verwendet, um das Erstellen oder Bearbeiten eines Screening-Kriterium-Objekts abzubrechen und die Formular-Eingaben zurückzusetzen.
* `newTool`: Diese Funktion wird verwendet, um ein neues Tool-Entscheidung-Objekt zu erstellen.
* `saveTool`: Diese Funktion wird verwendet, um ein Tool-Entscheidung-Objekt zu speichern oder zu aktualisieren. Sie überprüft die Eingaben und aktualisiert das Objekt in der Datenbank.
* `editTool`: Diese Funktion wird verwendet, um ein bestehendes Tool-Entscheidung-Objekt zu editieren.
* `deleteTool`: Diese Funktion wird verwendet, um ein Tool-Entscheidung-Objekt zu löschen.
* `cancelTool`: Diese Funktion wird verwendet, um das Erstellen oder Bearbeiten eines Tool-Entscheidung-Objekts abzubrechen und die Formular-Eingaben zurückzusetzen.
* `openScreen`: Diese Funktion wird verwendet, um eine Entscheidung für einen Treffer zu erstellen oder zu bearbeiten.
* `saveScreen`: Diese Funktion wird verwendet, um eine Entscheidung für einen Treffer zu speichern oder zu aktualisieren. Sie überprüft die Eingaben und aktualisiert das Objekt in der Datenbank.
* `cancelScreen`: Diese Funktion wird verwendet, um das Erstellen oder Bearbeiten einer Entscheidung für einen Treffer abzubrechen und die Formular-Eingaben zurückzusetzen.
* `deleteScreen`: Diese Funktion wird verwendet, um eine Entscheidung für einen Treffer zu löschen.

Die Funktionen in dieser Datei sind wichtig, um das Erstellen und Bearbeiten von Objekten im Recherche-Phase-5-Modul zu ermöglichen und die Beziehungen zwischen den verschiedenen Objekten zu verwalten.

---

### resources/views/livewire/recherche/phase-p6.blade.php
The file `resources/views/livewire/recherche/phase-p6.blade.php` is a Livewire component for displaying the results of a quality analysis and a lückenanalyse (a German term for missing data analysis) for a specific project. The component uses multiple classes and functions from the Laravel framework, including `App\Models\Recherche`, `Livewire\Volt\Component`, and `Illuminate\Support\Facades`.

The component mounts the project specified in the URL and retrieves its treffer (results) and bewertungen (quality assessments) from the database. It also provides buttons for creating new quality assessments, editing existing ones, deleting them, and cancelling the form.

The component uses a `with` method to retrieve additional data for visualization, including the distribution of quality assessment results by outcome.

Overall, this file appears to be used for displaying the results of a quality analysis and lückenanalyse for a specific project in a user-friendly way using Livewire.

---

### resources/views/livewire/recherche/phase-p7.blade.php
This file contains a Livewire component for the "recherche" page of a web application. The component is using classes from the `App\Models` namespace and importing various Livewire components. It also uses the `Illuminate\Support\Facades\Auth` facade to access the authentication system.

The component has several methods that handle CRUD (create, read, update, delete) operations for different types of data:

* Synthese-Methode: The component can create, edit, and delete synthesis methods for a project.
* Datenextraktion: The component can create, edit, and delete data extractions for a project.
* Muster & Konsistenz: The component can create, edit, and delete patterns and consistencies for a project.
* GRADE: The component can create, edit, and delete grades for a project.

The component also has a `mount` method that is called when the component is mounted (i.e., when it is first loaded). This method checks whether the current user has permission to access the project and sets the `$projekt` property to the project.

The component also has several methods for handling form submissions, such as `saveSm`, `editSm`, and `deleteSm` for synthesis methods, and similar methods for data extractions, patterns & consistencies, and grades.

---

### resources/views/livewire/recherche/phase-p8.blade.php
Diese Datei enthält eine Livewire-Komponente zur Verwaltung von Suchprotokoll, Limitation und Update-Plan für ein Projekt. Sie nutzt das Modell 'Projekt' und ist Teil der Benutzeroberfläche für die Erstellung und Verwaltung von Projekten. Die Klasse umfasst CRUD-Operationen für Suchprotokoll, Limitation und Update-Plan und bietet auch Formular-Generierung für jede Art von Dateneingabe.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Die Datei `resources/views/livewire/recherche/projekt-detail.blade.php` ist eine Livewire View, die für das Anzeigen von Details zu einem bestimmten Projekt in der Recherchen-Sektion zuständig ist. Hierbei werden verschiedene Phasen des Prozesses visualisiert, um den Benutzer dabei zu unterstützen, seine Arbeit effektiver zu organisieren.

Die Klasse `new class extends Component` definiert eine Livewire-Komponente für die Detailansicht eines Projekts. In dieser Komponentendefinition werden die folgenden wichtigen Funktionen und Eigenschaften definiert:

* `public $projekt`: Das aktuelle Projekt, das in der View angezeigt wird.
* `public int $activeTab = 1`: Die aktive Tab-Nr., die festlegt, welche Phase der Prozess visualisiert werden soll.
* `public function mount(Projekt $projekt): void`: Führt die Überprüfung durch, ob der Benutzer Zugriff auf das Projekt hat und lädt die Daten des Projekts einschließlich seiner Phasen in die Variable `$this->projekt`.
* `public function switchTab(int $tab): void`: Wird aufgerufen, wenn der Benutzer eine neue Tab-Nr. auswählt. Hierbei wird die aktive Tab-Nr. geändert und die entsprechende Phase des Prozesses visualisiert.
* `public function getPhaseStatus(int $nr): ?string`: Gibt den Status der angegebenen Phase zurück (entweder 'abgeschlossen', 'in_bearbeitung' oder null, wenn keine Statusinformationen vorhanden sind).

Die View enthält verschiedene Abschnitte, die die verschiedenen Funktionen und Informationen visualisieren:

* Header: Zeigt den Titel des Projekts und eine kurze Beschreibung darüber an.
* Tab Bar: Enthält einen Navigationsbereich mit Schaltflächen für die verschiedenen Phasen des Prozesses. Jede Schaltfläche enthält den Namen der Phase, eine Miniaturansicht ihres Status (grün für 'abgeschlossen', gelb für 'in_bearbeitung' oder transparent für unbekannt) und einen zugehörigen Tab-Nr.
* Phase Content: Zeigt die Inhalte der aktiven Phase an, je nach Wahl durch den Benutzer. Hierbei werden verschiedene Livewire-Komponenten verwendet (z.B. `livewire:recherche.phase-p1`, `livewire:recherche.phase-p2` usw.), um die entsprechenden Daten und Funktionen für die aktive Phase anzuzeigen.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
This is a Blade template file for the Livewire component `projekt-liste.blade.php`. The file contains a UI component that displays a list of projects created by a user, with the option to delete each project.

The file imports several external dependencies:

* `App\Models\Recherche\Projekt`: This is a model class for storing and retrieving project data from the database.
* `Illuminate\Support\Facades\Auth`: This is a facade that provides access to the Laravel authentication system, which allows us to retrieve the current user's ID and perform authorization checks.
* `Livewire\Volt\Component`: This is a Livewire component class that helps us build reusable UI components for our application.

The file defines two methods:

* `projekte()`: This method returns a collection of projects created by the current user, sorted by creation date in descending order.
* `deleteProjekt($id)`: This method deletes a project with the given ID from the database. It also performs authorization checks to ensure that the current user has permission to delete this project.

The file also defines a `with()` method that is used to pass data to the template. In this case, we're passing the result of the `projekte()` method to the template as an array with the key "projekte".

Finally, the file contains a `<section>` element and some HTML markup for displaying the list of projects. The code uses Livewire's built-in syntax for binding data to components, such as the `wire:navigate` attribute on the anchor tag, which allows us to navigate to a project's detail page when a user clicks on its title.

---

### resources/views/livewire/recherche/research-input.blade.php
This is a Livewire component for creating a research project. It contains two main parts: the input form and the output (redirecting to the created project).

The input form has a textarea where the user can enter their research question or topic, and a submit button to start the research process. The form also includes an error message if the input is not valid.

When the submit button is clicked, the component will validate the input data (using Livewire's built-in validation) and then create a new project in the database with the entered information. It will also dispatch a job to trigger the research process on a background worker using Laravel's queuing system. Finally, it will redirect the user to the created project page.

The component also includes some external dependencies, such as the `App\Models\Recherche\Projekt` model and the `App\Jobs\TriggerLangdockAgent` job. These are used to create a new research project and trigger the background worker job that will perform the research process.

---

### resources/views/livewire/settings/appearance.blade.php
This file is a view component for the "Appearance" settings page in Laravel Livewire. It displays a toggle button to change the theme of the application between light, dark, and system (based on user preference). The class `Component` extends Livewire's `Volt\Component` class.

The template contains several HTML elements:

* A `<section>` element with the ID "appearance-toggle" that holds the toggle buttons for the different themes.
* An `<x-settings.layout>` component that displays a heading and subheading for the settings page, as well as a form to submit the theme preference.
* Several `<button>` elements with the classes "appearance-btn" and "inline-flex" that serve as toggle buttons for the different themes.
* A `<script>` element that contains a JavaScript function to handle the click events on the button elements, as well as functions to apply the theme preference to the application and highlight the active button.

The script uses the `window.matchMedia()` method to check if the user prefers dark mode, and it sets the initial theme accordingly. It also stores the theme preference in local storage using `localStorage.setItem()`, so that it persists even after page refreshes.

---

### resources/views/livewire/settings/delete-user-form.blade.php
This is a Livewire component that allows the user to delete their account. It consists of several functions and classes, including:

* `openModal()`: This function is called when the user clicks on the "Delete Account" button, and it opens the modal window for deleting the account.
* `closeModal()`: This function is called when the user clicks on the cancel button in the modal window, and it closes the modal window and resets the form data.
* `deleteUser(Logout $logout)`: This function is called when the user submits the form in the modal window, and it deletes the account. The `$logout` parameter is a reference to the `Logout` class from the Laravel framework, which is used to log out the user after deleting their account.
* `password`: This is a public property that contains the password entered by the user in the form. It is validated using the `@validate()` directive.
* `showDeleteModal`: This is a public boolean property that indicates whether the delete modal window should be displayed or not. It is set to `true` when the modal window is opened, and `false` when it is closed.
* `Logout`: This is a class from the Laravel framework that is used to log out the user after deleting their account.
* `Auth::user()`: This is a reference to the currently logged-in user.
* `tap(Auth::user(), $logout(...))->delete()`: This code logs out the user and then deletes their account using the `delete()` method of the `Logout` class.
* `$wire.closeModal()`: This code closes the modal window when the user clicks on the cancel button in the form.
* `$wire.reset('password')` and `$wire.resetValidation()`: These functions are called when the user enters a new password or changes it, and they reset the `password` property and validate the form again.

---

### resources/views/livewire/settings/password.blade.php
This file contains a Livewire component that allows the user to update their password for an authenticated user. The component uses the `Illuminate\Support\Facades\Auth` facade to retrieve the currently authenticated user and the `Illuminate\Validation\Rules\Password` class to validate the inputted password fields.

The component also includes a form with three input fields for the current password, new password, and password confirmation. The form is submitted via the `updatePassword()` method, which validates the input using Laravel's built-in validation rules and updates the user's password in the database.

The component also uses the `x-action-message` Blade component to display a success message when the password is updated successfully.

Overall, this file provides a way for users to securely update their passwords using Livewire and Laravel's built-in validation and authentication mechanisms.

---

### resources/views/livewire/settings/profile.blade.php
Die Datei `resources/views/livewire/settings/profile.blade.php` enthält eine Livewire-Komponente, die für das Aktualisieren des Benutzernamens und der E-Mail-Adresse eines Benutzers verantwortlich ist.

Die Komponente verwendet die folgenden Klassen und Funktionen:

* `App\Models\User`: Die Modellklasse für den `User`-Tabelle, in der die Benutzerinformationen gespeichert sind.
* `Illuminate\Support\Facades\Auth`: Der Auth-Facade wird verwendet, um den aktuell angemeldeten Benutzer zu erhalten.
* `Illuminate\Support\Facades\Session`: Der Session-Facade wird verwendet, um die Sitzung des Benutzers zu verwalten.
* `Illuminate\Validation\Rule`: Die Rule-Klasse wird verwendet, um Validierungsregeln für die Eingaben der Komponente zu definieren.
* `Livewire\Volt\Component`: Die Component-Klasse von Livewire wird verwendet, um die Komponente zu definieren und zu rendern.
* `App\Http\Controllers\SettingsController`: Der SettingsController ist der Controller für den Einstellungsbereich des Systems und enthält Funktionen wie das Aktualisieren von Benutzerdaten oder das Löschen von Benutzerkonten.

Die Komponente verwendet auch die folgenden Endpunkte:

* `updateProfileInformation`: Diese Methode wird aufgerufen, wenn der Benutzer auf die Schaltfläche "Save" klickt, um seine Profilinformationen zu aktualisieren.
* `resendVerificationNotification`: Diese Methode wird aufgerufen, wenn der Benutzer auf den Link "Re-send verification email" klickt, um eine neue Bestätigungs-E-Mail anzufordern.

---

### resources/views/livewire/settings/two-factor.blade.php
Die Datei `resources/views/livewire/settings/two-factor.blade.php` ist ein Livewire-Komponenten-Template für die Einstellungen von Two-Factor-Authentifizierung (2FA) in einer Laravel-Anwendung. Die Komponente wird von Livewire verwendet, um die Benutzereinstellungen für 2FA zu bearbeiten und zu speichern.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `new class extends Component`: Dies ist eine neue Livewire-Komponente, die von der `Component` Klasse erbt.
* `$twoFactorEnabled`: Diese Variable enthält den aktuellen Status von 2FA für den aktuell angemeldeten Benutzer. Wenn true, bedeutet dies, dass 2FA für den Benutzer aktiviert ist und er aufgefordert wird, einen sicheren PIN während des Logins zu verwenden.
* `$requiresConfirmation`: Diese Variable enthält eine boolesche Werte, die angibt, ob der Benutzer eine Bestätigung für 2FA benötigt oder nicht. Wenn true, bedeutet dies, dass der Benutzer eine zusätzliche Bestätigungsschritt durchführen muss, bevor er seine Einstellungen speichern kann.
* `$qrCodeSvg`: Diese Variable enthält einen QR-Code für die 2FA-Einrichtung des Benutzers. Der QR-Code wird verwendet, um den Benutzer zu ermöglichen, seine 2FA-Einstellungen mit einer TOTP-kompatiblen App auf seinem Handy zu konfigurieren.
* `$manualSetupKey`: Diese Variable enthält einen Schlüssel für die manuelle Einstellung von 2FA für den Benutzer. Der Benutzer muss diesen Schlüssel in seiner TOTP-kompatiblen App eingeben, um seine 2FA-Einstellungen zu konfigurieren.
* `mount(DisableTwoFactorAuthentication $disableTwoFactorAuthentication)`: Diese Funktion wird aufgerufen, wenn die Komponente erstellt wird. Sie überprüft, ob das Feature 2FA in der Laravel-Anwendung aktiviert ist und löscht den Benutzer aus dem Cache, wenn er keine Bestätigung für 2FA benötigt.
* `enable(EnableTwoFactorAuthentication $enableTwoFactorAuthentication)`: Diese Funktion wird aufgerufen, um die Einstellungen von 2FA für den Benutzer zu aktivieren. Sie ruft die `EnableTwoFactorAuthentication`-Aktion auf und löst eine Ausnahme aus, wenn das Feature 2FA nicht in der Laravel-Anwendung aktiviert ist.
* `loadSetupData()`: Diese Funktion lädt die Setup-Daten für den Benutzer, um seine 2FA-Einstellungen zu konfigurieren. Sie ruft die `twoFactorQrCodeSvg`-Methode auf, um einen QR-Code für die 2FA-Einrichtung zu generieren und speichert ihn in der Variablen `$qrCodeSvg`.
* `showVerificationIfNecessary()`: Diese Funktion wird aufgerufen, um den Benutzer zur Überprüfung des Authentifizierungscodes für 2FA anzufordern. Sie ruft die `ConfirmTwoFactorAuthentication`-Aktion auf und löst eine Ausnahme aus, wenn das Feature 2FA nicht in der Laravel-Anwendung aktiviert ist.
* `confirmTwoFactor(ConfirmTwoFactorAuthentication $confirmTwoFactorAuthentication)`: Diese Funktion wird aufgerufen, um den Authentifizierungscode für 2FA zu bestätigen. Sie ruft die `ConfirmTwoFactorAuthentication`-Aktion auf und löst eine Ausnahme aus, wenn das Feature 2FA nicht in der Laravel-Anwendung aktiviert ist.
* `resetVerification()`: Diese Funktion wird aufgerufen, um den Benutzer zu ermöglichen, seine 2FA-Einstellungen zurückzusetzen. Sie ruft die `ResetTwoFactorAuthentication`-Aktion auf und löst eine Ausnahme aus, wenn das Feature 2FA nicht in der Laravel-Anwendung aktiviert ist.
* `disable(DisableTwoFactorAuthentication $disableTwoFactorAuthentication)`: Diese Funktion wird aufgerufen, um die Einstellungen von 2FA für den Benutzer zu deaktivieren. Sie ruft die `DisableTwoFactorAuthentication`-Aktion auf und löst eine Ausnahme aus, wenn das Feature 2FA nicht in der Laravel-Anwendung aktiviert ist.
* `closeModal()`: Diese Funktion wird aufgerufen, um das Modalfenster für die 2FA-Einstellungen zu schließen, wenn der Benutzer den Button "Schließen" drückt oder die ESC-Taste drückt.

---

### resources/views/livewire/settings/webhooks.blade.php
This file contains a Livewire component for managing webhooks related to the dashboard chat and recherche start features. It includes functions for saving and clearing webhooks, as well as mounting and validating the form data.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
This file is a Livewire component for displaying recovery codes in the Laravel Fortify 2FA settings page. It uses the `Laravel\Fortify\Actions\GenerateNewRecoveryCodes` action to regenerate the recovery codes if needed, and displays them in a list. The component also includes an error message if there is an issue generating the recovery codes.

The component extends the `Livewire\Component` class and uses the `[Locked]` attribute to lock the properties of the component to prevent unauthorized access. It also imports the `Laravel\Fortify\Actions\GenerateNewRecoveryCodes` action, which is used in the `regenerateRecoveryCodes()` method to regenerate the recovery codes.

The `mount()` method is called when the component is first mounted and loads the recovery codes for the current user using the `loadRecoveryCodes()` method. The `loadRecoveryCodes()` method decrypts the encrypted recovery codes and stores them in the `$recoveryCodes` property of the component.

The component includes a button to regenerate the recovery codes, which uses the `regenerateRecoveryCodes()` method when clicked. This method first checks if the user has enabled 2FA and then generates new recovery codes using the `GenerateNewRecoveryCodes` action. The generated codes are then loaded into the `$recoveryCodes` property of the component using the `loadRecoveryCodes()` method.

The component also includes a button to hide the recovery codes, which is shown when the user has regenerated the codes and needs to hide them again. This button uses the `showRecoveryCodes` property of the component to toggle the visibility of the recovery codes section.

Finally, the component includes an error message if there is an issue generating the recovery codes, which is displayed when the `$recoveryCodes` property is not empty and there is a validation error on the `recoveryCodes` field.

---

### resources/views/partials/head.blade.php
Die Datei `head.blade.php` ist ein Partial-Template in Laravel, das für die Anzeige von HTML-Head-Tags in verschiedenen Seiten verwendet wird. Es importiert einige CSS- und JavaScript-Dateien und initialisiert die Dark-Mode-Funktion für das Template.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* `meta charset="utf-8"`: Deklariert die Verwendung der UTF-8-Kodierung.
* `meta name="viewport" content="width=device-width, initial-scale=1.0"`: Deklariert die Ausdehnung und Skalierung des HTML-Dokuments auf den Browser-Fenster-Breitenrahmen.
* `title`: Legt den Titel der Webseite fest.
* `link rel="icon" href="/favicon.ico" sizes="any"`: Deklariert das Verwendung des Favicon-Icons für die Webseite.
* `link rel="icon" href="/favicon.svg" type="image/svg+xml"`: Deklariert das Verwendung des SVG-Favicon-Icons für die Webseite.
* `link rel="apple-touch-icon" href="/apple-touch-icon.png"`: Deklariert das Verwendung des PNG-Favicon-Icons für Apple-Geräte.
* `link rel="preconnect" href="https://fonts.bunny.net">`: Deklariert die Verbindung zu einem Font-Service.
* `@vite(['resources/css/app.css', 'resources/js/app.js'])`: Importiert die CSS- und JavaScript-Dateien für das Template.

Es gibt keine externen Abhängigkeiten in dieser Datei, da sie lediglich HTML-Head-Tags und Javascript-Initialisierung für die Dark-Mode-Funktion enthält.

---

### resources/views/partials/settings-heading.blade.php
{
  "file_summary": "This partial view is used to display a heading for the settings section of the user profile page. It includes a title, a subtitle, and a horizontal line as a divider."
}

The file contains the following classes:

* `relative`: positions this element relative to its normal position
* `mb-6`: sets the bottom margin to 6 units (or pixels)
* `w-full`: sets the width of this element to take up the full available space
* `text-xl`: sets the text size to extra large
* `font-semibold`: sets the font weight to semi-bold
* `text-zinc-900`/`dark:text-zinc-100`: sets the text color to zinc 900 (light mode) or zinc 100 (dark mode)
* `mt-1`: sets the top margin to 1 unit
* `text-base`: sets the text size to base
* `text-zinc-500`/`dark:text-zinc-400`: sets the text color to zinc 500 (light mode) or zinc 400 (dark mode)
* `mb-6`: sets the bottom margin to 6 units (or pixels)
* `border-zinc-200`/`dark:border-zinc-700`: sets the border color to zinc 200 (light mode) or zinc 700 (dark mode)

The file also includes the following external dependencies:

* `{{ __('Settings') }}`: a placeholder for translated text
* `{{ __('Manage your profile and account settings') }}`: a placeholder for translated text

---

### resources/views/recherche/index.blade.php
Die Datei enthält eine UI-Seite für die Recherche von Projekten im Projektmanagement-System. Sie verwendet das Layout 'app' und zeigt ein Formular zur Eingabe von Suchbegriffen an, ein horizontales Strichell unter dem Titel und eine Liste mit den gefundenen Projekten.

---

### resources/views/recherche/show.blade.php
This blade template file is part of the ui category and displays a livewire component to display the details of a projekt. It uses the layouts.app component as its root element and sets the title attribute to the value of the project's titel variable.

---

## uncategorized

### Readme.md
This is a Laravel application for managing research projects with AI-powered systematic literature review. It includes features such as user authentication, contact form, research project management, and a MCP PostgreSQL endpoint for KI-based database access.

---

### artisan
This file is an entry point for the Artisan console command in a Laravel application. It defines several constants and variables, including `LARAVEL_START`, which is used to measure the execution time of the command. The file also includes the Composer autoloader and bootstraps the Laravel application using the `bootstrap/app.php` file.

The file then handles the command by creating an instance of the `ArgvInput` class and passing it to the `$app->handleCommand()` method, which is responsible for executing the command. The exit status of the command is returned from this method and stored in the `$status` variable.

Overall, this file is a crucial part of the Laravel framework, as it allows users to interact with the application using the Artisan console commands.

---

### basemodel.md
Die Datei `basemodel.md` enthält ein Modell für eine PostgreSQL-Datenbank zur Speicherung von Forschungsprojekten. Die wichtigsten Klassen und Funktionen sind:

* `Projekte`: Tabelle zur Speicherung von Forschungsprojekten, mit Spalten wie ID, Titel, Forschungsfrage, Review-Typ, Verantwortlich, Startdatum, Letztes Update und Notizen.
* `Phasen`: Tabelle zur Speicherung der Phasen eines Projekts, mit Spalten wie ID, Projekt-ID, Phase-Nr., Titel, Status (offen, in Bearbeitung oder abgeschlossen) und Anmerkungen.
* `P1_strukturmodell_wahl`: Tabelle zur Speicherung der Wahl des Strukturmodells für die Phase 1, mit Spalten wie ID, Projekt-ID, Modell (PICO/SPIDER/PICOS) und Begruendung.
* `P1_komponenten`: Tabelle zur Speicherung der Komponenten des Strukturmodells, mit Spalten wie ID, Projekt-ID, Modell, Komponente-Kürzel (z.B. 'P', 'I', 'C', 'O', 'S'), Komponente-Label (z.B. 'Population / Sample') und Anmerkungen.
* `P1_kriterien`: Tabelle zur Speicherung der Kriterien für die Phase 1, mit Spalten wie ID, Projekt-ID, Kriterium-Typ ('einschluss' oder 'ausschluss'), Beschreibung und Begruendung.
* `P1_warnsignale`: Tabelle zur Speicherung von Warnzeichen für die Phase 1, mit Spalten wie ID, Projekt-ID, LFD-Nr. (z.B. 'W1', 'W2', 'W3'), Warnzeichen und Anmerkungen.
* `P2_review_typ_entscheidung`: Tabelle zur Speicherung der Entscheidung über den Review-Typ für die Phase 2, mit Spalten wie ID, Projekt-ID, Review-Typ (Systematischer Review/Scoping-Review oder Evidence Map) und Begruendung.
* `P2_mapping_suchstring_komponenten`: Tabelle zur Speicherung der Mapping-Suchstrings für die Phase 2, mit Spalten wie ID, Projekt-ID, Komponente-Label (z.B. 'Hauptbegriff A') und Suchbegriffe als OR-Gruppe.
* `P2_trefferlisten`: Tabelle zur Speicherung der Trefferlisten für die Phase 2, mit Spalten wie ID, Projekt-ID, Datenbank (z.B. 'PubMed'), Suchstring und Treffer-Gesamtzahl.
* `P2_cluster`: Tabelle zur Speicherung von Clustern für die Phase 2, mit Spalten wie ID, Projekt-ID, Cluster-ID (z.B. 'C-01') und Cluster-Label.
* `P3_disziplinen`: Tabelle zur Speicherung der Disziplinen für die Phase 3, mit Spalten wie ID, Projekt-ID, Disziplin (z.B. 'Medizin') und Art (kerndisziplin oder angrenzend).
* `P3_datenbankmatrix`: Tabelle zur Speicherung der Datenbankmatrix für die Phase 3, mit Spalten wie ID, Projekt-ID, Datenbank (z.B. 'PubMed'), Disziplin und Abdeckung.
* `P3_geografische_filter`: Tabelle zur Speicherung von geografischen Filtern für die Phase 3, mit Spalten wie ID, Projekt-ID, Region/Land (z.B. 'Deutschland') und Validierter Filter vorhanden (Ja oder Nein).
* `P3_graue_literatur`: Tabelle zur Speicherung von grauen Literatur für die Phase 3, mit Spalten wie ID, Projekt-ID, Quelle (z.B. 'PubMed'), Typ und Anmerkungen.
* `P4_suchstrings`: Tabelle zur Speicherung der Suchstrings für die Phase 4, mit Spalten wie ID, Projekt-ID, Datenbank (z.B. 'PubMed') und Suchstring.

Diese Tabelle dient zur Speicherung von Suchstrings, die während der Suche verwendet werden können.

---

### composer.json
This is a `composer.json` file for a Laravel project. It defines the dependencies, scripts, and configuration options for the project.

Here are some key points about this file:

* The `"name"` field specifies the name of the package (in this case, `nileneb/app-linn-games`).
* The `"type"` field specifies that this is a Laravel project (the value is "project").
* The `"description"` field provides a brief description of the project.
* The `"keywords"` field lists some keywords that can be used to search for the package on Packagist.
* The `"license"` field specifies the license under which this package is distributed (in this case, "proprietary").
* The `"require"` section lists the dependencies of the project, including PHP and other packages.
* The `"require-dev"` section lists the development dependencies of the project.
* The `"autoload"` section specifies how classes are loaded in the application.
* The `"autoload-dev"` section specifies how development dependencies are loaded.
* The `"scripts"` section defines commands that can be run with Composer.
* The `"extra"` section contains additional configuration options for the package.
* The `"config"` section specifies the optimizer and other settings for the composer autoloader.
* The `"minimum-stability"` field specifies the minimum stability level of packages required by this project (in this case, "stable").
* The `"prefer-stable"` field specifies whether to prefer stable versions of dependencies over unstable ones.

---

### CONTRIBUTING.md
Dies ist ein Markdown-Datei für die Contributing-Regeln des Projekts app.linn.games. Hier sind die wichtigsten Aspekte der Datei aufgelistet: Branch-Konvention, Merge-Fluss, Arbeitsablauf pro Issue, Lokale Entwicklung, Coding-Standards, Datenbank-Änderungen und Tests.

---

### deploy.sh
This is a Bash script that deploys the app.linn.games application to production. It uses Docker Compose to manage the application's services and containers. The script performs several actions during the deployment process, including building the production images, starting the infrastructure services (Postgres and Redis), running database migrations, creating a backup of the database, caching configuration, routes, and views, and finally starting all the services.

Here are the main functions of the script:

1. Pre-flight checks: The script checks if Docker is installed, if the .env file exists, and if Postgres is healthy before proceeding with the deployment.
2. Build: If the `--skip-build` option is not passed to the script, it builds the production images for the application using `docker compose build --no-cache`.
3. Start infrastructure: The script starts the Postgres and Redis services using `docker compose up -d`.
4. Migrate: If the `--skip-migrate` option is not passed to the script, it runs database migrations using `docker compose run --rm php-cli php artisan migrate --force`.
5. Backup: The script creates a backup of the Postgres database using `pg_dump` and saves it to a file in the current directory.
6. Cache optimisation: The script caches configuration, routes, and views using `docker compose run --rm php-cli php artisan config:cache`, `docker compose run --rm php-cli php artisan route:cache`, and `docker compose run --rm php-cli php artisan view:cache`.
7. Start all services: The script starts all the services using `docker compose up -d`.
8. Verify: The script waits for a few seconds to allow the services to settle, and then displays the status of the services using `docker compose ps`.
9. Deploy notification mail: The script sends a deploy notification mail using `docker compose run --rm php-cli php artisan deploy:notify`.

---

### package.json
Die Datei package.json enthält Informationen zu einem Node.js-Projekt und ist eine der wichtigsten Konfigurationsdateien für die Installation und Verwaltung von Abhängigkeiten in einem Projekt. Hier sind die wichtigsten Funktionen dieser Datei in maximal 10 Zeilen zusammengefasst:

* Die Datei beschreibt ein Node.js-Projekt, das private ist und als Modul (Type: module) erstellt wurde.
* Sie definiert Skripte für den Build-Prozess (scripts): "build" für die Vite-Build-Funktion und "dev" für die Vite-Entwicklungsfunktion.
* Die Datei enthält Abhängigkeiten für verschiedene Module, wie z.B. TailwindCSS, Axios, Laravel-Vite-Plugin, Vite und Autoprefixer.
* Optional werden weitere Abhängigkeiten definiert, die als "optional" markiert sind: "@rollup/rollup-linux-x64-gnu", "@tailwindcss/oxide-linux-x64-gnu" und "lightningcss-linux-x64-gnu".
* Die Datei verwendet das Schema von schemastore.org für die Verwendung mit Package.json.

---

### phpunit.xml
This file is a PHPUnit configuration file, which is used to configure the behavior of PHPUnit for testing Laravel applications.

The file defines several elements:

* `phpunit`: This element specifies the root element of the XML document, and contains attributes that define global settings for PHPUnit.
* `testsuites`: This element defines a list of test suites to run. In this case, there are two test suites defined: "Unit" and "Feature".
* `directory`: This element specifies a directory to include in the test suite. In this case, both directories "tests/Unit" and "tests/Feature" are included.
* `source`: This element specifies a list of files or directories to be excluded from code coverage analysis.
* `php`: This element defines PHP-specific settings for PHPUnit. It includes several environment variables that define the behavior of PHPUnit in various ways.
* `env`: This element defines an environment variable and its value. In this case, several environment variables are defined:
	+ `APP_ENV`: The application environment (testing)
	+ `APP_KEY`: The encryption key for encrypting cookies and other sensitive data (a random base64-encoded string)
	+ `APP_MAINTENANCE_DRIVER`: The driver to use for maintenance mode (file)
	+ `BCRYPT_ROUNDS`: The number of rounds to use when hashing passwords (4)
	+ `BROADCAST_CONNECTION`: The connection to use for broadcasting events (null)
	+ `CACHE_STORE`: The cache store to use (array)
	+ `DB_CONNECTION`: The database connection to use (sqlite)
	+ `DB_DATABASE`: The database name (in memory)
	+ `MAIL_MAILER`: The mailer to use for sending emails (array)
	+ `QUEUE_CONNECTION`: The queue connection to use (sync)
	+ `SESSION_DRIVER`: The session driver to use (array)
	+ `PULSE_ENABLED`: Whether Pulse is enabled or not (false)
	+ `TELESCOPE_ENABLED`: Whether Telescope is enabled or not (false)
	+ `NIGHTWATCH_ENABLED`: Whether Nightwatch is enabled or not (false)

These environment variables are used by Laravel to configure various aspects of the application, such as encryption keys, database connections, and queue drivers.

---

### vite.config.js
Die Datei `vite.config.js` ist eine Vite-Konfigurationsdatei für die Laravel-Applikation. Sie importiert die folgenden Plugins:

* `defineConfig`: Ein Vite-Standard-Plugin, das es ermöglicht, eine Konfigurationsoptionen zu definieren.
* `laravel`: Ein Laravel-Vite-Plugin, das es ermöglicht, die Laravel-Anwendung in Vite zu integrieren und es in der Lage zu sein, die Laravel-Anwendungsdatei zu verarbeiten.
* `tailwindcss`: Ein Tailwind CSS-Plugin, das es ermöglicht, Tailwind CSS-Komponenten in der Vue-Anwendung zu verwenden.

Die Konfiguration enthält auch einige Optionen für den Vite-Server:

* `cors`: Aktiviert die CORS-Unterstützung (Cross-Origin Resource Sharing) und ermöglicht es, dass Anfragen von verschiedenen Ursprüngen an die API der Applikation gesendet werden können.
* `watch`: Eine Option für das Überwachen des Verzeichnisses `resources/framework/views`, um sicherzustellen, dass die Ansichten aktualisiert werden, wenn sie geändert werden.

Insgesamt gibt es 10 Zeilen in der Datei `vite.config.js`.

---

### .dockerignore
This file is a `.dockerignore` file, which tells Docker to ignore certain files and directories during the build process. The file contains a list of patterns that match files and directories that should be excluded from the build context.

Here are the important classes, functions, or endpoints in this file:

* `*.env`: Ignores environment variable files with the `.env` extension.
* `!env.example`: Excludes the `env.example` file if it exists.
* `node_modules/`, `vendor/`: Ignores the `node_modules/` and `vendor/` directories, which are often used to store dependencies for Node.js or other languages.
* `storage/framework/*`: Ignores the `storage/framework/` directory, which is used by Laravel to store cache, sessions, and views.
* `public/build/`, `public/hot`: Ignores the `public/build/` and `public/hot` directories, which are used for caching or hot-reloading in some web frameworks.
* `tests/*`: Ignores the `tests/` directory, which is often used to store test cases for unit testing.
* `.phpunit.cache/`, `coverage/`: Ignores the `.phpunit.cache/` and `coverage/` directories, which are used by PHPUnit for caching and coverage analysis.
* `*.log`, `*.sqlite`, `*.sqlite-shm`, `*.sqlite-wal`: Ignores log files, SQLite databases, and related files.
* `docker-compose*.yml`, `docker-compose*.yaml`: Ignores Docker Compose configuration files with the `.yml` or `.yaml` extension.
* `.editorconfig`, `.styleci.yml`, `phpunit.xml`, `phpstan.neon`, `Readme.md`: Ignores editor configuration files, style checker configuration files, PHPUnit configuration files, and other miscellaneous files that are not necessary for the build process.

---

### .editorconfig
This is a configuration file for an editor (specifically, the VS Code editor). It specifies several settings that can be used to customize the behavior of the editor.

The file is written in the `.editorconfig` format, which is a standardized way of defining coding styles and conventions for various programming languages. The `root = true` line at the top of the file indicates that this is the root configuration file for an editor.

The rest of the file specifies several settings that can be applied to all files in the project, including:

* `charset`: the character set used for encoding and decoding text (in this case, UTF-8)
* `end_of_line`: the type of line ending used in the file (in this case, "lf" = line feed)
* `indent_size`: the number of spaces used for indentation
* `indent_style`: the style of indentation used (in this case, "space")
* `insert_final_newline`: whether to insert a final newline character at the end of the file
* `trim_trailing_whitespace`: whether to trim trailing whitespace characters from the end of lines in the file

Additionally, the file specifies that certain settings should be applied only to specific file extensions. For example:

* `[*.md]`: apply these settings only to files with the ".md" extension (Markdown files)
* `[*.{yml,yaml}]`: apply these settings only to files with the ".yml" or ".yaml" extension (YAML files)
* `[compose.yaml]`: apply these settings only to the "compose.yaml" file (used for Docker Compose configurations)

Overall, this configuration file is used to define a consistent coding style and conventions for a specific project or team, and can help ensure that all files in the project are written consistently with respect to formatting and whitespace.

---

### app/Actions/Fortify/CreateNewUser.php
Die Datei `app/Actions/Fortify/CreateNewUser.php` enthält eine Implementierung für das Erstellen neuer Benutzer in der Laravel-Anwendung, die das Interface `CreatesNewUsers` implementiert.

Die Klasse `CreateNewUser` verwendet die Validierungsregeln des Traits `PasswordValidationRules`, um den Input zu validieren und zu überprüfen, ob die Anfrage korrekt ist. Wenn der Input gültig ist, wird ein neuer Benutzer erstellt und in der Datenbank gespeichert.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* `CreateNewUser`: Die Klasse implementiert das Interface `CreatesNewUsers` und enthält die Implementierung für das Erstellen neuer Benutzer.
* `Validator::make`: Eine statische Methode des Validator-Facades, die verwendet wird, um den Input zu validieren.
* `Rule::unique`: Eine Validierungsregel, die verwendet wird, um sicherzustellen, dass der eingegebene Wert eindeutig ist und nicht bereits in der Datenbank vorhanden ist.
* `User::create`: Eine statische Methode des Models `User`, die verwendet wird, um einen neuen Benutzer in der Datenbank zu erstellen.

---

### app/Actions/Fortify/PasswordValidationRules.php
This file defines a trait called `PasswordValidationRules` that is used to validate passwords in the Laravel application. The trait contains a single method called `passwordRules`, which returns an array of validation rules for passwords.

The validation rules are:

* `required`: The password field must be present and not empty.
* `string`: The password must be a string value.
* `Password::default()`: This is a built-in Laravel method that sets the default password validation rules, which include requiring at least 8 characters, a mix of uppercase and lowercase letters, numbers, and special characters.
* `confirmed`: The confirmation password field must match the password field.

This trait is used by other parts of the application to validate passwords, such as when creating or updating user accounts. By using this trait, developers can ensure that their users' passwords are properly validated and meet certain standards, which can help improve the security of the application.

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei `app/Actions/Fortify/ResetUserPassword.php` enthält eine Klasse namens `ResetUserPassword`, die das Verwalten von Passwörtern für den Benutzer implementiert. Die Klasse verweist auf die `Laravel\Fortify\Contracts\ResetsUserPasswords`-Schnittstelle und erweitert diese mit dem `PasswordValidationRules`-Trait.

Die Methode `reset` in der Klasse akzeptiert ein Array von Eingaben und einen Benutzer als Parameter. Sie validiert das Passwort mithilfe des `Validator::make()`-Methodenaufrufs mit den Regeln, die in der `$this->passwordRules()`-Methode definiert sind. Wenn die Validierung erfolgreich ist, wird das Passwort des Benutzers geändert und gespeichert.

Die Klasse benötigt keine externen Abhängigkeiten und verwendet nur die standardmäßigen Laravel-Komponenten.

---

### app/Filament/Resources/ContactResource.php
Die Datei "app/Filament/Resources/ContactResource.php" enthält eine Klasse, die die Definition eines Filament-Ressourcenobjekts für die Verwendung in der Filament-Oberfläche darstellt. Die Klasse `ContactResource` erweitert die Klasse `Resource` von Filament und definiert das Modell `App\Models\Contact`, das die Kontaktanfragen repräsentieren soll.

Die Klasse enthält auch Methoden für die Definition der Formular- und Tabellenansicht der Ressource, um die Daten des Kontaktanfrage-Modells zu verwalten und anzuzeigen. Die `form`-Methode definiert das Formular für das Erstellen oder Bearbeiten eines neuen Kontaktanfragens, während die `table`-Methode die Tabellenansicht definiert, um eine Liste der vorhandenen Kontaktanfragen anzuzeigen.

Die Klasse enthält auch eine statische Methode `getPages`, die die Seitendefinitionen für die Ressource enthält. Hier sind zwei Seiten definiert: "index" und "view", die je nach Bedarf erstellt werden können.

---

### app/Filament/Resources/UserResource.php
The file `app/Filament/Resources/UserResource.php` is a PHP class that represents a resource in the Filament admin panel. It defines several static methods and properties that are used to configure the behavior of the resource, such as the model it operates on, its navigation icon and label, and its form and table schemas.

Here's a summary of the main functions of this file:

* `protected static ?string $model = User::class;`: This defines the model class that the resource is responsible for managing. In this case, it's the `User` model.
* `protected static \BackedEnum|string|null $navigationIcon = 'heroicon-o-users';`: This sets the icon that will be displayed in the navigation menu when this resource is selected. It uses the Heroicons library to define a custom icon.
* `protected static ?string $navigationLabel = 'Nutzer';`: This sets the label that will be displayed in the navigation menu when this resource is selected. In this case, it's the German word for "users".
* `public static function form(Schema $schema): Schema;`: This method defines the form schema for creating and editing users. It returns a Filament Form Schema object that includes several fields, such as name, email, status, etc.
* `public static function table(Table $table): Table;`: This method defines the table schema for listing users. It returns a Filament Table object that includes several columns, such as name, email, status, roles, email_verified_at, created_at, etc.
* `public static function getPages(): array;`: This method defines the pages that are available in this resource. In this case, it's two pages: a list page and an edit page.

Overall, this file is defining a Filament resource that allows administrators to manage users in the system, including creating, editing, and listing users.

---

### app/Http/Middleware/TrackPageView.php
This file is a middleware class in Laravel that tracks page views. The `TrackPageView` class extends the `Closure` class and overrides its `handle()` method to track page views.

Here's a breakdown of the functions in this file:

* `__construct()`: The constructor method is used to initialize the middleware class with the necessary dependencies, which in this case are the `App\Models\PageView` model and the `Symfony\Component\HttpFoundation\Response` class.
* `handle()`: This is the handle method that is executed when a request is received by the middleware. It takes two parameters, `$request` and `$next`, which represent the incoming request and the next middleware in the chain, respectively. The method first checks if the request method is GET and if it's not from a bot (using the `isBot()` method) before creating a new page view record using the `firstOrCreate()` method of the `PageView` model. It then increments the visit count for the current page path. Finally, it returns the response from the next middleware in the chain using the `$next` parameter.
* `anonymizeIp()`: This is a private method that takes an IP address as input and returns a new IP address with the last 80 bits zeroed out for IPv6 addresses or the last octet zeroed out for IPv4 addresses. This method is used to anonymize the user's IP address before saving it in the database.
* `isBot()`: This is a private method that takes a user agent string as input and returns true if the user agent matches any of the known bot patterns, which are stored in the `self::BOT_PATTERNS` array. The method uses strtolower() to convert the user agent to lowercase before checking for matching patterns.

Overall, this middleware class is used to track page views and save them to the database, while also anonymizing the user's IP address to protect their privacy.

---

### app/Http/Middleware/VerifyLangdockSignature.php
The code in this file defines a middleware for verifying the signature of requests sent by the Langdock webhook. It does this by checking the `X-Langdock-Signature` header to ensure that it matches the expected value, which is computed using the `secret` key from the configuration and the request's timestamp and content. The middleware also implements replay protection by storing a nonce in the cache for each signature it has seen, which prevents duplicate requests from being processed.

---

### app/Http/Middleware/VerifyMcpToken.php
* This middleware is used to verify the authentication token of requests to the MCP (Mobile Control Panel) API.
* It checks if a valid token was provided in the `Authorization` header, and if it matches the configured value in the `services.mcp.auth_token` config key.
* If no token is provided or it doesn't match the configured value, it returns a 401 Unauthorized response.
* The middleware uses the `hash_equals()` function to compare the tokens securely and avoid timing attacks.

---

### app/Jobs/IngestPaperJob.php
This is a PHP class file for an ingesting paper job. The class name is `IngestPaperJob`, and it implements the `ShouldQueue` interface from Laravel's job system.

The constructor of this class takes in several parameters:

* `paperId`: A unique identifier for the paper being ingested.
* `source`: The source of the paper (e.g. a URL).
* `title`: The title of the paper.
* `text`: The text of the paper.
* `projektId`: An optional ID for the project this paper belongs to.
* `metadata`: An optional array of metadata about the paper.

The `handle` method is where the ingestion process happens. It first splits the text into chunks using a regex pattern, and then sends each chunk to an Ollama API endpoint to get an embedding. If the response from Ollama has a status code other than 200 (OK), it logs the error and fails the job with a runtime exception.

After getting the embeddings for all chunks, the method inserts them into a PostgreSQL database using prepared statements. The `chunkText` method is used to split the text into chunks of fixed size, and the `implode` function is used to convert the array of words back into a string.

This job class also defines some instance properties:

* `tries`: The number of times to retry the job if it fails (defaults to 3).
* `backoff`: The amount of time to wait before retrying the job (defaults to 30 seconds).

---

### app/Jobs/TriggerLangdockAgent.php
The file `app/Jobs/TriggerLangdockAgent.php` is a Laravel job that triggers the Langdock agent to start a new project in the language modeling platform. It implements the `ShouldQueue` and `ShouldBeUnique` interfaces, which means it can be queued for later execution and has a unique identifier that prevents it from running multiple times simultaneously.

The class is constructed with three parameters: `userId`, `projektId`, and `eingabe`. These are used to configure the job and define what language modeling project should be started by the Langdock agent.

The `uniqueId` method is defined, which returns a unique identifier for the job that can be used to prevent multiple instances of the same job from running simultaneously.

The `handle` method is where the job actually starts the language modeling project using the Langdock API. It uses Laravel's built-in HTTP client (`Http`) to make a POST request to the webhook URL configured in the `services.langdock.webhook_url` configuration file or defined on the user account. The request includes the `user_id`, `projekt_id`, and `eingabe` parameters, which are used to start the language modeling project.

If the response from the Langdock API indicates a failure, the job is marked as failed with an error message containing the status code and body of the response. The log also includes details about the failed job, including the unique identifier for the job, the project ID, and the HTTP status code and response body.

---

### app/Livewire/Actions/Logout.php
This file is a Livewire action that logs the current user out of the application. It uses the `Auth` facade to log the user out and then invalidates the session by calling the `invalidate()` method on the `Session` class. The `regenerateToken()` method is also called to regenerate the CSRF token. Finally, the user is redirected to the homepage.

Here are the key points of this file:

* It uses the `Auth` facade to log the user out.
* It invalidates the session by calling the `invalidate()` method on the `Session` class.
* It regenerates the CSRF token by calling the `regenerateToken()` method on the `Session` class.
* It redirects the user to the homepage after logging them out.

The file does not have any external dependencies, as it uses only Laravel's built-in functionality.

---

### app/Mail/ContactConfirmationMail.php
Die Datei `app/Mail/ContactConfirmationMail.php` enthält eine Klasse namens `ContactConfirmationMail`, die von der Basisklasse `Mailable` erbt und die Funktion `envelope()` und `content()` implementiert. Diese Klasse dient zum Erstellen von E-Mails mit Bestätigungen für den Kontaktformular.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* Die Klasse `ContactConfirmationMail` erbt von der Basisklasse `Mailable` und implementiert die Funktionen `envelope()` und `content()`.
* Die Methode `__construct()` akzeptiert einen Parameter vom Typ `Contact`, der eine Instanz der Modell-Klasse `Contact` darstellt.
* Die Methode `envelope()` erstellt ein neues Envelope-Objekt, das die E-Mail-Adresse des Absenders und den Betreff der E-Mail enthält.
* Die Methode `content()` erstellt ein neues Content-Objekt, das die Ansicht `emails.contact-confirmation` verwendet, um den Inhalt der E-Mail zu generieren.
* Die Klasse `ContactConfirmationMail` implementiert auch die Schnittstelle `ShouldQueue`, was bedeutet, dass die E-Mails asynchron in der Warteschlange gespeichert werden und nicht sofort versendet werden.

Die Datei importiert auch die notwendigen Pakete, darunter `Illuminate\Mail\Mailable`, `App\Models\Contact` und `Illuminate\Contracts\Queue\ShouldQueue`. Diese Pakete sind für das Erstellen von E-Mails mit Laravel erforderlich.

---

### app/Mail/ContactInquiryMail.php
The file "app/Mail/ContactInquiryMail.php" contains a PHP class that extends the Laravel Mailable class and is used to send an email with the details of a contact inquiry. The class has a constructor that takes a `Contact` model as a parameter, which it uses to populate the email's content.

The `envelope()` method returns a new instance of the `Envelope` class, which contains information about the email's subject and reply-to address. The `content()` method returns a new instance of the `Content` class, which is used to specify the view that should be used to render the email's content.

The file also uses the `ShouldQueue` interface, which means that the email will be sent asynchronously using a queue. This can help improve the performance of the application by allowing it to continue processing other requests while the email is being sent.

Overall, this file appears to be responsible for sending an email with the details of a contact inquiry.

---

### app/Mail/DeployNotificationMail.php
This file appears to be a class in the `Illuminate\Mail` namespace, which is part of the Laravel PHP framework for building web applications. The class name is `DeployNotificationMail`, and it extends the `Mailable` class provided by the Laravel framework.

The class constructor takes two parameters: `deployedAt` and `appUrl`. These are used to populate the email's content with information about the deployment.

The `envelope()` method is used to specify the email's subject, which in this case is "Linn.Games — Deployment erfolgreich".

The `content()` method returns an instance of the `Content` class, which represents the email's body. In this case, the email's body is a Laravel view named "emails.deploy-notification".

It's worth noting that this code relies on external dependencies, such as the `Illuminate\Mail\Mailable` and `Illuminate\Mail\Mailables\Envelope`, which are provided by the Laravel framework. Additionally, it uses the `view()` method to specify the email's body, which is a feature of the Laravel framework for building dynamic email templates.

---

### app/Policies/ProjektPolicy.php
Die Datei `app/Policies/ProjektPolicy.php` enthält eine Klasse namens `ProjektPolicy`, die verschiedene Autorisierung-Regeln definiert, um zu kontrollieren, welche Aktionen ein Benutzer auf einem Projekt ausführen darf.

Die wichtigsten Funktionen in dieser Datei sind:

* Die `viewAny()`-Methode gibt an, dass jeder Benutzer (immer) sehen darf, ob ein anderer Benutzer ein bestimmtes Projekt erstellt hat.
* Die `view()`-Methode gibt an, dass ein Benutzer nur das Projekt sehen kann, das er selber erstellt hat.
* Die `create()`-Methode gibt an, dass jeder Benutzer (immer) ein neues Projekt erstellen darf.
* Die `update()`-Methode gibt an, dass ein Benutzer nur das Projekt aktualisieren kann, das er selber erstellt hat.
* Die `delete()`-Methode gibt an, dass ein Benutzer nur das Projekt löschen kann, das er selber erstellt hat.

Diese Regeln basieren auf dem `User`-Objekt und dem `Projekt`-Objekt und werden verwendet, um zu bestimmen, ob ein Benutzer eine bestimmte Aktion auf einem Projekt ausführen darf.

Es ist wichtig zu beachten, dass diese Regeln nur für die Anwendung gelten und möglicherweise nicht für alle Anwendungen oder Fälle geeignet sind. Es empfiehlt sich, vor der Verwendung dieser Regeln eine genaue Analyse der Sicherheitsanforderungen und einer gründlichen Überprüfung der Regeln durchzuführen.

---

### app/Providers/AppServiceProvider.php
Die Datei `AppServiceProvider.php` ist Teil des Laravel-Frameworks und enthält die Einstellungen für die Anwendung. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte, die in dieser Datei definiert sind:

* `Gate`: Dies ist eine Klasse, die verwendet wird, um Berechtigungsprüfungen zu durchführen. Sie wird in der Methode `boot()` verwendet, um die Richtlinie für das Modell `Projekt` zu registrieren.
* `RateLimiter`: Diese Klasse wird verwendet, um Limits für Anfragen festzulegen. In dieser Datei werden zwei Endpunkte definiert: `mcp` und `webhooks`. Für jeden Endpunkt wird ein Limit von 60 Anfragen pro Minute festgelegt, wenn der `bearerToken` vorhanden ist, oder 30 Anfragen pro Minute für den IP-Adressen.
* `URL`: Diese Klasse wird verwendet, um die URL-Schema-Einstellungen zu konfigurieren. In dieser Datei wird der Schema-Parameter `https` festgelegt, wenn die Anwendung im Produktionsmodus ausgeführt wird.

Die Datei enthält auch eine Methode `register()`, die verwendet wird, um Services und Provider für die Anwendung zu registrieren. Hier ist jedoch nichts spezielles definiert.

---

### app/Providers/FortifyServiceProvider.php
This is a Laravel service provider that extends the functionality of the Fortify authentication library. The class is responsible for configuring various aspects of the authentication process, such as creating new users and resetting passwords.

The `configureActions` method is used to configure the Fortify actions that are used to handle user registration and password resets. It specifies the `CreateNewUser` and `ResetUserPassword` classes as the actions for these tasks.

The `configureViews` method is used to specify the views that will be used by Fortify to display different aspects of the authentication process, such as login, verification email, two-factor challenge, confirm password, register, reset password, and request password reset link. These views are defined using the `view` helper function.

The `configureRateLimiting` method is used to configure rate limiting for various tasks in the authentication process, such as login and two-factor authentication. It specifies a limit of 5 requests per minute for each task, and uses a throttle key that includes the user ID and IP address.

Overall, this service provider provides a way to customize and extend the functionality of Fortify authentication in a Laravel application.

---

### app/Providers/VoltServiceProvider.php
This is a PHP file that defines a service provider for the Livewire framework in Laravel. The file contains two methods: `register()` and `boot()`.

* The `register()` method is called during the registration of the service provider, and it is used to register any necessary services with the Laravel container. In this case, no additional services are registered.
* The `boot()` method is called after all other service providers have been registered, and it is used to perform any necessary bootstrapping or initialization logic for the Livewire framework. In this case, the `mount()` method of the Volt class is called, which mounts two directories as view paths for Livewire components. The first directory is the default path where Livewire views are stored, and the second directory is a custom path that contains additional views for Livewire components.

---

### app/Providers/Filament/AdminPanelProvider.php
This file is part of the Filament admin panel for a Laravel application. It defines a custom provider for the admin panel, which allows for easy registration of resources and other functionality. The class `App\Providers\Filament\AdminPanelProvider` extends the base `Filament\PanelProvider` class and overrides the `panel()` method to configure the admin panel.

The `panel()` method first calls the `default()` method to define some default settings for the admin panel. It then sets the ID of the panel to "admin", its path to "admin", and defines that the login page should be displayed.

Next, it discovers resources in the `app_path('Filament/Resources')` directory and registers them with the Filament framework using the `discoverResources()` method. It also sets some middleware on the panel, including the `EncryptCookies`, `AddQueuedCookiesToResponse`, `StartSession`, `AuthenticateSession`, `ShareErrorsFromSession`, `VerifyCsrfToken`, `SubstituteBindings`, `DisableBladeIconComponents`, and `DispatchServingFilamentEvent` middleware.

Finally, it defines an authentication middleware that is used to protect the panel from unauthorized access. In this case, it uses the built-in Laravel authentication system, which is represented by the `Authenticate::class` class.

---

### bootstrap/app.php
This is a PHP file that bootstraps an application. It uses the Laravel framework to configure the application and set up middleware, routing, and other components.

Here's a summary of the main functions in this file:

* `return Application::configure(basePath: dirname(__DIR__))` sets up the application configuration with the base path being the directory containing this file.
* `->withRouting()` adds routing to the application, including web, API, and console routes. The `web` route is set to the `routes/web.php`, `api` route is set to the `routes/api.php`, and the `commands` route is set to the `routes/console.php`.
* `->withMiddleware()` adds middleware to the application, including stateful API, trusted proxies, and a page view tracking middleware. The `statefulApi()` function sets up the API for stateful requests, while the `trustProxies()` function sets up the proxy to trust all incoming headers. The `appendToGroup()` function appends the TrackPageView middleware to the "web" group of middleware.
* `->withExceptions()` adds exception handling to the application. However, since this is an empty function, it does not do anything and can be safely removed.
* `->create()` creates the application instance.

---

### bootstrap/providers.php
This file contains an array of service providers that are registered in the application. The first element is the AppServiceProvider class, which provides some basic functionality for the application. The second element is the AdminPanelProvider class, which provides functionality for the Filament admin panel. The third element is the FortifyServiceProvider class, which provides authentication and registration functionality using Laravel Fortify. The fourth element is the VoltServiceProvider class, which provides functionality for working with the volatile memory cache.

---

### bootstrap/cache/.gitignore
This .gitignore file tells Git to ignore all files and subdirectories in the bootstrap/cache directory, with the exception of the .gitignore file itself.

---

### docker/common/nginx/default.conf
This file is an Nginx configuration file used in a Docker container. It sets up the reverse proxy for several microservices, including a PostgreSQL database and a PHP-FPM service.

Here are the main components of this file:

1. Server block: This block specifies the server name `app.linn.games`, which is the hostname that Nginx listens on. It also specifies the listening port as 80.
2. Resolver directive: This directive specifies the DNS resolver to use for resolving service names lazily (not at startup). In this case, it is set to `127.0.0.11`, which is a loopback address that refers to the Docker DNS server running in the container.
3. Root directive: This directive sets the root directory for the server block. It specifies `/var/www/public` as the root directory.
4. Index directive: This directive specifies the index files to use when a directory is requested. In this case, it specifies `index.php` and `index.html`.
5. Logging directives: These directives specify where Nginx should log access and error logs. They are set to `/var/log/nginx/access.log` and `/var/log/nginx/error.log`, respectively.
6. Location blocks: These blocks define the proxying rules for different microservices. Each block defines a `location` directive, which specifies the URL pattern to match, followed by the proxying configuration. For example, the first location block proxies requests starting with `/mcp/` to the `postgres-mcp` service at port 8000.
7. PHP processing: This section defines how Nginx should handle PHP files. It specifies that any `.php` file should be passed to the PHP-FPM service running on port 9000.
8. Deny directives: These directives deny access to hidden files and sensitive files, such as `.env`, `.git`, `.htaccess`, etc.

Overall, this Nginx configuration file sets up a reverse proxy for several microservices in a Docker container, allowing the application to communicate with them securely.

---

### docker/common/nginx/docker-entrypoint.sh
This is a shell script that is used as the entrypoint for an Nginx container. The script does several things:

1. It sets the environment variable `set -e` to exit immediately if any command in the script fails.
2. It checks if a template file exists at `/etc/nginx/templates/default.conf.template`. If it does, it copies the contents of that file into `/etc/nginx/conf.d/default.conf`.
3. It replaces a placeholder for an MCP auth token in the Nginx configuration file with the value of the `MCP_AUTH_TOKEN` environment variable. If `MCP_AUTH_TOKEN` is not set, it warns the user that all requests will be rejected by the MCP endpoint.
4. It executes any additional arguments passed to the script as the command to run in the container.

In summary, this script is used to configure Nginx and replace placeholders in its configuration file with dynamic values from environment variables.

---

### docker/common/nginx/dockerfile
Here is a summary of the file's functions in 10 lines or less:

* It creates an image based on the `nginx:alpine` official Docker image.
* It copies a custom Nginx configuration file from the current directory to the container's `/etc/nginx/nginx.conf` file.
* It copies a site-specific configuration file (`./docker/common/nginx/default.conf`) to the container's `/etc/nginx/conf.d/default.conf` file.
* It copies an entrypoint script (`./docker/common/nginx/docker-entrypoint.sh`) to the container's `/docker-entrypoint.d/30-inject-secrets.sh` file and sets it as executable.
* It copies only static public files (such as `robots.txt`, `favicon`, `index.php`, and image assets) from the current directory to the container's `/var/www/public` directory.
* It sets the working directory of the container to `/var/www/public`.
* It exposes port 80 for HTTP traffic.
* It runs Nginx with a custom configuration file (`-g "daemon off;"`), which disables daemon mode.

This Dockerfile is used to build an Nginx image that serves static files from a directory on the host machine, and can be used as a base image for other Dockerfiles that need to serve static assets.

---

### docker/common/nginx/nginx.conf
This is a basic Nginx configuration file for a Laravel application. It sets up the necessary settings for a PHP-FPM connection, enables gzip compression, and includes additional configurations from other files in the `/etc/nginx/conf.d` directory.

Here are the main features of this file:

* `events`: This block defines the maximum number of worker connections that Nginx can handle. In this case, it is set to 1024.
* `http`: This block defines the settings for the HTTP server. It includes the following directives:
	+ `include`: This directive includes additional configuration files from the `/etc/nginx/conf.d` directory.
	+ `set_real_ip_from`: This directive sets up trusted IP ranges that can be used to determine the client's real IP address. In this case, it includes Docker networks (172.0.0.0/8), private networks (10.0.0.0/8 and 192.168.0.0/16), and upstream proxies.
	+ `real_ip_header`: This directive specifies the HTTP header that contains the client's real IP address. In this case, it is set to `X-Forwarded-For`.
	+ `real_ip_recursive`: This directive enables recursive lookups for the client's real IP address.
	+ `gzip`: This directive enables gzip compression. It sets the maximum compression level to 6 and includes a list of MIME types that can be compressed.
	+ `sendfile`: This directive enables the use of the `sendfile` system call for efficient file transfer.
	+ `keepalive_timeout`: This directive sets the keepalive timeout for connections to 65 seconds.
	+ `client_max_body_size`: This directive sets the maximum size of the client's request body, which is used for file uploads. In this case, it is set to 100 MB.
* `mime.types`: This directive includes a list of MIME types that are commonly used in web applications.
* `default_type`: This directive specifies the default MIME type for files that do not have an explicit MIME type. In this case, it is set to `application/octet-stream`.

---

### docker/common/php-fpm/dockerfile
This Dockerfile is for a PHP application using the `php:8.4-fpm` base image. It consists of three stages:

1. The first stage, named "builder", installs system dependencies and PHP extensions required for the application. It also copies the entire Laravel application code into the container.
2. The second stage, named "production", installs runtime libraries required for PHP extensions and copies the application code and dependencies from the build stage.
3. The third stage, named "testing", is a child of the production stage and adds the ability to run Composer in the testing environment. It also sets the user to `www-data` and copies the full source including tests and dev config.

The Dockerfile uses `COPY --chown=www-data:www-data . /var/www` to copy the entire Laravel application code into the container, and then sets the owner of the copied files to `www-data`. This is necessary because the default user in a PHP FPM container is root, but many Laravel applications assume that the storage directory belongs to the www-data user.

The Dockerfile also uses `RUN composer install --optimize-autoloader --no-interaction --no-progress --prefer-dist` to install all dependencies (including dev/test) in the testing stage. This is because the Laravel framework has a specific folder structure for storing configuration files, which may conflict with the default Composer behavior of creating a `vendor` directory outside of the `public` folder. By using this command, we ensure that Composer creates the `vendor` directory within the `public` folder and that all dependencies are installed correctly.

Finally, the Dockerfile uses `ENTRYPOINT ["/entrypoint.sh"]` to set the entrypoint script for the container, which runs as root for volume setup; php-fpm drops to www-data via pool config. This allows us to run the container with a non-root user and still have the correct permissions for the storage directories.

---

### docker/common/php-fpm/entrypoint.sh
This script is an entrypoint for a Docker container that runs PHP-FPM. It contains several functions to set up the environment, sync built frontend assets, wait for the database to be ready, run migrations in production and staging environments, clear and cache config/routes/views, and execute the main command (php-fpm or queue:work).

The file summary is:

* Sync built frontend assets to a shared volume.
* Wait for the database to be ready before starting PHP-FPM.
* Run migrations in production and staging environments.
* Clear and cache config/routes/views for production.
* Execute the main command (php-fpm or queue:work).

The file uses bash syntax and includes external dependencies such as php and artisan, which are necessary to run PHP-FPM and execute Laravel commands. The file also uses environment variables such as $APP_ENV, which is used to determine whether to run migrations and cache config/routes/views for production.

---

### docker/common/postgres/dockerfile
This Dockerfile installs PostgreSQL 16 and enables the `hypopg` and `pgvector` extensions. It is based on the official PostgreSQL 16 image. The Dockerfile uses `apt-get` to install the necessary packages, and then removes the local package list files (`/var/lib/apt/lists/*`) to save space in the final image.

Here's a summary of the functions performed by this file:

* Installs PostgreSQL 16 and enables the `hypopg` and `pgvector` extensions.
* Based on the official PostgreSQL 16 image.
* Uses `apt-get` to install necessary packages.
* Removes local package list files (`/var/lib/apt/lists/*`) to save space in the final image.

---

### docker/common/postgres/init-extensions.sql
Dies ist eine SQL-Datei, die PostgreSQL-Erweiterungen initialisiert, um die Leistung und Skalierbarkeit des Datenspeichers zu verbessern. Die Datei enthält Commands für die Erstellung von Erweiterungen wie 'hypopg', 'pg_stat_statements' und 'vector'. Es gibt keine externe Abhängigkeit oder Imports in dieser Datei.

---

### public/index.php
This file is the entry point of a Laravel application, responsible for handling incoming HTTP requests and executing the appropriate actions based on the URL path. It defines some constants and requires Composer's autoloader, as well as defining the `LARAVEL_START` constant to track the start time of the application.

The file then loads the bootstrap/app.php file, which sets up the Laravel framework and creates an instance of the Application class. The `$app->handleRequest()` method is then called with a Request object created from the incoming HTTP request. This method will execute the appropriate controller action based on the URL path and other parameters in the request.

The file also checks for a maintenance mode file in the storage/framework directory, which can be used to signal that the application is currently undergoing maintenance or upgrades. If this file exists, it will include it and exit.

In summary, this file sets up the Laravel framework and handles incoming HTTP requests by executing the appropriate controller action based on the URL path.

---

### public/robots.txt
Robots.txt is a text file that provides instructions to web robots (such as search engine crawlers) about which pages or URLs they should or should not access.

---

### public/.htaccess
File Summary: This .htaccess file is used to configure the rewrite engine for the web application. It handles requests and sends them to the front controller, which is the index.php file in this case. The file also handles authorization and XSRF-token headers, and redirects trailing slashes if necessary.

Here are the main functions of the .htaccess file:

* Enable the rewrite engine.
* Handle authorization and XSRF-token headers.
* Redirect trailing slashes to the front controller.
* Send all other requests to the front controller.

---

### resources/css/app.css
This file is a CSS file used in a Laravel project. It uses the TailwindCSS framework and defines some custom variables and styles for the application's dark mode theme.

Here are the main functions of this file:

* Imports the TailwindCSS framework.
* Defines custom variables for the theme, including colors and fonts.
* Sets the base styles for the application in the `base` layer.
* Provides a dark mode variant for the theme.

The file also imports some other CSS files from the Laravel project's views folder and the Laravel framework's pagination views. It does not have any external dependencies or APIs that need to be accessed.

---

### storage/app/.gitignore
Die Datei `.gitignore` ignoriert alle Dateien und Verzeichnisse in der `storage/app`-Verzeichnis, mit Ausnahme von `private/` und `public/` und selbst.

---

### storage/app/public/.gitignore
Definiert Dateien, die nicht in das Git-Repository hinzugefügt werden sollen.

---

### .github/copilot-instructions.md
{
  "file_summary": "This file contains instructions and guidelines for the Copilot team to follow when working on the app.linn.games project."
}

### Models:
- UUID as primary key for domain models (Webhook, ChatMessage, Project, Phase, etc.) via HasUuids trait
- Standard auto-increment id for User (Fortify compatibility)
- Custom Timestamps: Research models use created and updated at
- Activity Logging: Spatie\Activitylog\Models\Concerns\LogsActivity
- Password fields: always 'hashed' Cast, in $hidden and $fillable

### Volt / Livewire:
- Inline components in Blade files: new class extends Component { ... }
- Routing via Volt::route('path', 'component.name')
- No Alpine.js — only Livewire directives (wire:model, wire:click, wire:loading)
- Redirect with $this->redirect(route(...), navigate: true)

### Filament:
- Schema-based forms/tables
- DateTimes formatted as d.m.Y H:i
- NavigationLabel on German

### Routing:
- Web: Dot-notation (recherche.projekt, profile.edit)
- API: Explcit POST/GET, no Route::resource()
- Middleware: ['auth', 'verified'] for protected routes
- Custom Middleware: VerifyLangdockSignature, VerifyMcpToken

### Database:
- PostgreSQL is the production DB — use database features (Enums, pgvector, Extensions)
- Migrations with new class extends Migration
- pgsql-specific migrations need guard: if (DB::getDriverName() !== 'pgsql') return;
- SQLite in-memory tables for tests (separate migration 2026_03_31_99999)
- Foreign Keys: foreignId('user_id')->constrained()->cascadeOnDelete()
- New Enums: DB::statement("CREATE TYPE ... AS ENUM (...)") with DROP TYPE IF EXISTS ... CASCADE in down()

### Testing:
- Pest: test('description', function () { ... }) syntax
- SQLite in-memory for tests — no PostgreSQL features in tests expect
- User Factory: User::factory()->withoutTwoFactor()->create()
- Volt Testing: Volt::test('component.path')->set('prop', 'val')->call('method')
- Queue Testing: Queue::fake() before requests that dispatch jobs (particularly with QUEUE_CONNECTION=sync in CI)
- Config Override: Config::set('services.langdock.secret', 'test-secret')
- New Features require feature tests, bug fixes require regression tests

### Docker:
- 10 Services: web, php-fpm, php-cli, php-test, queue-worker, postgres, redis, postgres-mcp, ollama, mcp-paper-search
- php-test is a baked image — after code changes outside of tests/, phpunit.xml, .env must docker compose build php-test run
- Local tests: docker compose run --rm php-test vendor/bin/pest
- Port: nginx on 6481:80

### Git Conventions:
- Branches: feature/, fix/, docs/, refactor/
- Flow: feature/* → develop → main
- Squash Merge preferred
- Commit Messages: Conventional Commits (feat, fix, docs, refactor)
- Migrations in separate commits before code changes

### Workflow:
- Document completed changes thoroughly — include relevant architecture decisions into the Copilot Instructions
- Push changes regularly to GitHub (Conventional Commits, feature branch → develop)
- After each completed task: verify short commit stand and if necessary commit again

### Security:
- Webhooks: HMAC-SHA256 Signature + Timestamp validation (5 min tolerance) + Cache-Nonce (Replay-Protection)
- MCP: Bearer Token Auth via Nginx Proxy
- Ollama: Token Auth via Nginx /ollama/ Location Block
- Passwords: 'hashed' Cast, never in plain text logged or returned

---

### .github/instructions/app_linn_games_custom.instructions.md
Die Datei enthält eine kurze Beschreibung der Funktionen der Datei und wichtige Klassen, Funktionen oder Endpunkte. Es gibt keine Bewertung oder Fehlerbeurteilung in der Datei. Stattdessen wird die Bedeutung der verschiedenen Teile der Datei erläutert.

Die wichtigsten Klassen sind die Modelle, wie z. B. User, Contact und Webhook. Die Funktionen sind die Controller-Klassen, die die Anforderungen von Benutzern verarbeiten. Die Endpunkte sind die Routen, über die auf die Anwendungslogik zugegriffen wird.

Es gibt auch einige wichtige externe Abhängigkeiten in der Datei, wie z. B. Imports, APIs und DB-Zugriffe. Es ist jedoch nicht klar, ob diese Abhängigkeiten erforderlich sind oder nur für die Veranschaulichung der Funktionsweise der Datei verwendet werden.

Insgesamt ist die Datei eine kurze Beschreibung der Funktionen einer Anwendung und gibt einen Überblick über die wichtigsten Teile der Anwendung, aber es fehlt ein Detail-Beschreibungs.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker Dev-Workflow for app.linn.games: 10-line description

---
