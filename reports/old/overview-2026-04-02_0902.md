---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T09:02:47.483324
model: codellama:latest
run_id: codellama:latest
mode: overview
files_total: 244
errors: 0
run_time_s: 1829.7
---

# Funktions-Übersicht — 2026-04-02_0902

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 244
- **Fehler:** 0
- **Laufzeit:** 1829.7s

## api

### app/Http/Controllers/ContactController.php
This is a PHP file containing a class named `ContactController` that extends the `Controller` class from the Laravel framework. The controller contains a single method named `store`, which takes in a `Request` object and validates its data against a set of rules. If the validation passes, it creates a new instance of the `Contact` model and saves it to the database. It then sends two emails using the `Mail` facade: one to an email address specified in the code (`info@linn.games`), and another to the user's email address, with the data from the contact form. The method returns a JSON response indicating whether the request was successful or not, along with a message.

The class also uses the `App\Mail` namespace to access two classes named `ContactConfirmationMail` and `ContactInquiryMail`. These classes are likely to be used for sending emails related to contact form submissions.

The file imports several external dependencies:

* The `Illuminate\Http\Request` class, which is a part of the Laravel framework and provides a way to access HTTP request data in a structured way.
* The `App\Models\Contact` class, which is likely to be used for storing contact form submissions in the database.
* The `Mail` facade, which provides a simple API for sending emails.
* The `Illuminate\Support\Facades\Mail` class, which is a part of the Laravel framework and provides a way to send emails using the Mail facade.

Overall, this file appears to be a controller for handling contact form submissions in a web application built with the Laravel framework.

---

### app/Http/Controllers/Controller.php
This is a PHP abstract class named Controller that serves as the base class for all HTTP controllers in the application. It provides a number of methods and properties that can be used to handle HTTP requests and responses.

---

### app/Http/Controllers/DsgvoController.php
Die Datei `app/Http/Controllers/DsgvoController.php` ist eine PHP-Datei, die einen Controller für die Datenverarbeitung unterhält. Der Controller enthält zwei Funktionen:

1. `export()` - Diese Funktion exportiert den Benutzer-Kontenstand und alle zugehörigen Informationen in JSON-Format. Sie wird durch eine Anfrage über HTTP aufgerufen und gibt eine JSON-Antwort zurück.
2. `deleteAccount()` - Diese Funktion löscht das Benutzerkonto und alle verknüpften Daten. Sie wird durch eine Anfrage über HTTP aufgerufen und gibt eine JSON-Antwort zurück.

Die wichtigsten Klassen, die in dieser Datei verwendet werden, sind:

* `App\Http\Controllers\Controller` - Der Controller ist eine grundlegende Klasse, die alle Controllere von Laravel ableitet.
* `Illuminate\Http\Request` - Diese Klasse stellt eine Anfrage dar und enthält Informationen wie z.B. den Benutzer, der die Anfrage gesendet hat, sowie die Daten, die in der Anfrage enthalten sind.
* `Illuminate\Support\Facades\Auth` - Diese Klasse ist ein Facade-Objekt, das den Authentifizierungsdienst von Laravel darstellt und es ermöglicht, den aktuell angemeldeten Benutzer zu authentifizieren oder abzumelden.
* `Illuminate\Support\Facades\Response` - Diese Klasse ist ein Facade-Objekt, das die Antwort auf eine Anfrage darstellt und es ermöglicht, HTTP-Statuscodes, Inhalte und Kopfzeilen zu definieren.

---

### app/Http/Controllers/LangdockWebhookController.php
This is a PHP file containing the code for a webhook controller in Laravel. The controller handles incoming HTTP requests and processes them accordingly.

The file imports several classes from the Laravel framework:

* `Illuminate\Http\Request`: Represents an HTTP request.
* `App\Jobs\TriggerLangdockAgent`: A job class that dispatches a task to trigger the Langdock agent.
* `Illuminate\Http\JsonResponse`: Represents a JSON response object.

The controller defines a single method, `handle()`, which takes an incoming request as an argument and validates its contents. If the validation is successful, it creates a new job instance of `TriggerLangdockAgent` with the user ID, project ID, and input from the request, and dispatches it to the queue. The response returned by this method is a JSON object with a single key-value pair, where the key is "status" and the value is "queued".

This controller is part of an API that listens for webhook requests from a third-party service, and processes them accordingly. The job class `TriggerLangdockAgent` is responsible for triggering the Langdock agent with the given user ID, project ID, and input. The validation is performed to ensure that the request contains the necessary data and that it can be processed correctly.

---

### app/Http/Controllers/PaperRagController.php
The `PaperRagController` class is responsible for handling requests related to paper embedding and retrieval. It contains two methods: `ingest` and `search`.

The `ingest` method takes a JSON payload with the following structure:
```json
{
    "paper_id": "<string>",
    "source": "<string>",
    "title": "<string>",
    "text": "<string>",
    "projekt_id": "<uuid|null>",
    "metadata": "<array|null>"
}
```
It validates the input data and then dispatches an `IngestPaperJob` job to process the incoming paper. The job is responsible for embedding the paper text and storing it in the database.

The `search` method takes a JSON payload with the following structure:
```json
{
    "q": "<string>",
    "projekt_id": "<uuid|null>",
    "max_results": "<integer>"
}
```
It validates the input data and then sends a request to an external service (ollama) to retrieve the embedding of the search query. It then queries the database for papers that have similar embeddings, based on the incoming project ID and search query. The resulting rows are returned in the JSON response.

The controller uses several external dependencies, including:

* `Illuminate\Http\Request` to handle HTTP requests and validate input data
* `Illuminate\Support\Facades\DB` to interact with the database and retrieve paper embedding information
* `Illuminate\Support\Facades\Http` to send requests to external services (such as ollama)
* `App\Jobs\IngestPaperJob` to handle the incoming paper ingestion process

---

### routes/api.php
This file defines API routes for an application that uses Laravel. It includes several routes to handle incoming webhooks from third-party services and to perform various tasks such as ingesting data and searching for specific information.

---

### routes/console.php
This file contains two Artisan commands for managing the Laravel application: `inspire` and `deploy:notify`.

* The `inspire` command displays an inspiring quote using the `Illuminate\Foundation\Inspiring` class. It does not have any external dependencies.
* The `deploy:notify` command sends a deploy notification email to the address `bene@linn.games` using the `Illuminate\Support\Facades\Mail` class and the `App\Mail\DeployNotificationMail` mailable class. It also uses the `now()` function from the `Carbon\Carbon` class to get the current date and time in a format suitable for the deploy notification email.

The file does not have any external dependencies beyond the Laravel framework, such as the `Inspiring` and `Mail` classes.

---

### routes/web.php
This is a Laravel routes file, which defines the URL endpoints for the application. The file imports several classes and facades from the Laravel framework, including `Route` and `Volt`.

The file contains several route definitions, each of which corresponds to a specific URL endpoint. For example, the first route definition defines a GET request to `/`, which will return the welcome view when accessed. The second route definition defines a POST request to `/contact`, which will store the contact form data in the database and redirect to the homepage.

The file also contains several view routes, such as `Impressum.html` and `AGB.html`, which will render the respective views when accessed. Additionally, it contains several route definitions that use Volt to define routes for updating user settings, exporting DSGVO data, and deleting an account.

The file also includes middleware definitions, such as `auth` and `verified`, which are used to require authentication and verification of the user's email address before accessing certain routes.

---

## config

### docker-compose.dev.yml
This is a Docker Compose file for the development environment. It contains configuration options for the `web` service, which is used to run the application in a container. The most important part of this file is the `ports` section, which specifies that the container should map port 80 on the host to port 6480 inside the container. This allows the user to access the application using `http://localhost:6480`.

The file also includes a comment that overrides the default port for the nginx service in the main Docker Compose file (`docker-compose.yml`) to use port 6481 instead of 6480. This is done by specifying the `ports` section again with the new value.

This file can be used when running the application in a development environment, where it may be necessary to have the application accessible on a specific port for debugging or testing purposes.

---

### docker-compose.yml
This is a `docker-compose.yml` file that defines the services and networks for a multi-container application. The file includes the following elements:

* `services`: A list of the services that make up the application, each with its own configuration.
* `build`: Specifies how to build the Docker images for each service. In this case, each service uses a Dockerfile in the same directory as the `docker-compose.yml` file.
* `restart`: Indicates whether to restart the service if it fails or not.
* `volumes`: Specifies the volumes that should be mounted into the container for each service. In this case, there are three volumes defined: `linn-postgres-data`, `linn-storage-production`, and `linn-papers-data`.
* `environment`: Specifies the environment variables that should be passed to the containers as environment variables. In this case, there are several environment variables defined for each service.
* `ports`: Specifies which ports should be exposed from the container to the host machine. In this case, there is one port exposed for each service.
* `depends_on`: Indicates that one service depends on another service being up and running before it starts. This is useful for services that need to communicate with each other.
* `networks`: Defines the networks that should be created for this application. In this case, there is only one network defined: `linn-network`.
* `volumes`: Defines the volumes that should be created for this application. In this case, there are three volumes defined: `linn-postgres-data`, `linn-storage-production`, and `linn-papers-data`.

Overall, this file defines a complex set of services and networks that make up a multi-container application.

---

### .env.example
This file is a configuration file for a Laravel application, specifically the `.env.example` file. It contains various environment variables that are used by the application to configure its behavior and connect to external services such as databases and email providers.

The file includes settings for:

* The application's name, which is set to "Laravel" in this case.
* The application's environment, which is set to "local".
* The application's key, which is empty.
* Whether the application should be debugged or not, which is set to true.
* The application's URL, which is set to "http://localhost".
* The application's locale and fallback locale, which are both set to "en".
* The application's Faker locale, which is set to "en_US".
* The driver for the maintenance mode feature, which is set to "file".
* The store for the session, which is set to "redis".
* The lifetime of the session, which is set to 120.
* Whether the session should be encrypted or not, which is set to false.
* The path for the session, which is set to "/".
* The domain for the session, which is set to null.
* The driver for the broadcasting feature, which is set to "log".
* The filesystem disk used by the application, which is set to "local".
* The connection for the queue, which is set to "redis".
* The store for the cache, which is set to "redis".
* The hostname and port for the Memcached server, which are both set to "127.0.0.1" and 6379 respectively.
* The client used for Redis connections, which is set to "predis".
* The hostname, username, password, and port for the PostgreSQL database connection, which are all set to "postgres", "postgres", "change-me", and "5432" respectively.
* The driver for the AWS S3 bucket, which is set to null.
* The default region for the AWS S3 bucket, which is set to "us-east-1".
* The name of the AWS S3 bucket, which is set to null.
* Whether to use path style endpoint or not, which is set to false.
* The API key for the Langdock AI, which is empty.
* The webhook URL for the Langdock AI, which is empty.
* The secret for the webhook URL, which is empty.
* The username and password for the Langdock Agent's database connection, which are both set to "postgres" and "change-me" respectively.
* The name of the application used by Vite, which is set to "${APP_NAME}".

Overall, this file contains various settings that configure the behavior of the Laravel application and its connections to external services.

---

### config/app.php
This is the configuration file for a Laravel application. It contains various options and settings that can be used to customize the behavior of the application. Here's a summary of the important parts:

* `name`: The name of the application, which will be displayed in various places such as error messages or debug information. This is set using the `APP_NAME` environment variable.
* `env`: The current environment that the application is running in. This should be set to either `production`, `staging`, or `local`.
* `debug`: Whether or not the application should display detailed error messages when an exception occurs. This should be disabled in production environments.
* `url`: The URL of the application, which will be used for generating URLs by the framework. This is set using the `APP_URL` environment variable.
* `timezone`: The default timezone that the application should use for displaying dates and times. This should be set to a valid PHP timezone identifier, such as "UTC" or "America/New_York".
* `locale`: The default locale that the application should use for formatting dates and numbers. This should be set to a valid ISO 639-1 language code, such as "en" or "de".
* `fallback_locale`: The fallback locale to use when a translation is not available for the current locale. This should be set to a valid ISO 639-1 language code, such as "en" or "de".
* `faker_locale`: The locale that Faker should use for generating fake data in seeding and testing. This should be set to a valid ISO 639-1 language code, such as "en" or "de".
* `cipher`: The cipher algorithm used by the application for encrypting sensitive values, such as passwords. This should be set to a valid cipher string, such as "AES-256-CBC".
* `key`: A random string that is used to encrypt and decrypt sensitive values in the application. This should be set to a 32-character string using the `php artisan key:generate` command.
* `previous_keys`: An array of previous keys that have been generated by the application, which can be used for fallback or rotating the encryption key.
* `maintenance`: The driver and store to use for controlling the application's maintenance mode status. The available drivers are "file" and "cache". The available stores are "database" and "redis".

---

### config/auth.php
This file is part of a Laravel application and contains authentication configuration options. It defines the default authentication guard, password reset broker, and user providers for the application. The file also includes configurations for password resets, such as the expiration time and throttle settings.

The important classes and functions in this file are:

* `defaults`: Defines the default authentication guard and password reset broker for the application.
* `guards`: Defines the available authentication guards for the application.
* `providers`: Defines the available user providers for the application, which define how users are retrieved from the database or other storage system used by the application.
* `passwords`: Defines the password reset configurations for the application, including the token expiration time and throttle settings.
* `password_timeout`: Defines the number of seconds before a password confirmation window expires and users are asked to re-enter their password via the confirmation screen.

The file also imports external dependencies such as the `.env` file, which is used to store environment variables that can be used throughout the application, and the `App\Models\User` class, which is used to interact with the user table in the database.

---

### config/cache.php
Die Datei `config/cache.php` ist eine Einstellungsdatei für das Caching in Laravel. Sie definiert die Konfigurationen für den Cache und stellt Optionen zum Verwenden von verschiedenen Cache-Drivern zur Verfügung.

In der Datei werden die folgenden Funktionen definiert:

* Die Standard-Cache-Store (default)
* Die verschiedenen Cache-Stores, darunter "array", "database", "file", "memcached", "redis", "dynamodb" und "failover"
* Der Cache Key Prefix

Die Datei importiert auch die `Str`-Klasse von Laravel, um den Cache Key Prefix zu generieren.

---

### config/cookie-consent.php
This file contains settings for the cookie consent dialog in the application. The 'enabled' setting determines whether the dialog is displayed or not. If enabled, the user can agree to accept the conditions and a cookie will be set with the specified name and lifetime. Default values are used if no other values are provided.

---

### config/database.php
This is a Laravel configuration file for the database, located at `config/database.php`. It defines various options and settings related to the application's database connection.

The file starts by defining the default database connection name using the `default` key. The value of this key can be set using the `DB_CONNECTION` environment variable. If no value is provided, the default is set to `'sqlite'`.

Next, the file defines various database connections that Laravel supports. Each connection has a unique name and configuration options, such as the database host, port, username, password, and other settings. The `mysql`, `mariadb`, `pgsql`, and `sqlsrv` connections are supported out of the box by Laravel.

The `migrations` key defines an array with a single element that specifies the name of the migration repository table. The `update_date_on_publish` option is set to `true`, which means that the `updated_at` timestamp will be updated every time a new migration is published.

Finally, the file defines Redis databases for caching and queuing using the `redis` key. These settings can be configured using environment variables or by providing configuration options directly in the file.

Overall, this file provides a convenient way to manage various database connections and Redis caches for an Laravel application.

---

### config/filesystems.php
This file is a configuration file for Laravel's filesystem management system, which allows you to manage files and directories on your server. The file defines the following:

* The default filesystem disk that should be used by the framework (local)
* The available filesystem disks that can be used (local, public, s3)
* The configuration for each of these disks, including the driver, root path, and URL for the "public" disk.
* The symbolic links that will be created when the `storage:link` Artisan command is executed.

The file also includes environment variables that can be used to configure the disks, such as AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY for the s3 disk.

In summary, this file configures the filesystem management system in Laravel, allowing you to manage files and directories on your server with ease.

---

### config/fortify.php
This file is a configuration file for the Fortify package in a Laravel application. It contains various settings and options that can be used to customize the behavior of the package.

Here are some of the key features and options in this file:

* `guard`: This option specifies which authentication guard Fortify will use while authenticating users. By default, it is set to `'web'`.
* `passwords`: This option specifies which password broker Fortify can use when a user is resetting their password. The value should correspond with one of your password brokers setup in your "auth" configuration file.
* `username`: This option defines which model attribute should be considered as the application's "username" field. Typically, this might be the email address of the users but you are free to change this value here. By default, it is set to `'email'`.
* `lowercase_usernames`: This option specifies whether usernames should be lowercased before saving them in the database, as some database system string fields are case sensitive. You may disable this for your application if necessary. By default, it is set to `true`.
* `home`: This option defines which path Fortify will use when a user is authenticated successfully and the application is used by the user. By default, it is set to `'/dashboard'`.
* `prefix`, `domain`, `middleware`: These options specify the prefix, domain, and middleware that Fortify will assign to all the routes it registers with the application. If necessary, you may change these values here.
* `limiters`: These options specify which rate limiter Fortify should use for throttling logins to five requests per minute for every email and IP address combination. By default, it is set to `'login'` and `'two-factor'`.
* `views`: This option specifies if the routes returning views should be disabled as you may not need them when building your own application. By default, it is set to `true`.
* `features`: These options specify which Fortify features are optional. You may disable the features by removing them from this array. By default, all of these features are enabled.

---

### config/logging.php
This is a configuration file for logging in a Laravel application. It defines the different log channels and their respective configurations, including the handler, level of logging, and processors. The file also includes some pre-defined channels such as `stack`, `single`, `daily`, `slack`, `papertrail`, `stderr`, `syslog`, `errorlog`, and `null`.

Here are the main functions of this file:

* It defines the default log channel that is used to write messages to your logs. This can be changed using the `LOG_CHANNEL` environment variable.
* It provides a way to configure the different log channels, including the handler, level of logging, and processors.
* It includes some pre-defined channels such as `stack`, `single`, `daily`, `slack`, `papertrail`, `stderr`, `syslog`, `errorlog`, and `null`.
* It provides a way to set the log level for each channel, which can be changed using the `LOG_LEVEL` environment variable.
* It includes some additional configuration options such as `ignore_exceptions`, `days`, `url`, `username`, `emoji`, `connectionString`, and `formatter`.

---

### config/mail.php
Diese Datei ist ein Konfigurations-File für Laravel, das die Einstellungen für den Mailversand im Framework verwaltet. Hier sind die wichtigsten Funktionen und Klassen dieser Datei:

* `default`: Legt den Standard-Mailer fest, der für den Versand von E-Mails verwendet werden soll, wenn kein anderer Mailer explizit angegeben wird.
* `mailers`: Enthält eine Liste aller verfügbaren Mailer im System, die durch their respective settings konfiguriert werden können. Dazu gehören SMTP, Sendmail, Mailgun, Ses, Postmark und andere.
* `from`: Legt die globale E-Mail-Adresse fest, die für den Versand von E-Mails verwendet wird, wenn keine explizite Absenderadresse angegeben ist.

Zusätzlich zu diesen Funktionen und Klassen enthält diese Datei auch External Dependencies wie beispielsweise SMTP, Sendmail und Postmark, die für den Mailversand benötigt werden können.

---

### config/queue.php
The file config/queue.php contains the configuration for the queue system in a Laravel application. The file sets up the default queue connection name, connections to different queue drivers, and job batching options. It also defines the behavior of failed queue jobs logging.

Here are the main functionalities of the file:

* Default Queue Connection Name: This sets the default queue connection that will be used by Laravel for queued tasks. The value can be changed using the QUEUE_CONNECTION environment variable.
* Queue Connections: This section defines the connections to different queue drivers that are supported by Laravel. Each driver has its own configuration options, such as the database or Redis connection for the 'database' and 'redis' drivers respectively. The 'deferred', 'background', and 'failover' drivers are also defined in this section.
* Job Batching: This section defines the behavior of job batching in Laravel. It sets up the database and table that store job batching information, which can be changed using the DB_CONNECTION and QUEUE_BATCHING environment variables.
* Failed Queue Jobs: This section defines the behavior of failed queue jobs logging in Laravel. The driver used for storing failed jobs can be changed using the QUEUE_FAILED_DRIVER environment variable, which supports several drivers such as 'database-uuids', 'dynamodb', 'file', and 'null'.

In summary, config/queue.php is an important configuration file that sets up the queue system in a Laravel application. It defines the default queue connection, connections to different queue drivers, job batching options, and failed queue jobs logging behavior.

---

### config/services.php
This file contains a configuration for various third-party services used by the application. The `postmark` and `resend` keys are used to configure the Mailgun and Resend services, respectively. The `ses` key is used to configure the AWS Simple Email Service (SES). The `slack` key is used to configure the Slack service, which is used for sending notifications to a Slack channel. The `langdock` key is used to configure the Langdock service, which is used for language detection and translation. Finally, the `mcp` key is used to configure the MCP service, which is used for converting text from one language to another.

The file imports environment variables using the `env()` function, which loads environment variables from a `.env` file or other configuration files. It also uses the `config()` function to load configuration options from other configuration files.

---

### config/session.php
This file contains configuration options for the session driver used by the Laravel framework. The following are the main features of this file:

* `driver`: specifies the default session driver to be utilized for incoming requests. By default, it is set to "database".
* `lifetime`: determines the number of minutes that a session can remain idle before expiring. By default, it is set to 120 minutes (2 hours).
* `expire_on_close`: when set to true, sessions will expire immediately upon closing the browser.
* `encrypt`: enables session data encryption for all incoming requests. It is false by default.
* `files`: specifies the storage location where session files are placed on disk. By default, it is set to "storage/framework/sessions".
* `connection`: specifies the database connection that should be used to manage sessions when using a database or redis session driver.
* `table`: specifies the table to be used for storing sessions when using a database session driver. By default, it is set to "sessions".
* `store`: specifies the cache store that should be used to store session data between requests when using a cache-driven session backend like memcached or redis.
* `lottery`: specifies the probability of manually sweeping the session storage location to get rid of old sessions upon a given request. By default, it is set to [2, 100].
* `cookie`: specifies the name of the session cookie that is created by the framework. It is typically set to "APP_NAME-session" and can be changed using environment variables.
* `path`: specifies the path for which the session cookie will be regarded as available. By default, it is set to "/".
* `domain`: specifies the domain and subdomains that the session cookie is available to. By default, it is not set.
* `secure`: enables the "Secure" flag on cookies when using a secure connection (HTTPS). It is true by default.
* `http_only`: determines whether JavaScript can access the value of the session cookie and the cookie will only be accessible through the HTTP protocol. By default, it is set to true.
* `same_site`: specifies how cookies behave when cross-site requests take place, and can be used to mitigate CSRF attacks. By default, it is set to "lax" to permit secure cross-site requests.
* `partitioned`: enables the "Partitioned" flag on cookies when using a cache-driven session backend like memcached or redis. It is false by default.

---

### docker/common/php-fpm/php-dev.ini
This file provides some custom PHP settings for the development environment in a Docker container using the `opcache` extension. The file is loaded as an override configuration snippet after all other configuration snippets in the `php-fpm` service, but only if it is mounted as a bind mount in the `docker-compose.override.yml` file.

The main settings in this file are:

* `opcache.validate_timestamps = 1`: Enables the validation of the timestamps of PHP files to ensure that they have not been modified outside of the PHP runtime. This helps to detect changes to the codebase that may break the application.
* `opcache.revalidate_freq = 0`: Sets the frequency at which the opcache will revalidate the timestamps of PHP files. A value of 0 disables this feature, as it is not necessary in a development environment.
* `display_errors = On`: Enables the display of error messages when an error occurs during the execution of a PHP script. This helps to identify and debug errors more easily.
* `error_reporting = E_ALL`: Sets the level of error reporting for the PHP script. The `E_ALL` constant enables all error levels, including notices, warnings, and fatal errors.

Overall, this file provides some customized PHP settings that help to improve the development experience in a Docker container using the `opcache` extension.

---

### docs/api.yaml
Die Datei `docs/api.yaml` enthält eine API-Definition, die die Endpunkte und ihre Funktionen beschreibt. Hier sind die wichtigsten Klassen, Funktionen oder Endpunkte:

* `/api/user`: Gibt den authentifizierten Benutzer zurück. Dies ist ein GET-Endpunkt, der von Sanctum (ein Laravel-Framework) gesichert wird.
* `/api/webhooks/langdock`: Ein POST-Endpunkt, der von einem Webhook des Langdock-KI-Agents aufgerufen wird. Er startet einen TriggerLangdockAgent-Job in der Redis-Queue. Die Validierung erfolgt über eine HMAC-SHA256 Signatur, die mit dem Langdock-Secret verglichen wird.
* `/api/papers/ingest`: Ein POST-Endpunkt, der einen Paper-Text aufnimmt und ihn in Chunks aufteilt. Der Text wird von Ollama (nomic-embed-text) als Vektoren in pgvector gespeichert. Der Job wird von MCP (ein Laravel-Framework) gesichert.
* `/api/papers/rag-search`: Ein GET-Endpunkt, der eine Vektor-Ähnlichkeitssuche durchführt. Die Suche erfolgt über Cosine Distance und wird von MCP gesichert.

Die Datei beschreibt auch die Security-Schemes, die für die Endpunkte verwendet werden:

* `sanctum`: Ein HTTP Bearer Token, der von Sanctum verwendet wird, um den Zugriff auf `/api/user` zu sicherstellen.
* `mcpBearer`: Ein weiteres HTTP Bearer Token, das für die Sicherung von `/api/papers/ingest` und `/api/papers/rag-search` verwendet wird.
* `langdockHmac`: Ein API Key, der für die Überprüfung der HMAC-SHA256 Signaturen verwendet wird, die von Langdock-Webhooks gesendet werden.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
This is a bug report template for GitHub Issues, which is used to track and manage bugs in the project. The template includes several fields that are intended to help developers and maintainers understand the issue better and provide more accurate solutions. Here's a breakdown of each field:

1. **Description**: A brief description of the bug, including what is happening and what the expected behavior should be.
2. **Steps to Reproduce**: A list of steps that can be taken to reproduce the issue, starting from the initial state of the system and ending with the observed error. This helps developers and maintainers understand how to reproduce the bug and test potential fixes.
3. **Severity**: The severity of the bug is determined by its impact on the project's functionality and usability. Options include "Critical (Data loss / Security)", "High (Core function affected)", "Medium (Workaround available)", and "Low (Cosmetic / Edge case)".
4. **Affected Areas**: A list of files, routes, or services that are affected by the bug. This information helps developers and maintainers understand which parts of the project need to be fixed.
5. **Acceptance Criteria**: A list of criteria that must be met before the issue is considered resolved. This could include a specific version number or a set of test cases that must pass before the issue is considered fixed.

Overall, this template provides a structured way to report bugs and track their status in the project's workflow.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
This is a GitHub issue template for feature requests. The template includes several sections that are relevant to the development process, such as the issue title, description, and labels. It also includes fields for providing more detailed information about the requested feature, including its purpose, motivation, proposed solution, affected areas, potential breaking changes, and acceptance criteria.

The "name" field specifies the name of the template, which is "Feature Request" in this case. The "description" field provides a brief description of the template, which is useful for identifying the purpose of the template.

The "labels" section specifies the labels that are applied to the issue when it's created. In this case, the label "enhancement" is used to identify issues related to feature requests.

The "body" section includes several fields that provide more detailed information about the requested feature. The first field is a markdown field that provides guidance on how to write a clear and concise description of the feature request.

The next field is a textarea field for providing a summary of the requested feature. This field is labeled "Zusammenfassung" in German, which means "Summary" in English. The summary should provide a brief overview of what the requested feature does and why it's important.

The motivation field provides information about why the feature is needed. For example, if the feature request is related to a specific problem or issue, this field can be used to describe that problem.

The solution field proposes a possible implementation for the requested feature. This field should provide enough detail to understand how the feature will work and what changes will need to be made to the codebase.

The affected fields specify which parts of the codebase are impacted by the requested feature. For example, if the feature is related to a specific service or model, this field can be used to identify those services or models.

The breaking change dropdown specifies whether the requested feature will introduce any breaking changes to the existing codebase. This information is important because it can affect how easily the feature can be implemented and tested.

Finally, the acceptance criteria textarea provides a list of criteria that must be met before the feature request is considered complete. For example, if the feature request relates to a specific functionality or bug fix, the acceptance criteria may include testing that the new feature works as expected and doesn't cause any unexpected side effects.

---

### .github/ISSUE_TEMPLATE/refactor.yml
This is a GitHub issue template for refactoring code. It has three fields:

* `name`: A name for the issue, which can be anything you want.
* `description`: A brief description of the issue, such as "Code-Verbesserung ohne funktionale Änderung".
* `labels`: An array of labels that will be applied to the issue. In this case, it contains only one label: "refactor".
* `body`: The body of the issue template, which includes three fields:
	+ A markdown field with a brief description of what refactoring is.
	+ A textarea field for describing the problem or issue that needs to be addressed.
	+ A textarea field for listing the affected files or areas of code.

The `description` and `affected` fields are required, while the `acceptance` field has a placeholder with some suggested acceptance criteria for refactoring:

* [ ] Bestehende Tests bleiben grün
* [ ] Keine funktionale Änderung

These acceptance criteria suggest that the refactoring should not introduce any new functionality or break existing tests.

---

### .github/workflows/lint.yml
The purpose of this file is to configure a continuous integration pipeline using GitHub Actions to automatically lint the codebase using Pint. The file defines two jobs, one for installing dependencies and running Pint, and another for committing changes if any issues are found.

The file includes the following features:

* `name`: sets the name of the workflow to "linter"
* `on`: specifies the branches that trigger the workflow, including `develop`, `main`, `feature/**`, and `fix/**`
* `permissions`: grants the workflow permission to write to the repository's content
* `jobs`: defines two jobs: `quality` and `commit`
* `environment`: sets up a testing environment for the job
* `steps`: defines each step of the job, including:
	+ Installing PHP using shivammathur/setup-php@v2
	+ Adding Flux credentials loaded from ENV using composer config http-basic.composer.fluxui.dev "${{ secrets.FLUX_USERNAME }}" "${{ secrets.FLUX_LICENSE_KEY }}".
	+ Installing dependencies using `composer install` and `npm install`
	+ Running Pint using `vendor/bin/pint`
* `name`: sets the name of the job to "quality"
* `runs-on`: specifies that the job runs on Ubuntu latest
* `environment`: sets up a testing environment for the job
* `steps`: defines each step of the job, including:
	+ Installing PHP using shivammathur/setup-php@v2
	+ Adding Flux credentials loaded from ENV using composer config http-basic.composer.fluxui.dev "${{ secrets.FLUX_USERNAME }}" "${{ secrets.FLUX_LICENSE_KEY }}".
	+ Installing dependencies using `composer install` and `npm install`
	+ Running Pint using `vendor/bin/pint`
* `name`: sets the name of the job to "commit"
* `runs-on`: specifies that the job runs on Ubuntu latest
* `environment`: sets up a testing environment for the job
* `steps`: defines each step of the job, including:
	+ Installing PHP using shivammathur/setup-php@v2
	+ Adding Flux credentials loaded from ENV using composer config http-basic.composer.fluxui.dev "${{ secrets.FLUX_USERNAME }}" "${{ secrets.FLUX_LICENSE_KEY }}".
	+ Installing dependencies using `composer install` and `npm install`
	+ Running Pint using `vendor/bin/pint`

---

### .github/workflows/tests.yml
This is a GitHub Actions workflow file that defines a series of jobs and steps to be executed in the CI pipeline. Here's a breakdown of each section and what it does:

* `name`: The name of the workflow, which is "tests" in this case.
* `on`: This section specifies when the workflow should be triggered. In this case, it will be triggered on pushes to branches develop, main, and feature/ and fix/.
* `jobs`: This section defines a series of jobs that will be executed in the CI pipeline. There is only one job defined in this file, which is called "ci".
* `runs-on`: This specifies the operating system that the job should run on. In this case, it's set to "ubuntu-latest", which means it will use a Ubuntu image as the base for the job.
* `environment`: This sets up an environment variable called "Testing" that can be used throughout the workflow.
* `steps`: This section defines a series of steps that will be executed in the job. Each step is represented by a `- name` block, which specifies the name of the step and what it does. Here are the steps defined in this file:
	+ Checkout: This step checks out the code from the repository.
	+ Setup PHP: This step sets up the PHP environment using the "shivammathur/setup-php" action, which allows for easy installation of PHP and Composer packages. It specifies that we want to use version 8.4 of PHP and that we want to install the "composer:v2" tool and enable Xdebug coverage reporting.
	+ Setup Node: This step sets up the Node environment using the "actions/setup-node" action, which allows for easy installation of Node packages. It specifies that we want to use version 22 of Node and cache the package manager.
	+ Install Node Dependencies: This step installs any Node dependencies defined in the project's package.json file using npm.
	+ Add Flux Credentials Loaded From ENV: This step adds credentials for accessing the Flux UI repository, which is defined as secrets in the GitHub repository settings. The credentials are loaded from the environment variables FLUX_USERNAME and FLUX_LICENSE_KEY.
	+ Install Dependencies: This step installs any Composer dependencies defined in the project's composer.json file using Composer. It specifies that we want to use no interaction with Composer and prefer the fastest installation method possible.
	+ Copy Environment File: This step copies a sample environment file (".env.example") to the project's root directory as ".env".
	+ Generate Application Key: This step generates an application key for the Laravel framework using the "php artisan" command-line interface.
	+ Build Assets: This step builds the assets for the project using npm, which is defined in the package.json file.
	+ Run Tests: This step runs the tests for the project using the "pest" command-line interface, which is a tool for running PHPUnit tests in parallel.

---

## data_access

### app/Models/ChatMessage.php
File Summary: This file defines a `ChatMessage` model for the chat application, which is used to store messages in the database. The model extends Laravel's Eloquent Model class and includes several properties and methods that are necessary for storing and retrieving message data.

Key Features:

* Uses the `HasUuids` trait to generate UUIDs for the model's primary key.
* Defines a `$table` property to specify the name of the database table used by the model.
* Sets the `timestamps` property to `false` to disable automatic timestamping of the model.
* Uses the `$fillable` property to define which fields can be mass assigned when creating or updating a record.
* Uses the `$casts` property to define how certain fields should be cast to specific types when retrieved from the database.
* Defines two `BelongsTo` relationships with the `User` and `Webhook` models, respectively, using the `user()` and `webhook()` methods.

---

### app/Models/Consent.php
This file defines the Consent model for storing consent information in the application. The model has a belongs-to relationship with the User model and includes fields for user ID, IP address (anonymized), consent categories, consented at, and revoked at dates. Additionally, the model casts certain attributes to appropriate data types.

---

### app/Models/Contact.php
Die Datei `app/Models/Contact.php` enthält eine Klasse namens `Contact`, die von der Basisklasse `Model` in Laravel abgeleitet wird. Die Klasse definiert einen Eloquent-Modell-Builder, der verwendet wird, um Objekte des Modells `Contact` zu erstellen und zu aktualisieren.

In der Klasse sind auch einige Felder definiert, die als "fillable" markiert sind, was bedeutet, dass sie beim Erstellen oder Aktualisieren eines Objekts dieser Klasse übergeben werden können. Dies sind die Felder `name`, `company`, `email`, `project_type`, `message` und `timeline`.

Dieser Modell-Builder wird in der Datei `app/Http/Controllers/ContactController.php` verwendet, um Objekte des Modells `Contact` zu erstellen und zu aktualisieren. Die Datei enthält auch eine Route, die einen Endpunkt für die Kontaktanfrage bereitstellt.

Es gibt keine externen Abhängigkeiten in dieser Datei, da sie auf das Laravel-Framework und den Eloquent-Modell-Builder des Frameworks angewiesen ist.

---

### app/Models/PageView.php
{
"file_summary": "This file defines the PageView model, which is used to store information about page views in the database. It extends the Illuminate\Database\Eloquent\Model class and has four fillable attributes: path, ip_anonymous, user_agent, and visits."
}

---

### app/Models/User.php
Die Datei `app/Models/User.php` ist eine PHP-Klasse, die den Benutzer-Datensatz in der Laravel-Anwendung repräsentiert. Hier sind die wichtigsten Funktionen und Klassen der Datei:

* `use Illuminate\Contracts\Auth\MustVerifyEmail;` - Diese Zeile importiert die `MustVerifyEmail`-Schnittstelle, die für die E-Mail-Bestätigung von Benutzerkonten verwendet wird.
* `use Illuminate\Database\Eloquent\Factories\HasFactory;` - Diese Zeile importiert das Factory-Pattern für das Generieren von Objekten der Klasse `User`.
* `use Illuminate\Foundation\Auth\User as Authenticatable;` - Diese Zeile importiert die Basisklasse `User`, die für die Benutzerauthentifizierung verwendet wird.
* `use Illuminate\Notifications\Notifiable;` - Diese Zeile importiert das Notifications-Pattern, das für die Übermittlung von Benachrichtigungen an Benutzer verwendet wird.
* `use Laravel\Fortify\TwoFactorAuthenticatable;` - Diese Zeile importiert das Two-Factor-Authentifizierungs-Pattern, das für die erweiterte Benutzerauthentifizierung verwendet wird.
* `use Spatie\Activitylog\Models\Concerns\LogsActivity;` - Diese Zeile importiert das Logging-Pattern, das für das Protokollieren von Aktivitäten und Ereignissen verwendet wird.
* `use Spatie\Permission\Traits\HasRoles;` - Diese Zeile importiert das Roles-Pattern, das für die Verwaltung von Benutzerrollen und Berechtigungen verwendet wird.

Die Klasse `User` definiert verschiedene Attribute und Funktionen:

* `$fillable` - Eine Liste der Attributnamen, die während der Massenanfrage von Laravel unterstützt werden.
* `$hidden` - Eine Liste der Attributnamen, die für serialisierte Objekte ausgeblendet werden sollen.
* `$casts` - Eine Zuordnung der Attribute zu ihren Typen (Hashed, DateTime).
* `initials()` - Eine Funktion, die den Benutzernamen in eine Kombination von Initialen umwandelt.
* `getActivitylogOptions()` - Eine Funktion, die Optionen für das Protokollieren von Aktivitäten und Ereignissen definiert.
* `isActive()` - Eine Funktion, die bestimmt, ob der Benutzer aktiv ist (Status = "active").
* `isTrial()` - Eine Funktion, die bestimmt, ob der Benutzer ein kostenloses Test- oder Demo-Konto hat (Status = "trial").

---

### app/Models/Webhook.php
This file is a PHP class named `Webhook` that represents a webhook entity in the application. It extends the `Model` class from Laravel's Eloquent ORM, and uses the `HasUuids` trait to generate UUIDs for the model's primary key.

The `Webhook` class has several important methods:

* The constructor initializes the model with the necessary properties and sets up the relationships between the model and other entities in the application.
* The `fillable` property specifies which attributes are mass assignable, meaning that they can be assigned to the model via an array or a JSON object. In this case, it includes the `user_id`, `name`, `slug`, `url`, `secret`, and `frontend_object` properties.
* The `hidden` property specifies which attributes should not be included in the serialized representation of the model. In this case, it includes the `secret` attribute, as it is encrypted and should not be exposed to unauthorized users.
* The `casts` property specifies how certain attributes should be cast to different types when they are accessed or set. In this case, it includes the `created_at` attribute, which is a `datetime` object, and the `secret` attribute, which is an encrypted string.
* The `callUrl` method returns the webhook URL with a query parameter containing the secret key, if one is specified. This allows the webhook to be called securely using a secret key that is known only to the webhook creator.
* The `forUser` static method finds a webhook for a given user and frontend object. It uses Eloquent's `where` method to filter the query by the `user_id` and `frontend_object` columns, and then retrieves the first result using `first`. If no webhook is found, it returns null.
* The `user` method defines a relationship between the `Webhook` model and the `User` model, allowing you to retrieve the user who created the webhook.
* The `chatMessages` method defines a one-to-many relationship between the `Webhook` model and the `ChatMessage` model, allowing you to retrieve all chat messages associated with the webhook.

The `FRONTEND_OBJECTS` constant defines an array of known frontend objects that can be used with this webhook. It contains a mapping from a short string identifier (e.g., "dashboard_chat") to a human-readable name (e.g., "Dashboard Chat"). This allows you to use a more concise syntax when working with the frontend objects, rather than having to reference them by their full class names or database table names.

---

### app/Models/Recherche/P1Komponente.php
The file "app/Models/Recherche/P1Komponente.php" is a PHP class that defines a model for the P1Komponente entity in the "recherche" module of an application. It uses Eloquent ORM to interact with a database and define relationships between models.

Here's a summary of the main functionalities of this file:

* The `P1Komponente` class extends Laravel's `Model` class, which provides basic functionality for interacting with a database.
* The `HasUuids` trait is used to generate UUIDs for the model.
* The `protected $table` property specifies the name of the table in the database where the model data is stored.
* The `public $timestamps` property determines whether timestamps are updated when creating or updating records in the database. In this case, it is set to false, indicating that timestamps will not be updated.
* The `protected $fillable` property specifies which attributes of the model can be mass-assigned during creation or update operations. In this case, all columns except for "projekt_id" are fillable.
* The `protected $casts` property defines casts for the model's attributes. In this case, it specifies that the "synonyme" attribute should be treated as an array when interacting with the database.
* The `public function projekt()` method defines a relationship between the `P1Komponente` class and the `Projekt` class. It specifies that each `P1Komponente` instance belongs to a single `Projekt`, and returns a `BelongsTo` relationship object that can be used to query related records in the database.

Overall, this file defines a model for a specific entity in an application, including its attributes, relationships with other entities, and how it interacts with a database using Eloquent ORM.

---

### app/Models/Recherche/P1Kriterium.php
File: app/Models/Recherche/P1Kriterium.php
Category: data_access

This file defines a model for the P1Kriterium table in the database. The model is a subclass of the Model class provided by Laravel, and it uses the HasUuids trait to enable the use of UUIDs as primary keys.

The table has the following columns:

* project_id (integer) - the ID of the project that the kriterium belongs to
* kriterium_typ (string) - the type of the kriterium (e.g. "qualitative", "quantitative")
* beschreibung (string) - a short description of the kriterium
* begruendung (string) - a longer explanation of the kriterium, including its purpose and any relevant details
* quellbezug (string) - a reference to the source of the kriterium, if applicable

The model has the following methods:

* getAllKriteria() - retrieves all kriteria for a given project ID
* addKriterium() - adds a new kriterium to the table
* deleteKriterium() - deletes a kriterium from the table
* updateKriterium() - updates an existing kriterium in the table
* getKriteriumById() - retrieves a specific kriterium by its ID

The model also defines relationships with other models, such as the Project model, which it uses to retrieve the project name for a given project ID.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Diese PHP-Datei definiert eine Klasse für das Modell "P1Strukturmodellwahl", die im Eloquent-Framework von Laravel verwendet wird. Die Klasse "P1Strukturmodellwahl" erbt von der Klasse "Model" und implementiert die Traits "HasUuids". Die Klasse definiert eine Tabelle namens 'p1_strukturmodell_wahl' in der Datenbank, die für die Speicherung von Daten zu Modellwahlvorgängen verwendet wird. Sie enthält auch eine Beziehung zur Projekt-Tabelle per "BelongsTo"-Beziehung.

---

### app/Models/Recherche/P1Warnsignal.php
Die Datei `app/Models/Recherche/P1Warnsignal.php` enthält eine Klasse namens `P1Warnsignal`, die ein Eloquent-Modell für die Tabelle `p1_warnsignale` in der Datenbank darstellt.

Die Klasse erweitert die Basisklasse `Model` aus dem Laravel-Framework und verwendet das Trait `HasUuids`, um UUIDs zu generieren. Die Klasse hat eine Beziehung zu einer anderen Klasse namens `Projekt` mithilfe der Methode `belongsTo()`, mit der die ID eines Projekts in der Tabelle `p1_warnsignale` mit der ID eines Projekts in der Tabelle `projekte` verknüpft wird.

Die Klasse hat folgende Eigenschaften:

* `table`: Der Name der Datenbanktabelle, in der die Daten gespeichert werden sollen.
* `timestamps`: Wenn auf "true" gesetzt, werden Erstellungs- und Aktualisierungszeiten für das Modell generiert.
* `fillable`: Eine Liste von Spalten, die vom Model gefüllt werden dürfen.
* `casts`: Eine Liste von Spalten, die als bestimmte Typen interpretiert werden sollen.

Die Klasse hat auch eine Methode namens `projekt()`, mit der eine Beziehung zu einer anderen Klasse namens `Projekt` definiert wird.

---

### app/Models/Recherche/P2Cluster.php
Die Datei "app/Models/Recherche/P2Cluster.php" ist ein Eloquent-Modell in Laravel, das die Tabelle `p2_cluster` in der Datenbank repräsentiert. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und enthält folgende Funktionalitäten:

1. Verwendet den Trait `HasUuids`, um UUID-Werte für die Modell-Felder zu verwenden.
2. Definiert eine Variable `$table`, um den Namen der Datenbanktabelle anzugeben, in diesem Fall "p2_cluster".
3. Definiert eine Variable `$timestamps`, um anzugeben, dass das Modell keine Timestamps-Felder enthält.
4. Definiert ein Array `$fillable`, um anzugeben, welche Felder des Modells als "fillable" markiert werden dürfen (siehe Laravel-Dokumentation).
5. Definiert ein Array `$casts`, um anzugeben, wie die Werte von bestimmten Feldern des Modells gelesen und geschrieben werden sollen (hier: als Integer-Wert für das Feld "treffer_schaetzung").
6. Definiert eine Funktion `projekt()`, die den Eloquent-Beziehungsoperator `belongsTo` verwendet, um eine Beziehung zwischen dem Modell und der Tabelle "projekte" zu definieren.

Dieses Modell dient dazu, Daten aus der Tabelle "p2_cluster" in der Datenbank abzufragen und zu ändern. Es gibt keine Bewertung oder Fehler-Urteil, da die Datei lediglich ein Beispiel für eine Eloquent-Modell-Klasse ist.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Die Datei `app/Models/Recherche/P2MappingSuchstringKomponente.php` enthält eine Klasse `P2MappingSuchstringKomponente`, die ein Eloquent-Model darstellt. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und verwendet das Trait `HasUuids`.

Die Klasse hat folgende Funktionen:

1. Die Klasse hat eine Tabelle namens `p2_mapping_suchstring_komponenten`, die in der Datenbank gespeichert ist.
2. Die Klasse verwendet Eloquent-Verbindungen, um mit der Tabelle zu interagieren.
3. Die Klasse hat eine Beziehung zu einer anderen Tabelle namens `Projekt`, die ebenfalls in der Datenbank gespeichert ist. Diese Beziehung wird mithilfe des Methodenaufrufs `belongsTo()` hergestellt.
4. Die Klasse hat einige Felder, die als "fillable" definiert sind, d.h. sie können direkt in der Datenbank gespeichert werden. Dies sind die Felder `projekt_id`, `komponente_label`, `suchbegriffe`, `sprache`, `trunkierung_genutzt` und `anmerkung`.
5. Die Klasse hat auch einige Felder, die als "casts" definiert sind, d.h. sie werden automatisch in bestimmte Typen umgewandelt, wenn sie in der Datenbank gespeichert oder ausgegeben werden. Dies sind die Felder `suchbegriffe` (als Array), `trunkierung_genutzt` (als Boolean) und `anmerkung`.

Insgesamt stellt die Klasse eine Möglichkeit dar, Suchstring-Komponenten zu speichern und zu verwalten, indem sie diese als Eloquent-Models in der Datenbank abspeichert.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
This file defines the P2ReviewTypEntscheidung model for storing and retrieving data from the p2_review_typ_entscheidung table in the database. The model uses the Illuminate\Database\Eloquent\Model class, and it has a table named 'p2_review_typ_entscheidung' with timestamps turned off. It also has a fillable property that specifies which fields can be mass-assigned when creating or updating records in the database. The model also defines a belongsTo relationship with the Projekt model to retrieve the project details for each review type decision.

---

### app/Models/Recherche/P2Trefferliste.php
File: `app/Models/Recherche/P2Trefferliste.php`
Category: data_access

This PHP class is a model for the `p2_trefferlisten` table in the Laravel framework. It extends the `Model` class and uses the `HasUuids` trait to generate UUIDs for each instance of this class.

The class has the following properties:

* `projekt_id`: The ID of the project that the trefferliste belongs to (integer)
* `datenbank`: The name of the database that the trefferliste was retrieved from (string)
* `suchstring`: The search string used to retrieve the trefferliste (string)
* `treffer_gesamt`: The total number of hits found for the search string (integer)
* `einschaetzung`: A brief evaluation of the relevance of the trefferliste (string)
* `anpassung_notwendig`: Whether an adjustment is needed to improve the accuracy of the trefferliste (boolean)
* `suchdatum`: The date and time when the trefferliste was last retrieved (datetime)

The class also has a `projekt()` method that returns a `BelongsTo` relationship to the `Projekt` class, which represents the project that the trefferliste belongs to. This allows for easy retrieval of the project's information using the trefferliste instance.

---

### app/Models/Recherche/P3Datenbankmatrix.php
This file defines the P3Datenbankmatrix class for accessing the p3_datenbankmatrix table in the database. It extends the Model class and uses the HasUuids trait to automatically generate UUIDs for the model. The class also specifies the table name, timestamps, fillable fields, and casts boolean values to the appropriate type. The project() method is defined to return a belongsTo relationship with the Projekt class.

---

### app/Models/Recherche/P3Disziplin.php
Die Datei `app/Models/Recherche/P3Disziplin.php` definiert eine PHP-Klasse namens `P3Disziplin`, die als Eloquent-Model für den Zugriff auf die Tabelle `p3_disziplinen` in der Datenbank fungiert.

Die Klasse `P3Disziplin` erbt von der `Illuminate\Database\Eloquent\Model`-Klasse und verwendet das `HasUuids`-Trait, um die Verwendung von UUIDs bei der Identifikation von Modellinstanzen zu ermöglichen.

Die Klasse definiert mehrere Attribute, darunter den Namen der Tabelle (`$table`), in der die Daten gespeichert sind (`p3_disziplinen`). Sie enthält auch einen Boolean-Wert für das Timestamping (`$timestamps`), der angibt, ob Zeitstempel für die Datensätze gespeichert werden sollen.

Die Klasse definiert auch eine Variable `$fillable`, die die Spaltennamen des Modells enthält, die bereitgestellt werden dürfen, wenn neue Instanzen erstellt werden. In diesem Fall sind dies die Spalten `projekt_id`, `disziplin`, `art`, `relevanz` und `anmerkung`.

Die Klasse definiert auch eine Funktion `projekt()`, die einen Belongs-To-Beziehung zu einer anderen Modellklasse namens `Projekt` herstellt. Diese Beziehung ermöglicht es, die Disziplinen eines bestimmten Projekts aus der Datenbank abzurufen.

---

### app/Models/Recherche/P3GeografischerFilter.php
The file `app/Models/Recherche/P3GeografischerFilter.php` defines a Eloquent model class for the `p3_geografische_filter` table in the database. The class extends the default Laravel Model class and provides additional functionality through the use of traits.

The model has several properties, including:

* `projekt_id`: A foreign key referencing the `id` column of the `projekte` table.
* `region_land`: A string indicating the country or region in which the filter is valid.
* `validierter_filter_vorhanden`: A boolean flag indicating whether a validated filter exists for this project.
* `filtername_quelle`: A string containing the name of the source filter used to create this filter.
* `sensitivitaet_prozent`: A decimal value representing the sensitivity of the filter in percentage (e.g., 0.25 means 25%).
* `hilfsstrategie`: A string indicating the helper strategy used for the filter.

The model also defines several methods, including:

* `projekt()`: A method that returns a relationship between this model and the `Projekt` class through the `belongsTo` method. This allows accessing the project details associated with the filter.

In addition to these properties and methods, the file also imports several traits provided by Laravel, including `HasUuids`, which adds support for UUIDs in the model.

---

### app/Models/Recherche/P3GraueLiteratur.php
Die Datei `app/Models/Recherche/P3GraueLiteratur.php` enthält eine PHP-Klasse für die Model-Repräsentation einer Tabelle in der Datenbank. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model`, die es ermöglicht, das Erstellen von Modeln für das Speichern und Abrufen von Daten aus einer relationalen Datenbank.

Die Klasse definiert einige Eigenschaften und Methoden, um den Zugriff auf die Datenbank zu ermöglichen:

* Die `$table` -Eigenschaft enthält den Namen der Tabelle in der Datenbank, in der die Daten gespeichert sind.
* Die `$timestamps`-Eigenschaft ist auf `false` gesetzt, was bedeutet, dass die Tabelle keine Zeitstempel für das Erstellen und Ändern von Datensätzen enthält.
* Die `$fillable`-Eigenschaft definiert, welche Spalten der Tabelle in der Datenbank gefüllt werden dürfen, wenn ein neuer Datensatz erstellt wird. In diesem Fall sind dies die Spalten `projekt_id`, `quelle`, `typ`, `url`, `suchpfad`, `relevanz` und `anmerkung`.
* Die `projekt()`-Methode definiert eine Beziehung zwischen der Tabelle `p3_graue_literatur` und der Tabelle `projekte`, indem sie eine `BelongsTo`-Relation erstellt, die es ermöglicht, den Namen des Projekts abzurufen, zu dem jeder Datensatz gehört.

Die Klasse importiert auch einige PHP-Klassen aus anderen Namespaces:

* Die `HasUuids`-Klasse stammt vom Package `Illuminate\Database\Eloquent\Concerns`. Sie ermöglicht es, UUIDs (Universally Unique Identifiers) für die Datensätze in der Tabelle zu verwenden.
* Die `Model`-Klasse stammt ebenfalls aus dem Package `Illuminate\Database\Eloquent`. Sie enthält Methoden und Eigenschaften, die es ermöglichen, das Speichern und Abrufen von Daten aus einer relationalen Datenbank zu verwenden.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Die Datei app/Models/Recherche/P4Anpassungsprotokoll.php enthält eine Klasse namens P4Anpassungsprotokoll, die ein Modell in der Datenbank darstellt. Die Klasse hat mehrere Attribute und Methoden, um die Daten des Anpassungsprotokolls zu verwalten. Sie verwendet die Illuminate\Database\Eloquent\Concerns\HasUuids-Trait, um UUIDs zu generieren, und ist mit der P4Suchstring-Klasse in der Datenbank verbunden.

---

### app/Models/Recherche/P4Suchstring.php
* Die Datei "app/Models/Recherche/P4Suchstring.php" enthält eine Klasse namens "P4Suchstring", die den Eloquent-ORM-Modell für ein Suchstring in der Datenbank darstellt.
* Die Klasse erbt von "Model" und implementiert das "HasUuids" Trait, um UUIDs zu verwenden.
* Die Tabelle, auf die die Klasse zugreift, ist "p4_suchstrings".
* Die Klasse hat eine Beziehung zu einer "Projekt"-Klasse über die "belongsTo()"-Methode und eine Beziehung zu einer "P4Anpassungsprotokoll"-Klasse und einer "P8Suchprotokoll"-Klasse über die "hasMany()"-Methoden.
* Die Klasse hat folgende Eigenschaften:
	+ "projekt_id": Eine ID, die auf ein Projekt in der Datenbank verweist.
	+ "datenbank": Der Name der verwendeten Datenbank.
	+ "suchstring": Der tatsächliche Suchstring.
	+ "feldeinschraenkung": Ein Array von Feldern, die zum Einschränken des Suchbereichs verwendet werden können.
	+ "gesetzte_filter": Ein Array von Filterbedingungen, die angewendet werden können.
	+ "treffer_anzahl": Die Anzahl der Treffer für den Suchstring.
	+ "einschaetzung": Die Einschätzung des Suchstrings.
	+ "anpassung": Die Anpassungs-ID.
	+ "version": Die Version des Suchstrings.
	+ "suchdatum": Das Datum, an dem der Suchstring erstellt wurde.
* Die Klasse hat auch folgende Methoden:
	+ "projekt()": Gibt eine Beziehung zu einem Projekt zurück.
	+ "anpassungsprotokoll()": Gibt eine Beziehung zu einer Anpassungsprotokolleinstellung zurück.
	+ "suchprotokolle()": Gibt eine Beziehung zu einem Suchprotokoll zurück.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Die Datei `app/Models/Recherche/P4ThesaurusMapping.php` ist ein Eloquent-Model in Laravel, das die Tabelle "p4_thesaurus_mapping" in der Datenbank repräsentiert. Die Klasse erweitert die `Illuminate\Database\Eloquent\Model`-Klasse und verwendet den `HasUuids`-Trait, um UUIDs (Universally Unique Identifiers) für die Eloquent-Modelle zu verwenden.

Die Tabelle "p4_thesaurus_mapping" enthält Informationen über die Zuordnung zwischen einem Projekt und verschiedenen Thesaurus-Termen. Die Klasse definiert auch einige Felder, die in der Tabelle vorhanden sind, wie z.B. "projekt_id", "freitext_de", "freitext_en", "mesh_term", "emtree_term" und "psycinfo_term".

Die Klasse definiert auch eine Beziehung zwischen der Tabelle "p4_thesaurus_mapping" und der Tabelle "projekte" über die Spalte "projekt_id". Die `belongsTo`-Methode wird verwendet, um diese Beziehung zu definieren.

Die Klasse enthält auch eine Variable `$table`, die den Namen der Datenbanktabelle angibt, auf der das Model basiert. Die Variable `$timestamps` ist ein Boolescher Wert, der angibt, ob für das Modell Automatik-Zeitstempel (Created at und Updated at) erstellt werden sollen.

Die Klasse definiert auch eine Variable `$fillable`, die die Felder enthält, die in der Tabelle "p4_thesaurus_mapping" erlaubt sind.

Insgesamt definiert die Klasse eine Eloquent-Model-Klasse, die die Zuordnung zwischen einem Projekt und verschiedenen Thesaurus-Termen repräsentiert und über eine Beziehung mit der Tabelle "projekte" verfügt.

---

### app/Models/Recherche/P5PrismaZahlen.php
This PHP class defines a model for the P5PrismaZahlen table in the database. The model represents a record of a specific project's information on the number of records to be processed, including the number of records that have been processed and the number that have not been processed due to various reasons.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Die Datei `app/Models/Recherche/P5ScreeningEntscheidung.php` ist eine Klasse in Laravel, die eine Eloquent-Model-Klasse darstellt und die Datenbanktabelle `p5_screening_entscheidungen` in der MySQL-Datenbank darstellt.

Die Klasse umfasst folgende Funktionen:

* Es gibt eine Verbindung mit der Datenbanktabellen `p5_screening_entscheidungen` und verwaltet die Beziehungen zwischen dieser Tabelle und anderen Tabellen in der Datenbank, wie z.B. der Tabelle `p5_treffer`.
* Die Klasse definiert eine Reihe von Eigenschaften (`$fillable`) und Casts (`$casts`), um die Datenformatierung und Validierung zu konfigurieren.
* Die Klasse implementiert auch die Methode `save()`, um die Daten in der Datenbank zu speichern.

Die wichtigsten Klassen und Funktionen sind:

* `P5ScreeningEntscheidung`: Eine Eloquent-Model-Klasse, die die Datenbanktabelle `p5_screening_entscheidungen` darstellt und die Beziehungen zwischen dieser Tabelle und anderen Tabellen in der Datenbank verwaltet.
* `$table`: Die Name der Datenbanktabelle, auf die diese Klasse zugreift.
* `$timestamps`: Ein Boolean-Wert, der angibt, ob das Modell automatisch Zeitstempel für den Erstellungs- und Aktualisierungszeitpunkt verfolgt.
* `$fillable`: Eine Liste von Spaltennamen, die in der Datenbanktabelle gefüllt werden dürfen.
* `$casts`: Ein Array von Casts, das angibt, wie die Datenformatierung und Validierung konfiguriert werden soll.
* `save()`: Die Methode zum Speichern von Daten in der Datenbank.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Die Datei `app/Models/Recherche/P5ScreeningKriterium.php` enthält eine Klasse `P5ScreeningKriterium`, die ein Eloquent-Modell für die Tabelle `p5_screening_kriterien` in der Datenbank darstellt.

Die Klasse verwendet den Trait `HasUuids`, um die Verwendung von UUIDs anstelle von Auto-Inkrement-IDs zu ermöglichen.

Die Tabelle `p5_screening_kriterien` enthält folgende Spalten:

* `id`: Eine UUID, die jeder Datensatz in der Tabelle eindeutig identifiziert.
* `projekt_id`: Die ID des Projekts, dem das Screening-Kriterium zugeordnet ist.
* `level`: Der Level des Kriteriums (z.B. "Basic", "Intermediate" oder "Advanced").
* `kriterium_typ`: Der Typ des Kriteriums (z.B. "Text", "Numerical" oder "Boolean").
* `beschreibung`: Eine Beschreibung des Kriteriums.
* `beispiel`: Ein Beispiel für die Anwendung des Kriteriums.

Die Klasse hat auch eine Beziehung zur Klasse `Projekt` über die Eigenschaft `projekt()`. Diese Beziehung ermöglicht das Zuordnen eines Screening-Kriteriums zu einem bestimmten Projekt in der Datenbank.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Here is a summary of the file "app/Models/Recherche/P5ToolEntscheidung.php" in 10 lines or less:

* This file defines a model class for the `p5_tool_entscheidung` table in the database, which represents a decision made by a team member on which tool to use for a specific project.
* The model extends the `Illuminate\Database\Eloquent\Model` class and uses the `HasUuids` trait to enable UUIDs for the model.
* The table name is set to `p5_tool_entscheidung`, and timestamps are not used.
* The fillable fields of the model include `projekt_id`, `tool`, `gewaehlt`, and `begruendung`.
* The `gewaehlt` field is cast as a boolean to ensure it is saved as a boolean in the database.
* The model defines a relationship with the `Projekt` class, which represents a project in the database, using the `belongsTo` method.

---

### app/Models/Recherche/P5Treffer.php
File: app/Models/Recherche/P5Treffer.php
Category: data_access

This file defines a model for the `p5_treffer` table in the database. It uses the Eloquent ORM provided by Laravel to define the relationships between different models and the structure of the table.

The model has the following properties:

* `projekt_id`: A foreign key referencing the `projekte` table.
* `record_id`: An integer representing the unique identifier for the record in this table.
* `titel`: A string containing the title of the publication.
* `autoren`: A string containing the authors of the publication.
* `jahr`: An integer representing the year the publication was published.
* `journal`: A string containing the name of the journal the publication was published in.
* `doi`: A string containing the Digital Object Identifier (DOI) for the publication.
* `abstract`: A string containing the abstract of the publication.
* `datenbank_quelle`: A string containing the source of the data used to populate this table.
* `ist_duplikat`: A boolean indicating whether the record is a duplicate or not.
* `erstellt_am`: A datetime representing when the record was created in the database.

The model also defines several relationships with other models:

* `projekt()`: A belongs-to relationship to the `Projekte` model, which represents the project that this publication is associated with.
* `duplikatOriginal()`: A belongs-to relationship to the same model (`P5Treffer`), which represents the original record that this duplicate was derived from.
* `duplikate()`: A has-many relationship to the same model (`P5Treffer`), which represents all of the records in the table that are marked as duplicates.
* `screeningEntscheidungen()`: A has-many relationship to the `P5ScreeningEntscheidung` model, which represents all of the screening decisions made for this record.
* `qualitaetsbewertung()`: A has-many relationship to the `P6Qualitaetsbewertung` model, which represents all of the quality assessments made for this record.
* `datenextraktion()`: A has-many relationship to the `P7Datenextraktion` model, which represents all of the data extractions made for this record.

---

### app/Models/Recherche/P6Luckenanalyse.php
Die Klasse P6Luckenanalyse in der Datei app/Models/Recherche/P6Luckenanalyse.php stellt eine Verbindung zur Datenbank-Tabelle p6_luckenanalyse her und enthält die Methoden und Eigenschaften, um diese Daten zu bearbeiten und abzufragen.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
This is a PHP class file for the `App\Models\Recherche\P6Qualitaetsbewertung` model in a Laravel application. The file contains the definition of the `P6Qualitaetsbewertung` class, which extends the `Model` class from the `Illuminate\Database\Eloquent` namespace.

The `P6Qualitaetsbewertung` class has several properties and methods that are relevant for managing the data in the `p6_qualitaetsbewertung` table of the database. These include:

* The `$table` property, which defines the name of the table in the database that this model corresponds to.
* The `$timestamps` property, which controls whether timestamps (created and updated at) are automatically managed by Laravel.
* The `$fillable` array, which lists the properties of the model that can be mass-assigned during creation or update operations.
* The `$casts` array, which defines how certain columns should be cast to specific data types when retrieved from the database.
* The `treffer()` method, which is a "relationship" method that specifies the relationship between this model and another model (in this case, `P5Treffer`). This allows us to access the related `P5Treffer` record through this model.

Overall, this file provides a way to interact with the database table for `P6Qualitaetsbewertung` records in a Laravel application.

---

### app/Models/Recherche/P7Datenextraktion.php
Die Datei `app/Models/Recherche/P7Datenextraktion.php` enthält eine PHP-Klasse für die Datenextraktion von Daten aus einer Datenbank und stellt eine Beziehung zwischen den Modellen `P5Treffer` und `P7Datenextraktion` her.

Die Klasse `P7Datenextraktion` erweitert die Klasse `Model` von Laravel und verwendet das Trait `HasUuids`, um UUID-Feld für die Eloquent-Modellklasse zu generieren.

Die Klasse hat folgende wichtige Funktionen:

* Sie definiert eine Beziehung zwischen der aktuellen Klasse (`P7Datenextraktion`) und dem `P5Treffer`-Model über den `belongsTo()`-Methodenaufruf. Diese Beziehung ermöglicht es, die Daten zugeordneten Treffer-Datensatz zu erhalten.
* Sie definiert eine Tabelle namens `p7_datenextraktion`, in der die Daten extrahiert werden. Die Tabelle enthält verschiedene Felder wie `treffer_id`, `land`, `stichprobe_kontext`, `phaenomen_intervention`, `outcome_ergebnis`, `hauptbefund`, `qualitaetsurteil` und `anmerkung`.
* Sie definiert eine Liste von Fillable-Feldern (`$fillable`), die den Feldnamen enthält, die in der Datenbank gespeichert werden dürfen. Diese Felder sind: `treffer_id`, `land`, `stichprobe_kontext`, `phaenomen_intervention`, `outcome_ergebnis`, `hauptbefund`, `qualitaetsurteil` und `anmerkung`.
* Sie definiert das Timestamp-Feld (`$timestamps`) als `false`, was bedeutet, dass die Automatik der Zeitstempel-Spalte nicht aktiviert ist.

Insgesamt ist diese Klasse eine Beziehungs- und Datenmodell-Klasse, die die extrahierten Daten aus der Datenbank verwaltet und einen Bezug zwischen den Modellen `P5Treffer` und `P7Datenextraktion` definiert.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Die Datei `app/Models/Recherche/P7GradeEinschaetzung.php` ist eine Eloquent-Model-Klasse in Laravel, die es ermöglicht, `P7GradeEinschaetzung`-Objekte zu verwalten und auf ihre Beziehungen zu anderen Modellen zugreifen zu können.

Die Klasse erbt von der `Illuminate\Database\Eloquent\Model`-Klasse und verwendet die Traits `HasUuids` und `Illuminate\Database\Eloquent\Concerns\HasTimestamps`. Die `HasUuids`-Trait ermöglicht es, UUIDs (Universally Unique Identifiers) für die Objekte zu verwenden, während die `HasTimestamps`-Trait es ermöglicht, automatische Zeitstempel für das Erstellungs- und Aktualisierungsdatum der Objekte festzulegen.

Die Klasse definiert einen Tabellennamen (`p7_grade_einschaetzung`), indem sie die `$table`-Eigenschaft auf `p7_grade_einschaetzung` setzt. Die Eigenschaft `$timestamps` ist auf `false` gesetzt, da diese Tabelle keine Zeitstempel enthält.

Die Klasse definiert auch eine Gruppe von Füllfälten (`$fillable`), die den Attributen der Objekte entsprechen, die in dieser Tabelle gespeichert werden können. Diese Attribute sind `projekt_id`, `outcome`, `studienanzahl`, `rob_gesamt`, `inkonsistenz`, `indirektheit`, `impraezision`, `grade_urteil` und `begruendung`.

Die Klasse definiert auch eine Gruppe von Casts (`$casts`), die verwendet werden, um die Werte der Objekte zu übersetzen. In diesem Fall wird das Feld `studienanzahl` mit dem Typ `integer` castet.

Die Klasse definiert außerdem eine Beziehung zwischen den `P7GradeEinschaetzung`-Objekten und den `Projekt`-Objekten mithilfe der Methode `projekt()`, die einen `BelongsTo`-Typ zurückgibt. Diese Methode gibt an, dass ein Objekt von diesem Modell in einer Beziehung mit einem anderen Modell namens `Projekt` hat. Die `belongsTo()`-Methode dient dazu, die Beziehung zwischen den Modellen zu definieren und ermöglicht es, auf die Beziehungen zuzugreifen.

Insgesamt ist diese Klasse ein Eloquent-Model-Objekt, das in Laravel verwendet wird, um `P7GradeEinschaetzung`-Objekte zu verwalten und auf ihre Beziehungen zu anderen Modellen zugreifen zu können.

---

### app/Models/Recherche/P7MusterKonsistenz.php
Datei: app/Models/Recherche/P7MusterKonsistenz.php
Kategorie: data_access

* Die Datei enthält eine Klasse namens "P7MusterKonsistenz", die eine Modell-Klasse für den Eloquent-ORM in Laravel darstellt.
* Die Klasse erbt von der Basisklasse "Model" und verwendet das Trait "HasUuids" um UUIDs zu generieren.
* Die Tabelle, auf die die Klasse zugreift, heißt "p7_muster_konsistenz".
* Die Klasse hat ein Attribut "$timestamps" mit dem Wert false, das bedeutet, dass die Klasse keine Zeitstempel für die Datensätze generiert.
* Die Klasse hat einen "belongsTo"-Beziehung zu der Modell-Klasse "Projekt".
* Die Klasse hat ein Attribut "$fillable", das eine Liste von Spaltennamen enthält, die für Massenanfertigung geeignet sind.
* Die Klasse hat ein Attribut "$casts" mit einem Array von Spaltennamen und der Datentyp, um die Daten in der Spalte zu casten. In diesem Fall werden die Spalten "unterstuetzende_quellen" und "widersprechende_quellen" als Arrays interpretiert.
* Die Klasse hat eine Methode "projekt", die eine Beziehung zu einem anderen Modell-Objekt namens "Projekt" definiert.

---

### app/Models/Recherche/P7SyntheseMethode.php
Dies ist eine PHP-Datei mit einer Klasse 'P7SyntheseMethode', die als Modell für eine Tabelle in der Datenbank fungiert. Die Klasse verwendet Eloquent-ORM und definiert eine Beziehung zwischen dem Modell und einer anderen Tabelle namens 'Projekt'. Die Klasse hat auch eine Reihe von Attributen, die den Zugriff auf die Datenbank erleichtern. Die Datei enthält keine externen Abhängigkeiten oder API-Aufrufe.

---

### app/Models/Recherche/P8Limitation.php
This file contains the P8Limitation model for the project database. It is used to store and manage the limitations of a specific project in the application.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
File Summary: This is a PHP class file for the `P8Reproduzierbarkeitspruefung` model in an Laravel application. The model is defined as a subclass of the Eloquent Model class and uses the `HasUuids` trait to generate UUIDs for the primary key.

The table name is set to `p8_reproduzierbarkeitspruefung`, and timestamps are not used for this model. The `fillable` property specifies the fields that can be mass-assigned when creating or updating a record, and the `casts` property defines how certain attributes should be cast to specific types.

The model has a `projekt()` relationship with the `Projekt` model via a belongsTo relationship.

---

### app/Models/Recherche/P8Suchprotokoll.php
Die Datei `app/Models/Recherche/P8Suchprotokoll.php` definiert eine Klasse namens `P8Suchprotokoll`, die ein Modell für das Suchprotokolle der Anwendung darstellt. Die Klasse erweitert die Basisklasse `Illuminate\Database\Eloquent\Model` und verwendet den Namen `HasUuids`, um die Verwendung von Universally Unique Identifiers (UUIDs) für die primary key zu aktivieren.

Die Klasse definiert auch eine Reihe von Eigenschaften, die als Felder in der Datenbanktabellen `p8_suchprotokoll` gespeichert werden:

* `suchstring_id`: Eine UUID-Zeichenfolge, die das Suchstring-Objekt verknüpft, das für das Suchprotokolle verwendet wurde.
* `datenbank`: Der Name der Datenbank, in der das Suchprotokolle gespeichert ist.
* `suchdatum`: Ein Datums-Objekt mit der Zeitpunkt des Suchvorgangs.
* `db_version`: Die Version der Datenbank, die für das Suchprotokolle verwendet wurde.
* `suchstring_final`: Der endgültige Suchstring, der für das Suchprotokolle verwendet wurde.
* `gesetzte_filter`: Ein Array von Filternamen und Werten, die für das Suchprotokolle angewendet wurden.
* `treffer_gesamt`: Die Gesamtanzahl der Treffer des Suchvorgangs.
* `treffer_eindeutig`: Die Anzahl eindeutiger Treffer des Suchvorgangs.

Die Klasse definiert auch eine Beziehung zwischen `P8Suchprotokoll` und `P4Suchstring`, die über die Eigenschaft `suchstring()` implementiert wird. Diese Beziehung verknüpft das Suchprotokolle mit dem entsprechenden Suchstring-Objekt.

Die Klasse verwendet auch den Trait `HasUuids` um die Verwendung von Universally Unique Identifiers (UUIDs) für die primary key zu aktivieren, der Name der Tabelle ist `p8_suchprotokoll`.

---

### app/Models/Recherche/P8UpdatePlan.php
This is a PHP class for the P8UpdatePlan model. It extends the Eloquent Model class and uses the HasUuids trait to generate UUIDs. The class defines the following properties:

* $table - the name of the database table that stores the P8UpdatePlan data
* $timestamps - whether the model includes timestamps (default is false)
* $fillable - an array of attributes that are mass assignable
* $casts - an array of casts for converting attribute values to specific types

The class also defines a belongsTo relationship with the Projekt model, which represents the project that this update plan belongs to. The method name is "projekt" and it returns a BelongsTo object.

Overall, this class provides a way to interact with the P8UpdatePlan data in the database, including querying, creating, updating, and deleting records for P8UpdatePlans.

---

### app/Models/Recherche/PaperEmbedding.php
This file contains the code for a `PaperEmbedding` model in Laravel. The model is used to store data related to paper embeddings in the database. It extends the `Model` class from Laravel and uses the `HasUuids` trait to enable UUIDs for the model.

The `table` property specifies that this model should use the `paper_embeddings` table in the database, which is where the data related to paper embeddings will be stored. The `timestamps` property is set to `false`, which means that the created and updated timestamps for each record in the table will not be automatically managed by Laravel.

The `fillable` property specifies which fields can be mass-assigned when creating a new record or updating an existing one. In this case, it includes all of the fields in the `paper_embeddings` table that are not the primary key (`id`). The `casts` property defines how certain columns should be cast to specific types when retrieved from the database. In this case, the `metadata` column is cast as an array and the `erstellt_am` column is cast as a datetime object.

The model also includes a relationship with the `Projekt` model, which allows us to retrieve the project that a paper embedding belongs to. This relationship is defined using the `belongsTo()` method from Laravel's Eloquent ORM. The name of the foreign key in the `paper_embeddings` table that references the `projekte` table is specified as the first argument to the `belongsTo()` method, which in this case is `projekt_id`.

---

### app/Models/Recherche/Phase.php
File: app/Models/Recherche/Phase.php
Category: data_access

This file contains a PHP class for the `Phase` model in the `recherche` directory of the Laravel project. The `Phase` model represents a phase in a project, and it has the following properties:

* `projekt_id`: a unique identifier for the project that this phase belongs to
* `phase_nr`: the number of this phase in the project (starting from 1)
* `titel`: the title of this phase
* `status`: the status of this phase (e.g., "in progress", "completed")
* `notizen`: additional notes about this phase
* `abgeschlossen_am`: the date when this phase was completed (if applicable)

The `Phase` model extends the `Model` class from Laravel, which provides a variety of functionality for working with Eloquent models. In particular, it uses the `HasUuids` trait to generate UUIDs for each instance of the `Phase` model, and it specifies that the `phase_nr` property should be cast as an integer.

The `Phase` model also defines a `belongsTo` relationship with the `Projekt` model (which represents a project in the database). This means that each instance of the `Phase` model is associated with a specific instance of the `Projekt` model, and it can be retrieved using the `projekt()` method.

Overall, this file provides a simple way to interact with the `phasen` table in the Laravel project's database, which stores information about the different phases of each project.

---

### app/Models/Recherche/Projekt.php
Die Datei `app/Models/Recherche/Projekt.php` enthält eine Klasse, die den Eloquent-ORM für Laravel verwendet, um ein Modell von Projekten zu definieren. Die Klasse umfasst verschiedene Funktionen und Methoden, um die Interaktion mit der Datenbank zu ermöglichen und zu unterstützen.

Die wichtigsten Funktionen sind:

* **user(): BelongsTo** - Eine Beziehung zwischen einem Projekt und einem Benutzer, das über die `user_id`-Spalte in der Tabelle `projekte` definiert ist.
* **phasen(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Phasen, die über die `projekt_id`-Spalte in der Tabelle `phasen` definiert sind.
* **p1Strukturmodellwahl(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Strukturmodellwahlen, die über die `projekt_id`-Spalte in der Tabelle `p1_strukturmodellwahl` definiert sind.
* **p1Komponenten(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Komponenten, die über die `projekt_id`-Spalte in der Tabelle `p1_komponenten` definiert sind.
* **p1Kriterien(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Kriterien, die über die `projekt_id`-Spalte in der Tabelle `p1_kriterien` definiert sind.
* **p1Warnsignale(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Warnsignalen, die über die `projekt_id`-Spalte in der Tabelle `p1_warnsignale` definiert sind.
* **p5Treffer(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Treffern, die über die `projekt_id`-Spalte in der Tabelle `p5_treffer` definiert sind.
* **p2ReviewTypEntscheidungen(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Review-Typ-Entscheidungen, die über die `projekt_id`-Spalte in der Tabelle `p2_reviewtyp_entscheidungen` definiert sind.
* **p2Cluster(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Clustern, die über die `projekt_id`-Spalte in der Tabelle `p2_cluster` definiert sind.
* **p2MappingSuchstringKomponenten(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Suchstring-Komponenten, die über die `projekt_id`-Spalte in der Tabelle `p2_mapping_suchstring_komponenten` definiert sind.
* **p2Trefferlisten(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Trefferlisten, die über die `projekt_id`-Spalte in der Tabelle `p2_trefferlisten` definiert sind.
* **p3Datenbankmatrix(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Datenbank-Matrixen, die über die `projekt_id`-Spalte in der Tabelle `p3_datenbankmatrix` definiert sind.
* **p3Disziplinen(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Disziplinen, die über die `projekt_id`-Spalte in der Tabelle `p3_disziplinen` definiert sind.
* **p3GeografischeFilter(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren geografischen Filtern, die über die `projekt_id`-Spalte in der Tabelle `p3_geografische_filter` definiert sind.
* **p3GraueLiteratur(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren grauen Literaturen, die über die `projekt_id`-Spalte in der Tabelle `p3_graue_literatur` definiert sind.
* **p4Suchstrings(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Suchstrings, die über die `projekt_id`-Spalte in der Tabelle `p4_suchstrings` definiert sind.
* **p4ThesaurusMapping(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Thesaurus-Mappings, die über die `projekt_id`-Spalte in der Tabelle `p4_thesaurus_mapping` definiert sind.
* **p5PrismaZahlen(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Prisma-Zahlen, die über die `projekt_id`-Spalte in der Tabelle `p5_prisma_zahlen` definiert sind.
* **p5ScreeningKriterien(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Screening-Kriterien, die über die `projekt_id`-Spalte in der Tabelle `p5_screening_kriterien` definiert sind.
* **p5ToolEntscheidung(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Tool-Entscheidungen, die über die `projekt_id`-Spalte in der Tabelle `p5_tool_entscheidung` definiert sind.
* **p6Luckenanalyse(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Luckenanalysen, die über die `projekt_id`-Spalte in der Tabelle `p6_luckenanalyse` definiert sind.
* **p7SyntheseMethode(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Synthese-Methoden, die über die `projekt_id`-Spalte in der Tabelle `p7_synthese_methode` definiert sind.
* **p7GradeEinschaetzung(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Grade-Einschätzung, die über die `projekt_id`-Spalte in der Tabelle `p7_grade_einschaetzung` definiert sind.
* **p7MusterKonsistenz(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Muster-Konsistenzen, die über die `projekt_id`-Spalte in der Tabelle `p7_muster_konsistenz` definiert sind.
* **p8Limitationen(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Limitations, die über die `projekt_id`-Spalte in der Tabelle `p8_limitierung` definiert sind.
* **p8Reproduzierbarkeitspruefung(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Reproduzierbarkeit-Prüfungen, die über die `projekt_id`-Spalte in der Tabelle `p8_reproduzierbarkeit` definiert sind.
* **p8UpdatePlan(): HasMany** - Eine Beziehung zwischen einem Projekt und mehreren Update-Plänen, die über die `projekt_id`-Spalte in der Tabelle `p8_update_plan` definiert sind.

Die Klasse verwendet auch das Trait `HasUuids`, um UUIDs (Universally Unique Identifiers) für alle Modell-Instanzen zu generieren, und das Trait `LogsActivity`, um die Aktivitäten der Benutzer auf dem Server nachzuverfolgen.

---

### database/factories/UserFactory.php
This file is a factory class for creating users in the application. It uses the Eloquent ORM provided by Laravel to create new user instances. The factory class contains several methods that can be used to generate different types of user data, such as unverified and two-factor authentication disabled users.

The `definition()` method is responsible for defining the default state of a user instance, including its name, email address, password, and other fields. The `unverified()` method allows the creation of unverified users, while the `withoutTwoFactor()` method creates users without two-factor authentication configured.

The file also imports several Laravel helper functions, such as `fake()`, `now()`, `random()`, and `safeEmail()`. These functions are used to generate random data for the user instance, and ensure that the email address is unique.

Overall, this factory class provides a simple way to create new user instances in the application, while also allowing developers to customize the data generated using the various methods provided by the class.

---

### database/factories/Recherche/ProjektFactory.php
This file is a factory for the Projekt model in the Recherche module. It defines how to create new instances of the Projekt model, including setting the user ID and generating random titles and research questions.

---

### database/migrations/0001_01_01_000000_create_users_table.php
File Summary: This file contains a migration for creating the `users`, `password_reset_tokens`, and `sessions` tables in the Laravel framework's database. The migration defines three tables, each with its own columns and constraints. The `up` method creates these tables using the `Schema` facade, while the `down` method drops them if they exist.

External Dependencies:

* `Illuminate\Database\Migrations\Migration`: A class in the Laravel framework that provides a base implementation for migrations.
* `Illuminate\Database\Schema\Blueprint`: A class in the Laravel framework that allows you to define table structures using a fluent interface.
* `Illuminate\Support\Facades\Schema`: A facade in the Laravel framework that provides a convenient way to interact with the database schema.

Classes and Functions:

* `Migration`: This class is used to create the tables defined in the migration. It has an `up` method and a `down` method that are called when the migration is executed or rolled back, respectively.
* `Blueprint`: This class is used to define the structure of each table created by the migration. It provides a fluent interface for specifying columns, constraints, and other properties of the tables.
* `Schema`: This facade provides a convenient way to interact with the database schema. It allows you to create, modify, and drop tables using methods such as `create`, `dropIfExists`, and `table`.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
This is a migration file for creating two tables in the database: `cache` and `cache_locks`. The `cache` table has three columns: `key`, `value`, and `expiration`, while the `cache_locks` table has three columns as well: `key`, `owner`, and `expiration`.

The migration is using Laravel's schema builder to create these tables. It is defining two classes that extend the `Migration` class, one for creating the tables and another for dropping them if they already exist.

Inside the `up()` method of the first class, it is calling the `create()` method on the `Schema` facade to create the `cache` table. It is defining the columns of the table using the `$table` object provided by the schema builder. The `key` column is set as the primary key and has a type of string, the `value` column has a type of medium text, and the `expiration` column has an integer type.

The second class defines the `down()` method to drop the `cache_locks` table if it already exists. It does this by calling the `dropIfExists()` method on the `Schema` facade with the name of the table as a parameter.

This migration file is intended to create the necessary tables for caching and locking in Laravel.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
This is a Laravel migration file that creates three tables: `jobs`, `job_batches`, and `failed_jobs`. The `jobs` table stores information about queued jobs, including the job's unique ID, queue name, payload (the data for the job), attempts made to process the job, and time when the job was reserved. The `job_batches` table stores information about job batches, including the batch ID, name, total number of jobs in the batch, pending jobs, failed jobs, IDs of failed jobs, and options. The `failed_jobs` table stores information about failed jobs, including the UUID of the job, connection used to run the job, queue the job was pulled from, payload for the job, exception thrown during processing, and time when the job failed.

The migration file uses the `Illuminate\Database\Migrations\Migration` class and extends it to create the three tables. The `up()` method creates the tables and their columns, while the `down()` method drops the tables if they exist.

This migration file is part of Laravel's job queue system, which allows developers to run long-running tasks asynchronously. When a job is added to the queue, it is processed by a worker process. If an error occurs during processing, the job is moved to the `failed_jobs` table.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Die Datei `database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php` ist eine Migrationsdatei, die die Funktion hat, die Spalten für die zwei-Faktor-Authentifizierung in der `users`-Tabelle des Laravel-Frameworks hinzuzufügen.

Die Klasse `class extends Migration` erweitert die `Migration`-Klasse und implementiert die Methoden `up()` und `down()`. Die Methode `up()` wird ausgeführt, wenn die Migration angewendet werden soll, während die Methode `down()` ausgeführt wird, wenn die Migration zurückgesetzt werden muss.

In der `up()`-Methode wird die `Schema::table`-Methode verwendet, um die `users`-Tabelle zu ändern und den folgenden Spalten hinzuzufügen:

* `two_factor_secret`: Ein Textfeld, das für die Speicherung des Geheimnisses der zwei-Faktor-Authentifizierung verwendet wird.
* `two_factor_recovery_codes`: Ein Textfeld, das für die Speicherung von Wiederherstellungs-Codes verwendet wird, die bei einer verlorenen oder gestohlenen Geräte-Tokens verwendet werden können.
* `two_factor_confirmed_at`: Eine Zeitstempelspalte, die angibt, wann der Benutzer die zwei-Faktor-Authentifizierung bestätigt hat.

Die Methode `down()` wird verwendet, um die oben genannten Spalten zu entfernen und die Tabelle auf ihren Ursprungszustand zurückzusetzen.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
This is a Laravel migration file that creates a new table in the database called "contacts". The table has several columns, including an ID column, a name column, a company column (which can be nullable), an email column, a project_type column, a message column, and a timeline column (which can also be nullable). It also includes timestamps for the creation and modification of records in the table.

The file uses the `Illuminate\Database\Migrations\Migration` class provided by Laravel to define the migration. The `up` method is used to create the new table, while the `down` method is used to drop it if needed.

This file does not import any external libraries or APIs, but it does use the `Illuminate\Support\Facades\Schema` class provided by Laravel to interact with the database schema.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
This file is a migration for creating a new table called 'page_views' in the database. It uses the Laravel framework to create the table and define its schema.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
This is a migration file for creating the necessary tables to store roles and permissions in a Laravel application. The file uses the `Illuminate\Database\Migrations\Migration` class to create the tables, and it defines several functions:

* `up()`: This function creates the necessary tables using the `Schema::create()` method. It takes into account the customization options defined in the `config/permission.php` file. The table names are stored in the `$tableNames` variable, and the column names are stored in the `$columnNames` variable.
* `down()`: This function drops the tables created by the `up()` method using the `Schema::dropIfExists()` method. It takes into account the customization options defined in the `config/permission.php` file.

The migration file also includes a `throw_if()` statement to check if the `config/permission.php` file is loaded correctly. If it's not loaded, the migration will throw an error to prevent any further execution.

In addition, the migration file uses the `app('cache')` method to clear the cache for the `config/permission.php` file and the `config/permission.cache.store` store. This is necessary because the migration file needs to access the customization options defined in the `config/permission.php` file.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
This migration adds three columns to the page_views table: path, ip_anonymous, and user_agent. The path column is a string that represents the URL of the viewed page, the ip_anonymous column is a nullable string that represents an anonymous IP address, and the user_agent column is a nullable string that represents the User-Agent header sent by the client.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Die Datei "database/migrations/2026_03_31_000003_create_consents_table.php" enthält eine Migration für die Erstellung einer Tabelle mit dem Namen "consents". Die Tabelle hat folgende Spalten:

* id (eine eindeutige ID)
* user_id (eine ID, die einem Benutzer zugeordnet ist)
* ip_anonymous (die IP-Adresse des Benutzers)
* consent_categories (ein JSON-Objekt, das verschiedene Kategorien von Einstimmungen enthält)
* consented_at (ein Zeitstempel für das Datum und die Uhrzeit, zu der der Benutzer seine Zustimmung erteilt hat)
* revoked_at (ein Zeitstempel für das Datum und die Uhrzeit, zu der der Benutzer seine Zustimmung widerrufen hat)
* created_at (eine Spalte zur Speicherung des Erstellungsdatums der Tabelle)
* updated_at (eine Spalte zur Speicherung des Update-Datums der Tabelle)

Die Migration nutzt die Laravel-APIs "Schema" und "Blueprint", um die Tabelle zu erstellen. Sie verwendet auch die "cascadeOnDelete"-Option, um sicherzustellen, dass die Beziehung zwischen den Benutzern und den Konsenswerten aufgelöst wird, wenn der Benutzer gelöscht wird.

Die Datei ist Teil des Laravel-Frameworks und dient dazu, die Datenbankstruktur für das Speichern von Konsensdaten zu erstellen und zu verwalten.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Die Datei `database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php` enthält eine Migration für die Recherche-Core-Tabellen in SQLite. Die Migration erstelle neue Tabellen und füge Spalten hinzu, um die Recherche-Core-Funktionen zu unterstützen.

Die wichtigsten Klassen und Funktionen dieser Datei sind:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse für alle Migrationen in Laravel.
* `Illuminate\Database\Schema\Blueprint`: Eine Klasse zum Erstellen von Tabellen und Spalten.
* `Illuminate\Support\Facades\DB`: Eine Klasse, die Datenbank-Abfragen ausführt.
* `Illuminate\Support\Facades\Schema`: Eine Klasse, die Datenbank-Schemanutzer darstellt.

Die wichtigsten Funktionen dieser Datei sind:

* `up()`: Führt eine Migration auf einem bestimmten Projekt aus.
* `down()`: Führt eine Rollback-Operation für eine Migration aus.
* `createTable('projekte', ...)`: Erstellt eine neue Tabelle "projekte".
* `createTable('phasen', ...)`: Erstellt eine neue Tabelle "phasen".
* `createTable('p5_treffer', ...)`: Erstellt eine neue Tabelle "p5_treffer".
* `createTable('p1_strukturmodell_wahl', ...)`: Erstellt eine neue Tabelle "p1_strukturmodell_wahl".
* `createTable('p1_komponenten', ...)`: Erstellt eine neue Tabelle "p1_komponenten".
* `createTable('p1_kriterien', ...)`: Erstellt eine neue Tabelle "p1_kriterien".
* `createTable('p1_warnsignale', ...)`: Erstellt eine neue Tabelle "p1_warnsignale".
* `createTable('p2_review_typ_entscheidung', ...)`: Erstellt eine neue Tabelle "p2_review_typ_entscheidung".
* `createTable('p2_cluster', ...)`: Erstellt eine neue Tabelle "p2_cluster".
* `createTable('p2_mapping_suchstring_komponenten', ...)`: Erstellt eine neue Tabelle "p2_mapping_suchstring_komponenten".
* `createTable('p2_trefferlisten', ...)`: Erstellt eine neue Tabelle "p2_trefferlisten".
* `createTable('p3_disziplinen', ...)`: Erstellt eine neue Tabelle "p3_disziplinen".
* `createTable('p3_datenbankmatrix', ...)`: Erstellt eine neue Tabelle "p3_datenbankmatrix".

Die Datei importiert auch verschiedene Abhängigkeiten von Laravel, darunter `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint` und `Illuminate\Support\Facades\DB`.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Die Datei `database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php` enthält eine PHP-Klasse, die für die Verarbeitung von Datenbankmigrierungen in einer PostgreSQL-Datenbank zuständig ist.

In der `up`-Methode dieser Klasse wird geprüft, ob es sich bei der aktuellen Datenbankumgebung um eine PostgreSQL-Datenbank handelt. Falls ja, werden die folgenden Schritte ausgeführt:

1. Die PHP-Erweiterungen `uuid-ossp` und `pg_trgm` werden erstellt, falls sie noch nicht existieren.
2. Enumerationen (Enums) für verschiedene Typen von Daten werden erstellt, wenn sie noch nicht vorhanden sind. Es handelt sich um folgende Enums:
	* `phase_status`: Offen, In Bearbeitung, Abgeschlossen
	* `review_typ`: Systematischer Review, Scoping-Review, Evidence Map
	* `strukturmodell`: PICO, SPIDER, PICOS
	* `kriterium_typ`: Einschluss, Ausschluss
	* `screening_level`: L1_titel_abstract, L2_volltext
	* `screening_entscheidung`: Eingeschlossen, Ausgeschlossen, Unklar
	* `rob_tool`: RoB2, ROBINS-I, CASP_qualitativ, AMSTAR2, ROBINS-I_erweitert, narrativ
	* `rob_urteil`: Stark, Moderat, Hoch, Kritisch, Nicht bewertet
	* `synthese_methode`: Metaanalyse, Narrative-Synthese, Thematische-Synthese, Framework-Synthesis
	* `grade_urteil`: Stark, Moderat, Schwach, Sehr schwACH
	* `studientyp`: RCT, Nicht randomisiert, Qualitativ, Systematic Review, Guideline Framework, Konzeptionell
	* `tool_empfehlung`: Rayyan, Covidence, EPPI_Reviewer, DistillerSR, ASReview, SWIFT_ActiveScreener
3. Die Erweiterungen und Enumerationen werden durch den Aufruf der Methoden `CREATE EXTENSION IF NOT EXISTS` und `CREATE TYPE` erstellt.

In der `down`-Methode wird die Inverse dieser Migration durchgeführt: Alle Enumerationen und Erweiterungen werden gelöscht, falls sie vorhanden sind. Dies geschieht durch den Aufruf der Methoden `DROP TYPE IF EXISTS` und `DROP EXTENSION IF EXISTS`.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Die Datei `2026_03_31_100001_create_projekte_and_phasen_tables.php` enthält eine Migration-Datei, die für die Erstellung von Tabellen in der Datenbank zuständig ist. Die Datei verwendet Laravel's Framework-Klasse `Illuminate\Database\Migrations\Migration` und umfasst die Methoden `up()` und `down()`, die die Ausführung der Migration steuern.

Die Migration enthält zwei Tabellen: `projekte` und `phasen`. Die Tabelle `projekte` enthält Informationen zu den Projekten, während die Tabelle `phasen` Informationen zu den Phasen in den Projekten speichert.

In der `up()`-Methode wird zunächst überprüft, ob die Datenbank-Drivers von Laravel für PostgreSQL sind. Anschließend werden die Tabellen erstellt, indem die Methoden `Schema::create()` verwendet werden. Die Tabelle `projekte` enthält einige Spalten, darunter die Primärschlüsselspalte `id`, die eine UUID-Wert generiert und als Primärschlüssel fungieren soll. Es gibt auch einige weitere Spalten wie `user_id`, `titel`, `forschungsfrage`, `verantwortlich` usw.

Die Tabelle `phasen` enthält auch eine Primärschlüsselspalte `id` und eine Fremdschlüsselspalte `projekt_id`, die auf die Primärschlüsselspalte von `projekte` verweist. Die Tabelle enthält auch einige weitere Spalten wie `phase_nr`, `titel`, `notizen`, `abgeschlossen_am`.

Die Migration fügt auch eine Spalte `review_typ` zur Tabelle `projekte` hinzu und eine Spalte `phase_status` zur Tabelle `phasen`.

In der `down()`-Methode wird die Tabelle `phasen` gelöscht, falls sie existiert, und anschließend wird die Tabelle `projekte` gelöscht.

Dies sind die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse für Migrationen.
* `Schema::create()`: Die Methode zum Erstellen von Tabellen.
* `Schema::dropIfExists()`: Die Methode zum Löschen von Tabellen, wenn sie existieren.
* `Illuminate\Support\Facades\DB`: Das Facade-Objekt für den Datenbankzugriff.
* `Illuminate\Database\Schema\Blueprint`: Die Klasse für das Erstellen von Tabellen.
* `uuid_generate_v4()`: Die Funktion zum Generieren einer UUID-Wert.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
This migration file creates several tables related to the project management of a research project, including "p1_strukturmodell_wahl", "p1_komponenten", "p1_kriterien", and "p1_warnsignale". These tables are used to store information about the project's structure, components, criteria, and warning signals. The migration file also creates foreign key constraints between these tables and the "projekte" table, which stores information about the research projects.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
This is a Laravel migration file for creating several tables in the database. The file defines a class that extends the `Migration` class from the `Illuminate\Database\Migrations` namespace, and it contains several methods:

* `up()`: This method is called when the migration needs to be run, and it creates several tables in the database. It uses the `Schema` facade to create tables with columns and constraints.
* `down()`: This method is called when the migration needs to be rolled back, and it drops all of the tables that were created by the `up()` method.

The tables created by this migration are:

1. `p2_review_typ_entscheidung`: This table stores information about a decision made by a reviewer for a particular project. It has columns for the `id`, `projekt_id`, `passt`, `begruendung`, and `review_typ` (which is not actually used in this migration file).
2. `p2_mapping_suchstring_komponenten`: This table stores information about a mapping between a search string and a component label for a particular project. It has columns for the `id`, `projekt_id`, `komponente_label`, `sprache`, `trunkierung_genutzt`, `anmerkung`, and `suchbegriffe` (which is not actually used in this migration file).
3. `p2_trefferlisten`: This table stores information about a hit list for a particular project. It has columns for the `id`, `projekt_id`, `datenbank`, `suchstring`, `treffer_gesamt`, `einschaetzung`, `anpassung_notwendig`, and `suchdatum`.
4. `p2_cluster`: This table stores information about a cluster for a particular project. It has columns for the `id`, `projekt_id`, `cluster_id`, `cluster_label`, `beschreibung`, `treffer_schaetzung`, and `relevanz`. The `relevanz` column is constrained to be one of three values: "hoch", "mittel", or "gering".

This migration file does not import any external dependencies, but it uses the `Illuminate\Support\Facades\DB` facade to access the database driver and the `Illuminate\Database\Schema\Blueprint` class to define the table schema.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Die Datei `database/migrations/2026_03_31_100004_create_p3_tables.php` enthält eine Migration, die verschiedene Tabellen für das Projekt P3 erstellt oder entfernt. Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `Illuminate\Database\Migrations\Migration`: Diese Klasse wird verwendet, um Migrationen zu erstellen und auszuführen.
* `Illuminate\Database\Schema\Blueprint`: Diese Klasse wird verwendet, um Tabellenspalten und -indizes zu definieren.
* `Illuminate\Support\Facades\DB`: Diese Klasse ermöglicht den Zugriff auf die Datenbank und bietet eine Reihe von Methoden für die Erstellung, Aktualisierung und Löschung von Tabellen.
* `Illuminate\Support\Facades\Schema`: Diese Klasse ermöglicht den Zugriff auf das Schema der Tabelle und bietet eine Reihe von Methoden für die Tabellenerstellung, -aktualisierung und -löschung.

In dieser Migration werden vier Tabellen erstellt oder entfernt: `p3_disziplinen`, `p3_datenbankmatrix`, `p3_geografische_filter` und `p3_graue_literatur`. Diese Tabellen sind für das Projekt P3 spezifisch und enthalten verschiedene Spalten und Einschränkungen.

Die Migration hat zwei Methoden: `up()` und `down()`. Die `up()`-Methode erstellt die vier angegebenen Tabellen, wenn sie noch nicht existieren. Die `down()`-Methode entfernt diese Tabellen, wenn sie existieren. Wenn die Datenbank eine andere Art von Datenbank als PostgreSQL ist, wird die Migration ignoriert.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
The provided PHP file is a Laravel migration for a PostgreSQL database. It creates several tables:

1. `p4_suchstrings`: This table stores information about search strings and their associated metadata, such as the date of the search, the version number, and the number of hits.
2. `p4_thesaurus_mapping`: This table maps free-text terms to controlled vocabulary terms from external systems like MeSH, EMTree, and PsycINFO.
3. `p4_anpassungsprotokoll`: This table stores information about changes made to search strings, such as the date of the change, the version number, and the reason for the change.

The migration also includes foreign key constraints to ensure data consistency between tables. The file imports several classes from the Laravel framework and uses the `uuid` data type to generate unique identifiers for each table.

Overall, this migration file creates a database schema that is designed to store and manage search strings, as well as their associated metadata and the mapping of free-text terms to controlled vocabulary terms.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Diese Datei ist eine Datenbank-Migration, die Teil des Laravel-Frameworks ist. Sie enthält Klassen für die Erstellung von Tabellen in der PostgreSQL-Datenbank, die von der Anwendung verwendet wird.

Die wichtigsten Funktionen dieser Datei sind:

* Die `up`-Methode erstellt die Tabellen `p5_prisma_zahlen`, `p5_screening_kriterien`, `p5_treffer`, `p5_screening_entscheidungen` und `p5_tool_entscheidung`, wenn sie noch nicht existieren.
* Die `down`-Methode löscht die Tabellen, falls sie bereits vorhanden sind.

Die folgenden Klassen werden in dieser Datei verwendet:

* `Illuminate\Database\Migrations\Migration`: Diese Klasse stellt eine Basisklasse für Migrationen dar, die von Laravel-Frameworks bereitgestellt wird.
* `Illuminate\Support\Facades\DB`: Diese Klasse ermöglicht den Zugriff auf die Datenbank via Laravel-Frameworks Facade.
* `Illuminate\Support\Facades\Schema`: Diese Klasse ermöglicht den Zugriff auf das Schema der Datenbank via Laravel-Frameworks Facade.

Die folgenden Imports werden in dieser Datei verwendet:

* `Illuminate\Database\Migrations\Blueprint`: Diese Klasse stellt eine Basisklasse für Tabellen-Definitionen dar, die von Laravel-Frameworks bereitgestellt wird.
* `Illuminate\Support\Facades\DB::raw('uuid_generate_v4()')`: Diese Funktion gibt einen UUID mit einem zufälligen Wert zurück, der verwendet wird, um die ID-Spalte in den Tabellen zu initialisieren.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Die Datei `database/migrations/2026_03_31_100007_create_p6_tables.php` enthält eine Migration, die zwei Tabellen erstellt: `p6_qualitaetsbewertung` und `p6_luckenanalyse`.

Die Tabelle `p6_qualitaetsbewertung` hat folgende Felder:

* `id`: Eine UUID-Spalte, die die Primärschlüssel der Tabelle ist.
* `treffer_id`: Eine UUID-Spalte, die auf eine ID in der Tabelle `p5_treffer` verweist.
* `hauptproblem`: Ein Textfeld, das das Hauptproblem einer Bewertung speichert.
* `im_review_behalten`: Ein boolesches Feld, das angibt, ob die Bewertung beibehalten werden soll.
* `anmerkung`: Ein Textfeld, das eine Anmerkung zu der Bewertung enthält.
* `bewertet_von`: Ein Textfeld, das den Benutzer speichert, der die Bewertung durchgeführt hat.
* `bewertet_am`: Ein Datums-Feld, das das Datum der Bewertung angibt.
* `studientyp`: Eine Enum-Spalte, die den Typ des Studenten speichert (z.B. "Student", "Lehrer" oder "Administrator").
* `rob_tool`: Eine Enum-Spalte, die den Toolspeicher speichert, der für die Bewertung verwendet wurde (z.B. "RoboAdvisor" oder "QualityChecker").
* `gesamturteil`: Eine Enum-Spalte, die das Gesamthurteil der Bewertung speichert (z.B. " positiv", " negativ" oder " neutral").

Die Tabelle `p6_luckenanalyse` hat folgende Felder:

* `id`: Eine UUID-Spalte, die die Primärschlüssel der Tabelle ist.
* `projekt_id`: Eine UUID-Spalte, die auf eine ID in der Tabelle `projekte` verweist.
* `fehlender_aspekt`: Ein Textfeld, das den fehlenden Aspekt speichert, der für das Projekt bewertet wurde.
* `fehlender_studientyp`: Ein Textfeld, das den fehlenden Studenten-Typ speichert, der für das Projekt bewertet wurde.
* `moegliche_konsequenz`: Ein Textfeld, das die mögliche Konsequenz speichert, die für das Projekt empfohlen wird.
* `empfehlung`: Ein Textfeld, das die Empfehlung speichert, die für das Projekt empfohlen wird.

Die Migration verwendet die Laravel-Migrationsklasse `Illuminate\Database\Migrations\Migration`, um die Tabellen zu erstellen und zu aktualisieren. Die Migration verwendet auch die Laravel-Schema-Klasse `Illuminate\Support\Facades\Schema` für den Zugriff auf die Datenbank-Tabellen.

Die Migration enthält auch eine Rollback-Funktion, die die Tabellen löscht, wenn sie vorhanden sind.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Die Datei `2026_03_31_100008_create_p7_tables.php` in der Kategorie `data_access` enthält eine Laravel-Migration, die einige Tabelle für den Evaluierungsprozess eines Forschungsprojektes erstellt.

Die wichtigsten Klassen und Funktionen sind:

* `Illuminate\Database\Migrations\Migration`: Die Basisklasse aller Migrationen in Laravel, die die Methoden `up()` und `down()` definiert, um die Tabelle zu erstellen und sie wieder zu löschen.
* `Illuminate\Database\Schema\Blueprint`: Die Klasse zum Erstellen von Tabellenschemas. In dieser Datei wird eine Instanz der Klasse erstellt und verwendet, um die Schemas für die fünf neuen Tabellen `p7_synthese_methode`, `p7_datenextraktion`, `p7_muster_konsistenz`, `p7_grade_einschaetzung` zu erstellen.
* `Illuminate\Support\Facades\DB`: Die Klasse für den Zugriff auf die Datenbank in Laravel. In dieser Datei wird die Methode `getDriverName()` verwendet, um den aktuell verwendeten Datenbanktreiber zu ermitteln und abhängig davon, ob der Datenbanktreiber PostgreSQL ist oder nicht, entweder die Migration durchführt oder sie abbrechen muss.
* `Illuminate\Support\Facades\Schema`: Die Klasse für den Zugriff auf das Schema von Tabellen in Laravel. In dieser Datei wird die Methode `hasTable()` verwendet, um zu überprüfen, ob die Tabelle bereits existiert und dann entweder die Migration durchführt oder sie abbrechen muss.
* `Illuminate\Support\Facades\DB::statement()`: Die Methode zum Ausführen von SQL-Anweisungen direkt auf der Datenbank. In dieser Datei wird die Methode verwendet, um den Datentyp einer Spalte in der Tabelle `p7_synthese_methode` zu ändern und die Spaltennamen in den Tabellen `p7_datenextraktion`, `p7_muster_konsistenz` und `p7_grade_einschaetzung` zu ändern.

Die Migration enthält auch den Code für das Löschen der Tabelle bei der Rollback-Operation.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
This file contains a migration for the Laravel framework that creates several tables in the database:

* `p8_suchprotokoll`: A table to store search protocols.
* `p8_limitationen`: A table to store limitations.
* `p8_reproduzierbarkeitspruefung`: A table to store reproducibility checks.
* `p8_update_plan`: A table to store an update plan for a project.

The migration uses the Illuminate\Database\Migrations\Migration class, which is part of the Laravel framework. It also imports several dependencies, including the DB facade and the Schema facade, from the Illuminate\Support namespace. The migration creates new tables or modifies existing ones depending on whether they already exist in the database.

The `up` method of the migration first checks if the driver used by Laravel is PostgreSQL, since this migration only applies to that database. If it is not PostgreSQL, it returns early without doing anything.

Next, it checks if each table mentioned above exists in the database and creates them if they do not. It also adds foreign key constraints between these tables and the `projekte` table.

The `down` method of the migration is similar to the `up` method but performs the opposite operation. If the driver used by Laravel is PostgreSQL, it drops each of the tables mentioned above if they exist in the database.

In summary, this migration creates several tables in the database and sets up foreign key constraints between them.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
The file `database/migrations/2026_03_31_100010_create_recherche_indices.php` contains a database migration that creates indices on various tables in the database. The migration is written in PHP and uses the Laravel framework's migration class to create the indices.

The `up()` method of the migration creates indices on the following tables:

* `phasen`: Creates an index on the `projekt_id` column of the `phasen` table.
* `p1_komponenten`: Creates an index on the `projekt_id` column of the `p1_komponenten` table.
* `p5_treffer`: Creates indices on the following columns:
	+ `projekt_id`: Creates an index on the `projekt_id` column of the `p5_treffer` table.
	+ `record_id`: Creates an index on the `record_id` column of the `p5_treffer` table.
	+ `ist_duplikat`: Creates an index on the `ist_duplikat` column of the `p5_treffer` table.
* `p7_datenextraktion`: Creates a full-text search (German) index on the `abstract` and `titel` columns of the `p7_datenextraktion` table.

The `down()` method of the migration drops the indices created in the `up()` method, if they exist.

Overall, this migration creates several indices on various tables to optimize database performance for a web application that uses the Laravel framework.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Die Datei `2026_03_31_100011_create_prisma_function.php` ist eine PHP-Datei, die einen Migrationsprozess für eine PostgreSQL-Datenbank definiert. In dieser Datei wird eine neue Funktion erstellt, die `berechne_prisma_zahlen`, die PRISMA-2020-Kennzahlen für ein Recherche-Projekt direkt auf DB-Ebene berechnet. Die Funktion zählt aus dem `p5_treffer`-Tabellenmodell und dem `p5_screening_entscheidungen`-Tabellenmodell die Werte für das PRISMA-Flussdiagramm: Identifiziert → Duplikate → L1-Ausschluss → Volltext → L2-Ausschluss → Final.

Die Funktion wird von einer Klasse erstellt, die von der `Migration`-Klasse erbt. Die `up()`-Methode erstellt die Funktion, während die `down()`-Methode sie wieder löscht.

Die Funktion verwendet SQL-Syntax und benötigt eine PostgreSQL-Datenbank, um zu funktionieren. Das bedeutet, dass der Code nur auf Servern mit einer PostgreSQL-Datenbank ausgeführt werden kann, die dieselbe Datenbankstruktur hat wie die verwendete Datenbank.

Die Funktion akzeptiert ein `UUID`-Argument und verwendet es, um das entsprechende Projekt zu finden. Sie zählt dann die Anzahl der Treffer in den Tabellen `p5_treffer` und `p5_screening_entscheidungen`, die mit dem Projekt verbunden sind, und gibt die Ergebnisse zurück.

Die Funktion verwendet Filter-Klauseln, um nur die relevanten Treffer zu berücksichtigen und die Kennzahlen für das PRISMA-Flussdiagramm zu berechnen. Sie verwendet auch EXISTS-Klauseln, um festzustellen, ob es Treffer gibt, die einem bestimmten Level oder einer bestimmten Entscheidung entsprechen.

Die Funktion ist wichtig, weil sie den Recherche-Prozess beschleunigt und die Anzahl der Treffer für das PRISMA-Flussdiagramm direkt auf der Datenbankebene berechnet. Sie kann von verschiedenen Anwendungen verwendet werden, um das PRISMA-Flussdiagramm zu generieren oder um andere Kennzahlen für den Recherche-Prozess zu berechnen.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
This file is a Laravel migration for creating a new table named `chat_messages`. The table has the following columns:

* `id`: a UUID primary key that is generated using the `uuid_generate_v4()` function.
* `user_id`: a foreign key referencing the `users` table in the database.
* `role`: a string column with a maximum length of 20 characters, which can have one of two values: 'user' or 'assistant'.
* `content`: a text column for storing the message content.
* `created_at`: a timestamp column that stores the creation time of the message.

The migration also includes a check constraint on the `role` column to ensure that only valid values ('user' or 'assistant') are inserted into the table. The `uuid_generate_v4()` function is used to generate the primary key value.

The migration is written using the Laravel framework and makes use of several dependencies, including:

* `Illuminate\Database\Migrations\Migration`: This class is a base class for database migrations in Laravel. It provides methods for defining the up and down methods for the migration.
* `Illuminate\Database\Schema\Blueprint`: This class is used to define the schema of the table being created. It allows you to add columns, constraints, indexes, and other table-level features.
* `Illuminate\Support\Facades\DB`: This facade provides access to the Laravel database layer. It can be used to execute SQL queries or perform other database operations.
* `Illuminate\Support\Facades\Schema`: This facade provides access to the schema builder for creating and modifying tables in the database.

In this migration, the `up()` method is called when the migration is executed, which creates the `chat_messages` table if it does not already exist. The `down()` method is called when the rollback command is used to undo the migration.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
This PHP file is part of a Laravel application. It contains a migration for creating a new table called "webhooks" in the database. The migration class extends the `Migration` class provided by Laravel and implements the `up` and `down` methods to perform the desired actions when running the migration.

In the `up` method, the following things happen:

1. If the current driver is not PostgreSQL, the method returns immediately without performing any further actions.
2. The `Schema::create()` method is used to create a new table called "webhooks" in the database. The table has the following columns:
	* `id`: A unique identifier for each row in the table, with a default value of a newly generated UUID using the `uuid_generate_v4` function provided by PostgreSQL. This column is set as the primary key and has an index created on it.
	* `user_id`: A foreign key referencing the "users" table, which will be used to store the user who created the webhook. The `constrained()` method is used to create a constraint that ensures that this foreign key cannot be deleted if any records in the "chat_messages" table reference it.
	* `name`: A text column for storing the name of the webhook.
	* `slug`: A text column for storing the slug (or identifier) of the webhook. This column is set as unique to ensure that each slug is only used once in the database.
	* `url`: A text column for storing the URL of the webhook.
	* `password`: A text column for storing the password for the webhook.
	* `created_at`: A timestamp column indicating when the row was created. The default value is set to the current time using the `now()` function provided by PostgreSQL.
3. The `Schema::table()` method is used to add a new column called "webhook_id" to the "chat_messages" table. This column will store the ID of the webhook that the message was sent through, if any. The `nullable()` method is used to make this column nullable, as it may not always be present.
4. The `foreign()` method is used to create a foreign key constraint between the "chat_messages" table and the "webhooks" table on the "id" column. This ensures that any record in the "chat_messages" table referencing a webhook must also have a corresponding record in the "webhooks" table with the same ID.

In the `down` method, the following things happen:

1. The foreign key constraint between the "chat_messages" and "webhooks" tables is dropped using the `dropForeign()` method.
2. The "webhook_id" column is dropped from the "chat_messages" table using the `dropColumn()` method.
3. Finally, the "webhooks" table is dropped using the `dropIfExists()` method.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
This file is a migration for creating a new table in the database called "paper_embeddings". The table will have columns for an ID, project ID (if applicable), source, paper ID, title, text chunk, metadata, and created at timestamp. There will also be foreign key constraints and indexes on the project ID and source/paper ID columns.

The migration is using the Laravel framework's schema builder to create the table and add columns. The `use Illuminate\Database\Migrations\Migration;` statement imports the Migration class, which is used to define database migrations. The `return new class extends Migration` statement creates a new class that extends the Migration class and defines the up and down methods for the migration.

In the up method, the first if statement checks if the current driver is "pgsql". If it is not, then the rest of the code will not be executed. This is because the migration is using PostgreSQL specific syntax to create the table and add columns.

The next line creates an extension for the vector data type in PostgreSQL. This is necessary for creating a column with a "vector" data type. The `DB::statement()` method is used to execute the SQL statement that creates the extension.

After creating the extension, the migration creates the table "paper_embeddings" and adds columns for an ID, project ID (if applicable), source, paper ID, title, text chunk, metadata, and created at timestamp. The `Schema::create()` method is used to create the table and add columns.

The migration also adds foreign key constraints and indexes on the project ID and source/paper ID columns. The `->foreign('projekt_id') ->references('id')->on('projekte')->nullOnDelete();` statement creates a foreign key constraint that references the "id" column in the "projekte" table. The `->index()` method is used to create an index on the project ID and source/paper ID columns.

In the down method, the migration first checks if the current driver is "pgsql". If it is not, then the rest of the code will not be executed. This is because the migration is using PostgreSQL specific syntax to drop the table.

The next line drops the "paper_embeddings" table using the `Schema::dropIfExists()` method.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
This migration file creates a new table called "activity_log" in the database. The table has several columns, including an ID, a log name, a description of the activity, a subject (either a UUID or integer), an event type, a causer (either a UUID or integer), and two sets of JSON columns for storing attribute changes and properties.

The file also includes a `up` method that creates the table and an `down` method that drops it if it exists. The migration file uses the Laravel framework's built-in `Migration` class to perform the database operations.

The external dependencies in this file are:

* The `Illuminate\Database\Migrations\Migration` class, which provides a base class for creating and managing database migrations.
* The `Illuminate\Support\Facades\Schema` facade, which is used to interact with the schema of the application's database.
* The `Illuminate\Database\Schema\Blueprint` class, which is used to define the structure of the table being created.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
This is a Laravel migration file that adds a new column to the `users` table in the database, which will store the status of each user. The new column will have a default value of "trial" and will be an enumeration type with four possible values: "trial", "active", "suspended", and "cancelled".

The migration file uses a PostgreSQL-specific syntax for creating an enum type, which is necessary because Laravel does not currently support enums in migrations. The `if (DB::getDriverName() === 'pgsql')` statement checks if the database driver being used is PostgreSQL, and if it is, the migration will create the enum type using the `CREATE TYPE` syntax.

The `up()` method first checks if the column already exists in the users table by calling the `hasColumn` method on the Schema facade. If the column does not exist, the migration will add a new column to the table with the specified name and default value using the `table->string('status')->default('trial')->after('email')` syntax.

After adding the new column, the migration will update the default value of the column to be "trial" by calling the `ALTER TABLE users ALTER COLUMN status DROP DEFAULT` statement followed by the `ALTER TABLE users ALTER COLUMN status TYPE user_status USING status::user_status` statement.

Finally, the migration will set the default value of the column to be "trial" by calling the `ALTER TABLE users ALTER COLUMN status SET DEFAULT 'trial'::user_status` statement.

The `down()` method first drops the new column from the users table using the `dropColumn` method on the Schema facade. If the database driver being used is PostgreSQL, the migration will also drop the enum type by calling the `DROP TYPE IF EXISTS user_status CASCADE` statement.

Overall, this migration file adds a new column to the users table in the database with an enumeration type and default value of "trial", allowing developers to track the status of each user in their application.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Die Datei `database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php` enthält eine PHP-Klasse, die als Migration fungiert und die Datenbanktabelle `webhooks` modifiziert. Die Klasse definiert zwei Methoden: `up()` und `down()`.

Die Methode `up()` führt folgende Schritte aus:

1. Überprüft den Namen des verwendeten Datenbanktreibers auf 'pgsql'. Wenn der Name nicht 'pgsql' ist, wird die Migration sofort beendet.
2. Führt eine PostgreSQL-DDL-Anweisung aus, um einen neuen Typ 'webhook_frontend_object' zu definieren, der als Enum ('dashboard_chat', 'recherche_start') deklariert ist. Wenn ein Duplikat gefunden wird, wird die Anweisung sofort beendet.
3. Fügt einer Tabelle `webhooks` eine neue Spalte `frontend_object` hinzu und definiert den Typ als 'webhook_frontend_object'. Wenn die Spalte bereits vorhanden ist, wird sie nicht erstellt.
4. Fügt der Tabelle `webhooks` eine eindeutige Einschränkung hinzu, um sicherzustellen, dass jede Kombination aus `user_id` und `frontend_object` in der Tabelle einzigartig ist. Wenn die Einschränkung bereits vorhanden ist, wird sie nicht erstellt.

Die Methode `down()` führt folgende Schritte aus:

1. Überprüft den Namen des verwendeten Datenbanktreibers auf 'pgsql'. Wenn der Name nicht 'pgsql' ist, wird die Migration sofort beendet.
2. Löscht eine Einschränkung mit dem Namen `webhooks_user_frontend_object_unique` aus der Tabelle `webhooks`, wenn sie vorhanden ist.
3. Löscht die Spalte `frontend_object` aus der Tabelle `webhooks`, wenn sie vorhanden ist.
4. Löscht den Typ 'webhook_frontend_object' aus der Datenbank, falls er vorhanden ist.

Die Migration ist für PostgreSQL-Datenbanken ausgelegt und verwendet PostgreSQL-spezifische Syntax und Funktionen.

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
This file is a migration for the Laravel framework, specifically for the database. It contains two methods: `up` and `down`. The `up` method will be executed when running the `php artisan migrate` command to update the database schema, while the `down` method will be executed when running the `php artisan migrate:rollback` command to roll back the migration.

The file uses the `Illuminate\Database\Migrations\Migration` class from Laravel's core to define a new migration class. The `up` and `down` methods are defined using the `public function` keyword, which is required by the framework.

Inside the `up` method, we can see that it uses the `DB::getDriverName()` method to check if the current driver is PostgreSQL. If it is not, then it returns immediately without executing any further code. This is useful for ensuring that the migration only runs on PostgreSQL databases, as some database systems may not support certain features or syntax.

The `up` method then uses a series of `DB::statement()` calls to alter the schema of the `webhooks` table. The first call sets the `password` column to be nullable, which means that it can contain a null value in the database. The second call sets the default value for the `password` column to an empty string, which is useful for initializing the column with a blank value when creating new rows in the table.

The `down` method is similar to the `up` method, but it reverses the process by dropping the nullability constraint and resetting the default value of the `password` column. It also uses the `DB::statement()` method to execute SQL queries against the database.

Overall, this migration makes changes to the `webhooks` table schema to allow for a nullable password column and sets the default value of the column to an empty string when creating new rows in the table.

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
This file adds a new column to the "webhooks" table called "secret". The column is nullable, and its type is text. This change is made using the Laravel framework's migration system.

The file imports several classes from the `Illuminate\Support\Facades` namespace:

* `Migration`: The base class for all migrations in Laravel.
* `Schema`: Provides a way to interact with the database schema.
* `DB`: A facade for the Laravel Database module.

The file also uses several methods from the `DB` class:

* `getDriverName()`: Returns the name of the current driver being used by the database. In this case, it returns "pgsql".
* `statement()`: Executes a raw SQL statement against the database.
* `dropColumnIfExists()`: Drops a column from a table if it exists.

The file defines two methods: `up()` and `down()`. The `up()` method adds the new "secret" column to the "webhooks" table, while the `down()` method drops the column again. Both methods are executed by Laravel when running migrations.

In summary, this file adds a new column to the "webhooks" table and makes it nullable for use as a secret key in webhook requests. The migration is only applicable if the current driver being used by the database is PostgreSQL (hence the check against `getDriverName()`).

---

### database/seeders/DatabaseSeeder.php
Die Datei "DatabaseSeeder.php" ist ein Seeder-Datei, die in Laravel verwendet wird, um Daten für eine neue Anwendung zu initialisieren. Die Klasse `DatabaseSeeder` erbt von der Basisklasse `Illuminate\Database\Seeder`. In der `run`-Methode des Seeders werden zunächst die Rollen für den Benutzer mit dem Email-Adresse "test@example.com" überprüft und ggf. erstellt. Anschließend wird ein neuer Benutzer mit diesem Email-Adressen erstellt und der Rolle "admin" zugewiesen. Schließlich wird der Seeder `RechercheDemoSeeder` aufgerufen, um weitere Daten für die Anwendung hinzuzufügen.

Die wichtigsten Klassen in dieser Datei sind:

* `Database\Seeders\DatabaseSeeder`: Die Basisklasse, aus der die eigene Seeder-Klasse erbt.
* `App\Models\User`: Die Modellklasse für Benutzer in der Anwendung.
* `Illuminate\Database\Console\Seeds\WithoutModelEvents`: Eine Abhängigkeit, die von Laravel verwendet wird, um das Erzeugen von Daten für die Anwendung zu optimieren.
* `RechercheDemoSeeder`: Ein weiterer Seeder, der in dieser Datei aufgerufen wird, um weitere Daten für die Anwendung hinzuzufügen.

---

### database/seeders/RechercheDemoSeeder.php
Dies ist ein PHP-Seeder für einen Prototyp einer Recherche-Demo. Es wird eine Projektinstanz mit verschiedenen Modellen und Phasen erstellt. Beispiele für die Verwendung von Webhooks, Datenbankaufrufen und Modellübergreifenden Suchvorgängen sind darin enthalten.

---

### database/seeders/RoleSeeder.php
{
"file_summary": "This file is a seeder for the 'roles' table in the database. It uses the Spatie\Permission\Models\Role model to create two default roles: 'admin' and 'user'. The guard name for both roles is set to 'web'."
}

---

### database/sql/create_langdock_agent_user.sql
This SQL file creates a new user 'langdock_agent' in the MCP-Postgres DB and grants it read-only access to Recherche tables only. The user is created with restricted read/write access to Recherche tables only. The user can be used for MCP health check.

---

## temp_dummy

### .github/pull_request_template.md
Diese Datei enthält einen Vorlagen-Text für Pull Requests im GitHub-Repository. Sie listet die wichtigsten Klassen, Funktionen oder Endpunkte auf und erläutert ihre Änderungen. Die Datei enthält auch eine Checkliste mit Tipps zur Überprüfung der Code-Kompatibilität und -Qualität.

---

## tests

### tests/Pest.php
This file is a Pest test case for a Laravel application. It defines several things:

1. The main class that extends `PHPUnit\Framework\TestCase` and uses the `Illuminate\Foundation\Testing\RefreshDatabase` trait to refresh the database after each test.
2. An expectation function called "toBeOne" which returns a check for whether the value being tested is equal to 1.
3. A global helper function called "something" that performs some action in the application.

The file also uses the `pest()` function to bind the main class to the test case, and the `expect()` function to expose the expectation functions.

---

### tests/TestCase.php
This file defines a base test case class for the Laravel framework, which serves as a superclass for all other test cases in the project. It is located in the tests directory and imports the Illuminate\Foundation\Testing\TestCase class from the Laravel framework.

---

### tests/Feature/ContactTest.php
This file contains a set of test cases for the `contact` feature in an application. The tests are written using Laravel's built-in testing framework and use PHPUnit to run the tests.

Here is a summary of what each test does:

1. `test('contact form accepts valid submission')` - This test submits a valid contact form through the API and verifies that the submission was successful and the data was stored in the database.
2. `test('contact form works without optional fields')` - This test submits a contact form with only required fields (name, email, project type, message) and verifies that the submission was successful and the data was stored in the database.
3. `test('contact form requires name')` - This test submits a contact form without a name field and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `name` field is listed as an error.
4. `test('contact form requires email')` - This test submits a contact form without an email field and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `email` field is listed as an error.
5. `test('contact form requires valid email')` - This test submits a contact form with an invalid email address and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `email` field is listed as an error.
6. `test('contact form requires project_type')` - This test submits a contact form without a project type field and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `project_type` field is listed as an error.
7. `test('contact form requires message')` - This test submits a contact form without a message field and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `message` field is listed as an error.
8. `test('contact form rejects message exceeding 5000 characters')` - This test submits a contact form with a message field that exceeds the 5000 character limit and verifies that the submission fails with a 422 response code (indicating validation errors) and that the `message` field is listed as an error.

Overall, these tests cover the functionality of the contact form and verify that it behaves correctly when valid data is submitted, that it fails gracefully when invalid data is submitted, and that it enforces the proper validation rules for each field.

---

### tests/Feature/DashboardTest.php
This file contains two test cases for the dashboard feature in a Laravel application. The first test case tests that guests are redirected to the login page when attempting to access the dashboard without being authenticated. The second test case tests that authenticated users can visit the dashboard and receive a successful response.

The file imports the `App\Models\User` model, which is used in the second test case to create an authenticated user. It also uses the `get` method from the Laravel testing library to make HTTP requests to the application's routes. The `assertRedirect` and `assertStatus` methods are used to check that the response redirects to the login page or returns a successful status code, respectively.

---

### tests/Feature/ExampleTest.php
This file contains test cases for the home route. It uses the 'tests' framework to test the response status code and assert that it is 200 (successful).

---

### tests/Feature/LegalPagesTest.php
Die Datei `tests/Feature/LegalPagesTest.php` enthält PHP-Code, der die Funktion des Framework-Testers für Laravel verwendet, um verschiedene Legal-Seiten (Impressum, Datenschutz und AGB) auf der Webseite zu testen.

Die Klasse `LegalPagesTest` importiert die erforderlichen Pakete und definiert drei Tests:

1. Der erste Test überprüft, ob die Impressum-Seite korrekt gerendert wird. Hierfür wird ein GET-Request an die Route `/impressum` gesendet und die Antwort wird mit den Erwartungen verarbeitet (Status 200, Inhalt mit "Benedikt Linn" und "Erzbergerstraße 9a").
2. Der zweite Test überprüft, ob die Datenschutz-Seite korrekt gerendert wird. Hierfür wird ein GET-Request an die Route `/dsgvo` gesendet und die Antwort wird mit den Erwartungen verarbeitet (Status 200, Inhalt mit "Datenschutz").
3. Der dritte Test überprüft, ob die AGB-Seite korrekt gerendert wird. Hierfür wird ein GET-Request an die Route `/agb` gesendet und die Antwort wird mit den Erwartungen verarbeitet (Status 200, Inhalt mit "Geltungsbereich").

Die Tests verwenden die `get`-Methode des Request-Objektes, um eine HTTP GET-Anfrage an eine bestimmte Route zu senden. Die Antwort wird mit den Erwartungen verarbeitet, indem sie den erwarteten Statuscode hat und die erwarteten Inhalte enthält.

Die Tests nutzen die `assert`-Methode des Response-Objektes, um zu überprüfen, ob die Antwort korrekt ist. Wenn ein Fehler auftritt, wird ein Exception-Objekt erstellt und mit einem Fehlercode versehen.

Diese Datei dient als Beispiel für die Verwendung von PHPUnit für das Testen von Legal-Seiten in Laravel.

---

### tests/Feature/ProjektPolicyTest.php
This file is a PHPUnit test file for the `ProjektPolicy` class, which is used to authorize actions on projects. The tests defined in this file check that the policy correctly grants or denies access to various actions based on the user's permissions and the project's ownership.

The file imports several classes and dependencies:

* `App\Models\Recherche\Projekt` - The model class for projects.
* `App\Models\User` - The model class for users.
* `App\Policies\ProjektPolicy` - The policy class for projects.

The tests defined in this file test the following methods of the `ProjektPolicy`:

1. `view()` - Checks that the owner of a project can view it, but that other users cannot.
2. `update()` - Checks that the owner of a project can update it, but that other users cannot.
3. `delete()` - Checks that the owner of a project can delete it, but that other users cannot.
4. `create()` - Checks that any authenticated user can create a new project.

These tests are useful for ensuring that the policy is functioning correctly and that the correct permissions are enforced on projects.

---

### tests/Feature/RechercheTest.php
This is a PHPUnit test file for the "Recherche" feature of a Laravel application. The file contains several test methods that test different aspects of the research functionality, including creating a new research project, dispatching a job to trigger the Langdock agent, validating input fields, and checking user permissions.

The test file imports several classes and facades from the Laravel framework, such as `App\Models\Recherche\Projekt`, `App\Models\User`, `Illuminate\Support\Facades\Queue`, `App\Jobs\TriggerLangdockAgent`, and `Livewire\Volt\Volt`. It also uses the `use` keyword to import various other classes and functions from within the test file.

The test methods are defined using the `test()` function provided by PHPUnit, which takes a closure as an argument that defines the actual test code. Each test method has its own set of assertions that validate different aspects of the research functionality, such as whether a new project was created successfully or whether a job was dispatched correctly.

The file also includes some additional setup and teardown code, which is not strictly necessary for testing but can help to keep the tests organized and maintainable. For example, the `Queue::fake()` method is used to disable actual queueing of jobs during testing, which allows us to avoid having to worry about the timing of queued jobs while still being able to test that they are dispatched correctly.

Overall, this test file provides a comprehensive set of tests for the research functionality in the Laravel application, covering various aspects of the functionality and ensuring that it works as expected.

---

### tests/Feature/WebhookTest.php
Die Datei tests/Feature/WebhookTest.php enthält Tests für die Webhook-API des Projektes. Die wichtigsten Funktionen sind:

* Verwendung von Laravel Facades (`use Illuminate\Support\Facades\Config` und `use Illuminate\Support\Facades\Queue`)
* Testen der Signatur und Timestamp-Header in HTTP-Anfragen mit dem SHA256-Hashing Algorithmus
* Verwendung von Faker ( `\App\Models\User::factory()->withoutTwoFactor()->create()` ) zum Erstellen von Benutzern und Projekten zur Testszenario-Erstellung
* Einen Test, der sicherstellen soll, dass die Anfrage erfolgreich ist, wenn eine gültige Signatur und Timestamp-Header vorhanden sind (HTTP Status Code 200)
* Einen Test, der sicherstellen soll, dass die Anfrage fehlschlägt, wenn ein ungültiger Timestamp oder keine Signatur vorhanden ist (HTTP Status Code 403)
* Einen Test, der sicherstellen soll, dass die Anfrage fehlschlägt, wenn das Timestamp älter als 10 Minuten ist (HTTP Status Code 403)
* Verwendung von Laravel Mockery (`Queue::fake()`) zum Simulieren des Queuing-Prozesses für den Webhook-Handler.

---

### tests/Feature/WelcomeTest.php
This is a PHP file containing unit tests for the "welcome" page of an application. The file uses the Laravel Framework's built-in testing features and includes several test functions that test different aspects of the welcome page.

Here are the main functions in this file:

1. `test('welcome page returns successful response', function () {` - This function tests that the welcome page returns a 200 status code when accessed via GET request.
2. `test('welcome page increments visit counter', function () {` - This function tests that the visit counter is incremented every time the welcome page is accessed.
3. `test('welcome page creates page_view record if none exists', function () {` - This function tests that a new page view record is created in the database when the welcome page is accessed for the first time.
4. `test('guest sees login and register links', function () {` - This function tests that the welcome page displays the "Login" and "Registrieren" links to guests (users who are not logged in).
5. `test('authenticated user sees dashboard link', function () {` - This function tests that authenticated users see the "Dashboard" link on the welcome page.

The file also imports the `App\Models\PageView` and `App\Models\User` models, which are used in these test functions to interact with the database.

---

### tests/Feature/Auth/AuthenticationTest.php
Die Datei `tests/Feature/Auth/AuthenticationTest.php` enthält eine Reihe von Tests für die Authentifizierung in einer Laravel-Anwendung. Diese Tests werden mithilfe der PHPUnit-Bibliothek geschrieben und können ausgeführt werden, um die Funktionalität des Authentifizierungsmoduls zu überprüfen.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `User`: Die Modellklasse für Benutzer in der Anwendung.
* `Features::twoFactorAuthentication([...])`: Eine Hilfsfunktion, die zum Aktivieren der zweistufigen Authentifizierung verwendet wird.
* `route('login')` und `route('login.store')`: Die Endpunkte für das Anmelden und das Speichern des Login-Formulars.
* `route('logout')`: Der Endpunkt zum Abmelden.
* `session()->hasErrors()`: Eine Funktion, die den Status der Fehler-Meldungen in der aktuellen Session überprüft.
* `assertAuthenticated()` und `assertGuest()`: Assertions-Methoden, die verwendet werden, um zu überprüfen, ob ein Benutzer authentifiziert ist oder nicht.

---

### tests/Feature/Auth/EmailVerificationTest.php
This file contains four test cases for email verification functionality in a Laravel application. The tests are using the `tests` category and the `Feature` tag to indicate that they are part of the feature tests for this functionality.

The first test case, `email verification screen can be rendered`, is checking whether the email verification screen can be successfully rendered for an unverified user. The test uses the `actingAs()` method from Laravel's testing framework to authenticate as the user and then retrieves the response for the route `route('verification.notice')`. The response status code is checked to be 200, indicating that the request was successful.

The second test case, `email can be verified`, is checking whether an email address can be successfully verified after clicking on a verification link sent to the user's email address. The test creates an unverified user and uses the `fake()` method from Laravel's event testing framework to prevent any events from being dispatched during the test. It then retrieves the verification URL for the user using the `temporarySignedRoute()` method, and sends a GET request to that URL using the `actingAs()` method. The test checks whether the `Verified` event has been dispatched using the `assertDispatched()` method from Laravel's testing framework, and then checks whether the user's email address has been verified using the `hasVerifiedEmail()` method from Laravel's Auth facade.

The third test case, `email is not verified with invalid hash`, is checking whether an unverified user will not be able to verify their email address if they click on a verification link with an invalid hash. The test creates an unverified user and retrieves the verification URL for the user using the `temporarySignedRoute()` method, but uses a wrong hash in the URL. The test then sends a GET request to that URL using the `actingAs()` method, and checks whether the user's email address has been verified using the `hasVerifiedEmail()` method from Laravel's Auth facade.

The fourth test case, `already verified user visiting verification link is redirected without firing event again`, is checking whether an already verified user will be redirected to the dashboard if they try to verify their email address using a verification link that has already been used. The test creates a verified user and uses the `fake()` method from Laravel's event testing framework to prevent any events from being dispatched during the test. It then retrieves the verification URL for the user using the `temporarySignedRoute()` method, sends a GET request to that URL using the `actingAs()` method, and checks whether the `Verified` event has been dispatched using the `assertDispatched()` method from Laravel's testing framework. The test then checks whether the user's email address has not been verified again using the `hasVerifiedEmail()` method from Laravel's Auth facade.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Diese Datei ist Teil des Laravel-Frameworks und enthält einen Test für die PasswordConfirmation-Funktion.

* Die Klasse `PasswordConfirmationTest` erbt von der `FeatureTest`-Klasse und testet die Funktion, um sicherzustellen, dass das Passwortbestätigungs-Screen correctly rendered wird.
* Die Testmethode `test('confirm password screen can be rendered', function ())` testet die Route `password.confirm`, um zu überprüfen, ob sie ordnungsgemäß funktioniert und den erwarteten Statuscode 200 zurückgibt.
* Der Test verwendet die `actingAs`-Methode des `TestCase`-Objekts, um sich als authentifizierter Benutzer anzumelden, bevor er auf die Route zugreift.
* Die Testmethode überprüft auch den Statuscode der Antwort und stellt sicher, dass er 200 ist.

---

### tests/Feature/Auth/PasswordResetTest.php
The file is a test suite for the password reset functionality in an application. The tests are written using the Laravel testing framework and are designed to ensure that the password reset feature works as expected.

Here's a summary of the main functions and features tested in the file:

1. `use App\Models\User;`: This is an import statement for the User model, which is used to interact with the application's user database.
2. `use Illuminate\Auth\Notifications\ResetPassword;`: This is an import statement for the ResetPassword notification class, which is used to send password reset links to users.
3. `use Illuminate\Support\Facades\Notification;`: This is an import statement for the Notification facade, which is used to interact with notifications in the application.
4. `test('reset password link screen can be rendered', function () { ... })`: This test verifies that the password reset link screen can be rendered and returns a 200 status code.
5. `test('reset password link can be requested', function () { ... })`: This test verifies that a password reset link can be requested for a valid email address, and that the notification class is sent to the user.
6. `test('reset password screen can be rendered', function () { ... })`: This test verifies that the password reset screen can be rendered and returns a 200 status code.
7. `test('password can be reset with valid token', function () { ... })`: This test verifies that a user's password can be successfully reset using a valid reset token, and that the password is updated in the database.

Overall, this file contains a suite of tests for the password reset functionality in an application, which are designed to ensure that the feature works as expected and provides a good user experience.

---

### tests/Feature/Auth/RegistrationTest.php
This is a PHP file containing integration tests for the registration functionality of an application. The tests are written using Laravel's built-in testing framework and use the `phpunit` executable to run them.

The file consists of two tests:

1. The first test checks that the registration screen can be rendered and returns a 200 status code. This test is checking that the route for the registration page exists and can be accessed without any issues.
2. The second test registers a new user with valid credentials (name, email, password, and password confirmation) and verifies that the user has been successfully registered by checking that the session has no errors and the user is authenticated. This test is checking that the registration functionality works correctly and that the user can be created with the given credentials.

The file also imports some external dependencies, such as the `route` function to generate URLs and the `assertSessionHasNoErrors` and `assertAuthenticated` methods from Laravel's testing framework.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
This file contains two tests for the two-factor authentication feature in a Laravel application. The first test checks that when a user is not authenticated, they are redirected to the login page when trying to access the two-factor challenge endpoint. The second test checks that the two-factor challenge can be rendered correctly and that the form is displayed correctly with the correct fields.

The tests use the `Laravel\Fortify\Features` class to check if the two-factor authentication feature is enabled in the application. If it's not, the tests are skipped with a message indicating that the feature is not available.

The first test uses the `get` method to make a GET request to the two-factor challenge endpoint and checks that the response is a redirect to the login page. The second test uses the `post` method to make a POST request to the login endpoint with credentials for an existing user, and then checks that the response is a redirect to the two-factor challenge page.

The tests use the `User` model to create a new user and log them in using the `post` method. The `Features::twoFactorAuthentication` method is used to enable the two-factor authentication feature for the current test.

---

### tests/Feature/Chat/BigResearchChatTest.php
Dieser Test-Datei enthält eine Vielzahl von Funktionen und Endpunkten, die das Verhalten des Big Research Chat in verschiedenen Situationen testen. Hier sind einige der wichtigsten Klassen, Funktionen und Endpunkte:

* `App\Models\ChatMessage`: Diese Klasse repräsentiert eine einzelne Nachricht im Chat. Die Tests überprüfen, ob Nachrichten korrekt gespeichert werden und ob die DB-Queries ordnungsgemäß sind.
* `App\Models\User`: Diese Klasse repräsentiert einen Benutzer des Systems. Die Tests überprüfen, ob der Benutzer authentifiziert wird und ob die Nachrichten korrekt zugeordnet werden.
* `App\Models\Webhook`: Diese Klasse repräsentiert eine Webhooks-Instanz im System. Die Tests überprüfen, ob Webhooks ordnungsgemäß erstellt werden und ob die DB-Queries ordnungsgemäß sind.
* `Illuminate\Support\Facades\Http`: Diese Klasse bietet eine Möglichkeit, HTTP-Anfragen zu simulieren und zu testen. Die Tests überprüfen, ob HTTP-Anfragen korrekt gesendet werden und ob die Antworten ordnungsgemäß interpretiert werden.
* `Livewire\Volt\Volt`: Diese Klasse ermöglicht das Simulieren von Livewire-Komponenten für Tests. Die Tests überprüfen, ob die Komponenten ordnungsgemäß rendert und ob die Eigenschaften korrekt aktualisiert werden.
* `test('chat: nachricht senden erstellt user- und assistant-message')`: Diese Funktion testet, ob eine Nachricht erfolgreich gesendet wird und ob die Nachrichten in der DB ordnungsgemäß gespeichert werden.
* `test('chat: validiert nachricht als pflichtfeld')`: Diese Funktion testet, ob ein Fehler generiert wird, wenn keine Nachricht gesendet wird.
* `test('chat: ohne webhook wird nichts gespeichert')`: Diese Funktion testet, ob eine Nachricht erfolgreich gesendet wird, wenn es keinen Webhook gibt.
* `test('chat: http-fehler erzeugt fehlermeldung')`: Diese Funktion testet, ob ein Fehler generiert wird, wenn ein HTTP-Fehler auftritt.
* `test('chat: connection exception erzeugt fehlermeldung')`: Diese Funktion testet, ob ein Fehler generiert wird, wenn eine Verbindung refused wird.
* `test('chat: clearHistory löscht nur eigene nachrichten')`: Diese Funktion testet, ob die ClearHistory-Funktion nur Nachrichten löscht, die dem aktuellen Benutzer zugeordnet sind.
* `test('chat: getChatMessages gibt nachrichten chronologisch zurueck')`: Diese Funktion testet, ob die GetChatMessages-Funktion ordnungsgemäß Nachrichten aus der DB liest und die Ergebnisse in der richtigen Reihenfolge zurückgibt.
* `test('chat: ohne webhook zeigt leere liste')`: Diese Funktion testet, ob eine Liste von Nachrichten angezeigt wird, wenn es keinen Webhook gibt.
* `test('chat: nachricht max 10000 zeichen')`: Diese Funktion testet, ob ein Fehler generiert wird, wenn eine Nachricht länger als 10000 Zeichen ist.

---

### tests/Feature/Recherche/PhaseP1Test.php
Die Datei `tests/Feature/Recherche/PhaseP1Test.php` enthält Funktionen zur Testung von P1-Features in der Recherchephase. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte:

* `App\Models\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}`: Diese Klassen sind von der Laravel-Framework und dienen zum Speichern und Bearbeiten von Daten in der Datenbank.
* `App\Models\User`: Diese Klasse ist ebenfalls von der Laravel-Framework und dient zum Speichern und Bearbeiten von Nutzerdaten.
* `Livewire\Volt\Volt`: Dies ist eine Klasse, die von Livewire bereitgestellt wird und es ermöglicht, Livewire-Komponenten zu testen.
* `tests/Feature/Recherche/PhaseP1Test.php`: Diese Datei enthält Funktionen zur Testung von P1-Features in der Recherchephase. Hier sind die wichtigsten Klassen, Funktionen und Endpunkte:
	+ `test('P1: kann strukturmodellwahl erstellen', function () { ... })`: Diese Funktion testet, ob ein Benutzer eine Strukturmodellwahl erstellen kann.
	+ `test('P1: kann komponente erstellen', function () { ... })`: Diese Funktion testet, ob ein Benutzer eine Komponente erstellen kann.
	+ `test('P1: kann kriterium erstellen', function () { ... })`: Diese Funktion testet, ob ein Benutzer ein Kriterium erstellen kann.
	+ `test('P1: fremder user bekommt 403', function () { ... })`: Diese Funktion testet, ob ein Benutzer, der nicht der Besitzer des Projekts ist, keine Berechtigung hat, die P1-Features zu bearbeiten.
* `assertDatabaseHas('p1_strukturmodell_wahl', [...])`: Diese Anweisung testet, ob das Modell in der Datenbank vorhanden und mit den angegebenen Werten übereinstimmt.
* `assertDatabaseHas('p1_komponenten', [...])`: Diese Anweisung testet, ob die Komponente in der Datenbank vorhanden und mit den angegebenen Werten übereinstimmt.
* `assertDatabaseHas('p1_kriterien', [...])`: Diese Anweisung testet, ob das Kriterium in der Datenbank vorhanden und mit den angegebenen Werten übereinstimmt.

---

### tests/Feature/Recherche/PhaseP2Test.php
Diese Datei enthält Unit-Tests für die Funktionen der Recherche-Phase P2. Die Tests überprüfen die Validierung von Feldern und das Speichern von Daten in der Datenbank.

---

### tests/Feature/Recherche/PhaseP3Test.php
Die Datei `tests/Feature/Recherche/PhaseP3Test.php` enthält Tests für die Recherchen-Phase P3, die sich mit der Erfassung und Verwaltung von Datenbanken, Disziplinen, geografischen Filtern und gräuer Literatur in einem Projekt auseinandersetzt. Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `P3Datenbankmatrix`: Diese Klasse repräsentiert eine Datenbankmatrix in der Recherche-Phase P3 und enthält Informationen zur zugrunde liegenden Datenbank und ihrem Nutzungsrecht.
* `P3Disziplin`: Diese Klasse stellt eine Disziplin in einer Projektphase P3 dar und enthält Informationen zu den von der Disziplin abgedeckten Themengebieten.
* `P3GeografischerFilter`: Diese Klasse repräsentiert einen geografischen Filter in einer Projektphase P3 und enthält Informationen zu dem Land oder der Region, für das der Filter angewendet wird.
* `P3GraueLiteratur`: Diese Klasse stellt eine graue Literatur in einer Projektphase P3 dar und enthält Informationen zur Quelle der gräuer Literatur.

Die wichtigsten Funktionen in dieser Datei sind:

* `newDb()`: Diese Funktion erstellt eine neue Datenbankmatrix in einem Projekt.
* `saveDb()`: Diese Funktion speichert die Daten einer Datenbankmatrix in der Datenbank.
* `editDb()`: Diese Funktion bearbeitet eine vorhandene Datenbankmatrix in einem Projekt.
* `deleteDb()`: Diese Funktion löscht eine vorhandene Datenbankmatrix aus einem Projekt.
* `newDis()`: Diese Funktion erstellt eine neue Disziplin in einem Projekt.
* `saveDis()`: Diese Funktion speichert die Daten einer Disziplin in der Datenbank.
* `editDis()`: Diese Funktion bearbeitet eine vorhandene Disziplin in einem Projekt.
* `deleteDis()`: Diese Funktion löscht eine vorhandene Disziplin aus einem Projekt.
* `newGeo()`: Diese Funktion erstellt einen neuen geografischen Filter in einem Projekt.
* `saveGeo()`: Diese Funktion speichert die Daten eines geografischen Filters in der Datenbank.
* `editGeo()`: Diese Funktion bearbeitet einen vorhandenen geografischen Filter in einem Projekt.
* `deleteGeo()`: Diese Funktion löscht einen vorhandenen geografischen Filter aus einem Projekt.
* `newGrau()`: Diese Funktion erstellt eine neue graue Literatur in einem Projekt.
* `saveGrau()`: Diese Funktion speichert die Daten einer grauen Literatur in der Datenbank.
* `editGrau()`: Diese Funktion bearbeitet eine vorhandene graue Literatur in einem Projekt.
* `deleteGrau()`: Diese Funktion löscht eine vorhandene graue Literatur aus einem Projekt.

Die Datei importiert auch die benötigten Klassen und Funktionen von Laravel, um die Tests auszuführen.

---

### tests/Feature/Recherche/PhaseP4Test.php
{
  "file_summary": "Die Datei enthält Funktionen zur Verwaltung von Suchstrings und Anpassungsprotokollen für das Projekt P4."
}


Datei: models/Recherche/P4Suchstring.php
Kategorie: Modelle
```
<?php

namespace App\Models\Recherche;

use Illuminate\Database\Eloquent\Model;

class P4Suchstring extends Model
{
    protected $table = 'p4_suchstrings';

    public function projekt()
    {
        return $this->belongsTo(Projekt::class);
    }
}
```


Datei: models/Recherche/P4ThesaurusMapping.php
Kategorie: Modelle
```
<?php

namespace App\Models\Recherche;

use Illuminate\Database\Eloquent\Model;

class P4ThesaurusMapping extends Model
{
    protected $table = 'p4_thesaurus_mapping';

    public function projekt()
    {
        return $this->belongsTo(Projekt::class);
    }
}
```


Datei: models/Recherche/P4Anpassungsprotokoll.php
Kategorie: Modelle
```
<?php

namespace App\Models\Recherche;

use Illuminate\Database\Eloquent\Model;

class P4Anpassungsprotokoll extends Model
{
    protected $table = 'p4_anpassungsprotokoll';

    public function suchstring()
    {
        return $this->belongsTo(P4Suchstring::class);
    }
}
```

Datei: controllers/RechercheController.php
Kategorie: Controller
```
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\Projekt;
use App\Models\Recherche\{P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll};

class RechercheController extends Controller
{
    public function phaseP4($projekt)
    {
        $this->authorize('view', $projekt);

        return view('recherche.phase-p4', [
            'projekt' => $projekt,
            'suchstrings' => P4Suchstring::where('projekt_id', $projekt->id)->get(),
            'thesaurusMappings' => P4ThesaurusMapping::where('projekt_id', $projekt->id)->get(),
            'anpassungsprotokolle' => P4Anpassungsprotokoll::where('suchstring_id', $projekt->id)->get(),
        ]);
    }
}
```

Datei: routes/web.php
Kategorie: Routen
```
<?php

Route::group(['middleware' => ['auth']], function () {
    Route::post('/projects/{project}/recherche/phase-p4', 'RechercheController@phaseP4')->name('recherche.phaseP4');
});
```

---

### tests/Feature/Recherche/PhaseP5Test.php
This file contains tests for the P5 phase of a research project. It uses PHPUnit to test the functionality of various components of the P5 phase, such as the Prisma Zahlen, Screening Kriterien, Tool Entscheidung, and Screening Entscheidung.

The file imports several classes and functions from the App namespace, including the User model, the Projekt model, and the Volt template engine. It also uses Livewire's Volt test API to simulate user interactions with the P5 phase.

The tests are structured into five categories:

1. **Prisma Zahlen**: Tests for the Prisma Zahlen component, which allows users to input and save PRISMA zahlen data for their project. The tests create a new project and user, then use Livewire's Volt test API to simulate user interactions with the Prisma Zahlen form.
2. **Screening Kriterien**: Tests for the Screening Kriterien component, which allows users to input and save screening criteria for their project. The tests create a new project and user, then use Livewire's Volt test API to simulate user interactions with the Screening Kriterien form.
3. **Tool Entscheidung**: Tests for the Tool Entscheidung component, which allows users to select and save a tool for their project. The tests create a new project and user, then use Livewire's Volt test API to simulate user interactions with the Tool Entscheidung form.
4. **Screening Entscheidung**: Tests for the Screening Entscheidung component, which allows users to input and save screening decisions for their project. The tests create a new project and user, then use Livewire's Volt test API to simulate user interactions with the Screening Entscheidung form.
5. **Auth**: Tests for authentication and authorization in the P5 phase. The tests create two new users and verify that only one user can access the P5 phase of their project.

---

### tests/Feature/Recherche/PhaseP6Test.php
Die Datei `tests/Feature/Recherche/PhaseP6Test.php` enthält eine Reihe von Tests zur Überprüfung der Funktionalität der Phase 6 der Recherche-Anwendung, die auf Laravel Framework basiert. Die Tests testen das Erstellen, Bearbeiten und Löschen von Qualitätsbewertungen und Lückenanalysen für ein Projekt.

Die wichtigsten Klassen, Funktionen oder Endpunkte in dieser Datei sind:

* `App\Models\Recherche\Projekt`: Die Modell-Klasse für das Projekt, das die Qualitätsbewertungen und Lückenanalysen enthält.
* `App\Models\Recherche\P5Treffer`: Die Modell-Klasse für den Treffer, der in der Phase 5 generiert wird.
* `App\Models\Recherche\P6Qualitaetsbewertung`: Die Modell-Klasse für die Qualitätsbewertungen in der Phase 6.
* `App\Models\Recherche\P6Luckenanalyse`: Die Modell-Klasse für die Lückenanalysen in der Phase 6.
* `App\Models\User`: Die Modell-Klasse für den Benutzer, der das Projekt bearbeitet und die Qualitätsbewertungen und Lückenanalysen erstellt/bearbeitet/löscht.
* `Livewire\Volt\Volt`: Der Livewire Volt-Datenstrom, der verwendet wird, um die Benutzeroberfläche zu generieren.

Die Tests in dieser Datei testen folgende Funktionen:

1. Erstellen einer Qualitätsbewertung: Der Test erstellt eine neue Qualitätsbewertung und überprüft, ob sie erfolgreich gespeichert wurde.
2. Überprüfen der Pflichtfelder bei der Erstellung einer Qualitätsbewertung: Der Test überprüft, ob die Pflichtfelder für eine Qualitätsbewertung (Treffer-ID, Studientyp und Gesamturteil) obligatorisch sind.
3. Bearbeiten einer Qualitätsbewertung: Der Test bearbeitet eine vorhandene Qualitätsbewertung und überprüft, ob sie erfolgreich gespeichert wurde.
4. Löschen einer Qualitätsbewertung: Der Test löscht eine vorhandene Qualitätsbewertung und überprüft, ob sie erfolgreich gelöscht wurde.
5. Erstellen einer Lückenanalyse: Der Test erstellt eine neue Lückenanalyse und überprüft, ob sie erfolgreich gespeichert wurde.
6. Überprüfen des fehlenden Aspekts bei der Erstellung einer Lückenanalyse: Der Test überprüft, ob der fehlende Aspekt für eine Lückenanalyse obligatorisch ist.
7. Bearbeiten einer Lückenanalyse: Der Test bearbeitet eine vorhandene Lückenanalyse und überprüft, ob sie erfolgreich gespeichert wurde.
8. Löschen einer Lückenanalyse: Der Test löscht eine vorhandene Lückenanalyse und überprüft, ob sie erfolgreich gelöscht wurde.
9. Verifizieren des Zugriffs auf die Phase 6 für einen fremden Benutzer: Der Test überprüft, ob ein fremder Benutzer keinen Zugriff auf die Phase 6 hat.

---

### tests/Feature/Recherche/PhaseP7Test.php
Die Datei `tests/Feature/Recherche/PhaseP7Test.php` enthält PHPUnit-Tests für eine Webanwendung, die eine Recherchenphase P7 implementiert. Die wichtigsten Funktionen und Klassen in dieser Datei sind:

* `App\Models\Recherche`: Diese Klasse enthält Modelle für die verschiedenen Komponenten der Recherchenphase P7. Hierzu gehören beispielsweise das Modell `Projekt`, das Modell `P5Treffer` und das Modell `P7Datenextraktion`.
* `App\Models\User`: Diese Klasse enthält ein Modell für Benutzer der Anwendung. In diesem Test werden die Funktionen `actingAs()` und `assertDatabaseHas()` verwendet, um Benutzerdaten zu simulieren und die Datenbank zu überprüfen.
* `Livewire\Volt\Volt`: Diese Klasse wird für die Verarbeitung von Livewire-Anfragen in der Webanwendung verwendet. In diesem Test werden die Funktionen `test()`, `call()` und `assertSet()` zur Simulation von Livewire-Interaktionen verwendet.
* `P7SyntheseMethode`, `P7Datenextraktion`, `P7MusterKonsistenz` und `P7GradeEinschaetzung`: Diese Klassen enthalten Modelle für die verschiedenen Komponenten der Recherchenphase P7. Hierbei handelt es sich um eine Sammlung von Datenbanktabellen, die die verschiedenen Aspekte der Recherchephase P7 speichern.
* `tests/Feature/Recherche/PhaseP7Test`: Diese Klasse enthält PHPUnit-Tests für die Funktionen und Klassen in der Datei `Recherche/PhaseP7.php`. In diesem Test werden verschiedene Livewire-Interaktionen simuliert, um sicherzustellen, dass die Recherchenphase P7 funktionsfähig ist.

Insgesamt enthält diese Datei eine Sammlung von Tests, die die Funktionalität der Recherchenphase P7 in einer Webanwendung überprüfen und sicherstellen, dass sie ordnungsgemäß implementiert ist.

---

### tests/Feature/Recherche/PhaseP8Test.php
Die Datei `tests/Feature/Recherche/PhaseP8Test.php` enthält Unit-Tests für die Recherchen-Phase P8, die sich mit der Suche und dem Überwachen von Suchprotokoll und Limitationen beschäftigt. Die Tests überprüfen, ob die benötigten Klassen, Funktionen und Endpunkte ordnungsgemäß funktionieren und ob die Validierung der Formulare korrekt ist.

Die wichtigsten Klassen in dieser Datei sind:

* `App\Models\Recherche\Projekt` - Modell-Klasse für das Projekt, das die Recherchen durchführt.
* `App\Models\Recherche\P8Suchprotokoll` - Modell-Klasse für das Suchprotokoll.
* `App\Models\Recherche\P8Limitation` - Modell-Klasse für die Limitationen.
* `App\Models\Recherche\P8Reproduzierbarkeitspruefung` - Modell-Klasse für die Reproduzierbarkeitprüfungen.
* `App\Models\Recherche\P8UpdatePlan` - Modell-Klasse für den Update-Plan.

Die Tests in dieser Datei verwenden die `Volt`-Klasse der Livewire-Bibliothek, um die Komponenten und Funktionen der Recherchen-Phase P8 zu testen. Die wichtigsten Funktionen sind:

* `newSp` - Erstellen eines neuen Suchprotokolls.
* `editSp` - Bearbeiten eines Suchprotokolls.
* `deleteSp` - Löschen eines Suchprotokolls.
* `newLim` - Erstellen einer neuen Limitation.
* `editLim` - Bearbeiten einer Limitation.
* `deleteLim` - Löschen einer Limitation.
* `newRep` - Erstellen einer neuen Reproduzierbarkeitprüfung.
* `editRep` - Bearbeiten einer Reproduzierbarkeitprüfung.
* `deleteRep` - Löschen einer Reproduzierbarkeitprüfung.
* `newUp` - Erstellen eines neuen Update-Plans.
* `editUp` - Bearbeiten eines Update-Plans.
* `deleteUp` - Löschen eines Update-Plans.

Die Tests überprüfen, ob die Formulare korrekt validiert werden und ob die Daten ordnungsgemäß in die Datenbank geschrieben werden.

---

### tests/Feature/Recherche/ProjektDetailTest.php
Die Datei `tests/Feature/Recherche/ProjektDetailTest.php` enthält Tests für eine Livewire-Komponente namens "recherche.projekt-detail". Die Komponenten richtet sich an Benutzer, die einen Projekt-Eintrag in einem Recherchearbeitssystem bearbeiten möchten.

Die wichtigsten Klassen, Funktionen und Endpunkte sind:

* `Projekt`: Eine Modell-Klasse für die Datenbanktabelle "projekte".
* `User`: Eine Modell-Klasse für die Datenbanktabelle "users".
* `Volt`: Ein Test-Objekt, das verwendet wird, um Livewire-Komponenten zu testen.
* `recherche.projekt-detail`: Die Name der Livewire-Komponente, die im Code verwendet wird.
* `switchTab`: Eine Funktion, die verwendet wird, um den aktiven Tab in der Komponente zu ändern.
* `getPhaseStatus`: Eine Funktion, die verwendet wird, um den Status einer bestimmten Phase (5-8) zurückzugeben.
* `assertSet`: Eine Assertion, die verwendet wird, um sicherzustellen, dass eine bestimmte Eigenschaft einer Komponente auf einen bestimmten Wert gesetzt ist.
* `assertDontSee`: Eine Assertion, die verwendet wird, um sicherzustellen, dass ein bestimmter Text in der Komponente nicht angezeigt wird.
* `assertStatus`: Eine Assertion, die verwendet wird, um sicherzustellen, dass eine HTTP-Antwort einen bestimmten Status hat.
* `assertSee`: Eine Assertion, die verwendet wird, um sicherzustellen, dass ein bestimmter Text in der Komponente angezeigt wird.

---

### tests/Feature/Settings/AppearanceTest.php
This is a test file for the "Settings - Appearance" feature in an application. The file contains several tests that verify different aspects of the appearance settings page, including the rendering of the page itself, the presence and functionality of certain elements, and the authorization requirements for accessing the page.

Here's a breakdown of the code:

* `use App\Models\User;`: This line imports the User model, which is used in the tests to create a test user account that can access the settings page.
* `use Livewire\Volt\Volt;`: This line imports the Volt class from the Livewire library, which provides a fluent interface for making assertions about HTML content.
* `uses(\Illuminate\Foundation\Testing\RefreshDatabase::class);`: This line includes the RefreshDatabase trait, which provides functionality for resetting the database to its original state before each test.
* `beforeEach(function () { ... })`: This function is called before each test and sets up a test user account with the necessary permissions to access the settings page.
* `test('appearance: rendert alle drei buttons mit data-mode attribut', function () { ... })`: This test verifies that all three appearance mode buttons are rendered with the correct "data-mode" attribute value.
* `test('appearance: rendert appearance-toggle container', function () { ... })`: This test verifies that the appearance toggle container is present and correctly rendered.
* `test('appearance: rendert client-seitiges script', function () { ... })`: This test verifies that the client-side script for setting the appearance mode is correctly rendered.
* `test('appearance: seite ist nur fuer eingeloggte user erreichbar', function () { ... })`: This test verifies that the settings page can only be accessed by authenticated users.
* `test('appearance: seite laesst sich aufrufen', function () { ... })`: This test verifies that the settings page is accessible and renders correctly when a user is logged in.

---

### tests/Feature/Settings/PasswordUpdateTest.php
This file contains two tests for the password update functionality in the settings page of a web application. The first test, `password can be updated`, creates a new user with a password and then logs in as that user using Livewire. It then sets the current password to "password", the new password to "new-password", and the password confirmation to "new-password". It calls the updatePassword method on the settings page, which updates the user's password to the new value. The test checks that no errors are returned and that the hashed password in the database has been updated correctly.

The second test, `correct password must be provided to update password`, also creates a new user with a password and logs in as that user using Livewire. It then sets the current password to "wrong-password", the new password to "new-password", and the password confirmation to "new-password". It calls the updatePassword method on the settings page, which should return an error because the current password is incorrect. The test checks that the errors returned by the Livewire component include the expected field name (current_password).

---

### tests/Feature/Settings/ProfileUpdateTest.php
This is a PHPUnit test file for testing the functionality of the profile update feature in a Laravel application. The file contains several tests that simulate different scenarios for updating user profiles and deleting user accounts.

The tests use the `actingAs()` method provided by the Laravel Testing Helpers to create a logged-in user and then perform actions on their profile. They also use the `Volt` class provided by Livewire to make requests to the profile settings page and test the functionality of the update profile information and delete user account features.

The tests cover several scenarios, including:

* Testing that the profile page is displayed correctly for a logged-in user.
* Testing that the user's name and email address can be updated successfully.
* Testing that the user's email verification status remains unchanged if their email address is not changed.
* Testing that a user can delete their account by providing the correct password.
* Testing that an error is returned if the wrong password is provided when trying to delete an account.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
The file `tests/Feature/Settings/TwoFactorAuthenticationTest.php` contains unit tests for the two-factor authentication feature in a Laravel application. It uses the PHPUnit testing framework to test various aspects of the two-factor authentication functionality.

Here are the main functionalities of the file:

1. The `use` statements import the necessary classes and namespaces needed for the tests.
2. The `beforeEach` method is called before each test, and it checks if two-factor authentication is enabled on the application. If not, the test is skipped.
3. The first test, `two factor settings page can be rendered`, verifies that the two-factor settings page can be displayed properly. It creates a user without two-factor authentication and logs in as that user, then visits the two-factor settings page and checks for various elements such as "Two Factor Authentication" and "Disabled".
4. The second test, `two factor settings page requires password confirmation when enabled`, verifies that when two-factor authentication is enabled, the user must enter their password to access the settings page. It creates a user with two-factor authentication disabled and tries to visit the two-factor settings page without entering their password, which results in a redirect to the password confirmation page.
5. The third test, `two factor settings page returns forbidden response when two factor is disabled`, verifies that when two-factor authentication is disabled on the application, the user cannot access the two-factor settings page. It disables the two-factor authentication feature by setting the `fortify.features` configuration key to an empty array, creates a user with two-factor authentication enabled, and tries to visit the two-factor settings page without entering their password, which results in a forbidden response.
6. The fourth test, `two factor authentication disabled when confirmation abandoned between requests`, verifies that if a user has abandoned the two-factor authentication process by not confirming their code, the two-factor authentication is disabled for them. It creates a user with two-factor authentication enabled, and visits the two-factor settings page without entering their password or confirming their code, which results in the two-factor authentication being disabled.

Overall, these tests verify that the two-factor authentication feature works properly and that it correctly handles various scenarios related to enabling, disabling, and abandoning the two-factor authentication process.

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei `tests/Feature/Settings/WebhooksTest.php` enthält eine Reihe von Tests zur Überprüfung der Funktionalität des Webhook-Features in einer Laravel-Anwendung. Die wichtigsten Klassen und Funktionen sind:

* `App\Models\User`: Modell für den Benutzer, der die Anwendung verwendet.
* `App\Models\Webhook`: Modell für den Webhook, der die Verbindung zwischen der Anwendung und einem externen System herstellt.
* `Livewire\Volt\Volt`: Klasse zur Erzeugung von Test-Scenarios in Livewire-Komponenten.
* `Illuminate\Foundation\Testing\RefreshDatabase`: Trait für die Durchführung von Datenbanktests.
* `settings.webhooks`: Livewire-Komponente, die den Webhook-Zustand und -Verhalten verwaltet.

Die Tests in dieser Datei testen verschiedene Aspekte des Webhook-Features:

* Speichern eines neuen webhooks: Das Test-Szenario erstellt einen neuen webhook und überprüft, ob die URL und der Secret korrekt gespeichert werden.
* Aktualisieren eines bestehenden webhooks: Das Szenario aktualisiert einen bestehenden webhook und überprüft, ob die URL und der Secret korrekt aktualisiert werden.
* Überprüfung des URL-Formats: Das Szenario testet, ob die Methode `saveDashboard()` den Fehler "url" auslöst, wenn ein ungültiges Format für die URL verwendet wird.
* Überprüfung der Maximalgröße der URL: Das Szenario testet, ob die Methode `saveDashboard()` den Fehler "max" auslöst, wenn die URL eine Größe von mehr als 500 Zeichen hat.
* Löschen eines webhooks: Das Szenario löscht einen bestehenden webhook und überprüft, ob der Zustand zurückgesetzt wird und der Webhook nicht mehr in der Datenbank vorhanden ist.
* Überprüfung des Zugriffs auf fremde Webhooks: Das Szenario testet, ob die Methode `mount()` den Fehler "forbidden" auslöst, wenn versucht wird, einen fremden webhook zu laden.

---

### tests/Unit/ExampleTest.php
Tests a simple truthy value using the expect() method from PHPUnit.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Die Datei `app/Filament/Resources/ContactResource/Pages/ListContacts.php` ist eine PHP-Datei und enthält eine Klasse, die von Filament verwendet wird, um eine Liste von Kontakten anzuzeigen. Die wichtigsten Funktionen dieser Datei sind:

* Es definiert eine neue Klasse namens `ListContacts`, die von der Basisklasse `Filament\Resources\Pages\ListRecords` erbt.
* Die Klasse verwendet die `ContactResource`-Klasse, um die Kontakt-Daten zu verwenden.
* Die Klasse definiert den Namen der Ressourcen-Klasse als `$resource`, damit sie verwendet werden kann, um die Kontakte in einer Liste anzuzeigen.

Die Datei importiert keine external libraries oder APIs und hat keinerlei Datenbank-Zugriffe.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Die Datei app/Filament/Resources/ContactResource/Pages/ViewContact.php enthält eine PHP-Klasse, die eine Ansicht für ein Objekt des Typs "Contact" in einer Filament-Ressource darstellt. Die Klasse erweitert das ViewRecord-Objekt von Filament und legt die zugehörige Ressourcenklasse fest.

Die wichtigsten Funktionen dieser Datei sind:

* Eine Ansicht für ein Objekt des Typs "Contact" in einer Filament-Ressource darstellt.
* Erweitert das ViewRecord-Objekt von Filament.
* Legt die zugehörige Ressourcenklasse fest.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Die Datei `app/Filament/Resources/UserResource/Pages/EditUser.php` ist eine Implementierung von Filaments `EditRecord`-Klasse für die Bearbeitung eines Benutzers. Es handelt sich um eine PHP-Datei, die in der Laravel-Anwendung enthalten ist und durch die Benutzerinformationen bearbeitet werden können.

Die Klasse `EditUser` erweitert die Basisklasse `Filament\Resources\Pages\EditRecord`, was bedeutet, dass sie alle Funktionen und Eigenschaften dieser Klasse hat. Die wichtigsten Funktionen sind:

* Definieren des zu bearbeitenden Benutzer-Records mit der statischen Eigenschaft `$resource`
* Implementieren der Basisfunktionalität für die Bearbeitung von Benutzerdaten, wie z.B. das Anzeigen und Ändern von Benutzernamen, Email-Adresse etc.

Die Datei importiert auch die `UserResource`-Klasse, die die Definition des zu bearbeitenden Benutzer-Records enthält.

Insgesamt ist die Funktion dieser Datei, Benutzerinformationen zu bearbeiten und zu ändern.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
This file defines a list of users page for the 'UserResource' resource in Filament. It extends the 'ListRecords' class provided by Filament and sets the 'resource' property to the 'UserResource' class.

---

### resources/views/dashboard.blade.php
Die Datei "resources/views/dashboard.blade.php" ist eine Blade-Template-Datei, die den Inhalt eines Dashboards für den Benutzer darstellt. Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `x-layouts.app`: Dies ist ein Layout-Komponenten, das für die gesamte Anwendung verwendet wird. Es stellt den Hauptteil des Dashboards bereit und enthält eine Navigation und einen Footer.
* `dashboard.blade.php`: Diese Datei ist eine Blade-Template-Datei, die den Inhalt des Dashboards für den Benutzer darstellt. Sie enthält verschiedene Elemente wie eine Liste der letzten Projekte, ein Chat-Fenster und einige Schaltflächen.
* `recherche`: Dies ist ein Controller, der die Recherchen-Funktion der Anwendung verwaltet. Er enthält Methoden wie `index()`, `create()`, `store()`, `show()` und `destroy()`.
* `auth()->user()->name`: Diese Funktion ruft den Namen des aktuellen Benutzers ab. Sie wird verwendet, um den Willkommen-Text auf der Seite anzupassen.
* `\App\Models\Contact::class`: Dies ist eine Modell-Klasse, die Kontaktinformationen verwaltet. Die `@can`-Anweisung in dieser Datei überprüft, ob der aktuelle Benutzer die Berechtigung hat, Kontakte und Benutzer zu verwalten.
* `\App\Models\Recherche\Projekt::class`: Dies ist eine Modell-Klasse, die Projekte verwaltet. Sie wird verwendet, um die Liste der letzten Projekte anzuzeigen und die Recherchen-Funktion der Anwendung zu verwenden.
* `route('recherche')`: Diese Funktion ruft den Namen der Route für die Recherchen-Funktion ab. Sie wird verwendet, um einen Link zum Recherchieren-Formular auf der Seite zu erstellen.
* `route('recherche.projekt', $projekt)`: Diese Funktion ruft den Namen der Route für ein bestimmtes Projekt ab. Sie wird verwendet, um einen Link zum Detailansicht des Projektes auf der Seite zu erstellen.

---

### resources/views/welcome.blade.php
This file is a Blade template for the welcome page of a web application. It contains HTML and CSS code to style the layout, as well as some JavaScript for animations and interactivity. The file also includes some PHP code to generate dynamic content, such as the navigation menu and the "Hover Glow Effect" that is triggered by hovering over links.

The file includes several classes and functions, including:

* `page-wrap`: A container class for the main content of the page, with a maximum width of 1200px and some padding.
* `reveal`: A CSS class for animating elements into view when they become visible within the user's scrollable area.
* `grid-overlay`: A background element that creates a grid-like pattern over the entire page.
* `site-header`: The main navigation bar of the site, with a fixed position and some CSS animations for blurred effects.
* `nav-inner`: A container class for the logo and navigation links in the header.
* `logo-stack`: A container class for the logo image and text elements.
* `logo-mark`: A container class for the logo image element, with some CSS animations for hover effects.
* `logo-text-main`: A CSS class for styling the main title of the logo, with a gradient background and no fill color.
* `logo-text-sub`: A CSS class for styling the subtitle of the logo, with a smaller font size and a different color scheme.
* `nav-links`: A container class for the navigation links in the header, with some CSS animations for hover effects.
* `reveal`: A CSS class for animating elements into view when they become visible within the user's scrollable area.
* `grid-overlay`: A background element that creates a grid-like pattern over the entire page.

---

### resources/views/components/action-message.blade.php
This is a Blade component file for displaying an action message, such as a success or error message after a form submission. The component is defined in the `resources/views/components` directory.

The component uses the `@props` directive to define two props: `on` and `timeout`. The `on` prop specifies the event that triggers the display of the message, such as a successful form submission. The `timeout` prop specifies the duration in milliseconds for which the message should be displayed before it is automatically dismissed.

The component uses the `@this` directive to access the Alpine.js context and the `x-init` directive to initialize the component's state. When the `on` event is triggered, the component sets the `shown` variable to true, clears any existing timeout (if there is one), and sets a new timeout for 2 seconds. After the timeout expires, the `shown` variable is set to false, causing the message to be dismissed.

The component uses the `x-show` directive to conditionally render the message based on the value of the `shown` variable. The `x-transition:leave.opacity.duration.1500ms` directive is used to animate the hiding of the message after it has been dismissed.

The component also uses the `style="display: none"` attribute to initially hide the message and the `{{ $attributes->merge(['class' => 'text-sm']) }}` attribute to merge additional classes with the default class of `text-sm`. The `$slot` variable is used to pass content into the message, if any.

---

### resources/views/components/app-logo-icon.blade.php
Die Datei `resources/views/components/app-logo-icon.blade.php` ist eine Blade-Vorlage, die ein SVG-Icon für das Logo der Anwendung darstellt. Die Vorlage enthält einen `<svg>`-Tag mit einer `viewBox`-Eigenschaft, die die Größe des SVG-Icons definiert, sowie einige `path`-Elemente, die den Konturen des Icons entsprechen. Die `$attributes`-Variable wird verwendet, um den `fill` und `clip-rule`-Wert der Paths anzupassen.

Die Datei importiert keine anderen Klassen oder Funktionen, sondern ist eine eigenständige Blade-Vorlage. Sie kann in einer anderen Datei als `<x-app-logo-icon/>` eingebunden werden, um das Logo zu rendern.

---

### resources/views/components/app-logo.blade.php
This file appears to be a Blade template for the application logo, which consists of an SVG logo icon and a text label. The template uses Laravel's Blade templating engine.

---

### resources/views/components/auth-header.blade.php
This is a Blade component file for an authentication header. It defines a reusable UI component that displays a header with a title and description. The component takes two props, `title` and `description`, which are used to populate the header's content.

The file consists of a single Blade template, which uses the `@props()` directive to define the accepted props and their types. It then defines a `<div>` element that contains the header's title and description, using the passed-in `title` and `description` values as content. The `text-xl`, `font-semibold`, `text-zinc-900`, `dark:text-zinc-100`, `mt-1`, `text-sm`, `text-zinc-500`, and `dark:text-zinc-400` classes are used to style the header's text.

The component does not have any external dependencies, such as imports or database accesses, since it only uses the props passed in by its parent component.

---

### resources/views/components/auth-session-status.blade.php
File Summary: This file is a Blade component used for displaying session status messages. It takes in a `status` prop and outputs a `<div>` element with the given status message, using the CSS classes `font-medium text-sm text-green-600`.

External Dependencies: None.

---

### resources/views/components/placeholder-pattern.blade.php
This is a Blade component file for a placeholder pattern. It consists of a single SVG element with a `<defs>` tag that defines a pattern, and a `<rect>` element that uses the pattern to fill its interior. The `id` prop is used to give each instance of this component a unique identifier, which is necessary for proper CSS styling.

The file also imports the `uniqid()` function from PHP's built-in `strings` extension, which is used to generate a unique identifier for the pattern.

This component can be used as a drop-in replacement for the `<img>` tag in HTML, and it will render an SVG placeholder with the same dimensions as the image that would have been loaded if it were available. This can be useful for displaying placeholders while images are loading or for creating a fallback solution when images fail to load.

---

### resources/views/components/layouts/app.blade.php
The file `resources/views/components/layouts/app.blade.php` is a Blade template that serves as the base layout for an application. It defines a sidebar with a title and a main content area, where the actual application content is rendered. The layout uses a custom component called `<x-layouts.app.sidebar>`, which is defined in a separate file.

The file imports two external dependencies:

* `Illuminate\Support\Facades\View`: This facade provides access to the view factory, which is used to render views.
* `App\Models\User`: This model class is used to fetch data about the current user.

The layout defines two slots:

* `$title`: The title of the page, which can be set by passing a value to the `:title` attribute in the `<x-layouts.app.sidebar>` component. If no title is specified, `null` will be used as the default value.
* `$slot`: This slot contains the actual application content, which is rendered inside the main content area of the layout.

Overall, this file provides a basic structure for an application's layout, allowing developers to easily add components and functionality to their views.

---

### resources/views/components/layouts/auth.blade.php
This file is a Laravel Blade component that defines the layout for authentication pages.

---

### resources/views/components/layouts/app/header.blade.php
This code is using the `x-show` and `x-transition` directives from Alpine.js to create a responsive navigation menu that appears when the screen size is below a certain breakpoint (in this case, `lg:hidden`). The menu is positioned at the start of the page (`start-0`) and has a z-index of 30.

The `x-show` directive is used to toggle the visibility of the menu based on a boolean variable called `mobileNav`. When the screen size is below the breakpoint, the menu will be visible (`x-show="mobileNav"`), and when it's not (`lg:hidden`), it will be hidden.

The `x-transition` directive is used to create a transition effect for the menu when it's shown or hidden. In this case, the menu will slide in from the left (`enter-start="-translate-x-full"` and `leave-start="translate-x-0"`) and slide out to the left (`leave-end="-translate-x-full"`). The transition is set to take 200 milliseconds (`duration-200`).

The menu also includes a button to close the menu when it's shown. This button has an `@click` event that sets `mobileNav` to `false`, which will hide the menu and end the transition.

---

### resources/views/components/layouts/app/sidebar.blade.php
Das ist eine sehr einfache und minimalistische Implementierung von Bootstrap, die auf einer einzigen HTML-Seite funktioniert. Die wichtigsten Elemente sind:

* `<div class="container">`: Dies ist der Container, in dem die anderen Elemente positioniert werden.
* `<div class="row">`: Dies ist eine Zeile, die die Spalten enthält.
* `<div class="col-md-4">`: Diese sind die Spalten, die die einzelnen Teile der Bootstrap-Komponente enthalten. Im Beispiel sind es 3 Spalten mit einer Breite von jeweils 40%.
* `<h1>...</h1>`: Dies ist ein Titel, der oben in der Mitte der Seite positioniert ist.
* `<p>...</p>`: Diese sind einige Textzeilen, die unten auf der Seite positioniert sind.

Das ist nur eine kleine Einführung in die Welt von Bootstrap und kann als Referenz für eine schnelle Implementierung dienen, aber es gibt viele weitere Optionen und Möglichkeiten, um Ihre Webseite mit Bootstrap zu erstellen.

---

### resources/views/components/layouts/auth/card.blade.php
Die Datei `resources/views/components/layouts/auth/card.blade.php` enthält eine Blade-Vorlage für die Authentifizierungsseite einer Webanwendung, die mit Laravel entwickelt wurde. Die Datei verwendet verschiedene Komponenten und Funktionen von Laravel, um die Benutzeroberfläche zu erstellen und anzupassen.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `resources/views/components/layouts/auth/card.blade.php`: Diese Datei enthält die Blade-Vorlage für die Authentifizierungsseite. Es wird verwendet, um die Benutzeroberfläche zu erstellen und anzupassen.
* `partials/head`: Diese Datei enthält eine Blade-Partial-Datei, die den Kopfabschnitt der Seite einschließt. Es enthält HTML-Tags und -Attribute für die Definition von Metadaten und anderen Einstellungen, die für die Benutzeroberfläche wichtig sind.
* `wire:navigate`: Diese Funktion wird verwendet, um eine Route in einer Livewire-Anwendung zu navigieren. Sie überträgt die Anfrage an den entsprechenden Controller und führt die erforderlichen Aktionen aus, um die Benutzeroberfläche zu aktualisieren.
* `route('home')`: Diese Funktion wird verwendet, um eine Route in einer Livewire-Anwendung zu definieren. Es überträgt die Anfrage an den entsprechenden Controller und führt die erforderlichen Aktionen aus, um die Benutzeroberfläche zu aktualisieren.
* `config('app.name', 'Laravel')`: Diese Funktion wird verwendet, um den Namen der Anwendung abzurufen. Wenn keine Konfiguration für den App-Namen gefunden wird, wird als Standardwert "Laravel" verwendet.
* `livewireScriptConfig`: Diese Funktion wird verwendet, um die Livewire-JavaScript-Bibliothek zu konfigurieren. Es überträgt die Anfrage an den entsprechenden Controller und führt die erforderlichen Aktionen aus, um die Benutzeroberfläche zu aktualisieren.
* `app()->getLocale()`: Diese Funktion wird verwendet, um das aktuelle Gebietsschema der Anwendung abzurufen. Es überträgt die Anfrage an den entsprechenden Controller und führt die erforderlichen Aktionen aus, um die Benutzeroberfläche zu aktualisieren.
* `cookie-consent::index`: Diese Datei enthält eine Blade-Partial-Datei, die die Cookie-Einwilligung für die Nutzung von Cookies anzeigt. Es enthält HTML-Tags und -Attribute für die Definition von Metadaten und anderen Einstellungen, die für die Benutzeroberfläche wichtig sind.

---

### resources/views/components/layouts/auth/simple.blade.php
This is a simple authentication layout file for an application using the Laravel framework. The file contains a `doctype`, `html` tag, and several other HTML elements. It also includes a few CSS classes that are used to style the layout.

Here's a summary of the important parts of the file:

* `<!DOCTYPE html>`: This is the document type declaration, which specifies the document type and version of HTML used in the document.
* `<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">`: This sets the language attribute of the `html` tag to the value returned by the `str_replace()` function, which replaces underscores with hyphens in the current locale set for the application.
* `<head>`: This is a section that contains metadata about the document, such as the character encoding and title. It also includes the following tags:
	+ `<title>{{ config('app.name', 'Laravel') }}</title>`: This sets the title of the page to the value returned by the `config()` function with the keys "app.name" or "Laravel", if the first one doesn't exist.
	+ `<meta charset="UTF-8">`: This specifies the character encoding used in the document, which is UTF-8 in this case.
	+ `<meta name="viewport" content="width=device-width, initial-scale=1.0">`: This sets the viewport of the page to fit the device width and initial scale to 1.0, which allows for responsive design.
* `<body class="min-h-screen bg-white antialiased dark:bg-linear-to-b dark:from-neutral-950 dark:to-neutral-900">`: This sets the body element of the document to have a minimum height of the screen, set the background color to white, and apply anti-aliasing styles for a more polished look.
* `<div class="bg-background flex min-h-svh flex-col items-center justify-center gap-6 p-6 md:p-10">`: This sets the background color of the page to `bg-background`, which is a CSS class that sets the background color to a light gray, and uses flexbox to center the content in the middle of the screen.
* `<div class="flex w-full max-w-sm flex-col gap-2">`: This creates a flex container with a maximum width of `max-w-sm`, which is a CSS class that sets the maximum width of the element to 640px, and uses flexbox to create a column layout.
* `<a href="{{ route('home') }}" class="flex flex-col items-center gap-2 font-medium" wire:navigate>`: This creates an anchor tag with a hyperlink to the home page, using the `route()` function to generate the URL, and sets the font size to medium.
* `<x-app-logo-icon class="size-9 fill-current text-black dark:text-white" />`: This is a custom Blade component that displays an icon with a size of 9 pixels and fills it with the current color or a light gray when the `dark` CSS class is applied.
* `<span class="sr-only">{{ config('app.name', 'Laravel') }}</span>`: This sets the text content to the value returned by the `config()` function with the keys "app.name" or "Laravel", if the first one doesn't exist, and applies the `sr-only` CSS class, which hides the element from assistive technologies but still allows it to be read visually.
* `<div class="flex flex-col gap-6">`: This creates a flex container with a column layout using flexbox.
* `<x-jet-validation-errors />`: This is a custom Blade component that displays validation errors for the current form, using the `X-Jetstream` library.
* `<form method="POST" wire:submit.prevent="login">`: This creates a form element with a POST request method and uses the `wire:submit.prevent` modifier to prevent the default submission of the form. The `login` method is called when the form is submitted, which performs the authentication process.
* `<div class="flex flex-col gap-2">`: This creates another flex container with a column layout using flexbox.
* `<input type="email" wire:model="email" autofocus />`: This creates an input field for entering the email address, uses the `wire:model` modifier to bind the input value to the `email` property of the component, and sets the focus on the input element when the page loads using the `autofocus` attribute.
* `<input type="password" wire:model="password" />`: This creates an input field for entering the password, uses the `wire:model` modifier to bind the input value to the `password` property of the component, and sets the focus on the input element when the page loads using the `autofocus` attribute.
* `<div class="flex flex-col gap-2">`: This creates another flex container with a column layout using flexbox.
* `<button type="submit" class="bg-blue-500 hover:bg-blue-600 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-white px-5 py-2.5 text-center dark:bg-linear-to-b dark:from-neutral-950 dark:to-neutral-900" wire:loading.attr="disabled">`: This creates a button element with a blue background and rounded corners, uses the `wire:loading.attr` modifier to disable the button when the form is being submitted, sets the focus on the input element when the page loads using the `autofocus` attribute, and applies the dark mode styles from the `dark` CSS class.
* `<x-jet-input-error for="email" />`: This is a custom Blade component that displays an error message for the email field if it's not filled or has an invalid format.
* `<x-jet-input-error for="password" />`: This is a custom Blade component that displays an error message for the password field if it's not filled or has an invalid format.
* `@include('cookie-consent::index')` and `@livewireScripts`: These are includes of other Blade components, which display the cookie consent message and load the Livewire JavaScript library.

---

### resources/views/components/layouts/auth/split.blade.php
This is a Blade template file for an authentication layout, which includes a quote of the day and a login form. The file imports various components and partials from other files in the project.

The most important classes and functions in this file are:

* `Illuminate\Foundation\Inspiring::quotes()->random()`: This function is used to get a random inspirational quote from the `quotes` method of the `Illuminate\Foundation\Inspiring` class. The quotes are stored in an array and are retrieved randomly using the `random()` method.
* `$message, $author = str(Illuminate\Foundation\Inspiring::quotes()->random())->explode('-')`: This line of code splits the random inspirational quote into its message and author parts using the `explode` function. The `-` is used as a delimiter to split the string.
* `@include('partials.head')` and `@livewireScriptConfig`: These include other files in the project that are needed for the layout to work properly.
* `wire:navigate`: This is a directive that allows the user to navigate between pages using Livewire.

The file uses various external dependencies, such as Laravel's `Illuminate\Foundation\Inspiring` class and the `cookie-consent::index` package, which are imported through the use of `@include` directives in the file.

---

### resources/views/components/settings/layout.blade.php
Dies ist eine blade-Datei, die ein Settings Layout für eine Laravel-Anwendung bereitstellt. Die Datei definiert eine Navigation mit verschiedenen Einstellungen und einem Inhaltshalter für den Hauptinhalt der Seite.

Die wichtigsten Klassen in dieser Datei sind:

* `flex`: Diese Klasse wird verwendet, um das Element als flexibler Container zu deklarieren.
* `items-start`: Diese Klasse wird verwendet, um die Elemente innerhalb des Containers anzupassen und sie auf der linken Seite zu positionieren.
* `max-md:flex-col`: Diese Klasse wird verwendet, um das Element bei einerBreite von 1024 Pixel (maximal) als Spalte zu deklarieren.
* `me-10`: Diese Klasse wird verwendet, um ein Abstand von 10 Pixeln zwischen den Elementen in der Navigation hinzuzufügen.
* `w-full`: Diese Klasse wird verwendet, um das Element als volle Breite zu deklarieren.
* `pb-4`: Diese Klasse wird verwendet, um ein Abstand von 4 Pixeln unten zwischen den Elementen in der Navigation hinzuzufügen.
* `md:w-[220px]`: Diese Klasse wird verwendet, um die Breite des Elements bei einerBreite von 1024 Pixel (maximal) auf 220 Pixel zu deklarieren.
* `space-y-1`: Diese Klasse wird verwendet, um ein Abstand von 1 Pixel zwischen den Elementen in der Navigation hinzuzufügen.
* `block`: Diese Klasse wird verwendet, um das Element als Blockelement zu deklarieren.
* `rounded-md`: Diese Klasse wird verwendet, um das Element als abgerundetes Element zu deklarieren.
* `px-3 py-2`: Diese Klasse wird verwendet, um die Größe des Elements anzupassen.
* `text-sm font-medium`: Diese Klasse wird verwendet, um den Textgrößen und die Schriftschnittart anzupassen.
* `bg-zinc-200/70 text-zinc-900 dark:bg-zinc-700/50 dark:text-zinc-100`: Diese Klasse wird verwendet, um den Hintergrund und den Textfarbe des aktiven Elements anzupassen.
* `text-zinc-700 hover:bg-zinc-200/50 dark:text-zinc-300 dark:hover:bg-zinc-700/30`: Diese Klasse wird verwendet, um den Textfarbe und den Hintergrund des aktiven Elements anzupassen, wenn der Benutzer mit dem Mauszeiger darüber fährt.

Die wichtigsten Funktionen in dieser Datei sind:

* Die Navigation mit verschiedenen Einstellungen für den Benutzer.
* Der Hauptinhalt der Seite, der als Inhaltshalter deklariert wird.
* Die Verwendung von blade-Anweisungen und variablen (`$heading`, `$subheading`, `$slot`) zur Anpassung des Layouts an die spezifischen Bedürfnisse der Anwendung.

---

### resources/views/emails/contact-confirmation.blade.php
Maximal 10-zeilige Beschreibung der Datei-Funktionen: Diese PHP-Datei ist eine E-Mail-Vorlage für die Anfragekontaktformular von Linn Games. Sie enthält eine HTML-Seite mit einer Vorlage für eine E-Mail, die den Benutzer informiert, dass seine Nachricht empfangen wurde und ein Team von Linn Games sich so schnell wie möglich bei ihm melden wird.

---

### resources/views/emails/contact-inquiry.blade.php
This is a Blade template file for an email that is sent when someone submits a contact inquiry on the linn.games website. The template includes several variables that are passed in from the controller, which are then used to populate the fields of the email.

Here's a breakdown of each line:

1. `<!DOCTYPE html>`: This is the doctype declaration for an HTML5 document.
2. `<html lang="de">`: This is the root element of the HTML document, and it specifies the language of the content. In this case, it's German (`"de"`).
3. `<head>`: This is the head section of the HTML document, which contains metadata about the page.
4. `<meta charset="UTF-8">`: This specifies the character encoding for the HTML document.
5. `<title>Neue Kontaktanfrage</title>`: This sets the title of the HTML document, which is displayed in the browser's title bar and in search engine results.
6. `</head>`: This closes the head section of the HTML document.
7. `<body style="font-family: sans-serif; line-height: 1.6; color: #333;">`: This is the body section of the HTML document, which contains the main content of the page. The `style` attribute specifies the font family, line height, and text color for the entire body of the email.
8. `<h2>Neue Kontaktanfrage über linn.games</h2>`: This is a level 2 heading element that contains the main title of the email.
9. `<table style="border-collapse: collapse; width: 100%; max-width: 600px;">`: This is an HTML table element that contains several rows and columns. The `style` attribute specifies the border collapse property, which controls how borders are drawn around cells with collapsed borders, and the width of the table, which is set to 100% of the parent container.
10. `<tr>`: This is a row element within the HTML table.
11. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Name</td>`: This is a table data element that contains the text "Name" and some basic styling for padding, font weight, and border bottom.
12. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->name }}</td>`: This is another table data element that displays the value of the `$contact->name` variable from the controller.
13. `@if($contact->company)`: This is an Blade directive that checks whether the `company` property exists on the `$contact` object, and if it does, executes the code inside the directive.
14. `<tr>`: This is another row element within the HTML table.
15. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Firma</td>`: This is a table data element that contains the text "Firma" and some basic styling for padding, font weight, and border bottom.
16. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->company }}</td>`: This is another table data element that displays the value of the `$contact->company` variable from the controller.
17. `</tr>`: This closes the row element within the HTML table.
18. `<tr>`: This is another row element within the HTML table.
19. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">E-Mail</td>`: This is a table data element that contains the text "E-Mail" and some basic styling for padding, font weight, and border bottom.
20. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->email }}</td>`: This is another table data element that displays the value of the `$contact->email` variable from the controller.
21. `</tr>`: This closes the row element within the HTML table.
22. `<tr>`: This is another row element within the HTML table.
23. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Projekttyp</td>`: This is a table data element that contains the text "Projekttyp" and some basic styling for padding, font weight, and border bottom.
24. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->project_type }}</td>`: This is another table data element that displays the value of the `$contact->project_type` variable from the controller.
25. `@if($contact->timeline)`: This is an Blade directive that checks whether the `timeline` property exists on the `$contact` object, and if it does, executes the code inside the directive.
26. `<tr>`: This is another row element within the HTML table.
27. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Zeitrahmen</td>`: This is a table data element that contains the text "Zeitrahmen" and some basic styling for padding, font weight, and border bottom.
28. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->timeline }}</td>`: This is another table data element that displays the value of the `$contact->timeline` variable from the controller.
29. `</tr>`: This closes the row element within the HTML table.
30. `<tr>`: This is another row element within the HTML table.
31. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Nachricht</td>`: This is a table data element that contains the text "Nachricht" and some basic styling for padding, font weight, and border bottom.
32. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->message }}</td>`: This is another table data element that displays the value of the `$contact->message` variable from the controller.
33. `</tr>`: This closes the row element within the HTML table.
34. `<tr>`: This is another row element within the HTML table.
35. `<td style="padding: 8px; font-weight: bold; border-bottom: 1px solid #eee;">Eingegangen am</td>`: This is a table data element that contains the text "Eingegangen am" and some basic styling for padding, font weight, and border bottom.
36. `<td style="padding: 8px; border-bottom: 1px solid #eee;">{{ $contact->created_at->format('d.m.Y H:i') }}</td>`: This is another table data element that displays the value of the `$contact->created_at` variable from the controller, formatted as "dd.mm.yyyy HH:ii".
37. `</tr>`: This closes the row element within the HTML table.
38. `</table>`: This closes the HTML table element.
39. `</body>`: This closes the body section of the HTML document.
40. `</html>`: This closes the root element of the HTML document.

---

### resources/views/emails/deploy-notification.blade.php
Die Datei resources/views/emails/deploy-notification.blade.php enthält eine E-Mail-Vorlage für das Deployment von Versionen in der UI-Kategorie. Die Vorlage verwendet PHP-Code, um dynamische Werte wie den Namen der Anwendung, die URL und die Umgebung zu generieren.

---

### resources/views/layouts/legal.blade.php
This is a Blade template file for an HTML page that includes the necessary elements and styling for a legal document, such as a privacy policy or terms of service. The file contains several sections, including:

* `<!DOCTYPE html>` - This is the document type declaration, which specifies the document type as HTML.
* `<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">` - This line sets the language of the page to be `en-US`. The `str_replace` function is used to replace underscores with hyphens in the language code.
* `<head>` - This section contains metadata about the page, such as the charset and viewport settings. It also includes the stylesheet link for the legal document.
* `<link rel="icon" type="image/x-icon" href="/favicon.ico">` - This line links to a favicon file that will be used as the icon for the page in the browser's tab.
* `<link rel="preconnect" href="https://fonts.googleapis.com">` - This line establishes a connection between the page and Google Fonts, which is used to load fonts from the Google Fonts API.
* `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` - This line establishes a connection between the page and Google's static font server, which allows for faster loading of fonts.
* `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">` - This line loads the Inter font from Google Fonts and sets it as the default font for the page.
* `<style>` - This section contains CSS styles that are applied to the entire page. It includes variables for colors, fonts, and other styling elements.
* `<body>` - This section contains the content of the page, which is displayed once the page has loaded. It includes a navigation bar with links to other pages on the site, as well as a footer with copyright information.

In summary, this file provides a basic template for creating legal documents using Laravel Blade templates. The `@yield` directives allow for the inclusion of dynamic content in specific sections of the page, making it easy to create and update legal documents without having to modify the entire template each time.

---

### resources/views/legal/agb.blade.php
* Die Datei `resources/views/legal/agb.blade.php` enthält die Allgemeinen Geschäftsbedingungen (AGB) des Online-Spiels von Linn.Games.
* Die Datei verwendet das Laravel-Framework und basiert auf dem Layout `layouts.legal`.
* Der Inhalt der Seite ist in Abschnitte unterteilt, z.B. Geltungsbereich und Anbieter, Nutzungsregeln für den Multiplayer und die Kommunikation, Kosten, Werbung und Inhalte des Angebots, Digitale Käufe und Widerrufsrecht, Haftung und Gewährleistung, Sanktionen bei Verstößen / Sperrung des Accounts, Haftungsfreistellung durch den Nutzer, Laufzeit, Kündigung und Änderungen des Dienstes, Schlussbestimmungen.
* Es gibt verschiedene Funktionen und Endpunkte auf der Seite, wie z.B. Registrierung, Benutzerkonto und Anmeldung für den Multiplayer.
* Die Datei importiert PHP-Klassen aus verschiedenen Laravel-Paketen, darunter `Illuminate\Support\Facades\Auth` und `App\Models\User`, um die Authentifizierung und Benutzerverwaltung zu implementieren.

---

### resources/views/legal/dsgvo.blade.php
Die Datei `resources/views/legal/dsgvo.blade.php` in Laravel ist eine Blade-Templates-Datei, die die Datenschutzerklärung des Websites und des Spiels für die Benutzer enthält.

Es gibt viele Funktionen und Klassen in dieser Datei, aber hier sind einige wichtige Punkte:

* Die Datei erweitert das Layout `layouts.legal`, was bedeutet, dass sie eine standardmäßige Datenschutzerklärung für alle Websites und Spiele auf Basis von Laravel bietet.
* Die Titel der Seite lauten "Datenschutzerklärung".
* Die Datei enthält eine Beschreibung der Verantwortlichen Stelle für die Datenverarbeitung, die Arten der verarbeiteten Daten (Registrierungs- und Kontodaten, Authentifizierungsdaten, Spieldaten und Kommunikationsinhalte, Nutzungs- und Verbindungsdaten, Kontaktdaten und Kommunikationsinhalte), den Zwecken und Rechtsgrundlagen der Datenverarbeitung (Vertragserfüllung, Kommunikation und Support, Sicherheit und technische Protokollierung, Keine Werbung oder Profiling ohne Einwilligung, Rechte der Betroffenen), den Maßnahmen zur Aufbewahrung und Löschung der Daten (Account- und Profildaten, Kontaktanfragen, Nutzungs- und Log-Daten, Rechte der Betroffenen), die Verwendung von Cookies und eingebundenen Drittdiensten, die Hosting- und Infrastrukturdetails und die Möglichkeiten des Datenexports und der Kontolöschung.
* Es gibt auch Informationen über das Datenschutzrecht (DSGVO) in Deutschland, wie z.B. Art. 6 Abs. 1 lit. b/f DSGVO (Recht auf Einwilligung, Recht auf Widerruf einer Einwilligung, Recht auf Beschwerde bei einer Aufsichtsbehörde).
* Die Datenschutzerklärung ist seit März 2026 aktuell.

---

### resources/views/legal/impressum.blade.php
This file is a part of the Linn.games website and contains the impressum page content, including contact information and legal disclaimers.

---

### resources/views/livewire/auth/confirm-password.blade.php
This file is a Blade template for the "Confirm Password" page in a Laravel application. It uses the `livewire` package to handle the client-side UI and state management.

The template includes the following functionalities:

1. Header with title and description of the page.
2. Form for entering the password.
3. Button for submitting the form.
4. Session status message (if any).
5. Error handling for the password field.
6. Uses the `x-auth-header` component to display the header with title and description.
7. Uses the `x-auth-session-status` component to display the session status message.
8. Imports the necessary CSS classes from the `tailwindcss` package for styling.
9. Includes a data attribute (`data-test`) on the button element with the value "confirm-password-button" for testing purposes.

The template also uses the `x-layouts.auth` layout component, which is responsible for displaying the overall structure of the page.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei ist eine Blade-Datei, die den Inhalt des Livewire-Komponenten-Templates `forgot-password` darstellt. Die Komponente enthält einen Formular, der die Benutzer-E-Mail-Adresse erfüllt und anschließend einen Password-Reset-Link an das angegebene E-Mail-Konto sendet.

Die wichtigsten Klassen in dieser Datei sind:

* `x-layouts.auth`: Die Authentifizierungs-Layout-Komponente, die die Haupt-Navigation, den Authentifizierungs-Formular und andere verwandte Elemente enthält.
* `x-auth-header`: Die Kopfzeile der Authentifizierungs-Seite, die den Titel, die Beschreibung und das Status-Meldungsfeld enthält.
* `x-auth-session-status`: Eine Session-Status-Komponente, die die Statusmeldung für den Benutzer anzeigt.

Die wichtigsten Funktionen in dieser Datei sind:

* Die Initialisierung der Livewire-Komponente mit einem Formular, das E-Mail-Adresse und Passwort erfüllt.
* Das Senden des Password-Reset-Links an das angegebene E-Mail-Konto per POST-Request an die Route `password.email`.
* Die Anzeige der Statusmeldung für den Benutzer, falls eine Fehlermeldung oder ein Erfolgskommentar vorhanden ist.

---

### resources/views/livewire/auth/login.blade.php
Blade template for a login form in the Laravel Livewire framework

---

### resources/views/livewire/auth/register.blade.php
Die Datei `resources/views/livewire/auth/register.blade.php` ist eine Benutzerregistrierungsseite, die das Formular zum Erstellen eines neuen Benutzers auf der Livewire-Plattform enthält.

Hier sind die wichtigsten Klassen und Funktionen in dieser Datei:

* `x-layouts.auth`: Dies ist ein Layout-Tag, das für die Authentifizierung verwendet wird. Es enthält ein Formular und einige weitere Elemente, wie z.B. eine Schaltfläche zum Anmelden.
* `div`: Dies ist ein HTML-Div-Element, das verwendet wird, um die verschiedenen Abschnitte der Registrierungsseite zu trennen.
* `form`: Dies ist ein HTML-Formular-Element, das verwendet wird, um die Benutzerdaten einzugeben. Es enthält einige Eingabefelder für den Namen, E-Mail-Adresse und Passwort.
* `input`: Dies sind HTML-Input-Felder, die verwendet werden, um die Benutzerdaten einzugeben. Sie werden für Name, E-Mail-Adresse und Passwort verwendet.
* `button`: Dies ist ein HTML-Schaltflächen-Element, das verwendet wird, um den Benutzer anzuweisen, auf die Registrierungsseite zu klicken. Es enthält den Text "Create account".
* `wire:navigate`: Dies ist eine Livewire-Aufrufzeichenfolge, die verwendet wird, um den Benutzer zur Anmeldeseite zu navigieren, wenn er auf die Schaltfläche zum Anmelden klickt.

Dies sind die wichtigsten Klassen und Funktionen in dieser Datei. Die Datei importiert auch einige andere Abhängigkeiten, wie z.B. `x-auth-header`, `x-auth-session-status` und `wire:navigate`.

---

### resources/views/livewire/auth/reset-password.blade.php
This file is a view for the reset password feature in Laravel, specifically the `reset-password` blade template. It uses Livewire to handle form submissions and validation.

Here are the main components of this file:

1. `<x-layouts.auth>`: This is a layout component that includes the HTML structure for an authentication page, including the header, footer, and navigation menu.
2. `div class="flex flex-col gap-6"`: This div element creates a flexible container with a columnar layout, allowing for easy spacing between elements using the `gap-6` class.
3. `<x-auth-header>`: This is a reusable component that displays the header section of the authentication page, including the title and description.
4. `@csrf`: This adds a CSRF token to the form, which helps protect against cross-site request forgery attacks.
5. `input type="hidden" name="token" value="{{ request()->route('token') }}">`: This input field is used to pass the reset password token to the server when submitting the form.
6. `div class="flex items-center justify-end">`: This div element creates a flexible container for the submit button, allowing for easy positioning of the button relative to other elements.
7. `<button type="submit" data-test="reset-password-button" class="inline-flex w-full items-center justify-center rounded-md bg-zinc-900 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-zinc-500 focus:ring-offset-2 dark:bg-white dark:text-zinc-900 dark:hover:bg-zinc-200">`: This is the submit button for the form, which displays a rounded-cornered button with a gradient background and shadow effects. The button text is "Reset password" and has a white font color, with hover and focus states that change the background color to darker shades of zinc-900.

Overall, this file provides a user-friendly interface for users to reset their passwords using the Laravel framework's built-in features.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
This Blade template is part of the Laravel framework and is used to render a two-factor authentication challenge page for users who have enabled two-factor authentication on their account. The template includes a form with an input field where the user can enter the authentication code provided by their authenticator application, as well as an option to use a recovery code if necessary.

The template is using the Livewire framework for client-side state management and event handling. It also uses the `x-data` directive to define a reactive component that manages the visibility of the recovery input field and the authentication code input fields, as well as the toggle functionality between these two options. The `x-show` directive is used to conditionally render different parts of the template based on the current state of the component.

The template also includes a button to submit the form and continue with the login process, as well as a link to display the recovery code option if necessary.

Overall, this template provides a simple and intuitive way for users to complete their two-factor authentication challenge.

---

### resources/views/livewire/auth/verify-email.blade.php
The file `resources/views/livewire/auth/verify-email.blade.php` appears to be a Blade template that is part of the Laravel Livewire authentication system. The template contains a form with two buttons, one for resending the verification email and another for logging out of the application.

The file imports the `x-layouts.auth` component, which is likely a custom layout for the authentication pages. It also includes a few other components, such as `session`, `route`, and `csrf`. The file also uses the `__()` function to translate text in the template, which suggests that it may be using Laravel's built-in internationalization (i18n) support.

Overall, this file appears to be a simple authentication page for verifying an email address during registration. It uses Livewire to handle form submissions and navigation between pages, and includes some basic i18n functionality to support multiple languages.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Die Datei ist ein Blade-Template für das Livewire-Komponenten-Framework und stellt eine Chat-Benutzeroberfläche bereit, die mit einem Webhook verbunden ist. Es gibt folgende wichtige Klassen und Funktionen in dieser Datei:

* `App\Models\ChatMessage`: Dies ist ein Modell für die Chat-Nachrichten, das mit der Datenbank interagiert und Nachrichten speichert und abruft.
* `App\Models\Webhook`: Dies ist ein Modell für den Webhook, der mit der Datenbank interagiert und Webhooks speichert und abruft.
* `Illuminate\Support\Collection`: Diese Klasse stellt eine Sammlung von Chat-Nachrichten dar und wird verwendet, um die Nachrichten zu sammeln und anzuzeigen.
* `Illuminate\Support\Facades\Auth`: Diese Facade ist für die Authentifizierung des Benutzers in der Anwendung verantwortlich.
* `Illuminate\Support\Facades\Http`: Diese Facade ist für den HTTP-Zugriff auf den Server verantwortlich und wird verwendet, um eine Webhook-Anfrage zu senden und die Antwort abzurufen.
* `Livewire\Volt\Component`: Diese Klasse stellt eine Livewire-Komponente dar und ist für die Darstellung der Benutzeroberfläche zuständig.

Insgesamt stellt diese Datei eine Chat-Benutzeroberfläche bereit, die mit einem Webhook verbunden ist und die Möglichkeit bietet, Nachrichten zu senden und zu empfangen.

---

### resources/views/livewire/recherche/phase-p1.blade.php
This PHP file defines a Livewire component for the first phase of a research project. It provides CRUD operations for different types of entities such as Strukturmodellwahl, Komponente, Kriterium, and Warnsignal. The component also includes functionality to edit and delete existing records.

---

### resources/views/livewire/recherche/phase-p2.blade.php
The file resources/views/livewire/recherche/phase-p2.blade.php is a Livewire component for the project phase 2 of a research project. The component manages several classes and functions related to review, cluster, mapping, and trefferliste (hit list) management.

Here's a brief summary of the main features:

* ReviewTypEntscheidung: CRUD operations for review type decisions, including validating input and saving or updating data.
* Cluster: CRUD operations for clusters, including validating input and saving or updating data.
* MappingSuchstringKomponente: CRUD operations for mapping such string components, including validating input and saving or updating data.
* Trefferliste: CRUD operations for hit lists, including validating input and saving or updating data.
* Data management: The component also manages data related to the project, including loading and displaying project data.

The file uses Livewire framework to create reactive components that can handle user interactions and update the UI in real-time. It also makes use of Laravel's Eloquent ORM for database operations and Volt templating engine for generating HTML views.

The file is well-organized with clear comments, making it easy to understand the functionality and how to use the component. The code is also well-formatted and follows best practices for coding conventions.

---

### resources/views/livewire/recherche/phase-p3.blade.php
This Blade file is part of a Livewire component that allows users to create and edit various types of data for a research project. It uses models from the App\Models\Recherche namespace to interact with the database and validates user input using Laravel's built-in validation functionality.

---

### resources/views/livewire/recherche/phase-p4.blade.php
This file contains a Livewire component that manages the data and functionality for a project's research phase. The component is used to display and edit various metadata related to the research phase, such as search strings and thesaurus mappings.

The component uses the following classes and functions:

* `App\Models\Recherche\{Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll}`: These are the models used to store the data for each of the different types of metadata.
* `Illuminate\Support\Facades\Auth`: This is a facade that provides access to the Auth class, which is used to authenticate users and check their permissions.
* `Livewire\Volt\Component`: This is the base class for Livewire components.
* `mount(Projekt $projekt)`: This function is called when the component is mounted (i.e., when it is first created). It takes a `Projekt` object as an argument and sets it as the project associated with this component.
* `newSs()`: This function creates a new search string.
* `saveSs()`: This function saves changes to a search string.
* `editSs(string $id)`: This function edits a specific search string.
* `deleteSs(string $id)`: This function deletes a search string.
* `cancelSs()`: This function cancels any editing or deleting of a search string.
* `newTh()`: This function creates a new thesaurus mapping.
* `saveTh()`: This function saves changes to a thesaurus mapping.
* `editTh(string $id)`: This function edits a specific thesaurus mapping.
* `deleteTh(string $id)`: This function deletes a thesaurus mapping.
* `cancelTh()`: This function cancels any editing or deleting of a thesaurus mapping.
* `newAp(string $suchstringId = '')` : This function creates a new anpassungsprotokoll (anamplification protocol) for a specific search string.
* `saveAp()`: This function saves changes to an anpassungsprotokoll.
* `editAp(string $id)`: This function edits a specific anpassungsprotokoll.
* `deleteAp(string $id)`: This function deletes an anpassungsprotokoll.
* `cancelAp()`: This function cancels any editing or deleting of an anpassungsprotokoll.
* `with(): array`: This function returns an array containing the search strings and thesaurus mappings associated with this project.

The component also uses several external dependencies, including Livewire and Illuminate\Support\Facades\Auth.

---

### resources/views/livewire/recherche/phase-p5.blade.php
Die Datei `resources/views/livewire/recherche/phase-p5.blade.php` enthält eine Livewire-Komponente für die Phase P5 der Recherche-Projektmanagement-Anwendung. Die Komponente besteht aus verschiedenen Funktionen und Variablen, um die verschiedenen Bereiche des Screening-Prozesses zu bearbeiten.

Die wichtigsten Klassen, Funktionen und Endpunkte in dieser Datei sind:

* `use App\Models\Recherche\{Projekt, P5Treffer, P5ScreeningKriterium, P5ScreeningEntscheidung, P5ToolEntscheidung, P5PrismaZahlen};`: Diese Import-Anweisungen importieren die Modellklassen und -funktionen, die für das Recherche-System benötigt werden.
* `use Illuminate\Support\Facades\Auth;`: Diese Import-Anweisung importiert den Auth-Helper, der für die Autorisierung von Benutzern verwendet wird.
* `new class extends Component { ... }`: Diese Klasse ist eine Livewire-Komponente, die die verschiedenen Funktionen und Variablen enthält, um das Recherche-System zu bearbeiten.
* `$this->projekt = $projekt;`: Diese Variable speichert einen Verweis auf das Projekt, das der Benutzer bearbeitet.
* `$this->showPrismaForm = false;`: Diese Variable zeigt an, ob das Formular für die Prisma-Zahlen sichtbar ist.
* `$this->editingPrismaId = null;`: Diese Variable speichert den ID-Wert des aktuell bearbeiteten Prisma-Eintrags.
* `$this->prismaIdentGesamt = null;`: Diese Variable speichert den Wert der insgesamt identifizierten Literaturen.
* `$this->prismaDatenbankTreffer = null;`: Diese Variable speichert den Wert der Datenbank-Treffer für Prisma.
* `$this->prismaGraueLit = null;`: Diese Variable speichert den Wert der grauen Literaturen für Prisma.
* `$this->prismaNachDedup = null;`: Diese Variable speichert den Wert der nach Deduplizierung identifizierten Literaturen für Prisma.
* `$this->prismaAusgeschlossenL1 = null;`: Diese Variable speichert den Wert der ausgeschlossenen Literaturen auf Stufe L1 für Prisma.
* `$this->prismaVolltextGeprueft = null;`: Diese Variable speichert den Wert der volltext-geprüften Literaturen für Prisma.
* `$this->prismaAusgeschlossenL2 = null;`: Diese Variable speichert den Wert der ausgeschlossenen Literaturen auf Stufe L2 für Prisma.
* `$this->prismaEingeschlossen = null;`: Diese Variable speichert den Wert der eingeschlossenen Literaturen für Prisma.
* `$this->showSkForm = false;`: Diese Variable zeigt an, ob das Formular für die Screening-Kriterien sichtbar ist.
* `$this->editingSkId = null;`: Diese Variable speichert den ID-Wert des aktuell bearbeiteten Screening-Kriteriums.
* `$this->skLevel = 'L1_titel_abstract';`: Diese Variable speichert die Stufe des aktuell bearbeiteten Screening-Kriteriums.
* `$this->skTyp = 'einschluss';`: Diese Variable speichert den Typ des aktuell bearbeiteten Screening-Kriteriums.
* `$this->skBeschreibung = null;`: Diese Variable speichert die Beschreibung des aktuell bearbeiteten Screening-Kriteriums.
* `$this->showToolForm = false;`: Diese Variable zeigt an, ob das Formular für den Tool-Eintrag sichtbar ist.
* `$this->editingToolId = null;`: Diese Variable speichert den ID-Wert des aktuell bearbeiteten Tool-Eintrags.
* `$this->toolName = 'Rayyan';`: Diese Variable speichert den Namen des aktuell bearbeiteten Tools.
* `$this->toolGewaehlt = (bool) $r->gewaehlt;`: Diese Variable speichert das Flag, das angibt, ob das Tool ausgewählt wurde.
* `$this->toolBegruendung = $r->begruendung ?? null;`: Diese Variable speichert die Begründung für den ausgewählten Tool-Eintrag.
* `$this->showScreenForm = false;`: Diese Variable zeigt an, ob das Formular für die Screening-Entscheidung sichtbar ist.
* `$this->screenTrefferId = null;`: Diese Variable speichert den ID-Wert des aktuell bearbeiteten Treffer-Eintrags.
* `$this->screenLevel = 'L1_titel_abstract';`: Diese Variable speichert die Stufe der aktuell bearbeiteten Screening-Entscheidung.
* `$this->screenEntscheidung = 'eingeschlossen';`: Diese Variable speichert den Status der aktuell bearbeiteten Screening-Entscheidung.
* `$this->screenAusschlussgrund = null;`: Diese Variable speichert die Ausschlussgründe für die aktuelle Screening-Entscheidung.
* `$this->screenReviewer = null;`: Diese Variable speichert den Autor des aktuellen Treffer-Eintrags.
* `$this->screenAnmerkung = null;`: Diese Variable speichert die Anmerkungen für den aktuellen Treffer-Eintrag.

Diese Variablen und Funktionen ermöglichen es dem Benutzer, verschiedene Aspekte des Recherche-Prozesses zu bearbeiten, wie z.B. die Prisma-Zahlen, die Screening-Kriterien, den Tool-Eintrag und die Screening-Entscheidungen.

---

### resources/views/livewire/recherche/phase-p6.blade.php
This file is a Livewire component for the project P6. It provides an interface for creating, editing, and deleting qualities ratings and lucken analyses. It also includes data visualization options using charts.

---

### resources/views/livewire/recherche/phase-p7.blade.php
File: resources/views/livewire/recherche/phase-p7.blade.php
Category: ui

This file is a Livewire component for the P7 (grade) tab in the research phase of the projekt app. It uses Laravel's Livewire framework to create a dynamic and interactive user interface. The component consists of several methods and properties that handle various CRUD operations, as well as validation and error handling.

The main classes used in this file are:

* `Component` - A base class for Livewire components. It provides some basic functionality such as managing the component's state and rendering the component's template.
* `Projekt` - A model representing a research project in the app. It has properties for the project's name, description, and other metadata.
* `P7SyntheseMethode`, `P7Datenextraktion`, `P7MusterKonsistenz`, `P7GradeEinschaetzung` - These are models representing different types of data related to the research project. They have properties for the data's fields and other metadata.
* `Illuminate\Support\Facades\Auth` - This is a facade class that provides an interface for interacting with the authentication system in Laravel. It is used to check if the current user has permission to access the component.

The file also imports several external dependencies, such as:

* `Livewire\Volt\Component` - A base class for Livewire components that uses Volt templates instead of Blade templates.
* `Illuminate\Support\Facades\DB` - This is a facade class that provides an interface for interacting with the database in Laravel. It is used to perform various database operations such as querying and modifying data.
* `Illuminate\Validation\Validator` - This is a class that provides an interface for validating data against a set of rules. It is used to validate user input in the component's methods.

---

### resources/views/livewire/recherche/phase-p8.blade.php
Die Datei `resources/views/livewire/recherche/phase-p8.blade.php` enthält ein Livewire-Komponentenmodul für die Projektphase P8 (Recherche). Die Komponentenklasse ist in der ersten Zeile durch `use App\Models\Recherche\{Projekt, P4Suchstring, P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung, P8UpdatePlan};` importiert und definiert mehrere Funktionen für die Behandlung von Suchprotokollen, Limitierungen, Reproduzierbarkeitstests und Update-Plänen.

Die wichtigsten Klassen und Funktionen sind:

* `Projekt`: Die Projektklasse, die den Eigentümer des Projekteinsatzes speichert.
* `P4Suchstring`: Die Suchzeichenkettenklasse, die die Suchzeichenkette für das Projekt speichert.
* `P8Suchprotokoll`: Die Suchprotokolle-Klasse, die die Suchprotokolle für das Projekt speichert.
* `P8Limitation`: Die Limitations-Klasse, die die Limitierungen des Projekts speichert.
* `P8Reproduzierbarkeitspruefung`: Die Reproduzierbarkeitstests-Klasse, die die Reproduzierbarkeitstests des Projekts speichert.
* `P8UpdatePlan`: Die Update-Pläne-Klasse, die den Update-Plan für das Projekt speichert.

Die Komponentenklasse ist in der `mount`-Methode mit der `abort_unless`-Funktion gesichert, dass der Benutzer dem Projekt als Eigentümer vorhanden sein muss. Anschließend wird die `Projekt`-Instanz im Konstruktor initialisiert und die Suchprotokolle, Limitierungen, Reproduzierbarkeitstests und Update-Pläne mithilfe von Eloquent-Modellen geladen.

Die wichtigsten Funktionen sind:

* `newSp()`: Öffnet das Formular für das Erstellen eines neuen Suchprotokolls.
* `saveSp()`: Speichert ein neues oder bestehendes Suchprotokoll.
* `editSp(string $id)`: Öffnet das Formular zum Bearbeiten eines Suchprotokolls.
* `deleteSp(string $id)`: Löscht ein Suchprotokoll.
* `cancelSp()`: Schließt das Suchprotokoll-Formular und resettet die Eingabefelder.

Ähnlich wie bei den Suchprotokollen können auch Limitierungen, Reproduzierbarkeitstests und Update-Pläne erstellt, bearbeitet und gelöscht werden. Die `with`-Methode wird verwendet, um die Suchzeichenketten und die Suchprotokolle, Limitierungen, Reproduzierbarkeitstests und Update-Pläne mit Eloquent-Modellen zu laden.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Diese Datei ist eine Livewire-Komponente, die das Detail-Seiten-Template für ein Recherchen-Projekt implementiert. Sie verwendet verschiedene Livewire-Komponenten als Teil des Templates, um die verschiedenen Phasen des Projekts anzuzeigen und zu bearbeiten.

Die wichtigsten Klassen in dieser Datei sind:

* `Projekt` - Eine Modellklasse für das Recherchen-Projekt, die Daten über das Projekt enthält, wie z.B. den Titel, die Forschungsfrage, die Phasen und so weiter.
* `Recherche\PhaseP1` - Eine Livewire-Komponente, die die erste Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. die Quellen zu bearbeiten.
* `Recherche\PhaseP2` - Eine Livewire-Komponente, die die zweite Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Review-Typ und die Suchstrings zu bearbeiten.
* `Recherche\PhaseP3` - Eine Livewire-Komponente, die die dritte Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. die Quellen und die Suchstrings zu bearbeiten.
* `Recherche\PhaseP4` - Eine Livewire-Komponente, die die vierte Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Screening-Prozess und die Qualität zu bearbeiten.
* `Recherche\PhaseP5` - Eine Livewire-Komponente, die die fünfte Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Syntheseprozess und die Dokumentation zu bearbeiten.
* `Recherche\PhaseP6` - Eine Livewire-Komponente, die die sechste Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Syntheseprozess und die Dokumentation zu bearbeiten.
* `Recherche\PhaseP7` - Eine Livewire-Komponente, die die siebte Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Syntheseprozess und die Dokumentation zu bearbeiten.
* `Recherche\PhaseP8` - Eine Livewire-Komponente, die die achte Phase des Projekts anzeigt und ermöglicht es dem Nutzer, Daten wie z.B. den Syntheseprozess und die Dokumentation zu bearbeiten.

Die Datei verwendet auch eine Reihe von Importe aus anderen Klassen und Namespaces, um die verschiedenen Komponenten und Funktionen der Anwendung zu unterstützen.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Die Datei resources/views/livewire/recherche/projekt-liste.blade.php enthält eine Livewire-Komponente, die ein Liste von Recherche-Projekten anzeigt. Die Komponente verwendet das Modell Projekt der Anwendung und ruft Daten aus der Datenbank ab.

Die wichtigsten Funktionen dieser Datei sind:

1. `projekte()`: Diese Funktion ruft alle Projekte des aktuellen Benutzers aus der Datenbank ab und gibt sie zurück.
2. `deleteProjekt(string $id)`: Diese Funktion löscht ein Projekt mit einer bestimmten ID aus der Datenbank. Sie verwendet die Authorisierung-Funktion von Livewire, um sicherzustellen, dass nur der aktuelle Benutzer das Projekt löschen darf.
3. `with(): array`: Diese Funktion gibt eine Liste von Projekten zurück, die in der View angezeigt werden sollen. Sie verwendet die `projekte()`-Funktion, um die Daten aus der Datenbank abzurufen und gibt sie als Array zurück.
4. `<section> ... </section>`: Diese HTML-Tags definieren den Bereich, in dem die Projekte angezeigt werden.
5. `@forelse ($projekte as $projekt) ... @endforelse`: Diese Blade-Syntax durchläuft das Array der Projekte und zeigt jede Einheit in einem div-Element an. Wenn das Array leer ist, wird eine Nachricht anstelle des Divs angezeigt.
6. `<a href="{{ route('recherche.projekt', $projekt) }}" wire:navigate class="min-w-0 flex-1">`: Diese HTML-Tag definiert einen Link zu einem bestimmten Projekt. Der Link verwendet die `route()`-Funktion von Livewire, um eine URL für das Projekt zu generieren und `wire:navigate`, um den Benutzer zu einem neuen Route zu navigieren, wenn er auf den Link klickt.
7. `<button ... wire:click="deleteProjekt('{{ $projekt->id }}')">`: Diese HTML-Tag definiert einen Button zum Löschen eines bestimmten Projekts. Der Button verwendet die `wire:click`-Funktion von Livewire, um eine JS-Methode aufzurufen, wenn der Benutzer auf ihn klickt. Die Methode ruft die `deleteProjekt()`-Funktion auf und übergibt den ID des Projekts als Parameter.
8. `<svg ... class="size-4">`: Diese HTML-Tag definiert ein SVG-Symbol, das verwendet wird, um das Löschen eines Projekts anzuzeigen. Das Symbol ist mit einer CSS-Klasse `size-4` versehen, die die Größe des Symbols bestimmt.

---

### resources/views/livewire/recherche/research-input.blade.php
This file is a Livewire component for the search input form on the research project creation page. It defines a `ResearchInput` class that extends the `Component` class from the Livewire framework. The component has two public properties: `eingabe` and `titel`. The `eingabe` property is a text input field where users can enter their research question or topic, while the `titel` property is a computed property that creates a shortened version of the entered text for the project title.

The component also defines two methods: `starteRecherche()` and `validate()`. The `starteRecherche()` method is called when the user submits the form, and it validates the input using Livewire's built-in validation system. If the input is valid, it creates a new project in the database using the Eloquent ORM provided by Laravel, then dispatches a job to trigger the Langdock Agent, which will perform the actual research.

The `validate()` method is used to validate the input and display any errors that may occur during the validation process.

The component also includes an error message for the `eingabe` property, which displays if the input is invalid.

Overall, this component provides a simple form for users to enter their research question or topic, and then creates a new project in the database with a shortened title based on that input.

---

### resources/views/livewire/settings/appearance.blade.php
This file is a part of a Laravel Livewire component that provides the appearance settings for a user's account. It consists of a button group with three buttons: "Light", "Dark", and "System". The buttons are displayed in a dropdown menu. Each button represents a different theme mode: Light, Dark, and System (which means that the theme will automatically adjust based on the user's system preferences).

When a user clicks on one of these buttons, it sets the theme to the corresponding mode by calling the `setAppearance()` function. The function first checks whether the requested mode is valid (i.e., it must be either "light", "dark", or "system"). If so, it saves the chosen mode in local storage using `localStorage.setItem('appearance', mode)`. It then applies the theme to the document by toggling a CSS class on the `<html>` element based on the chosen mode and calling the `applyTheme()` function. Finally, it highlights the clicked button as the active one by adding a CSS class called "ACTIVE" using `document.querySelectorAll('#appearance-toggle .appearance-btn').forEach(function (btn) { ... })`

The `highlightButtons(mode)` function takes care of updating the appearance of all buttons in the dropdown menu based on the chosen mode. If the chosen mode is "system", it adds a CSS class called "INACTIVE" to all buttons, indicating that the theme will automatically adjust based on the user's system preferences. Otherwise, it removes this class from all buttons and adds another CSS class called "ACTIVE" to the button that corresponds to the chosen mode.

The `applyTheme(mode)` function takes care of actually applying the chosen theme to the document by toggling a CSS class on the `<html>` element based on the chosen mode. If the chosen mode is "light", it adds a CSS class called "bg-zinc-200 dark:bg-zinc-700 text-zinc-900 dark:text-zinc-100" to the `<html>` element, which sets the background color and font color of the page to light theme colors. If the chosen mode is "dark", it adds a CSS class called "bg-zinc-700 text-zinc-100 dark:text-zinc-900" to the `<html>` element, which sets the background color and font color of the page to dark theme colors. Finally, if the chosen mode is "system", it removes this class from the `<html>` element and adds a CSS class called "bg-zinc-100 dark:bg-zinc-800 text-zinc-900" to it, which sets the background color and font color of the page to system theme colors.

---

### resources/views/livewire/settings/delete-user-form.blade.php
This file is a Livewire component for the "Delete Account" functionality in the Laravel application. It allows the user to delete their account and all of its resources by providing their password. The component consists of several classes, functions, and endpoints:

* `use App\Livewire\Actions\Logout;` - Importing the `Logout` class from the `App\Livewire\Actions` namespace. This class will be used to log out the user after deleting their account.
* `use Illuminate\Support\Facades\Auth;` - Importing the `Auth` facade from Laravel's built-in support for authentication. This will be used to retrieve the currently logged in user and perform the deletion action.
* `new class extends Component {` - Defining a new Livewire component that extends the `Component` class provided by Livewire. This class will handle the presentation logic of the "Delete Account" functionality.
* `public string $password = '';` - Declaring a public property `$password` as an empty string. This will be used to store the user's password for validation purposes.
* `public bool $showDeleteModal = false;` - Declaring a public property `$showDeleteModal` as a boolean value that is initially set to `false`. This will be used to control the visibility of the "Delete Account" modal dialog.
* `public function openModal(): void { ... }` - Defining a public method `openModal()` that sets the value of `$showDeleteModal` to `true`.
* `public function closeModal(): void { ... }` - Defining a public method `closeModal()` that sets the value of `$showDeleteModal` to `false` and resets the values of `$password` and `$errors` to their initial state.
* `public function deleteUser(Logout $logout): void { ... }` - Defining a public method `deleteUser()` that performs the actual deletion of the user's account by calling the `delete()` method on the currently logged in user, passing in an instance of the `Logout` class. It then redirects the user to the homepage using the `redirect()` method.
* `@if($showDeleteModal)` - Conditionally rendering a modal dialog with the "Delete Account" form if `$showDeleteModal` is set to `true`.
* `<div class="relative z-10 w-full max-w-lg rounded-lg border border-zinc-200 bg-white p-6 shadow-xl dark:border-zinc-700 dark:bg-zinc-800">` - Defining a container element with a `relative` position, `z-10` layer, and `w-full` width that matches the parent element. It has a `max-w-lg` maximum width of 1024 pixels (the default maximum width for Livewire components), rounded corners, a gray border color with a `border-2` thickness, a white background color, and an `xl` shadow size.
* `<form wire:submit="deleteUser" class="space-y-6">` - Defining a form element that submits the "Delete Account" form when the user clicks the submit button. It has a `space-y-6` margin spacing of 6 pixels between the child elements and uses the `wire:submit` attribute to bind the submit event to the `deleteUser()` method.
* `<label for="delete_password" class="block text-sm font-medium text-zinc-700 dark:text-zinc-300">{{ __('Password') }}</label>` - Defining a label element with a `block` display type, a `space-y-6` margin spacing of 6 pixels between the child elements, and a font size of 14 pixels. It has a `for` attribute set to "delete_password" and is used to label the password input field below.
* `<input id="delete_password" wire:model="password" type="password" class="mt-1 block w-full rounded-md border border-zinc-300 bg-white px-3 py-2 text-sm text-zinc-900 shadow-sm placeholder:text-zinc-400 focus:border-zinc-500 focus:outline-none focus:ring-1 focus:ring-zinc-500 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-100 dark:placeholder:text-zinc-500" />` - Defining a password input field with an `id` attribute set to "delete_password", a `wire:model="password"` attribute that binds the value of this field to the `$password` property, and a `type` attribute set to "password". It has a `mt-1` margin top spacing of 1 pixel, a `block` display type, `w-full` width that matches its parent element, rounded corners, a gray border color with a `border-2` thickness, a white background color, an `xl` shadow size, and a `px-3 py-2` padding of 3 pixels on the left and right.
* `@error('password') <p class="mt-1 text-sm text-red-600">{{ $message }}</p> @enderror` - Rendering an error message if there is a validation error for the `password` field. It has a `mt-1` margin top spacing of 1 pixel, a `text-sm` font size of 12 pixels, and a red text color with an `text-red-600` class.
* `<button type="button" wire:click="closeModal" class="rounded-md border border-zinc-300 bg-white px-4 py-2 text-sm font-medium text-zinc-700 shadow-sm hover:bg-zinc-50 dark:border-zinc-600 dark:bg-zinc-700 dark:text-zinc-300 dark:hover:bg-zinc-600">{{ __('Cancel') }}</button>` - Defining a button element with an `id` attribute set to "delete_cancel", a `wire:click="closeModal"` event that calls the `closeModal()` method, and a `type` attribute set to "button". It has a `rounded-md` border radius of 4 pixels, a gray border color with a `border-2` thickness, a white background color, an `xl` shadow size, and a `px-4 py-2` padding of 4 pixels on the left and right.
* `<button type="submit" data-test="confirm-delete-user-button" class="rounded-md bg-red-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">{{ __('Delete account') }}</button>` - Defining a button element with an `id` attribute set to "delete_submit", a `type` attribute set to "submit" that submits the form, and a `data-test` attribute for testing purposes. It has a `rounded-md` border radius of 4 pixels, a red background color with a `bg-red-600` class, an `xl` shadow size, a `px-4 py-2` padding of 4 pixels on the left and right, a `font-semibold` font weight, white text color with a `text-white` class, and an `hover:bg-red-500` hover effect that changes the background color to a darker red.
* `@endif` - Closing the conditional rendering of the modal dialog if `$showDeleteModal` is set to `true`.

---

### resources/views/livewire/settings/password.blade.php
This file contains a Livewire component for updating the password of the currently authenticated user. The component has three properties: `current_password`, `password`, and `password_confirmation`. These properties are used to validate the input data and update the user's password in the database.

The component also includes an error handling mechanism to display any validation errors that may occur during the update process.

The component uses the `Auth` facade to access the currently authenticated user, and the `update()` method of the `App\Models\User` model to update the password in the database. The `Password` class is used to validate the input data according to Laravel's built-in password validation rules.

The component also includes a form for updating the password, which uses Livewire's wire:submit method to call the `updatePassword()` method on submission. The form includes three fields for entering the current password, a new password, and a confirmation field for the new password.

The component also includes an error handling mechanism to display any validation errors that may occur during the update process.

Overall, this file appears to be a user interface component for updating a user's password using Livewire.

---

### resources/views/livewire/settings/profile.blade.php
The file `resources/views/livewire/settings/profile.blade.php` contains a Livewire component that updates the profile information of the currently authenticated user. The component uses the `App\Models\User` model to retrieve and update the user's name and email address.

Here is a summary of the main functions in this file:

1. The `mount()` method is called when the component is initialized, and it retrieves the current user's name and email address from the `Auth` facade.
2. The `updateProfileInformation()` method updates the profile information for the currently authenticated user. It validates the input data using the Laravel validation system, and then saves the updated information to the database using Eloquent models.
3. The `resendVerificationNotification()` method sends a new verification email to the current user if their email address is not yet verified.
4. The component includes a delete user form that allows the user to delete their account. This form is also built using Livewire and is located in the same folder as this profile component.

External dependencies:

* The `App\Models\User` model is used to interact with the database and retrieve information about the current user.
* The `Illuminate\Support\Facades\Auth` facade is used to access the authenticated user's information.
* The `Illuminate\Support\Facades\Session` facade is used to flash a success message when the profile update is successful.
* The `Livewire\Volt\Component` class is used to define the Livewire component.

---

### resources/views/livewire/settings/two-factor.blade.php
Die Datei `resources/views/livewire/settings/two-factor.blade.php` ist ein Blade Template, das die Funktionalität der Two-Factor Authentication (2FA) in der Laravel-Anwendung "Laravel Installer" implementiert.

Hier sind die wichtigsten Funktionen der Datei:

1. Mounting der Komponenten: Die Datei verwendet die `Livewire`-Komponente, um die Funktionalität der 2FA zu implementieren.
2. Enable/Disable Two-Factor Authentication: Der Benutzer kann über die Komponente die 2FA einschalten oder ausschalten.
3. Load Setup Data: Die Komponente lädt die Daten für die Einrichtung der 2FA.
4. Verification Step: Wenn die 2FA konfiguriert ist, zeigt die Komponente einen Verifizierungsschritt an, um den Benutzer zu bitten, einen sicheren, zufälligen Code zu überprüfen.
5. Confirm Two-Factor Authentication: Der Benutzer kann über die Komponente bestätigen, dass er den sicheren Code eingegeben hat und damit die 2FA aktivieren.
6. Reset Verification State: Wenn der Benutzer den Schritt für die Bestätigung durchgeführt hat, wird der Status zurückgesetzt.
7. Close Modal: Die Komponente schließt das Modalfenster nach dem Beenden des Prozesses.

Die Datei importiert verschiedene Komponenten und Funktionen aus Laravel und Livewire, um die Funktionalität der 2FA zu implementieren. Es gibt auch einige Validierungsschritte und Logik für den Schritt zur Bestätigung des sicheren Codes.

---

### resources/views/livewire/settings/webhooks.blade.php
Die Datei bezieht sich auf das Livewire-Komponentenobjekt 'Webhooks', das die Einstellungen für Webhook-Einträge in der Dashboard-Seite und Recherche-Slot enthält.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
Die Datei ist eine Livewire-Komponente für die Anzeige und Verwaltung von 2FA-Wiederherstellungscodes. Die Komponente verwendet das Laravel Fortify-Paket, um die Wiederherstellungscodegenerierung und -verwendung zu verwalten.

Die Komponente besteht aus mehreren Funktionen und Variablen:

* `mount()`: Der Mount-Method wird verwendet, um die Komponenteninstanz zu initialisieren. Hier wird die Methode `loadRecoveryCodes()` aufgerufen, um die Wiederherstellungscodes für den aktuellen Benutzer zu laden.
* `regenerateRecoveryCodes(GenerateNewRecoveryCodes $generateNewRecoveryCodes)`: Diese Funktion wird verwendet, um neue Wiederherstellungscodes für den aktuellen Benutzer zu generieren. Hier wird die `GenerateNewRecoveryCodes`-Methode aus dem Laravel Fortify-Paket aufgerufen, um die neuen Codes zu generieren. Anschließend wird die Methode `loadRecoveryCodes()` aufgerufen, um die neuen Codes in der Komponente zu laden.
* `loadRecoveryCodes()`: Diese Funktion wird verwendet, um die Wiederherstellungscodes für den aktuellen Benutzer zu laden. Hier wird der aktuelle Benutzer aus dem Auth-Trait von Laravel geladen und dann die Wiederherstellungscodes durch den Fortify-Service abgerufen. Wenn die Codes erfolgreich geladen wurden, werden sie in der Komponenteninstanz als Array gespeichert.
* `recoveryCodes`: Diese Variable enthält das Array der Wiederherstellungscodes für den aktuellen Benutzer.
* `showRecoveryCodes`: Diese Variable wird verwendet, um zu steuern, ob die wiederherstellungscode-Abschnitt in der Komponente sichtbar ist oder nicht. Wenn die Codes geladen wurden und der Benutzer die Schaltfläche "Anzeigen" drückt, wird diese Variable auf `true` gesetzt, sodass der Abschnitt angezeigt wird.
* `aria-expanded`: Diese Attribute wird verwendet, um den Fokus auf das Element mit der ID "recovery-codes-section" zu setzen, wenn die Schaltfläche "Anzeigen" drückt wird.
* `aria-controls`: Diese Attribut wird verwendet, um zu steuern, welche Inhalte durch die Schaltfläche "Anzeigen" geändert werden sollen. Hier wird der Abschnitt mit der ID "recovery-codes-section" als Ziel festgelegt.
* `@if (filled($recoveryCodes))`: Diese Direktive wird verwendet, um zu überprüfen, ob die recoveryCodes-Variable gefüllt ist. Wenn dies der Fall ist, werden die Wiederherstellungscodes in der Komponente angezeigt.

Die Komponentenfunktionen und Variablen sind in 10 Zeilen zusammengefasst, inklusive aller relevanten Import-Anweisungen und -Deklarationen.

---

### resources/views/partials/head.blade.php
Die Datei `resources/views/partials/head.blade.php` ist ein Teil der Laravel-Anwendung und enthält eine Liste von HTML-Tags, die den Kopf einer Webseite ausfüllen. Die wichtigsten Funktionen dieser Datei sind:

* Das Setzen des Charsets auf `utf-8`
* Das Definieren der Viewport-Breite und -Skalierung für responsive Designs
* Das Einbinden von CSS-Stylesheets aus der `resources/css`-Verzeichnis und JavaScript-Skripts aus der `resources/js`-Verzeichnis mithilfe des `@vite` Tags.
* Das Einbinden von zwei Icon-Links, die auf den Dateien `/favicon.ico` und `/apple-touch-icon.png` verweisen.
* Das Einbinden eines `<link>`-Tags für das Font-Familie `instrument-sans`, das von der Datei `resources/css/app.css` geladen wird.
* Das Setzen einer Dark-Mode-Klasse (`dark`) auf den HTML-Elementen, wenn eine Benutzerpräferenz für Dunkelmodus festgelegt ist oder wenn die Appearance-Einstellung auf `dark` gesetzt wurde. Dies wird mithilfe eines Skripts in der Datei `resources/js/app.js` durchgeführt.
* Das Einbinden von JavaScript-Skripts, die vor und nach dem `@vite`-Tag geladen werden.

---

### resources/views/partials/settings-heading.blade.php
Here is the file summary in 10 lines or less:

This Blade template file provides a heading for the settings section of a website. It contains a h1 element with the text "Settings", a p element with a subtitle, and an hr element that separates the heading from the rest of the content. The template uses Laravel's localization feature to provide translated text for the headings and subtitles.

The file does not import any external libraries or frameworks. It is designed to be used within a Laravel application and relies on the app's internationalization configuration to provide translated strings.

---

### resources/views/recherche/index.blade.php
Die Datei `resources/views/recherche/index.blade.php` ist ein Blade-Template, das die Seite für die Recherche der App verwendet.

Es importiert das `Layout`-Component von `resources/views/layouts/app.blade.php`, um die Benutzeroberfläche zu erstellen. Es gibt auch eine Livewire-Komponente namens `recherche.research-input`, die für die Eingabe der Suche verwendet wird, sowie eine weitere Livewire-Komponente namens `recherche.projekt-liste`, die die Ergebnisse der Suche anzeigt.

Das Template setzt auch Verweise auf das Thema `zinc` und die Farbe `zinc-200` oder `zinc-700` für die Textfarbe in dunklen Modi.

Insgesamt ist diese Datei Teil der Benutzeroberfläche der App und ermöglicht es den Benutzern, nach Projekten zu suchen und sie anzuzeigen.

---

### resources/views/recherche/show.blade.php
This file is a view for displaying the details of a project in the 'recherche' category. It uses the <livewire:recherche.projekt-detail /> component to display the project information.

---

## uncategorized

### Readme.md
Maximal 10-zeilige Beschreibung der Datei-Funktionen

---

### artisan
This is the main file for Artisan, the command-line interface for Laravel. It defines the Composer autoloader and bootstraps the Laravel application, which handles the command passed to it using the Symfony Console component.

---

### basemodel.md
Die Datei enthält ein Modell für eine PostgreSQL-Datenbank zur Speicherung von Daten zu systematischen Reviews und der zugehörigen Projekten. Die wichtigsten Klassen und Funktionen sind:

* `projekte`: Tabelle mit den wichtigsten Informationen zu den Projekten, wie dem Titel, der Forschungsfrage, dem Review-Typ, dem Verantwortlichen und anderen relevanten Daten.
* `phasen`: Tabelle zur Nachverfolgung der Phasen des Reviews, wie P1 bis P8. Jede Phase hat einen eigenen Status (offen, in Bearbeitung oder abgeschlossen) und kann auch Notizen enthalten.
* `p1_strukturmodell_wahl`: Tabelle zur Auswahl des Strukturmodells für die PHASE 1.
* `p1_komponenten`: Tabelle zur Aufnahme von Komponenten-Kürzel und -Beschriftungen für das Strukturmodell, sowie Optionale Felder wie Synonyme oder Anmerkungen.
* `p1_kriterien`: Tabelle zur Aufnahme der Kriterien und Begründungen für die PHASE 1.
* `p1_warnsignale`: Tabelle zur Aufnahme von Warnsignalen und möglichen Handlungsempfehlungen für die PHASE 1.
* `p2_review_typ_entscheidung`: Tabelle zur Entscheidungsfindung des Review-Typs in der PHASE 2.
* `p2_mapping_suchstring_komponenten`: Tabelle zur Aufnahme von Suchstrings und zugehörigen Komponenten in der PHASE 2.
* `p2_trefferlisten`: Tabelle zur Aufnahme von Treffern in der PHASE 2.
* `p2_cluster`: Tabelle zur Aufnahme von Clustern in der PHASE 2.
* `p3_disziplinen`: Tabelle zur Auswahl der Disziplinen für die PHASE 3.
* `p3_datenbankmatrix`: Tabelle zur Aufnahme von Datenbanken und zugehörigen Filtern in der PHASE 3.
* `p3_geografische_filter`: Tabelle zur Aufnahme geographischer Filter in der PHASE 3.
* `p3_graue_literatur`: Tabelle zur Aufnahme von grauen Literaturen in der PHASE 3.
* `p4_suchstrings`: Tabelle zur Aufnahme von Suchstrings in der PHASE 4.
* `p4_thesaurus_mapping`: Tabelle zur Aufnahme von Thesaurus-Mapping in der PHASE 4.

Es gibt auch einige externe Abhängigkeiten wie z.B. die Erweiterung "uuid-ossp" und die Verwendung von "pg_trgm" für Volltextsuche auf Strings.

---

### composer.json
This is a `composer.json` file for a Laravel application. It defines the dependencies, scripts, and configuration options for the project.

Here are the key parts of the file:

* `name`: The name of the package (in this case, "nileneb/app-linn-games").
* `type`: The type of package (in this case, "project").
* `description`: A brief description of the project.
* `keywords`: An array of keywords to help with search and discovery.
* `license`: The license for the project (in this case, "proprietary").
* `require`: An array of dependencies that are required by the project.
* `require-dev`: An array of development dependencies that are not required in production.
* `autoload`: A list of files and directories to be autoloaded by Composer.
* `scripts`: An array of scripts that can be run by Composer, such as "setup", "test", and "post-update-cmd".
* `extra`: Additional configuration options for the project.
* `config`: Configuration settings for Composer.
* `minimum-stability`: The minimum stability level required for dependencies (in this case, "stable").
* `prefer-stable`: Whether to prefer stable versions of dependencies over unstable ones.

---

### CONTRIBUTING.md
This file provides guidelines for contributing to the app.linn.games project, including branch naming conventions, a merge flow, and local development workflow. It also outlines coding standards, database changes, and testing practices.

---

### deploy.sh
This is a shell script that deploys the application to production. It performs several tasks, including building and deploying the application, starting up infrastructure components such as PostgreSQL and Redis, running migrations, optimizing the cache, and sending a notification email upon deployment completion.

Here's a breakdown of each section:

1. Pre-flight checks: The script checks if `docker` is installed and if the `.env` file exists. If not, it exits with an error message.
2. Building: The script pulls the latest Docker images for PostgreSQL, Redis, and the application. It then builds the production images using `docker compose build`. The `--no-cache` flag disables the image cache, which helps ensure that the most recent changes are picked up.
3. Starting infrastructure: The script starts the PostgreSQL and Redis containers in detached mode (`-d`) using `docker compose up`. It then waits for PostgreSQL to become ready using a health check.
4. Backup: The script creates a backup of the database using `pg_dump`. The backup is saved to the `./backups` directory with a timestamped filename.
5. Migrations: If the `--skip-migrate` flag is not provided, the script runs the migrations using `docker compose run --rm php-cli php artisan migrate --force`. If it fails, it rolls back the backup and exits with an error message.
6. Cache optimization: The script caches the application configuration, routes, and views using `docker compose run --rm php-cli php artisan config:cache`, `route:cache`, and `view:cache` respectively.
7. Starting all services: The script clears the stale build-asset volume (`applinngames_linn-build-assets`) and starts all services using `docker compose up -d`.
8. Verification: The script waits for 5 seconds before checking the service status using `docker compose ps`.
9. Deploy notification mail: If the `--skip-deploy-notify` flag is not provided, the script sends a deployment notification email using `docker compose run --rm php-cli php artisan deploy:notify`.
10. Cleanup: The script removes any orphaned containers and volumes using `docker compose down --remove-orphans 2>/dev/null || true` and `docker volume rm applinngames_linn-build-assets 2>/dev/null || true`.

---

### package.json
This package.json file is a configuration file for the Vite build tool, which is used to create and serve web applications. It specifies the dependencies required by the project and configures some of the behavior of Vite.

The file includes several sections:

* "$schema": This line specifies the JSON schema that this file should follow.
* "private": This field indicates whether the package is intended for use only within your own organization, or whether it can be published to a registry and used by others. In this case, it is set to "true", indicating that the package is private.
* "type": This field specifies the type of module that is being defined. In this case, it is set to "module", which indicates that the file defines a Node.js module that can be imported and used in other projects.
* "scripts": This section contains scripts that are executed by Vite during certain events, such as building or serving the application. The "build" script is set to "vite build", which tells Vite to run its built-in build command. The "dev" script is set to "vite", which tells Vite to start its development server.
* "dependencies": This section lists the dependencies required by the project, including libraries such as axios, tailwindcss, and vite. These dependencies are used by the application and must be included in order for it to function correctly.
* "optionalDependencies": This section contains optional dependencies that can be used by the project, but are not required to function properly. In this case, there are three optional dependencies specified: @rollup/rollup-linux-x64-gnu, @tailwindcss/oxide-linux-x64-gnu, and lightningcss-linux-x64-gnu. These dependencies may be used by the project if necessary, but they are not required to function properly.

Overall, this package.json file is used to configure Vite and specify the dependencies that are needed for a Node.js application.

---

### phpunit.xml
The `phpunit.xml` file is a configuration file for the PHPUnit testing framework. It defines various settings and options that are used by PHPUnit to run tests, including the location of test suites, source code directories, and environment variables.

Here's a summary of the most important elements in the `phpunit.xml` file:

* `<testsuites>`: This element defines the directory where PHPUnit should look for test suites to run. In this case, it is set to `tests/Unit` and `tests/Feature`.
* `<source>`: This element specifies the directories that contain the source code that should be tested. In this case, it is set to `app`.
* `<php>`: This element defines various environment variables that are used by PHPUnit during testing. The most important ones are:
	+ `APP_ENV`: Set to "testing" to indicate that the tests are running in a test environment.
	+ `APP_KEY`: Set to a base64-encoded random string for security purposes.
	+ `APP_MAINTENANCE_DRIVER`: Set to "file" to enable file-based maintenance mode.
	+ `BCRYPT_ROUNDS`: Set to 4 to optimize the performance of bcrypt hashing algorithms used by PHPUnit.
	+ `BROADCAST_CONNECTION`: Set to "null" to disable broadcasting for testing purposes.
	+ `CACHE_STORE`: Set to "array" to use an in-memory cache store for testing purposes.
	+ `DB_CONNECTION`: Set to "sqlite" to use a SQLite database for testing purposes.
	+ `DB_DATABASE`: Set to ":memory:" to use an in-memory SQLite database for testing purposes.
	+ `MAIL_MAILER`: Set to "array" to disable mail delivery for testing purposes.
	+ `QUEUE_CONNECTION`: Set to "sync" to enable synchronous queueing for testing purposes.
	+ `SESSION_DRIVER`: Set to "array" to use an in-memory session store for testing purposes.
	+ `PULSE_ENABLED`: Set to "false" to disable Pulse debugging for testing purposes.
	+ `TELESCOPE_ENABLED`: Set to "false" to disable Telescope debugging for testing purposes.
	+ `NIGHTWATCH_ENABLED`: Set to "false" to disable Nightwatch testing for testing purposes.

Overall, this file is used to configure PHPUnit's behavior and environment for running tests in a test environment.

---

### vite.config.js
Die Datei `vite.config.js` ist eine Konfigurationsdatei für die Vite-Buildsystem-Engine. Sie enthält Konfigurationen und Plugins, die verwendet werden, um die Buildprozesse der Anwendung zu optimieren und zu verbessern.

In dieser Datei sind einige wichtige Funktionen und Endpunkte:

* `defineConfig`: Eine Funktion, die verwendet wird, um eine Konfiguration für Vite zu definieren.
* `laravel`: Ein Plugin, das verwendet wird, um Laravel-Projekte mit Vite zu verbinden. Es enthält Optionen wie den Pfad zu den Input-Dateien und die Option zum Aktualisieren der Dateien bei Änderungen.
* `tailwindcss`: Ein Plugin, das verwendet wird, um TailwindCSS-Komponenten in die Anwendung einzubinden.
* `server`: Eine Eigenschaft, die verwendet wird, um den Serverkonfigurationen für Vite zu definieren. Es enthält Optionen wie den CORS-Header und die ignored Dateien.

Insgesamt enthält diese Datei eine Vielzahl von Funktionen und Endpunkten, die verwendet werden, um die Anwendung zu erstellen, zu debuggen und zu optimieren.

---

### .dockerignore
This file is a .dockerignore file, which tells Docker which files and directories to ignore when building a Docker image. It contains a list of glob patterns that specify what files and directories should be excluded from the build process.

The contents of this file are as follows:

* `.git`: Ignore the Git directory.
* `.github`: Ignore the GitHub directory.
* `.gitignore`: Ignore the .gitignore file itself.
* `.env`: Ignore any environment variables files (e.g., .env, .env.example).
* `node_modules/`: Ignore the node_modules directory.
* `vendor/`: Ignore the vendor directory.
* `storage/framework/cache/`: Ignore the cache directory for Laravel framework.
* `storage/framework/sessions/`: Ignore the sessions directory for Laravel framework.
* `storage/framework/views/`: Ignore the views directory for Laravel framework.
* `storage/logs/`: Ignore the logs directory for Laravel framework.
* `public/build/`: Ignore the build directory for Laravel framework.
* `public/hot`: Ignore the hot directory for Laravel framework.
* `tests/`: Ignore the tests directory.
* `.phpunit.cache/`: Ignore the cache directory for PHPUnit testing framework.
* `coverage/`: Ignore the coverage directory for PHPUnit testing framework.
* `*.log`: Ignore any log files (e.g., app.log, debug.log).
* `*.sqlite`: Ignore any SQLite databases.
* `*.sqlite-shm`: Ignore any SQLite shared memory files.
* `*.sqlite-wal`: Ignore any SQLite write-ahead logs.
* `docker-compose*.yml`: Ignore any Docker Compose files (e.g., docker-compose.yml, docker-compose.yaml).
* `.editorconfig`: Ignore the editor configuration file.
* `.styleci.yml`: Ignore the style checker configuration file.
* `phpunit.xml`: Ignore the PHPUnit configuration file.
* `phpstan.neon`: Ignore the PHPStan configuration file.
* `Readme.md`: Ignore the readme file.

This .dockerignore file is meant to be used in a Laravel project, so it includes files and directories that are specific to this framework. It excludes certain files and directories that are not relevant for the Docker image build process.

---

### .editorconfig
This is an EditorConfig file that sets the configuration for various code editors and IDEs. The file is written in a format that can be read by the EditorConfig parser, which is supported by many popular text editors and integrated development environments (IDEs).

The main sections of this file are:

* `root = true`: This indicates that this is the root configuration file for the project.
* `[*]`: This section applies to all files in the project. The options specified here will be applied to all files unless they are overridden by more specific settings.
	+ `charset = utf-8`: Sets the character encoding to UTF-8.
	+ `end_of_line = lf`: Sets the line ending format to LF (Linux/Mac style).
	+ `indent_size = 4`: Sets the indent size to 4 spaces.
	+ `indent_style = space`: Sets the indent style to use spaces instead of tabs.
	+ `insert_final_newline = true`: Specifies whether or not to insert a newline character at the end of files.
	+ `trim_trailing_whitespace = true`: Specifies whether or not to trim trailing whitespace characters from lines.
* `[*.md]`: This section applies to all files with a `.md` extension (Markdown files).
	+ `trim_trailing_whitespace = false`: Overrides the setting specified in the previous section and disables trimming trailing whitespace for Markdown files.
* `[*.{yml,yaml}]`: This section applies to all files with either the `.yml` or `.yaml` extension (YAML files).
	+ `indent_size = 2`: Sets the indent size to 2 spaces for YAML files.
* `[compose.yaml]`: This section applies to the file named `compose.yaml`.
	+ `indent_size = 4`: Overrides the setting specified in the previous sections and sets the indent size to 4 spaces specifically for the `compose.yaml` file.

---

### app/Actions/Fortify/CreateNewUser.php
The file `app/Actions/Fortify/CreateNewUser.php` is part of a Laravel application and contains a class that implements the `CreatesNewUsers` contract provided by the `Laravel\Fortify\Contracts` namespace. This class is responsible for validating and creating new users.

The class defines a method called `create`, which takes an array of input data as its only parameter. The method first uses Laravel's validation feature to validate the input data against a set of rules defined in the `$this->passwordRules()` method, which is a trait provided by the `PasswordValidationRules` class.

The validation rules include requiring the presence and format of the user's name, email, and password, as well as ensuring that the email address is unique and does not already exist in the `User` model.

If the input data is validated successfully, the method creates a new instance of the `User` model using the provided input data. The created user object is then returned from the method.

Overall, this class provides a convenient way to create and validate new users within the Laravel application, making it easier for developers to manage user accounts without having to manually write validation logic.

---

### app/Actions/Fortify/PasswordValidationRules.php
This file contains a PHP trait called `PasswordValidationRules` that defines some validation rules for passwords. The trait is part of the Laravel Fortify package, which provides authentication and registration features for Laravel applications.

The `passwordRules` method returns an array of validation rules for passwords. These rules include:

* `required`: The password field must be present in the input data.
* `string`: The password field must be a string value.
* `Password::default()`: This is a default validation rule provided by Laravel that ensures the password meets certain security standards, such as being at least 8 characters long and containing at least one letter and one number.
* `confirmed`: The password confirmation field must match the password field.

The `PasswordValidationRules` trait is used to define these validation rules for passwords in the Laravel application. It is typically used in forms that require users to enter their passwords, such as login forms or registration forms.

---

### app/Actions/Fortify/ResetUserPassword.php
This is a PHP class that implements the `ResetsUserPasswords` contract from Laravel's Fortify package. The class contains a method named `reset`, which takes in an array of user input and a `User` model as arguments.

The `reset` method first validates the input using Laravel's built-in validation rules, specifically the `passwordRules()` method from the `PasswordValidationRules` trait. If the input is validated successfully, it sets the user's password to the provided value and saves it to the database.

The class also uses a trait named `PasswordValidationRules`, which provides additional validation rules for passwords. The `passwordRules()` method in this trait checks that the password is at least 8 characters long and contains at least one letter, one number, and one special character.

Overall, this class is responsible for resetting a user's forgotten password by validating the input and updating the user's password in the database.

---

### app/Filament/Resources/ContactResource.php
Die Datei `app/Filament/Resources/ContactResource.php` definiert eine Kontakt-Ressource für die Verwendung in Filament, einem PHP-basierten Admin-Panel. Die wichtigsten Klassen und Funktionen dieser Datei sind:

* `App\Models\Contact`: Diese Klasse repräsentiert das Modell der Kontakte in der Datenbank.
* `Filament\Forms`: Diese Klasse ist für die Definition von Formularen zuständig, die verwendet werden, um neue oder bestehende Kontakte zu erstellen oder zu bearbeiten.
* `Filament\Schemas\Schema`: Diese Klasse wird verwendet, um Schemas zu definieren, die den Datensatzstruktur von Filament entsprechen.
* `Filament\Resources\Resource`: Diese Klasse ist für die Definition von Ressourcen zuständig, die es ermöglichen, eine bestimmte Modellklasse in einem Admin-Panel darzustellen und zu verwalten.
* `Filament\Tables\Table`: Diese Klasse wird verwendet, um Tabellendefinitionen zu erstellen, die verwendet werden können, um Kontakte in der Tabelle anzuzeigen oder zu bearbeiten.
* `App\Filament\Resources\ContactResource\Pages\ListContacts`: Diese Klasse definiert eine Seite, die verwendet wird, um alle Kontakte in der Datenbank aufzulisten.
* `App\Filament\Resources\ContactResource\Pages\ViewContact`: Diese Klasse definiert eine Seite, die verwendet wird, um einen bestimmten Kontakt anzuzeigen oder zu bearbeiten.

Die Datei enthält auch einige importierte Namespaces, die den Zugriff auf die erforderlichen Klassen und Funktionen ermöglichen, wie zum Beispiel `BackedEnum`, `Forms\Components\TextInput` und andere.

---

### app/Filament/Resources/UserResource.php
This is a PHP class that defines a Filament resource for managing user records. The resource provides a form and table to interact with the user model.

Here's a summary of the main functionalities in 10 lines or less:

* It uses the `User` model as its underlying data source.
* It has a navigation icon and label for displaying it in the Filament dashboard.
* The form component is used to create, edit, and delete user records. The schema defines two text inputs (name and email) and a select input with options for setting the status of the user.
* The table component is used to display a list of users, including their name, email, status, role, and creation date. It has filters and actions that allow for searching, sorting, and editing user records.
* The `getPages` method returns an array of page objects that define the URLs for the resource's pages. In this case, it includes a list page (index) and an edit page (edit).

---

### app/Http/Middleware/TrackPageView.php
This is a middleware class in Laravel that tracks page views by logging them to the database. The `TrackPageView` class uses the `PageView` model from the app's models directory to store page view information.

The `handle()` method checks if the incoming request is a GET request and if it's not coming from a bot (based on a list of predefined patterns in the `BOT_PATTERNS` constant). If both conditions are true, it creates or updates a record in the `page_views` table with the request path, anonymized IP address, and user agent. The `increment('visits')` method is used to increment the `visits` column for the corresponding page view record.

The `anonymizeIp()` method takes an IP address as input and returns its anonymized form. It replaces the last octet of an IPv4 address with 0, while zeroing out the last 80 bits of an IPv6 address. This is done to protect the privacy of the users who are visiting the website.

The `isBot()` method takes a user agent string as input and checks if it contains any predefined patterns in the `BOT_PATTERNS` constant. If any pattern is found, the method returns `true`, indicating that the request is coming from a bot.

Overall, this middleware class is responsible for tracking page views and logging them to the database, while also protecting users' privacy by anonymizing their IP addresses.

---

### app/Http/Middleware/VerifyLangdockSignature.php
This is a PHP file that defines a middleware class for Laravel named "VerifyLangdockSignature". The middleware class is used to verify the integrity of webhook requests from Langdock, a language learning platform. It does this by checking the request's signature header against a known secret key, and also replay protection via a cache nonce.

Here are the main features of this file:

* It imports the "Closure" and "Request" classes from Laravel, as well as the "hash_hmac", "is_numeric", and "time" functions from PHP.
* It defines a class named "VerifyLangdockSignature" that extends the "Middleware" class from Laravel.
* The constructor of the class sets up some constants for replay protection, such as the tolerance time window for request timestamps.
* The handle method is the entry point for the middleware class. It first checks if a secret key has been configured in the application's configuration file, and aborts with an error message if not. Then it retrieves the signature header from the incoming request and checks its validity against the known secret key using the "hash_hmac" function.
* If the signature is invalid or missing, the method aborts with a 403 Forbidden response.
* Next, the method checks if the timestamp in the request header is within the tolerance window set by the TIMESTAMP_TOLERANCE constant. If not, it aborts with a 403 Forbidden response.
* Then, the method retrieves the request content and constructs an expected signature using the "hash_hmac" function with the same parameters as before. It then compares this expected signature to the actual signature in the header using the "hash_equals" function. If the signatures are not equal, the method aborts with a 403 Forbidden response.
* Finally, the method uses the Laravel Cache facade to create and store a cache nonce for the incoming request's signature. This nonce is used for replay protection, as it prevents the same signature from being processed more than once in the specified tolerance window. If the nonce already exists in the cache, the method aborts with a 403 Forbidden response.

Overall, this middleware class helps ensure that only valid and unreplayed webhook requests are processed by the application, which is important for security reasons.

---

### app/Http/Middleware/VerifyMcpToken.php
Die Datei `app/Http/Middleware/VerifyMcpToken.php` enthält eine Middleware-Klasse, die sicherstellt, dass jede Anfrage an den Server mit einem gültigen Token versehen ist, das von der MCP (Multiple Concurrency Protocol) verwendet wird.

Die Klasse hat folgende Funktionen:

* `handle()`: Die Hauptfunktion der Klasse, die die Anfrage verarbeitet und einen Response-Objekt zurückgibt.
* `config('services.mcp.auth_token')`: Ruft den MCP-Authentifizierungstoken aus der Konfiguration ab.
* `str_starts_with($header, 'Bearer ')`: Überprüft, ob die Anfrage mit dem Präfix "Bearer" versehen ist.
* `hash_equals($token, substr($header, 7))`: Überprüft, ob der Token in der Anfrage mit dem gültigen MCP-Token übereinstimmt.
* `abort(500, 'MCP auth token not configured.')` und `abort(401, 'Missing Bearer token.')`: Wenn der MCP-Authentifizierungstoken nicht konfiguriert ist oder wenn die Anfrage keinen gültigen Token enthält, wird eine Fehlerantwort mit dem Statuscode 500 (Interner Serverfehler) oder 401 (Unauthorized) zurückgegeben.

Die Klasse verwendet folgende External-Dependencies:

* `Symfony\Component\HttpFoundation\Response`: Das Response-Objekt wird verwendet, um eine HTTP-Antwort zurückzugeben.
* `Illuminate\Http\Request`: Das Request-Objekt wird verwendet, um die Anfrage zu verarbeiten und den gültigen Token abzurufen.

---

### app/Jobs/IngestPaperJob.php
This file contains a job class for ingesting paper data into the database. It implements the `ShouldQueue` interface and uses the Laravel framework's built-in queue system to handle asynchronous processing of large amounts of data. The class defines several public properties:

* `$paperId`: The ID of the paper being processed.
* `$source`: The source of the paper (e.g., a URL or a file).
* `$title`: The title of the paper.
* `$text`: The full text of the paper.
* `$projektId`: An optional ID of a project to associate with the paper.
* `$metadata`: An optional array of metadata associated with the paper (e.g., author names, publication date).

The `handle` method is called when the job is executed by the queue system. It first chunks the text of the paper into smaller pieces using the `chunkText` method, which splits the text into chunks of up to 500 words each, with an overlap of 100 words between adjacent chunks.

For each chunk, it makes a POST request to the Ollama API (an external service) to get an embedding for the chunk. The response from the API is then inserted into the `paper_embeddings` table in the database as a new row, along with the other properties of the job (e.g., `$projektId`, `$source`, etc.).

If there are any errors during processing, the `fail` method is called to mark the job as failed and log an error message. The `chunkText` method is also used to split the text into chunks for processing.

The class requires the following external dependencies:

* `Illuminate\Support\Facades\DB`: Provides access to the database.
* `Illuminate\Support\Facades\Http`: Provides a simple HTTP client for making requests to external services.
* `Illuminate\Support\Facades\Log`: Provides a logging system for error handling and debugging.

---

### app/Jobs/TriggerLangdockAgent.php
This file is part of a Laravel project. It contains a class named `TriggerLangdockAgent` that implements the `ShouldQueue` and `ShouldBeUnique` interfaces from Laravel. The class is used to trigger the Langdock agent for a specific user, project, and input.

The constructor takes three parameters:

* `$userId`: the ID of the user who owns the project
* `$projektId`: the ID of the project that the agent should be triggered for
* `$eingabe`: the input that will be passed to the Langdock agent

The `handle()` method is where the magic happens. It first retrieves the webhook URL from the database or from a config file, and then makes an HTTP POST request to the URL with the payload containing the user ID, project ID, and input.

If the response status code is not 200 (OK), the method logs an error message and fails the job by throwing a `RuntimeException`. This means that if the job fails for any reason, it will be retried up to 3 times with an exponential backoff of 30 seconds.

The class also defines some properties that are used in the implementation:

* `$tries`: the number of attempts to make before failing the job
* `$backoff`: the amount of time to wait between attempts
* `$uniqueFor`: the length of time for which the job should be considered unique

Overall, this class is responsible for triggering the Langdock agent for a specific user and project, and passing the input to it. It also implements some error handling and retry logic to ensure that the job succeeds or fails gracefully.

---

### app/Livewire/Actions/Logout.php
Die Datei "app/Livewire/Actions/Logout.php" enthält eine Klasse mit einer Methode `__invoke()`, die den aktuellen Benutzer aus dem Anwendungs-System ausloggt und die Sitzung invalidiert. Die Methode verwendet die Laravel-Facades `Auth` und `Session`, um den Benutzer auszuloggen und die Sitzung zu invalidieren. Sie erstellt auch eine neue Sitzungstoken-ID für den Benutzer.

Die Klasse Logout ist ein Teil von Livewire, einem Framework für Echtzeit-Interaktion in der Webentwicklung. Es ist nicht notwendig, dass die Klasse eine bestimmte Bewertung oder Fehlerbehandlung enthält, da sie direkt auf den Auth- und Session-Facades des Laravel-Frameworks zurückgreift.

Die Datei "app/Livewire/Actions/Logout.php" ist von der Datei "app/Http/Controllers/Auth/LoginController.php" aufgerufen, die für das Anmelden von Benutzern zuständig ist. Wenn ein Benutzer sich anmeldet, wird er durch die Methode `__invoke()` der Klasse Logout ausgeloggt und die Sitzung invalidiert.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `App\Livewire\Actions\Logout`: Die Klasse für das Ausloggen von Benutzern.
* `__invoke()`: Die Methode für das Ausloggen von Benutzern.
* `Auth::guard('web')`: Das Facade für die Web-Authentifizierung.
* `Session::invalidate()`: Die Methode zum Invalidieren der Sitzung.
* `Session::regenerateToken()`: Die Methode zum Generieren einer neuen Sitzungstoken-ID.
* `redirect('/')`: Der Redirect zu der Startseite des Anwendungs-Systems nach dem Ausloggen.

---

### app/Mail/ContactConfirmationMail.php
Maximal 10-zeilige Beschreibung der Datei-Funktionen. Diese Datei enthält eine Klasse namens `ContactConfirmationMail`, die von `Mailable` erbt und eine Implementierung für das `ShouldQueue`-Interface besitzt. Sie verwendet den Trait `Queueable` und den Trait `SerializesModels`. Die Klasse hat einen Konstruktor, der einen Parameter `contact` enthält, der ein Objekt vom Typ `Contact` ist. Die Methode `envelope()` erstellt eine neue `Envelope`-Instanz mit einem Betreff für die E-Mail und gibt sie zurück. Die Methode `content()` erstellt eine neue `Content`-Instanz mit einer Ansicht, die in der Datei `emails.contact-confirmation` definiert ist und gibt sie zurück.

---

### app/Mail/ContactInquiryMail.php
This file is a PHP class named `ContactInquiryMail` that extends the `Mailable` class from Laravel and implements the `ShouldQueue` interface. It is used to send an email notification when a new contact inquiry is submitted through the website.

The constructor takes a single argument, which is an instance of the `Contact` model. This model represents a single contact inquiry in the database.

The `envelope()` method returns an instance of the `Envelope` class, which contains information about the email's subject, reply-to address, and other delivery options. In this case, the subject is set to "Neue Kontaktanfrage von {name}", where `{name}` is replaced with the name of the contact inquiry sender. The reply-to address is set to the email address of the contact inquiry sender.

The `content()` method returns an instance of the `Content` class, which contains information about the email's content, such as the view file and any variables that should be passed to the view. In this case, the view file is set to `emails.contact-inquiry`.

This class does not have any external dependencies, other than the Laravel framework itself. It uses the `Queueable` trait to enable queuing of email messages, and the `SerializesModels` trait to serialize models before serializing them into a message payload. The `Contact` model is also used in this class, which is why it must be imported at the top of the file.

---

### app/Mail/DeployNotificationMail.php
Die Datei 'app/Mail/DeployNotificationMail.php' enthält eine Klasse 'DeployNotificationMail', die von der Klasse 'Mailable' erbt und es ermöglicht, E-Mails an Benutzer zu senden, wenn ein neuer Deployment auf dem Server durchgeführt wurde.

---

### app/Policies/ProjektPolicy.php
Die Datei "app/Policies/ProjektPolicy.php" enthält die Definition von Richtlinien (Policy), die es dem Autorisierungsmodul ermöglichen, zu bestimmen, ob ein Benutzer bestimmte Aktionen auf einem Projekt ausführen darf.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `ProjektPolicy`: Die Hauptklasse der Richtlinie, die eine Reihe von Methoden enthält, um zu bestimmen, ob ein Benutzer bestimmte Aktionen auf einem Projekt ausführen darf.
* `viewAny()`: Diese Methode gibt an, ob ein Benutzer alle Projekte im Modell sehen kann. In diesem Fall gibt es keinen Autorisierungsmechanismus und jeder Benutzer kann alle Projekte sehen.
* `view()`: Diese Methode gibt an, ob ein Benutzer ein bestimmtes Projekt sehen darf. Sie überprüft, ob der Benutzer der Besitzer des Projekts ist.
* `create()`: Diese Methode gibt an, ob ein Benutzer ein neues Projekt erstellen darf. In diesem Fall gibt es keinen Autorisierungsmechanismus und jeder Benutzer kann ein neues Projekt erstellen.
* `update()`: Diese Methode gibt an, ob ein Benutzer ein bestimmtes Projekt aktualisieren darf. Sie überprüft, ob der Benutzer der Besitzer des Projekts ist.
* `delete()`: Diese Methode gibt an, ob ein Benutzer ein bestimmtes Projekt löschen darf. Sie überprüft, ob der Benutzer der Besitzer des Projekts ist.

Es ist wichtig zu beachten, dass diese Richtlinie nur die Basisauthentifizierung und keine weitere Autorisierung durchführt, d.h., es gibt keinen Mechanismus, um zu bestimmen, ob ein Benutzer eine bestimmte Aktion auf einem Projekt ausführen darf, wie z.B. das Hinzufügen von Kommentaren oder dem Projektmitgliedern. Es ist auch wichtig zu beachten, dass diese Richtlinie nur für Projekte gilt und nicht für andere Modelle, die in der Anwendung verwendet werden können.

---

### app/Providers/AppServiceProvider.php
Die Datei `AppServiceProvider.php` ist Teil des Laravel-Frameworks und enthält die Einstellungen für die Anwendung. Hier sind einige wichtige Informationen, die in dieser Datei enthalten sind:

* Die Klasse `AppServiceProvider` erweitert die Basisklasse `Illuminate\Support\ServiceProvider`. Diese Klasse ist verantwortlich für die Registrierung von Diensten und Einstellungen, die für die Anwendung erforderlich sind.
* In der Methode `register()` werden keine Dienste registriert, da diese Datei nur für die Einstellungen der Anwendung verwendet wird.
* In der Methode `boot()` werden einige Einstellungen für die Anwendung konfiguriert:
	+ Die Klasse `Gate` wird mit der Policypolicy für das Modell `Projekt` verbunden, um die Autorisierung von Aktionen auf diesem Modell zu kontrollieren.
	+ Für den Rate Limitierer werden zwei Endpunkte konfiguriert: "mcp" und "webhooks". Der Rate Limiter ist verantwortlich für die Einschränkung der Anzahl von Anfragen, die pro Minute von einem Benutzer oder einer IP-Adresse gesendet werden können.
	+ Wenn die Anwendung in der Produktionsumgebung läuft (gemäß dem Testergebnis `$this->app->isProduction()`), wird die URL automatisch auf "https" geändert, um das Senden von Daten über HTTP zu verhindern.

Insgesamt ist diese Datei eine wichtige Konfigurationsdatei für die Anwendung und enthält Einstellungen, die für die Verarbeitung von Anfragen notwendig sind.

---

### app/Providers/FortifyServiceProvider.php
This file is a part of the Laravel framework, specifically in the `app/Providers` directory. It contains a service provider class called `FortifyServiceProvider`, which is used to configure the behavior of the Fortify authentication system.

The `FortifyServiceProvider` class extends the `Illuminate\Support\ServiceProvider` class and overrides its `register()` and `boot()` methods. In the `register()` method, it configures the actions used by Fortify for creating new users and resetting user passwords. It also imports the `App\Actions\Fortify\CreateNewUser` and `App\Actions\Fortify\ResetUserPassword` classes, which define these actions.

In the `boot()` method, it configures the views used by Fortify for various authentication tasks such as login, verification of email addresses, two-factor authentication, password resetting, and confirming passwords. It also configures the rate limiting for these tasks using the `RateLimiter` class.

The `configureActions()` method is responsible for configuring the actions used by Fortify for creating new users and resetting user passwords. The `configureViews()` method is responsible for configuring the views used by Fortify for various authentication tasks. The `configureRateLimiting()` method is responsible for configuring the rate limiting for these tasks using the `RateLimiter` class.

The external dependencies in this file include the `Illuminate\Support\Facades\RateLimiter`, which provides a convenient way to manage rate limits, and the `Laravel\Fortify\Fortify` facade, which provides access to various methods for configuring Fortify authentication.

---

### app/Providers/VoltServiceProvider.php
Diese Datei ist Teil von Laravel und stellt einen ServiceProvider für das Livewire-Framework bereit. Der Provider wird im bootstrap-Prozess von Laravel aufgerufen und registriert sich bei Livewire, um es mit der Anwendung zu verknüpfen. Es konfiguriert auch den Pfad zu den Livewire-Ansichten.

Die wichtigsten Klassen und Funktionen in dieser Datei sind:

* `VoltServiceProvider`: Dies ist die Hauptklasse des Providers, die sich bei der Anwendung registriert und den Pfad zu den Livewire-Ansichten konfiguriert.
* `mount()`: Diese Funktion wird aufgerufen, um das Verknüpfen von Livewire mit der Anwendung zu initialisieren. Es gibt zwei Argumente an diese Funktion, die den Pfad zu den Livewire-Ansichten und einen optionalen dritten Parameter annehmen, der die Ansichten für die dynamische Seite konfiguriert.
* `config()`: Diese Funktion wird verwendet, um die Konfiguration des Anwendungs-Kontexts abzufragen oder zu ändern. In diesem Fall wird der Pfad zu den Livewire-Ansichten abgerufen oder konfiguriert.

Externe Abhängigkeiten in dieser Datei sind:

* `Illuminate\Support\ServiceProvider`: Die Klasse "ServiceProvider" ist von Laravel und stellt eine Basisklasse für die Implementierung von ServiceProviders bereit.
* `Livewire\Volt\Volt`: Diese Klasse ist Teil von Livewire und stellt eine API zum Einbetten von Livewire-Ansichten in Laravel-Templates bereit.

---

### app/Providers/Filament/AdminPanelProvider.php
This is a PHP class file for the `AdminPanelProvider` class in the Filament package. The class provides a panel that serves as an administration dashboard for the application.

The `panel()` method is overridden from the parent class and sets up the panel with default settings. The panel is assigned an ID of "admin" and has a path of "/admin". It also specifies the login page and enables resource discovery for resources in the `app/Filament/Resources` directory.

The `middleware()` method is used to specify the middleware stack that should be applied to all routes within this panel. The middleware includes several Filament-specific middleware classes, as well as some Laravel-provided middleware classes. These middleware classes include:

* `EncryptCookies`: Encrypts cookies sent back and forth between the client and server.
* `AddQueuedCookiesToResponse`: Adds queued cookies to the response.
* `StartSession`: Starts a session for the user.
* `AuthenticateSession`: Authenticates the user's session.
* `ShareErrorsFromSession`: Shares errors from the session with the view.
* `VerifyCsrfToken`: Verifies the CSRF token sent in the request to prevent cross-site request forgery attacks.
* `SubstituteBindings`: Substitutes bindings for Eloquent models.
* `DisableBladeIconComponents`: Disables Blade icon components.
* `DispatchServingFilamentEvent`: Dispatches a serving Filament event.

The `authMiddleware()` method is used to specify the authentication middleware that should be applied to all routes within this panel. The authentication middleware includes the `Authenticate` class, which handles user authentication and authorization.

---

### bootstrap/app.php
This file is the main entry point for the Laravel application. It defines the root directory of the project and sets up the routing, middleware, and exception handling for the application.

Here's a summary of the important parts of the code:

* The `Application` class is imported from the `Illuminate\Foundation` namespace, which provides an object-oriented interface for building Laravel applications.
* The `configure()` method sets up the root directory of the project to be the parent directory of the current file (`dirname(__DIR__)`).
* The `withRouting()` method defines the routing configuration for the application. It specifies the paths to the web, API, and console routes files.
* The `withMiddleware()` method sets up middleware for the application. It specifies that all incoming requests should be considered stateful and that the `X-Forwarded-For` and `X-Forwarded-Proto` headers should be trusted. Additionally, it appends the `TrackPageView` class to the `web` group middleware.
* The `withExceptions()` method sets up exception handling for the application. It is currently empty, but can be used to customize the error handling behavior of the application.
* Finally, the `create()` method creates an instance of the `Application` object and returns it. This is the main entry point for the Laravel application and provides access to all the configuration and functionality defined in this file.

---

### bootstrap/providers.php
Diese Datei enthält eine Liste von Provider-Klassen für die Anwendung. Es handelt sich um PHP-Dateien, die den Code bereitstellen, der zum Registrieren von Paketen, Services und anderen Komponenten in Laravel verwendet wird.

---

### docker/common/nginx/default.conf
This is an Nginx configuration file that sets up a reverse proxy for a Laravel application running on `php-fpm`. It listens on port 80 and serves requests from the `/` path to the `index.php` script in the root directory of the document tree.

The main location block includes several directives:

* `try_files $uri $uri/ /index.php?$query_string`: This directive tries to find a file or directory that matches the requested URI, and if it doesn't exist, it passes the request to the `/index.php` script with the query string attached.
* `fastcgi_pass php-fpm:9000`: This directive specifies the FastCGI backend for PHP processing. It uses the hostname `php-fpm` and port 9000 as the address of the backend server.
* `include fastcgi_params`: This directive includes the `fastcgi_params` file from the Nginx configuration directory, which contains a set of predefined variables that are used by the FastCGI protocol.
* `fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name`: This directive sets the `SCRIPT_FILENAME` variable to the path of the PHP script being executed. It uses `$document_root`, which is set by Nginx, to specify the root directory for the document tree. The `$fastcgi_script_name` variable contains the name of the PHP script being requested.
* `fastcgi_param PATH_INFO $fastcgi_path_info`: This directive sets the `PATH_INFO` variable to the path of the resource being requested, relative to the directory where the PHP script is located.
* `fastcgi_read_timeout 120; fastcgi_send_timeout 120`: These directives set the read and send timeouts for FastCGI requests to 120 seconds.

The file also includes several location blocks that configure reverse proxying for certain endpoints:

* `/mcp/`, `/paper-mcp/`, and `/ollama/`: These locations are used by the MCP (Multi-Channel Platform) system, which is a chat platform used by the game. The `resolver` directive is used to enable DNS resolution of service names in the container. The `proxy_pass` directive specifies the upstream server for the proxy request.
* `/messages/`: This location is used by the MCP system for message handling. It uses the same `proxy_pass` directive as the previous location block, but with a different upstream server (`postgres-mcp`) and path prefix (`/messages/`).
* `/ollama/`: This location is used by the Ollama embedding system, which is used to embed video content in the game. It uses the same `proxy_pass` directive as the previous location block, but with a different upstream server (`ollama`) and path prefix (`/ollama/`).

Finally, the file includes several directives that control access to hidden files and sensitive files:

* `deny all; access_log off; log_not_found off`: These directives deny access to any files or directories that start with a dot (`.`), which are typically considered hidden files. The `access_log` and `log_not_found` directives disable logging for these requests, as they do not contain useful information.
* `deny all;`: This directive denies access to the `.env`, `.git`, `.gitignore`, and `.htaccess` files, which are typically considered sensitive files that should not be accessed directly by users.

---

### docker/common/nginx/docker-entrypoint.sh
The file `docker-entrypoint.sh` is the entry point for an Nginx container. It is executed when the container starts up, and it performs several tasks:

1. It checks if a template file exists at `/etc/nginx/templates/default.conf.template`. If it does, it copies that file to `/etc/nginx/conf.d/default.conf`, which is where Nginx looks for its configuration files by default.
2. It replaces the `__MCP_AUTH_TOKEN__` placeholder in the `default.conf` file with the value of the `$MCP_AUTH_TOKEN` environment variable, if it is set. If the variable is not set, it logs a warning message.
3. It runs the command specified as the last argument to the entry point, which defaults to running Nginx with the configuration files in `/etc/nginx`.

This script assumes that an Nginx configuration template file exists at `/etc/nginx/templates/default.conf.template`, and it generates a configuration file for Nginx based on that template. The generated configuration file is then used by Nginx to serve requests. The `MCP_AUTH_TOKEN` environment variable is used to configure authentication with the MCP (Microservices Container Platform) API.

---

### docker/common/nginx/dockerfile
This Dockerfile sets up an Nginx container based on the `nginx:alpine` image. It copies several configuration files and scripts into the container, which are used to customize the Nginx configuration and set up a secure HTTPS connection using Let's Encrypt certificates. The container also exposes port 80 for HTTP traffic and starts the Nginx web server with the `-g` option to run in the foreground.

Here is a summary of the main features of this Dockerfile:

* It uses the `nginx:alpine` image as its base, which is a slimmed-down version of the official Nginx image that only includes the essential components and reduces the attack surface.
* It copies several configuration files into the container to customize the Nginx configuration, including a custom `nginx.conf` file and a `default.conf` file that defines the site configuration.
* It also copies an entrypoint script (`docker-entrypoint.sh`) into the container that is used to inject secrets at runtime using environment variables. This script is responsible for setting up the Let's Encrypt certificates and generating self-signed certificates if necessary.
* It copies only static public files (such as `robots.txt`, `favicon`, `index.php`, and images) into the container from the shared Docker volume at runtime, while building assets are generated on demand using a separate build process.
* The container exposes port 80 for HTTP traffic and starts the Nginx web server with the `-g` option to run in the foreground.

---

### docker/common/nginx/nginx.conf
This is a basic Nginx configuration file for a Laravel application, which serves as the reverse proxy for the application's API endpoints. The following are some of the key features and functionalities that this file provides:

1. Trusting X-Forwarded headers from Docker and upstream proxies: This is necessary because Nginx is running behind a reverse proxy in this case, and it needs to trust the IP addresses of the proxy servers in order to determine the client's IP address correctly.
2. Enabling gzip compression: This reduces the size of the response body and improves page load times.
3. Setting the maximum body size for file uploads: This prevents Nginx from consuming too much memory when handling large files.
4. Including other configuration files in the `conf.d` directory: This allows you to have multiple configuration files for different servers or APIs without having a single, monolithic configuration file.

Overall, this file provides a basic setup for an Nginx server that can be used with Laravel applications and is suitable for most use cases.

---

### docker/common/php-fpm/dockerfile
This Dockerfile defines a multi-stage build for a PHP application. The first stage, `builder`, is used to set up the environment and install all necessary dependencies. It uses the `php:8.4-fpm` base image and installs system dependencies, PHP extensions, Composer, and Node.js.

The second stage, `production`, copies the built assets from the first stage and sets up the production environment by installing runtime libraries required for PHP extensions and configuring the default production PHP configuration. It uses the `php:8.4-fpm` base image as well.

The third stage, `testing`, is used to set up a testing environment that includes all dependencies, including development and testing dependencies. It copies the full source code with tests and development config, installs all dependencies, and sets up permissions for the `www-data` user.

The Dockerfile defines an entrypoint script (`/entrypoint.sh`) that runs as root and then drops to the `www-data` user. This is necessary for setting up the correct file ownership and permissions. The default command for the container is `php-fpm`.

---

### docker/common/php-fpm/entrypoint.sh
This is an entrypoint script for a Docker container that runs PHP-FPM. It sets up some basic functionality, such as synchronizing built frontend assets to a shared volume and waiting for the database to be ready before starting the main process. The script also checks the environment variable `APP_ENV` to determine whether or not to run migrations in production/staging mode. Finally, it executes the main command (php-fpm or queue:work) passed as an argument when the container starts.

---

### docker/common/postgres/dockerfile
This Dockerfile is building a PostgreSQL 16 image using the official `postgres:16` base image. It installs two additional modules, `postgresql-16-hypopg` and `postgresql-16-pgvector`, which are not part of the standard PostgreSQL distribution but provide additional features for hyperscale and vectorized query processing. The installation process uses `apt-get`, so it may be necessary to install some dependencies before building the image. The `RUN` instruction is used to execute a shell command, in this case, updating the package list with `apt-get update` and installing the additional modules using `apt-get install`. Finally, the `rm -rf /var/lib/apt/lists/*` command is used to remove the downloaded packages and save space in the final image.

---

### docker/common/postgres/init-extensions.sql
This SQL file initializes PostgreSQL extensions on a Docker container. It creates the hypopg, pg_stat_statements, and vector extensions if they do not already exist.

---

### public/index.php
This is a PHP file that sets up a Laravel application and handles incoming HTTP requests. Here are the important parts of the code:

* `use Illuminate\Foundation\Application;` - This line imports the Application class from the Laravel framework, which is used to bootstrap the application.
* `use Illuminate\Http\Request;` - This line imports the Request class from the Laravel framework, which is used to handle incoming HTTP requests.
* `define('LARAVEL_START', microtime(true));` - This line defines a constant called LARAVEL_START that holds the current time in seconds since the Unix epoch. This constant is used to measure the execution time of the application.
* `if (file_exists($maintenance = __DIR__.'/../storage/framework/maintenance.php')) {` - This line checks if a file named maintenance.php exists in the storage/framework directory. If it does, the code inside the if statement is executed.
* `require $maintenance;` - This line requires the maintenance.php file to be included in the script.
* `// Register the Composer autoloader...` - This line registers the Composer autoloader, which is used to load classes and interfaces defined in the Composer package manager.
* `require __DIR__.'/../vendor/autoload.php';` - This line requires the autoload.php file from the vendor directory, which is generated by Composer during installation.
* `$app = require_once __DIR__.'/../bootstrap/app.php';` - This line requires the app.php file from the bootstrap directory, which is used to bootstrap the Laravel application. The `require_once` function is used to ensure that the file is only included once in the script.
* `$app->handleRequest(Request::capture());` - This line handles incoming HTTP requests by creating a new Request object and passing it to the handleRequest method of the Application class. The capture() method is used to capture the current request from the global $_SERVER variable.

---

### public/robots.txt
Die Datei 'public/robots.txt' enthält Informationen zur Roboter-Verhaltensweise für das Web-Anwendungsprogramm. Es handelt sich um eine Standarddatei, die von verschiedenen Suchmaschinen und Web-Crawlern verwendet wird, um zu erfahren, welche Ressourcen des Web-App-Programms nicht durch die Suchmaschine gesucht werden dürfen.

---

### public/.htaccess
This is an Apache HTTP server configuration file, stored in the `public/.htaccess` location. The file defines some rewrite rules for handling requests and managing security headers.

Here are the important functions and endpoints:

* RewriteEngine: enables rewriting of URLs.
* RewriteCond: defines conditions that need to be met in order for the next rule to be applied.
* RewriteRule: defines a target URL or action to be taken when the conditions are met.
* Handle Authorization Header: checks if the `Authorization` header is set, and if so, stores it in an environment variable (`HTTP_AUTHORIZATION`) that can be accessed by PHP scripts.
* Handle X-XSRF-Token Header: checks if the `x-xsrf-token` header is set, and if so, stores it in an environment variable (`HTTP_X_XSRF_TOKEN`) that can be accessed by PHP scripts.
* Redirect Trailing Slashes If Not A Folder: redirects any URL with a trailing slash to the same URL without the slash, unless the requested file or folder exists.
* Send Requests To Front Controller: sends all requests to the `index.php` script.

This `.htaccess` file is likely used in a Laravel application, as it defines some rewrite rules that are commonly used in such applications. The presence of the `mod_rewrite` and `mod_negotiation` modules suggests that this is an Apache HTTP server configuration file, and not a Nginx configuration file.

The external dependencies for this `.htaccess` file are the `mod_rewrite` and `mod_negotiation` modules, which provide the functionality needed to define rewrite rules and handle security headers. These modules are typically included in the default installation of Apache HTTP server.

---

### resources/css/app.css
The file resources/css/app.css is a CSS stylesheet for the Laravel framework. It defines a custom theme variant called "dark" and a base layer that sets the border color of various elements to a grayish-white color. Additionally, it imports some other resources, including tailwindcss and the views directory.

Here are the key points about this file:

* The file is named app.css and is located in the resources/css directory of the Laravel project.
* It defines a custom theme variant called "dark" that sets the border color of various elements to a grayish-white color.
* It imports some other resources, including tailwindcss and the views directory.
* The file uses the `@import` directive to import the `tailwindcss` library and the `../views` directory.
* The file uses the `@source` directive to import additional Blade templates from the `../../vendor/laravel/framework/src/Illuminate/Pagination/resources/views` directory.
* The file defines a base layer called "base" that sets the border color of various elements to a grayish-white color using the `@layer` directive.
* The file uses the `@custom-variant` directive to define a custom variant called "dark" that sets the border color of various elements to a grayish-white color.
* The file uses the `@theme` directive to define some theme variables, including `--font-sans`, `--color-zinc-50`, and others.
* The file uses the `@layer` directive to define a layer called "base" that sets the border color of various elements to a grayish-white color.
* The file uses the `@custom-variant` directive to define a custom variant called "dark" that sets the border color of various elements to a grayish-white color.

---

### storage/app/public/.gitignore
Diese .gitignore-Datei ignoriert alle Dateien und Verzeichnisse im Storage-Ordner der Anwendung. Dies ermöglicht es, dass die Dateien und Verzeichnisse, die Teil des Projekts sind, nicht versehentlich in das Git-Repository aufgenommen werden.

---

### storage/logs/.gitignore
This file specifies which files and directories in the storage/logs directory should be ignored by Git.

---

### .github/copilot-instructions.md
{
  "file_summary": "Copilot Instructions for app.linn.games — Software Architecture and Workflow"
}

This file provides an overview of the architecture and workflow for the app.linn.games project, with a focus on software development best practices and conventions.

## Language

The primary language used in this project is German. All code and commit messages should be written in English.

## Stack

The following technologies are used in this project:

* Backend: Laravel 12, PHP 8.2+ (8.4 in CI)
* Frontend: Livewire 3 / Volt (inline components), Tailwind CSS 4
* Admin: Filament 4.9 (schema-based)
* Database: PostgreSQL 16 (UUID, pgvector, Custom Enums), Redis (Cache/Session/Queue)
* AI: Langdock Webhook → TriggerLangdockAgent Job, Ollama (Embeddings), Paper-Search MCP
* Auth: Laravel Fortify + 2FA (plain Blade, no Livewire)
* Testing: Pest (no PHPUnit class-style tests), SQLite in-memory
* Linting: Laravel Pint (CI-enforced)

## Deployment & CI

* No automatic deployment — deployment is done manually on the server via `./deploy.sh`
* Tests and lint run through GitHub Actions (`tests.yml`, `lint.yml`)
* No `deploy.yml` or similar automated workflows created
* Docker on Synology NAS: `docker compose up -d`

## Architecture Decisions

### Models

* UUID as primary key for domain models (Webhook, ChatMessage, Project, Phase, …) via `HasUuids` trait
* Standard auto-increment ID for users (Fortify compatibility)
* Custom timestamps: Research models use `erstellt_am`, `letztes_update` instead of `created_at`/`updated_at`
* Activity logging: `Spatie\Activitylog\Models\Concerns\LogsActivity` (NOT `Traits\LogsActivity`)
* Password fields: always `'hashed'` cast, in `$hidden` and `$fillable`

### Volt / Livewire

* Inline components in Blade files: `new class extends Component { ... }`
* Routing via `Volt::route('path', 'component.name')`
* No Alpine.js — only Livewire directives (`wire:model`, `wire:click`, `wire:loading`)
* Redirect with `$this->redirect(route(...), navigate: true)`

### Filament

* Schema-based forms/tables
* DateTimes formatted as `d.m.Y H:i`
* Navigation label in German

### Routing

* Web: Dot-notation (`recherche.projekt`, `profile.edit`)
* API: Explicit POST/GET, no `Route::resource()`
* Middleware: `['auth', 'verified']` for protected routes
* Custom middleware: `VerifyLangdockSignature`, `VerifyMcpToken`

## Database

* PostgreSQL is the production DB — utilize DB features (Enums, pgvector, Extensions)
* Migrations with `return new class extends Migration`
* Pgsql-specific migrations need guard: `if (DB::getDriverName() !== 'pgsql') return;`
* SQLite-compatible test tables in separate migration (`2026_03_31_099999`)
* Foreign keys: `foreignId('user_id')->constrained()->cascadeOnDelete()`
* New enums: `DB::statement("CREATE TYPE ... AS ENUM (...)")` with `DROP TYPE IF EXISTS ... CASCADE` in down()

## Testing

* Pest: `test('description', function () { ... })` syntax
* SQLite in-memory for tests — no PostgreSQL features in tests expect
* User Factory: `User::factory()->withoutTwoFactor()->create()`
* Volt Testing: `Volt::test('component.path')->set('prop', 'val')->call('method')`
* Queue testing: `Queue::fake()` before requests that dispatch jobs (especially with `QUEUE_CONNECTION=sync` in CI)
* Config override: `Config::set('services.langdock.secret', 'test-secret')`
* New features need feature tests, bugfixes need regression tests

## Docker

* 10 services: web, php-fpm, php-cli, php-test, queue-worker, postgres, redis, postgres-mcp, ollama, mcp-paper-search
* `php-test` is a baked image — after code changes outside of `tests/`, `phpunit.xml`, `.env` must run `docker compose build php-test`
* Local tests: `docker compose run --rm php-test vendor/bin/pest`
* Port: nginx on `6481:80`

## Git Conventions

* Branches: `feature/`, `fix/`, `docs/`, `refactor/`
* Flow: `feature/*` → `develop` → `main`
* Squash merge preferred
* Commit messages: Conventional Commits (`feat:`, `fix:`, `docs:`, `refactor:`)
* Migrations in separate commits before code changes

## Workflow

* Document completed tasks thoroughly — document relevant architecture decisions in the Copilot Instructions
* Push changes regularly to GitHub (Conventional Commits, feature branch → develop)
* After each completed task: briefly check commit stand and possibly commit

## Security

* Webhooks: HMAC-SHA256 signature + Timestamp validation (5 minute tolerance) + Cache nonce (replay protection)
* MCP: Bearer token authentication via Nginx proxy
* Ollama: Token authentication via Nginx `/ollama/` location block
* Passwords: `hashed` cast, never log or return in plaintext
```

---

### .github/instructions/app_linn_games_custom.instructions.md
The file `.github/instructions/app_linn_games_custom.instructions.md` is a Markdown file that contains instructions for contributing to the project `app.linn.games`. The file is structured as follows:

1. Project overview: This section provides an overview of the project and its purpose. It explains that the project is a proprietary research management platform for AI-powered systematic literature review.
2. Stack (exact versions): This section lists the exact versions of various software components used in the project, including PHP 8.2+, Laravel 12, Livewire 3, Filament 4.9, Fortify 1.30, Tailwind CSS 4, PostgreSQL 16, and Redis Alpine.
3. Directory structure: This section describes the directory structure of the project, including the directories and files that make up the project. It highlights some of the key directories and files that are relevant to the development process.
4. Data model (critical details): This section provides a detailed description of the data model used in the project. It explains that the project uses UUID primary keys, timestamps, custom enums, and pgvector embeddings. The section also highlights some of the key entities and relationships in the data model.
5. Authentication & authorization: This section provides an overview of the authentication and authorization mechanisms used in the project. It explains that the project uses Laravel Fortify for authentication and authorization, and that certain middleware are used to enforce security policies.
6. KI integration (critical path): This section describes the critical path for integrating AI-powered tools into the project. It explains that the project uses a combination of webhooks and queues to trigger the AI agents and manage the workflow. The section also highlights some of the key components and mechanisms used in the integration process.

Overall, this file provides a comprehensive overview of the `app.linn.games` project, including its purpose, technology stack, directory structure, data model, authentication & authorization, and KI integration.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker Dev Workflow — app.linn.games - Maximale Zusammenfassung der Datei-Funktionen

---
