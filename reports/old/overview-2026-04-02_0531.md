---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T05:31:51.101018
model: llama3.1:8b
mode: overview
files_total: 238
errors: 0
run_time_s: 558.8
---

# Funktions-Übersicht — 2026-04-02_0531

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 238
- **Fehler:** 0
- **Laufzeit:** 558.8s

## api

### app/Http/Controllers/ContactController.php
```
{
  "file_summary": "Diese Datei enthält den Kontakt-Controller für die API. Sie ermöglicht es Nutzern, Anfragen zu senden und diese werden anschließend per E-Mail an die Verwaltung und den Nutzer selbst weitergeleitet.",
  " wichtigste_Klassen": ["ContactController", "Contact", "ContactInquiryMail", "ContactConfirmationMail"],
  "Funktionen/Endpunkte": ["store (POST)"],
  "externe_Abhängigkeiten": ["App\Mail\ContactConfirmationMail", "App\Mail\ContactInquiryMail", "Illuminate\Support\Facades\Mail"]
}
```

---

### app/Http/Controllers/Controller.php
Basisklasse für API-Controller, bietet keine direkten Funktionalitäten

---

### app/Http/Controllers/DsgvoController.php
```json
{
  "file_summary": "Der DsgvoController stellt API-Endpunkte für die Datenexportierung und das Konto löschen bereit.\n\
Wichtige Funktionen:\n\
- export: Daten von einem Nutzer in einer JSON-Datei exportieren\n\
- deleteAccount: Konto und alle verknüpften Daten eines Nutzers löschen\n\
\
Externe Abhängigkeiten:\n\
- Illuminate\Http\Request\n\
- Illuminate\Support\Facades\Auth"
}
```

---

### app/Http/Controllers/LangdockWebhookController.php
```json
{
  "file_summary": "Webhook-Controller für Langdock-Agents; validiert Anfragen und dispatcht Trigger-Jobs.",
  "main_classes": ["LangdockWebhookController"],
  "important_functions": ["handle", "TriggerLangdockAgent::dispatch"],
  "external_dependencies": [
    "App\Jobs\TriggerLangdockAgent",
    "Illuminate\Http\Request"
  ]
}
```

---

### app/Http/Controllers/PaperRagController.php
```json
{
  "file_summary": "Controller für Paper-Rag-Funktionalität. Zwei öffentliche Methoden: ingest und search. 
                   ingest prüft ein Paper-Objekt, wenn notwendig erstellt oder aktualisiert es in der Datenbank.
                   search macht einen Aufruf an den Embedding-Service, um ähnliche Papers zu finden und 
                   gibt eine Liste von Papers mit Ähnlichkeits-Werten zurück.",
  "klasse": ["PaperRagController"],
  "funktionen": ["ingest", "search"],
  "extern_abhaengigkeiten": ["IngestPaperJob", "DB-Zugriffe", "HTTP-Anfragen an Embedding-Service"]
}
```

---

### routes/api.php
API-Route-Konfiguration für Webhooks und Papierverwaltung mit Authentifizierungsmiddleware.

---

### routes/console.php
```
{
  "file_summary": "Definiert Befehle für Artisan zum Ausführen bestimmter Aktionen.\n\nWichtige Funktionen/Endpunkte:\n- inspire: Zeigt eine Inspirationszitate an.\n- deploy:notify: Sendet eine Deploy-Benachrichtigung per E-Mail.\n\nExterne Abhängigkeiten:\n- App\Mail\DeployNotificationMail\n- Illuminate\Foundation\Inspiring\n- Illuminate\Support\Facades\Artisan\n- Illuminate\Support\Facades\Mail"
}
```

---

### routes/web.php
```
{
  "file_summary": "Die Datei routes/web.php definiert Routen für die Web-Application. Es werden folgende Funktionen bereitgestellt:
 
 Routen für die Benutzeroberfläche (z.B. /, Impressum, DSGVO)
 API-Routen für den Kontaktformular-Ablauf und DSGVO-Funktionen
 Mittlerweile müssen sich authentifizierte Benutzer anmelden, um auf bestimmte Routen zuzugreifen
 Routen für das Dashboard und Einstellungen werden bereitgestellt, nachdem der Benutzer angemeldet ist
 
 Die wichtigsten Klassen sind ContactController und DsgvoController. Externe Abhängigkeiten sind nicht vorhanden."
}
```

---

## config

### docker-compose.dev.yml
Konfiguration für Dev-Umgebung, überlagert Docker-Compose-Konfiguration. Ändert den Port von Nginx auf 6480.

---

### docker-compose.yml
```
{
  "file_summary": "Docker-Konfiguration für eine Web-Anwendung\n\
Die Datei konfiguriert Docker-Container für eine Web-Anwendung.\n\
Es werden fünf Dienste definiert: web, php-fpm, php-cli, php-test und queue-worker.\n\
Jeder Container hat eine spezifische Konfiguration für Build, Volumes, Ports, Umgebungsvariablen und Abhängigkeiten."
}
```

---

### .env.example
Dies ist eine Konfigurationsdatei für ein Laravel-Projekt. Sie enthält Einstellungen für die Anwendung, Datenbank, Sitzung und Cache sowie für externe Dienste wie Redis, Mail und AWS.

---

### config/app.php
Datenbank- und Anwendungskonfiguration für Laravel

---

### config/auth.php
Konfiguration für die Authentifizierung in Laravel
- Ermittlung der Standard-Guard und -Broker für Authentifizierung und Passwort-Restellung
- Definition von Auth-Guards, einschließlich der verwendeten Anbieter (z.B. "session", "database")
- Spezifizierung der User-Anbieter und Modell-Klassen

---

### config/cache.php
```
{
  "file_summary": "Konfiguration für die Cache-Funktionen in einer Laravel-Anwendung. Definiert die Standard-Cache-Speicherung und verschiedene Store-Optionen mit ihren Konfigurationsparametern.",
  "key_classes_functions_endpoints": ["Illuminate\Support\Str"],
  "external_dependencies": [
    {"name": "env", "description": "Zugriff auf Umgebungsvariablen"},
    {"name": "DB_CACHE_CONNECTION", "description": "Verbindung zur Datenbank für die Cache-Tabelle"},
    {"name": "MEMCACHED_PERSISTENT_ID", "description": "Persistente ID für Memcached"},
    {"name": "MEMCACHED_USERNAME", "description": "Benutzername für Memcached"},
    {"name": "MEMCACHED_PASSWORD", "description": "Kennwort für Memcached"}
  ]
}
```

---

### config/cookie-consent.php
Konfiguration für die Zustimmungszuweisung von Cookies. Enthält Einstellungen für den Dialog und das Cookie selbst.

---

### config/database.php
```
{
  "file_summary": "Diese Datei konfiguriert die Datenbankverbindung für ein Laravel-Projekt. Sie ermöglicht es, zwischen verschiedenen Datenbanktypen wie SQLite, MySQL und MariaDB zu wählen.",
  "key_classes_functions_endpoints": [
    "default",
    "connections",
    "sqlite",
    "mysql",
    "mariadb"
  ],
  "external_dependencies": ["Illuminate\Support\Str", "env"]
}
```

---

### config/filesystems.php
Diese Datei konfiguriert die Dateisysteme für das Framework. Die wichtigsten Klassen sind Disk und Link.

---

### config/fortify.php
```
{
  "file_summary": "Config-Datei für Fortify-Einstellungen.\n\
Hauptklasse: Laravel\\Fortify\\Features\n\
Einstellungen für den Auth-Guard, Password-Broker und andere Einstellungen.\n\
Externe Abhängigkeiten: Laravel-Fortify (eingeschränktes Paket)\n"
}
```

---

### config/logging.php
Die Datei config/logging.php konfiguriert die Log-Funktionen der Anwendung. Sie stellt eine Liste von Log-Kanälen bereit, darunter standardmäßige Kanäle wie 'stack', 'single' und 'daily'. Die Konfiguration erfolgt über Umgebungsvariablen, um die Log-Einstellungen flexibel an die spezifischen Bedürfnisse der Anwendung anzupassen. Die Datei verwendet externe Abhängigkeiten wie Monolog für die Log-Funktionen.

---

### config/mail.php
Konfiguration für den E-Mail-Versand in einer Laravel-Anwendung.
Wichtigste Funktionen:
- Auswahl des Standard-E-Mail-Senders (default).
- Konfiguration von E-Mail-Mitgliedern und deren Eigenschaften, wie SMTP-Einstellungen oder die Verwendung von AWS SES.
Externe Abhängigkeiten: .env-Dateien für Umgebungsvariablen wie MAIL_MAILER, MAIL_SCHEME, MAIL_HOST usw.

---

### config/queue.php
Diese Datei definiert Konfigurationen für die Verarbeitung von Aufgaben (Jobs) in einer Laravel-Anwendung. Sie beinhaltet verschiedene Verbindungen zu Job-Warteschläufen, wie z.B. 'sync', 'database', 'beanstalkd', 'sqs' und 'redis'. Die wichtigsten Klassen sind QueueConnection und Job. Externe Abhängigkeiten sind z.B. die Umgebungsvariablen (env), AWS und Redis.

---

### config/services.php
Diese Datei enthält Konfigurationen für Drittanbieter-Dienste wie Mailgun, Postmark, AWS und Slack. Sie speichert die API-Schlüssel, Webhook-URLs und andere Anmeldeinformationen in Umgebungsvariablen.

---

### config/session.php
```
{
  "file_summary": "Diese Datei definiert die Konfiguration für die Sitzungsverwaltung in einer Laravel-Anwendung. Es werden Einstellungen für den Session-Tracker, die Sitzungslaufzeit und die Verschlüsselung der Sitzungsdaten definiert. Die wichtigsten Klassen sind `Illuminate\Support\Str` und es wird auf externe Abhängigkeiten wie Datenbank-Verbindungen und Dateisysteme zugegriffen.",
  "key_classes": ["Illuminate\Support\Str"],
  "main_configurations": [
    {"name": "driver", "description": "Der verwendete Session-Tracker"},
    {"name": "lifetime", "description": "Die maximale Sitzungslaufzeit in Minuten"},
    {"name": "encrypt", "description": "Ob die Sitzungsdaten verschlüsselt werden sollen"}
  ],
  "externals": ["env('SESSION_DRIVER'), env('SESSION_LIFETIME'), env('SESSION_ENCRYPT')"]
}
```

---

### docker/common/php-fpm/php-dev.ini
Diese Datei enthält Konfigurations-Einstellungen für die PHP-FPM-Umgebung, insbesondere für OPcache und Fehler-Ausgabe. Sie wird als zz-dev.ini geladen und kann durch einen Bind-Mount in docker-compose.override.yml aktiviert werden.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Dies ist eine ISSUE_TEMPLATE-Datei für Bug Reports, die zur Erstellung von Fehlermeldungen verwendet wird. Die Datei enthält verschiedene Felder wie Problembeschreibung, Schritte zur Reproduktion und Schweregrad.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Das ist ein Template für neue Features. Es enthält mehrere Textfelder, um das geplante Feature zu beschreiben und die Vorgaben für den Entwickler zu definieren. Die wichtigsten Felder sind 'Zusammenfassung', 'Motivation/Problem', 'Vorgeschlagene Lösung' und 'Akzeptanzkriterien'. Außerdem muss angegeben werden, ob es sich um eine Breaking-Change handelt.

---

### .github/ISSUE_TEMPLATE/refactor.yml
Dieses Template unterstützt die Erstellung von Refactoring-Vorschlägen, ohne funktionsbezogene Änderungen. Es enthält Felder für Beschreibung, betroffene Dateien/Bereiche und Akzeptanzkriterien.

---

### .github/workflows/lint.yml
```json
{
  "file_summary": "Automatisierung von Linter-Abfragen und Code-Sanfierung in CI/CD-Pipeline,
   einschließlich PHP-Einstellung, Composer-Installieren, NPM-Installation und Pint-Ausführung."
}
```

---

### .github/workflows/tests.yml
```
{
  "file_summary": "Diese Datei definiert die Workflow-Konfiguration für automatisierte Tests. 
  Es wird eine CI-Umgebung mit PHP (8.4) und Node.js (22) eingerichtet.
  Die Abhängigkeiten werden installiert, ein Application Key generiert und Assets gebaut.
  Zum Schluss werden die Tests mit Pest durchgeführt.
  Hauptklasse: Workflow
  Hauptschritte: Checkout, Setup PHP/Node, Install Dependencies, Build Assets, Run Tests
}
```

---

## data_access

### app/Models/ChatMessage.php
```json
{
  "file_summary": "Das Modell ChatMessage stellt Datenbank-Operationen für Nachrichten in einem Chat dar. Es bietet Zugriff auf Benutzer und Webhooks, an denen die Nachrichten beteiligt sind. Die wichtigen Klassen, Funktionen oder Endpunkte sind: - ChatMessage (Modell) - user() (Beziehung zum Benutzer) - webhook() (Beziehung zum Webhook)",
  "key_classes": ["ChatMessage", "User", "Webhook"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Consent.php
Consent-Modell der Consent-Daten. Konsentierungseinstellungen eines Nutzers speichert und verarbeitet.

---

### app/Models/Contact.php
```
{
  "file_summary": "Definiert die Struktur für ein Kontaktmodell. 
    Die Klasse erbt von der Eloquent-Model-Klasse (Illuminate\Database\Eloquent\Model).
    Definiert die Felder, die in der Datenbank eingebunden werden dürfen ($fillable)."
}
```

---

### app/Models/PageView.php
Die Datei PageView.php definiert eine Eloquent-Model-Klasse für die Verwaltung von Seitenaufrufen. Sie enthält die speicherbaren Eigenschaften path, ip_anonymous, user_agent und visits.

---

### app/Models/User.php
Die Datei app/Models/User.php definiert die User-Model-Klasse, die die Datenbank-Abfragen für Benutzerkonten steuert. Die wichtigsten Funktionen sind: User erstellen und Bearbeiten, Authentifizierung und Autorisierung. Die externe Abhängigkeit ist die Laravel-Datenbank-Eloquent-Bibliothek.

---

### app/Models/Webhook.php
Datenmodell für Webhooks, die von Benutzern konfiguriert und verwendet werden können. Es enthält Methoden zur Abruf-URL-Erstellung und zum Finden eines bestimmten Webhooks.

---

### app/Models/Recherche/P1Komponente.php
Datenmodell für die Tabelle p1_komponenten. Definiert die Beziehung zu Projekt und ermöglicht den Zugriff auf die Daten in der Tabelle.

---

### app/Models/Recherche/P1Kriterium.php
Das Modell P1Kriterium stellt eine Datenbank-Tabelle dar und bietet Zugriff auf Kriterien eines Projekts. Es verfügt über eine Beziehung zu einem Projekt und kann über die Kriteriumstypen, Beschreibung, Begründung und Quellbezug gefiltert werden.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Datenausgabe-Modell für P1-Strukturmodell-Wahl. Enthält Beziehungen zu Projekt und Casts für boolean-Felder.

---

### app/Models/Recherche/P1Warnsignal.php
Datenmodell für die Tabelle p1_warnsignale. Definiert die Relation zu Projekt und stellt Eloquent-Methoden zur Verfügung.

---

### app/Models/Recherche/P2Cluster.php
Eloquent-Modell für die Tabelle p2_cluster. Definiert Beziehungen zu Projekt und castet treffer_schaetzung als Integer.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
```json
{
  "file_summary": "Datenzugriff für Projekt-Komponenten in der Suchfunktion. Es handelt sich um eine Eloquent-Model-Erweiterung für die Tabelle p2_mapping_suchstring_komponenten. Diese Klasse verfügt über Beziehungen zur Entität 'Projekt' und ermöglicht es, Daten aus dieser Tabelle abzurufen.",
  "important_classes": ["P2MappingSuchstringKomponente"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
```json
{
  "file_summary": "Diese Datei stellt das Model für die Tabelle 'p2_review_typ_entscheidung' dar. Sie ermöglicht den Zugriff auf Entscheidungen über Review-Typen in einem Projekt. Das Modell hat eine Beziehung zu dem Projekt und wird durch den UUID-Standard unterstützt.",
  "important_classes": ["P2ReviewTypEntscheidung"],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Concerns\HasUuids",
    "Illuminate\Database\Eloquent\Model",
    "Projekt"
  ]
}
```

---

### app/Models/Recherche/P2Trefferliste.php
Modell für die Tabelle 'p2_trefferlisten' - dient zur Datenbank-Abfrage und -Auswertung von Treffern in Projekten.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Repräsentiert die Datenbankmatrix für das Projekt P3 und stellt einen Bezug zwischen Projekten und Datenbanken her. Es enthält die Disziplinen, Abdeckung, Besonderheit, Zugang und Empfehlung für jede Datenbank. Die Matrix ist eine Tabelle im Datenbank-Modell p3_datenbankmatrix.

---

### app/Models/Recherche/P3Disziplin.php
Die Datei P3Disziplin.php definiert eine Eloquent-Model-Klasse für die Tabelle p3_disziplinen. Sie stellt eine Beziehung zur Projekt-Tabelle her und ermöglicht den Zugriff auf die Daten dieser Tabelle.

---

### app/Models/Recherche/P3GeografischerFilter.php
```
{
  "file_summary": "Dies ist eine Datenbank-Abstraktion für den P3-GEO-FILTER-Tabelle. 
    Die Tabelle wird über das Projekt Modell geerbelt und verfügt über Filter-Eigenschaften wie Land, Sensitivität und Validierung.",
  "main_classes": ["P3GeografischerFilter"],
  "important_functions": ["projekt"],
  "externale_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P3GraueLiteratur.php
Definiert das Modell für die Tabelle "p3_graue_literatur" und verfügt über Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
```
{
  "file_summary": "Definiert Eloquent-Modell für die Tabelle \"p4_anpassungsprotokoll\" mit UUID und Beziehungen zu P4Suchstring.",
  "main_classes": ["P4Anpassungsprotokoll"],
  "key_functions": ["suchstring"],
  "external_dependencies": [
    {"class": "Illuminate\Database\Eloquent\Concerns\HasUuids"},
    {"class": "Illuminate\Database\Eloquent\Model"}
  ]
}
```

---

### app/Models/Recherche/P4Suchstring.php
```
{
  "file_summary": "Modell für die Datenbank-Tabelle p4_suchstrings, das Such-Strings und ihre zugehörigen Projekte, Anpassungsprotokolle und Suchprotokolle verwaltet. Die Tabelle enthält Informationen wie Suchdaten, Einschränkungen und Trefferzahlen.",
  "key_classes": ["P4Suchstring", "Projekt", "P4Anpassungsprotokoll", "P8Suchprotokoll"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo", "Illuminate\Database\Eloquent\Relations\HasMany"]
}
```

---

### app/Models/Recherche/P4ThesaurusMapping.php
```json
{
  "file_summary": "Model für Datenbank-Tabelle p4_thesaurus_mapping. Dient der Umwandlung von Projekt-Daten in Thesauri-Begriffe.",
  "important_classes": ["P4ThesaurusMapping"],
  "important_methods": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P5PrismaZahlen.php
```json
{
  "file_summary": "Ein Prisma-Modell für die Tabelle p5_prisma_zahlen. Hier werden Datenbank-Treffer und -Fehler gezählt.\n\
Die wichtigsten Klassen sind P5PrismaZahlen mit einem BelongsTo-Verhältnis zum Projekt.\n\
Es wird eine Eloquent-Model-Datenbankverbindung verwendet und keine externe Abhängigkeit ist vorhanden."
}
```

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
```
{
  "file_summary": "Datenmodell für Screening-Entscheidungen: Beziehungen zu Treffern und Felder zur Aufzeichnung der Entscheidungsprozesse",
  "key_classes": ["P5ScreeningEntscheidung"],
  "important_methods": ["treffer", "belongsTo"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P5ScreeningKriterium.php
Die Datei P5ScreeningKriterium.php stellt ein Eloquent-Modell für die Tabelle p5_screening_kriterien dar. Es enthält Beziehungen zu Projekt und definiert Spalten wie projekt_id, level und kriterium_typ.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Diese Datei stellt eine Datenbank-Tabelle "p5_tool_entscheidung" dar und definiert die Beziehungen zu einer Tabelle "projekte". Sie bietet Eloquent-Beziehungen zum Lesen von Projekt-Daten.

---

### app/Models/Recherche/P5Treffer.php
```json
{
  "file_summary": "Die Datei P5Treffer.php definiert eine Eloquent-Model-Klasse für die Datenbanktabelle p5_treffer. Sie ermöglicht das Arbeiten mit Treffern von Suchanfragen und beinhaltet Beziehungen zu anderen Tabellen wie Projekt, Duplikate und Entscheidungen.",
  "key_classes": ["P5Treffer"],
  "important_functions": ["projekt", "duplikatOriginal", "duplikate", "screeningEntscheidungen", "qualitaetsbewertung", "datenextraktion"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P6Luckenanalyse.php
Diese Datei stellt das Datenmodell für die P6-Lückenanalyse dar und definiert die Beziehung zur Projekt-Tabelle. Sie ist Teil des data_access-Kategories und importiert Eloquent-Beziehungen von Illuminate.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Eloquent-Model für P6Qualitaetsbewertung mit Beziehung zu P5Treffer und Datenbankzugriff

---

### app/Models/Recherche/P7Datenextraktion.php
Ermöglicht die Datenextraktion aus der Tabelle p7_datenextraktion. Definiert Beziehungen zu P5Treffer und kapselt den Zugriff auf diese Tabelle.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Die Datei P7GradeEinschaetzung.php definiert ein Model für die Datenbanktabelle 'p7_grade_einschaetzung'. Es bietet Zugriff auf Projekt-Daten und ermöglicht die Zusammenfassung von Graden und Einschätzungen.

---

### app/Models/Recherche/P7MusterKonsistenz.php
```json
{
  "file_summary": "Die Datei definiert eine Eloquent-Model-Klasse für die Tabelle p7_muster_konsistenz. Sie ermöglicht das Laden von Daten und bietet Beziehungen zu der Projekt-Tabelle.",
  "main_classes": ["P7MusterKonsistenz"],
  "main_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P7SyntheseMethode.php
Das Modell für die Tabelle "p7_synthese_methode" beinhaltet Funktionen zur Abfrage und Bearbeitung von Projekt-Synthesemethoden. Es enthält eine Beziehung zu einem Projekt. Externe Abhängigkeit: Projekt-Modell.

---

### app/Models/Recherche/P8Limitation.php
Eloquent-Model für Datenzugriff auf Tabelle p8_limitationen. Definiert die Beziehung zu Projekt und ermöglicht das Einfügen, Bearbeiten und Löschen von Einträgen in dieser Tabelle.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Modell für die Tabelle 'p8_reproduzierbarkeitspruefung' in der Datenbank. Die Tabelle enthält Informationen zu Reproduzierbarkeitsprüfungen von Projekten. Es besteht eine Beziehung zwischen dieser Tabelle und der Projekt-Tabelle. Externe Abhängigkeiten: Projekt-Modell.

---

### app/Models/Recherche/P8Suchprotokoll.php
```json
{
  "file_summary": "Diese Datei implementiert die Eloquent-Model-Klasse für das Suchprotokoll-Tabelle 'p8_suchprotokoll'. 
                  Es enthält Beziehungen zur Suchstring-Tabelle und definiert Felder wie Suchdatum, Gesetzte Filter usw.
                  Die wichtigsten Klassen sind P8Suchprotokoll und P4Suchstring.
                  Externe Abhängigkeiten: Eloquent-Model-Klasse von Laravel."
}
```

---

### app/Models/Recherche/P8UpdatePlan.php
```
{
  "file_summary": "Die Datei P8UpdatePlan.php definiert eine Eloquent-Model-Klasse für die Tabelle p8_update_plan. Sie ermöglicht das Verwalten von Update-Plänen für Projekte und enthält Beziehungen zur Projekt-Tabelle.",
  "key_classes": ["P8UpdatePlan"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/PaperEmbedding.php
```json
{
  "file_summary": "Diese Datei stellt das Modell für Papier-Embeddings dar, die Datenbanktabelle \"paper_embeddings\" repräsentieren. Sie bezieht sich auf Projekte und bietet eine Möglichkeit, die Metadaten der Embeddings als Array zu casten.",
  "main_classes": ["PaperEmbedding"],
  "important_functions": ["projekt", "projekt()"],
  "externals": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/Phase.php
```json
{
  "file_summary": "Diese Datei stellt den Eloquent-Model-Klassentyp \"Phase\" dar, der Daten aus der Tabelle \"phasen\" in der Datenbank abruft und verwaltet. Sie enthält Beziehungen zu anderen Modellen (Projekt) und besitzt Castings für bestimmte Spalten.",
  "important_classes": ["Phase", "Projekt"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/Projekt.php
Das Model Projekt stellt eine Datenbanktabelle "projekte" dar und enthält Beziehungen zu anderen Modellen. Es verwaltet Projekte mit ihren zugehörigen Phasen, Komponenten, Kriterien und Warnsignalen.

---

### database/factories/UserFactory.php
```
{
  "file_summary": "Erstellt Fakes für die Tabelle Users, einschließlich Namen, E-Mails, Passwörtern und weiterer Authentifizierungsdetails.",
  "key_classes": ["UserFactory"],
  "key_functions": ["definition", "unverified", "withoutTwoFactor"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Factories\Factory", "Illuminate\Support\Facades\Hash", "Illuminate\Support\Str"]
}
```

---

### database/factories/Recherche/ProjektFactory.php
Die ProjektFactory-Datei ist Teil der Datenbank-Fabrik und dient dazu, Projekte für Testzwecke zu erzeugen. Sie erstellt ein Projekt mit einem zugehörigen Benutzer und generiert ein Titel und eine Forschungsfrage auf Basis von Falschdaten.

---

### database/migrations/0001_01_01_000000_create_users_table.php
```
{
  "file_summary": "Dieses Skript definiert eine Migrationsdatei für die Datenbank, die drei Tabelle erstellt: \"users\", \"password_reset_tokens\" und \"sessions\". Es wird verwendet, um die grundlegenden Benutzer- und Sitzungsdaten in der Datenbank zu speichern.",
  "key_classes": ["Migration"],
  "key_functions": ["up", "down"],
  "extern_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Erstellt die Datenbank-Tabelle für den Cache und die dazugehörige Tabelle für Cache-Locks.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Diese Datei definiert Migrationen für die Tabelle "jobs" und deren zugehörige Tabellen. Sie erstellt drei Tabellen: "jobs", "job_batches" und "failed_jobs" in der Datenbank. Die Migration wird durchgeführt, wenn die Funktion up() aufgerufen wird und rückgängig gemacht, wenn down() aufgerufen wird.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
```
{
  "file_summary": "Migrationsdatei zur Einführung zweistufiger Authentifizierung für Benutzer. 
  Fügt drei neue Spalten ('two_factor_secret', 'two_factor_recovery_codes', 'two_factor_confirmed_at') zum \"users\"-Table hinzu.
  Funktioniert mit den Klassen 'Migration' und 'Blueprint'."
}
```

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Migration zur Erstellung der Tabelle "contacts" im Datenbank-Schema. Enthält Felder für Kontaktdaten und Projektinformationen.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Migration, um eine Datenbank-Tabelle für Seite-Ansichten zu erstellen. Die Tabelle enthält einziges ID und Anzahl von Besuchen.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Migration zum Erstellen von Tabellen für Benutzerrechte und Rollen sowie deren Beziehungen. Die wichtigsten Klassen sind Migration und Blueprint. Externe Abhängigkeiten beinhalten die Einstellungen für Benutzerrechte (config/permission.php).

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Migration zum Hinzufügen von Spalten zur Tabelle page_views in der Datenbank: path, ip_anonymous und user_agent. Kann umgekehrt auch Spalten löschen.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Die Datei erstellt eine Tabelle für Einwilligungen in der Datenbank mit Feldern wie UserID, Consent-Kategorien und Zeitstempeln. Sie bietet Einarbeitung von Fällen und Entfernung von Einträgen.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
```
{
  "file_summary": "Migrationsdatei für SQLite-Datenbank, erstellt Recherche-Tabellen.",
  "key_classes": ["Illuminate\Database\Migrations\Migration"],
  "main_functions": [
    "up",
    "Schema::create",
    "Schema::table"
  ],
  "external_dependencies": ["Illuminate\Database\Schema", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Dieser Migrations-Script erstellt Postgres-SQL-Ausnahmen und -Typen für ein Projekt zur Recherche-Erweiterung. Es wird eine UUID-OSSP- und PG-Trgm-Ausnahme erstellt, sowie mehrere Enum-Typen wie 'phase_status', 'review_typ' usw., die in der Datenbank verwendet werden können.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
```
{
  "file_summary": "Erstellt die Datenbank-Tabellen 'projekte' und 'phasen'. Die Tabelle 'projekte' enthält Felder wie Titel, Forschungsfrage, Verantwortlicher und Startdatum. Die Tabelle 'phasen' enthält Felder wie Phase-Nr., Titel, Notizen und Abgeschlossen-am-Datum. Beide Tabellen haben Fremdschlüsselbeziehungen zur 'users'-Tabelle.",
  "key_classes": ["Migration", "Blueprint"],
  "important_functions": ["Schema::create()", "Schema::hasTable()", "DB::statement()"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
Diese Datei erstellt Tabellen in der Datenbank für ein Strukturmodell. Es handelt sich um die Tabelle 'p1_strukturmodell_wahl' mit Spalten wie 'id', 'projekt_id', 'gewaehlt', und 'begruendung'. Außerdem werden die Tabellen 'p1_komponenten', 'p1_kriterien' und 'p1_warnsignale' erstellt. Die Tabelle 'p1_komponenten' enthält Spalten wie 'id', 'projekt_id', 'komponente_kuerzel', und 'synonyme'.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
```
{
  "file_summary": "Dieser Datei-Code erstellt bestimmte Datenbank-Tabelle-kompatible Datenstrukturen für das Projekt P2, indem es mit den gegebenen Datenbank-Verbindungen interagiert. Dazu gehören die Erstellung von Tischen wie p2_review_typ_entscheidung, p2_mapping_suchstring_komponenten und p2_trefferlisten sowie die Definition der Beziehungen zwischen diesen Tabellen.",
  "important_classes": ["Migration"],
  "functions": [
    "Schema::create", 
    "DB::statement",
    "DB::getDriverName"
  ],
  "external_dependencies": [
    "Illuminate\Database\Migrations\Migration", 
    "Illuminate\Database\Schema\Blueprint", 
    "Illuminate\Support\Facades\DB", 
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Migration für Datenbank-Tabellen (p3_disziplinen, p3_datenbankmatrix, p3_geografische_filter, p3_graue_literatur) erstellt. Tabelle 'projekte' muss bereits existieren.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
```
{
  "file_summary": "Dieses Script erstellt oder aktualisiert drei Datenbanktabellen (p4_suchstrings, p4_thesaurus_mapping, p4_anpassungsprotokoll) für die Projekt-IDs in der Datenbank. Es wird nur ausgeführt, wenn PostgreSQL als Datenbank-System verwendet wird.",
  "classes": ["Migration"],
  "functions": ["up", "down"],
  "endpoints": [null],
  "external_dependencies": [
    {"name": "Illuminate\Database\Migrations\Migration"},
    {"name": "Illuminate\Database\Schema\Blueprint"},
    {"name": "Illuminate\Support\Facades\DB"},
    {"name": "Illuminate\Support\Facades\Schema"}
  ]
}
```

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Migration für Datenbank-Tabellen 'p5_prisma_zahlen', 'p5_screening_kriterien' und 'p5_treffer'. Diese Migration erstellt Tabellen in der Datenbank, wenn sie noch nicht existieren. Die Tabelle 'p5_prisma_zahlen' enthält Informationen zu Prisma-Zahlen, die Tabelle 'p5_screening_kriterien' enthält Screening-Kriterien und die Tabelle 'p5_treffer' ist eine zentrale Treffer-Tabelle.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
```
{
  "file_summary": "Erstellt Datenbanktabellen für Qualitätsbewertungen und Lückenanalysen.
  Wichtigste Klassen: Migration, Blueprint.
  Externe Abhängigkeiten: DB, Schema.
  Funktionen: create_p6_qualitaetsbewertung, create_p6_luckenanalyse,
             dropIfTableExists (im down-Methode)."
}
```

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Diese Datei erstellt Tabelle p7_synthese_methode mit Synthesemethoden und Tabelle p7_datenextraktion mit Datenextrakten. Es werden auch Spalten für Qualitätsergebnisse und Konsistenzprüfungen hinzugefügt.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Erstellt Datenbank-Tabellen für P8-Projekt (suchprotokoll, limitationen, reproduzierbarkeitspruefung, update_plan) bei PostgreSQL.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
```json
{
  "file_summary": "Diese Datei erstellt und löscht Indices in der Datenbank für die Recherche-Funktionen. Die wichtigsten Klassen sind 'Migration' und 'DB'. Externe Abhängigkeiten sind 'Illuminate\Database\Migrations\Migration', 'Illuminate\Support\Facades\DB'."
}
```

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Erstellt eine PostgreSQL-Funktion "berechne_prisma_zahlen" zur Berechnung von Prisma-Zahlen für ein Projekt. Die Funktion gibt Tabellen zurück, die verschiedene statistische Werte für das Projekt berechnen. Die Berechnungen basieren auf Daten aus der Tabelle "p5_screening_entscheidungen" und berücksichtigen Entscheidungen zu Treffern in verschiedenen Ebenen (L1_titel_abstract, L2_volltext). Die Funktion wird nur bei PostgreSQL-Datenbank-Systemen erstellt.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Migration zur Erstellung der Tabelle 'chat_messages' in der Datenbank. Die Tabelle enthält die Spalten 'id', 'user_id', 'role', 'content' und 'created_at'. Die Migration prüft, ob es sich um eine PostgreSQL-Datenbank handelt und erstellt die Tabelle nur bei PostgreSQL.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Migration zum Erstellen einer Tabelle für Webhooks und Hinzufügen eines Foreign Keys zu der 'chat_messages'-Tabelle. Die Migration richtet sich speziell an PostgreSQL-Datenbanken.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
```
{
  "file_summary": "Migration zur Erstellung einer Tabelle für Papier-Embeddings. Externe Abhängigkeiten: PostgreSQL, Illuminate\Database. Hauptfunktionen: Migration auf Basis von PostgreSQL, Erstellung der Tabelle paper_embeddings mit Spalten und Indizes.",
  "main_classes": ["Migration", "Blueprint"],
  "external_dependencies": ["PostgreSQL", "Illuminate\Database"],
  "key_functions": ["create_table", "add_column", "drop_if_exists"]
}
```

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
```
{
  "file_summary": "Migration für Tabelle 'activity_log', um Aktivitäten-Log zu speichern.
  Diese Tabelle enthält Daten über durchgeführte Aktionen, darunter Logname, Beschreibung, Subjekt und Event.
  Externe Abhängigkeiten: Illuminate\Database\Migrations\Migration,
                        Illuminate\Database\Schema\Blueprint,
                        Illuminate\Support\Facades\Schema.
  Wichtige Funktionen/Klassen: up(), down()
  Tabelle 'activity_log' wird bei der Migration erstellt oder gelöscht."
}
```

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
```json
{
  "file_summary": "Migration, um eine Spalte \"status\" in der Tabelle \"users\" zu erstellen und die möglichen Werte mit einem Typen zu definieren. Unterstützt PostgreSQL und SQLite.",
  "main_class": "Illuminate\Database\Migrations\Migration",
  "key_functions": ["up", "down"],
  "external_dependencies": [
    {"name": "DB"},
    {"name": "Schema"},
    {"name": "Blueprint"}
  ]
}
```

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Migrationsdatei für die Datenbank. Hinzufügen einer Spalte 'frontend_object' zur Tabelle 'webhooks'.

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
```
{
  "file_summary": "Migrationsdatei für das Ändern der Spalte 'password' in der Tabelle 'webhooks' von nicht nullable zu nullable und setzt einen Standardwert auf ''",
  "key_classes_functions_endpunkte": {
    "Migration": null,
    "DB": ["statement"],
    "Schema": null
  },
  "externale_abhaengigkeiten": ["Illuminate\Database"]
}
```

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
Migration zur Hinzufügung einer Spalte "secret" in der Tabelle "webhooks", wenn PostgreSQL verwendet wird.

---

### database/seeders/DatabaseSeeder.php
```json
{
  "file_summary": "Diese Datei ist für die Initialisierung der Datenbank verantwortlich. Sie ruft Seedern auf, um Beispieldaten in die Datenbank einzufügen.",
  "important_classes": ["DatabaseSeeder", "RoleSeeder", "RechercheDemoSeeder"],
  "key_functions": ["run"],
  "extern_abhängigkeiten": ["App\Models\User"]
}
```

---

### database/seeders/RechercheDemoSeeder.php
```
{
  "file_summary": "Dieser Seeder füllt die Datenbank mit Beispielinhalten für das Recherche-Demo. Er erstellt Benutzer, Projekte, Phasen und Trefferliste sowie Webhooks. Die wichtigsten Klassen sind Projekt, Phase, P1Komponente, P2Trefferliste, P4Suchstring usw. Externe Abhängigkeiten sind die Models aus dem Namespace App\Models\Recherche und die Datenbank-Abfragen über DB."
}
```

---

### database/seeders/RoleSeeder.php
Diese Datei seedingt die Rollen "admin" und "user" in der Datenbank. Es wird davon ausgegangen, dass diese Rollen bereits existieren, falls nicht werden sie erstellt.

---

### database/sql/create_langdock_agent_user.sql
Die Datei "create_langdock_agent_user.sql" ist ein SQL-Skript zur Erstellung eines Datenbankbenutzers namens 'langdock_agent' und Ermittlung von Berechtigungen. Der Benutzer erhält Lese-/Schreibzugriff auf bestimmte Tabelle, einschließlich Recherche-Tabellen. Es wird jedoch Zugriff auf sensible Tabellen wie Passwortspezialfunktionen und Sitzungsdaten verwehrt.

---

## temp_dummy

### .github/pull_request_template.md
Das Template für Pull-Requests enthält eine Beschreibung der geplanten Änderungen, einen Typ der Änderung und eine Checkliste für die Überprüfung. Es kann auch Deploy-Hinweise enthalten.

---

## tests

### tests/Pest.php
```
{
  "file_summary": "Diese Datei ist ein Testframework und verfügt über Erweiterungen zur Funktionsumfang der Pest-Testbibliothek. Sie enthält außerdem Funktionen für die Anweisung von Ausgabewerten und externe Abhängigkeiten wie Laravel.",
  "key_classes": ["Tests\TestCase", "Illuminate\Foundation\Testing\\RefreshDatabase"],
  "main_functionality": [
    "Erweiterungen zur Pest-Testbibliothek",
    "Funktionen für die Anweisung von Ausgabewerten",
    "Hilfsfunktionen"
  ],
  "external_dependencies": ["Laravel", "PHPUnit"]
}
```

---

### tests/TestCase.php
Basisklasse für unit- und integrationstests in Laravel, erweitert die Foundation von Illuminate.

---

### tests/Feature/ContactTest.php
```
{
  "file_summary": "Diese Datei enthält eine Sammlung von Einheitstests für das Kontaktformular. Es werden verschiedene Szenarien getestet, wie z.B. die Übermittlung eines gültigen Formulars, das Fehlen optionaler Felder und die Validierung verschiedener Felder (Name, Email, Projekt-Typ, Nachricht). Die wichtigsten Klassen oder Funktionen sind App\Models\Contact und die verschiedenen Testfunktionen (z.B. test('contact form accepts valid submission')). Externe Abhängigkeiten sind die Datenbank (AssertDatabaseHas) und die Route 'contact.store'."
}
```

---

### tests/Feature/DashboardTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Anmeldeseite und das Dashboard einer Web-Anwendung. Sie prüft, ob Gäste auf die Login-Seite umgeleitet werden und ob angemeldete Benutzer den Dashboard-Zugriff haben.",
  "important_classes": ["User", "factory"],
  "important_functions": [route(), get, assertRedirect, actingAs],
  "external_dependencies": ["database" (für User-Model)]
}
```

---

### tests/Feature/ExampleTest.php
Bereitet Funktionstests für die Endpunkt 'home' vor und prüft, ob dieser erfolgreich ist. Die getestete Funktion stellt eine erfolgreiche Antwort sicher.

---

### tests/Feature/LegalPagesTest.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Anzeige von rechtlichen Seiten wie Impressum, DSGVO und AGB.\nHauptfunktion: Aufruf der Tests\nWichtige Funktionen: get(route('...')), assertStatus(200), assertSee('\')\nExterne Abhängigkeiten: Laravel-Route-Routen"
}
```

---

### tests/Feature/ProjektPolicyTest.php
```
{
  "file_summary": "Die Datei enthält Einheitstests für die Projekt-Policy, um sicherzustellen, dass Benutzer ihre eigenen Projekte bearbeiten und löschen können, aber nicht fremde Projekte. Die wichtigsten Klassen sind ProjektPolicy und User. Externe Abhängigkeiten sind App\Models\Recherche\Projekt, App\Models\User und App\Policies\ProjektPolicy."
}
```

---

### tests/Feature/RechercheTest.php
```
{
  "file_summary": "Die Datei definiert Tests für die Recherche-Funktionen. Es werden verschiedene Szenarien getestet, wie z.B. die Erstellung eines Forschungsprojekts, das Absetzen von Jobs und die Anzeige der eigenen Projekte. Externe Abhängigkeiten sind Queue, App\Models\Recherche\Projekt, App\Models\User, Illuminate\Support\Facades\Queue und App\Jobs\TriggerLangdockAgent."
}
```

---

### tests/Feature/WebhookTest.php
Diese Datei enthält Einheitstests für die Webhook-Funktion. Es werden verschiedene Szenarien getestet, wie z.B. das Ablehnen von Anfragen ohne oder mit ungültiger Signatur sowie das Akzeptieren einer gültigen Anfrage. Die wichtigsten Klassen und Funktionen sind test, postJson, call und Queue::fake(). Externe Abhängigkeiten sind Config (Services), Queue und die Model-Classes User und Projekt.

---

### tests/Feature/WelcomeTest.php
```
{
  "file_summary": "Dieser Test-Code überprüft die Funktionalität der Startseite einer Anwendung. 
                 Es werden folgende Szenarien getestet:
                 - Eine erfolgreiche Antwort der Startseite.
                 - Die Zählung von Seitenbesuchen erhöht sich korrekt.
                 - Wenn keine Seite besucht wurde, wird eine neue PageView-Eintrag erstellt.
                 - Ein Gast sieht Login- und Registrierungslinks.
                 - Ein angemeldeter User sieht einen Dashboard-Link.",
  "klasse/namespace": ["PageView", "User"],
  "funktionen/endpunkte": ["home", "get", "actingAs"],
  "externe abhängigkeiten": ["route", "factory"]
}
```

---

### tests/Feature/Auth/AuthenticationTest.php
Diese Datei enthält Tests zur Authentifizierung von Benutzern. Sie testet die Funktionen des Login-Screens, das Anmelden mit gültigen und ungültigen Passwörtern sowie das Abmelden und den Einsatz zweifaktorischer Authentifizierung.

---

### tests/Feature/Auth/EmailVerificationTest.php
```json
{
  "file_summary": "Diese Datei enthält Tests für die E-Mail-Verifizierung von Benutzern. 
    Die wichtigsten Funktionen sind:
    - Prüfung der Verifizierungs-Bildschirm-Renderung
    - Überprüfung des E-Mail-Verifizierungsprozesses
    - Überprüfung, ob ein bereits verifizierter User durch einen weiteren Link nicht neu verifiziert wird.
    Die wichtigsten Klassen sind: App\Models\User und Illuminate\Auth\Events\Verified.
    Externe Abhängigkeiten sind: Event, URL, Factory (für die User-Erstellung)."
}
```

---

### tests/Feature/Auth/PasswordConfirmationTest.php
```json
{
  "file_summary": "Dokumentiert Funktionen für Passwort-Bestätigung-Tests. \
  - Importiert User-Model aus App/Models. \
  - Testet Renderung des Passwort-Bestätigungs-Screens mit erfolgreicher Status-Kode 200."
}
```

---

### tests/Feature/Auth/PasswordResetTest.php
Authentifizierungstests für Passwortspezifikationen wie Token-Generierung und -Verwendung sowie Eingabevalidierung.

---

### tests/Feature/Auth/RegistrationTest.php
```json
{
  "file_summary": "Diese Datei enthält Registrierungstests für Authentifizierung. Die Datei definiert zwei Tests: 
  - Das Rendering der Registrierungsseite ist erfolgreich.
  - Ein neuer Nutzer kann sich erfolgreich registrieren lassen und wird automatisch angemeldet.",
  "wichtige_Klassen": ["Feature"],
  "wichtige_Funktionen": ["test", "get", "post", "assertStatus", "assertSessionHasNoErrors", "assertRedirect", "assertAuthenticated"],
  "externe_Abhängigkeiten": ["route"]
}
```

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
```
{
  "file_summary": "Diese Datei enthält Funktionalitäts-Tests für die Authentifizierungsmethode Two-Factor mit den wichtigsten Funktionen: 
    - Überprüfung der Weiterleitung auf das Login-Bildschirm, wenn nicht angemeldet ist.
    - Überprüfung der Renderng von Two-Factor Challenge unter bestimmten Einstellungen.
    Externe Abhängigkeiten: Laravel/Fortify und App\Models\User",
  "important_classes": ["User", "Features"],
  "important_functions": ["canManageTwoFactorAuthentication", "twoFactorAuthentication", "get", "post", "assertRedirect"],
  "external_dependencies": ["Laravel\\Fortify", "App\\Models\\User"]
}
```

---

### tests/Feature/Chat/BigResearchChatTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Chat-Funktionen. Es werden vier Tests durchgeführt: 
  - Ein Test, bei dem eine Nachricht gesendet und der Assistant-Antwort erhalten wird.
  - Ein Test, bei dem die Validierung von Nachrichten als Pflichtfeld geprüft wird.
  - Ein Test, bei dem keine Webhook-Verbindung vorhanden ist, um sicherzustellen, dass nichts gespeichert wird.
  - Ein Test, bei dem ein HTTP-Fehler generiert und eine Fehlermeldung ausgegeben wird.
 
 Die wichtigsten Klassen sind User, ChatMessage, Webhook und Http.
 Externe Abhängigkeiten sind die Livewire-Volt-Klasse und das Http-Client."
}
```

---

### tests/Feature/Recherche/PhaseP1Test.php
```json
{
  "file_summary": "Die Datei definiert Testfälle für die Phase P1 der Recherche-Funktionalität. Sie prüft die Funktionalitäten von Strukturmodellwahl-Erstellung, -Bearbeitung und -Löschen sowie die Validierung von Pflichtfeldern und die Zugangsbeschränkungen für Fremduser.",
  "imported_classes": ["App\Models\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}", "App\Models\User", "Livewire\Volt\Volt"],
  "external_dependencies": ["database (DB-Zugriffe)"]
}
```

---

### tests/Feature/Recherche/PhaseP2Test.php
Diese Datei enthält Tests für die Phase P2 der Recherche-Funktion. Es werden Funktionen wie die Erstellung und Bearbeitung von Review-Typen Entscheidungen, Clustern und Treffern getestet.

---

### tests/Feature/Recherche/PhaseP3Test.php
```
{
  "file_summary": "Test-Funktionen für die Phase P3 der Recherche-Funktionalität. 
  - Erstellung, Validierung und Bearbeitung von Datenbank-Matrizen.
  - Löschung von Datenbank-Matrizen.
  - Erstellung und Bearbeitung von Disziplinen.",
  "key_classes": ["Volt", "P3Datenbankmatrix", "P3Disziplin"],
  "external_dependencies": ["App\Models\Recherche\*", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP4Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Suchfunktion P4, 
                    einschließlich der Erstellung, Validierung und Bearbeitung von Suchstrings. 
                    Die wichtigsten Klassen sind Projekt, P4Suchstring, P4ThesaurusMapping und P4Anpassungsprotokoll.
                    Externe Abhängigkeiten: App\Models\Recherche, Livewire\Volt und DB-Zugriffe.",
  "key_classes": ["Projekt", "P4Suchstring", "P4ThesaurusMapping", "P4Anpassungsprotokoll"],
  "important_functions": ["newSs", "saveSs", "editSs"]
}
```

---

### tests/Feature/Recherche/ProjektDetailTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Projekt-Detail-Seite in der Recherche-Funktionalität. Die Tests prüfen, ob die Seite korrekt rendern wird, Tab-Umschalter funktionieren, Phasen-Zustand zurückgegeben werden und Back-Links auf die richtige Liste führen.",
  "klasse": ["Projekt", "User", "Volt"],
  "funktionen_endpunkte": ["switchTab", "getPhaseStatus", "call"],
  "externe_abhaengigkeiten": ["App\Models\Recherche\Projekt", "App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/AppearanceTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Erscheinung (Appearance) einer Benutzeroberfläche. Es werden Funktionen wie das Rendern von Schaltflächen, Toggle-Containern und clientseitigen Skripten getestet. Externe Abhängigkeiten sind Livewire, User-Model und Authentifizierung. Wichtige Klassen sind Volt und User.",
  "key_classes": ["Volt", "User"],
  "important_functions": ["test", "assertSee", "assertSeeHtml"],
  "external_dependencies": ["Livewire", "Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/Feature/Settings/PasswordUpdateTest.php
Test-Datei für die Passwort-Update-Funktion. Überprüft, ob das Passwort erfolgreich geändert werden kann und ob ein falsches aktuelles Passwort einen Fehler auslöst.

---

### tests/Feature/Settings/ProfileUpdateTest.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Profil-Einstellungen. 
                   Die Funktionen umfassen das Anzeigen der Profil-Seite, das Ändern des Benutzer-Profil-Information und das Löschen des Benutzerkontos.
                   Wichtige Klassen: User, Volt, Livewire,
                   Aufruf von Routen: profile.edit, settings.profile, settings.delete-user-form"
}
```

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
```json
{
  "file_summary": "Tests für die Einstellungen zur zwei-Faktor-Authentifizierung. Die Datei testet das Anzeigen der Einstellungsseite, die Anforderung einer Passwortsicherheit bei aktivierter Zwei-Faktor-Authentifizierung und den Fall, in dem die Zwei-Faktor-Authentifizierung deaktiviert ist.",
  "important_classes": ["User", "Features", "Volt"],
  "key_functions": ["test", "actingAs", "withSession", "get", "assertOk", "assertSee", "assertForbidden"],
  "external_dependencies": ["Laravel\Fortify\Features", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/WebhooksTest.php
Diese Datei enthält Tests für die Webhook-Einstellungen in der Anwendung. Sie überprüft, ob ein neuer oder bestehender Webhook erfolgreich gespeichert wird und ob die URL-Validierung funktioniert.

---

### tests/Unit/ExampleTest.php
Einheitstests für eine Beispiel-Logik, wobei die Wahrheit als wahr getestet wird.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
```json
{
  "file_summary": "Diese Datei definiert eine Liste von Kontakten in der Anwendung. 
                   Sie ist Teil des Filament-Resourcen-Bundles und ermöglicht die Auflistung von Kontaktdaten.
                   Die wichtigsten Klassen sind ListContacts, ContactResource und ListRecords."
}
```

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert eine Bildschirm-Seite für den Anzeigemodus einer Kontakt-Einheit in Filament. Es handelt sich um eine Erweiterung der ViewRecord-Klasse, die die Ressourcenklasse ContactResource bezieht.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Diese Datei definiert eine Seiten-Klasse für das Bearbeiten von Benutzern in Filament. Die wichtigsten Klassen sind EditUser und UserResource.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Dieses PHP-Script ist Teil des Filament-Ressourcen-Moduls und definiert eine Seite zur Anzeige einer Liste von Benutzern. Die wichtigste Klasse ist ListUsers, die eine Liste von Benutzern aus der UserResource anzeigt. Diese Seite verfügt über keine externen Abhängigkeiten oder API-Aufrufe.

---

### resources/views/dashboard.blade.php
```
{
  "file_summary": "Dashboard-Bereich mit Willkommensnachricht, schnellen Links und Überblick über aktuelle Projekte. Nutzer muss angemeldet sein.",
  " wichtige Klassen/Funktionen": [
    "auth()->user()",
    "Recherche\Projekt"
  ],
  "externe Abhängigkeiten": [
    "RouteServiceProvider (route())",
    "\App\Models\Contact",
    "\App\Models\Recherche\Projekt"
  ]
}
```

---

### resources/views/welcome.blade.php
```
{
  "file_summary": "Diese Datei ist für die Anzeige der Startseite der Webanwendung verantwortlich.
    Sie enthält HTML-Markup und CSS-Styling für den Benutzer.
    Die wichtigsten Klassen sind:
      - WelcomeView (die gesamte Seite)
      - AppLayout (Struktur für Seitenleiste und Inhalt)
    Externe Abhängigkeiten sind:
      - Favicon-Datei
      - Google Fonts (Inter und JetBrains Mono)"
}
```

---

### resources/views/components/action-message.blade.php
Darstellung einer Nachricht nach erfolgreicher Aktion, die nach 2 Sekunden automatisch verschwindet.

---

### resources/views/components/app-logo-icon.blade.php
```
{
  "file_summary": "Die Datei app-logo-icon.blade.php renderisiert ein SVG-Logo, das von einer SVG-Darstellung des App-Logos erzeugt wird. 
  Die Klassen, Funktionen oder Endpunkte sind nicht explizit genannt.
  Externe Abhängigkeiten: Keine."
}
```

---

### resources/views/components/app-logo.blade.php
Das Blade-Templat "app-logo.blade.php" dient als Komponente für das Anzeigen des Logos und den Namen der Anwendung. Es enthält zwei div-Elemente, eines für das Logo und eines für den Namensbereich. Die Logik ist einfach und konzentriert sich auf die Darstellung.

---

### resources/views/components/auth-header.blade.php
Diese Datei stellt den Authentifizierungsheader einer Anwendung dar. Sie enthält eine Komponente, die Titel und Beschreibung anzeigt. Die Komponente wird über das Blade-Template-System definiert.

---

### resources/views/components/auth-session-status.blade.php
Bildet den Status einer Authentifizierungssitzung in der Benutzeroberfläche ab, indem ein Div-Block mit einem grünen Text darstellt, falls die Sitzung bestätigt wurde.

---

### resources/views/components/placeholder-pattern.blade.php
Rendert ein SVG-Muster für Platzhalter. Wichtigste Komponente: 'pattern' in SVG, Aufruf über 'url(#pattern-{{ $id }})'

---

### resources/views/components/layouts/app.blade.php
Diese Datei definiert die Layout-Komponente für das Anwendungs-Framework. Sie enthält einen Seitenleistelement, ein Hauptbereich und übernimmt Inhalte des Slots.

---

### resources/views/components/layouts/auth.blade.php
Diese Datei ist ein Bladereinzeichnung für eine autorisierte Benutzeroberfläche. Sie enthält eine einfache Layout-Komponente, die einen Titel und ein Inhaltsfeld enthält.

---

### resources/views/components/layouts/app/header.blade.php
```
{
  "file_summary": "Die Datei app/header.blade.php enthält die Header-Struktur für das Anwendungsfenster.
    Sie importiert partials.head und verwendet Livewire für Navigation und einen App-Logo-Icon.
    Es gibt auch eine mobile-Navigation mit einem Seitenmenü für kleinere Bildschirme."
}
```

---

### resources/views/components/layouts/app/sidebar.blade.php
Dieses Blade-Templates stellt das Sidebar-Layout für die Anwendung dar. Es enthält den Seitenleisteninhalt und bietet Navigationselemente zur Verfolgung der Benutzerbewegungen.

---

### resources/views/components/layouts/auth/card.blade.php
Dies ist eine Blade-View für ein Autorisierungs-CARD-Komponenten Layout. Sie enthält einen Header mit Logo, Navigationslink zum Home-Endpunkt und eine rechte Inhaltszone, die von der übergeordneten Komponente gefüllt wird.

---

### resources/views/components/layouts/auth/simple.blade.php
Eine einfache Layout-Vorlage für Auth-Bereiche. Sie enthält einen Header mit App-Logo, Navigation zum Startseite und ein Bereich für Content. Die Vorlage verwendet Blade-Templates wie 'partials.head' und externe Packages wie 'cookie-consent'.

---

### resources/views/components/layouts/auth/split.blade.php
Das Layout einer authentifizierten Anwendung, das eine Halbseitenansicht bereitstellt. Es enthält ein Logo und eine Zitate-Box auf der linken Seite sowie Inhalt auf der rechten Seite.

---

### resources/views/components/settings/layout.blade.php
Das Layout für die Einstellungen-Seite. Es enthält eine Navigation, einen Titel und eine Überschrift. Die Navigation enthält Links zu verschiedenen Bereichen wie Profil, Passwort, Zwei-Faktor-Authentifizierung, Erscheinungsbild und Webhooks.

---

### resources/views/emails/contact-confirmation.blade.php
E-Mail-Vorlage für die Bestätigung einer Kontaktaufnahme. Die E-Mail enthält eine persönliche Ansprache, eine Zusammenfassung der Anfrage und eine Abschlussbotschaft.

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei stellt eine E-Mail-Vorlage für Kontaktanfragen bereit. Sie enthält die Kontaktdaten des Anfragenden sowie den Inhalt der Nachricht. Die wichtigsten Informationen werden in einer Tabelle formatiert und können je nach Anforderung weitere Daten enthalten (z.B. Firma, Zeitrahmen).

---

### resources/views/emails/deploy-notification.blade.php
Email-Template für Deploy-Notifikationen. Inklusive Zeitpunkt, URL und Umgebung.

---

### resources/views/layouts/legal.blade.php
HTML-Vorlage für die Anzeige rechtlicher Informationen mit dynamisch generierten Titel und CSS-Stilen.

---

### resources/views/legal/agb.blade.php
Diese Datei ist ein Template für die Darstellung der Allgemeinen Geschäftsbedingungen (AGB) einer Website. Es handelt sich um eine Benutzeroberfläche in HTML und Blade, die verschiedene Abschnitte enthält, wie Geltungsbereich, Registrierung, Nutzungsregeln, Kosten, digitale Käufe und Haftung.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei ist eine Datenschutzerklärung für die Website und das Spiel. Sie enthält Informationen über die Verantwortliche Stelle, Arten der verarbeiteten Daten, Zweck und Rechtsgrundlage der Verarbeitung sowie Angaben zu Sicherheit und technischer Protokollierung.

---

### resources/views/legal/impressum.blade.php
Das Impressum-Template ist eine Vorlage für die Anzeige von Impressum-Daten in einer Website. Es erweitert das Layout 'layouts.legal' und definiert Inhalte für den Titel, Header und Inhalt des Seitenabschnitts. Es enthält Informationen wie Kontaktdaten, Berufsbezeichnung und Steuernummer (oder deren Abwesenheit). Zudem verlinkt es zur Datenschutzerklärung und AGB. Die wichtigsten Klassen oder Funktionen in dieser Datei sind die Template-Erweiterungen und Inhaltsdefinitionen.

---

### resources/views/livewire/auth/confirm-password.blade.php
Die Datei 'confirm-password.blade.php' stellt eine UI-Komponente zum Bestätigen des Passworts dar. Sie enthält ein Formular, das vom Benutzer ausgefüllt werden muss, um die Passwortbestätigung abzuschließen. Die wichtigsten Klassen sind: x-auth-header, x-auth-session-status und die eigenen komponierten UI-Komponenten von Livewire.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei ist ein Teil der Authentifizierung und dient zur Wiederherstellung eines Passworts. Sie enthält einen Formular-Teil, um eine E-Mail-Adresse einzugeben, über die eine Passwort-Wiederherstellungs-Link gesendet wird.

---

### resources/views/livewire/auth/login.blade.php
Die Datei 'login.blade.php' ist Teil der Livewire-Bibliothek und dient als Login-Schleife für Benutzer. Sie enthält Formularelemente für Email-Adresse, Passwort, Erinnerungsfunktion und einen Submit-Button.

---

### resources/views/livewire/auth/register.blade.php
Das Register-Formular einer Authentifizierung-Anwendung mit Livewire.
Die Datei enthält eine Registrierungsform, die Daten wie Name, E-Mail und Passwort verlangt. Die Formulareingaben werden über ein POST-Angebot an den Server gesendet.
Importiert werden Livewire-Komponenten sowie Funktionen für das Handling von Sessions-Status und Error-Meldungen.

---

### resources/views/livewire/auth/reset-password.blade.php
Diese Datei ist eine Blade-Vorlage für das Zurücksetzen eines Passworts. Sie enthält ein Formular zur Eingabe eines neuen Passworts und einer Bestätigung des Passworts sowie einen Button zum Absenden des Forms. Die Vorlage verwendet die Livewire-Bibliothek, um eine dynamische Interaktion mit dem Benutzer zu ermöglichen.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Zwei-Faktor-Authentifizierung-Angebot in Livewire mit Formular für Authentikationscode und Recovery-Codes

---

### resources/views/livewire/auth/verify-email.blade.php
E-Mail-Vereifikations-Seite. Verwaltet Login-Prozess und E-Mail-Verifizierung.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Livewire-Komponente für Chat-Funktionen wie Nachrichtensendung und Historie-Löschen. Externe Abhängigkeiten: Webhook-Modell, ChatMessage-Modell, Auth, Http.

---

### resources/views/livewire/recherche/phase-p1.blade.php
```
{
  "file_summary": "Livewire-Komponente für die Recherche-Phase P1, um Projekte und ihre Strukturmodelle, Komponenten, Kriterien und Warnsignale zu verwalten. Die Komponente verwendet Livewire und interagiert mit der Datenbank mittels Eloquent.",
  "key_classes": ["Livewire\Volt\Component", "App\Models\Recherche"],
  "key_functions": ["newSmw", "saveSmw", "editSmw", "deleteSmw"],
  "external_dependencies": ["Auth", "Eloquent (Datenbankzugriff)"]
}
```

---

### resources/views/livewire/recherche/phase-p2.blade.php
```
{
  "file_summary": "Livewire-Component für Projektphasen P2; bietet CRUD-Funktionen für ReviewTypEntscheidung, Cluster, MappingSuchstringKomponente und Trefferliste.",
  "important_classes": ["P2ReviewTypEntscheidung", "P2Cluster", "P2MappingSuchstringKomponente", "P2Trefferliste"],
  "external_dependencies": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p3.blade.php
Die Datei ist ein Livewire-Component, der eine Benutzeroberfläche für die Eingabe von Daten in drei Kategorien anbietet: Datenbankmatrix, Disziplin und Geografischer Filter. Es besteht auch eine Möglichkeit zur Grauen Literatur einzugeben. Die Component überprüft die Authentifizierung und gibt Zugriff nur auf Projekt-Daten des aktuellen Benutzers.

---

### resources/views/livewire/recherche/phase-p4.blade.php
```
{
  "file_summary": "Livewire-Komponente für die Phase P4 der Recherche,
  die Suchstrings, Thesaurus-Mappe und Anpassungsprotokolle verwalten kann.
  Hauptklassen: Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll.
  Externe Abhängigkeiten: Auth, Livewire/Volt."
}
```

---

### resources/views/livewire/recherche/phase-p5.blade.php
```
{
  "file_summary": "Livewire-Komponente zur Recherche-Phase P5, umfasst CRUD-Funktionen für Prisma-Zahlen und Screening-Kriterien sowie Filter-Einstellungen.",
  "important_classes_functions_endpoints": {
    "Klassen": ["P5PrismaZahlen", "Projekt"],
    "Funktionen": ["newPrisma", "savePrisma", "editPrisma"],
    "Endpunkte": ["trefferFilter"]
  },
  "externe_abhaengigkeiten": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth", "Livewire\Volt\Component"]
}
```

---

### resources/views/livewire/recherche/phase-p6.blade.php
```
{
  "file_summary": "Diese Datei stellt die Livewire-Komponente für die Phase P6 der Recherche bereit. Sie ermöglicht das Anlegen, Bearbeiten und Speichern von Qualitätsbewertungen und Lückenanalysen. Die Komponente wird mit den Modellen Projekt, P5Treffer, P6Qualitaetsbewertung und P6Luckenanalyse verbunden.",
  "main_classes": ["Component", "Projekt"],
  "functions": ["newQb", "saveQb", "editQb"],
  "endpoints": [],
  "imports": [
    "App\Models\Recherche\{Projekt, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse}",
    "Illuminate\Support\Facades\Auth",
    "Livewire\Volt\Component"
  ],
  "db Zugriffe": ["P5Treffer", "P6Qualitaetsbewertung", "P6Luckenanalyse"]
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
```
{
  "file_summary": "Diese Datei stellt eine Livewire-Komponente zur Bearbeitung einer Forschungsphase dar. Sie enthält Formulare für die Synthese-Methode, Datenextraktion, Muster und Konsistenz sowie GRADE-Erstellung. Die Komponente ist an den Auth-Service gebunden und erfordert eine gültige Projekt-ID. Externe Abhängigkeiten: App\Models\Recherche, Livewire\Volt\Component, Illuminate\Support\Facades\Auth."
}
```

---

### resources/views/livewire/recherche/phase-p8.blade.php
```
{
  "file_summary": "Diese Datei definiert eine Livewire-Komponente für die Bearbeitung von Phasen in einem Projekt. Sie enthält Formulare für Suchprotokoll, Limitation, Reproduzierbarkeit und Update-Plan. Die Komponente wird mit Datenbankabfragen synchronisiert und bietet Funktionen zur Erstellung, Bearbeitung und Löschen dieser Daten. Externe Abhängigkeiten: App\Models\Recherche\*, Illuminate\Support\Facades\Auth, Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Dieser Blade-Templates zeigt Details eines einzelnen Projekts. Es enthält eine Übersicht über das Projekt und Tabellen für verschiedene Phasen des Projekts. Die wichtigsten Klassen sind die Livewire-Komponente und das Projekt-Modell. Externe Abhängigkeiten umfassen Authentifizierungsdienste und Datenbankzugriffe zum Laden von Projekt-Daten.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Die Datei 'projekt-liste.blade.php' ist eine Livewire-Komponente, die Projekte einer bestimmten BenutzerID aus der Datenbank abruft und anzeigt. Die Komponente enthält Funktionen zum Abrufen von Projekten, zum Löschen eines Projekts und zu deren Darstellung in der Anwendung.

---

### resources/views/livewire/recherche/research-input.blade.php
```
{
  "file_summary": "Livewire-Komponente für die Recherche-Eingabe. Fügt neue Projekte in der Datenbank hinzu, löst Trigger für Langdock-Agent aus und leitet zum Projekt-Übersichts-Endpunkt weiter.",
  "important_classes_and_functions": {
    "Component": "Livewire-Volt-Komponente",
    "starteRecherche": "Funktion zur Validierung der Eingabe und Erstellung eines neuen Projekts",
    "TriggerLangdockAgent": "Job zum Auslösen des Langdock-Agents"
  },
  "external_dependencies": {
    "Illuminate\Support\Facades\Auth": "Authentifizierungs-Facade",
    "App\Jobs\TriggerLangdockAgent": "Job für den Langdock-Agent",
    "App\Models\Recherche\Projekt": "Model für die Projektdatenbank"
  }
}
```

---

### resources/views/livewire/settings/appearance.blade.php
```
{
  "file_summary": "Diese Datei definiert ein Livewire-Component zur Anzeige und Verwaltung der Erscheinungsbild-Einstellungen. Es handelt sich um eine Benutzeroberfläche für das Ändern von Farbthemas, einschließlich Helligkeit, Dunkelheit und Systemfarbe.",
  "klasse": "Livewire\Volt\Component",
  "funktionen": [
    "applyTheme"
  ],
  "externale_abhaengigkeiten": [
    "window.matchMedia"
  ]
}
```

---

### resources/views/livewire/settings/delete-user-form.blade.php
Diese Datei enthält eine Livewire-Komponente für das Löschen eines Benutzerkontos. Sie ermöglicht die Einblendung eines Modals und die Validierung des Passworts. Nach erfolgreichem Löschvorgang leitet sie den Nutzer zum Startseite zurück.

---

### resources/views/livewire/settings/password.blade.php
Diese Datei stellt ein Formular zur Änderung des Benutzerpassworts bereit. Es überprüft das aktuelle Passwort und erfordert ein neues Passwort mit der Bestätigung. Die Änderungen werden direkt im Benutzerkonto gespeichert.

---

### resources/views/livewire/settings/profile.blade.php
```
{
  "file_summary": "Leitet Nutzerprofile an. 
  Die Komponente ermöglicht das Update des Benennamens und der E-Mail-Adresse eines Nutzers.
  Es gibt drei Funktionen: mount, updateProfileInformation und resendVerificationNotification."
}
```

---

### resources/views/livewire/settings/two-factor.blade.php
```
{
  "file_summary": "Ein Teil der Einstellungen zum Zweifaktor-Authentifizierung ist hier implementiert. 
    Die Funktionen sind wie folgt: 
    - Montage des Komponents mit Abfrage ob zwei-Faktor Auth. aktiv ist und das Abrufen des QR-Codes.
    - Aktivieren von zwei-Faktor Auth. für einen Benutzer.
    - Laden der Konfiguration für den zweifachen Faktor.
    - Anzeigen des Verifikationsschritts, wenn notwendig.
    - Bestätigung der zwei-Faktor-Authentifizierung."
}
```

---

### resources/views/livewire/settings/webhooks.blade.php
```
{
  "file_summary": "Diese Datei ist ein Teil der Livewire-UI und stellt die Funktionen für Webhooks dar. 
                   Es werden zwei Slots bereitgestellt: Dashboard Chat Slot und Recherche starten Slot.
                   Die Einstellungen können gespeichert werden, um Webhooks zu erstellen oder zu aktualisieren.
                   Die wichtigsten Klassen sind Livewire\Volt\Component und App\Models\Webhook.
                   Externe Abhängigkeiten sind Illuminate\Support\Facades\Auth und Illuminate\Support\Str."
}
```

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
```
{
  "file_summary": "Diese Datei stellt den Livewire-Component für die Two-Factor-Recovery-Codes dar.\n\t- Das Component ermöglicht das Laden und Generieren neuer Recovery-Codes für einen Benutzer.\n\t- Die Recovery-Codes werden in einem geschützten Array gespeichert und können nach dem Anlegen neu generiert werden.",
  " wichtigste_Klassen": ["Livewire\\Volt\Component", "GenerateNewRecoveryCodes"],
  "externen_Abhängigkeiten": ["Laravel\Fortify\Actions\GenerateNewRecoveryCodes"]
}
```

---

### resources/views/partials/head.blade.php
Diese Datei enthält HTML- und CSS-Kodeteile für die Anwendungsinhalte, einschließlich Metatags, Titelblatt, Iconen, Stylesheets und JavaScript-Funktionen zur Verwaltung der dunklen Mode. Es beinhaltet zudem externe Abhängigkeiten wie Fonts von 'fonts.bunny.net'.

---

### resources/views/partials/settings-heading.blade.php
Erstellt eine Überschrift für die Einstellungsseite, inkl. Titel und Beschreibung.

---

### resources/views/recherche/index.blade.php
Das Blade-Template für die Recherche-Ansicht enthält eine einfachere Struktur und bietet einen Eingabebereich für die Recherche und eine Liste der Projekte. Die Komponenten werden mithilfe von Livewire bereitgestellt.

---

### resources/views/recherche/show.blade.php
Rendert die Detailansicht eines Projekts in einer Recherche-Anwendung.

Die Datei verwendet Blade-Vorlagen, um eine einfache und benutzerfreundliche Darstellung zu ermöglichen.

Die wichtigsten Komponenten sind:
- livewire:recherche.projekt-detail (zeigt Details des ausgewählten Projekts an)
- $projekt (übergebenes Projektobjekt zur Anzeige der Details)

---

## uncategorized

### Readme.md
```
{
  "file_summary": "Laravel-Anwendung für die Erfassung und Analyse von Forschungsprojekten mit KI-gestützter systematischer Literaturrecherche. \
                    Verfügt über Authentifizierung, Benutzerverwaltung, systematische Literaturrecherche, Admin-Panel (Filament) und Kontaktformular.\
                    Utilisiert externe Abhängigkeiten wie Livewire, Volt, Filament, Fortify, Tailwind CSS, PostgreSQL und Redis."
}
```

---

### artisan
Laravel-Artisan, eine Artisan-Skript für die Ausführung von Konsolen-Befehlen. Es verwendet Composer und setzt Laravel voraus.

---

### basemodel.md
```
{
  "file_summary": "PostgreSQL-Datenbankmodell für systematische Literaturrecherche mit abgeschlossenen Phasen-Tracking. 
  - Erfasst Projekte mit Titel, Forschungsfrage, Review-Typ und Verantwortlichem.
  - Führt ein Phasen-Tracking durch, das acht Phasen (P1–P8) umfasst, darunter Phase 1: Fragestellung strukturiert.
  - Definiert verschiedene Typen wie Phasestatus, Reviewtyp, Strukturmodelle und Screening-Level.
  - Verwendet UUIDs für eindeutige Identifizierung von Projekten und Phasen.
  - Befügt die Datenbank mit Extensions wie uuid-ossp und pg_trgm."
}
```

---

### composer.json
Die Datei composer.json enthält die Composer-Konfiguration für das Laravel-Projekt 'nileneb/app-linn-games'. Sie definiert Abhängigkeiten, Autoloading und Skripte für die Projektumgebung. Die wichtigsten Klassen sind Laravel-Framework-Abhängigkeiten wie Filament, Fortify und Sanctum. Externe Abhängigkeiten sind z.B. Predis (Redis-Konnektor) und Spatie-Libraries (z.B. Activitylog, Cookie Consent).

---

### CONTRIBUTING.md
Diese Datei beschreibt die Konventionen und Richtlinien für das Beitragsmanagement der app.linn.games. Es handelt sich um eine Dokumentation zur strukturierten Arbeit an neuen Features, Bugfixes und Code-Änderungen. Die wichtigsten Klassen oder Funktionen sind die Branch-Konventionen, der Merge-Fluss und die Arbeitsabläufe für Issue-Lösung. Externe Abhängigkeiten sind Composer für Paketverwaltung und Pest Tests sowie PHPUnit für Einheitstests.

---

### deploy.sh
Dieses Skript (`deploy.sh`) bereitet eine Anwendung für die Produktion vor und startet sie dann. Es enthält Funktionen zur Image-Pull, Build, Migration, Cache-Optimierung und Start von Diensten. Externe Abhängigkeiten sind Docker und `.env`-Datei.

---

### package.json
```json
{
  "file_summary": "Paketkonfiguration für Node.js-Projekt: "
  + "Verwaltet Abhängigkeiten, Skripte und Builds mit Vite."
}
```

---

### phpunit.xml
Konfiguration für PHPUnit-Tests. Definiert Testsuites, Quellcode-Pfad und Umgebungsvariablen.

---

### vite.config.js
Konfiguriert Vite, einen Build-Tool-Chainer für Web-Anwendungen, mit Laravel-Vite-Plugin und TailwindCSS. Importiert CSS und JS-Dateien aus dem Laravel-Projekt und aktiviert Watcher für automatisches Rucken.

---

### .dockerignore
Diese Datei spezifiziert, welche Dateien und Ordner vom Docker-Build-Prozess ignoriert werden. Dazu gehören Git-Verzeichnisse, Entwickler-Konfigurationen, Logdateien, Cache-, Sitzung- und Ansichtsordner im Framework-Verzeichnis sowie das Tests- und Coverage-Verzeichnis. Zudem werden bestimmte Dateitypen wie SQL-Datenbanken und Docker-Kompositionen ausgeschlossen.

---

### .editorconfig
Konfiguration für Editor-Einstellungen wie Zeichencodec, Zeilenende und Einfügung von Leerzeichen.

---

### app/Actions/Fortify/CreateNewUser.php
Diese Datei implementiert das Interface CreatesNewUsers und ermöglicht die Erstellung neuer Benutzer. Sie überprüft Eingabewerte, erstellt einen neuen Benutzer und speichert ihn in der Datenbank. Die wichtigsten Klassen sind User und Validator.

---

### app/Actions/Fortify/PasswordValidationRules.php
Validiert Passwörter anhand von Regeln (z.B. Mindestlänge, Sicherheitsmerkmalen) und erfordert Bestätigung.

---

### app/Actions/Fortify/ResetUserPassword.php
Diese Datei implementiert die Funktion zum Zurücksetzen des Benutzerpassworts nach dem Fortify-Protokoll. Die wichtigsten Klassen sind ResetUserPassword, User und Validator. Externe Abhängigkeiten sind Laravel\Fortify\Contracts\ResetsUserPasswords und Illuminate\Support\Facades\Validator.

---

### app/Filament/Resources/ContactResource.php
Definiert die Ressourcen für den Kontaktmodul in Filament. Hierfür wird der Contact-Model verwendet und es gibt Funktionen für Formulare, Tabelle-Ansichten und Seiten zur Darstellung der Kontaktanfragen.

---

### app/Filament/Resources/UserResource.php
Diese Datei definiert eine Ressourcenzuweisung für das Modell User in Filament. Sie bietet eine Form zur Anzeige und Bearbeitung von Benutzerdaten sowie eine Tabelle zum Anzeigen aller Nutzer mit verschiedenen Filtermöglichkeiten.

---

### app/Http/Middleware/TrackPageView.php
```
{
  "file_summary": "Diese Middleware verfolgt die Seitenanforderungen auf der Website und fügt den IP-Adressen von Nutzern anonymisiert in der Datenbank ein. Es wird unterschieden zwischen Bots und normalen Usern.",
  "key_classes": ["TrackPageView"],
  "main_function": "handle",
  "external_dependencies": [
    "App\Models\PageView"
  ],
  "extern_imports": [
    "Illuminate\Http\Request",
    "Symfony\Component\HttpFoundation\Response"
  ]
}
```

---

### app/Http/Middleware/VerifyLangdockSignature.php
Mittelt durch Webhook-Signaturen für Langdock und überprüft die Gültigkeit der Signatur. Wenn die Signatur gültig ist, gibt die Middleware die Anfrage weiter.

---

### app/Http/Middleware/VerifyMcpToken.php
```
{
  "file_summary": "Mittelware-Verarbeitung zur Token-Validierung für die API-Anfragen. 
  Überprüft den MCP-Token im Header und gibt ein 401- oder 500-Fehler zurück, wenn der Token fehlt oder ungültig ist.
  Wichtige Funktionen: handle()
  Externe Abhängigkeit: config('services.mcp.auth_token')
}
```

---

### app/Jobs/IngestPaperJob.php
```json
{
  "file_summary": "Diese Datei implementiert eine Job-Klasse, die Papiere in Embeddings umwandelt und diese in einer Datenbank speichert. Die Funktionen sind: 
  - Das Teilen großer Textblöcke in kleinere Chunks.
  - Der Aufruf der Ollama-Service-API zum Erstellen von Embeddings.
  - Der Einsatz von DB-Zugriffen, um die Ergebnisse zu speichern.
  Wichtige Klassen und Funktionen sind: IngestPaperJob, chunkText, Http.
  Externe Abhängigkeiten sind die Illuminate-Bibliothek (für Queueing), Ollama-API und PostgreSQL-Datenbank."
}
```

---

### app/Jobs/TriggerLangdockAgent.php
```
{
  "file_summary": "Diese Datei definiert eine Job-Klasse namens TriggerLangdockAgent, die dafür sorgt, dass der Langdock-Agent aufgerufen wird. Die Klasse verwendet die Laravel-Queue-Funktionen und sendet einen POST-Anfrage an den Webhook-URL des Projekts. Wenn die Antwort fehlschlägt, wird ein Fehler im Log geschrieben und der Job abgebrochen.",
  "Klassen": ["TriggerLangdockAgent"],
  "Funktionen/Endpunkte": ["handle", "uniqueId"],
  "externe Abhängigkeiten": ["Illuminate\Support\Facades\Http", "Illuminate\Support\Facades\Log", "\App\Models\Webhook"]
}
```

---

### app/Livewire/Actions/Logout.php
```
{
  "file_summary": "Die Datei verantwortet das Loggen eines Benutzers aus der Anwendung. Sie verwendet den Auth-Guard und die Sitzungs-Manager von Laravel.",
  "main_class": "Logout",
  "key_function": "__invoke",
  "external_dependencies": [
    "Illuminate\Support\Facades\Auth",
    "Illuminate\Support\Facades\Session"
  ]
}
```

---

### app/Mail/ContactConfirmationMail.php
```
{
  "file_summary": "Diese Datei erstellt eine E-Mail zur Bestätigung von Kontaktanfragen. Sie nutzt die Laravel-Mailable-Funktion und setzt externe Abhängigkeiten zum App\Models\Contact-Modell und der E-Mail-Ansicht 'emails.contact-confirmation'."
}
```

---

### app/Mail/ContactInquiryMail.php
```json
{
  "file_summary": "Diese Datei definiert eine Benachrichtigungs-E-Mail für neue Kontaktanfragen. Sie verwendet die Mailable-Klasse von Laravel und setzt externe Abhängigkeiten wie App\Models\Contact und Illuminate\Mail\Mailables verwenden.",
  "key_classes": ["ContactInquiryMail", "Envelope", "Content"],
  "external_dependencies": ["App\Models\Contact"]
}
```

---

### app/Mail/DeployNotificationMail.php
```
{
  "file_summary": "Diese Datei stellt eine Benachrichtigungs-Mail für erfolgreiche Deployment-Vorgänge bereit. 
                   Die wichtigsten Klassen sind DeployNotificationMail, Envelope und Content.
                   Externe Abhängigkeiten: Illuminate-Mail."
}
```

---

### app/Policies/ProjektPolicy.php
Diese Datei implementiert eine Policy-Klasse für Projekte. Sie steuert Zugriffsrechte auf Projekte basierend auf Benutzerrollen. Die wichtigsten Funktionen sind die Verwaltung von Leserechten, Erstellung, Aktualisierung und Löschung von Projekten.

---

### app/Providers/AppServiceProvider.php
Registriert Projekt-Policies und konfiguriert HTTPS fuer Production-Umgebungen. Wichtige Klassen: Projekt, ProjektPolicy, Gate, URL.

---

### app/Providers/FortifyServiceProvider.php
```json
{
  "file_summary": "Diese Datei, FortifyServiceProvider.php, stellt den Service-Provider für die Sicherheitsfunktionen des Laravel-Frameworks dar. Sie konfiguriert und bootet Fortify-Einstellungen wie Benutzererstellung, Passwortspezifizierung und Rate-Limits.",
  "key_classes": ["FortifyServiceProvider"],
  "main_functionalities": ["Benutzererstellung", "Passwortspezifizierung", "Rate-Limiting"],
  "external_dependencies": ["Illuminate\Support\Facades\RateLimiter", "Laravel\Fortify\Fortify"]
}
```

---

### app/Providers/VoltServiceProvider.php
```
{
  "file_summary": "Dient als Service Provider für Livewire und mountet die Views für Livewire.",
  "key_classes_functions": {
    "Livewire\Volt\Volt": "",
    "App\\Providers\\VoltServiceProvider": "mounts View-Ordner"
  },
  "external_dependencies": {
    "Illuminate\Support\ServiceProvider": "Basis-Klasse",
    "config('livewire.view_path')": "Konfiguration",
    "resource_path('views/livewire'), resource_path('views/pages')" : "Views-Pfad"
  }
}
```

---

### app/Providers/Filament/AdminPanelProvider.php
```
{
  "file_summary": "Diese Datei stellt den Administrator-Panel-Anbieter für Filament dar und definiert die Sicherheits- und Authentifizierungs-Richtlinien. Der Anbieter bietet verschiedene Mittelweiser, um die Verbindung zum Backend sicher zu machen.",
  "kern_klassen": ["AdminPanelProvider", "Panel", "PanelProvider"],
  "haupt_funktionen": ["panel"],
  "externer_abhaengigkeiten": [
    "Filament\Http\Middleware\Authenticate",
    "Filament\Http\Middleware\AuthenticateSession",
    "Illuminate\Cookie\Middleware\AddQueuedCookiesToResponse",
    "Illuminate\Cookie\Middleware\EncryptCookies",
    "Illuminate\Foundation\Http\Middleware\VerifyCsrfToken",
    "Illuminate\Routing\Middleware\SubstituteBindings",
    "Illuminate\Session\Middleware\StartSession",
    "Illuminate\View\Middleware\ShareErrorsFromSession"
  ]
}
```

---

### bootstrap/app.php
```
{
  "file_summary": "Konfiguriert die Basis-Einstellungen für eine Laravel-Application.\n\nWichtige Klassen: Application, Request, Middleware, Exceptions\n\nExterne Abhängigkeiten: Illuminate\Foundation/Application, Illuminate\Http\Request"
}
```

---

### bootstrap/providers.php
Bootstrap-Datei, die die Registrierung von Dienstanbietern für eine Laravel-Anwendung enthält. Wichtigste Klassen sind die AppServiceProvider und FortifyServiceProvider. Externe Abhängigkeiten: Laravel-Dienstleister (z.B. 'App\Providers\AppServiceProvider').

---

### docker/common/nginx/default.conf
Diese Datei konfiguriert Nginx als Reverse-Proxy für ein Webanwendungssystem. Sie handhabt HTTP-Anfragen, Routen und Proxying zu internen Docker-Diensten sowie externen Abhängigkeiten wie der Postgres Datenbank. Die Konfiguration umfasst Zugriffskontrollen, Caching, Bilderverwaltung und dynamische Routing für PHP- und Livewire-Anwendungen.

---

### docker/common/nginx/docker-entrypoint.sh
Dieser Skript-Datei ist das Docker-Eintrittspunkt für NGINX. Sie generiert eine Konfigurationsdatei aus einer Vorlage und ersetzt einen Platzhalter durch die MCP-Berechtigungstoken. Anschließend startet sie NGINX.

---

### docker/common/nginx/dockerfile
Docker-File für Nginx-Bereitstellung, enthält Konfiguration und Einbindung von geheimen Werten

---

### docker/common/nginx/nginx.conf
```
{
  "file_summary": "Docker-Nginx-Konfiguration für Laravel-Server: 
                  - Sicherheit und RealIP-Einstellungen,
                  - Gzip-Kompression,
                  - Datei-Aufladegrenzen und
                  - Einbindung von externen Konfigurationsdateien."
}
```

---

### docker/common/php-fpm/dockerfile
```
{
  "file_summary": "Docker-Datei für PHP-FPM-Umgebung mit Build und Produktionsumgebungen.\
     Installiert PHP-Erweiterungen, Composer und npm-Abhängigkeiten.\
     Kopiert Anwendungscod und buildt Frontend-assets.\
     Verwendet php-fpm als Entrypoint."
}
```

---

### docker/common/php-fpm/entrypoint.sh
Dieses Script verwaltet den PHP-FPM-Service, sorgt für die Synchronisation von Frontend-Assets und wartet auf eine bereitgestellte Datenbank. Es führt dann Migrationen durch und caching relevanter Konfigurationen aus.

---

### docker/common/postgres/dockerfile
Dieses Dockerfile baut auf dem offiziellen PostgreSQL-Image von Docker auf und installiert zusätzliche Postgres-Erweiterungen (hypopg, pgvector).

---

### docker/common/postgres/init-extensions.sql
Initialisiert PostgreSQL-Erweiterungen für Performance-Monitoring und Daten-Optimierung.
Hauptklasse: Keine explizite Klasse, sondern SQL-Anweisungen.
Funktionen: Erstellung von Erweiterungen (hypopg, pg_stat_statements, vector).
Externe Abhängigkeiten: PostgreSQL-Datenbank.

---

### public/index.php
Bootstrapping von Laravel, einschließlich der Ausführung von Komponisten und der Bereitstellung der Anwendung mit dem Request-Handler.

---

### public/robots.txt
Konfiguriert die Zugriffsbeschränkungen für Web-Crawler wie Google. Derzeit kein explizites Verbot (Disallow) für alle User-Agents definiert.

---

### public/.htaccess
Diese Datei,.htaccess, konfiguriert Apache Server-Einstellungen für das Projekt. Sie handelt die Autorisierungs- und CSRF-Tokens, führt Umleitungen auf fehlende Trennstriche durch und sendet alle Anfragen an den Frontcontroller.

---

### resources/css/app.css
@import 'tailwindcss'; @custom-variant dark (&:where(.dark, .dark *)); @theme { Definiert Farben und Schriftarten } @layer theme { Definiert Variablen für dunkles Theme }; @layer base { Setzt Grenzen für alle Elemente }

---

### storage/app/private/.gitignore
```json
{
  "file_summary": "Gitignore-Datei, definiert ignorierte Dateien und Verzeichnisse im Git-Repository. 
    Ignoriert alle Dateien ( * ) außer der aktuellen .gitignore-Datei."
}
```

---

### .github/copilot-instructions.md
Diese Datei enthält Anweisungen für das Projekt 'app.linn.games' und beschreibt die Technologien, Architektur-Entscheidungen und Routing. Die wichtigsten Klassen, Funktionen oder Endpunkte sind: Laravel 12, Livewire 3, Tailwind CSS 4, Filament 4.9, PostgreSQL 16, Redis, Langdock Webhook, Ollama (Embeddings), Paper-Search MCP, Auth durch Fortify und Testing mit Pest. Externe Abhängigkeiten sind: GitHub Actions, Docker auf Synology NAS.

---

### .github/instructions/app_linn_games_custom.instructions.md
Dokumentation der Architektur und Konventionen von app.linn.games, einer proprietären Research-Management-Plattform für KI-gestützte systematische Literaturrecherche. Die Plattform besteht aus verschiedenen Komponenten wie PHP, Laravel, Livewire/Volt, Filament, Fortify, Tailwind CSS, PostgreSQL, Redis und Ollama. Die Architektur ist in 8 Phasen unterteilt (P1-P8) und enthält ein Kernmodell für Projekte sowie spezifische Modelle für jeden Schritt. Die Plattform verwendet auch pgvector-Embeddings für die Vektorsuche nach Fachartikeln.

---

### .github/instructions/docker-dev-workflow.instructions.md
```
{
  "file_summary": "Das Docker-Dev-Workflow-Dokument definiert die Konfiguration für den lokalen Entwicklungsbetrieb von app.linn.games. Es handelt sich um ein komplexes Workflow, der verschiedene Funktionen und Rebuild-Anforderungen nach Code-Änderungen enthält.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:
* `docker-compose.yml`: Die Haupt-Konfigurationsdatei für Docker Compose.
* `docker-compose.override.yml`: Die Datei, die im lokalen Entwicklungsbetrieb geändert wird und Source-Code als Bind-Mounts einbindet.
* `php-dev.ini`: Eine Datei, die OPcache-Einstellungen überschreibt, um PHP-Dateiänderungen bei jedem Request zu erkennen.

Externe Abhängigkeiten sind:
* Docker Compose
* Nginx
* PHP-FPM

Das Dokument definiert verschiedene Rebuild-Anforderungen nach Code-Änderungen, einschließlich:
* Kompilierte View-Cache st
* Filament-Assets fehlen
* `docker/common/nginx/` Konfiguration
* Tests (außerhalb `tests/`, `phpunit.xml`, `.env`)
* Jede Code-Änderung in Production"
}
```

---
