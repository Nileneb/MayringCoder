---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T05:16:46.390878
model: llama3.1:8b
mode: overview
files_total: 237
errors: 0
run_time_s: 598.9
---

# Funktions-Übersicht — 2026-04-02_0516

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 237
- **Fehler:** 0
- **Laufzeit:** 598.9s

## api

### app/Http/Controllers/ContactController.php
API-Funktion zum Senden von Kontaktanfragen: Überprüfung der Anforderungen, Erstellen einer neuen Contact-Eintrag und Versenden von Bestätigungs- und Informationenmails.

---

### app/Http/Controllers/Controller.php
Abstract-Controller-Klasse, von der alle API-Controllern erben sollen. Enthält keine spezifischen Funktionen oder Endpunkte.

---

### app/Http/Controllers/DsgvoController.php
Controller für DSGVO-basierte Funktionalitäten.
- Exportiert Nutzerdaten in einer JSON-Datei.
- Löscht das Benutzerkonto und alle damit verbundenen Daten.

---

### app/Http/Controllers/LangdockWebhookController.php
Diese Datei ist ein API-Controller, der Langdock-Webhook-Vorgänge verarbeitet. Sie validiert die Eingaben und startet den TriggerLangdockAgent-Job. Die wichtigsten Klassen sind LangdockWebhookController, TriggerLangdockAgent und Request.

---

### app/Http/Controllers/PaperRagController.php
```json
{
  "file_summary": "Papieringestions- und Suchfunktionen für Papierdaten.
Klassen: PaperRagController, IngestPaperJob
Funktionen: ingest, search
Externe Abhängigkeiten: DB-Zugriff, HTTP-Anfrage zu Embedding-Service"
}
```

---

### routes/api.php
```
{
  "file_summary": "API-Route-Konfiguration für verschiedene HTTP-Anfragen. 
                    Authentifizierung und Validierung von Signaten über Middleware.
                    Verbindung zu Controller-Classen: LangdockWebhookController, PaperRagController."
}
```

---

### routes/console.php
Das Skript bietet zwei Artisan-Commands: inspire, um eine Zitate auszugeben, und deploy:notify, um eine Benachrichtigung zum Deploy zu senden.

---

### routes/web.php
```
{
  "file_summary": "Diese Datei definiert Routen für die Web-App. Sie verfügt über Anforderungen, Antworten und verschiedene Controller. Die wichtigsten Klassen sind ContactController, DsgvoController und Projekt. Externe Abhängigkeiten beinhalten Laravel-Funktionen wie Route und Fortify.",
  "key_classes": ["ContactController", "DsgvoController"],
  "key_functions": ["store", "export", "deleteAccount"],
  "key_endpoints": ["/", "/contact", "/dashboard", "/settings/profile"],
  "extern_dependencies": ["Illuminate\Support\Facades\Route", "Laravel\Fortify\Features"]
}
```

---

## config

### docker-compose.dev.yml
Dev-Override für Docker-Komposition, ändert Nginx-Port von 6481 zu 6480.

---

### docker-compose.yml
Diese Datei verwalgt die Konfiguration für ein Docker-Entwicklungsumfeld. Es werden verschiedene Dienste wie Nginx, PHP-FPM, PostgreSQL und Redis konfiguriert. Die Dienste werden in verschiedenen Netzwerken gruppiert und abhängig voneinander. Externe Abhängigkeiten sind Docker-Bildsätze für die einzelnen Dienste, sowie Umgebungsvariablen wie AuthTokens.

---

### .env.example
Diese Datei enthält eine Reihe von Umgebungsvariablen, die für die Konfiguration der Anwendung verwendet werden. Sie umfasst Anzeigename, Umgebung, Schlüssel und Debug-Modus. Außerdem werden Datenbank-, Sitzungs- und Cachingsysteme konfiguriert sowie Anbindung an externe Dienste wie Redis, Mailserver und AWS.

---

### config/app.php
Diese Datei enthält Einstellungen für die Anwendung. Es werden wichtige Konstanten wie Anwendungsname, Umgebung, Debug-Modus und Zeitzone definiert.

---

### config/auth.php
```json
{
  "file_summary": "Authentifizierungs- und Passwortschutzkonfiguration für eine Anwendung\nDie Datei config/auth.php enthält Einstellungen für Authentifizierung und Passwortschutz.\nHauptfunktionen:\n- Definition der Standardauthentifizierungs-ID (guard) und Passwort-Wiederherstellungsbroker.\n- Definition von Authentifizierungs-Guard-Einstellungen, einschließlich des verwendeten Treibers (z.B. Session).\n- Definition von Benutzer-Anbietern, die den Zugriff auf Benutzerdaten ermöglichen.\nExterne Abhängigkeiten:\n- Umgebungsvariablen wie AUTH_GUARD und AUTH_PASSWORD_BROKER werden verwendet.\n- Die Klassen App\Models\User und Laravel\\Eloquent\\Model werden als Modell für die Benutzerdaten verwendet.",
  "main_classes": ["App\Models\User", "Laravel\\Eloquent\\Model"],
  "main_functions": [""],
  "main_endpoints": [""],
  "external_dependencies": ["env()", "App\Models\User::class"]
}
```

---

### config/cache.php
Die Datei config/cache.php konfiguriert die Cache-Funktionen für das Framework. Sie definiert den Standard-Cache-Store und verschiedene Cache-Stores (z.B. Array, Datenbank, File, Memcached, Redis).

---

### config/cookie-consent.php
Konfiguriert Einstellungen für das Cookie-Einwilligungsdialog.

---

### config/database.php
Konfiguration von Datenbankverbindungen für das Laravel-Projekt
Wird verwendet, um die Verbindung zu einer SQLite-, MySQL- oder MariaDB-Datenbank herzustellen
Umweltvariablen werden zur Konfiguration verwendet

---

### config/filesystems.php
Datenbank-Framework für Dateisystem-Logik und -Konfiguration. Definiert die Standard-Disk, unterstützte Disk-Arten und Links zwischen öffentlichen und privaten Storage-Verzeichnissen.

---

### config/fortify.php
```
{
  "file_summary": "Konfiguration für Laravel Fortify mit Einstellungen für Authentifizierung und Passwort-Restore. 
   - Definiert den Auth-Guard (web) und den Password-Broker (users).
   - Setzt das Feld für Benutzername/E-Mail auf 'email'.
   - Aktiviert den Umbau von Benutzernamen in Kleinbuchstaben.
   - Routen werden mit dem Prefix '/auth' versehen."
}
```

---

### config/logging.php
```json
{
  "file_summary": "Konfiguration für die Log-Ausgabe der Anwendung. Es werden verschiedene Kanäle (z.B. Datei, E-Mail, Slack) und deren Eigenschaften definiert.",
  "klasse_funktionen_endpunkte": {
    "Monolog\Handler\NullHandler",
    "Monolog\Handler\StreamHandler",
    "Monolog\Handler\SyslogUdpHandler",
    "PsrLogMessageProcessor"
  },
  "externe_abhaengigkeiten": [
    "env() Funktion für die Zugriff auf Umgebungsvariablen",
    "storage_path()" Funktion für den Speicherort von Log-Dateien
  ]
}
```

---

### config/mail.php
Konfiguration von E-Mail-Parametern und -Treibern für die Anwendung. Es werden verschiedene Treiber wie SMTP, SES, Postmark, Sendmail und Log konfiguriert und ihre entsprechenden Einstellungen angegeben.

---

### config/queue.php
Konfiguration von Queue-Backends für die Anwendung, wie z.B. Database, Beanstalkd oder SQS.

---

### config/services.php
Konfiguration für Drittanbieter-Dienste und APIs. Fehlt sind keine Informationen über externe Abhängigkeiten.

---

### config/session.php
```json
{
  "file_summary": "Konfiguration für Sitzungsmgmt (Datenbank, Datei, Verschlüsselung, etc.)\n\nWichtige Klassen:\n- keine explizit genannte Klasse in diesem File\n\nFunktionen/Endpunkte:\n- keine explizit genannte Funktion in diesem File\n\nExterne Abhängigkeiten:\n- Illuminate\Support\Str\n- Datenbankkonfiguration (umgebungsspezifisch)"
}
```

---

### docker/common/php-fpm/php-dev.ini
Diese Datei enthält Konfigurationen für PHP-FPM, insbesondere für den OPcache und die Fehler-Display-Einstellungen. Die Datei wird als Bind-Mount in docker-compose.override.yml aktiviert.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Dies ist eine Issue-Vorlage für Bug-Reports, die Benutzer dazu auffordert, Fehler zu melden und detaillierte Informationen zu liefern. Die Vorlage besteht aus mehreren Textfeldern zur Beschreibung des Problems, der Schritte zur Reproduktion und dem Schweregrad.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
```json
{
  "file_summary": "Template für die Erstellung von Feature-Anfragen durch Benutzer. 
   Es enthält mehrere Felder zur Beschreibung des Features, wie Zusammenfassung, Motivation, Vorgeschlagene Lösung und Akzeptanzkriterien.
   Eine Auswahlmöglichkeit für Breaking Changes ergänzt das Template."
}
```

---

### .github/ISSUE_TEMPLATE/refactor.yml
```
{
  "file_summary": "Vorlage für issue-basierte Code-Verbesserungen, ohne funktionalen Impact.\n\
Hauptfunktionen:\n\
- Label 'refactor' zur Kennzeichnung von Refactoring-Anfragen\n\
- Formulare für Beschreibung, betroffene Dateien und Akzeptanzkriterien\n\
Externe Abhängigkeiten: GitHub-Api (durch .github-Konfiguration)"
}
```

---

### .github/workflows/lint.yml
Workflow für Linter-Aktivitäten auf GitHub Actions Plattform
Wird auf Push und Pull-Request von Entwicklungs- und Hauptzweigen ausgeführt
Installiert PHP 8.4, Composer und Node.js
Lädt Flux-Credentials aus Umgebungsvariablen
Instanziert Pint für Codeformatierung

---

### .github/workflows/tests.yml
```
{
  "file_summary": "Workflows-Datei für Automatisierte Tests und Builds",
  "key_classes_functions_endpunkte": [
    "actions/checkout@v4", 
    "shivammathur/setup-php@v2", 
    "actions/setup-node@v4"
  ],
  "externale_abhaengigkeiten": [
    "composer", 
    "npm", 
    "github Actions API" (z.B. `secrets.FLUX_USERNAME`)
  ]
}
```

---

## data_access

### app/Models/ChatMessage.php
```
{
  "file_summary": "Die Datei definiert die Datenbank-Tabelle 'chat_messages'. Sie umfasst Eigenschaften für Nachrichten in einem Chat wie Benutzer- und Webhook-ID, Rolle und Inhalt. Außerdem enthält sie Beziehungen zu den zugehörigen Benutzern und Webhooks.",
  "important_classes": ["ChatMessage"],
  "important_functions": ["user", "webhook"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "User", "Webhook"]
}
```

---

### app/Models/Consent.php
Die Consent-Model-Datenbanktabelle verwalten. Dazu gehören die Zuweisung von Consent-Kategorien, Bestätigung und Widerruf von User-Einstellungen.

---

### app/Models/Contact.php
Definiert die Datenmodellierung für Kontakte und legt fest, welche Felder im Contact-Modell bearbeitet werden können.

---

### app/Models/PageView.php
Definiert die Eloquent-Modelklasse für PageViews mit fillable Feldern: path, ip_anonymous, user_agent und visits.

---

### app/Models/User.php
Die Datei definiert die User-Eigenschaften und -Verhaltensweisen. Sie umfasst Funktionen für Authentifizierung, Zwei-Faktor-Authentifizierung und Aktivitätsprotokollierung. Die wichtigsten Klassen sind Authenticatable, HasFactory, HasRoles, LogsActivity, Notifiable und TwoFactorAuthenticatable.

---

### app/Models/Webhook.php
```
{
  "file_summary": "Die Datei \"Webhook.php\" im Namespace \"App\Models\" stellt das Modell für Webhooks dar. Sie beinhaltet Funktionen zur Datenbank-Interaktion, wie z.B. die Ermittlung der Webhook-URL mit einem möglichen Query-Parameter zum Secret-Handling und den Zugriff auf verwandte Modelle wie User und ChatMessage.",
  "main_classes": ["Webhook"],
  "key_functions": ["callUrl", "forUser"],
  "important_relationships": ["user", "chatMessages"],
  "external_dependencies": [
    {"class": "User"},
    {"class": "ChatMessage"}
  ]
}
```

---

### app/Models/Recherche/P1Komponente.php
```json
{
  "file_summary": "Diese Datei stellt die Datenbanktabelle p1_komponenten der Datenbank-Abteilung dar. Sie enthält eine Beziehung zur Projekt-Tabelle und ermöglicht den Zugriff auf Komponentendaten sowie das Speichern von Änderungen an diesen Daten.",
  "key_classes": ["P1Komponente", "Projekt"],
  "key_methods": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P1Kriterium.php
```
{
  "file_summary": "Das Model P1Kriterium stellt Kriterien für Projekte dar. Es enthält Funktionen zum Beschreiben und Beziehen von Projekt-Kriterien. Die Kriterien werden in der Datenbanktabelle p1_kriterien gespeichert.",
  "key_classes_functions": {
    "P1Kriterium": "Model",
    "projekt(): BelongsTo": "Relation zur Tabelle \"projekte\"",
    "HasUuids": "Trait für UUID-Felder"
  },
  "external_dependencies": {
    "Illuminate\Database\Eloquent\Model": "Eloquent-Modell",
    "Illuminate\Database\Eloquent\Relations\BelongsTo": "Eloquent-BelongsTo-Beziehung",
    "Projekt::class": "zugehöriges Projekt-Model"
  }
}
```

---

### app/Models/Recherche/P1Strukturmodellwahl.php
```json
{
  "file_summary": "Eloquent-Modell für Datenzugriff auf p1_strukturmodell_wahl-Tabelle. Beinhaltet Beziehung zu Projekt-Modell und UUID-Funktionalität.",
  "key_classes": ["P1Strukturmodellwahl"],
  "external_dependencies": [
    {"Illuminate\Database\Eloquent\Model"},
    {"Illuminate\Database\Eloquent\Concerns\HasUuids"}
  ]
}
```

---

### app/Models/Recherche/P1Warnsignal.php
Mehrdimensionale Suche in der Datenbank nach Warnsignalen für Projekte. Einbinden von Projekt-Daten über Beziehung. Datentypen korrekt setzen.

---

### app/Models/Recherche/P2Cluster.php
```
{
  "file_summary": "Modell für die Tabelle p2_cluster, repräsentiert einen Cluster in einem Projekt mit Beziehungen zum Projekt und besitzt UUIDs.",
  "important_classes": ["P2Cluster"],
  "key_functions": ["projekt", "belongsTo"],
  "external_dependencies": [
    {"class": "Illuminate\Database\Eloquent\Model"},
    {"class": "Illuminate\Database\Eloquent\Concerns\HasUuids"},
    {"class": "Projekt"}
  ]
}
```

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Datenmodell für P2-Mapping-Suchstring-Komponenten. Speichert Verbindungen zwischen Projekten und Suchbegriffen. Beziehungen zu Projekt-Modellen und Datenbanktabellen.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Eloquent-Modell für Datenbank-Tabelle p2_review_typ_entscheidung. Erfasst Entscheidungen zu Review-Typen in Bezug auf Projekte.

---

### app/Models/Recherche/P2Trefferliste.php
```json
{
  "file_summary": "Die Datei P2Trefferliste.php definiert ein Model für die Datenbanktabelle \"p2_trefferlisten\". Es enthält Eloquent-Beziehungen zu anderen Modellen und spezifiziert die Schemakonforme Attribute und -Methoden.",
  "key_classes": ["P2Trefferliste"],
  "important_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P3Datenbankmatrix.php
```
{
  "file_summary": "Die Datei stellt eine Eloquent-Modell-Klasse für die Tabelle p3_datenbankmatrix dar. Sie definiert Beziehungen zu einer Projekt-Tabelle und ermöglicht den Zugriff auf spezifische Daten in dieser Tabelle.",
  "key_classes": ["P3Datenbankmatrix"],
  "key_functions": ["projekt", "project"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P3Disziplin.php
```
{
  "file_summary": "Eloquent-Modell für die Datenbank-Tabelle p3_disziplinen. Beinhaltet eine Beziehung zu Projekt-Model.",
  "key_classes": ["P3Disziplin", "Projekt"],
  "important_methods": ["projekt()"],
  "extern_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P3GeografischerFilter.php
```json
{
  "file_summary": "Die Datei P3GeografischerFilter.php stellt eine Datenbank-Tabelle dar und bietet Zugriffsmöglichkeiten auf diese. Die wichtigsten Klassen sind P3GeografischerFilter, Projekt. Externe Abhängigkeiten sind: Projekt.",
}
```

---

### app/Models/Recherche/P3GraueLiteratur.php
Eloquent-Modell für die Tabelle p3_graue_literatur, darin ist der Projektbezug implementiert.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
```json
{
  "file_summary": "Modell für das P4-Anpassungsprotokoll - Speichert Daten zu Anpassungen an Suchstrings. Verwaltet Beziehungen zu Suchstrings.",
  "key_classes": ["P4Anpassungsprotokoll"],
  "important_functions": ["suchstring"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P4Suchstring.php
```
{
  "file_summary": "Die Datei P4Suchstring.php stellt eine Eloquent-Model-Klasse dar, die Daten aus der Tabelle 'p4_suchstrings' abruft und verarbeitet. Sie bietet Beziehungen zu anderen Modellen (Projekt, Anpassungsprotokoll und Suchprotokolle) und ermöglicht das Abrufen von Suchstring-Daten.",
  "key_classes": ["P4Suchstring"],
  "key_functions": ["projekt", "anpassungsprotokoll", "suchprotokolle"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt", "P4Anpassungsprotokoll", "P8Suchprotokoll"]
}
```

---

### app/Models/Recherche/P4ThesaurusMapping.php
```json
{
  "file_summary": "Die Datei P4ThesaurusMapping.php definiert eine Eloquent-Model-Klasse für die Tabelle p4_thesaurus_mapping. Sie ermöglicht das Abfragen und Bearbeiten von Mapping-Beziehungen zwischen Projekten und Thesauri.",
  "key_classes": ["P4ThesaurusMapping"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P5PrismaZahlen.php
Diese Datei definiert die Datenbank-Tabelle p5_prisma_zahlen und ihre Beziehungen. Sie enthält die Daten für Prisma-Zahlen eines Projekts. Die Tabelle wird durch das Modell P5PrismaZahlen abgebildet, welches eine Beziehung zu der Projekt-Tabelle herstellt.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Eloquent-Modell für die Tabelle p5_screening_entscheidungen. Fügt einer Screening-Entscheidung Beziehungen zu einem P5Treffer hinzu.

---

### app/Models/Recherche/P5ScreeningKriterium.php
```json
{
  "file_summary": "Die Datei P5ScreeningKriterium.php enthält eine Eloquent-Model-Klasse für das p5_screening_kriterien-Tabelle. Sie ermöglicht die Ausführung von CRUD-Operationen auf diese Tabelle und bietet eine Beziehung zu der Projektentität.",
  "key_classes": ["P5ScreeningKriterium"],
  "important_functions": ["projekt", "belongsTo"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P5ToolEntscheidung.php
```json
{
  "file_summary": "Das Modell P5ToolEntscheidung dient der Datenbank-Abstraktion für die Tabelle p5_tool_entscheidung. Es enthält Beziehungen zu anderen Modellen (Projekt) und ermöglicht den Zugriff auf die darin gespeicherten Werte.",
  "key_classes": ["P5ToolEntscheidung"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P5Treffer.php
Eloquent-Modell für den Treffer-Tabelle in der Datenbank. Definiert Beziehungen zu anderen Modellen und stellt Zugriff auf die Tabelle bereit.

---

### app/Models/Recherche/P6Luckenanalyse.php
Das Modell P6Luckenanalyse stellt die Beziehungen zwischen Projekten und Luckenanalysen dar. Es enthält die Eigenschaften einer Luckenanalyse, einschließlich fehlenden Aspekten und möglichen Konsequenzen. Die Tabelle wird im Datenbank-Tabellenname 'p6_luckenanalyse' gespeichert. Das Modell hat eine Beziehung zu einem Projekt über den Attribut projekt_id.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
```json
{
  "file_summary": "Dies ist eine Eloquent-Model-Klasse für die Datenbank-Tabelle p6_qualitaetsbewertung. Sie stellt eine Qualitätsbewertung eines P5Treffer dar und enthält Beziehungen zur dazugehörigen Treffer-ID.",
  " wichtigste_Klassen_Funktionen": [
    "P6Qualitaetsbewertung (Model-Klasse)",
    "treffer() (Beziehung zur dazugehörigen Treffer-ID)"
  ],
  "externe_Abhängigkeiten": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model", "P5Treffer"]
}
```

---

### app/Models/Recherche/P7Datenextraktion.php
```
{
  "file_summary": "Die Datei P7Datenextraktion.php stellt ein Model für die Datenbank-Tabelle \"p7_datenextraktion\" dar. Sie enthält eine Verbindung zur Tabelle \"p5_treffer\" über den Primärschlüssel \"treffer_id\".",
  "main_classes": ["P7Datenextraktion"],
  "key_functions": ["treffer"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Ein Modell für die Datenbanktabelle 'p7_grade_einschaetzung', das Informationen zu Projektgraden enthält. Es verfügt über Beziehungen zur Tabelle 'projekte'.

---

### app/Models/Recherche/P7MusterKonsistenz.php
```
{
  "file_summary": "Definiert die Datenbanktabelle p7_muster_konsistenz mit ihren Spalten und Beziehungen. Die Tabelle enthält Informationen zu Mustern in Projekten und hat eine Beziehung zum Projekt-Modell. Externe Abhängigkeiten: Projekt-Modell.",
  " wichtigste_Klassen": ["P7MusterKonsistenz", "Projekt"],
  "wichtige_Funktionen": ["projekt()"],
  "externe_Abhängigkeiten": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P7SyntheseMethode.php
Eloquent-Modell für die Datenbank-Tabelle p7_synthese_methode. Es enthält die Beziehungen zum Projekt und kastet den gewählten Status als Boolean.

---

### app/Models/Recherche/P8Limitation.php
```
{
  "file_summary": "Die Datei P8Limitation.php stellt eine Datenbanktabelle für Projektlimitierungen dar. Sie enthält die Beziehungen zu Projekten und ermöglicht den Zugriff auf diese Tabelle über Eloquent.",
  " wichtigste Klassen/Funktionen/Endpunkte": ["P8Limitation", "projekt()"],
  "externe Abhängigkeiten": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
```
{
  "file_summary": "Modell für die Tabelle p8_reproduzierbarkeitspruefung. 
  Ein Modell zur Abfrage von Reproduzierbarkeitstests für ein Projekt. 
  Beinhaltet eine Beziehung zum Projekt, auf das sich der Test bezieht."
}
```

---

### app/Models/Recherche/P8Suchprotokoll.php
```
{
  "file_summary": "Modell für P8-Suchprotokolle. Mithilfe von Eloquent-Relations besteht eine Beziehung zu P4-Suchstrings. Fügt Daten zur Suchaktion hinzu und verwaltet die Beziehungen zwischen Suchprotokollen und Suchstrings.",
  "main_classes": ["P8Suchprotokoll"],
  "important_methods": ["suchstring", "__construct"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P8UpdatePlan.php
Datenzugriff für Tabelle p8_update_plan, bietet eine Verbindung zu Projekt-Daten und unterstützt Eloquent-Beziehungen.

---

### app/Models/Recherche/PaperEmbedding.php
Diese Datei stellt eine Datenbank-Model-Klasse für Paper-Embeddings bereit. Sie verfügt über Beziehungen zu einer Projekt-Tabelle und ermöglicht den Zugriff auf Embedding-Daten.

---

### app/Models/Recherche/Phase.php
Das Modell für die Datenbank-Tabelle 'phasen' stellt eine Phase eines Projekts dar. Es enthält Beziehungen zu einem Projekt und ermöglicht den Zugriff auf Phase-Daten.

---

### app/Models/Recherche/Projekt.php
Diese Datei definiert die Model-Klasse 'Projekt' für Datenbank-Beziehungen und -Abfragen. Die Klasse verwendet Eloquent von Laravel für ORM-Funktionen. Sie enthält Beziehungen zu anderen Modellen wie User, Phase, P1Strukturmodellwahl usw. sowie Log-Funktionen mit Spatie Activitylog.

---

### database/factories/UserFactory.php
```json
{
  "file_summary": "Factory für Benutzer in der Datenbank, generiert Dummy-Daten zur Simulation von Nutzern. Wichtige Funktionen sind die Definition des Standard-Zustands eines Benutzers und Möglichkeiten für spezielle Zustände wie nicht verifizierter E-Mail-Adresse oder Nicht-Konfiguration der zweistufigen Authentifizierung.",
  "important_classes": ["UserFactory"],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Factories\Factory",
    "Illuminate\Support\Facades\Hash",
    "Illuminate\Support\Str"
  ]
}
```

---

### database/factories/Recherche/ProjektFactory.php
Erstellt Faktorien für Projekte in der Datenbank mit zufälligen Beiträgen.

---

### database/migrations/0001_01_01_000000_create_users_table.php
Migration zum Erstellen von Benutzer-, Passwort-Restart-Token- und Sitzungstabelle in der Datenbank.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Erstellt Migrations, um zwei Datenbanktabellen für Cache-Daten und -Locks zu erstellen.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
```json
{
  "file_summary": "Dieser PHP-Code definiert eine Migration für die Datenbank, um drei Tabellen zu erstellen: 'jobs', 'job_batches' und 'failed_jobs'. Die Tabelle 'jobs' enthält Felder für die Job-Daten, die Tabelle 'job_batches' enthält Metadaten für Job-Batches und die Tabelle 'failed_jobs' enthält Daten für fehlgeschlagene Jobs. Die Migration wird durch die Klassen \"Illuminate\Database\Migrations\Migration\" und \"Illuminate\Database\Schema\Blueprint\" unterstützt."
}
```

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
```
{
  "file_summary": "Die Datei fügt Spalten für zwei-Faktor-Authentifizierung zu der Benutzer-Tabelle hinzu und ermöglicht eine Umkehrung dieser Änderungen.",
  "key_classes_functions_endpoints": {
    "Migration": "",
    "Blueprint": "Definiert Tabellenspalten",
    "Schema": ""
  },
  "external_dependencies": {
    "Illuminate\Database\Migrations\Migration": "",
    "Illuminate\Database\Schema\Blueprint": "",
    "Illuminate\Support\Facades\Schema": "",
    "DB-Zugriffe": "Ermöglicht Zugriff auf die Benutzer-Tabelle"
  }
}
```

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
```json
{
  "file_summary": "Diese Datei erstellt eine Datenbank-Tabelle 'contacts' und legt ihre Spalten fest. Die Tabelle enthält Felder für Namen, Unternehmen, E-Mail-Adressen, Projekttypen, Nachrichten und Zeitlinien. Die Funktion kann aufgerufen werden, um die Tabelle zu erstellen oder bei Bedarf wieder zu löschen.",
  "imported_classes": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"],
  "database_interaction": ["Schema::create('contacts', ...)", "Schema::dropIfExists('contacts')"]
}
```

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
```
{
  "file_summary": "Erstellt eine Tabelle für Seitenaufrufe, um Besucherzahlen zu tracken. 
Funktionen: up (Tabelle erstellen), down (Tabelle löschen).
Klassen: Migration.
Externe Abhängigkeiten: Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\Schema."
}
```

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Ermöglicht die Erstellung von Tabelle für Benutzungs- und Rollenverwaltung.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Migration zur Hinzufügung von Spalten in der Datenbank-Tabelle page_views: path, ip_anonymous und user_agent. Die Migration ermöglicht die Persistenz von anonymen Zugriffen auf Webseiten.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
```
{
  "file_summary": "Migration zum Erstellen einer Tabelle für Zustimmungen. Die Tabelle enthält Informationen über die Zustimmungserklärungen der Nutzer, einschließlich der IP-Adresse (optional), den zustimmbaren Kategorien und den Zeitpunkten der Zustimmung und ggf. des Widerrufs.",
  "key_classes": ["Migration"],
  "main_function": ["up", "down"],
  "important_tables": ["consents"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Migrationsdatei für SQLite, erstellt Recherche-Tabelle in der Datenbank. Hinzufügt Spalte 'status' zu 'users', erzeugt Tabellen: 'projekte', 'phasen', 'p5_treffer'.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Migration für die Datenbank, um neue Tabellen und Datentypen zu erstellen. Führt die PostgreSQL-Extensions uuid-ossp und pg_trgm ein. Definiert Ensembles für verschiedene Typen.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
```json
{
  "file_summary": "Migration für die Erstellung von Tabellen in einer PostgreSQL-DB: Projekt-Tabelle, Phase-Tabelle und Beziehungen zwischen ihnen. Die Migration wird nur ausgeführt, wenn PostgreSQL als Datenbank-System verwendet wird.",
  "key_classes_functions_endpoints": [
    "Migration",
    "Schema::create('projekte')",
    "Schema::create('phasen')",
    "DB::statement(\"ALTER TABLE projekte ADD COLUMN review_typ review_typ\")"
  ],
  "externale_dependencies": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint",
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
```
{
  "file_summary": "Dieser Datei ist eine Migrationsdatei für die Datenbank-Struktur. Sie erstellt vier Tabellen: p1_strukturmodell_wahl, p1_komponenten, p1_kriterien und p1_warnsignale. Die Tabelle p1_strukturmodell_wahl enthält Informationen über Wahlstrategien. Die Tabelle p1_komponenten enthält Informationen über Komponenten eines Strukturmodells. Die Tabelle p1_kriterien enthält Informationen über Kriterien. Die Tabelle p1_warnsignale enthält Informationen über Warnsignale.",
  " wichtigste Klassen/Funktionen": "Migration, Blueprint",
  "Hauptfunktionalität": "Datenbank-Struktur erstellen",
  "externe Abhängigkeiten": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
```
{
  "file_summary": "Erstellt drei Datenbanktabellen für das Projekt P2: p2_review_typ_entscheidung, p2_mapping_suchstring_komponenten und p2_trefferlisten sowie eine Tabelle p2_cluster. Die Tabellen werden nur dann erstellt, wenn sie nicht bereits existieren.",
  "key_classes": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint"
  ],
  "key_functions": [
    "up",
    "Schema::create",
    "DB::getDriverName",
    "DB::statement"
  ],
  "external_dependencies": [
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
```json
{
  "file_summary": "Datenbankmigration, die drei Tabellen erstellt: p3_disziplinen, p3_datenbankmatrix und p3_geografische_filter. Es werden auch Primary-Keys und Foreign Keys definiert.",
  "important_classes": ["Migration"],
  "key_functions": ["up", "Schema::create", "DB::statement"],
  "external_dependencies": ["Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
Migration zur Erstellung von drei Datenbanktabellen: p4_suchstrings, p4_thesaurus_mapping und p4_anpassungsprotokoll. Die Tabellen enthalten Informationen zu Suchstrings, Thesaurusschlüsseln und Anpassungshistorie. Externe Abhängigkeit: PostgreSQL-Datenbank.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
```
{
  "file_summary": "Diese Datei erstellt oder aktualisiert drei Datenbanktabellen: p5_prisma_zahlen, p5_screening_kriterien und p5_treffer. Es werden außerdem einige Spalten hinzugefügt oder aktualisiert.",
  "important_classes": ["Migration"],
  "functions": ["up", "down"],
  "endpoints": [],
  "external_dependencies": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint",
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
```
{
  "file_summary": "Migration für die Erstellung von zwei Datenbanktabellen (p6_qualitaetsbewertung und p6_luckenanalyse) im PostgreSQL-Format. Die Tabellen enthalten Bewertungen und Analysen zu Projekten.",
  "key_classes": ["Migration", "Blueprint"],
  "important_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Diese Datei erstellt Tabellen in der Datenbank für die Projekt-Beurteilung. Es werden vier Tabellen erstellt: p7_synthese_methode, p7_datenextraktion, p7_muster_konsistenz und p7_grade_einschaetzung. Die Tabellen enthalten Informationen über die Beurteilungsmethoden, Datenextraction, Musterkonsistenz und Grade-Einschätzungen.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Erstellt Datenbank-Tabellen für Projekt-Pflege und -Anpassung.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
```
{
  "file_summary": "Migration, die Indizes in der Datenbank erzeugt und löscht. Erstellt Full-Text-Suche für Abstracts und Titel. Abhängigkeit: Illuminate\Database\Migrations\Migration"
}
```

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
```
{
  "file_summary": "Erstellt eine Prisma-Funktion \"berechne_prisma_zahlen\" zur Berechnung von Metriken für Projekte. Funktion kann nur in einer PostgreSQL-Datenbank erstellt werden.",
  "key_elements": ["Migration", "Prisma-Funktion", "PostgreSQL-Datenbank"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
```
{
  "file_summary": "Migration zur Erstellung der Tabelle 'chat_messages' im PostgreSQL-Datenbank-System. Die Tabelle enthält Spalten für 'id', 'user_id', 'role', 'content' und 'created_at'. Außerdem wird eine Fremdschluesselbeziehung zum 'users'-Tabelle aufgebaut.",
  "main_classes": ["Migration", "Blueprint"],
  "important_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Erstellt Tabelle für Webhooks und fügt Spalte zur Chat-Messages-Tabelle hinzu, falls PostgreSQL verwendet wird. Beinhaltet Migration sowohl aufwärts als auch abwärts.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Migration zum Erstellen einer Tabelle für Paper-Einbettungen, abhängig von der Datenbank-Engine. Die Tabelle enthält Einträge mit uuid-für ID und Projektbeziehung sowie metadata als JSON.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
```
{
  "file_summary": "Erstellt eine Tabelle für die Protokollierung von Aktivitäten im System. Die Tabelle enthält Informationen über den Log-Eintragsnamen, Beschreibung, betroffene Objekte und Änderungen. Sie verfügt über Indizes für effiziente Abfragen.",
  "key_classes": ["Migration", "Blueprint"],
  "key_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
```json
{
  "file_summary": "Migration zur Einführung einer Status-Tabelle in der Benutzer-Datenbank. 
                    Fügt die Spalte \"status\" mit einem Enum-Werttyp hinzu, falls nicht bereits vorhanden.
                    Unterstützt PostgreSQL und SQLite (für Tests)."
}
```

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
```json
{
  "file_summary": "Migration zur Hinzufügung der Spalte 'frontend_object' zu Tabelle 'webhooks' und Definition eines Enums 'webhook_frontend_object'.",
  "classes": ["Migration"],
  "functions": ["up", "down"],
  "endpunkte": [DB::statement],
  "externale_abhaengigkeiten": ["pgsql-Database"]
}
```

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
```
{
  "file_summary": "Die Datei ändert die Datenbank-Struktur für ausgehende Webhooks: sie macht den Passwort-Spalte in der Tabelle 'webhooks' optional (nullable) und setzt das Standard-Default-Wert auf leer. In der Down-Funktion werden wieder eine NotNull-Einschränkung und ein Default-Wert eingefügt.",
  "key_classes": ["Illuminate\Database\Migrations\Migration"],
  "main_functions": ["up", "down"],
  "external_dependencies": ["DB", "Schema"]
}
```

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
```json
{
  "file_summary": "Migration zur Hinzufügung einer 'secret'-Spalte in der Tabelle 'webhooks' für PostgreSQL-Datenbanken.",
  "key_classes_functions_endpoints": ["DB::statement", "up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/seeders/DatabaseSeeder.php
```
{
  "file_summary": "Datei für die Datenbank-Seedung, Initialisierung von Benutzer und Rollen.",
  "important_classes": ["DatabaseSeeder", "RoleSeeder", "RechercheDemoSeeder"],
  "functions_endpunkte": ["call()"],
  "external_dependencies": ["App\Models\User"]
}
```

---

### database/seeders/RechercheDemoSeeder.php
Diese Datei ist ein Seeder für die Datenbank und füllt sie mit Beispiel-Daten. Sie nutzt verschiedene Modelle wie Projekt, Phase, P1Komponente usw. und erstellt Webhooks. Die wichtigsten Klassen sind RechercheDemoSeeder, Projekt, Phase, P1Komponente usw. Externe Abhängigkeiten sind die Datenbank (durch Laravel) und Webhook-URLs.

---

### database/seeders/RoleSeeder.php
Seedet Rollen in der Datenbank, erstellt 'admin' und 'user' mit entsprechenden Guard-Names.

---

### database/sql/create_langdock_agent_user.sql
Dieses SQL-File erstellt und konfiguriert einen DB-User 'langdock_agent' für die Datenbank "linn_games". Der User erhält Zugriff auf bestimmte Tabelle in der Datenbank, aber nicht auf sensible Daten wie z.B. Password-Resset-Tokens oder Sitzungen.

---

## temp_dummy

### .github/pull_request_template.md
Diese Datei ist ein Pull-Request-Vorlage-Dokument für das GitHub-Projekt und dient zur Dokumentation von Änderungen an dem Projekt. Sie enthält eine Beschreibung der geplanten Änderung, einen Typ der Änderung (Bug-Fix, Feature, Refactoring, Dokumentation oder CI/Infrastruktur), eine Checkliste für die Einhaltung von Konventionen und Tests sowie Hinweise zur Bereitstellung.

---

## tests

### tests/Pest.php
```
{
  "file_summary": "Dieses Skript stellt Testfunktionen und Erwartungen zur Verfügung. Es bindet die TestCase-Klasse von PHPUnit an und ermöglicht den Zugriff auf Datenbank-Verbindungen via RefreshDatabase. Außerdem wird eine Erweiterung der Expectation-API durchgeführt, um spezifische Überprüfungen zu ermöglichen.",
  " wichtige_Klassen": ["Tests\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"],
  "wichtige_Funktionen": ["pest()", "expect()"],
  "externe_Abhängigkeiten": ["PHPUnit\Framework\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/TestCase.php
```json
{
  "file_summary": "Basis-Klasse für Einheitstests in Laravel. Erweitert die `BaseTestCase` von `Illuminate\Foundation\Testing`. Leitet von `BaseTestCase` ab."
}
```

---

### tests/Feature/ContactTest.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Kontaktformular-Logik. Die wichtigsten Funktionen sind die Überprüfung von Validierungsfehlern und die Assertion der Datenbank-Änderungen nach einem erfolgreichen Formular-Senden. Externe Abhängigkeiten: App\Models\Contact, route('contact.store')."
}
```

---

### tests/Feature/DashboardTest.php
Diese Datei enthält Tests für die Dashboard-Funktion des Anwendungsprojekts. Sie überprüft, ob Gäste zur Login-Seite weitergeleitet und autorisierte Benutzer Zugriff auf die Dashboard-Seite haben.

---

### tests/Feature/ExampleTest.php
Diese Datei enthält ein Unit-Test für die Route 'home'. Die Funktion prüft, ob diese Route eine erfolgreiche Antwort (Statuscode 200) zurückgibt.

---

### tests/Feature/LegalPagesTest.php
Diese Datei enthält Tests für die Anzeige von rechtlichen Seiten wie Impressum, DSGVO und AGB. Die wichtigsten Funktionen sind: get(route) für den Aufruf von Routen und assertStatus / assertSee für die Überprüfung der Seite. Externe Abhängigkeiten sind die Routing-Funktion route() und möglicherweise eine Datenbank-Abfrage.

---

### tests/Feature/ProjektPolicyTest.php
```
{
  "file_summary": "Die Datei enthält Tests für die Projekt-Politik, insbesondere zur Sichtbarkeit, Aktualisierbarkeit und Löschanzeige von Projekten. Es werden Szenarien zum Zugriff durch Eigentümer und nicht-Eigentümer getestet. Die wichtigsten Klassen sind ProjektPolicy und die Modelle User und Projekt.",
  "wichtige_Klassen": ["ProjektPolicy", "User", "Projekt"],
  "wichtige_Funktionen": ["view", "update", "delete", "create"],
  "externe_Abhängigkeiten": ["App\Models\Recherche\Projekt", "App\Models\User", "App\Policies\ProjektPolicy"]
}
```

---

### tests/Feature/RechercheTest.php
Diese Datei enthält Testfälle für die Recherche-Funktion des Systems. Es werden Funktionen wie Projekt-Erstellung, Projektliste und Projektdetailansicht getestet. Die Tests überprüfen auch den Zugriff auf diese Funktionen als angemeldeter Benutzer oder Gast.

---

### tests/Feature/WebhookTest.php
```
{
  "file_summary": "Diese Datei enthält Webhook-Tests, um sicherzustellen, dass der Webhook korrekt funktioniert. Es werden verschiedene Szenarien getestet, wie z.B. das Ablehnen von Anfragen ohne Signatur oder mit einer ungültigen Signatur. Die wichtigsten Klassen und Funktionen sind die Laravel-Tests-Funktionen und die Config-Facade. Externe Abhängigkeiten sind Queue, Config, App\Models\User und App\Models\Recherche\Projekt."
}
```

---

### tests/Feature/WelcomeTest.php
Diese Datei enthält funktionale Tests für die Startseite einer Webanwendung. Sie prüft, ob die Anwendung korrekt antwortet und ob wichtige Daten wie Besucherzähler aktualisiert werden. Darüber hinaus werden Tests durchgeführt, um sicherzustellen, dass sich Gäste richtig anmelden können, aber angemeldete Nutzer den Zugriff auf das Dashboard haben.

---

### tests/Feature/Auth/AuthenticationTest.php
```
{
  "file_summary": "Authentifizierungstests für Benutzer mit und ohne Zwei-Faktor-Authentifizierung.",
  "important_classes": ["User", "Features"],
  "important_functions": ["login", "logout", "twoFactorAuthentication"],
  "external_dependencies": ["Laravel\\Fortify\\Features", "App\Models\User"]
}
```

---

### tests/Feature/Auth/EmailVerificationTest.php
```json
{
  "file_summary": "Diese Datei enthält Tests für die E-Mail-Verifizierung in einer Anwendung. Es werden die folgenden Funktionen getestet:\n\n* Die Darstellung der E-Mail-Verifizierungsseite\n* Die erfolgreiche Verifizierung einer E-Mail-Adresse\n* Die Nichtverifizierung einer E-Mail-Adresse mit einem ungültigen Hash\n* Die Weiterleitung eines bereits verifizierten Benutzers zu seiner Dashboard-Seite ohne erneutes Auslösen des Events",
  "important_classes_functions_endpoints": ["User", "Verified", "Event", "URL", "get", "actingAs", "assertStatus", "assertDispatched"],
  "external_dependencies": ["Illuminate\Support\Facades\Event", "Illuminate\Support\Facades\URL"]
}
```

---

### tests/Feature/Auth/PasswordConfirmationTest.php
```
{
  "file_summary": "Diese Datei enthält einen Test für die Bestätigung des Passworts. 
Es wird ein Benutzer erstellt und überprüft, ob die Bestätigungsseite korrekt gerendert wird.
Wichtige Klassen: User (Modell), Tests für das Routing (password.confirm)",
  "imported_classes": ["User", "$this"],
  "api_endpunkte": ["route('password.confirm')"]
}
```

---

### tests/Feature/Auth/PasswordResetTest.php
```json
{
  "file_summary": "Test-Datei für Authentifizierungs-Funktionen, insbesondere Passwort-Zurücksetzen.\
                    Überprüft die korrekte Funktionsweise der folgenden Prozesse:\
                     - Die Anzeige des Passwort-Restart-Screens,\
                     - Die erfolgreiche Anfrage für den Passwort-Restart-Link,\
                     - Die Anzeige des Passwort-Restart-Screens nach Erhalt des Link-Anfrages,\
                     - Das erfolgreiche Zurücksetzen eines Passworts mit einem gültigen Token.\
                    Die wichtigsten Klassen, Funktionen oder Endpunkte sind:\
                      - route('password.request'),\
                      - route('password.reset'),\
                      - route('password.update') und die zugehörigen Methoden,\
                      - User-Model zur Erstellung von Testnutzern,\
                      - ResetPassword-Notification für den Passwort-Restart-Anfragen."
}
```

---

### tests/Feature/Auth/RegistrationTest.php
Diese Datei enthält Unit-Tests für die Registrierungsfunktion eines Auth-Systems. Sie prüft, ob sich der Benutzer erfolgreich registrieren kann und ob es nach einer erfolgreichen Registrierung zur Dashboard-Seite weitergeleitet wird.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
```json
{
  "file_summary": "Die Datei enthält Tests für die Zwei-Faktor-Authentifizierung in Laravel Fortify.\n\
Sie prüft, ob sich unautentierte Benutzer auf die Login-Seite umgeleitet werden und\n\
ob die Zwei-Faktor-Anforderung korrekt dargestellt wird. Die Datei verwendet User-Model,\n\
Laravel Fortify Features und API-Zugriffe zur Benutzerverwaltung."
}
```

---

### tests/Feature/Chat/BigResearchChatTest.php
Diese Datei enthält Testfälle für das Chat-Feature. Sie überprüft die korrekte Funktion von Nachrichten-Senden und -Verarbeitung. Die wichtigsten Klassen sind User, Webhook, ChatMessage und Volt. Externe Abhängigkeiten gibt es durch Http-Fake.

---

### tests/Feature/Recherche/PhaseP1Test.php
Diese Datei enthält Funktionen für die Testabwicklung von Recherche-Phase-P1. Die wichtigsten Funktionen sind die Erstellung, Bearbeitung und Löschung von Strukturmodellwahlen sowie die Validierung von Pflichtfeldern. Die wichtigsten Klassen sind 'P1Strukturmodellwahl', 'User' und 'Projekt'. Externe Abhängigkeiten werden über Livewire-Volt verwendet. Einige Tests simulieren Benutzer mit unterschiedlichen Rechten (z.B. Besitzer vs. Fremder User).

---

### tests/Feature/Recherche/PhaseP2Test.php
Diese Datei enthält Tests für die Phase P2 der Recherche-Funktion. Sie prüft die Erstellung, Bearbeitung und Löschung von Review-Typen sowie die Erstellung von Clustern. Die wichtigsten Klassen sind Projekt, User, P2ReviewTypEntscheidung, P2Cluster und Volt. Externe Abhängigkeiten sind Livewire und der Datenbank-Zugriff.

---

### tests/Feature/Recherche/PhaseP3Test.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Phase P3 der Recherchefunktion. Es werden folgende Funktionen getestet: 
    - Die Erstellung einer Datenbankmatrix,
    - Die Validierung von Datenbank als Pflichtfeld,
    - Das Bearbeiten einer bestehenden Datenbankmatrix und
    - Das Löschen einer Datenbankmatrix.
  Wichtige Klassen, Funktionen oder Endpunkte: P3Datenbankmatrix, Volt, Projekt, User.
  Externe Abhängigkeiten: Livewire\Volt\Volt (Frontend-Rendering), App\Models\Recherche\* (Modelle für Rechercheprozess)"
}
```

---

### tests/Feature/Recherche/PhaseP4Test.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Phase P4 der Recherche-Funktion.
   - Testen von Suchstring-Erstellung und Validierung
   - Überprüfung der Speicherung von Filtern als Array
   - Testen der Bearbeitung von Suchstrings
   - Überprüfung des Löschens von Suchstrings
  ",
  "important_classes": ["P4Suchstring", "P4ThesaurusMapping", "P4Anpassungsprotokoll"],
  "external_dependencies": ["App\Models\Recherche", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/ProjektDetailTest.php
```
{
  "file_summary": "Diese Datei enthält Ladeautomatik-Tests für die Projekt-Detail-Seite. Es werden verschiedene Szenarien getestet, wie z.B. das Rendern der Standard-Tabs und das Wechseln zwischen Tabs. Die Tests prüfen auch die Rechteprüfung für nicht-Eigentümer und das Anzeigen von Phase-Komponenten. Externe Abhängigkeiten: App\Models\Recherche\Projekt, App\Models\User, Livewire\Volt\Volt."
}
```

---

### tests/Feature/Settings/AppearanceTest.php
```json
{
  "file_summary": "Diese Datei führt Funktions- und Verhaltenstests für die Einstellungsansicht durch, um sicherzustellen, dass sie wie erwartet funktioniert. Die Tests prüfen, ob alle erforderlichen Elemente (Schaltflächen, Container) korrekt angezeigt werden und ob die Seite nur für eingeloggte Benutzer zugänglich ist.",
  "wichtige_Klassen": ["User", "Volt"],
  "wichtige_Funktionen": ["test", "assertSee", "assertSeeHtml", "get", "actingAs"],
  "externe_Abhängigkeiten": ["Livewire\Volt\Volt", "\Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/Feature/Settings/PasswordUpdateTest.php
```json
{
  "file_summary": "Dokumentiert Funktionalität von Passwort-Updates. Zwei Unit-Tests sind integriert: ein Test für korrekte Passwort-Aktualisierung und einen weiteren, bei dem kein korrektes Passwort angegeben wird.",
  "key_classes": ["App\Models\User", "Illuminate\Support\Facades\Hash", "Livewire\Volt\Volt"],
  "key_functions": ["test", "actingAs", "set", "call", "assertHasNoErrors", "expect", "Hash::check"],
  "externale_abhaengigkeiten": ["factory", "Volt"]
}
```

---

### tests/Feature/Settings/ProfileUpdateTest.php
```json
{
  "file_summary": "Diese Datei enthält Funktionen für das Testen von Profil-Update-Fällen.\n\
    - Überprüft, ob die Profildaten angezeigt werden können.\n\
    - Prüft, ob Benutzerinformationen aktualisiert werden können.\n\
    - Überprüft, ob die E-Mail-Verifizierungsstatus bei unveränderter E-Mailadresse geändert wird.\n\
    - Testet, ob ein Benutzer seinen Account löschen kann.\n\
    - Prüft, ob ein falsches Passwort zur Löschung des Accounts verwendet werden kann.",
  "main_classes": ["User", "Volt"],
  "main_functions": ["updateProfileInformation", "deleteUser"],
  "external_dependencies": ["Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
```json
{
  "file_summary": "Diese Datei enthält Funktionen zum Testen der Zwei-Faktor-Authentifizierung in Laravel. Die wichtigsten Klassen sind User und Features. Externe Abhängigkeiten sind die Laravel-Fortify-Funktionen und Livewire-Volt.",
  "main_classes": ["User", "Features"],
  "main_functions": ["test", "beforeEach"],
  "external_dependencies": ["Laravel\Fortify\Features", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/WebhooksTest.php
Die Datei WebhooksTest.php enthält Funktionen für die Tests von Dashboard-Chat-Funktionalitäten in der Anwendung. Die wichtigsten Klassen und Funktionen sind Volt und Webhook. Die externe Abhängigkeit ist die Livewire-Volt-Bibliothek.

---

### tests/Unit/ExampleTest.php
Dokumentiert eine Einheitstests-Datei, die die Funktion 'true ist wahr' überprüft.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Definiert die Liste von Kontakt-Einträgen für die Ressource 'ContactResource'.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert eine Filament-Pagina für die Anzeige von Kontaktdaten. Sie erstellt eine View-Instanz für den Kontakt-Ressourcen-Typ "ContactResource" und ermöglicht das Anzeigen der Details einer bestimmten Kontakt-Entität.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Definiert eine Seite für das Bearbeiten von Benutzerdaten. Verwendet die Filament-Ressource 'UserResource'.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
```
{
  "file_summary": "Definiert eine Seite zur Auflistung von Benutzern in der Filament-Anwendung.",
  "key_classes": ["ListUsers", "UserResource"],
  "main_function": "ListRecords",
  "external_dependencies": ["Filament\Resources"]
}
```

---

### resources/views/dashboard.blade.php
```
{
  "file_summary": "Dieses Blade-Template ist die Hauptansicht für den Benutzer und besteht aus mehreren Abschnitten. Es enthält eine Überschrift, willkommene Botschaft, Schnelllinks zur Recherche und Administration sowie einen Bereich mit den letzten Projekten des Benutzers. Die Template verwendet verschiedene Klassen und Funktionen von Laravel für die Darstellung der Daten und verfügt über Zugriffe auf das Auth-System und die Modelle von 'Recherche\Projekt'.",
  "classes": ["App\Models\Contact", "App\Models\Recherche\Projekt"],
  "functions": ["route", "auth()->user()", "str()->limit()"],
  "endpunkte": ["recherche", "admin", "recherche.projekt"],
  "externe_Abhängigkeiten": ["Laravel-Auth-System", "DB-Zugriffe auf 'Recherche\Projekt'-Model"]
}
```

---

### resources/views/welcome.blade.php
Diese Datei ist die View für die Startseite der Anwendung und definiert das Layout und die Designsprache für die gesamte Oberfläche. Sie beinhaltet Meta-Tags für den Browser, Favicon, Google Fonts, Stylesheets und eine grundlegende CSS-Styling.

---

### resources/views/components/action-message.blade.php
Dies ist ein Blade-Templat, das einen Aktions-Bereich für Benutzer-Meldungen anzeigt. Es verwendet X-State und X-Transition für eine elegante Anzeige von Nachrichten nach erfolgreicher Aktion.

---

### resources/views/components/app-logo-icon.blade.php
Diese Blade-Vorlage generiert ein SVG-Icon für das Anwendungslogo. Es enthält eine SVG-Komponente mit einem Pfad, der die Logo-Form definieren sollte.

---

### resources/views/components/app-logo.blade.php
Bereitstellt das Logo und den Namen der Anwendung in einer Komponente.

---

### resources/views/components/auth-header.blade.php
```
{
  "file_summary": "Auth-Bereichs-Anzeige mit Titel und Beschreibung, 
                   wird als Teil einer UI-Komponente verwendet.",
  "key_classes": ["auth-header"],
  "important_functions": ["render auth-header-Element"],
  "external_dependencies": []
}
```

---

### resources/views/components/auth-session-status.blade.php
Präsentiert Authentifizierungs-Status in einer Blade-Vorlage. Die Funktionen beinhalten das Überprüfen des Status und die Anzeige eines grünen Hintergrundfarb-Felds mit dem Status-Text, falls der Status vorhanden ist.

---

### resources/views/components/placeholder-pattern.blade.php
Die Datei erstellt ein SVG-Musterpatterntemplate, das auf verschiedenen Größen skaliert werden kann.

---

### resources/views/components/layouts/app.blade.php
Das Blade-Templat definiert die Struktur einer Layout-Seite. Es enthält einen Sidebar und ein Hauptbereich, der von einem Slot gefüllt wird. Der Titel der Seite kann übergeben werden.

---

### resources/views/components/layouts/auth.blade.php
Layous-Component für Authentifizierung-Fenster. Einbindet einen Titel und ein Platzhalter für Inhalt.

---

### resources/views/components/layouts/app/header.blade.php
Dieses Blade-Template definiert den Header für die Anwendung. Es enthält Links zum Dashboard und zur Repository-Übersicht sowie eine mobile-Navigation.

---

### resources/views/components/layouts/app/sidebar.blade.php
Layout für das Sidebar-Component einer Webbewerbung. Enthält ein Backdrop, eine Sidebar mit Navigation und einem Logo.

---

### resources/views/components/layouts/auth/card.blade.php
Diese Datei definiert ein Authentifizierungs-Card-Komponenten, die als Teil einer Authentifizierung-Anwendung verwendet wird. Sie enthält eine Verlinkung zur Startseite und einen Platzhalter für Benutzerdaten. Die Komponente ist designorientiert und kann mit verschiedenen Stilen angepasst werden.

---

### resources/views/components/layouts/auth/simple.blade.php
```
{
  "file_summary": "Ein einfacher Layout-Template für Auth-Funktionen.
  Funktionen: 
  - Erstellt ein HTML-Dokument mit Head und Body.
  - Inkludiert die partials.head Datei in den <head>-Tag.
  - Setzt das Sprachprofil basierend auf der locale-Einstellung des Laravel-Anwendungsprozesses.
  - Definiert eine klare Design-Struktur für Auth-Funktionen mit einem Logo und einem Slot für weitere Inhalte.
  - Fügt die cookie-consent-Bibliothek hinzu.
  Die wichtigsten Klassen sind Blade-Templates und die partials.head Datei.
  Externe Abhängigkeiten: 
  - Laravel-Anwendungsprozess (locale-Einstellung)
  - Partial-Dateien
  - cookie-consent-Bibliothek"
}
```

---

### resources/views/components/layouts/auth/split.blade.php
```
{
  "file_summary": "Das Template für eine authentifizierte Seite, die sich aus zwei Bereichen zusammensetzt. Der linke Bereich enthält ein Zitat und das Logo, der rechte Bereich enthält die eigentliche Inhaltsebene.",
  "wichtige_Klassen": ["Html", "Head", "Body"],
  "wichtige_Funktionen": ["/route('home')", "/Illuminate\Foundation/Inspiring::quotes()->random()"],
  "externe_Abhängigkeiten": ["partials.head", "cookie-consent::index"]
}
```

---

### resources/views/components/settings/layout.blade.php
```
{
  "file_summary": "Layout für Einstellungen mit Navigation und Überschrift. 
Entspricht einer Komponente, die aus einer Navigation, einem Titel und einem Inhaltsbereich besteht.
Benutzt Blade-Tags wie wire:navigate und route() Funktionen."
}
```

---

### resources/views/emails/contact-confirmation.blade.php
E-Mail-Vorlage für die Bestätigung einer Kontaktanfrage. Enthält eine Anrede, eine Zusammenfassung der Anfrage und einen Abschluss.

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei dient als Template für Kontaktanfragen und stellt relevanten Informationen in einer tabellarischen Form dar. Es werden Name, E-Mail, Projekttyp, Nachricht und Zeitrahmen des Kontakts angezeigt, sowie das Datum der Anfrage. Die wichtigsten Klassen sind keine explizit genannten, da es sich um eine Vorlage für eine Benutzereingabe handelt. Externe Abhängigkeiten bestehen über die blade-Engine, welche Template-Inhalte einbindet und anzeigt.

---

### resources/views/emails/deploy-notification.blade.php
Generiert eine erfolgreiche Deploy-Benachrichtigung per E-Mail. Die Benachrichtigung enthält Informationen über das erfolgreich deployed Release, einschließlich des Zeitpunkts, der URL und der Umgebung. Die Datei verwendet die in Laravel konfigurierten Werte wie den Namen der Anwendung und die aktuelle Umgebung.

---

### resources/views/layouts/legal.blade.php
```
{
  "file_summary": "HTML-Struktur für die Anzeige von rechtlichen Informationen mit einem einheitlichen Layout.",
  "key_elements": {
    "Layout-Klassen": ["legal-wrap", "legal-nav", "legal-nav-inner"],
    "Funktionen": [Yield-Funktion zum Übergeben des Titels],
    "Externe Abhängigkeiten": ["Google Fonts"]
  }
}
```

---

### resources/views/legal/agb.blade.php
Diese Datei stellt die Allgemeinen Geschäftsbedingungen (AGB) für das Online-Spiel von Linn.Games dar. Sie regeln die Nutzung der Website und des Spiels durch registrierte Nutzer. Die AGB enthalten Bestimmungen zum Geltungsbereich, Registrierung, Nutzungsregeln, Kosten, digitalen Käufen und Haftung. Der Anbieter behält sich vor, Sanktionen gegen Verstöße zu ergreifen und den Account zu sperren.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei stellt die Datenschutzerklärung für die Website und das Spiel bereit. Sie enthält Informationen über die Verantwortliche Stelle, die Arten der verarbeiteten Daten, den Zweck und die Rechtsgrundlage der Datenverarbeitung.

---

### resources/views/legal/impressum.blade.php
Diese Datei stellt das Impressum der Website dar. Sie enthält wichtige Informationen über die Verantwortung für den Inhalt, Kontaktdaten und rechtliche Hinweise wie Datenschutzerklärung und Allgemeine Geschäftsbedingungen.

---

### resources/views/livewire/auth/confirm-password.blade.php
Lade-Seite für die Passwort-Bestätigung, enthält eine Form zum Bestätigen des Passworts und Anzeige von Benutzerstatus.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei bietet eine Benutzeroberfläche zum Zurücksetzen eines Passworts. Es handelt sich um eine einfache Email-Benutzereingabefeld, mit einem Button zum Versenden einer Passwort-Wiederherstellungs-Link. Die Seite zeigt optional einen Status des Wiederherstellungsvorgangs an.

---

### resources/views/livewire/auth/login.blade.php
Diese Datei enthält eine Login-Seite für Benutzer. Sie ist Teil der Livewire-Applikation und besteht aus einer Formularausgabe für den Nutzer, um sich anzumelden. Die wichtigsten Funktionen sind die Erfassung von E-Mail-Adresse und Passwort sowie die Option zum Speichern des Kennworts.

---

### resources/views/livewire/auth/register.blade.php
```
{
  "file_summary": "Diese Datei ist Teil der Authentifizierung von Laravel Livewire. Sie enthält eine Form für das Registrieren eines neuen Benutzers. Die wichtigsten Funktionen sind: 
  - Eine Form für die Eingabe von Name, Email-Adresse und Passwort.
  - Die Validierung dieser Eingaben durch die Funktion @error().
  - Die Verbindung zu einer Route namens 'register.store' zum Absenden der Registrierungsanfrage.
  Die wichtigsten Klassen sind: Livewire und Auth.
  Externe Abhängigkeiten: Laravel Livewire, Bootstrap CSS für die Layout- und Design-Vorlagen."
}
```

---

### resources/views/livewire/auth/reset-password.blade.php
Reset-Passwort-Seite, die ein Benutzer anbietet, sein Passwort zu ändern. Die Seite enthält eine Form, in der der Benutzer seinen neuen Passwort eingebeuten muss.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
```
{
  "file_summary": "Vorlage für die 2-Faktor-Challenge-Authentifizierung in Livewire. \
    Zeigt ein Eingabefeld für den Authentikationscode an und bietet eine Option, einen Recovery-Code einzugeben, wenn der Authentikationscode fehlerhaft ist. \
    Verwendet X-State und Livewire zum Managen des Zustands und des Datenflusses zwischen Client und Server. \
    Benutzt die Komponente <x-auth-header> für den Überschriftstext.",
  "wichtige_Klassen_Funktionen": {
    "Livewire-Component": "Die Hauptkomponente der 2-Faktor-Challenge-Authentifizierung",
    "ToggleInput()": "Funktion zum Wechsel zwischen Authentikationscode-Eingabe und Recovery-Code-Eingabe"
  },
  "externe_Abhängigkeiten": [
    "Livewire-API",
    "X-State-Engine",
    "Blade-Templating"
  ]
}
```

---

### resources/views/livewire/auth/verify-email.blade.php
Email-Verifizierungsbereich: Resend von E-Mail-Link und Logout. Hauptfunktionen sind zwei Formulare, darunter eines für den Wiederaufruf des E-Mail-Link und eines für das Abmelden.

---

### resources/views/livewire/chat/big-research-chat.blade.php
```json
{
  "file_summary": "Livewire-Komponente für einen großen Chat, der Webhooks und Benutzer-Nachrichten verarbeitet.\n\n\
* Webhook-Verbindung herstellen\n\
* Benutzer-Nachrichten abrufen und senden\n\
* Assistant-Antworten von Webhooks empfangen und anzeigen\n\
* Historie löschen"
}
```

---

### resources/views/livewire/recherche/phase-p1.blade.php
```json
{
  "file_summary": "Livewire-Komponente für die Phase P1 einer Recherche. Erfasst Strukturmodellwahl, Komponenten und Kriterien sowie Warnsignale. Verfügt über CRUD-Operationen für diese Daten.",
  "important_classes": ["P1Strukturmodellwahl", "P1Komponente", "P1Kriterium", "P1Warnsignal"],
  "external_dependencies": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p2.blade.php
```
{
  "file_summary": "Livewire-Komponente für Projekt-Phase P2 mit folgenden Funktionen:\n- Anzeige von Review-Typ-Entscheidungen\n- CRUD-Operationen für Review-Typ-Entscheidungen\n- Anzeige von Clustern\n- CRUD-Operationen für Cluster\n- Anzeige von Mapping-Suchstring-Komponenten\n- CRUD-Operationen für Mapping-Suchstring-Komponenten\n- Anzeige von Trefferlisten\n- CRUD-Operationen für Trefferlisten\nImportiert:\n- App\Models\Recherche\Projekt, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, P2Trefferliste\n- Illuminate\Support\Facades\Auth\n- Livewire\Volt\Component"
}
```

---

### resources/views/livewire/recherche/phase-p3.blade.php
```
{
  "file_summary": "Diese Datei stellt ein Livewire-Component für die Phasen 3 des Recherche-Prozesses dar. Es werden Datenbankmatrix, Disziplin, geografischer Filter und graue Literatur verarbeitet. Die wichtigsten Klassen sind Projekt, P3Datenbankmatrix, P3Disziplin, P3GeografischerFilter und P3GraueLiteratur. Externe Abhängigkeiten sind App\Models\Recherche und Illuminate\Support\Facades\Auth."
}
```

---

### resources/views/livewire/recherche/phase-p4.blade.php
```
{
  "file_summary": "Livewire-Component für die Phase P4 eines Projekts. Hier sind Funktionen zur Verwaltung von Suchstrings, Thesaurus-Mappings und Anpassungsprotokollen enthalten.
  - Klassen: Livewire\Volt\Component, App\Models\Recherche\Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll
  - Funktionen: newSs(), saveSs(), editSs()
  - Endpunkte: Livewire-Events (z.B. mount())
  - Externe Abhängigkeiten: Auth-Service, Projekt-Model"
}
```

---

### resources/views/livewire/recherche/phase-p5.blade.php
Die Datei handle die Livewire-Komponente 'phase-p5', welche verschiedene Funktionen für die Recherche in einer Datenbank bereitstellt. Sie ermöglicht das Erstellen und Verwalten von PRISMA-Zahlen, Screening-Kriterien und -Tools sowie das Durchführen von Treffer-Screening. Die Komponente ist speziell auf Benutzer mit bestimmten Rollen zugeschnitten und verfügt über Filterfunktionen. Sie verwendet die Livewire-Bibliothek und interagiert mit verschiedenen Modellen in der Datenbank.

---

### resources/views/livewire/recherche/phase-p6.blade.php
```
{
  "file_summary": "Diese Datei enthält die Livewire-Komponente 'phase-p6' für die Recherche-Phase P6. Sie verwaltet Qualitätsbewertungen und Lückenanalysen für ein bestimmtes Projekt.",
  " wichtigste_Klassen_Funktionen_Endpunkte": ["Livewire\Volt\Component", "App\Models\Recherche\\{Projekt, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse}", "P6Qualitaetsbewertung::create", "P6Qualitaetsbewertung::update"],
  "externe_Abhängigkeiten": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
Die Datei definiert ein Livewire-Component für die Projektbearbeitung in der Phase P7. Sie umfasst Eingabefelder und Formularsteuerelemente für Synthese-Methode, Datenextraktion, Muster und Konsistenz sowie GRADE-Einschätzung. Die Datei verwendet verschiedene Modelle (Projekt, SyntheseMethode, Datenextraktion, MusterKonsistenz) und Livewire-Methoden zum Verwalten der Formulare.

---

### resources/views/livewire/recherche/phase-p8.blade.php
```
{
  "file_summary": "Livewire-Component für Recherche-Phase P8. Darin: Suchprotokoll, Limitation, Reproduzierbarkeit und Update-Plan CRUD-Funktionen für Projekt-P8. Externe Abhängigkeiten: App\Models\Recherche*, Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Die Datei enthält eine Livewire-Komponente für das Anzeigen von Projekt-Details. Sie beinhaltet einen Header mit Projekttitel und Erstellungsdatum sowie Tab-Leiste mit Phasen des Projekts (Strukturierung, Review-Typ, Quellen, Suchstrings, Screening, Qualität, Synthese, Dokumentation).

---

### resources/views/livewire/recherche/projekt-liste.blade.php
```json
{
  "file_summary": "Livewire-Komponente für Projektliste: Projekte eines Benutzers aufbauen und löschen.",
  "key_classes": ["Component", "Projekt"],
  "main_functions": ["projekte", "deleteProjekt", "with"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/research-input.blade.php
```json
{
  "file_summary": "Diese Datei ist eine Livewire-Komponente, die eine einfache Recherche-Funktion bietet. Der Benutzer kann hier eine Forschungsfrage oder ein Thema eingeben und mit einem Klick starten. Die Eingabe wird dann in einer Datenbank gespeichert und ein Agent-Server getriggert.",
  "important_classes": ["Livewire\Volt\Component"],
  "key_functions": ["starteRecherche", "validate"],
  "external_dependencies": ["App\Jobs\TriggerLangdockAgent", "App\Models\Recherche\Projekt", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/settings/appearance.blade.php
Die Datei Appearance.blade.php verwaltet die Erscheinung von Benutzerkonten und ermöglicht das Ändern der Hintergrundfarbe zwischen Licht, Dunkel und dem System-Modus.

---

### resources/views/livewire/settings/delete-user-form.blade.php
```
{
  "file_summary": "Delete-Account-Funktion: Ein Formular für die Löschung eines Benutzerkontos mit Bestätigungsabfrage und Passwortschutz. Das Formular öffnet sich bei Klick auf einen Button in der Oberfläche, enthält ein Feld für das Eingeben des aktuellen Passworts und führt eine Validierung durch, bevor es den Benutzer löscht und ihn dann zur Startseite weiterleitet.",
  "key_classes": ["Livewire\Volt\Component", "Logout"],
  "main_functionality": ["Open Delete-Account-Formular", "Close Delete-Account-Formular", "Delete User"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/settings/password.blade.php
Diese Datei enthält ein Livewire-Component zur Passwortänderung eines authentifizierten Benutzers. Es wird die aktuelle Eingabeprüfung und Aktualisierung des Passworts für den aktuellen Nutzer durchgeführt.

---

### resources/views/livewire/settings/profile.blade.php
```
{
  "file_summary": "Diese Datei ist Teil der Livewire-Anwendung und dient zum Bearbeiten des Benutzerprofils. Sie besteht aus drei Hauptfunktionen: 
- Dem Mounting, bei dem die aktuellen Benutzerdaten geladen werden.
- Der Aktualisierung der Profildaten, wobei validierte Eingaben gespeichert und gegebenfalls eine E-Mail-Vereifizierung zurückgesetzt wird.
- Die Resendung einer E-Mail-Bestätigungsanfrage.
 
Wichtige Klassen sind User und die von Livewire bereitgestellten Komponenten. Es gibt außerdem Zugriffe auf Auth, Session und Rule."
}
```

---

### resources/views/livewire/settings/two-factor.blade.php
```
{
  "file_summary": "Livewire-Komponente zur Verwaltung der Zwei-Faktor-Authentifizierung eines Benutzers. Die Komponente ermöglicht das Einrichten, Überprüfen und Bestätigen der Zwei-Faktor-Authentifizierung. Die notwendigen Abhängigkeiten sind Laravel\Fortify\* und Livewire\Volt\Component.",
  " wichtigsten_Klassen": ["Livewire\Volt\Component"],
  "wichtige Funktionen": ["enable", "showVerificationIfNecessary", "confirmTwoFactor"],
  "externe_Abhängigkeiten": ["Laravel\Fortify\Actions\ConfirmTwoFactorAuthentication", "Laravel\Fortify\Actions\DisableTwoFactorAuthentication", "Laravel\Fortify\Actions\EnableTwoFactorAuthentication"]
}
```

---

### resources/views/livewire/settings/webhooks.blade.php
```
{
  "file_summary": "Diese Datei ist ein Livewire-Component, der Benutzern ermöglicht, Webhooks für Dashboard-Chats und Recherche-Starts zu erstellen bzw. zu bearbeiten.\n
Wichtige Klassen/Funktionen: \n
* Component (Livewire)\n
* Webhook (Model)\n
* Auth (Facade)
\n
Es gibt eine Validierung von Eingabedaten, sowie DB-Zugriffe zum Speichern und Aktualisieren der Webhooks."
}
```

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
Livewire-Komponente zur Darstellung und Verwaltung von zwei-Faktor-Authentifizierung-Wiederherstellungs-Codes.

---

### resources/views/partials/head.blade.php
Diese Datei definiert die HTML-Head-Sektion einer Webseite, einschließlich Titel, Meta-Tags und CSS/JS-Bundles. Sie verwendet Lokalstorage, um den dunklen Modus zu aktivieren und bindet externe Fonts von fonts.bunny.net ein.

---

### resources/views/partials/settings-heading.blade.php
Diese Datei stellt den Überschriftsteil für die Einstellungsseite dar. Sie enthält einen Titel, eine Beschreibung und einen Hr-Line.

---

### resources/views/recherche/index.blade.php
Blade-Vorlage für die Recherche-Seite. Einbinden von Livewire-Komponenten für Eingabefelder und Projektlisten.

---

### resources/views/recherche/show.blade.php
Liefert die Details eines Projekts in einer Projekt-Detail-Ansicht, wird von einer Layout-Template verwendet und verbindet sich mit der Livewire-Komponente "projekt-detail".

---

## uncategorized

### Readme.md
```
{
  "file_summary": "Laravel-Anwendung für systematische Literaturrecherche mit KI-gestützter Forschungsfrage-Eingabe. 
  - Authentifizierung und Benutzerverwaltung via Fortify.
  - Systematische Literaturrecherche in 8 Phasen, inklusive Vektor-Embeddings via pgvector.
  - Admin-Panel (Filament) für Kontaktanfragen, Benutzerverwaltung und Rollen-Berechtigungen.
  - Kontaktformular mit Rate-Limiting, Bestätigungsmail und Team-Benachrichtigung.
  - CI/CD-Prozess mit automatisiertem Deployment über `deploy.sh`.
  - Docker-Services: `web`, `php-fpm`, `php-cli`, `php-test`, `queue-worker`, `postgres`, `redis`."
}
```

---

### artisan
```json
{
  "file_summary": "Dies ist der Haupt-Einstiegspunkt für die Laravel-Application, 
                    er lädt die Composer-Autoloader und bootet die Anwendung. 
                    Der Command-Handler wird dann ausgeführt und das Ergebnis wird zurückgegeben."
}
```

---

### basemodel.md
PostgreSQL-Datenbankmodell für Systematische Literaturrecherche mit 8 Phasen (P1–P8). Es werden verschiedene Typen wie Phase-Status, Review-Typen und Strukturmodelle definiert. Die Modellierung umfasst Tabellen wie 'projekte' und 'phasen', die die verschiedenen Phasen eines Projekts abbilden und wichtige Informationen über das Projekt speichern.

---

### composer.json
Composer-Datei für Laravel-Projekt. Definiert Abhängigkeiten und Konfigurationen für den Aufbau des Projekts.

---

### CONTRIBUTING.md
Diese Datei beschreibt die Konventionen und Best Practices für das Contribueren an dem Projekt app.linn.games. Sie umfasst die Branch-Konvention, den Merge-Fluss, Arbeitsablauf pro Issue, lokale Entwicklung, Coding-Standards, Datenbank-Änderungen und Tests.

---

### deploy.sh
Dieses Skript (deploy.sh) ist für die automatisierte Bereitstellung einer Anwendung verantwortlich. Es überprüft, ob alle Voraussetzungen erfüllt sind, baut die Produktion-Images, startet die Infrastruktur und führt Datenbankmigrationen durch. Abschließend werden Cache und Anwendungsstatus überprüft und eine Benachrichtigungs-E-Mail an die Administratoren gesendet.

---

### package.json
Verwaltet Abhängigkeiten und Skripte für Vite-Entwicklungsumgebung.
	- Paketabhängigkeiten (z.B. Axios, Tailwindcss) verwaltet.
	- Entwicklungsskripte (z.B. "build", "dev") definiert.
	- Optional Dependencies für bestimmte Plattformen.

---

### phpunit.xml
```
{
  "file_summary": "PHPUnit-Konfigurationsdatei zur Durchführung von Unit- und Feature-Tests. 
                   Konfiguriert Umgebungsvariablen, Test-Suiten und Quellcode-Inklusionspfade.",
  " wichtigste_Klassen_Funktionen_Oder_Endpunkte": "PHPUnit, Testsuite, PHPUnit-Konfiguration",
  "externe_Abhängigkeiten": "vendor/autoload.php (Autoloader), PHPUnit-XSD (Schema-Definition)"
}
```

---

### vite.config.js
Vite-Konfiguration für Laravel-Projekt. Integriert TailwindCSS und die laravel-vite-Plugin. Konfiguriert Vite-Server mit CORS und Watch-Funktionen.

---

### .dockerignore
Dies ist eine Dockerignore-Datei, die bestimmte Dateien und Verzeichnisse aus der Docker-Build-Prozess ausschließt. Sie enthält Pfadangaben zu git-Verzeichnissen, node_Modules, Framework-Cache, Session- und View-Store sowie Build- und Hot-Folder im öffentlichen Verzeichnis. Außerdem werden bestimmte Dateitypen wie Logdateien, Datenbankdateien und Docker-Konfigurationsdateien ausgeschlossen.

---

### .editorconfig
Diese Datei definiert die Editor-Formatierung für das Projekt.

---

### app/Actions/Fortify/CreateNewUser.php
Diese Datei implementiert den "CreateNewUser"-Endpunkt, um neue Benutzer in der Anwendung anzulegen. Sie überprüft die Eingaben auf Gültigkeit und erstellt einen neuen Benutzer, falls alle Informationen korrekt sind.

---

### app/Actions/Fortify/PasswordValidationRules.php
Die Datei definiert die Regeln für die Validierung von Passwörtern. Sie enthält eine Funktion zur Rückschluss von Validierungsregeln für Passwörter und verwendet dabei den Standard-Password-Validator von Laravel.

---

### app/Actions/Fortify/ResetUserPassword.php
Diese Datei implementiert das Reset-Verfahren für Benutzerpasswörter. Sie überprüft die Eingaben, legt den Benutzer mit einem neuen Passwort fest und speichert es.

---

### app/Filament/Resources/ContactResource.php
```json
{
  "file_summary": "Diese Datei definiert eine Ressourcenklasse für Kontaktanfragen in Filament. Sie enthält Formulare, Tabellen und Seitendefinitionen. Die wichtigsten Klassen sind ContactResource, ListContactsPage und ViewContactPage. Die externe Abhängigkeit ist die App\Models\Contact-Klasse."
}
```

---

### app/Filament/Resources/UserResource.php
```json
{
  "file_summary": "Diese Datei definiert eine Ressource für Nutzer in Filament. Es handelt sich um eine Benutzerdatenbank mit Funktionen zur Anzeige, Bearbeitung und Sortierung von Nutzereinträgen. Die wichtigsten Klassen sind UserResource, Form und Table.",
  "important_classes": ["UserResource", "Form", "Table"],
  "external_dependencies": ["Filament\Forms", "Filament\Schemas\Schema", "Filament\Tables\Table"]
}
```

---

### app/Http/Middleware/TrackPageView.php
Diese Datei ist ein Middleware-Filter zum Verfolgen der Seitenaufrufe. Sie anonymisiert die IP-Adressen und zählt jede Seiteaufruf-Anzahl.

---

### app/Http/Middleware/VerifyLangdockSignature.php
Dieses Middleware-Objekt überprüft die Gültigkeit von Signaturen bei Anfragen aus Langdock. Es überprüft das Vorhandensein einer gültigen Signatur, einen gültigen Timestamp und verhindert Replay-Angriffe durch Caching.

---

### app/Http/Middleware/VerifyMcpToken.php
Middleware zum Überprüfen von MCP-Token. Prüft, ob Bearer-Tokens im Request-Header vorhanden sind und ob der Token gültig ist.

---

### app/Jobs/IngestPaperJob.php
```
{
  "file_summary": "Diese Datei enthält eine Job-Klasse für die Ingestion von Papieren. Sie verwendet den Ollama-Service, um Embeddings für Textabschnitte zu generieren und diese dann in einer Datenbank abzulegen.",
  "important_classes": [
    "IngestPaperJob"
  ],
  "function_or_endpoints": [
    "__construct",
    "handle",
    "chunkText"
  ],
  "external_dependencies": [
    "Illuminate\Bus\Queueable",
    "Illuminate\Contracts\Queue\ShouldQueue",
    "Illuminate\Foundation\Bus\Dispatchable",
    "Illuminate\Queue\InteractsWithQueue",
    "Illuminate\Queue\SerializesModels",
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Http",
    "Illuminate\Support\Facades\Log"
  ]
}
```

---

### app/Jobs/TriggerLangdockAgent.php
Diese Datei implementiert eine Job-Klasse für die Auslösung eines Agents bei Langdock. Die Klasse nimmt einen Benutzer-ID, eine Projekt-ID und eine Eingabe als Parameter entgegen. Sie versucht dann, eine Webhook-URL zu finden und stellt eine Anfrage an den Langdock-Agent. Wenn die Antwort fehlschlägt, wird ein Fehler aufgetreten.

---

### app/Livewire/Actions/Logout.php
```json
{
  "file_summary": "Diese Datei definiert eine Livewire-Aktion für den Ausloggen von Benutzern. Sie verwendete die Auth- und Session-Facade, um das Logout zu übernehmen.",
  "key_classes_functions": ["Logout"],
  "important_methods": ["__invoke", "Auth::guard('web')->logout()", "Session::invalidate()", "Session::regenerateToken()"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth", "Illuminate\Support\Facades\Session"]
}
```

---

### app/Mail/ContactConfirmationMail.php
```json
{
  "file_summary": "Diese Datei sendet eine Bestätigungs-E-Mail an einen Kontakt.",
  "key_classes_functions_endpoints": [
    "ContactConfirmationMail",
    "__construct",
    "envelope",
    "content"
  ],
  "externale_abhaengigkeiten": [
    "App\Models\Contact",
    "Illuminate\Bus\Queueable",
    "Illuminate\Mail\Mailables\Content",
    "Illuminate\Mail\Mailables\Envelope",
    "Illuminate\Queue\SerializesModels"
  ]
}
```

---

### app/Mail/ContactInquiryMail.php
```
{
  "file_summary": "Diese Datei definiert eine Mailable-Klasse für E-Mails über Kontaktanfragen. Es handelt sich um eine Queue-fähige Klasse, die ein Contact-Objekt als Konstruktorparameter benötigt. Die Klasse enthält Methoden zur Definition der Briefumschlags- und -inhaltseigenschaften.",
  "hauptklasse": "ContactInquiryMail",
  "wichtige_methoden": ["__construct", "envelope", "content"],
  "externe_abhaengigkeiten": ["App\Models\Contact", "Illuminate\Mail\Mailables\Content", "Illuminate\Mail\Mailables\Envelope"]
}
```

---

### app/Mail/DeployNotificationMail.php
```
{
  "file_summary": "Diese Datei stellt eine Mailable-Klasse dar, die für die Benachrichtigung über erfolgreiche Bereitstellungen verwendet wird. Die wichtigsten Funktionen sind der Konstruktor, welcher den Zeitpunkt der Bereitstellung und die URL des Anwendungs-Backends speichert, sowie die Methoden envelope() und content(), welche die E-Mail-Betreffszeile und die zu verwendende View definieren.",
  "key_elements": ["DeployNotificationMail", "envelope()", "content()"],
  "external_dependencies": ["Illuminate\Mail\Mailable", "Illuminate\Mail\Mailables\Content", "Illuminate\Mail\Mailables\Envelope"]
}
```

---

### app/Policies/ProjektPolicy.php
Projekt-Policy-Datei, die Berechtigungen für Projekte verwaltet. Es handelt sich um eine Policy-Klasse, die drei Hauptbereiche unterstützt: Anzeigen (view), Erstellen (create) und Bearbeiten/Auslösen von Änderungen (update/delete). Die Klasse setzt voraus, dass die Projekt-Model Instanzen vorliegen. Zu den wichtigsten Funktionen gehören 'viewAny', 'view', 'create', 'update' und 'delete'.

---

### app/Providers/AppServiceProvider.php
Dienstanbieter-Registrierung für Laravel-Anwendung. Registriert Gate-Policy und setzt HTTPS-Scheme in Produktionsumgebung.

---

### app/Providers/FortifyServiceProvider.php
```
{
  "file_summary": "Dienstanbieter für die Einführung der Fortify-Erweiterung von Laravel, um Benutzerauthentifizierung und -autorisation zu verwalten.",
  "important_classes_functions_endpoints": [
    "FortifyServiceProvider",
    "CreateNewUser",
    "ResetUserPassword",
    "RateLimiter"
  ],
  "external_dependencies": [
    "Illuminate\Http\Request",
    "Illuminate\Support\Facades\RateLimiter",
    "Laravel\Fortify\Fortify"
  ]
}
```

---

### app/Providers/VoltServiceProvider.php
```json
{
  "file_summary": "Registriert Livewire-Volt-Endpunkte und konfiguriert die Ansichtspfade für Livewire-Anwendungen.",
  "important_classes": ["VoltServiceProvider", "Volt"],
  "key_functions": ["mount", "register", "boot"],
  "external_dependencies": ["Illuminate\Support\ServiceProvider", "Livewire\Volt\Volt"]
}
```

---

### app/Providers/Filament/AdminPanelProvider.php
Durch die AdminPanelProvider-Klasse wird das Filament-Admin-Panel konfiguriert. Sie stellt den Zugriff auf das Backend bereit und legt das Standard-Sicherheitsprotokoll fest.

---

### bootstrap/app.php
```json
{
  "file_summary": "Konfiguriert die Anwendung und definiert Middleware-Regeln für Web-Routing, API-Routing und Console-Kommandos.",
  "key_classes_functions": ["Application", "TrackPageView"],
  "externale_dependencies": [
    "Illuminate\Http\Request",
    "App\Http\Middleware\TrackPageView"
  ]
}
```

---

### bootstrap/providers.php
Hier ist eine Übersicht über die Datei `bootstrap/providers.php` in 10 Zeilen oder weniger:

```json
{
  "file_summary": "Datei enthält eine Liste von Dienst-Providern für das Laravel-Anwendungszentrum.\nWichtigste Klassen: App\Providers*\nExterne Abhängigkeiten: Keine (internes Framework-Layout)"
}
```

---

### docker/common/nginx/default.conf
```
{
  "file_summary": "Dieser Nginx-Server-Konfiguration-File konfiguriert den Standard-Webserver für ein LAMP-Projekt. Es handelt sich um eine komplexe Konfiguration mit mehreren lokalen und externen Abhängigkeiten, die verschiedene Funktionen implementieren: 
    - Livewire-V3-Anpassungen
    - Static-Files-Kaching (mit bestimmten Ausnahmen)
    - Bild-Dateien mit Caching
    - Einbindung von PHP-FPM für Dynamic-Assete und Debugging-Zwecken
    - Eine spezielle Abhängigkeit zu PostgreSQL via Reverse-Proxy für eine Anwendung namens Langdock-AI"
}
```

---

### docker/common/nginx/docker-entrypoint.sh
Dies ist die Docker-Entry-Point-Skript für Nginx, das automatisch eine Konfiguration generiert und einen Authentifizierungstoken in die Nginx-Konfiguration einsetzt.

---

### docker/common/nginx/dockerfile
Diese Datei erstellt eine Docker-Image für Nginx. Sie enthält Konfigurationen, die geheime Informationen einsetzen und statische Inhalte übertragen.

---

### docker/common/nginx/nginx.conf
Diese Datei ist die Nginx-Konfiguration für eine Docker-Umgebung. Sie definiert grundlegende Einstellungen, wie Verbindungen zwischen Worker-Prozessen und das Komprimieren von Inhalten mit gzip.

---

### docker/common/php-fpm/dockerfile
Docker-Datei für die Konfiguration von PHP-FPM. Sie stellt eine umfassende Umgebung für die Entwicklung, Produktionsumgebung und Testumgebungen bereit. Die Datei installiert PHP, benötigte Erweiterungen und Bibliotheken, wie Redis, PDO_MYSQL, PDO_PGSQL usw. Außerdem werden Composer und npm für Abhängigkeiten verwaltet. Es gibt auch eine Option zum Ausführen von Tests in einer separaten Umgebung.

---

### docker/common/php-fpm/entrypoint.sh
Dieses Skript ist das Entry Point für PHP-FPM in einem Docker-Container. Es synchronisiert Frontend-Assets, wartet auf die Datenbank, läuft Migrations ab und aktualisiert Caches.

---

### docker/common/postgres/dockerfile
Dieser Dockerfile dient zur Konfiguration von PostgreSQL-Bildern. Es basiert auf dem offiziellen PostgreSQL 16 Bild und installiert zwei weitere PostgreSQL-Extensionen: Hypopg und Pgvector.

---

### docker/common/postgres/init-extensions.sql
Diese Datei initialisiert PostgreSQL-Erweiterungen für Datenbank-Statistiken und Optimierungen. Die wichtigsten Klassen, Funktionen oder Endpunkte sind nicht direkt vorhanden, aber die Datei enthält SQL-Befehle zur Erweiterungsinitalisierung von: 1) hypopg (Hybrid Query Processing), 2) pg_stat_statements (Datenbank-Statistiken) und 3) vector. Externe Abhängigkeiten sind nicht bekannt, da es sich um eine rein SQL-basierte Datei handelt.

---

### public/index.php
Laravel-Anwendungsvorlage, die Bootstrap und Request-Handling enthält. Hier wird das Autoloader-System registriert und der Application-Kontext initialisiert.

---

### public/robots.txt
Definition von Robots-Exclusion-Richtlinien für Web-Crawlers und Suchmaschinen

---

### public/.htaccess
Apache-Konfigurationsdatei zum Konfigurieren von URL-Regeln und Header-Handling. Wichtigste Funktionen: Registrierung von URL-Modifikationen, Handling von Autorisierungs- und XSRF-Tokens sowie Umleitung von URLs mit Schlusspunkt zu fehlenden.

---

### resources/css/app.css
@import Tailwindcss; Definiert Anpassungen für Laravel-Anwendung; Einrichtet Dark-Modus; Konfiguriert thematische Variablen

---

### .github/copilot-instructions.md
Diese Datei beschreibt die Funktionsweise des Projekts app.linn.games. Es handelt sich um eine Anwendung mit Backend in Laravel 12 und Frontend in Livewire/Volt, unter Verwendung von PostgreSQL als Datenbank und Redis für Cache/Session/Queue. Die Datei enthält auch Informationen zu Deploy- und CI-Prozessen sowie Architektur-Entscheidungen wie UUID als Primary Key und Custom Timestamps.

---

### .github/instructions/app_linn_games_custom.instructions.md
Dokumentation der Architektur, Konventionen und kritischen Details von app.linn.games. Enthält Projektübersicht, Stack-Informationen (Versionen), Verzeichnisstruktur und wichtige Klassen/Funktionen.

---

### .github/instructions/docker-dev-workflow.instructions.md
```
{
  "file_summary": "Diese Datei beschreibt den Docker Dev-Workflow für die Anwendung app.linn.games.
Die wichtigsten Funktionen sind:

* Code-Deployment: PHP-Code wird beim Docker-Build via COPY . /var/www in die Images gebaked (php-fpm, php-cli, queue-worker).
* Code-Deployment (Dev): app/, resources/, config/, routes/, database/, bootstrap/ werden als Bind-Mounts in die Container eingebracht.
* Static Assets: ./public ist als :ro Bind-Mount in web (nginx) eingebunden.

Die wichtigsten Klassen, Funktionen oder Endpunkte sind:

* docker-compose.yml
* docker-compose.override.yml
* docker/**, Dockerfile*

Externe Abhängigkeiten sind:

* composer.json / composer.lock
* package.json / Vite-Config
* php-fpm
* queue-worker
* postgres
* redis
* postgres-mcp
* ollama
* mcp-paper-search
* nginx

Die Datei wird zur Beschreibung des Docker Dev-Workflows für die Anwendung verwendet."
}
```

---
