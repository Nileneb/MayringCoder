---
repo: https://github.com/Nileneb/app.linn.games
commit: unknown
date: 2026-04-02T15:49:58.503935
model: codellama:latest
run_id: codellama:latest
files_total: 250
files_checked: 12
files_skipped: 0
findings_count: 17
run_time_s: 132.0
---

# RepoChecker Report — 2026-04-02_1549

## Summary

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Analysiert:** 12 Dateien
- **Geändert/Neu:** 0 / 2
- **Übersprungen (Budget):** 0
- **Findings:** 17  (🔴 0 · 🟡 0 · 🟢 10)
- **Laufzeit:** 132.0s

## Category Digest

- **api**: app/Http/Controllers/PaperRagController.php
- **config**: .env.example, config/services.php
- **data_access**: app/Models/Recherche/Projekt.php
- **tests**: tests/Feature/IngestPaperJobTest.php, tests/Feature/PaperRagTest.php
- **uncategorized**: CLAUDE.md, app/Http/Middleware/VerifyLangdockSignature.php, app/Http/Middleware/VerifyMcpToken.php, app/Jobs/IngestPaperJob.php, .claude/agents/code-quality-architect.md, .github/instructions/app_linn_games_custom.instructions.md

## Top Findings

1. 🟢 **zombie_code** — `CLAUDE.md` (Konfidenz: medium)
   > ```bash
# Development
npm run dev              # Start Vite dev server
docker compose up -d     # Start all containers (Dev mit Bind-Mounts)
```
   → Remove redundant code or clarify use case

2. 🟢 **zombie_code** — `CLAUDE.md` (Konfidenz: medium)
   > ```bash
# Testing
composer test                              # Lokal ohne Docker
docker compose run --rm php-test vendor/bin/pest  # Docker (empfohlen)
docker compose build php-test              # Nac
   → Remove redundant code or clarify use case

3. 🟢 **zombie_code** — `CLAUDE.md` (Konfidenz: medium)
   > ```bash
# Build & Deploy
npm run build              # Vite build für Production
./deploy.sh                # Manuelles Deployment (Synology NAS)
./deploy.sh --skip-build   # Ohne Docker-Rebuild
./depl
   → Remove redundant code or clarify use case

4. 🟢 **zombie_code** — `CLAUDE.md` (Konfidenz: medium)
   > ```bash
# Cache & Assets
docker compose exec php-fpm php artisan view:clear      # View-Cache leeren
docker compose exec php-fpm php artisan filament:assets # Filament-Assets neu publizieren
```
   → Remove redundant code or clarify use case

5. 🟢 **inconsistent_pattern** — `CLAUDE.md` (Konfidenz: medium)
   > ```php
DB::statement('INSERT INTO ... (embedding) VALUES (?::vector)', [$array]);
DB::select('SELECT *, 1 - (embedding <=> ?::vector) FROM ...', [$queryVector]);
```
   → Use consistent pattern for raw SQL queries


## Per-File Findings

### CLAUDE.md
*Kategorie: uncategorized*

CLAUDE.md provides guidance to Claude Code for working with this repository

- 🟢 **zombie_code** (Zeile ~None, Konfidenz: medium)
  > ````bash
# Development
npm run dev              # Start Vite dev server
docker compose up -d     # Start all containers (Dev mit Bind-Mounts)
````
  → Remove redundant code or clarify use case
- 🟢 **zombie_code** (Zeile ~None, Konfidenz: medium)
  > ````bash
# Testing
composer test                              # Lokal ohne Docker
docker compose run --rm php-test vendor/bin/pest  # Docker (empfohlen`
  → Remove redundant code or clarify use case
- 🟢 **zombie_code** (Zeile ~None, Konfidenz: medium)
  > ````bash
# Build & Deploy
npm run build              # Vite build für Production
./deploy.sh                # Manuelles Deployment (Synology NAS)
./dep`
  → Remove redundant code or clarify use case
- 🟢 **zombie_code** (Zeile ~None, Konfidenz: medium)
  > ````bash
# Cache & Assets
docker compose exec php-fpm php artisan view:clear      # View-Cache leeren
docker compose exec php-fpm php artisan filament:`
  → Remove redundant code or clarify use case
- 🟢 **inconsistent_pattern** (Zeile ~None, Konfidenz: medium)
  > ````php
DB::statement('INSERT INTO ... (embedding) VALUES (?::vector)', [$array]);
DB::select('SELECT *, 1 - (embedding <=> ?::vector) FROM ...', [$que`
  → Use consistent pattern for raw SQL queries
- 🟢 **overengineering** (Zeile ~None, Konfidenz: medium)
  > ````php
$this->authorize('update', $record);
if ($record->user_id === Auth::id()) {
  // do something...
}
````
  → Refactor redundant authorization check

---

### .env.example
*Kategorie: config*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
The `.env.example` file contains environment variables that are used by the Laravel application to configure various aspects of its behavior and func`

---

### app/Http/Controllers/PaperRagController.php
*Kategorie: api*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
* File Summary: This is a PHP class file that defines the `PaperRagController` class, which handles the ingestion of paper data into the database and`

---

### app/Http/Middleware/VerifyLangdockSignature.php
*Kategorie: uncategorized*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
The code is a PHP class that implements the `VerifyLangdockSignature` middleware for Laravel. The class is responsible for validating requests coming`

---

### app/Http/Middleware/VerifyMcpToken.php
*Kategorie: uncategorized*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
This code is a PHP file that defines a middleware class called `VerifyMcpToken` for verifying an HTTP Authorization header that contains a Bearer tok`

---

### app/Jobs/IngestPaperJob.php
*Kategorie: uncategorized*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
This code is a PHP class file for the `App\Jobs\IngestPaperJob` job, which is part of a Laravel application. The job handles sending a webhook reques`

---

### app/Models/Recherche/Projekt.php
*Kategorie: data_access*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
After analyzing the given file, I found a few potential issues:

1. Zombie code (unclear): There are several instances of `TODO` comments with no act`

---

### config/services.php
*Kategorie: config*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
This file is a configuration file for Laravel's third-party services. It defines the credentials and settings for various services such as Mailgun, P`

---

### tests/Feature/IngestPaperJobTest.php
*Kategorie: tests*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
This file contains tests for the `IngestPaperJob` class in Laravel. The tests are written using the PHPUnit framework and use the `tests` namespace t`

---

### tests/Feature/PaperRagTest.php
*Kategorie: tests*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
Diese Datei enthält Tests für die `PaperRag`-Klasse in Laravel. Die Klasse ist dafür verantwortlich, RAG-Scoring (Red, Amber, Green) für einen Satz v`

---

### .claude/agents/code-quality-architect.md
*Kategorie: uncategorized*
*⚠ Inhalt gekürzt auf 9000 Zeichen*

This file provides guidance on using Claude Code (claude.ai/code) for working with the app.linn.games repository, which is a Laravel 12 research management platform for AI-assisted systematic literature reviews.

- 🟢 **zombie_code|redundancy|inconsistent_pattern|error_handling|overengineering|security|unclear** (Zeile ~~42, Konfidenz: high|medium|low)
  > `maximal 10 Zeilen relevanter Code`
  → konkrete Handlungsempfehlung

---

### .github/instructions/app_linn_games_custom.instructions.md
*Kategorie: uncategorized*
*⚠ Inhalt gekürzt auf 9000 Zeichen*

- 🟢 **freetext** *(Explikation empfohlen)* (Zeile ~, Konfidenz: low)
  > `
This is a very comprehensive guide on how to write a secure Laravel application. It covers various aspects of security, including authentication, aut`

---

## Explikation empfohlen (manueller Re-Run)

Diese Findings haben niedrige Konfidenz. Erneut analysieren mit `--prompt prompts/explainer.md`:

- `.env.example` — **freetext**: `
The `.env.example` file contains environment variables that are used by the Laravel application to `
- `app/Http/Controllers/PaperRagController.php` — **freetext**: `
* File Summary: This is a PHP class file that defines the `PaperRagController` class, which handles`
- `app/Http/Middleware/VerifyLangdockSignature.php` — **freetext**: `
The code is a PHP class that implements the `VerifyLangdockSignature` middleware for Laravel. The c`
- `app/Http/Middleware/VerifyMcpToken.php` — **freetext**: `
This code is a PHP file that defines a middleware class called `VerifyMcpToken` for verifying an HT`
- `app/Jobs/IngestPaperJob.php` — **freetext**: `
This code is a PHP class file for the `App\Jobs\IngestPaperJob` job, which is part of a Laravel app`
- `app/Models/Recherche/Projekt.php` — **freetext**: `
After analyzing the given file, I found a few potential issues:

1. Zombie code (unclear): There ar`
- `config/services.php` — **freetext**: `
This file is a configuration file for Laravel's third-party services. It defines the credentials an`
- `tests/Feature/IngestPaperJobTest.php` — **freetext**: `
This file contains tests for the `IngestPaperJob` class in Laravel. The tests are written using the`
- `tests/Feature/PaperRagTest.php` — **freetext**: `
Diese Datei enthält Tests für die `PaperRag`-Klasse in Laravel. Die Klasse ist dafür verantwortlich`
- `.github/instructions/app_linn_games_custom.instructions.md` — **freetext**: `
This is a very comprehensive guide on how to write a secure Laravel application. It covers various `

## Empfohlene nächste Schritte

1. Remove redundant code or clarify use case
2. Use consistent pattern for raw SQL queries
3. Refactor redundant authorization check
4. konkrete Handlungsempfehlung
