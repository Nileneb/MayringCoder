---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T03:00:06.883920
model: qwen2.5-coder:7b
mode: overview
files_total: 282
errors: 0
run_time_s: 459.3
---

# Funktions-Übersicht — 2026-04-02_0300

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 282
- **Fehler:** 0
- **Laufzeit:** 459.3s

## api

### app/Http/Controllers/ContactController.php
Die Datei implementiert einen Controller für die Kontaktaufnahme. Sie verarbeitet Anfragen, speichert sie in der Datenbank und sendet Bestätigungsmails an den Absender und die Linn.Games-Adresse.

---

### app/Http/Controllers/Controller.php
Abstrakte Klasse für alle API-Controller im Projekt.

---

### app/Http/Controllers/DsgvoController.php
Dieser Controller bietet API-Endpunkte zur Exportierung von Benutzerdaten gemäß DSGVO und zum Löschen des Benutzerkontos.

---

### app/Http/Controllers/LangdockWebhookController.php
Handhabt eingehende Webhooks, validiert Anfragen und dispatcht Jobs an den Langdock-Agenten.

---

### app/Http/Controllers/PaperRagController.php
API-Controller für das Ingestion und Suchen von Papierartikeln. Unterstützt die Endpunkte 'ingest' und 'search'.

---

## config

### docker-compose.dev.yml
Konfigurationsdatei zur Entwicklungsumgebung, die den nginx-Port von 6481 auf 6480 ändert.

---

### docker-compose.yml
Konfiguration für eine Docker-compose-Umgebung mit diversen Services wie web, php-fpm, queue-worker, postgres usw. Jeder Service hat seine eigenen Build-Kontexte, Volumes und Abhängigkeiten.

---

### .env.example
Konfigurationsdatei für Laravel-Anwendungen, enthält Einstellungen für die App-Name, Umgebung, Datenbankverbindung, Cache, Sessions, Mail und externe Dienste.

---

### docker/common/php-fpm/php-dev.ini
Konfigurationsdatei für PHP-FPM im Entwicklungsmodus, aktiviert via Bind-Mount. Aktiviert OPcache-Timestamp-Check und Error Display.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Konfiguration für einen Bug-Report-Issue-Templat mit Markdown, Textareas und Dropdowns.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Konfigurationsdatei für ein Issue-Template zur Vorschlag von neuen Features in einem Projekt.

---

### .github/ISSUE_TEMPLATE/refactor.yml
Konfigurationsdatei für GitHub Issue Templates zur Code-Verbesserung (Refactoring)

---

### .github/workflows/lint.yml
GitHub Actions workflow for PHP and npm dependencies installation, and running Pint for code style checks.

---

### .github/workflows/tests.yml
Konfiguration für GitHub Actions Workflow zur Ausführung von Tests. Beinhaltet Schritte wie Checkout, PHP und NodeJS Setup, Abhängigkeiten installieren, Umgebung konfigurieren und Tests durchführen.

---

## data_access

### app/Models/ChatMessage.php
Definiert das ChatMessage-Modell mit Eloquent ORM, um Chat-Nachrichten in der Datenbank zu verwalten. Stellt Beziehungen zu User und Webhook her.

---

### app/Models/Consent.php
Definiert ein Eloquent-Modell für die Zustimmungskonzepte in der Datenbank, einschließlich Benutzer-ID, anonymen IP-Adresse, Zustimmungs-Kategorien und Datum der Zustimmung oder Widerruf.

---

### app/Models/Contact.php
Definiert eine Eloquent-MODEL für den Contact-Datensatz in einer Laravel-Anwendung, um Datenbankoperationen durchzuführen.

---

### app/Models/PageView.php
Die Datei definiert eine Eloquent-Model-Klasse für die Datenbank-Tabelle 'page_views', die verwendet wird, um Informationen über die Besucher einer Webseite zu speichern.

---

### app/Models/User.php
Definiert eine Eloquent-Model-Klasse für Benutzer mit Authentifizierung, Rollenverwaltung, Aktivitätseinzelbenachrichtigungen und Zustandsabfrage-Funktionen.

---

### app/Models/Webhook.php
Die Datei 'Webhook.php' definiert eine Eloquent-Model-Klasse für Webhooks, die in einer Anwendung verwendet wird. Sie verwalten die Datenbanktabelle 'webhooks', bieten Methoden zur Erstellung der Aufruf-URL inklusive des geheimen Schlüssels und haben Beziehungen zu dem Benutzer und Chatnachrichtenmodellen.

---

### database/migrations/0001_01_01_000000_create_users_table.php
Die Datei erstellt und löscht die Datenbanktabellen für Benutzer, Passwort-Zurücksetzungen und Sitzungen.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Datei erstellt und verwaltet die Migrationen für die Cache-Tabelle und den Cache-Lock-Tabelle.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Die Datei enthält Migrationen zur Erstellung von Tabellen für Jobs, Job-Batches und fehlgeschlagene Jobs in einer Datenbank. Sie erstellt Strukturdefinitionen mit den entsprechenden Spalten und Indexen.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
Diese Datei enthält Migrationen zur Erweiterung der users-Tabelle um Zwei-Faktor-Authentifizierungsfelder.

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Dieses PHP-Skript erstellt und löscht die 'contacts' Tabelle im Datenbank-Schema. Die Tabelle enthält Informationen zu Kontakten, einschließlich Namen, Firmenangaben, E-Mails, Projekttypen, Nachrichten, Zeiträumen und Zeitstempeln.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Die Datei erstellt und löscht eine Datenbank-Tabelle 'page_views' für die Speicherung der Seitenansichten.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Erstellt Tabellen für Berechtigungen und Rollen in der Datenbank.

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Migration adds details to the page_views table including path, ip_anonymous, and user_agent fields. Provides up() and down() methods for database schema changes.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Dieses Skript erstellt eine Datenbanktabelle für Zustimmungen mit Benutzer-ID, IP-Adresse, Zustimmungskategorien und Zeiten.

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
Die Datei enthält Migrationskoden für SQLite, um Tabellen in einer Datenbank zu erstellen oder zu aktualisieren. Sie fügt Tabelle 'users' mit einer neuen Spalte 'status' hinzu und erstellt Tabellen 'projekte', 'phasen' und 'p5_treffer'. Die Migration wird nur ausgeführt, wenn der Datenbanktreiber nicht PostgreSQL ist.

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Migrates PostgreSQL database to include UUID and trigram extensions, creates custom ENUM types for various review phases and criteria.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Dieses PHP-Skript erstellt und verwaltet PostgreSQL-Tabelle 'projekte' und 'phasen'. Es fügt zusätzliche Spalten hinzu, setzt Constraints und DefaultValue. Die Migration ist DBMS-abhängig.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
Erstellt PostgreSQL-Tabellen für P1-Datenmodelle wie Strukturmodell, Komponenten, Kriterien und Warnsignale. Stellt sicher, dass externe Tabellen nicht neu erstellt werden.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Erstellt und modifiziert PostgreSQL-Tabellen für P2-Review-Typ-Einscheidung, Komponenten-Mapping-Suchstring und Trefferlisten. Führt DB-Zugriffe durch.

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Diese Datei ist eine Datenbank-Migration für PostgreSQL, die Tabellen 'p3_disziplinen', 'p3_datenbankmatrix', 'p3_geografische_filter' und 'p3_graue_literatur' erstellt. Jede Tabelle hat eine UUID als Primärschlüssel und foreign keys zur Tabelle 'projekte'. Es werden auch Constraints für die Spalten 'art' in 'p3_disziplinen' und 'zugang' in 'p3_datenbankmatrix' definiert.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
This file contains database migration scripts for PostgreSQL. It creates three tables: p4_suchstrings, p4_thesaurus_mapping, and p4_anpassungsprotokoll. Each table is associated with a foreign key to the 'projekte' table. The 'up' method includes checks for the PostgreSQL driver and table existence before creating new tables. The 'down' method includes checks for the PostgreSQL driver and drops existing tables.

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Dieses PHP-Skript ist ein Datenbank-Migrationsskript für PostgreSQL. Es erstellt drei Tabellen: 'p5_prisma_zahlen', 'p5_screening_kriterien' und 'p5_treffer'. Jede Tabelle hat eine UUID als Primärschlüssel, außer bei den Screening-Kriterien werden ENUMs verwendet. Die Skripte überprüfen, ob die Tabellen bereits existieren, bevor sie erstellt werden.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
Erstellt Tabellen `p6_qualitaetsbewertung` und `p6_luckenanalyse` für PostgreSQL, um Qualitätserwertungen und Lückenanalysen zu speichern. Enthält Migrationen für die Erstellung und Rückgabe der Tabellen.

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Dieses PHP-Skript erstellt PostgreSQL-Tabelle迁移文件，定义了多个数据表结构，包括p7_synthese_methode、p7_datenextraktion、p7_muster_konsistenz和p7_grade_einschaetzung，并添加了外键约束和特定列类型。

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Erstellt Tabellen für P8-Datenbanken (suchprotokoll, limitationen, reproduzierbarkeitsprüfung, update_plan) in PostgreSQL.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Erstellt oder löscht PostgreSQL-Indizes für verschiedene Tabellen und Sichten, einschließlich Full-Text-Suche auf Titeln und Abstrakten.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Diese Datei enthält eine Migration für PostgreSQL, die eine Funktion namens berechne_prisma_zahlen erstellt. Die Funktion berechnet Statistiken für Projekte basierend auf Entscheidungen in der Tabelle p5_screening_entscheidungen.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Migration to create the 'chat_messages' table with PostgreSQL-specific constraints.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Migrates the database to create a 'webhooks' table and add a 'webhook_id' column to 'chat_messages'. PostgreSQL-specific UUID generation and constraints.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Erstellt und migriert die Tabelle 'paper_embeddings' für PostgreSQL, einschließlich Erweiterungen, Indizes und Fremdschlüssel.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
Migration to create the activity_log table for logging user activities.

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
Die Datei erstellt eine PostgreSQL-ENUM-Typen `user_status` und fügt dem `users`-Tabelle das `status`-Feld hinzu. Bei Rückwirkung wird das Feld entfernt und der ENUM-Typ gelöscht.

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Erstellt eine PostgreSQL-Enum-Typ und sperrt die Tabelle 'webhooks' für das Hinzufügen eines neuen Feldes 'frontend_object'.

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
Migrates the 'webhooks' table by making the 'password' column nullable and setting a default value.

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
Führt PostgreSQL-spezifische Migrationen durch, um eine neue Spalte 'secret' zu einer Tabelle 'webhooks'.

---

## temp_dummy

### .github/pull_request_template.md
Dies ist ein Vorlage für Pull-Request-Beschreibungen in GitHub. Sie enthält einen Beschreibungsfeld, eine Änderungstypen-Auswahl, eine Überprüfungszettel und Anweisungen für Deploy-Hinweise.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Klasse ListContacts erbt von ListRecords, um eine Seite zum Anzeigen der Kontakte im Filament Admin UI zu erstellen.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Erlaubt das Anzeigen eines Kontakts über die Benutzeroberfläche.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Klasse zur Bearbeitung des Benutzerprofils.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Diese Datei definiert eine Seite zum Anzeigen einer Liste von Benutzern im Filament-Admin-Bereich.

---

### public/js/filament/forms/components/checkbox-list.js
Diese Datei enthält eine Komponente für eine Checkbox-Liste mit Livewire-Bindungen. Sie bietet Funktionen zum Überprüfen der Auswahl aller Optionen, dem Umschalten von alleinigen und dem Filtern nach Suchbegriffen.

---

### public/js/filament/forms/components/code-editor.js
Diese Datei enthält JavaScript-Code für eine Code-Editor-Komponente. Sie verarbeitet Texteingaben und bietet Funktionen zur Syntaxhighlightierung, Zeilenumbrüche und Textmanipulation.

---

### public/js/filament/forms/components/color-picker.js
Dieses JavaScript-Modul implementiert ein Farbpicker, das sowohl RGB als auch HSL-Farbmodelle unterstützt. Es bietet Funktionen zur Konvertierung zwischen verschiedenen Farbformaten und ermöglicht die Interaktion mit einer Benutzeroberfläche.

---

### public/js/filament/forms/components/date-time-picker.js
Die Datei 'date-time-picker.js' ist ein JavaScript-Modul, das dayjs Bibliothek verwendet um erweiterte Formatierung und Analyse von Datum- und Zeitangaben bereitzustellen. Das Modul enthält Klassen und Funktionen für die Erstellung eines benutzerfreundlichen Datums- und Uhrzeit-Wählers.

---

### public/js/filament/forms/components/file-upload.js
Diese Datei enthält Komponenten und Funktionen für ein Datei-Upload-Formular in einer UI-Anwendung. Sie verarbeitet Benutzerinteraktionen, validiert Eingaben, aktualisiert den Uploadstatus und kommuniziert mit einem Backend-Service.

---

### public/js/filament/forms/components/key-value.js
Eine Vue.js Komponente für das Anzeigen und Verwalten von Schlüssel-Wert-Paaren. Stellt Funktionen zur Hinzufügung, Löschen und Umordnen von Einträgen bereit.

---

### public/js/filament/forms/components/markdown-editor.js
Diese Datei enthält den JavaScript-Code für eine Markdown-Editor-Komponente in einer UI-Anwendung. Sie definiert Klassen und Funktionen zur Erstellung und Verwaltung des Editors, einschließlich HTML-Manipulation, Benutzerinteraktionen und Textformatierung.

---

### public/js/filament/forms/components/rich-editor.js
Klasse `ge` implementiert einen einfachen Datenstruktur zur Verwaltung von Schlüssel-Wert-Paaren. Kritische Funktionen sind `find`, `get`, `update`, `remove`, `addToStart`, `addToEnd` und `addBefore`. Die Klasse bietet auch Methoden zur Transformation und Iteration über die Inhalte. Externe Abhängigkeiten: Keine. 

---

### public/js/filament/forms/components/select.js
Dieses Skript enthält Funktionen zur Berechnung von Positionierung und Ausrichtung von UI-Elementen wie Select-Listen. Es bietet Middleware für die Platzierung der Floating-UI-Componennten, berücksichtigt Layout-Beschränkungen und Runtimespezifische Eigenschaften.

---

### public/js/filament/forms/components/slider.js
JavaScript-Datei für ein Schiebereglerkomponenten in einer UI. Bietet Funktionen zur Initialisierung, Steuerung und Darstellung des Schiebereglers.

---

### public/js/filament/forms/components/tags-input.js
Eine Vue.js Komponente für eine Tags-Eingabe mit Erstellung, Löschen und Umordnen von Tags.

---

### public/js/filament/forms/components/textarea.js
Eine React-Komponente für eine automatisch anpassende Textarea. Beinhaltet Funktionen zur Initialisierung, Größe setzen und Anpassung bei Änderungen.

---

### public/js/filament/schemas/components/actions.js
Diese Datei enthält eine JavaScript-Komponente, die das Verhalten von Elementen steuert, insbesondere deren Breite und Ankerposition (sticky). Sie verwendet ResizeObserver für宽度更新 und eventuell externe Stylesheets.

---

### public/js/filament/schemas/components/tabs.js
Diese Datei enthält eine JavaScript-Komponente für Tabs mit Funktionen zur Initialisierung, Berechnungen von Tabbreiten und Positionen sowie Update der QueryString. Sie verwendet externe Abhängigkeiten wie Livewire.

---

### public/js/filament/schemas/components/wizard.js
Eine Klasse für einen Schrittweiser Wizard mit Funktionen zur Navigation zwischen Schritten und Aktualisierung der Query-String.

---

### public/js/filament/widgets/components/chart.js
Die Datei enthält Funktionen zur Verarbeitung von Farben, einschließlich Konvertierungen zwischen verschiedenen Farbformaten und Berechnungen für Farbeffekte.

---

### public/js/forms/components/checkbox-list.js
Diese Datei enthält eine Vue.js Komponente für eine Checkbox-Liste. Sie verwalten die Zustände der Checkboxen, aktualisieren die sichtbaren Optionen basierend auf Suchbegriffen und reagieren auf Livewire-Commit-Ereignisse.

---

### public/js/forms/components/code-editor.js
JavaScript-Datei für einen Code-Editor mit Funktionen zur Textverarbeitung und Editor-Funktionalitäten.

---

### public/js/forms/components/color-picker.js
Die Datei `color-picker.js` enthält Funktionen zur Konvertierung von Farben zwischen verschiedenen Formaten wie RGB, RGBA, HSL, HSLA und Hex. Sie bietet auch Tools für den Erkennen der Benutzerinteraktion mit Farbpicker-Elementen und das Dispatching von benutzerdefinierten Ereignissen.

---

### public/js/forms/components/date-time-picker.js
Dieses Skript bereitstellt Plugins für das JavaScript-Bibliothek day.js um fortgeschrittene Datumsformate und benutzerdefinierte Parse-Formate zu unterstützen.

---

### public/js/forms/components/file-upload.js
Die Datei 'file-upload.js' implementiert eine benutzerdefinierte Datei-Upload-Komponente mit Funktionen zur Verwaltung von Upload-Prozessen, Optionen und Plugins. Sie bietet Methoden zur Erstellung, Destruktion, Aktualisierung und Abfrage des Zustands des Uploads sowie zum Registrieren und Verwalten von Plugins.

---

### public/js/forms/components/key-value.js
Diese Datei enthält eine JavaScript-Komponente für ein Key-Value-Formular. Sie verwalten Zeilen mit Schlüsseln und Werten, bietet Funktionen zur Hinzufügung, Entfernung und Umordnen von Zeilen sowie zum Aktualisieren des Zustands.

---

### public/js/forms/components/markdown-editor.js
Diese Datei enthält den JavaScript-Code für eine Markdown-Editor-Komponente. Sie definiert Klassen und Funktionen zur Erstellung und Verwaltung des Editors, einschließlich der Unterstützung von Syntax-Highlighting, Vorschau und Textverarbeitung.

---

### public/js/forms/components/rich-editor.js
Die Datei rich-editor.js enthält eine benutzerdefinierte Klasse `ge` zur Verwaltung von inlinedaten im Rich-Text-Editor. Sie bietet Funktionen für das Einfügen, Entfernen und Aktualisieren von Elementen, sowie die Konvertierung zwischen verschiedenen Datenstrukturen.

---

### public/js/forms/components/select.js
Dieser Datei enthält eine Reihe von Funktionen und Klassen zur Berechnung der Positionierung von UI-Elementen, insbesondere für Dropdown-Menüs oder Selektoren. Die wichtigsten Funktionen umfassen die Ermittlung des optimalen Platzierungsverhaltens basierend auf dem Ausrichtungsmodus, den Rändern der Elemente und der Richtung (links/rechts, oben/unten). Sie verwendet externe Abhängigkeiten wie `getBoundingClientRect`, um die Positionierung der UI-Elemente zu berechnen.

---

### public/js/forms/components/slider.js
Implementiert einen Schieberegler mit Optionen für Bereich, Schritte und Werte. Stellt Funktionen zur Steuerung der Interaktion und Anzeige des Schiebereglers bereit.

---

### public/js/forms/components/tags-input.js
Eine Vue.js-Komponente zur Erstellung und Verwaltung von Tags. Stellt Funktionen für das Hinzufügen, Entfernen und Neuordnen von Tags bereit.

---

### public/js/forms/components/textarea.js
Diese Datei enthält eine Komponente für einen Textarea-Element, die automatische Anpassung der Höhe basierend auf dem Inhalt unterstützt. Die Komponente verwendet Vue.js.

---

### public/js/schemas/components/actions.js
Definiert eine Klasse zur Steuerung der Position und Größe eines Elements, einschließlich einer Anpassung auf Resize-Ereignisse und Sticky-Positionierung.

---

### public/js/schemas/components/tabs.js
Eine Komponente zur Verwaltung von Tabs mit Funktionen zur Initialisierung, QueryString-Verwaltung und Anpassung an Dropdowns.

---

### public/js/schemas/components/wizard.js
Diese Datei enthält eine Vue.js-Komponente für einen Schritt-Ansicht-Assistenten. Sie verwalten die Schritte, den Fortschritt und die Interaktion des Benutzers.

---

### public/js/widgets/components/chart.js
Diese Datei implementiert eine Farbverarbeitungslib mit Funktionen für RGB, HSLA und HEX-Konvertierung.

---

### resources/views/dashboard.blade.php
Die Datei `dashboard.blade.php` ist eine Blade-Vorlage für das Forschungs-Dashboard. Sie enthält die Struktur und Inhalte des Dashboards, wie Willkommenstext, Schnelllinks zu Recherchen und Admin-Funktionen sowie eine Liste der letzten Projekte des Benutzers.

---

### resources/views/welcome.blade.php
Diese Datei ist ein Blade-Template für die WELCOME-Seite von Linn Games. Es enthält HTML, CSS und Metadaten zur Anzeige des Logos, der Google Fonts und grundlegender Stile. External: None

---

### resources/views/components/action-message.blade.php
Komponente für Anzeige einer Aktionnachricht mit automatischer Versteigerung.

---

### resources/views/components/app-logo-icon.blade.php
Rendert ein SVG-Bild eines App-Logos.

---

### resources/views/components/app-logo.blade.php
zeigt das Logo und den Namen des Anwendungsfensters an

---

### resources/views/components/auth-header.blade.php
Die Datei auth-header.blade.php definiert einen Blade-Zeiger für eine Authentifizierungsüberschrift, die einen Titel und eine Beschreibung anzeigt. Sie verwendet keine externen Abhängigkeiten.

---

### resources/views/components/auth-session-status.blade.php
Dieses Blade-Vorlage zeigt den Authentifizierungsstatus, wenn vorhanden.

---

### resources/views/components/placeholder-pattern.blade.php
Generiert ein SVG-Muster mit einem Liniendiagramm in einem Muster.

---

## uncategorized

### Readme.md
Linn.Games - Laravel Anwendung für die systematische Literaturrecherche mit KI-gestütztem Agenten und Admin-Panel.

---

### artisan
Startet die Laravel-Kommandozeilenanwendung, verarbeitet eingehende Befehle und beendet den Prozess mit dem Exit-Status.

---

### basemodel.md
Erstellt ein PostgreSQL-Datenschema für Systematische Literaturrecherche. Definiert ENUM-Typen und Tabellen für Projekte, Phasen, Einzelschritte, Bewertungen und Ergebnisse.

---

### composer.json
Diese Datei definiert die Abhängigkeiten und Konfigurationen für ein Laravel-Projekt namens 'app-linn-games'. Sie enthält Listen von erforderlichen, optionalen und Entwickler-abhängigen Paketen, assoziierte Autoload-pfade sowie Skripte zur Setup- und Entwicklungsumgebung. Die Datei ist spezifisch für ein Laravel-Framework-Projekt mit zusätzlichen Komponenten wie Filament UI, Fortify, Sanctum und anderen Tools.

---

### composer.lock
composer.lock - Speichert abhängige Pakete für PHP-Projekt

---

### CONTRIBUTING.md
Enthält Richtlinien für die Softwareentwicklung, einschließlich Branch-Konventionen, Merge-Fluss, Arbeitsablauf pro Issue, Lokale Entwicklung und Coding-Standards.

---

### deploy.sh
Shell script for deploying an application using Docker Compose, handling build, migration, and notification.

---

### package.json
Die Datei 'package.json' enthält die Abhängigkeiten und Skripte für das Projekt.

---

### phpunit.xml
Konfiguration für PHPUnit-Testumgebung mit Bootstrapping, Farbenansicht und Test-Suites

---

### vite.config.js
Konfiguration für Vite mit Laravel-Plugin und TailwindCSS. Ermöglicht die Verwendung von CSS und JavaScript in Laravel-Projekten.

---

### .dockerignore
Diese Datei ist ein Dockerignore-File, das spezifische Verzeichnisse und Dateien aus dem Docker-Build-Prozess ausschließt.

---

### .editorconfig
Konfigurationsdatei für Editor-Regeln

---

### app/Actions/Fortify/CreateNewUser.php
Diese Datei implementiert die Erstellung neuer Benutzer. Sie verwendet Laravel-Fortify zur Validierung und Erstellung von Benutzern in der Datenbank.

---

### app/Actions/Fortify/PasswordValidationRules.php
Definiert eine Trait für Passwortvalidierung, die standardmäßige Regel zur Anforderung von Passwörtern und deren Bestätigung bereitstellt.

---

### app/Actions/Fortify/ResetUserPassword.php
Die Datei implementiert das PasswordReset-Interface und bietet eine Methode zur Validierung und Aktualisierung eines Benutzers Passwortes.

---

### app/Filament/Resources/ContactResource.php
Definiert eine Filament-Ressource für die Anzeige und Verwaltung von Kontaktanfragen. Beinhaltet Formular- und Tabellendefinitionen sowie Seiten für die Liste und Detailansicht.

---

### app/Filament/Resources/UserResource.php
Definiert eine Filament-Ressource für den Benutzer-Modell mit Formular- und Tabellenansichten.

---

### app/Http/Middleware/TrackPageView.php
Middleware zur Verfolgung von Seitenansichten. Speichert Anzeige-Daten in der Datenbank und ignoriert Anfragen von Bots.

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware zum Überprüfen der Langdock-Signatur und -Zeitstempel. Stellt sicher, dass Anfragen authentifiziert sind und keine Wiederholungen erlauben.

---

### app/Http/Middleware/VerifyMcpToken.php
Middleware zur Überprüfung eines MCP-Authentifizierungstokens in den HTTP-Anfragen.

---

### app/Jobs/IngestPaperJob.php
Die Datei `IngestPaperJob.php` ist ein Laravel-Job, der einen Papier-Text in Text-Chunks unterteilt und diese an eine externe API (Ollama) sendet, um Eckenvektoren zu generieren. Die Ergebnisse werden in einer Datenbank gespeichert.

---

### app/Jobs/TriggerLangdockAgent.php
TriggerLangdockAgent job dispatches a request to the Langdock API when triggered. It logs errors and retries up to three times with a backoff.

---

### app/Livewire/Actions/Logout.php
Die Datei Logout.php enthält die Livewire-Aktion zur Abmeldung des aktuellen Benutzers. Sie führt den Logout-Vorgang durch, indem sie die Authentifizierung für das 'web'-Guard aufhebt und die Session ungültigt macht. Anschließend wird der Benutzer umgeleitet zur Startseite.

---

### app/Mail/ContactConfirmationMail.php
Eine Klasse zum Versenden einer Bestätigungsmail für Kontakte. Verwendet Laravel-Mailer, Queueable und Models.

---

### app/Mail/ContactInquiryMail.php
Diese Datei definiert eine Mailable-Klasse zur Verarbeitung von Kontaktanfragen per E-Mail. Sie nutzt die Illuminate-Mailing-Framework-Komponenten und ein Contact-Model.

---

### app/Mail/DeployNotificationMail.php
Dieses PHP-Skript definiert eine Klasse für die Erstellung von E-Mails zur Benachrichtigung über erfolgreiche Bereitstellungen. Es verwendet Laravel's Mailable-Klasse und enthält Konstruktorparametern für die Bereitstellungszeit und die Anwendungs-URL.

---

### app/Models/Recherche/P1Komponente.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p1_komponenten', welche die Komponenten eines Projekts repräsentiert. Unterstützt UUIDs und hat eine Beziehung zu einem Projekt.

---

### app/Models/Recherche/P1Kriterium.php
Definiert ein Eloquent-Model für die Tabelle 'p1_kriterien'. Stellt eine Beziehung zu dem Projekt-Model. Enthält Attribute wie projekt_id, kriterium_typ, beschreibung, begründung und quellbezug.

---

### app/Models/Recherche/P1Strukturmodellwahl.php
Definiert das Eloquent-Model für die Tabelle p1_strukturmodell_wahl, umstrukturiert in Klassenspezifikationen und Beziehungen.

---

### app/Models/Recherche/P1Warnsignal.php
Definiert ein Eloquent-Model für P1-Warnsignale mit Beziehung zu Projekten.

---

### app/Models/Recherche/P2Cluster.php
Definiert ein Eloquent-Model für die Tabelle p2_cluster mit Uuid, ohne Timestamps und spezifischen Attributen.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Definiert ein Eloquent-Modell für die Tabelle p2_mapping_suchstring_komponenten, mit Beziehungen zu Projekten.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Definiert eine Eloquent-Model-Klasse für die Tabelle p2_review_typ_entscheidung mit Beziehungen zu Projekten und Validierungsfeldern.

---

### app/Models/Recherche/P2Trefferliste.php
Die Datei definiert ein Eloquent-Modell für P2Trefferlisten in einer Datenbank. Es enthält Methoden zur Verbindung mit dem Projektmodell und setzt externe Abhängigkeiten von Laravel's Eloquent ORM.

---

### app/Models/Recherche/P3Datenbankmatrix.php
Die Datei definiert eine Eloquent-Model-Klasse für die Tabelle 'p3_datenbankmatrix'. Sie enthält Eigenschaften wie 'datenbank', 'disziplin' und 'zugang'. Die Klasse verwendet das UUID-Stapel und verfügt über einen Beziehungsaufbau zu einer anderen Model-Klasse namens 'Projekt'.

---

### app/Models/Recherche/P3Disziplin.php
Definiert ein Eloquent-Model für die Tabelle 'p3_disziplinen' mit einer UUID und Beziehungen zu anderen Modellen.

---

### app/Models/Recherche/P3GeografischerFilter.php
Klasse für Geografische Filter in Projekten. Enthält Eloquent Model mit UUIDs, Tabellenbeziehung zu Projekt.

---

### app/Models/Recherche/P3GraueLiteratur.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p3_graue_literatur' mit Beziehungen und mass assignable Attributen.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
Definiert das Eloquent-Modell für Anpassungsprotokolle in der Recherche-Datenbanktabelle. Enthält Methoden für die Beziehung zum Suchstring und Konfiguration von Datumsformaten und automatischen Zeitstempeln.

---

### app/Models/Recherche/P4Suchstring.php
Definiert das Eloquent-Modell für Suchstrings in der Datenbank. Stellt Methoden zur Verbindung mit Projekten und Anpassungsprotokollen zur Verfügung.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Die Datei P4ThesaurusMapping.php definiert eine Eloquent-Model-Klasse für die Tabelle 'p4_thesaurus_mapping'. Sie enthält Attributdefinitionen, einschließlich UUIDs und Zeitspalten. Eine zentrale Funktion ist der belongsTo-Beziehung zur Projekt-Tabelle.

---

### app/Models/Recherche/P5PrismaZahlen.php
Definiert das Eloquent-Model für P5PrismaZahlen in der Datenbank mit Beziehungen und Casting.

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Definiert die Einstellungen und Methoden für das Model P5ScreeningEntscheidung, einschließlich der Zuordnung zu einem Treffer.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Definiert ein Eloquent-Model für die Tabelle 'p5_screening_kriterien' mit UUIDs, ohne Zeitenstempel und verbindet sich mit der Projekt-Tabelle.

---

### app/Models/Recherche/P5ToolEntscheidung.php
Definiert die Datenmodell-Klasse P5ToolEntscheidung für die Tabelle p5_tool_entscheidung mit Eloquent ORM und Beziehung zu Projekt.

---

### app/Models/Recherche/P5Treffer.php
Definiert das Eloquent-Model P5Treffer für die Tabelle p5_treffer mit Beziehungen zu anderen Tabellen wie Projekt, Duplikate und Bewertungen.

---

### app/Models/Recherche/P6Luckenanalyse.php
Definiert das Eloquent-Model für die Tabelle 'p6_luckenanalyse', mit UUIDs, ohne Zeitenstempel und relatierend zum Projekt.

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
Definiert das Eloquent-Model für die P6Qualitaetsbewertung-Tabelle mit UUIDs, ohne Zeitenstempel. Beinhaltet Felder für Bewertungen und eine Beziehung zur P5Treffer-Tabelle.

---

### app/Models/Recherche/P7Datenextraktion.php
Definiert eine Eloquent-Modelklasse für die Datenextraktion in der Tabelle p7_datenextraktion. Stellt einen Beziehungsendpunkt zu P5Treffer.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p7_grade_einschaetzung' mit Attributen wie 'outcome', 'studienanzahl' und Beziehungen zu anderen Modellen.

---

### app/Models/Recherche/P7MusterKonsistenz.php
Definiert ein Eloquent-Model für P7MusterKonsistenz mit UUIDs, Tabellenzuordnung und Beziehungen zu Projekten.

---

### app/Models/Recherche/P7SyntheseMethode.php
```json
{
  "file_summary": "Definiert ein Eloquent-Model für die Tabelle 'p7_synthese_methode'. Stellt Methoden zur Verfügung, um mit den Daten der Tabelle interagieren zu können. Bezieht sich auf das Projekt-M rescue.
  
  Wichtige Elemente:
  - Klassendefinition: P7SyntheseMethode
  - Importierte Traits und Klassen: HasUuids, Model, BelongsTo
  
  Externe Abhängigkeiten:
  - Projekt::class (Importiert aus App\Models\Recherche)
  - Illuminate\Database\Eloquent\Model
  - Illuminate\Database\Eloquent\Concerns\HasUuids
  - Illuminate\Database\Eloquent\Relations\BelongsTo"
}
```

---

### app/Models/Recherche/P8Limitation.php
Klasse P8Limitation definiert ein Eloquent-Model für die Tabelle p8_limitationen. Sie verfügt über eine Beziehung zu der Projektklasse und ermöglicht das Speichern von Limitierungsdaten mit einer UUID.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Definiert eine Eloquent-Model-Klasse für die Tabelle 'p8_reproduzierbarkeitspruefung', mit UUIDs, Zeitstempeln ausgeschaltet und Beziehungen zu anderen Tabellen.

---

### app/Models/Recherche/P8Suchprotokoll.php
Eloquent Model für das P8Suchprotokoll mit UUIDs, Tabellenverbindung, Zeitstempel-Deaktivierung und massenfliessendes Update. Bezieht sich auf das Suchstring-Modell.

---

### app/Models/Recherche/P8UpdatePlan.php
Definiert eine Eloquent-Model-Klasse P8UpdatePlan zur Verwaltung von Updateplänen in einer Datenbank. Stellt Beziehungen zu einem Projekt und bietet Zugriff auf bestimmte Felder wie 'update_typ' und 'naechstes_update'.

---

### app/Models/Recherche/PaperEmbedding.php
Die Datei definiert eine Eloquent-MODEL Klasse für Paper-Embeddings in einer Datenbank. Sie enthält Eigenschaften wie projekt_id, source, paper_id und mehr, sowie eine Beziehung zur Projekt-Klasse.

---

### app/Models/Recherche/Phase.php
Diese Datei enthält die Phase-Modelklasse für eine Laravel-Anwendung. Sie definiert die Struktur der Phasentabelle mit UUIDs und Beziehungen zu anderen Modellen.

---

### app/Models/Recherche/Projekt.php
Definiert das Projekt-Modell mit Eloquent ORM und Spatie Activitylog für Protokollierung. Verbindet Projekte mit Benutzern und anderen Modellen wie Phasen und Strukturmodellen.

---

### app/Policies/ProjektPolicy.php
Diese Datei enthält eine Policy-Klasse 'ProjektPolicy', die Berechtigungen für das Anzeigen, Erstellen, Aktualisieren und Löschen von Projekten definiert. Die Policies werden auf Benutzer basiert, wobei bestimmte Aktionen nur durch den Eigentümer des Projekts gestattet sind.

---

### app/Providers/AppServiceProvider.php
Initialisiert Anwendungsrichtlinien und konfiguriert Produktionssicherheit.

---

### app/Providers/FortifyServiceProvider.php
Konfiguriert Anmelde- und Registrierungsprozesse mit Fortify Actions, definiert benutzerdefinierte Ansichten für verschiedene Authentifizierungsflüsse und einschränkt Anfragemässigung.

---

### app/Providers/VoltServiceProvider.php
Dieses Provider registriert und bootet die Livewire-Volt-Klasse, um Seiten mit Livewire zu mounten.

---

### app/Providers/Filament/AdminPanelProvider.php
Konfiguriert das Filament Admin-Panel mit Authentifizierung, Middleware und Ressourcen.

---

### bootstrap/app.php
Initialisiert die Anwendung mit Routing, Middleware und Fehlertoleranz. Stellt HTTP-Anfragen und Befehle zur Verfügung.

---

### bootstrap/providers.php
Diese Datei definiert eine Liste von Service-Providers für ein Laravel-Anwendung. Die wichtigsten Klassen sind Providers wie AppServiceProvider, AdminPanelProvider, FortifyServiceProvider und VoltServiceProvider.

---

### config/app.php
Konfigurationsdatei für Laravel-Anwendungen, definiert grundlegende Einstellungen wie Name, Umgebung, Debug-Modus, URL, Zeitzone und Lokalisation.

---

### config/auth.php
Konfiguration der Authentifizierungseinstellungen in Laravel, einschließlich Guards, Providers und Password Reset.

---

### config/cache.php
Konfiguration für verschiedene Cache-Speicherstapel in einer Laravel-Anwendung.

---

### config/cookie-consent.php
Konfiguration für den Cookie-Bestimmungsbanner mit Optionen zur Aktivierung, Namen und Lebensdauer des Cookies.

---

### config/database.php
Konfiguration der Datenbankverbindungen für verschiedene DB-Typen (SQLite, MySQL, MariaDB). Setzt Umgebungsvariablen zur Verbindungseinstellung. Stellt API-Endpunkte zur Datenbankinteraktion bereit.

---

### config/filesystems.php
Konfiguration der Dateisysteme für verschiedene Anwendungen wie lokale Speicherung und AWS S3.

---

### config/fortify.php
Konfiguration für Laravel-Fortify: Authentifizierungsguard, Passwortschutz, Benutzernamen/email, Kleinbuchstaben für Benutzernamen, Startseite nach Authentifizierung

---

### config/logging.php
Konfiguriert Log-Systeme für Laravel-Anwendungen mit Monolog.

---

### config/mail.php
Konfigurationsdatei für die E-Mail-Sending-Funktionalität. Definiert verschiedene Mailer wie SMTP, SES, Postmark usw. mit ihren jeweiligen Einstellungen.

---

### config/queue.php
Konfiguration der Queue-Verbindungen in Laravel-Apps. Definiert Standardverbindung und Optionen für verschiedene Queue-Typen wie Datenbank, Beanstalkd, SQS, Redis etc.

---

### config/services.php
Stores configuration data for third-party services including Postmark, Resend, AWS SES, Slack, Langdock, and MCP. Retrieves API keys and other credentials from environment variables.

---

### config/session.php
Konfigurationsdatei für das Laravel-Sessions-System. Setzt Parameter wie Driver, Lifetime und Verschlüsselung.

---

### database/factories/UserFactory.php
Generiert User-Modelle für die Datenbank mit fälschten Daten. Stellt Methoden zur Verfügung, um das Modell zu verifizieren oder ohne Zweifaktor zu konfigurieren.

---

### database/factories/Recherche/ProjektFactory.php
Erstellt Factory-Klasse für Projekte, um zufällige Daten zu generieren.

---

### database/seeders/DatabaseSeeder.php
Initialisiert die Datenbank mit Rollen, einem Admin-Benutzer und Recherchendemo-Daten.

---

### database/seeders/RechercheDemoSeeder.php
Diese Datei ist ein Seeder für eine Recherche-Demo. Er erstellt Benutzer, Webhooks und verschiedene Phasen eines Projekts mit spezifischen Daten.

---

### database/seeders/RoleSeeder.php
Fügt standardmäßige Rollen ('admin', 'user') in das System ein, wenn sie noch nicht existieren. Verwendet die Spatie Permission Library.

---

### database/sql/create_langdock_agent_user.sql
Erstellt einen PostgreSQL-Benutzer 'langdock_agent' mit begrenzten Rechten auf bestimmte Tabellen und Schemas.

---

### docker/common/nginx/default.conf
Konfigurationsdatei für Nginx mit Einstellungen für statische Dateien, PHP-Präfixe und Authentifizierung via Bearer-Token oder Query-Param. Interne DNS-Resolution und Logging.

---

### docker/common/nginx/docker-entrypoint.sh
Dieses Skript kopiert eine Nginx-Konfigurationsvorlage, ersetzt einen Platzhalter durch eine Umgebungsvariable und startet Nginx.

---

### docker/common/nginx/dockerfile
Dockerfile for Nginx, copying custom config, entrypoint script, and static files. Exposes port 80.

---

### docker/common/nginx/nginx.conf
Nginx configuration for Laravel application, setting up basic parameters like worker connections, gzip compression, and file upload limits.

---

### docker/common/php-fpm/dockerfile
Dockerfile für ein PHP-FPM-Bild mit Composer und npm-Abhängigkeiten. Bietet Stufen für Entwicklung, Produktion und Testumgebung.

---

### docker/common/php-fpm/entrypoint.sh
Starts PHP-FPM, syncs frontend assets, waits for database, runs migrations in production/staging, caches config/routes/views in production, and executes the main command.

---

### docker/common/postgres/dockerfile
Dockerfile für eine PostgreSQL 16-Basisimage mit zusätzlichen Erweiterungen

---

### docker/common/postgres/init-extensions.sql
Erstellt PostgreSQL-Erweiterungen auf Start.

---

### public/index.php
Startet eine Laravel-Anwendung, verarbeitet Anfragen und liefert Antworten.

---

### public/robots.txt
Konfigurationsdatei für Robots.txt, die die Zugriffsteuerung auf eine Website definiert.

---

### public/.htaccess
Konfiguriert Apache um URL-Rewriting, Autorisierung und XSRF-Token zu verwalten. Sendet Anfragen an den Front-Controller.

---

### public/css/filament/app.css
CSS-Datei für Tailwind CSS Version 4.1.18 mit Farbdefinitionen und Layout-Einstellungen.

---

### public/css/filament/filament/app.css
Tailwind CSS v4.1.18 stylesheet defining properties and theme variables.

---

### public/fonts/filament/filament/inter/index.css
Definiert Variante von Inter Schriftart für verschiedene Unicode-Bereiche.

---

### public/fonts/filament/inter/index.css
CSS-Datei, die verschiedene Schreibschnitte der Schriftart Inter Variable als WOFF2-Variationen definiert.

---

### public/js/actions/actions.js
Eine JavaScript-Datei zur Verwaltung von Aktionen und Modalen in einer Anwendung. Sie definiert eine Klasse 'n' mit Methoden wie 'init', 'syncActionModals', 'generateModalId', 'openModal' und 'closeModal'. Diese Methoden handhaben die Synchronisierung von Aktionen und das Öffnen/Schließen von Modalen basierend auf einer Livewire-ID. Die Klasse wird als Alpine.js-Datenverbindung registriert.

---

### public/js/filament/app.js
Die Datei app.js enthält eine benutzerdefinierte JavaScript-Klasse 'app', die verschiedene Funktionen und Methoden zur Verwaltung von Benutzereingaben, Events und DOM-Interaktionen bietet. Sie handelt sich um ein Tool für das Erstellen interaktiver Benutzeroberflächen.

---

### public/js/filament/echo.js
Eine JavaScript-Datei mit einer Base64-Encoding-Klasse 'f'. Die Klasse enthält Methoden zur Berechnung des Encoded-Längens, der Codierung von Daten in einen Base64-String und der Decodierung eines Base64-Strings. Die Datei ist Teil eines größeren Skripts oder einer Bibliothek.

---

### public/js/filament/actions/actions.js
Implementiert eine Alpine.js-Directive für Synchronisierung von Modalen basierend auf der Aktionen-Nesting-Struktur.

---

### public/js/filament/filament/app.js
JavaScript-Datei für das Filament-Framework, um Eingabeereignisse wie Tastatureingaben und Mausinteraktionen zu verwalten.

---

### public/js/filament/filament/echo.js
Eine JavaScript-Datei, die eine Base64-Kodierung und -dekodierungsfunktion bietet. Sie enthält eine Klasse `Base64` mit Methoden `encodedLength`, `encode`, `maxDecodedLength`, `decodedLength`, und `decode`. Die Datei wird wahrscheinlich für das Verschlüsseln oder Verwenden von Daten in einer Anwendung verwendet.

---

### public/js/filament/notifications/notifications.js
Diese Datei enthält JavaScript-Code für Benachrichtigungs-Komponenten und verarbeitet Benachrichtigungen über Livewire-Hooks. Sie definiert Klassen wie `p` für Benachrichtigungs-Komponenten und `o` für Benachrichtigungs-Einstellungen.

---

### public/js/filament/schemas/schemas.js
Initialisiert und verwalte das Layout und das Verhalten von Filament-Schemas, einschließlich Stimmung und Breiteaktualisierung. Definiert auch Alpine.js-Datenmixin für Formularvalidierungsfehler und Zustandshandhabung.

---

### public/js/filament/support/support.js
MD5-Hashing Library for JavaScript. Provides functions to compute MD5 hashes of various types of data (strings, arrays, buffers). Supports web and Node.js environments.

---

### public/js/filament/tables/tables.js
Diese Datei enthält Funktionen für die Berechnung von Layouts und Positionierung von Elementen in einer Benutzeroberfläche. Sie bietet Tools für das Anordnen von浮动-Elementen im Bezug auf Referenzelemente, die Positionierung basierend auf unterschiedlichen Strategien wie 'absolute', 'fixed' oder 'sticky' sowie Unterstützung für verschiedene Pufferungen und Scrollregionen.

---

### public/js/filament/tables/components/columns/checkbox.js
Diese Datei enthält eine Klasse für einen Checkbox-Column in einer Tabellenansicht. Sie verarbeitet Änderungen des Zustands und kommuniziert mit einem Livewire-Hook.

---

### public/js/filament/tables/components/columns/select.js
Dieses JavaScript-Modul enthält Funktionen zur Berechnung von Positionen und Platzierung von Elementen im DOM, insbesondere für Popover-, Tooltip- und Dropdown-Komponenten. Er bietet Tools zur Verwendung mit Positionierungsbibliotheken wie Popper.js.

---

### public/js/filament/tables/components/columns/text-input.js
Diese Datei enthält eine JavaScript-Komponente für einen Text-Eingabefeld in einer Tabelle. Sie verarbeitet Benutzerinteraktionen, synchronisiert den Zustand mit einem Server über Livewire und aktualisiert den UI bei Änderungen.

---

### public/js/filament/tables/components/columns/toggle.js
Diese Datei enthält eine JavaScript-Komponente für ein Toggle-Spalte in einer Tabelle. Sie verwendet Alpine.js und Livewire zur Verwaltung des Zustands und der Kommunikation mit dem Server.

---

### public/js/filament/widgets/components/stats-overview/stat/chart.js
JavaScript-Datei für das Styling und Darstellen von statistischen Daten in einem Chart. Enthält Farbkonvertierungen und -manipulationen sowie Funktionen zur Berechnung und Anzeige von Statistiken.

---

### public/js/notifications/notifications.js
Verwaltet Benachrichtigungen. Hauptklasse: NotificationManager, Notification. Abhängigkeiten: Livewire-API

---

### public/js/schemas/schemas.js
Diese Datei enthält JavaScript-Code für eine Alpine.js-Komponente, die Formularvalidierungen verarbeitet und Schema-Components steuert. Sie bietet Funktionen zum Updaten der Breite eines Elements, dem Sticky-Behavior aktivieren und zu deaktivieren sowie das Auslösen benutzerdefinierter Events.

---

### public/js/support/support.js
Diese Datei enthält eine Implementierung des MD5-Hashing-Algorithmus in JavaScript. Sie bietet Funktionen zur Berechnung von MD5-Hashes für verschiedene Datentypen wie Strings, Arrays und Buffers.

---

### public/js/tables/tables.js
JavaScript-Datei für das Manipulieren und Positionieren von Tabellen in einer Webanwendung. Enthält Funktionen zur Berechnung von Positionen, Middleware für Platzierungsmiddleware und Utilitäten für Elemente. Abhängigkeiten: Mathematiksfunktionen, externe APIs (z.B. für Layout-Positionierung).

---

### public/js/tables/components/columns/checkbox.js
Implementiert ein Kontrollkästchen-Column für Tabellen, das den Zustand über Livewire synchronisiert. Verwendet Alpine.js und Livewire Hooks.

---

### public/js/tables/components/columns/select.js
Bietet Hilfsmethoden für Positionierung und Layout von Elementen in einer Benutzeroberfläche.

---

### public/js/tables/components/columns/text-input.js
Dieses Skript definiert eine JavaScript-Komponente für einen Text-Eingabefeld in einer Tabelle. Sie handhabt den Zustand des Eingabefelds, synchronisiert ihn mit einem Server und aktualisiert sich bei Änderungen.

---

### public/js/tables/components/columns/toggle.js
Die Datei exportiert eine Funktion, die ein Toggel-Komponenten für Tabellenzellen implementiert. Sie verarbeitet Livewire-Hooks und state-Änderungen um den Zustand der Zelle zu aktualisieren.

---

### public/js/widgets/components/stats-overview/stat/chart.js
Die Datei handelt sich um eine JavaScript-Datei, die Farbmanipulationen durchführt. Sie definiert Funktionen für die Umwandlung zwischen verschiedenen Farbformaten (z.B. RGB, HEX, HSL) und Berechnet Farben basierend auf gegebenen Parametern. Die Datei enthält auch Funktionen zur Veränderung der Helligkeit und Sättigung von Farben.

---

### resources/css/app.css
CSS-Datei für das Design und Styling der Anwendung. Importiert Tailwind CSS, definiert benutzerdefinierte Variablen und Farbschemata.

---

### resources/views/components/layouts/app.blade.php
Renders an app layout with a sidebar and main content area. Sidebar title is optional.

---

### resources/views/components/layouts/auth.blade.php
Eine Blade-Template-Datei für Authentifizierungslayout. Zeigt ein einfaches Auth-Lay aus.

---

### resources/views/components/layouts/app/header.blade.php
Die Datei enthält das HTML-Layout für den Header einer Anwendung, einschließlich des Navigationsmenüs, der Markenlogo und der Optionen zur Navigation auf verschiedene Seiten. Sie verwendet Tailwind CSS für Styling und Alpine.js für interaktive Elemente wie die Mobile-Navigation.

---

### resources/views/components/layouts/app/sidebar.blade.php
Diese Datei enthält das Layout der Seitenleiste für eine Anwendung mit einer Schaltfläche zur Rückkehr zum Dashboard und einer Navigationsleiste. Es verwendet Tailwind CSS, Alpine.js und Blade Templates. Die Seite hat einen Dark-Mode und ist responsive.

---

### resources/views/components/layouts/auth/card.blade.php
Blade-Template für eine Authentifizierungs-Karte mit Logo, App-Namen und Inhaltslot.

---

### resources/views/components/layouts/auth/simple.blade.php
HTML-Vorlage für ein einfaches Authentifizierungslayout mit Logo, Link zur Startseite und Platzhalter für Inhalte. Ermöglicht das Einbinden von Kopfzeilen, Cookie-Banner und Live-Wire-Skripte.

---

### resources/views/components/layouts/auth/split.blade.php
Diese Datei ist eine Blade-Vorlage für einen Authentifizierungs-Layout. Sie enthält ein Layout mit einer linken und rechten Spalte, die sich auf das Login und Register-FORMular konzentrieren. Die linke Spalte zeigt den App-Namen und einen zufälligen Zitat an.

---

### resources/views/components/settings/layout.blade.php
Diese Datei definiert die Layoutstruktur für Einstellungsseiten in einer Webanwendung. Sie enthält eine Navigation für verschiedene Einstellungskategorien und ein Hauptinhaltsbereich, der von Child-Komponenten gefüllt wird.

---

### resources/views/emails/contact-confirmation.blade.php
E-Mail-Vorlage für Bestätigung der Kontaktanfrage bei Linn Games. Enthält Benutzerdaten und Anfragezusammenfassung.

---

### resources/views/emails/contact-inquiry.blade.php
Renders an HTML email template for contact inquiries, displaying details like name, company, email, project type, timeline, message, and timestamp.

---

### resources/views/emails/deploy-notification.blade.php
HTML-Vorlage für eine Erfolgsmeldung bei der Deployment-Benachrichtigung

---

### resources/views/layouts/legal.blade.php
Diese Datei definiert das Layout für die Rechtliche Informationen auf einer Website. Sie enthält einen Header mit dem Branding und Links, sowie einen Footer mit der letzten Aktualisierung.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für einen Online-Spiel. Sie regeln die Nutzung der Website und des Spiels durch Nutzer, einschließlich der Registrierung, Nutzungsregeln, Kosten, digitale Käufe, Haftung und Sanktionen bei Verstößen.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei enthält eine Datenschutzerklärung für eine Website und ein Spiel. Sie verarbeitet verschiedene Kategorien personenbezogener Daten wie Registrierungsdaten, Authentifizierungsdaten, Spieldaten, Nutzungsdaten und Kontaktinhalte. Die Verarbeitung erfolgt im Zusammenhang mit der Bereitstellung des Angebots (Art. 6 Abs. 1 lit. b DSGVO), Kommunikation und Support (Art. 6 Abs. 1 lit. f DSGVO) sowie zur Sicherheit (Art. 6 Abs. 1 lit. f DSGVO). Die Daten werden auf der Website gespeichert und für die Erreichung der oben genannten Zwecke verwendet.

---

### resources/views/legal/impressum.blade.php
Diese Datei enthält den HTML-Code für die Impressum-Seite eines Webseitens. Erweitert eine grundlegende Layout-Vorlage mit Titel, Kontaktinformationen und Links zur Datenschutzerklärung und Allgemeinen Geschäftsbedingungen.

---

### resources/views/livewire/auth/confirm-password.blade.php
Blade-Templat für das Passwortbestätigungsfenster im Authentifizierungsbereich. Zeigt ein Formular zur Bestätigung des Benutzers Passworts.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei enthält das Blade-Vorlage für die 'Forgot Password'-Seite im Authentifizierungs-Layout. Sie zeigt ein Formular zum Senden eines Passwort-Zurücksetzungsmails an den Benutzer und enthält Nachrichten für erfolgreiche Sitzungen.

---

### resources/views/livewire/auth/login.blade.php
Renders the login form for users to authenticate with their email and password. Includes fields for email, password, and remember me checkbox. Displays session status and error messages. Provides a link to reset forgotten passwords.

---

### resources/views/livewire/auth/register.blade.php
Diese Datei enthält ein Blade-Template für den Registrierungsformular der Authentifizierung. Es zeigt ein Formular mit Eingabefeldern für Name, E-Mail und Passwort sowie Validierungsfehlermeldungen. Das Formular sendet die Daten an den Endpunkt 'register.store' für die Verarbeitung des Registrierungsantrags.

---

### resources/views/livewire/auth/reset-password.blade.php
Diese Datei enthält das Blade-Vorlage für den Passwortzurücksetzung in der Authentifizierungsmodul. Sie verwendet Klassen wie x-layouts.auth, x-auth-header und x-auth-session-status für die Struktur und Anzeige von Formularn zur Passwortveränderung. Die Datei handhabt Token, E-Mail-Adresse, Passwörter und deren Bestätigung. Sie enthält externe Abhängigkeiten wie CSRF-Token und Formularmethoden.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
Diese Datei enthält die Blade-Vorlage für den Zweifaktor-Authentifizierungschallenge in einer Anwendung. Sie definiert das Benutzeroberfläche und das Verhalten zur Eingabe des Authentifizierungs- oder Wiederherstellungs-Codes, sowie den Umgang mit Fehlern und der Schaltfläche zum Wechseln zwischen beiden Modi.

---

### resources/views/livewire/auth/verify-email.blade.php
Renders a Blade template for email verification. Includes buttons to resend the verification link and log out.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Livewire-Komponente für einen großen Forschungspannungschat. Verwendet ChatMessage und Webhook-Modelle, HTTP für API-Anfragen, Authentifizierung via Laravel.

---

### resources/views/livewire/recherche/projekt-detail.blade.php
Zeigt die Detailansicht eines Projekts mit Informationen wie Titel, Erstellung, Forschungsfrage, Phasen und Treffern. Rechtlich sichert Zugriff auf das Projekt basierend auf Benutzer-ID.

---

### resources/views/livewire/recherche/projekt-liste.blade.php
Dies ist eine Blade-Vorlage für eine Livewire-Komponente zur Anzeige und Verwaltung von Projekten. Sie zeigt Projekte eines Benutzers an und bietet Funktionen zum Löschen von Projekten.

---

### resources/views/livewire/recherche/research-input.blade.php
Eine Livewire-Komponente für die Recherche-Eingabe. Zeigt ein Formular zur Eingabe der Forschungsfrage, validiert die Eingabe, speichert den Projektitel und initiert einen Hintergrundjob für die Langdock-Agenten-Synchronisierung.

---

### resources/views/livewire/settings/appearance.blade.php
Livewire Component for Appearance Settings in the user interface.

---

### resources/views/livewire/settings/delete-user-form.blade.php
Einen React-Komponenten-Code für ein Benutzerkontoeinstellungsformular in JavaScript/TypeScript. Die Komponente stellt ein Formular zur Löschung eines Kontos bereit, das nur nach erfolgreicher Überprüfung der aktuellen Passwort-Eingabe freigegeben wird.

---

### resources/views/livewire/settings/password.blade.php
Livewire-Komponente für das Ändern des Passworts des authentifizierten Benutzers. Stellt ein Formular zur Eingabe des aktuellen Passworts und der neuen Passworteinträge bereit, validiert die Eingaben und aktualisiert das Passwort im System.

---

### resources/views/livewire/settings/profile.blade.php
Das PHP-Skript definiert ein Livewire-Komponentenklasse namens `settings.profile`. Die Klasse bietet Funktionen zur Anzeige und Aktualisierung des Benutzerprofils, einschließlich Name und E-Mail-Adresse. Es verwendet externe Abhängigkeiten wie Authentifizierungs-, Validierungs- und Benutzermodell-Klassen sowie Livewire-Volt für die Komponente.

---

### resources/views/livewire/settings/two-factor.blade.php
Dies ist eine Livewire-Komponente für die Verwaltung der Zweifaktor-Authentifizierung in einer Anwendung. Sie ermöglicht das Aktivieren, Deaktivieren und Bestätigen der Zweifaktor-Authentifizierung sowie das Generieren von QR-Codes und Manuellem Setup-Schlüsseln.

---

### resources/views/livewire/settings/webhooks.blade.php
Die Datei enthält Livewire-Komponenten zum Verwalten von Webhooks für Dashboard Chat und Recherche Start. Sie bietet Funktionen zur Anzeige und Speicherung der Webhook-URLs und -Geheimnissen pro Benutzer.

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
Livewire-Komponente zur Verwaltung von Zwei-Faktor-Authentifizierungs-Wiederherstellungs-Codes. Die Komponente lädt die Codes, generiert neue und zeigt sie an.

---

### resources/views/partials/head.blade.php
Generiert den Kopfteil der HTML-Datei mit Metadaten, Titel und Stileinstellungen.

---

### resources/views/partials/settings-heading.blade.php
Rendert den Überschriftsbereich für die Einstellungen-Seite, einschließlich des Titels und einer Beschreibung.

---

### resources/views/recherche/index.blade.php
Blade template for the research page, using Livewire components for input and project list.

---

### resources/views/recherche/show.blade.php
Zeigt ein Detailansichts-LiveWire-Endpunkt für ein Projekt.

---

### routes/api.php
Definiert API-Endpunkte für Authentifizierung und Datenverarbeitung. Verwendet Middleware zur Signaturprüfung und Tokenvalidierung.

---

### routes/console.php
Definiert Artisan-Kommandos für Ansporns citas und Benachrichtigungen bei der Bereitstellung.

---

### routes/web.php
Definiert Web-Routen für verschiedene Seiten und Funktionen wie Kontaktformular, Impressum, Datenschutzrichtlinie, Nutzungsbedingungen, Dashboard, Einstellungen und Rechercheschnittstelle.

---

### tests/Pest.php
Pest.php erweitert PHPUnit, definiert Erwartungen und bietet Hilfsmethoden für Tests.

---

### tests/TestCase.php
Abstrakte Basisklasse für alle Testfälle in einer Laravel-Anwendung.

---

### tests/Feature/ContactTest.php
Diese Datei enthält Testfallen für die Kontaktaufnahme-Funktionalität. Die Tests überprüfen die Validierung der Eingabe, den Erfolg der Anfrage und die Datenbankinteraktion.

---

### tests/Feature/DashboardTest.php
Diese Datei enthält Testfälle für den Dashboard-Zugriff. Sie prüfen, ob Gastbenutzer auf die Login-Seite umgeleitet werden und angemeldete Benutzer den Dashboard-Zugriff erhalten.

---

### tests/Feature/ExampleTest.php
Tests die erfolgreiche Antwort eines Home-Routen-Endpunkts prüft.

---

### tests/Feature/LegalPagesTest.php
Die Datei enthält Tests für die Renderung der Impressum-, Datenschutz- und AGB-Seiten. Sie verwendet Laravel Testing Framework.

---

### tests/Feature/ProjektPolicyTest.php
Die Datei enthält Testfälle für die ProjektPolicy, um das Berechtigungsverhalten bei verschiedenen Aktionen auf Projekten zu überprüfen.

---

### tests/Feature/RechercheTest.php
Dieses PHP-Testskript verwendet Laravel Test-Framework um die Funktionalität von Recherche-Projekten und deren Verwaltung zu testen. Es erstellt Benutzer, simuliert Benutzeraktionen (z.B. das Starten einer Forschung), überprüft Datenbankinteraktionen und prüft Zugriff auf bestimmte Seiten/Endpunkte.

---

### tests/Feature/WebhookTest.php
Die Datei enthält Tests für einen Webhook, der Anfragen mit Signatur und Zeitstempel überprüft. Sie verwendet Laravel-Framework-Klassen wie Config, Queue, und testet verschiedene Szenarien wie ungültige Signatur, fehlender Zeitstempel, abgelaufener Zeitstempel sowie gültige Anfragen.

---

### tests/Feature/WelcomeTest.php
Die Datei enthält Tests für die Willkommensseite. Sie überprüft, ob der Status des Anfrages 200 ist, ob der Besuchszähler korrekt inkrementiert wird, ob eine neue PageView-Record erstellt wird, wenn keine vorhanden ist, und ob bestimmte Links basierend auf der Authentifizierung angezeigt werden.

---

### tests/Feature/Auth/AuthenticationTest.php
Testet die Authentifizierungsfunktionalität in einer Laravel-Anwendung, einschließlich Login, Logout und 2FA-Authentifizierung.

---

### tests/Feature/Auth/EmailVerificationTest.php
Die Datei enthält Testfälle für die E-Mail-Verifizierungsfunktionalität in einem Laravel-Anwendungsprojekt. Sie testet, ob der Verifizierungs-Link gerendert werden kann, ob ein Benutzer erfolgreich verifiziert wird, ob ein ungültiger Hash korrekt behandelt wird und ob ein bereits verifizierter Benutzer den Verifizierungslink ohne erneutes Dispatchen des Verified-Ereignisses aufgerufen wird.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Die Datei enthält einen Testfall, der überprüft, ob das Passwortbestätigungsformular gerendert werden kann. Er verwendet die Factory-Methode zur Erstellung eines Benutzers und stellt sicher, dass der HTTP-Status 200 zurückgegeben wird.

---

### tests/Feature/Auth/PasswordResetTest.php
Dieses PHP-Testskript testet das Passwort-Wiederherstellungs-Funktionalität des Authentifizierungsmoduls. Es verwendet Laravel's Testing Framework und fake Notifications für eine isolierte Testsuite.

---

### tests/Feature/Auth/RegistrationTest.php
Die Datei enthält Tests für die Benutzerregistrierung. Sie prüft, ob das Registrierungsformular gerendert wird und ob neue Benutzer erfolgreich registriert werden können.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Diese Datei enthält Tests für die Two-Factor-Authentifizierung in Laravel-Fortify. Sie überprüft, ob der Two-Faktor-Ausforderungspunkt korrekt umleitet, wenn nicht authentifiziert und ob er gerendert werden kann, wenn zwei-Faktor-Authentifizierung aktiviert ist.

---

### tests/Feature/Settings/PasswordUpdateTest.php
Die Datei enthält Tests für das Ändern des Passworts eines Benutzers. Sie verwendet Laravel Testing Library, Livewire und das Hashing-Modul zur Überprüfung der Passwortänderung.

---

### tests/Feature/Settings/ProfileUpdateTest.php
Die Datei enthält Tests für das Profilaktualisieren, die Verifizierung des E-Mail-Status und das Löschen von Benutzerkonten. Sie verwendet Laravel's Testing-Framework sowie externe Abhängigkeiten wie Livewire und Volt.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
Dieses Testskript überprüft die Funktionalität des Two-Factor-Authentifizierungsseitens. Es verwendet Laravel, Livewire und verschiedene User-Cases um sicherzustellen, dass die Seite korrekt gerendert wird und dass Benutzer die erforderlichen Schritte durchführen müssen.

---

### tests/Unit/ExampleTest.php
Dies ist ein einfacher Unit-Test, der prüft, ob der Wert 'true' tatsächlich 'true' ist.

---

### .github/copilot-instructions.md
Enthaelt Anweisungen zur Konfiguration und Entwicklung eines Laravel-Backends für app.linn.games. Enthält Informationen über das technische Stack, Architekturentscheidungen, Datenbanken (PostgreSQL), Testing mit Pest sowie CI/CD.

---

### .github/instructions/app_linn_games_custom.instructions.md
Dokumentation für app.linn.games Architektur, Konventionen und kritische Details. Beschreibt Projektübersicht, Stack, Verzeichnisstruktur, Haupt-Klassen und Funktionen wie Controllers, Jobs und Models.

---

### .github/instructions/docker-dev-workflow.instructions.md
Dokumentiert das Dev-Workflow für app.linn.games mit Docker, inklusive Code-Deployment, Static Asset-Handling und Rebuild-Richtlinien.

---
