---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T08:19:04.943710
model: llama3.1:8b
run_id: llama3.1:8b
mode: overview
files_total: 242
errors: 0
run_time_s: 632.0
---

# Funktions-Übersicht — 2026-04-02_0819

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 242
- **Fehler:** 0
- **Laufzeit:** 632.0s

## api

### app/Http/Controllers/ContactController.php
```json
{
  "file_summary": "Die Datei definiert die Funktionen zum Senden von Kontaktanfragen über eine API. 
                   Die wichtigsten Klassen sind ContactController und die dazugehörigen Mails (ContactInquiryMail, ContactConfirmationMail) und das Contact-Modell.
                   Externe Abhängigkeiten: Illuminate\Http\Request, App\Mail\*, App\Models\Contact"
}
```

---

### app/Http/Controllers/Controller.php
Basiert auf einer abstrakten Basis-Klasse, die von allen API-Controllern erben muss. Definiert den Grundstein für alle anderen Controller in der Anwendung.

---

### app/Http/Controllers/DsgvoController.php
Controller für API-Anfragen zu DSGVO-Beziehungen von Nutzern. Zwei Methoden: export und deleteAccount. Externe Abhängigkeiten sind Auth und Request.

---

### app/Http/Controllers/LangdockWebhookController.php
```json
{
  "file_summary": "Der Controller verarbeitet Webhooks von Langdock und startet einen Job, der das Agent-System auslöst. Die erforderlichen Daten werden überprüft und dann an den Job weitergegeben.",
  "important_classes": ["LangdockWebhookController", "TriggerLangdockAgent"],
  "external_dependencies": [
    "App\Jobs\TriggerLangdockAgent",
    "Illuminate\Http\Request",
    "Illuminate\Http\JsonResponse"
  ]
}
```

---

### app/Http/Controllers/PaperRagController.php
```json
{
  "file_summary": "Der PaperRagController verarbeitet API-Anfragen zur Textinkorporierung und -suche. 
                    Die wichtigsten Funktionen sind ingest (Text inkorporieren) und search (Text suchen). 
                    Die Controller nutzt Jobs, DB-Zugriffe und externe APIs für die Embedding-Berechnung."
}
```

---

### routes/api.php
Diese Datei definiert APIs für Authentifizierung und Datenabrufe. Sie verwendet Middleware zum Signatur-Verifizieren von Webhook-Ereignissen und Schwellenwert-Kontrollen für bestimmte Endpunkte.

Hauptfunktionen:
• Authentifizierte API-Zugriff über /user-Endpunkt
• Langdock-Webhook-Handling mit Signaturprüfung
• Paper-RAG-Controller für Papierdaten-Suche und -Import

Externe Abhängigkeiten: Sanitum-Authentifizierung, Illuminate-Route-Framework

---

### routes/console.php
Definiert Artisan-Kommandos für Benutzerinteraktion. Zwei Kommandos: 'inspire' und 'deploy:notify'.

---

### routes/web.php
Die Datei 'routes/web.php' definiert Routen für die Web-Anwendung, darunter Anfragen zum Impressum, DSGVO-Bestimmungen und Benutzerkontakt. Sie enthält auch Routen zur Verwaltung von Benutzerprofilen und Einstellungen.

---

## config

### docker-compose.dev.yml
Dev-Override für den nginx-Port, ändert ihn auf 6480 statt 6481.

---

### docker-compose.yml
```
{
  "file_summary": "Dies ist ein Docker-Compose-Skript, das eine Anzahl von Diensten startet und konfiguriert. 
    - Der Web-Dienst verwendet Nginx als Webserver und bindet sich an den Port 80.
    - Der PHP-FPM-Dienst enthält die Umgebungsvariablen aus dem .env-File.
    - Die PostgreSQL-Datenbank wird mit einem Healthcheck konfiguriert, um sicherzustellen, dass sie verfügbar ist, bevor andere Dienste starten.
    - Die Redis-Datenbank wird verwendet, um die Kommunikation zwischen den Diensten zu ermöglichen.
    - Der MCP-Paper-Search-Dienst bindet sich an Port 8089 und verwendet den Bearer-Token zur Authentifizierung.
    Die wichtigsten Klassen/Funktionen sind:
      - Nginx
      - PHP-FPM
      - PostgreSQL
      - Redis
      - MCP-Paper-Search
    Externe Abhängigkeiten sind:
      - Nginx
      - PHP-FPM
      - PostgreSQL
      - Redis
      - Ollama
      - CrystalDBA/Postgres-MCP"
}
```

---

### .env.example
Diese Datei enthält Konfigurationsparameter für eine Laravel-Anwendung. Sie speichert Werte wie Application-Name, Umgebung, Schlüssel und Debug-Modus. Darüber hinaus sind hier Datenbankverbindungen (z.B. PostgreSQL), Sitzungsmanagement, Log-Einstellungen, Cache, Queue-Verbindung und andere Einstellungen für die Anwendung zu finden.

---

### config/app.php
Diese Datei enthält die grundlegenden Einstellungen für eine Laravel-App. Sie beinhaltet Informationen wie Application Name, Application Environment, Debug Mode und mehr.

---

### config/auth.php
```
{
  "file_summary": "Konfiguration für die Benutzerauthentifizierung und Passwörter",
  "klasse/namen": ["defaults", "guards", "providers"],
  "funktionen/endpunkte": [],
  "externe abhängigkeiten": [
    "env('AUTH_GUARD', 'web')",
    "env('AUTH_PASSWORD_BROKER', 'users')",
    "App\Models\User::class"
  ]
}
```

---

### config/cache.php
```
{
  "file_summary": "Diese Datei definiert die Konfiguration für den Cache von Laravel. 
  Es werden verschiedene Cache-Speicher (z.B. Array, Datenbank, Filesystem, Memcached, Redis) konfiguriert.
  Der Standard-Cache-Speicher wird über die Umgebungsvariable CACHE_STORE bestimmt.
  Die wichtigsten Klassen oder Funktionen sind 'Illuminate\Support\Str' und die Cache-Store-Treiber (z.B. 'array', 'database', 'file')."
}
```

---

### config/cookie-consent.php
Konfiguration für Cookie-Einwilligung, enthält Einstellungen zum Aussehen und Verhalten der Einwilligungsmaske. Wichtigste Klassen/Funktionen: keine (konstante Konfigurationsdatei). Externe Abhängigkeiten: env('COOKIE_CONSENT_ENABLED')

---

### config/database.php
Datenbank-Konfiguration für das Laravel-Projekt. Hier werden die Verbindungsparameter für verschiedene Datenbank-Systeme definiert.

---

### config/filesystems.php
Konfiguration für Dateisysteme, einschließlich Standard-Dateisystem und Verweis auf externe Storage-Dienste.

---

### config/fortify.php
Die Datei config/fortify.php definiert die Einstellungen für Fortify, ein Authentifizierungs- und Autorisierungssystem in Laravel. Sie beinhaltet die Auswahl des Authentifizierungs-Guards, den Benutzername, den Email-Attributnamen, sowie die Konfiguration von Features wie Registrierung, Passwort-Restitution, E-Mail-Bestätigung und zweifaktorische Authentifizierung.

---

### config/logging.php
```
{
  "file_summary": "Die Datei config/logging.php konfiguriert das Logging für eine Laravel-Application. Sie enthält die Einstellungen für verschiedene Log-Kanäle, wie z.B. Daily, Slack und Syslog. Die wichtigsten Klassen sind Monolog\Handler\NullHandler, Monolog\Handler\StreamHandler und Monolog\Processor\PsrLogMessageProcessor.",
  "important_classes": ["Monolog\\Handler\\NullHandler", "Monolog\\Handler\\StreamHandler", "Monolog\\Processor\\PsrLogMessageProcessor"],
  "externale_dependencies": ["Monolog"]
}
```

---

### config/mail.php
Diese Datei enthält E-Mail-Konfigurationen für die Anwendung. Es werden verschiedene Mailer konfiguriert, darunter SMTP, SES und Log-Mailer. Außerdem wird ein globaler "From"-Absender für alle E-Mails festgelegt. Externe Abhängigkeiten sind Umgebungsvariablen (env()) für Mail-Konfigurationen.

---

### config/queue.php
Konfiguration für das Job-Warteschlangensystem. Beschreibt die Standardwarte-Konnektion, verfügbare Warteschlangen-Bezeichner und Konnektionsdaten für einzelne Datenbanken.

---

### config/services.php
Datenbankschlüssel und API-Schlüssel für Drittanbieter-Dienste wie Postmark, Resend, SES, Slack, Langdock und MCP.

---

### config/session.php
```json
{
  "file_summary": "Diese Datei config/session.php definiert die Einstellungen für Sitzungen in einer Laravel-Anwendung. Sie enthält Einstellungen wie die Session-Driver, die Sitzungslebensdauer, die Verschlüsselung und den Speicherort der Sitzungsdaten.",
  "klasse": "",
  "funktionen": [],
  "endpunkte": [],
  "externe_abhaengigkeiten": ["Illuminate\Support\Str", "env()"]
}
```

---

### docker/common/php-fpm/php-dev.ini
Die Datei konfiguriert die PHP-Umgebung für Entwicklungszwecke.
Sie enthält Einstellungen für OPcache und Fehlerausgabe.
Externe Abhängigkeiten: None.

---

### docs/api.yaml
API-Dokumentation für app.linn.games, die Webhooks, Paper-Ingestion, Vektorsuche und User-Info unterstützt. Umfasst Endpunkte wie getUser, handleLangdockWebhook, ingestPaper, ragSearch sowie Sicherheitsmechanismen wie Bearer-Tokens und HMAC-Signaturen.

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Dies ist ein ISSUE-Template für Bug Reports. Es enthält Felder wie Problembeschreibung, Schritte zur Reproduktion, Schweregrad und mehr. Die wichtigsten Funktionen sind die Erstellung von Formularen für Benutzerfehlermeldungen mit variablen Feldtypen (Markdown, Textarea, Dropdown). Externe Abhängigkeiten: keine.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Dokumentation für die Erstellung eines neuen Features in Form einer Vorlage.

Wichtige Teile:
- Beschreibung des gewünschten Features
- Konkretes Problem, welches gelöst werden soll
- Vorgeschlagene Lösung und Umsetzung
- Betroffene Bereiche (Dateien, Routes, Services etc.)
- Breaking Change (Nein, Ja mit Migration oder API-Änderung)
- Akzeptanzkriterien für die Fertigstellung des Features.

Externe Abhängigkeit: GitHub-Template-Syntax

---

### .github/ISSUE_TEMPLATE/refactor.yml
```
{
  "file_summary": "Definiert ein ISSUE_TEMPLATE für Refactoring-Anträge.\n\
  Enthält Felder für Beschreibung, betroffene Dateien/Bereiche und Akzeptanzkriterien.\n\
  Keine externe Abhängigkeiten."
}
```

---

### .github/workflows/lint.yml
```json
{
  "file_summary": "Dieser Workflow überprüft die Qualität des Codes in bestimmten Branches und liefert Fehlermeldungen. Die wichtigsten Klassen, Funktionen oder Endpunkte sind: 
- `actions/checkout@v5` für das Auschecken des Repositories
- `shivammathur/setup-php@v2` für die Einrichtung einer PHP-Umgebung
- `composer install` für die Installation von Abhängigkeiten
- `pint` für die Überprüfung der Codequalität
- Die Verwendung von `secrets` für die Weitergabe von Benutzernamen und Lizenzschlüsseln"
}
```

---

### .github/workflows/tests.yml
Diese Datei definiert ein GitHub-Workflow, das den Build- und Test-Prozess für das Projekt verwaltet. Es deckt die Schritte vom Checkout bis hin zum Ausführen der Tests ab.

---

## data_access

### app/Models/ChatMessage.php
```
{
  "file_summary": "Modell für Chat-Nachrichten: Datenbank-Abfrage und Beziehungen zu User und Webhook. Importiert Eloquent-Klassen.",
  "key_classes_functions_endpoints": ["ChatMessage", "belongsTo Relations: user(), webhook()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Consent.php
Diese Datei definiert eine Consent-Model-Klasse, die Daten für Benutzereinwilligungen verwaltet. Sie importiert User und hat Beziehungen zu ihm. Es werden Eloquent-Casts und -Fillable-Member definiert.

---

### app/Models/Contact.php
Definiert die Contact-Model-Klasse für Datenbankanbindung. Die Klasse erweitert die Eloquent-Model-Klasse von Laravel und definiert die Felder, die füllbar sind (fillable).

---

### app/Models/PageView.php
Hier ist eine Zusammenfassung der Funktionen in dieser Datei:

```json
{
  "file_summary": "Die PageView-Eintragstabelle in der Datenbank definieren. Die Tabelle speichert Informationen über Seitenaufrufe und enthält Felder wie Pfad, anonyme IP-Adresse, User-Agent und Besucheranzahl.",
  "key_classes": ["PageView"],
  "important_methods": [""],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/User.php
Datenmodell für Benutzer mit E-Mail-Adresse, Rolle und Status. Beinhaltet Logik zur Aktivitätsprotokollierung und Benutzerinitialen.

---

### app/Models/Webhook.php
Webhook-Model mit Funktionen für die Verwaltung von Webhooks, darunter Auslieferung einer URL mit geheimer Marke und Zugriff auf den konfigurierten Webhook eines Benutzers.

---

### app/Models/Recherche/P1Komponente.php
Diese Datei implementiert die Datenbank-Tabelle für Komponenten der Projekt-Recherche (P1Komponente). Sie enthält Beziehungen zur Projekt-Tabelle und ermöglicht den Zugriff auf bestimmte Felder in der Tabelle. Die wichtigsten Funktionen sind: Projekt-Beziehung, Speicherung von Synonymen als Array und Kastierung von Feldern.

---

### app/Models/Recherche/P1Kriterium.php
```json
{
  "file_summary": "Definiert die Datenbank-Tabelle für P1-Kriterien. 
                  Beinhaltet die Definition der Tabelle und Beziehungen zu anderen Tabellen. 
                  Die wichtigsten Klassen sind P1Kriterium und Projekt.
                  Externe Abhängigkeiten: Illuminate\Database\Eloquent\Model, Projekt"
}
```

---

### app/Models/Recherche/P1Strukturmodellwahl.php
```
{
  "file_summary": "Modell für die Tabelle p1_strukturmodell_wahl. Es handelt sich um eine Eloquent-Basisklasse, die das Beziehungsverhältnis zur Projekt-Tabelle abbildet.",
  "key_classes": ["P1Strukturmodellwahl"],
  "important_methods": ["projekt", "projekt(): BelongsTo"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P1Warnsignal.php
Die Datei P1Warnsignal.php stellt die Datenbanktabelle 'p1_warnsignale' dar und bietet Zugriffsmöglichkeiten darauf. Die Tabelle enthält Informationen zu Warnsignalen für Projekte, einschließlich der möglichen Auswirkungen und Handlungsempfehlungen. Die wichtigsten Funktionen sind das Projekt-Beziehungsverhältnis über die Spalte 'projekt_id'. Externe Abhängigkeiten beinhalten die Eloquent-Tabelle 'Projekt' und die Datenbanktabelle 'p1_warnsignale'.

---

### app/Models/Recherche/P2Cluster.php
Datentabelle für die Cluster-Tabelle "p2_cluster" im Datenbank-Modell. Ein P2Cluster besitzt ein Projekt und hat verschiedene Attribute wie Beschreibung, Treffer-Schätzung und Relevanz.

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
```
{
  "file_summary": "Definiert eine Eloquent-Model-Klasse für Datenbank-Tabelle \"p2_mapping_suchstring_komponenten\".",
  "main_class": "P2MappingSuchstringKomponente",
  "important_methods": ["projekt", "__construct"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
```json
{
  "file_summary": "Eloquent-Model für P2-Review-Typ-Entscheidung mit Projekt-Beziehung.",
  "key_classes": ["P2ReviewTypEntscheidung"],
  "key_functions": ["projekt", "projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P2Trefferliste.php
```json
{
  "file_summary": "Diese Datei stellt eine Eloquent-Einheit für die Tabelle p2_trefferlisten dar und bietet Zugriff auf Datenbank-Beziehungen.",
  "key_classes": ["P2Trefferliste"],
  "important_methods": ["projekt", "getAttributes", "setAttributes"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P3Datenbankmatrix.php
Die Datei P3Datenbankmatrix.php ist eine Datenbanktabelle-Abstraktion für ein Projekt. Sie definiert die Beziehungen zwischen einer Projekt-Tabelle und einer Tabelle für die Datenbank-Matrix. Die wichtigsten Klassen sind P3Datenbankmatrix, Projekt und die Beziehung 'projekt()'. Externe Abhängigkeiten sind nicht vorhanden.

---

### app/Models/Recherche/P3Disziplin.php
Die Datei P3Disziplin.php stellt das Modell für die Tabelle p3_disziplinen dar und bietet Zugriff auf Disziplinen eines Projekts. Es gibt eine Beziehung zu der Projekt-Tabelle über den projekt_id-Schlüssel.

---

### app/Models/Recherche/P3GeografischerFilter.php
```json
{
  "file_summary": "Diese Datei definiert die Datenbanktabelle p3_geografische_filter und stellt einen Eloquent-Modell für diese Tabelle bereit. Sie enthält Beziehungen zu Projekt-Daten und bietet Methoden zur Abfrage und Manipulation der Filter-Einträge.",
  "key_classes": ["P3GeografischerFilter"],
  "important_functions": ["projekt", "validate", "fillable", "casts"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Projekt"]
}
```

---

### app/Models/Recherche/P3GraueLiteratur.php
Die Datei app/Models/Recherche/P3GraueLiteratur.php definiert ein Datenmodell für Graue Literatur in der Recherche. Es handelt sich um eine Eloquent-Model-Klasse, die mit der Tabelle p3_graue_literatur synchronisiert ist. Die Klasse bietet Zugriff auf Projektbeziehungen und ermöglicht das Speichern von Metadaten zu Grauer Literatur.

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
```json
{
  "file_summary": "Definiert die P4Anpassungsprotokoll-Model-Klasse, 
                   die Datenbank-Tabelle \"p4_anpassungsprotokoll\" 
                   und Beziehungen zur Tabelle \"p4_suchstring\".",
  "main_class": "P4Anpassungsprotokoll",
  "key_relationships": ["suchstring"],
  "casts": ["datum", "treffer_vorher", "treffer_nachher"]
}
```

---

### app/Models/Recherche/P4Suchstring.php
Die Datei P4Suchstring.php dient als Eloquent-Model für die Tabelle p4_suchstrings und bietet Zugriff auf Projektdaten sowie Suchanpassungs- und -protokoll-Daten.

---

### app/Models/Recherche/P4ThesaurusMapping.php
Datenmodell für P4-Thesaurus-Mappings. Es handelt sich um eine Eloquent-Model-Klasse in Laravel, die Zugriff auf die Tabelle 'p4_thesaurus_mapping' ermöglicht. Die Klasse verfügt über Beziehungen zur Projekt-Tabelle und enthält die spezifischen Felder für diese Mappings.

---

### app/Models/Recherche/P5PrismaZahlen.php
```
{
  "file_summary": "Datenbank-Modell für Projekt-Zahlen mit Zählungen und Ausgaben. Verwaltet Daten für einzelne Projekte.",
  "key_classes": ["P5PrismaZahlen"],
  "important_methods": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
Data Access Object (DAO) für die Tabelle p5_screening_entscheidungen. Die Klasse P5ScreeningEntscheidung ermöglicht das Arbeiten mit Einträgen in dieser Tabelle und beinhaltet Beziehungen zur Tabelle p5_treffer.

---

### app/Models/Recherche/P5ScreeningKriterium.php
Model-Klasse für das Screeningkriterium-Tabelle-Modell. Dient als Abstraktion der Datenbanktabellen p5_screening_kriterien und verknüpft diese mit der Projekt-Tabelle über die projekt_id Felder.

---

### app/Models/Recherche/P5ToolEntscheidung.php
P5ToolEntscheidung-Model: Zugriff auf p5_tool_entscheidung-Tabelle
Repräsentiert Entscheidungen zu Tools für Projekte
Mit Projekt-Beziehung und Booleschem Wert "gewaehlt"
Hält UUIDs, Spalten "projekt_id", "tool" und "begruendung"

---

### app/Models/Recherche/P5Treffer.php
```json
{
  "file_summary": "Dieses Modell stellt die Tabelle p5_treffer der Datenbank dar und bietet Zugriff auf verwandte Modelle. Es ermöglicht das Lesen und Schreiben von Daten zu einzelnen Treffern in der Recherche. Es gibt Beziehungen zu Projekten, Duplikaten, Screening-Entscheidungen, Qualitätsbewertungen und Datenextraktionen.",
  "key_classes": ["P5Treffer"],
  "important_functions": [
    "projekt", 
    "duplikatOriginal",
    "duplikate",
    "screeningEntscheidungen",
    "qualitaetsbewertung",
    "datenextraktion"
  ],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P6Luckenanalyse.php
```
{
  "file_summary": "Die Datei P6Luckenanalyse.php modelliert ein Eloquent-Modell für die Tabelle p6_luckenanalyse in der Datenbank. Sie definiert Beziehungen zu anderen Tabellen und stellt Attribute zur Verfügung, die im Tabellen-Design festgelegt sind.",
  "key_classes": ["P6Luckenanalyse"],
  "key_methods": ["projekt", "__construct"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
```
{
  "file_summary": "Die Datei P6Qualitaetsbewertung ist ein Model zur Datenabfrage. Sie enthält Funktionen zum Lesen und Bearbeiten von Qualitätsbewertungen in der Datenbank.",
  "key_classes_functions_endpoints": [
    "P6Qualitaetsbewertung (Model)",
    "treffer() (BelongsTo-Beziehung)"
  ],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Model",
    "Illuminate\Database\Eloquent\Concerns\HasUuids"
  ]
}
```

---

### app/Models/Recherche/P7Datenextraktion.php
Die Datei P7Datenextraktion.php bietet Zugriff auf Daten aus der Tabelle p7_datenextraktion in der Datenbank. Sie verfügt über eine Beziehung zu der P5Treffer-Klasse und ermöglicht das Abfragen von spezifischen Spalten wie land, stichprobe_kontext und hauptbefund.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
```json
{
  "file_summary": "Datenaufnahme-Modell für P7-Grade-Einschätzungen",
  "main_class": "P7GradeEinschaetzung",
  "important_methods": ["projekt"],
  "table_name": "p7_grade_einschaetzung",
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P7MusterKonsistenz.php
```
{
  "file_summary": "Eloquent-Model für Datenbank-Tabelle p7_muster_konsistenz. Beinhaltet Funktionen zum Lesen und Schreiben von Projekt-Konsistenz-Daten mit UUIDs.",
  "key_classes": ["P7MusterKonsistenz"],
  "main_functionality": ["Datenabfragen", "Datenänderungen"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Projekt::class"]
}
```

---

### app/Models/Recherche/P7SyntheseMethode.php
Das Modell P7SyntheseMethode stellt die Datenstruktur für Synthesemethoden in der Tabelle p7_synthese_methode dar. Es beinhaltet Beziehungen zu Projekten und ermöglicht das Speichern von Projektinformationen.

---

### app/Models/Recherche/P8Limitation.php
P8Limitation ist eine Eloquent-Modell-Klasse für die Datenbank-Tabelle p8_limitationen. Sie enthält eine Beziehung zum Projekt und ermöglicht den Zugriff auf diese Tabelle in der Datenbank.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
```
{
  "file_summary": "Definiert die Datenbank-Tabelle für P8Reproduzierbarkeitspruefung mit Beziehungen zur Projekt-Tabelle.",
  "important_classes_functions_endpoints": {
    "Klassen": ["P8Reproduzierbarkeitspruefung"],
    "Beziehungen": ["projekt()"],
    "Eigenschaften": ["projekt_id", "pruefpunkt", "erfuellt", "anmerkung"]
  },
  "externale_abhaengigkeiten": {
    "DB-Zugriffe": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"],
    "APIs/imports": []
  }
}
```

---

### app/Models/Recherche/P8Suchprotokoll.php
Datentabelle für Suchprotokolle im Format P8. Hiermit werden Ergebnisse von Suchfunktionen nachverfolgt und gespeichert.

---

### app/Models/Recherche/P8UpdatePlan.php
```
{
  "file_summary": "Die Datei P8UpdatePlan.php ist ein Model für die Datenbanktabelle p8_update_plan. Sie stellt eine Verbindung zum Projekt her und bietet eine Möglichkeit, das nächste Update zu planen.",
  "key_classes": ["P8UpdatePlan"],
  "key_methods": ["projekt", "getFillable", "getCasts"],
  "external_dependencies": ["Projekt", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/PaperEmbedding.php
```json
{
  "file_summary": "Datenmodell für das Tableau \"paper_embeddings\", das Projektbezogene Metadaten zu Dokumenten enthält. Das Modell verfügt über Beziehungen zum zugehörigen Projekt und ermöglicht den Zugriff auf die Metadaten des Dokuments sowie den Zeitpunkt der Erstellung des Embeddings.",
  "key_classes": ["PaperEmbedding"],
  "important_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/Phase.php
Phase-Modell mit Eloquent-Bindung an Projekt. Speichert Phasen-Daten mit UUID und definiert die zugriffsbaren Attributen sowie die Beziehung zur Projekts-Tabelle.

---

### app/Models/Recherche/Projekt.php
Diese Datei definiert das Projekt-Modell in der Datenbank. Es enthält Beziehungen zu Benutzern und verschiedenen Phasen eines Recherche-Projekts. Die Klassen 'Projekt', 'User' und verschiedene Phase-Klassen (z.B. 'Phase', 'P1Strukturmodellwahl') werden definiert.

---

### database/factories/UserFactory.php
```
{
  "file_summary": "Erzeugt fiktive User-Daten für die Anwendung mithilfe von Factories.\nWichtige Klassen: UserFactory, Factory\nFunktionen: definition(), unverified(), withoutTwoFactor()\nExterne Abhängigkeiten: Illuminate\Database\Eloquent\Factories\Factory, Illuminate\Support\Facades\Hash, Illuminate\Support\Str"
}
```

---

### database/factories/Recherche/ProjektFactory.php
```json
{
  "file_summary": "Erstellt Projekt-Instanzen mit fiktiven Daten für Tests und Entwicklung.",
  "important_classes": ["ProjektFactory", "Projekt"],
  "function": "definition",
  "external_dependencies": [
    "User:factory()",
    "Illuminate\Database\Eloquent\Factories\Factory"
  ]
}
```

---

### database/migrations/0001_01_01_000000_create_users_table.php
Diese Datei erstellt die Tabellen users, password_reset_tokens und sessions in der Datenbank. Die Tabellen enthalten Felder für Benutzerkennungen, Passwörter und Sitzungsdaten.

---

### database/migrations/0001_01_01_000001_create_cache_table.php
```json
{
  "file_summary": "Datenbankmigration für die Erstellung von Cache-Tabellen. 
  Die Datei erstellt zwei Tabellen: 'cache' und 'cache_locks'. 
  Beide Tabellen werden bei der Umkehrung wieder gelöscht."
}
```

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
```
{
  "file_summary": "Migration für die Datenbank-Struktur: Jobs, Job-Batches und fehlgeschlagene Jobs",
  "main_class": "Illuminate\Database\Migrations\Migration",
  "important_functions": ["up", "down"],
  "extern_abhängigkeiten": [
    "Illuminate\Database\Schema\Blueprint",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
```json
{
  "file_summary": "Liefert zwei Faktor-Sicherheitsfunktionen zur Benutzertabelle hinzu.",
  "key_classes": ["Migration"],
  "main_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
```
{
  "file_summary": "Migration zur Erstellung einer 'kontakte'-Tabelle in der Datenbank. Die Tabelle enthält Felder wie Name, E-Mail, Projekttyp und Nachricht. Externe Abhängigkeiten: Laravel's Migrations- und Schema-Klassen.",
  "main_classes": ["Migration", "Blueprint"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Migration zur Erstellung einer 'page_views'-Tabelle in der Datenbank. Tabelle enthält Spalten für ID, Besucherzähler und Zeitstempel.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
Erfasst die Funktionen der Datei database/migrations/2026_03_31_000001_create_permission_tables.php

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Datenbankmigration zur Hinzufügung von Spalten an der 'page_views'-Tabelle: path, ip_anonymous und user_agent.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
Migrationsdatei für die Erstellung einer Tabelle zur Verwaltung von Nutzungsrechten (Consents).
Klasse: Migration, wichtigste Funktionen: up() und down().
Erstellt eine Tabelle 'consents' mit folgenden Spalten:
- id
- user_id (FK zu der Benutzer-Tabelle)
- ip_anonymous
- consent_categories (JSON-Feld)
- consented_at
- revoked_at
- created_at und updated_at (Timestamps).

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
```json
{
  "file_summary": "SQLite-aktualisierungen für die Recherche-Tabelle in der Datenbank. Fügt Tabellen hinzu oder ändert Spalten bei Bedarf.",
  "key_classes": ["Migration", "Blueprint", "DB"],
  "important_functions": ["up"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Erstellt und löscht PostgreSQL-Enums und -Extensions in einer Datenbankmigration. Enthält Funktionen zum Erstellen von UUID-OSSP- und pg_TRGM-Extensions, sowie zum Definieren von Enums für verschiedene Datenfelder.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Migrationsdatei zur Erstellung der Tabellen "projekte" und "phasen" im PostgreSQL-Datenbank-System. Die Tabelle "projekte" enthält Informationen zu Projekten, wie Titel, Forschungsfrage, Verantwortlichem, Startdatum, Notizen sowie Timestamps für das letzte Update und die Erstellung. Die Tabelle "phasen" enthält Informationen zu Phasen innerhalb von Projekten, wie Titel, Notizen und ein Status-Field. Beide Tabellen werden mit Foreign Keys und Indices konfiguriert.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
```
{
  "file_summary": "Migration der Datenbank für das Projekt 'p1' mit folgenden Funktionen: 
  - Erstellen von vier Tabellen ('p1_strukturmodell_wahl', 'p1_komponenten', 'p1_kriterien', 'p1_warnsignale') 
  - Definition der Spalten und Beziehungen zwischen den Tabellen. Externe Abhängigkeiten: Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\DB"
}
```

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
```
{
  "file_summary": "Diese Datei definiert Migrations für die Erstellung von Tabellen in der Datenbank. Die wichtigsten Funktionen sind: 
- Erstellung von vier Tabellen ('p2_review_typ_entscheidung', 'p2_mapping_suchstring_komponenten', 'p2_trefferlisten' und 'p2_cluster') 
- Einfügen von Fremdschlüsseln für die Beziehungen zwischen diesen Tafeln und der 'projekte'-Tabelle
- Setzen von Konstanten und Defaults für bestimmte Spalten in den Tabellen.
  Die wichtigsten Klassen sind: Migration, Blueprint, DB und Schema.
  Externe Abhängigkeiten sind: Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\DB und Illuminate\Support\Facades\Schema."
}
```

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
Dieser Datei enthält eine Migration, die vier neue Tabellen in der Datenbank erstellt: 'p3_disziplinen', 'p3_datenbankmatrix', 'p3_geografische_filter' und 'p3_graue_literatur'. Die Tabelle 'p3_disziplinen' hat eine Beziehung zu 'projekte', die anderen Tabellen haben ebenfalls eine Beziehung zu 'projekte'.

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
```
{
  "file_summary": "Migrationsdatei für PostgreSQL-Datenbank, um Tabelle p4_suchstrings, p4_thesaurus_mapping und p4_anpassungsprotokoll zu erstellen.\nKlassen: Migration\nFunktionen: up, down\nEndpunkte: keine\nExterne Abhängigkeiten: Illuminate\Database\Schema\Blueprint, DB"
}
```

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Erstellt und konfiguriert Tabelle-Struktur für Datenbank, um die Verwaltung von Projekt-Daten zu unterstützen. Fügt Tabellen wie 'p5_prisma_zahlen', 'p5_screening_kriterien', 'p5_treffer' und 'p5_screening_entscheidungen' hinzu.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
```json
{
  "file_summary": "Datenbankmigration, um zwei Tische zu erstellen: p6_qualitaetsbewertung und p6_luckenanalyse. Die Migration wird nur ausgeführt, wenn die Datenbank ein PostgreSQL-System ist.",
  "key_classes_functions": ["Migration", "Blueprint", "DB"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Datenbankmigrationen für die Tabelle p7_*

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Dieses Skript erstellt Datenbanktabellen für das Projekt 'P8' und definiert die Beziehungen zwischen ihnen. Die Tabellen umfassen Informationen zu Suchprotokollen, Limitationen, Reproduzierbarkeitsprüfungen und Update-Plänen. Es wird davon ausgegangen, dass es sich um eine PostgreSQL-Datenbank handelt.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Diese Datei erstellt und löscht PostgreSQL-Indizes für das Projekt. Hauptfunktionen sind die Erstellung von Indizes auf bestimmten Tabellenfeldern sowie die Erstellung eines Full-Text-Suchindizes für Abstracts und Titel in deutscher Sprache.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
```
{
  "file_summary": "Diese Datei erstellt eine PostgreSQL-Funktion zum Berechnen von PRISMA-2020-Kennzahlen für ein Recherche-Projekt. Die Funktion zählt Werte für das PRISMA-Flussdiagramm und wird direkt auf DB-Ebene ausgeführt.",
  "main_classes": ["Migration"],
  "important_functions": ["up", "down"],
  "externale_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB"]
}
```

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Erstellt die Tabelle für Chat-Nachrichten in der Datenbank. Verwaltet die Beziehungen zwischen Benutzern und Nachrichten sowie prüft den Wert des Spalten "role".

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Erstellt eine Tabelle für Webhooks und fügt eine Beziehung zu der "chat_messages"-Tabelle hinzu. Die Migration unterstützt nur PostgreSQL als Datenbank-System.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Migration zum Erstellen einer Tabelle für Papier-Embeddings im PostgreSQL-Datenbank-System. Hierfür wird die Extension 'vector' erstellt und eine Tabelle mit folgenden Feldern erstellt: id, projekt_id, source, paper_id, title, chunk_index, text_chunk, metadata (JSON), erstellt_am. Danach wird eine Spalte für den Embedding-Vektor hinzugefügt und ein Index auf die Embeddings-Spalte erstellt.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
```
{
  "file_summary": "Migration zum Erstellen einer Tabelle für Aktivitätsprotokolle. 
   - Eine Tabelle 'activity_log' wird mit Spalten für Log-Name, Beschreibung, 
   - Subjekt-ID und -Typ, Ereignis, Causer-ID und -Typ sowie Attribut-Änderungen und Eigenschaften erstellt.
   - Die Migration unterstützt UUID und Integer als Primärschlüssel."
}
```

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
```
{
  "file_summary": "Migration für PostgreSQL-Datenbank: 
    - Erstellung eines Status-Typs mit Werten \"trial\", \"active\", \"suspended\" und \"cancelled\"
    - Hinzufügen einer Spalte \"status\" zum Benutzer-Table mit Standardwert \"trial\"
    - Konvertierung der Spalte \"status\" vom String-Typ zum erstellten Status-Typ
    In SQLite-Datenbank (für Tests): 
    - Hinzufügen einer Spalte \"status\" zum Benutzer-Table mit Standardwert \"trial\"",
  "main_classes": ["Migration"],
  "important_functions": ["DB::statement", "Schema::table", "Blueprint::string", "DB::getDriverName"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", 
                            "Illuminate\Database\Schema\Blueprint", 
                            "Illuminate\Support\Facades\DB",
                            "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
Migriert Datenbank-Struktur: Füttige ein neues Feld "frontend_object" in der Tabelle "webhooks".

---

### database/migrations/2026_04_01_200001_make_webhook_password_nullable.php
```json
{
  "file_summary": "Migration, um das Attribut 'password' in der Tabelle 'webhooks' optional zu machen, wenn PostgreSQL als Datenbank-System verwendet wird. Wenn PostgreSQL nicht als Datenbank-System verwendet wird, werden die Änderungen ignoriert.",
  "main_classes": ["Migration"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_04_01_200002_add_secret_to_webhooks.php
```
{
  "file_summary": "Migration, die eine Spalte namens 'secret' zur Tabelle 'webhooks' im PostgreSQL-Datenbank-System hinzufügt und wieder entfernt. Diese Migration ist spezifisch für das PostgreSQL-Datenbank-System.",
  " wichtigste Klassen/Funktionen": "Illuminate\Database\Migrations\Migration",
  "externe Abhängigkeiten": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/seeders/DatabaseSeeder.php
```
{
  "file_summary": "Datenbank-Seed für den Testbetrieb: Erzeugt Administrator-Konto, ruft Rollen-Seeds auf.",
  "important_classes": ["DatabaseSeeder", "RoleSeeder", "RechercheDemoSeeder"],
  "key_functions": ["run", "firstOrCreate"],
  "external_dependencies": ["App\Models\User", "Illuminate\Database\Seeder"]
}
```

---

### database/seeders/RechercheDemoSeeder.php
Seeds eine Demo-Datei für ein Forschungsprojekt, um die Verwendung von KI-gestützter Bildanalyse in der Radiologie zu erforschen. Sie erstellt verschiedene Modelle und Strukturen, wie das PICO-Muster und Trefferlisten in verschiedenen Datenbanken, sowie Webhooks für die Kommunikation mit externen Systemen.

---

### database/seeders/RoleSeeder.php
Diese Datei erstellt standardmäßige Rollen in der Datenbank, darunter Admin und User. Es handelt sich um eine Seeder-Datei für die Datenbank.

---

### database/sql/create_langdock_agent_user.sql
Erstellt DB-Benutzer 'langdock_agent' mit eingeschränkten Zugriffsmöglichkeiten auf bestimmte Tabellen. Importiert keine externen Abhängigkeiten.

---

## temp_dummy

### .github/pull_request_template.md
Vorlage für Pull-Request Beschreibungen und -Checklisten in Markdown-Format.

---

## tests

### tests/Pest.php
```
{
  "file_summary": "Die Datei \"Pest.php\" definiert Testfunktionen für PHPUnit-Tests, wie z.B. Erweiterungen der `expect()` Funktion und globale Funktionen.",
  "wichtige_Klassen_Funktionen_Endpunkte": ["PHPUnit\Framework\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"],
  "externe_Abhängigkeiten": ["PHPUnit\Framework\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/TestCase.php
Basis-Klasse für unitäre Tests, erweitert die Basis-Klasse von Laravel. Enthält keine eigenen Funktionen oder Endpunkte.

---

### tests/Feature/ContactTest.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Kontaktformular-Funktionen. Es werden mehrere Tests durchgeführt, um sicherzustellen, dass die Form korrekt funktioniert, wenn alle Pflichtfelder eingeben werden und auch wenn einige Felder optional sind. Zusätzlich werden Szenarien getestet, in denen die Eingabe fehlt oder ungültig ist.",
  "klasse": "ContactTest",
  "funktionen": ["test"],
  "endpunkte": ["contact.store"],
  "extern_imports": ["App\Models\Contact"]
}
```

---

### tests/Feature/DashboardTest.php
```json
{
  "file_summary": "Diese Datei definiert Tests für das Dashboard der Anwendung. Sie prüft die Zugriffssteuerung und Routing.",
  "important_classes_functions_endpoints": ["User", "route('dashboard')", "route('login')"],
  "external_dependencies": ["App\Models\User"]
}
```

---

### tests/Feature/ExampleTest.php
Diese Datei enthält einen Einheitstest für die API-Route 'home'. Der Test überprüft, ob eine erfolgreiche Antwort (HTTP-Statuscode 200) erhalten wird.

---

### tests/Feature/LegalPagesTest.php
Diese Datei enthält Tests für die Anzeige von Impressum-, DSGVO- und AGB-Seiten. Es werden GET-Anfragen an Endpunkte ausgelöst und der Status-Code (200) sowie bestimmte Textinhalte überprüft.

---

### tests/Feature/ProjektPolicyTest.php
```
{
  "file_summary": "Test-Datei für das Projekt-Policy-Verhalten. Diese Datei enthält sechs Test-Fälle, um die Funktionalität der Projekt-Police zu überprüfen.",
  "key_classes": ["ProjektPolicy", "User", "Projekt"],
  "important_functions": ["view", "update", "delete", "create"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "App\Models\User", "App\Policies\ProjektPolicy"]
}
```

---

### tests/Feature/RechercheTest.php
```
{
  "file_summary": "Dokumentiert Funktionen zur automatischen Erstellung von Rechercheprojekten und deren Zugriffsberechtigungen.\n\nWichtige Klassen/Funktionen: Volt, User, Projekt, TriggerLangdockAgent.\n\nExterne Abhängigkeiten: Livewire\Volt, Illuminate\Support\Facades\Queue.",
  "key_classes": ["Volt", "User", "Projekt", "TriggerLangdockAgent"],
  "important_functions": ["starteRecherche", "recherche.research-input"]
}
```

---

### tests/Feature/WebhookTest.php
Diese Datei enthält Tests für die Funktion der Webhook-Endpunkte. Es werden Szenarien getestet, bei denen ein Webhook-Ruf abgelehnt wird, wenn kein Signatur-Headereintritt oder eine gültige Signatur fehlt. Zudem wird getestet, ob ein Ruf akzeptiert wird, wenn alle Voraussetzungen erfüllt sind. Die wichtigsten Klassen und Funktionen hierbei sind die Webhook-Endpunkte (/api/webhooks/langdock) sowie die Verwendung von Queue::fake() zum Simulieren von Nachrichten in der Warteschlange.

---

### tests/Feature/WelcomeTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Funktionen der 'Welcome'-Seite eines Webapplikations-Systems. Es gibt vier Funktionsprüfungen: 
   - Die 'Welcome'-Seite sollte eine erfolgreiche Antwort liefern.
   - Die Besuche auf der Seite sollten im Datenbank-Tableau 'page_views' richtig gezählt werden.
   - Wenn noch keine Einträge in 'page_views' vorhanden sind, sollte nach dem Besuch der Seite genau ein Eintrag vorhanden sein.
   - Für ungeleisterte Benutzer sollte die Anmeldung und Registrierungsmöglichkeit angezeigt werden, während angemeldete Nutzer eine Verwaltungsoberfläche sehen sollten."
}
```

---

### tests/Feature/Auth/AuthenticationTest.php
Diese Datei enthält ein Test-Szenario für die Authentifizierung in der Anwendung. Es werden Funktionalitäten wie Login, Benutzerauthentifizierung und Logout getestet.

---

### tests/Feature/Auth/EmailVerificationTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die E-Mail-Verifizierungsfunktionen. Die wichtigsten Funktionen sind: \
    - Prüfung der Renderung des E-Mail-Verifizierungs-Screens, \
    - Prüfung der erfolgreichen Verifizierung einer E-Mail-Adresse und Abschluss der Verifizierung durch einen Event,\
    - Überprüfung, ob die E-Mail nicht mit einem falschen Hash verifiziert werden kann,\
    - Prüfung, dass eine bereits verifizierte Benutzerin auf den Verifizierungslink umgeleitet wird, ohne den Event erneut auszulösen.",
  "main_classes": ["User", "Verified"],
  "main_functions": ["URL::temporarySignedRoute", "Event::fake", "Event::assertDispatched"],
  "external_dependencies": ["Illuminate\Support\Facades\Event", "Illuminate\Auth\Events\Verified", "Illuminate\Support\Facades\URL"]
}
```

---

### tests/Feature/Auth/PasswordConfirmationTest.php
Diese Datei ist ein Test für die Authentifizierung von Benutzern, insbesondere die Bestätigung der Passwörter. Sie verwendet den Factory für Benutzer und stellt sicher, dass das Password-Confirmation-Screen korrekt rendern kann.

---

### tests/Feature/Auth/PasswordResetTest.php
Diese Datei enthält Tests für das Passwort-Restore-Feature. Es prüft die Funktionalität des Anfragens und der Bearbeitung eines Passworts, einschließlich der Überprüfung von Benutzerbestätigungen und der Änderung des Passworts.

---

### tests/Feature/Auth/RegistrationTest.php
Diese Datei enthält Registrierungstests für Benutzer. Es gibt zwei Tests: Der erste Test prüft, ob die Registrierungsseite erfolgreich gerendert werden kann. Der zweite Test prüft, ob ein neuer Benutzer erfolgreich registriert werden kann. Die wichtigen Funktionen sind get und post, sowie die Verwendung von route und assert. Externe Abhängigkeiten sind none.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
```
{
  "file_summary": "Diese Datei definiert zwei Testfälle für die Zwei-Faktor-Authentifizierung: \
  - Überprüft, ob ein nicht angemeldeter User auf eine Login-Seite weitergeleitet wird. \
  - Prüft, ob eine Zwei-Faktor-Challenge korrekt gerendert und ausgeführt wird."
}
```

---

### tests/Feature/Chat/BigResearchChatTest.php
```json
{
  "file_summary": "Dokumentiert Funktionalität für Chat-Funktionalitäten wie Nachrichten-Senden, Webhook-Verbindungen und Fehlerbehandlung",
  "wichtige Klassen/Funktionen/Endpunkte": ["ChatMessage", "User", "Webhook", "Http", "Volt"],
  "externe Abhängigkeiten": ["Illuminate\Support\Facades\Http", "Illuminate\Support\Str"]
}
```

---

### tests/Feature/Recherche/PhaseP1Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Phase P1 der Recherche-Funktion. Die Tests prüfen die Funktionalität von Strukturmodellwahlen, Komponenten und Kriterien. Es gibt Tests für die Erstellung, Bearbeitung und Löschung dieser Daten. Externe Abhängigkeiten sind App\Models\* (Models) und Livewire\Volt\Volt. Die wichtigsten Klassen/Funktionen/Endpunkte sind P1Strukturmodellwahl, P1Komponente, P1Kriterium und Volt.",
  "important_classes": ["P1Strukturmodellwahl", "P1Komponente", "P1Kriterium", "Volt"],
  "external_dependencies": ["App\Models\*", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP2Test.php
```json
{
  "file_summary": "Diese Datei enthält unitäre Tests für die Phase P2 des Recherche-Prozesses. Die Funktionen umfassen das Erstellen, Bearbeiten und Löschen von Review-Typ-Entscheidungen, Clustern, Mapping-Suchstring-Komponenten und Trefferlisten. Die wichtigsten Klassen sind Projekt, User, P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente und P2Trefferliste. Externe Abhängigkeiten sind die Dateien App\Models\*, App\Models\User und Livewire\Volt\Volt."
}
```

---

### tests/Feature/Recherche/PhaseP3Test.php
```
{
  "file_summary": "Dieser Code enthält Testfälle für die Phase P3 der Recherche-Funktion. Es werden verschiedene Funktionen wie Datenbankmatrix, Disziplin, geografischer Filter und graue Literatur getestet. Die Tests prüfen sowohl die Erstellung als auch die Bearbeitung und Löschung dieser Elemente.",
  "main_classes": ["P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"],
  "key_functions": ["newDb", "saveDb", "editDb", "deleteDb", "newDis", "saveDis", "editDis", "deleteDis", "newGeo", "saveGeo", "editGeo", "deleteGeo", "newGrau", "saveGrau", "editGrau"],
  "external_dependencies": ["App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP4Test.php
Diese Datei enthält Tests für die Phase P4 der Recherche-Funktionalität. Sie umfasst Tests für Suchstrings (P4Suchstring), Thesaurus-Mappings (P4ThesaurusMapping) und Anpassungsprotokolle (P4Anpassungsprotokoll). Die wichtigsten Klassen sind P4Suchstring, P4ThesaurusMapping und P4Anpassungsprotokoll. Externe Abhängigkeiten beinhalten die Datebanken PubMed, CINAHL und Embase.

---

### tests/Feature/Recherche/PhaseP5Test.php
```
{
  "file_summary": "Diese Datei besteht aus Tests für die Phase P5 in einer Recherchemanifestation. Sie prüft Funktionen wie das Erstellen, Bearbeiten und Löschen von Prisma-Zahlen, Screening-Kriterien, Tool-Entscheidungen und Screening-Entscheidungen. Darüber hinaus wird die Authentifizierung getestet.",
  "main_classes": ["P5PrismaZahlen", "P5ScreeningKriterium", "P5ToolEntscheidung", "P5Treffer"],
  "key_functions": ["newPrisma", "editPrisma", "deletePrisma", "saveSk", "deleteSk", "newTool", "deleteTool", "openScreen", "saveScreen"],
  "external_dependencies": ["App\Models\Recherche*", "App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP6Test.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Funktionen der Phase P6 in einem Recherche-Projekt.
    Die Tests überprüfen das Erstellen, Bearbeiten und Löschen von Qualitätsbewertungen (P6Qualitaetsbewertung) und Lückenanalysen (P6Luckenanalyse).
    Die Tests verwenden Livewire für die Interaktion mit der Anwendung und prüfen verschiedene Szenarien, wie das Erstellen einer neuen Qualitätsbewertung,
    das Bearbeiten oder Löschen bestehender Einträge sowie das Prüfen von Authentifizierungs-Szenarien.
    Externe Abhängigkeiten sind die Livewire-Bibliothek und die Modelle der Anwendung (Projekt, User, P5Treffer, P6Qualitaetsbewertung, P6Luckenanalyse)."
}
```

---

### tests/Feature/Recherche/PhaseP7Test.php
```
{
  "file_summary": "Datei definiert Tests für Phase P7 der Recherche-Funktion.
   - Prüft die Erstellung, Bearbeitung und Löschung von Synthese-Methode, Datenextraktion und Muster/Konsistenz.
   - Überprüft die Validierung von Pflichtfeldern.
   - Testet die Authentifizierung für fremde Benutzer.",
  "important_classes": ["P7SyntheseMethode", "P7Datenextraktion", "P7MusterKonsistenz", "P7GradeEinschaetzung"],
  "externale_abhaengigkeiten": ["App\Models\Recherche\", \"Livewire\Volt\Volt\"],
  "hauptfunktionen": ["Test der verschiedenen Phasen von P7"]
}
```

---

### tests/Feature/Recherche/PhaseP8Test.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Funktionalität der Phase P8 einer Recherche. Die folgenden Funktionen werden getestet:\n- Erstellung eines Suchprotokolls\n- Validierung von Pflichtfeldern im Suchprotokoll\n- Bearbeitung eines Suchprotokolls\n- Löschung eines Suchprotokolls\n- Erstellung einer Limitation\n- Löschung einer Limitation\n- Erstellung einer Reproduzierbarkeitsprüfung\n- Löschung einer Reproduzierbarkeitsprüfung\n- Erstellung eines Update-Plans\n- Löschung eines Update-Plans\nAußerdem wird der Zugriff auf die Phase P8 durch einen fremden Benutzer getestet.",
  " wichtigste Klassen": "P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung, P8UpdatePlan",
  "Hauptfunktionen": "newSp, editSp, saveSp, newLim, saveLim, deleteLim, newRep, saveRep, deleteRep, newUp, saveUp, deleteUp",
  "externe Abhängigkeiten": "App\Models\User, App\Models\Recherche, Livewire\Volt\Volt"
}
```

---

### tests/Feature/Recherche/ProjektDetailTest.php
Diese Datei enthält Tests für den Projekt-Detail-Bereich in der Recherche-Funktion. Sie testet die Rendern von Tab-Panels, die Wechselbarkeit zwischen Tabs und die Renderung von Phase-Komponenten. Zudem wird die Funktion zur Ermittlung des Status einer Phase getestet und der Zurück-Link zum Projekt-Listeneintrag überprüft.

---

### tests/Feature/Settings/AppearanceTest.php
```json
{
  "file_summary": "Diese Datei enthält Tests für die Erscheinungsform-Einstellungen in einer Anwendung. Es werden Funktionen wie die Rendrung von Buttons, Containers und clientseitigen Skripten sowie die Authentifizierungsbegrenzung getestet.",
  "klasse_namen": ["User", "Volt"],
  "funktionen": ["test", "assertSee", "assertSeeHtml", "get", "assertRedirect", "assertOk"],
  "externe_abhaengigkeiten": ["App\Models\User", "Livewire\Volt\Volt", "\Illuminate\Foundation\Testing\RefreshDatabase"]
}
```

---

### tests/Feature/Settings/PasswordUpdateTest.php
```json
{
  "file_summary": "Tests die das Aktualisieren von Passwörtern überprüfen, wobei dabei die korrekte Eingabe des aktuellen Passworts und der Bestätigung erforderlich ist.",
  "main_classes": ["User", "Hash", "Volt"],
  "main_functions": ["test", "create", "actingAs", "refresh"],
  "external_dependencies": ["App\Models\User", "Illuminate\Support\Facades\Hash", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/ProfileUpdateTest.php
```json
{
  "file_summary": "Diese Datei definiert Tests für das Profil-Update-Funktionalität, einschließlich der Überprüfung von Benutzerinformationen und der Möglichkeit, sein Konto zu löschen. Die wichtigsten Funktionen sind: Profile-Informationen aktualisieren, E-Mail-Verifizierungsstatus überprüfen und Konto löschen.",
  "important_classes": ["User", "Volt"],
  "key_functions": ["updateProfileInformation", "deleteUser"],
  "external_dependencies": ["Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
```json
{
  "file_summary": "Diese Datei enthält Tests für die Zwei-Faktor-Authentifizierung in Laravel. Sie prüft, ob die Einstellungsbildschirm für Zwei-Faktor-Authentifizierung korrekt gerendert wird und ob bestimmte Anforderungen erfüllt sind.",
  "wichtige_Klassen": ["User", "Features", "Volt"],
  "wichtige_Funktionen": [""twoFactorAuthentication"", ""assertOk"", ""assertSee""],
  "externe_Abhängigkeiten": ["Laravel\Fortify\Features", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Settings/WebhooksTest.php
```
{
  "file_summary": "Dokumentiert Funktionen für die Verwaltung von Webhooks (Dashboard Chat und Recherche starten) in Tests. Prüft Validierung und Speicherung von Webhook-Einstellungen.",
  "key_classes": ["Webhook", "User"],
  "key_functions": ["saveDashboard", "clearDashboard", "saveRecherche", "clearRecherche"],
  "external_dependencies": ["Livewire\Volt\Volt", "App\Models\User", "App\Models\Webhook"]
}
```

---

### tests/Unit/ExampleTest.php
```json
{
  "file_summary": "Dies ist ein Einheitstest-File, welches die Funktion \"toBeTrue()\" des PHPUnit-Frameworks ausprobieren soll. Der Test überprüft, ob der Wert True tatsächlich wahr ist.",
  "important_classes": ["PHPUnit\Framework\TestCase"],
  "key_functions": ["expect", "toBeTrue"],
  "external_dependencies": ["PHPUnit\Framework\TestCase"]
}
```

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
Definiert den List-Modus für die Kontakt-Ressource. Verwendet die Filament-Pages-Bibliothek.

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert eine Filament-Paginierung zur Anzeige von Kontaktdaten. Sie erweitert die Basis-Klasse 'ViewRecord' und weist die Ressource 'ContactResource' zu.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
```json
{
  "file_summary": "Diese Datei definiert die Edit-Page für den Benutzer in der Filament-Ressource. 
   Sie erstellt eine Verbindung zur Ressourcendefinition 'UserResource' und ermöglicht Änderungen am Benutzerprofil.
   Die wichtigste Klasse ist 'EditUser'."
}
```

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
```json
{
  "file_summary": "Definiert eine Seite zur Anzeige von Benutzern im Admin-Bereich von Filament. Dient als Basis für die Darstellung aller Benutzer in einer Liste.",
  "main_class": "ListUsers",
  "important_functions": [],
  "endpoints": [],
  "imports": ["App\Filament\Resources\UserResource"],
  "api_calls": [],
  "db_access": []
}
```

---

### resources/views/dashboard.blade.php
Dieses Blade-Template stellt das Dashboard dar und enthält Links zu verschiedenen Funktionen, wie Recherche und Admin. Es zeigt auch die letzten Projekte des Benutzers an.

---

### resources/views/welcome.blade.php
HTML-Datei für die Benutzeroberfläche von Linn Games. Definiert Stil und Layout der Website sowie einige Interaktions-Effekte.

---

### resources/views/components/action-message.blade.php
```json
{
  "file_summary": "Blade-Template für Action-Messages, die bei einer Aktion angezeigt werden. 
Bietet x-data-basierten Overlay mit Zeitverlauf und Übergangseffekten.
Funktioniert mit 'on'-Prop und Slot."
}
```

---

### resources/views/components/app-logo-icon.blade.php
Diese Datei enthält einen SVG-Bildschirm für das Logo einer Anwendung. Sie übernimmt die Attribute des Eltern-Knotens und rendernt ein komplexes Grafikmuster.

---

### resources/views/components/app-logo.blade.php
Dokumentiert den App-Logo-Design-Bereich in einer Blade-Vorlage von Laravel. Der Bereich enthält ein Logo-Symbol und die Anwendungsnamen. Die Blade-Vorlage wird zum Erstellen einer App-Logo-Ansicht verwendet.

---

### resources/views/components/auth-header.blade.php
Rendert ein Auth-Header mit Titel und Beschreibung basierend auf den Prop-Übergebenen Werten.

---

### resources/views/components/auth-session-status.blade.php
Diese Datei ist für die Darstellung des Authentifizierungsstatus verwendet. Sie zeigt den Status als grünen Text an, falls er vorliegt. Hauptfunktion: Darstellen von Auth-Status-Meldungen.

---

### resources/views/components/placeholder-pattern.blade.php
Diese Datei ist ein Blade-Templat, das ein SVG-Pattern für einen Placeholder erzeugt. Es verwendet ein einzigartiges ID-Attribut und ein SVG-Element mit einem definierenden Bereich für ein Muster. Das Muster selbst besteht aus zwei sich kreuzenden Linien, die durch eine Pattern-Instanz in einem Defs-Bereich repräsentiert werden.

---

### resources/views/components/layouts/app.blade.php
Layout-Ansicht für die gesamte Anwendung mit Sidebar und Hauptinhaltsbereich. Die Sidebar enthält ein Titel-Feld und der Hauptinhaltsbereich enthält den anzuzeigenden Inhalt (Slot).

---

### resources/views/components/layouts/auth.blade.php
```json
{
  "file_summary": "Definiert ein einfaches Layout für Authentifizierung-Seiten. 
  Dazu wird das Component <x-layouts.auth.simple> verwendet, um Titel und Inhalt zu rendern."
}
```

---

### resources/views/components/layouts/app/header.blade.php
Diese Datei stellt das Header-Bereich einer Website dar. Sie enthält Links zur Hauptseite und zur Benutzerkontrolle, einschließlich Anmelden- und Abmelde-Funktionen.

---

### resources/views/components/layouts/app/sidebar.blade.php
Das ist ein Sidebar-Layout für eine Webanwendung. Es enthält Navigationsmenü, User-Menü und Links zu externen Ressourcen.

---

### resources/views/components/layouts/auth/card.blade.php
```
{
  "file_summary": "HTML-Templates für Authentifizierungs-Karten. 
  Inkludiert Logik für Links, Logos und Cookie-Bestätigungen.
  Abhängigkeit: cookie-consent::index"
}
```

---

### resources/views/components/layouts/auth/simple.blade.php
```
{
  "file_summary": "HTML-Struktur für Auth-Layouts, beinhaltet Anker und Inhaltsbereich.",
  "important_elements": ["@include('partials.head'), $slot, @include('cookie-consent::index')"],
  "external_dependencies": ["route('home'), config('app.name', 'Laravel')"
}
```

---

### resources/views/components/layouts/auth/split.blade.php
Blade-Template für die Layout-Komponente, die sich zwischen den beiden Seiten eines authentifizierten Users befindet. Es enthält ein Logo und einen Zitat-Bereich sowie Platzhalter für Inhalt. Externe Abhängigkeiten: partials.head, cookie-consent::index, livewireScriptConfig.

---

### resources/views/components/settings/layout.blade.php
Die Datei layout.blade.php definiert ein Seitenlayout für die Einstellungen einer Anwendung. Sie enthält eine Navigationsleiste mit Verknüpfungen zu verschiedenen Einstellungsseiten, wie Profil, Passwort, Zwei-Faktor-Authentifizierung und Änderung der Aufmachung. Außerdem ist Platz für einen Titel und Untertitel sowie ein Inhaltsbereich reserviert.

---

### resources/views/emails/contact-confirmation.blade.php
```
{
  "file_summary": "Diese Datei erstellt eine E-Mail-Bestätigung für Kontaktaufnahmen. Die wichtigsten Funktionen sind: 
  - Ausgabe von Anfragen-Daten (Name, Projekt-Typ, Zeit-Rahmen, Nachricht)
  - Formatierung der E-Mail mit HTML-Struktur
  Keine externen Abhängigkeiten vorhanden"
}
```

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei erstellt ein E-Mail-Templates für Kontaktanfragen. Es werden Informationen wie Name, E-Mail, Projekttyp und Nachricht des Kontakts angezeigt. Die Datei verwendet die $contact-Variable, um die erforderlichen Daten anzufügen.

---

### resources/views/emails/deploy-notification.blade.php
Die Datei deploy-notification.blade.php generiert eine E-Mail, die bei einem erfolgreichen Deployment versendet wird. Sie enthält Informationen über den Zeitpunkt, URL und Umgebung des neuen Releases sowie einen Hinweis auf eine erfolgreiche Konfiguration der Mail-Versand-Funktion.

---

### resources/views/layouts/legal.blade.php
Layout-Datei für rechtliche Inhalte. Definiert ein standardisiertes Aussehen und Struktur für legal relevante Seiten, wie Datenschutz-, AGB- und Impressum-Seiten.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für das Online-Spiel von Linn.Games. Die AGB regeln die Nutzung der Website und des Spiels durch registrierte Nutzer. Wichtige Themen sind Registrierung, Benutzerkonto, Nutzungsregeln, Kosten, Widerrufsrecht, Haftung, Sanktionen und Änderungen der AGB.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei enthält die Datenschutz-Erklärung für die Website und das Spiel. Sie erläutert, welche Daten erhoben werden, wie sie verwendet werden und welche Rechte Nutzer haben. Die wichtigsten Klassen sind die verschiedenen Abschnitte der Erklärung (Verantwortliche Stelle, Arten der verarbeiteten Daten, Zweck, Aufbewahrung etc.). Externe Abhängigkeiten gibt es nicht. Die Datei verweist auf einige externe Services wie Langdock für den KI-gestützten Recherche-Dienst.

---

### resources/views/legal/impressum.blade.php
Diese Datei zeigt das Impressum für die Website an. Sie enthält die Kontaktdaten des Verantwortlichen sowie eine Beschreibung der rechtlichen Informationen.

---

### resources/views/livewire/auth/confirm-password.blade.php
Diese Datei stellt die Benutzerschaltfläche für das Bestätigen des Passworts dar. Sie enthält ein Formular, in dem der Benutzer sein Passwort eingeben kann, um den Zugriff auf die Anwendung zu bestätigen.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei ist Teil der UI des Authentifizierungsprozesses und dient zur Erstellung einer Seite für das Vergessen eines Passworts. Sie enthält eine Form, um die E-Mail-Adresse einzugeben, anhand deren eine Passwort-Rest-Link gesendet wird.

---

### resources/views/livewire/auth/login.blade.php
Dieses Blade-Template stellt das Login-Fenster für Benutzer bereit. Es enthält Eingabefelder für Email-Adresse und Passwort, ein Abmelden-Button und optional einen Link zum Registrieren.

---

### resources/views/livewire/auth/register.blade.php
Bietet eine Register-Funktion mit allen erforderlichen Feldern für die Registrierung eines Benutzers. Die wichtigsten Klassen sind x-auth-header und x-auth-session-status. Externe Abhängigkeiten: route('register.store'), route('login').

---

### resources/views/livewire/auth/reset-password.blade.php
Diese Datei stellt ein Formular zur Passworrnerneuerung bereit. Die Benutzer müssen ihren neuen Passwort und dessen Bestätigung eingeben, um den Passwort-Reset zu bestätigen.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
```
{
  "file_summary": "Dieses Blade-Templat (View) stellt ein Authentifizierungs-Formular für die Zweistufige Authentifizierung dar. 
  Es enthält eine Form, um einen Authentifizierungscod zu eingeben oder alternativ einen Recovery-Cod zur Wiederherstellung der Zugriffsrechte.
  Die wichtigsten Funktionen sind die Validierung von Eingaben und das Senden des Codes an den Server für die Authentifizierung.",
  "key_classes": ["livewire"],
  "external_dependencies": ["Route", "Form", "Auth-Header", "Layouts.Auth"]
}
```

---

### resources/views/livewire/auth/verify-email.blade.php
Diese Datei zeigt eine E-Mail-Vereifizierungsbildschirm, auf dem Benutzer nach dem Einloggen bestätigen müssen. Es gibt zwei Formulare: Einer zum erneuten Versenden des Bestätigungslinks und einer zum Abmelden. Die wichtigsten Klassen sind x-layouts.auth. Externe Abhängigkeiten sind die Routing-Funktionen von Laravel.

---

### resources/views/livewire/chat/big-research-chat.blade.php
Livewire-Komponente für einen großen Chat-Dialog mit Webhook-Unterstützung.

---

### resources/views/livewire/recherche/phase-p1.blade.php
```
{
  "file_summary": "Diese Datei ist ein Livewire-Component für die Phase P1 einer Recherche. Sie enthält CRUD-Funktionen für Strukturmodelle, Komponenten, Kriterien und Warnsignale sowie eine Authentifizierung, um sicherzustellen, dass nur der Besitzer eines Projekts darauf zugreifen kann.",
  "main_classes": ["Livewire\Volt\Component"],
  "main_functions": ["mount", "newSmw", "saveSmw", "editSmw", "deleteSmw", "cancelSmw", "newKomp", "saveKomp", "editKomp", "deleteKomp", "cancelKomp", ...],
  "externale_abhaengigkeiten": ["App\Models\Recherche\Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal"]
}
```

---

### resources/views/livewire/recherche/phase-p2.blade.php
```
{
  "file_summary": "Die Datei ist Teil einer Livewire-Komponente, die CRUD-Operationen für verschiedene Datenarten ermöglicht. Die wichtigsten Funktionen sind: 
    - ReviewTypEntscheidung: Neuanlegen, Bearbeiten und Löschen von Entscheidungen
    - Cluster: Neuanlegen, Bearbeiten und Löschen von Clustern
    - MappingSuchstringKomponente: Neuanlegen, Bearbeiten und Löschen von Suchkomponenten
    - Trefferliste: Neuanlegen, Bearbeiten und Löschen von Trefferlisten
  Die wichtigsten Klassen sind P2ReviewTypEntscheidung, P2Cluster, P2MappingSuchstringKomponente, P2Trefferliste. 
  Externe Abhängigkeiten sind die Modelle Projekt und Auth.",
}
```

---

### resources/views/livewire/recherche/phase-p3.blade.php
```
{
  "file_summary": "Diese Datei enthält ein Livewire-Component zum Management von Datenbankmatrixen, Disziplinen, geografischen Filtern und grauer Literatur in einem Projekt. Es bietet Funktionen zum Anlegen, Bearbeiten und Löschen dieser Einträge sowie Formulare für die Eingabe der Daten.",
  "main_classes": ["Livewire\Volt\Component"],
  "important_functions": [
    "newDb", "saveDb", "editDb", "deleteDb",
    "newDis", "saveDis", "editDis", "deleteDis",
    "newGeo", "saveGeo", "editGeo", "deleteGeo",
    "newGrau", "saveGrau", "editGrau", "deleteGrau"
  ],
  "external_dependencies": ["App\Models\Recherche\"]
}
```

---

### resources/views/livewire/recherche/phase-p4.blade.php
```
{
  "file_summary": "Livewire-Komponente für Recherche-Projekt-P4 mit CRUD-Funktionen für Suchstrings, Thesaurus-Mappings und Anpassungsprotokolle.",
  " wichtigste Klassen": "P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll",
  "Hauptfunktionen": "newSs, saveSs, editSs, deleteSs, newTh, saveTh, editTh, deleteTh, newAp, saveAp, editAp, deleteAp",
  "externe Abhängigkeiten": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p5.blade.php
```
{
  "file_summary": "Die Datei verantwortet das Eingeben und Verwalten von Daten zu einem Projekt. Sie ermöglicht die Änderung der Prisma-Zahlen, Screening-Kriterien und des verwendeten Tools sowie die Durchführung einer Treffer-Screening-Erkennung.",
  "important_classes": ["P5PrismaZahlen", "P5ScreeningKriterium", "P5ToolEntscheidung", "P5Treffer", "P5ScreeningEntscheidung"],
  "key_functions": ["savePrisma", "saveSk", "saveTool", "openScreen", "saveScreen"],
  "external_dependencies": ["App\Models\Recherche\", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p6.blade.php
```
{
  "file_summary": "Diese Datei ist Teil einer Livewire-Komponente und bietet Funktionen zur Verwaltung von Qualitätsbewertungen (Risk of Bias) und Lückenanalysen in einem Projekt. Sie verfügt über CRUD-Operationen für beide Arten von Analysen sowie eine Möglichkeit, Treffer auszuwählen. Externe Abhängigkeiten: App\Models\Recherche, Illuminate\Support\Facades\Auth, Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
```json
{
  "file_summary": "Livewire-Component für die Projektphase P7 mit Funktionen zur Synthese-Methode-Erstellung, Datenextraktion, Muster-Konsistenz-Bewertung und GRADE-Einschätzung. Die Komponente bietet CRUD-Funktionen für die einzelnen Formulare.",
  "important_classes": ["P7SyntheseMethode", "P7Datenextraktion", "P7MusterKonsistenz", "P7GradeEinschaetzung"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p8.blade.php
```
{
  "file_summary": "Dieses Blade-Templat handelt sich um eine Livewire-Komponente für die Projekt-Phase P8. Es ermöglicht das Bearbeiten und Verwalten von Suchprotokollen, Einschränkungen und Reproduzierbarkeitsprüfungen sowie des Update-Plans.",
  "key_classes": ["P4Suchstring", "P8Suchprotokoll", "P8Limitation", "P8Reproduzierbarkeitspruefung", "P8UpdatePlan"],
  "external_dependencies": ["App\Models\Recherche", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/projekt-detail.blade.php
```
{
  "file_summary": "Dies ist eine Livewire-Komponente, die einen Einzel-Projekt- Detail-Bereich darstellt. Sie enthält ein Header mit Projekt-Titel, -Erstellungsdatum und -Forschungsfrage sowie einen Tab-Menü zur Navigation zwischen verschiedenen Phasen des Projekts.",
  "key_classes": ["Livewire\Volt\Component"],
  "important_functions": ["mount", "switchTab", "getPhaseStatus"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/projekt-liste.blade.php
```
{
  "file_summary": "Diese Datei zeigt eine Liste von Recherche-Projekten, die vom aktuellen Nutzer erstellt wurden. Die Projekte werden in absteigender Reihenfolge nach Erstellungsort geordnet angezeigt. Ein Button ermöglicht es, ein Projekt zu löschen.",
  " wichtigsten Klassen/Funktionen/Endpunkte": ["projekte", "deleteProjekt"],
  "externe Abhängigkeiten": ["App\Models\Recherche\Projekt", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/research-input.blade.php
```json
{
  "file_summary": "Livewire-Komponente für die Recherchefunktion, die einen Benutzer über ein Formular ermöglicht, eine Forschungsfrage oder ein Thema einzugeben und die Recherche zu starten. Die Komponente validiert die Eingabe, erstellt ein neues Projekt in der Datenbank und startet den Langdock-Agent.",
  "important_classes": ["Livewire\Volt\Component", "App\Jobs\TriggerLangdockAgent"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth", "App\Models\Recherche\Projekt"]
}
```

---

### resources/views/livewire/settings/appearance.blade.php
```json
{
  "file_summary": "Livewire-Komponente für die Einstellungen der Oberflächengestaltung, ermöglicht den Wechsel zwischen Licht-, Dunkel- und System-Thema",
  "main_classes": ["Livewire\Volt\Component", "x-settings.layout"],
  "key_functions": ["applyTheme", "highlightButtons", "setAppearance"],
  "external_dependencies": ["localStorage", "window.matchMedia"]
}
```

---

### resources/views/livewire/settings/delete-user-form.blade.php
```json
{
  "file_summary": "Dieses Blade-Templat zeigt ein Modal-Fenster zur Löschung eines Benutzerkontos an. Das Fenster enthält einen Formularbereich, in dem der Benutzer sein Passwort eingeben muss, um das Konto zu löschen.",
  "key_classes": ["Livewire\Volt\Component", "Logout"],
  "functions": ["openModal", "closeModal", "deleteUser"],
  "endpunkte": [],
  "externe_abhaengigkeiten": ["App\Livewire\Actions\Logout", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/settings/password.blade.php
```json
{
  "file_summary": "Diese Datei ist ein Livewire-Komponenten-Template zur Änderung des Passworts eines Benutzers. Es enthält Formularelemente für das aktuelle Passwort, das neue Passwort und die Bestätigung des neuen Passworts. Wenn das Update abgeschlossen ist, wird der Benutzer aktualisiert und eine Nachricht wird gesendet.",
  " wichtigste Klassen": ["Livewire\Volt\Component"],
  "Hauptfunktionen": ["updatePassword"],
  "Externe Abhängigkeiten": ["Illuminate\Support\Facades\Auth", "Illuminate\Validation\Rules\Password"]
}
```

---

### resources/views/livewire/settings/profile.blade.php
```
{
  "file_summary": "Diese Datei enthält ein Livewire-Component, das die Profilinformationen des aktuellen Benutzers anzeigt und ändern lässt. Es bietet Funktionen zum Updaten des Namens und der E-Mail-Adresse sowie zum Senden einer Bestätigungs-E-Mail.",
  "important_classes_functions_endpunkte": ["Livewire\Volt\Component", "updateProfileInformation", "resendVerificationNotification"],
  "externe_abhaengigkeiten": ["Illuminate\Support\Facades\Auth", "Illuminate\Support\Facades\Session", "Illuminate\Validation\Rule"]
}
```

---

### resources/views/livewire/settings/two-factor.blade.php
```
{
  "file_summary": "Dies ist ein Livewire-Komponenten-Blade-Datei, die Funktionen für die Einstellung der zweifachen Authentifizierung (2FA) bereitstellt. Sie bietet Funktionen zur Aktivierung, Deaktivierung und Verwaltung von 2FA sowie einen Modalkonfigurationseigenschaften. Externe Abhängigkeiten beinhalten die Laravel-Fortify-Actions und Livewire-Attributen.",
  "key_classes": ["Livewire\Component", "ConfirmTwoFactorAuthentication", "DisableTwoFactorAuthentication", "EnableTwoFactorAuthentication"],
  "important_functions": ["enable", "disable", "confirmTwoFactor", "resetVerification"],
  "external_dependencies": ["Laravel\Fortify\Actions", "Laravel\Fortify\Features", "Livewire\Attributes"]
}
```

---

### resources/views/livewire/settings/webhooks.blade.php
```
{
  "file_summary": "Diese Datei konfiguriert Webhooks für verschiedene Frontend-Objekte in einer Anwendung. Es gibt zwei Slots: Dashboard Chat und Recherche starten. Für jeden Slot können die Webhook-URL und -Secret konfiguriert werden. Die Konfiguration kann gespeichert oder gelöscht werden.",
  "main_classes": ["Webhook", "Livewire\Volt\Component"],
  "important_functions": ["saveDashboard", "saveRecherche", "clearDashboard", "clearRecherche"],
  "external_dependencies": ["App\Models\Webhook", "Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
```
{
  "file_summary": "Die Datei verarbeitet die Recovery-Codes für die 2-Faktor-Authentifizierung eines Benutzers. 
    Der Benutzer kann Recovery-Codes generieren, sie anzeigen und wenn nötig neu generieren lassen. Die Recovery-Codes werden in einem JSON-Array gespeichert und bei Bedarf geladen."
}
```

---

### resources/views/partials/head.blade.php
Bereitstellt HTML-Header-Inhalte für Anwendungen wie Titel, Meta-Tags und CSS/JS-Ressourcen. Integriert externe Bibliotheken wie Fonts von Bunny.net.

---

### resources/views/partials/settings-heading.blade.php
Bereitstellt die Überschrift für Einstellungsbereich und Beschreibung der darin enthaltenen Funktionen. Die Datei enthält HTML-Elemente, um eine Überschrift und Beschreibung anzuzeigen.

---

### resources/views/recherche/index.blade.php
Layout und Anordnung der Recherche-Seite in einer UI-Komponente. Die Seite enthält Eingabefelder für eine Recherche (livewire:recherche.research-input) und eine Liste von Projekten (livewire:recherche.projekt-liste).

---

### resources/views/recherche/show.blade.php
Diese Datei definiert eine Blade-Vorschau für die Anzeige von Projekt-Detail-Inhalten in der Suchansicht. Es handelt sich um einen Teil des Frontend-Codes einer Webanwendung.

Hauptfunktionen:
- Rendern der Projektdaten
- Einbindung der Livewire-Komponente 'projekt-detail'

Wichtige Klassen/Funktionen/Endpunkte: x-layouts.app, livewire:recherche.projekt-detail

Externe Abhängigkeiten: Livewire-Komponente

---

## uncategorized

### Readme.md
Linn.Games ist eine Laravel-Anwendung für die systematische Literaturrecherche mit KI-gestützter Funktion. Die Anwendung verwendet PostgreSQL, Redis und Tailwind CSS und enthält Features wie Authentifizierung, Benutzerverwaltung, Kontaktformular und Admin-Panel (Filament).

---

### artisan
Laravel-Artisan-Datei, die Skripte zum Ausführen von Befehlen in der Konsole bereitstellt. Dazu gehören das Laden des Composer-Autoloader und das Bootstraping von Laravel.

---

### basemodel.md
Dieser SQL-Code definiert ein Datenbankmodell für Systematische Literaturrecherchen. Es umfasst Tabelle "projekte" und "phasen", sowie verschiedene Tabellen für die einzelnen Phasen der Recherche (P1-P8). Es werden auch ENUM-Typen und Funktionen zur Volltextsuche verwendet.

---

### composer.json
```
{
  "file_summary": "Composer-Datei für das Laravel-Projekt 'nileneb/app-linn-games'. 
Funktionalität: Projekt-Einstellungen, Abhängigkeiten, Autoload und Skripte für Entwicklung, Testen und Deployment."
}
```

---

### CONTRIBUTING.md
Beschreibt die Vorgehensweise zur Mitarbeit an einer Softwareentwicklung. Enthält Branch-Konventionen, Merge-Fluss, Arbeitsablauf pro Issue und weitere Best Practices.

---

### deploy.sh
Dieses Skript bereitet eine Produktionsumgebung für die Anwendung 'app.linn.games' vor und deployt sie. Es führt Pre-Flight Checks durch, baut die Docker-Images, startet die Infrastruktur, erstellt einen Backup der Datenbank, läuft migrations durch und optimiert den Cache.

---

### package.json
Paket-Datei zur Verwaltung Abhängigkeiten und Skripte für die Anwendung.
- Verwendet Vite als Build-Tool und Entwicklungsumgebung.
- Wird verwendet von 'build' und 'dev'-Skripten.
- Importiert u.a. Tailwind CSS, Autoprefixer und Axios.

---

### phpunit.xml
Konfiguration für die Laufzeit von PHPUnit-Tests mit Definition der zu testenden Klassen und Umgebungsvariablen

---

### vite.config.js
Vite-Konfigurationsdatei für Laravel-Projekt. Konfiguriert Vite als Build-Tool und integriert Laravel-Vite-Plugin, TailwindCSS und ermöglicht CORS auf lokalem Server.

---

### .dockerignore
Dieses Dockerignore-File definiert, welche Dateien und Verzeichnisse während einer Docker-Build-Prozess ignoriert werden sollen.

Es enthalten sind:
- Entwicklungsverzeichnisse wie .git und node_modules
- Framework-Cache, -Sessions und -Logs des Laravel-Frameworks
- Build-Verzeichnisse für Webseiten-Content
- Testumgebungen wie tests/ und PHPUnit-Cache
- Dateitypen wie *.log und Datenbankdateien
- Dokumentationen wie Readme.md und .editorconfig

---

### .editorconfig
Konfiguration für Editor-Einstellungen, wie Zeichensatz, Zeilenende und Formatierung.

---

### app/Actions/Fortify/CreateNewUser.php
```json
{
  "file_summary": "Diese Datei implementiert die Schutzfunktionen von Laravel Fortify, um einen neuen Benutzer zu erstellen. Sie validiert die Eingabedaten und speichert den Benutzer in der Datenbank.",
  "key_classes_functions": ["CreateNewUser", "Validator", "User"],
  "external_dependencies": ["Laravel\Fortify\Contracts\CreatesNewUsers", "Illuminate\Support\Facades\Validator"]
}
```

---

### app/Actions/Fortify/PasswordValidationRules.php
```
{
  "file_summary": "Die Datei definiert eine Trait für die Validierung von Passwörtern, welches von der Fortify-Bibliothek verwendet wird. Sie setzt Standard-Validierungsregeln für Passwörter fest und erfordert ein bestätigtes Passwort.",
  "key_classes_functions": ["PasswordValidationRules", "passwordRules"],
  "external_dependencies": ["Illuminate\Validation\Rules\Password"]
}
```

---

### app/Actions/Fortify/ResetUserPassword.php
Diese Datei implementiert die Funktionen für das Zurücksetzen eines Benutzerpasses. Sie überprüft die neuen Passwörter auf Gültigkeit und aktualisiert dann den Benutzeraccount.

---

### app/Filament/Resources/ContactResource.php
Definiert die Ressourcen für Kontaktdaten. Dazu gehören Formulare und Tabellen, um Kontaktanfragen zu verwalten.

---

### app/Filament/Resources/UserResource.php
```
{
  "file_summary": "Diese Datei enthält die Filament-Ressourcen für den Nutzer, einschließlich Formularfelder und Tabellen. Die Ressource ist UserResource und verwendet das Modell App\Models\User.",
  "important_classes": ["UserResource", "Schema", "Table"],
  "key_functions": ["form", "table", "getPages"],
  "external_dependencies": ["App\Models\User", "Filament\Forms", "Filament\Schemas\Schema", "Filament\Resources\Resource", "Filament\Tables"]
}
```

---

### app/Http/Middleware/TrackPageView.php
```
{
  "file_summary": "Dieser Middleware-Handler verfolgt die Seite-Aufrufe und speichert die Informationen in der Datenbank. 
    - Es wird überprüft, ob es sich um einen Bot handelt oder nicht.
    - Wenn nicht ein Bot, dann wird eine neue Seiteaufruf-Eintrag erstellt oder aktualisiert.
    - Der PageView-Modell ist für den Datenbank-Zugriff verantwortlich.
    - Die anonymisierte IP-Adresse wird verwendet."
}
```

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware zur Überprüfung von Langdock-Signaturen. Wird auf Webhooks angewendet um Authentizität und Replay-Schutz sicherzustellen.

---

### app/Http/Middleware/VerifyMcpToken.php
Verifiziert den MCP-Authorisierungstoken in jeder Anfrage. Die wichtigsten Funktionen sind: handle(Request, Closure), die das Eingehende Request-Objekt und das Weiterleiten der Anfrage prüft. Externe Abhängigkeiten: config('services.mcp.auth_token') zur Token-Einstellung und Symfony's HTTP Foundation zur Weiterleitung.

---

### app/Jobs/IngestPaperJob.php
```
{
  "file_summary": "Das Skript IngestPaperJob übernimmt die Verarbeitung von Papieren, indem es das Text-Chunking durchführt und Ollama-Embeddings generiert. Die Ergebnisse werden dann in der Datenbank gespeichert. Es handelt sich um eine Queue-Funktion, die mit der Implementierung von Dispatchable, InteractsWithQueue, Queueable und SerializesModels verwendet wird.",
  "wichtige_Klassen_Funktionen": [
    "IngestPaperJob",
    "chunkText()",
    "handle()"
  ],
  "externe_Abhängigkeiten": [
    "Illuminate\Http\Client\Support\Facades\Http",
    "Illuminate\Support\Facades\DB",
    "Illuminate\Support\Facades\Log"
  ]
}
```

---

### app/Jobs/TriggerLangdockAgent.php
```json
{
  "file_summary": "Datenübertragung an Langdock-Agent, um Projektstart zu erkennen. Die Funktion übermittelt die Nutzer-ID, das Projekt-Id und den Eingabewert per HTTP-POST-Anfrage. Die Antwort wird überprüft und eine Fehlermeldung wird auf dem Server protokolliert, wenn der API-Aufruf fehlschlägt.",
  "important_classes": ["TriggerLangdockAgent"],
  "external_dependencies": ["Illuminate\Support\Facades\Http", "Illuminate\Support\Facades\Log"]
}
```

---

### app/Livewire/Actions/Logout.php
Diese Datei ist für das Abmelden eines Benutzers aus der Anwendung verantwortlich. Sie nutzt die Authentifizierung und Sitzungs-Handling von Laravel, um sicherzustellen, dass der Benutzer ordnungsgemäß abgemeldet wird.

---

### app/Mail/ContactConfirmationMail.php
```
{
  "file_summary": "Diese Datei definiert eine Benachrichtigungs-Mail für Kundenanfragen bei Linn Games. Sie enthält die wichtigsten Klassen und Funktionen zum Versenden einer E-Mail an den Kunden.",
  "important_classes": ["ContactConfirmationMail", "Envelope", "Content"],
  "important_functions": ["__construct", "envelope", "content"],
  "externale_abhaengigkeiten": ["App\Models\Contact", "Illuminate\Mail\Mailables\Enveloppe", "Illuminate\Mail\Mailables\Content"]
}
```

---

### app/Mail/ContactInquiryMail.php
```
{
  "file_summary": "Erfasst Daten von Contact-Objekten und sendet Kontaktanfragen per E-Mail.",
  "main_classes": ["ContactInquiryMail"],
  "important_functions": ["envelope", "content"],
  "external_dependencies": ["App\Models\Contact", "Illuminate\Mail\Mailable"]
}
```

---

### app/Mail/DeployNotificationMail.php
```
{
  "file_summary": "Sendet eine E-Mail über erfolgreiche Deployments. \
Die Datei importiert die Illuminate-Mailable-Klasse und verfügt über zwei öffentliche Konstruktoren. \
Die wichtigsten Klassen sind DeployNotificationMail und Mailable von Laravel. \
Externe Abhängigkeiten: Laravel-Einstellungen"
}
```

---

### app/Policies/ProjektPolicy.php
```
{
  "file_summary": "Diese Datei definiert die Zugriffsrechte für Projekte im System. Es handelt sich um eine Policy-Klasse, die die Vererbung von Zugriffsrechten für Benutzer auf Projekte regelt.\n\nDie wichtigsten Klassen oder Funktionen in dieser Datei sind:\n- ProjektPolicy: Die Hauptklasse, die die Zugriffsrechte für Projekte definiert.\n- User und Projekt: Dazu gehören die Model-Klassen, die die Benutzer- und Projekt-Daten abbilden.\n\nExterne Abhängigkeiten sind:\n- App\Models\Recherche\Projekt (Projekt-Modell)\n- App\Models\User (Benutzer-Modell)"
}
```

---

### app/Providers/AppServiceProvider.php
Anbieter von Diensten für das Laravel-Anwendungsprojekt. Registriert Politiken, limitiert Rate und stellt sicher, dass im Produktionsmodus immer HTTPS verwendet wird.

---

### app/Providers/FortifyServiceProvider.php
```
{
  "file_summary": "Diese Datei stellt den Fortify-Service bereit und konfiguriert die Sicherheitsfunktionen in der Anwendung. Sie definiert Aktionen für Benutzer-Erstellung, Passwort-Restart und Zwei-Faktor-Authentifizierung sowie konfiguriert Rate Limiting für Login-Versuche.",
  "classes": ["FortifyServiceProvider"],
  "functions": ["configureActions", "configureViews", "configureRateLimiting"],
  "endpoints": [],
  "externals": [
    {"name": "Laravel\Fortify\Fortify"},
    {"name": "Illuminate\Cache\RateLimiting\Limit"},
    {"name": "Illuminate\Http\Request"},
    {"name": "Illuminate\Support\Facades\RateLimiter"}
  ]
}
```

---

### app/Providers/VoltServiceProvider.php
Dienstanbieter für Livewire, konfiguriert mit der Ansichts-Verbindung und den zu ladenden Seiten

---

### app/Providers/Filament/AdminPanelProvider.php
```
{
  "file_summary": "Diese Datei implementiert einen Provider für das Admin-Panel von Filament.
Klasse: AdminPanelProvider
Funktion: panel()
Wichtige Klassen: Panel, PanelProvider
Externe Abhängigkeiten:
  - Filament (Bibliothek)
  - Illuminate (Framework)
"
}
```

---

### bootstrap/app.php
```json
{
  "file_summary": "Bootstrapping der Laravel-Application. Konfiguriert Routing, Middleware und Ausnahmen für Web-, API- und Konsolen-Anfragen.",
  "main_classes": ["Illuminate\Foundation\Application"],
  "external_dependencies": [
    {
      "type": "framework",
      "name": "Laravel"
    }
  ]
}
```

---

### bootstrap/providers.php
Registriert Service-Provider für Anwendung.

---

### docker/common/nginx/default.conf
Nginx-Konfiguration für die Webanwendung, einschließlich Proxy-Einstellungen, Loggung und Sicherheit. Die Konfiguration ermöglicht den Zugriff auf verschiedene APIs und Endpunkte sowie die Durchführung von PHP-Code im Hintergrund.

---

### docker/common/nginx/docker-entrypoint.sh
Dieses Skript generiert eine Nginx-Konfiguration und ersetzte einen Auth-Token-Platzhalter. Es wird als Docker-Eintrittspunkt verwendet und ermöglicht es, eine Konfigurationsdatei aus einer Vorlage zu erstellen.

---

### docker/common/nginx/dockerfile
```json
{
  "file_summary": "Docker-Datei zum erstellen eines Nginx-Dockercontainers. 
    - Von der offiziellen nginx:alpine-Basisbild kopiert.
    - Custom-Nginx-Konfiguration und site-Konfiguration eingefügt.
    - Entrypoint-Skript für die Injectierung von Geheimnissen.
    - Statik-Dateien in das Docker-Container kopiert.
    - Nginx-Port 80 offen."
}
```

---

### docker/common/nginx/nginx.conf
Diese Datei ist eine Nginx-Konfigurationsdatei für Laravel-Projekte und enthält Grundkonfigurationen für Worker-Verbindungen, gzip-Kompression, Dateiaufnahmelimits und mehr.

---

### docker/common/php-fpm/dockerfile
Dies ist ein Dockerfile, das die Bildung eines PHP-FPM-Containers für eine Laravel-Anwendung beschreibt. Es gibt drei Stufen (Build, Produktionsumgebung und Testumgebung). Die wichtigsten Klassen sind PHP-Extensions wie PDO_MYSQL, Redis und Vite. Externe Abhängigkeiten werden über Composer und npm installiert.

---

### docker/common/php-fpm/entrypoint.sh
Docker-Eintragskript für PHP-FPM, das Build-Assets synchronisiert, die Datenbank wartet und Migrations durchführt, sowie Cache und Routen konfiguriert.

---

### docker/common/postgres/dockerfile
Baut ein Docker-Image für PostgreSQL basierend auf Version 16 auf. Installiert die Extensions HypoPG und PGVector.

---

### docker/common/postgres/init-extensions.sql
```json
{
  "file_summary": "Die Datei init-extensions.sql erstellt notwendige PostgreSQL-Erweiterungen.\n\
Es handelt sich um die Erweiterungen 'hypopg' für bessere JOIN-Fähigkeiten,\n\
'pg_stat_statements' für statistische Auswertung von SQL-Anfragen und\n\
'vector' für geodätische Berechnungen. Diese Erweiterungen werden erstellt, wenn sie nicht bereits vorhanden sind.\n\
Die Datei gehört zum PostgreSQL-Docker-Container."
}
```

---

### public/index.php
```
{
  "file_summary": "Initialisiert die Laravel-Application und handelt die Anfragen ab.
- Definiert den Ausführungsbeginn der Anwendung.
- Registriert die Composer-Autoloader-Datei.
- Bootstrapt die Anwendung mithilfe des App-Konfigurations-Files.
- Handhabt HTTP-Anfragen mithilfe von Request-Objekten."
}
```

---

### public/robots.txt
```json
{
  "file_summary": "Konfiguriert die Zugriffsmöglichkeiten von Crawlern und Bots. 
Hauptklasse: RobotsTxt,
Keine externe Abhängigkeiten.",
  "schlüsselklassen/funktionen/endpunkte": ["User-agent", "Disallow"],
  "externe_abhaengigkeiten": []
}
```

---

### public/.htaccess
Diese Datei definiert Apache-Spezifische Regeln für die Umleitung von Anfragen zu einem Front Controller. Sie handelt Autorisierungs- und CSRF-Tokens, entfernt unnötige Index-Seiten und leitet nicht-für-Folder-Anfragen um.

---

### resources/css/app.css
Diese Datei definiert Stile und Variablen für die CSS-Anwendung. Sie importiert Tailwindcss und setzt einige Standards für Formen, Farben und Schriftarten fest. Die Datei enthält auch Code für verschiedene Farbschemata, darunter das Dark-Schema.

---

### .github/copilot-instructions.md
```
{
  "file_summary": "Diese Datei beschreibt die Funktionen und Architektur des Projekts app.linn.games.
    - Kommunikation auf Deutsch, Code und Commits auf Englisch.
    - Laravel 12 als Backend-Framework mit PHP 8.2+.
    - Livewire 3 und Tailwind CSS 4 für Frontend.
    - Filament 4.9 für Administration.
    - PostgreSQL 16 als Produktions-Datenbank mit UUID, pgvector und Custom Enums.
    - Redis für Caching und Sitzungsverwaltung.
    - Langdock Webhook und Ollama für AI-Funktionalitäten.
    - Authentifizierung über Laravel Fortify und 2FA.
    - Pest als Testframework und SQLite in-Memory-Testdatenbank.
    - Docker-Komposition mit 10 Services und lokalen Tests über `docker compose run`.
    - Git-Konventionen wie Branching, Commits und Dokumentation."
}
```

---

### .github/instructions/app_linn_games_custom.instructions.md
Dokumentation der Architektur und Konventionen von app.linn.games. Diese Datei ist für alle Entwickler und KI-Agenten verbindlich und beschreibt die Projektstruktur, Datenmodell, Authentifizierung und Autorisierung sowie die KI-Integration.

---

### .github/instructions/docker-dev-workflow.instructions.md
Das Dokument beschreibt den Docker Dev-Workflow für das Projekt app.linn.games. Es erläutert die Architektur mit 10 Containern, die Code-Deployment in Production und Development sowie die Möglichkeiten zur sofortigen Aktualisierung von Code-Änderungen. Außerdem werden die Bind-Mounts, Volumes und Sicherheitsregeln für docker-compose.yml beschrieben.

---
