---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-04T21:23:55.285671
model: mistral:7b-instruct
mode: turbulence
files_total: 313
run_time_s: 1348.7
---

# Turbulenz-Analyse — 2026-04-04 21:23

## Zusammenfassung

| Metrik | Wert |
|--------|------|
| Dateien gesamt | 313 |
| 🔴 Kritisch (>50%) | 10 |
| 🟡 Mittel (20-50%) | 79 |
| ⬛ Stabil (<20%) | 224 |
| Findings | 4 |
| Redundanzen | 3989 |

## 🔴 Kritische Dateien

### `tests/Feature/Recherche/AgentActionButtonTest.php` — 60% Turbulenz

- Hot-Zone Zeile 16–57 (Peak: 67%)
- Hot-Zone Zeile 87–113 (Peak: 67%)
- Hot-Zone Zeile 129–158 (Peak: 67%)

**[MEDIUM]** Es gibt eine Vermischung von Testlogik und Anwendungslogik. Die Konfiguration der Langdock-API-Schlüssel sollte in einer separaten Funktion oder Klasse abgehandelt werden.
> Empfehlung: Die Konfiguration der Langdock-API-Schlüssel sollte in eine separate Funktion oder Klasse verlagert werden, die vor dem Test aufgerufen wird. Dies würde die Testlogik und Anwendungslogik trennen.

**[MEDIUM]** Es ist vermischt, da die Simulation einer fehlgeschlagenen Aufgabe innerhalb des Tests stattfindet.
> Empfehlung: Die Simulation der fehlgeschlagenen Aufgabe könnte in eine separate Funktion oder Methode ausgelagert werden, um das Test-Code zu isolieren und die Lesbarkeit zu verbessern.

**[HIGH]** Es gibt eine Vermischung von Testlogik und Anwendungslogik, da die Erstellung eines Projekts und der Zugriff auf die Laravel-Authentifizierung innerhalb des Tests stattfindet.
> Empfehlung: Die Erstellung des Projekts und der Zugriff auf die Authentifizierung sollten in eine Setup-Funktion für den Test extrahiert werden, um die Testlogik von Anwendungslogik zu trennen.

### `app/Services/UnpaywallService.php` — 53% Turbulenz

- Hot-Zone Zeile 27–55 (Peak: 67%)

**[MEDIUM]** Die Funktionalität zur Abfrage der Unpaywall API ist vermischt mit der Logik für die Verarbeitung der Antwort.
> Empfehlung: Es könnte sinnvoll sein, die Logik für die Verarbeitung der Antwort in eine separate Methode zu überführen und diese von der Funktion zur Abfrage der API aufzurufen.

### `app/Actions/Fortify/ResetUserPassword.php` — 50% Turbulenz


### `app/Http/Controllers/DsgvoController.php` — 50% Turbulenz


### `app/Mail/ContactInquiryMail.php` — 50% Turbulenz


### `app/Providers/Filament/AdminPanelProvider.php` — 50% Turbulenz


### `database/factories/WorkspaceUserFactory.php` — 50% Turbulenz


### `database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php` — 50% Turbulenz


### `resources/views/dashboard.blade.php` — 50% Turbulenz


### `tests/Feature/Auth/TwoFactorChallengeTest.php` — 50% Turbulenz


## 🔄 Mögliche Redundanzen

- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
- `nicht_erkannt` ≈ `nicht_erkannt` (100% Ähnlichkeit)
