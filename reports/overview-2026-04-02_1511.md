---
repo: https://github.com/Nileneb/app.linn.games
date: 2026-04-02T15:11:39.638200
model: llama3.1:8b
run_id: llama3.1:8b
mode: overview
files_total: 250
errors: 0
run_time_s: 676.2
---

# Funktions-Übersicht — 2026-04-02_1511

- **Repo:** https://github.com/Nileneb/app.linn.games
- **Dateien:** 250
- **Fehler:** 0
- **Laufzeit:** 676.2s

## api

### app/Http/Controllers/ContactController.php
Controller für Kontaktformular-Abfrage. Überprüft die Eingaben und speichert sie in der Datenbank. Versendet eine Bestätigungs-E-Mail an den Nutzer und eine Information-E-Mail an info@linn.games.

---

### app/Http/Controllers/Controller.php
Abstracte Basisklasse für alle Controllers. Enthält keine spezifischen Funktionen, sondern dient als Schnittstelle für Controller in der API.

---

### app/Http/Controllers/DsgvoController.php
Dsgvo-Controller für die Verarbeitung von DSGVO-Anfragen, darunter das Exportieren von Benutzerdaten und das Löschen des Benutzerkontos.

---

### app/Http/Controllers/LangdockWebhookController.php
Diese Datei verarbeitet Webhooks von Langdock. Es werden die gegebenen Daten validiert und dann ein Job gestartet, um den Langdock-Agent auszuführen. Die wichtigsten Klassen sind LangdockWebhookController und TriggerLangdockAgent.

---

### app/Http/Controllers/PaperRagController.php
```
{
  "file_summary": "Die PaperRagController handle die API-Endpunkte für den Eingang von Papieren und die Suche nach Papieren.
 
 * ingest: Ein Paper wird anhand der übergebenen Daten in das System aufgenommen.
 * search: Nachdem ein Paper oder eine Liste von Papieren aufgrund eines Suchbegriffs abgefragt wurde, werden ähnliche Ergebnisse zurückgegeben.
 
 Hauptklassen/Funktionen/Endpunkte:
 * IngestPaperJob: Ein Job zur Aufnahme eines Papers in das System.
 * search: API-Endpunkt für die Suche nach Papieren.
 
 Externe Abhängigkeiten:
 * Illuminate\Http\Request
 * Illuminate\Support\Facades\DB
 * Illuminate\Support\Facades\Http
 * App\Jobs\IngestPaperJob"
}
```

---

### routes/api.php
Diese Datei definiert API-Routen für die Webanwendung. Es handelt sich um eine Gruppe von Routen zum Empfangen von Langdock-Webhooks und zum Ingestion von Papern via MCP-Token. Externe Abhängigkeiten sind Langdock und MCP (MCP-Token). Die wichtigsten Klassen sind LangdockWebhookController und PaperRagController.

---

### routes/console.php
Definiert zwei Artisan-Commands: inspire und deploy:notify. Der erste Command gibt eine inspirierende Zitat aus, während der zweite einen Deploy-Benachrichtigungs-E-Mail an einen bestimmten Empfänger sendet.

---

### routes/web.php
```json
{
  "file_summary": "Web-Routen definieren für API und Anwendungs-UI. 
    Routen für Kontaktformular, Datenschutz, AGB, Dashboard und Einstellungen.
    Mittels Livewire werden Seiten für Einstellungen und Profil generiert.
    Authentifizierung und Autorisierungen sind erforderlich für Zugriff auf bestimmte Routen.",
  "important_classes": ["ContactController", "DsgvoController"],
  "important_functions": ["/contact (POST)", "/dsgvo/export (GET)"],
  "extern_imports": ["Laravel\\Fortify\\Features", "Livewire\\Volt\\Volt"]
}
```

---

## config

### docker-compose.dev.yml
Dev-Override für docker-compose, ändert Port von 6481 zu 6480

---

### docker-compose.yml
```
{
  "file_summary": "Docker-Compose-Konfiguration für komplexe Anwendung mit mehreren Services und Netzwerken.
Knoten: web, php-fpm, php-cli, php-test, queue-worker, postgres, redis, postgres-mcp, ollama, mcp-paper-search
Netzwerke: linn-network
Abhängigkeiten: Postgres, Redis, CrystalDBA/Postgres-MCP-Image"
}
```

---

### .env.example
Diese Datei enthält Konfigurationseinstellungen für eine Laravel-Application. Die wichtigsten Einstellungen betreffen die Datenbankverbindung, den Session-Manager und die Authentifizierung. Externe Abhängigkeiten sind zum Beispiel Postgres, Redis und Mailgun. Die wichtigsten Klassen oder Funktionen sind keine explizit definierten, aber die Datei enthält Einstellungen für verschiedene Komponenten der Anwendung wie das Logging, den Cache und die Queue.

---

### config/app.php
Diese Datei enthält die Anwendungskonfiguration für ein Laravel-Projekt. Sie beinhaltet Einstellungen wie Anwendungsnamen, Umgebung, Debug-Modus, URL, Zeitzone und Lokalisierung. Externe Abhängigkeiten sind .env-Dateien, die die Werte dieser Konfigurations-Einträge festlegen.

---

### config/auth.php
```
{
  "file_summary": "Die Datei config/auth.php enthält die Einstellungen für die Benutzerauthentifizierung. Sie definiert die Authentifizierungs-Wächter, den Passwort-Reset-Broker und die Anwendungsmodelle. Externe Abhängigkeiten sind z.B. das 'users'-Modell von App\Models und der Umgebungsvariable AUTH_GUARD.",
  "key_klassen_funktionen_endpunkte": ["defaults", "guards", "providers", "passwords"],
  "externe_abhaengigkeiten": ["env()", App\Models\User::class]
}
```

---

### config/cache.php
Diese Datei konfiguriert die Cache-Einstellungen für eine Laravel-Anwendung. Sie enthält Konfigurationen für verschiedene Cache-Stores, wie Array, Database, File, Memcached, Redis und DynamoDB. Die wichtigsten Einstellungen sind 'default', 'stores' und 'prefix'.

---

### config/cookie-consent.php
Konfiguration für das Cookie-Consent-Dialog-Modul: Aktivierung, Cookie-Name und -Dauer

---

### config/database.php
```
{
  "file_summary": "Konfiguration für die Datenbankverbindung, einschließlich der Verbindungseinstellungen für verschiedene Datenbanktypen (z.B. MySQL, PostgreSQL) und Einstellungen für die Migrationstabelle.",
  "classes_functions_endpunkte": [
    "default",
    "connections",
    "migrations",
    "redis"
  ],
  "externe_abhaengigkeiten": [
    "Illuminate\Support\Str",
    "env() (um Umgebungsvariablen zu lesen)",
    "DB_URL, DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD etc. (Umgebungsvariablen)"
  ]
}
```

---

### config/filesystems.php
Konfiguration von Dateisystemen für das Framework, einschließlich Standard-Datenspeicher und verschiedenen unterstützten Speicherdiensten. Wichtigste Klassen: 'local', 'public', 's3'. Externe Abhängigkeiten: Umgebungsvariablen (env), AWS-Access-Keys.

---

### config/fortify.php
```
{
  "file_summary": "Konfiguration für die Anwendungsbibliothek Fortify.\n\
Diese Datei enthält Einstellungen zum Verwenden von Fortify in der Anwendung, einschließlich des verwendeten Guards, Passwort-Verwalter und Authentifizierungs-Funktionen.\n\
Die wichtigsten Klassen und Funktionen sind die Features und das Password Broker. Externe Abhängigkeiten bestehen zur Laravel-Anwendungsbibliothek und den auth-Configuration Dateien."
}
```

---

### config/logging.php
```
{
  "file_summary": "Konfiguration von Logging-Channel für die Laravel-Anwendung. Definiert die Standard-Log-Kanal und Log-Kanäle, einschließlich E-Mail, Slack, Paperrtrail und Syslog.",
  "key_classes_functions_endpoints": {
    "Key-Klassen/Funktionen/Endpunkte": [
      "NullHandler",
      "StreamHandler",
      "SyslogUdpHandler",
      "PsrLogMessageProcessor"
    ],
    "Externe Abhängigkeiten": [
      "Monolog\Handler\NullHandler",
      "Monolog\Handler\StreamHandler",
      "Monolog\Handler\SyslogUdpHandler",
      "Monolog\Processor\PsrLogMessageProcessor"
    ]
  }
}
```

---

### config/mail.php
Diese Datei enthält Konfigurationen für die E-Mail-Funktion in einem Laravel-Projekt. Sie enthält die Einstellungen für verschiedene Mailer, darunter SMTP, SES, Postmark und Log-Mailer. Die wichtigsten Klassen sind keine explizit definierten, sondern eher die benutzten Einstellungen wie z.B. 'default', 'mailers' und 'from'. Externe Abhängigkeiten sind die Umgebungsvariablen (env()) und möglicherweise externe APIs für den Mailer.

---

### config/queue.php
Konfiguration für die Arbeit mit der Warteschlange. Enthält Informationen zur Verbindung, Batcharbeit und fehlgeschlagenen Aufträgen.

---

### config/services.php
```
{
  "file_summary": "Datei beinhaltet Konfigurationen für Drittanbieter-Dienste wie E-Mail-Transport, Slack und AWS. Enthält Umgebungsvariable-Beziehungen zur Verwaltung von API-Schlüsseln und Zugriffstasten.",
  "key_classes_functions_endpoints": ["postmark", "resend", "ses", "slack", "langdock", "mcp", "ollama"],
  "external_dependencies": ["env()", AWS, Mailgun, Postmark, Slack]
}
```

---

### config/session.php
```
{
  "file_summary": "Diese Datei definiert die Konfiguration für die Sitzung (Session) in einer Laravel-Anwendung. Sie legt fest, wie die Sitzungen gespeichert und abgerufen werden, einschließlich der verwendeten Speicherart, Lebensdauer, Verschlüsselung und Cookie-Eigenschaften.",
  "key_classes_functions": ["Illuminate\Support\Str"],
  "external_dependencies": ["database", "cache stores (optional)"]
}
```

---

### docker/common/php-fpm/php-dev.ini
Konfiguriert PHP-FPM für Entwicklungsumgebungen: OPcache-Überprüfung, Fehlermeldungen und Ausgaben aktiviert.

---

### docs/api.yaml
```
{
  "file_summary": "Die Datei beschreibt die OpenAPI-Dokumentation für das API von app.linn.games, 
                  einschließlich Endpunkte für Authentifizierung, Webhooks, Paper-Ingescion und Vektorsuche.
                   Die wichtigsten Klassen sind User, PaperChunk und die Sicherheitsmechanismen Sanctum Bearer-Token, MCP Bearer-Token und HMAC-SHA256 Signatur.
                   Externe Abhängigkeiten sind Ollama (nomic-embed-text) für Text-Vektorisierung und pgvector für Vektorspeicherung."
}
```

---

### .github/ISSUE_TEMPLATE/bug_report.yml
Diese Datei definiert ein Issue-Template für Bug Reports. Sie enthält Formularelemente zur Erfassung von Fehlerbeschreibungen, Schritten zur Reproduktion und zur Priorisierung des Fehlers. Externe Abhängigkeiten sind nicht vorhanden.

---

### .github/ISSUE_TEMPLATE/feature_request.yml
Diese Datei ist ein ISSUE_TEMPLATE für neue Features. Sie besteht aus mehreren Teilen, um eine detaillierte Beschreibung des Vorschlags zu ermöglichen.
Wichtige Elemente sind:
- Eine Zusammenfassung des Vorgeschlagenen Features
- Die Motivation hinter dem Vorschlag und die damit gelösten Probleme
- Die vorgeschlagene Lösung
- Betroffene Bereiche im Projekt (Dateien, Routes, Services oder Models)
- Angabe einer möglichen Breaking Change und der notwendigen Migrationen oder API-Änderungen
- Akzeptanzkriterien für das fertige Feature

---

### .github/ISSUE_TEMPLATE/refactor.yml
```
{
  "file_summary": "Definiert ein ISSUE_TEMPLATE für Refactoring-Anfragen. 
  Einzelne Felder sind für die Beschreibung, den Aufgabenbereich und die Akzeptanzkriterien vorgesehen.
  Externe Abhängigkeiten: GitHub API (für Issue-Templates)"
}
```

---

### .github/workflows/lint.yml
Definiert ein Workflow für Code-Qualität und -Stil-Überprüfungen nach Push-Vorgängen oder Pull-Request-Anfragen auf den Branches develop, main, feature/* und fix/*. Das Setup verwendet PHP 8.4, installiert Abhängigkeiten mit Composer und npm und führt Pint aus, um Code-Stil-Probleme zu korrigieren.

---

### .github/workflows/tests.yml
Automatisierung der Test-Workflows für die Anwendung. Es werden verschiedene Schritte durchgeführt, um sicherzustellen, dass die Anwendung korrekt funktioniert. Die wichtigsten Klassen, Funktionen oder Endpunkte sind: - actions/checkout@v4 - shivammathur/setup-php@v2 - actions/setup-node@v4 - composer install - npm run build - ./vendor/bin/pest Externe Abhängigkeiten: - Composer - Node.js

---

## data_access

### app/Models/ChatMessage.php
Diese Datei stellt den ChatMessage-Model im Kontext von Laravel Eloquent dar. Sie ermöglicht das Arbeiten mit Daten aus der Tabelle "chat_messages" und bietet Beziehungen zu User und Webhook Modellen.

---

### app/Models/Consent.php
Die Consent-Datei stellt das Datenmodell für Einwilligungen dar. Sie definiert die Tabelle, in der Einwilligungen gespeichert werden und enthält Beziehungen zur Benutzer-Tabelle.

---

### app/Models/Contact.php
```json
{
  "file_summary": "Definiert die Contact-Model-Klasse für Datenbankzuführungen, 
                    enthält Felder für Kontaktinformationen und Projekttyp."
}
```

---

### app/Models/PageView.php
Dieser Datei-Modell definiert die Datenstruktur für eine PageView-Einheit. Die wichtigsten Klassen sind PageView, der mit Eloquent abgeleitet wird und über den folgenden Abschnitt $fillable verfügt: 

* path (String): Pfad zur Seite
* ip_anonymous (Boolean): Anonyme IP-Adresse
* user_agent (String): Benutzeragentur des Besuchers
* visits (Integer): Anzahl der Seitenbesuche

---

### app/Models/User.php
```
{
  "file_summary": "Diese Datei definiert die User-Model-Funktionen. Sie erweitert das Authentifizierungsmodell und ermöglicht E-Mail-Vereifizierung, Zwei-Faktor-Authentifizierung und Rollenverwaltung. Die Datei enthält auch Funktionen für Log-Aktivitäten und Statusüberprüfungen.",
  "kennzeichnende Klassen/Funktionen": ["User", "HasFactory", "LogsActivity", "Notifiable", "TwoFactorAuthenticatable"],
  "externe Abhängigkeiten": ["Illuminate\Database\Eloquent\Factories\HasFactory", "Spatie\Activitylog\Models\Concerns\LogsActivity", "Spatie\Permission\Traits\HasRoles"]
}
```

---

### app/Models/Webhook.php
```json
{
  "file_summary": "Webhook-Modell für die Datenbank, das Benutzer-Webhooks zur Kommunikation mit Frontend-Objekten verwalten kann.",
  "key_classes_functions": ["Webhook", "callUrl", "forUser", "user", "chatMessages"],
  "externale_abhaengigkeiten": ["Illuminate\Database\Eloquent\Model", "User", "ChatMessage"]
}
```

---

### app/Models/Recherche/P1Komponente.php
```
{
  "file_summary": "Datentabellenmodell für die Tabelle p1_komponenten. Es handelt sich um eine Eloquent-Model-Klasse, die den Projekt-Komponenten-Typ darstellt. Die Klasse ermöglicht das Abfragen und Bearbeiten von Komponentendaten in der Datenbank.",
  "important_classes": ["P1Komponente"],
  "important_functions": ["projekt"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P1Kriterium.php
```
{
  "file_summary": "Das Model P1Kriterium stellt eine Datenbanktabelle \"p1_kriterien\" dar. Es enthält Beziehungen zu anderen Modellen wie Projekt und bietet Funktionen für den Zugriff auf die Tabelle.",
  "key_classes": ["P1Kriterium"],
  "important_functions": ["projekt", "fillable"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P1Strukturmodellwahl.php
```
{
  "file_summary": "Definiert die Datenbank-Tabelle p1_strukturmodell_wahl und stellt eine Beziehung zu Projekt-Modellen her.",
  "key_classes": ["P1Strukturmodellwahl"],
  "important_methods": ["projekt", "projekt(): BelongsTo"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Relations\BelongsTo"]
}
```

---

### app/Models/Recherche/P1Warnsignal.php
```
{
  "file_summary": "Die Datei P1Warnsignal.php stellt eine Eloquent-Model-Klasse dar, die Zugriff auf das Datenbanktabell 'p1_warnsignale' ermöglicht. Sie definiert Beziehungen zu anderen Tabellen und kapselt Funktionalitäten für die Verarbeitung von Warnsignalen in Projekten.",
  "kernklassen": ["P1Warnsignal"],
  "wichtige_funktionen": ["projekt()"],
  "externer_abhaengigkeiten": ["Projekt::class", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P2Cluster.php
```
{
  "file_summary": "P2Cluster-Model zur Datenbankabfrage des P2-Clusters. 
                    Enthält Beziehungen zum Projekt und stellt Daten ab.
                    Importiert die Model-Klasse von Eloquent.",
  "key_classes": ["P2Cluster", "Projekt"],
  "important_methods": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P2MappingSuchstringKomponente.php
Modell für die Tabelle p2_mapping_suchstring_komponenten. Definiert ein Verhältnis zu Projekt (Projekt ID) und bietet Funktionen zum Lesen von Suchbegriffen, Sprache und anderen Attributen.

---

### app/Models/Recherche/P2ReviewTypEntscheidung.php
Datenzugriff-Modell für Entscheidungen zu Reviews eines Projekts. Definiert die Tabelle "p2_review_typ_entscheidung" mit Beziehungen zum Projekt.

---

### app/Models/Recherche/P2Trefferliste.php
```
{
  "file_summary": "Repräsentiert die P2-Trefferliste-Datenbanktabelle. Enthält Funktionen für das Arbeiten mit der Tabelle und Beziehungen zu anderen Tabellen.",
  "key_classes": ["P2Trefferliste"],
  "important_functions": ["projekt"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P3Datenbankmatrix.php
```json
{
  "file_summary": "Repräsentiert eine Datenbankmatrix mit Projektbeziehungen. Enthält Eloquent-Model für die Tabelle p3_datenbankmatrix mit Beziehung zu Projekt.",
  "main_class": "P3Datenbankmatrix",
  "important_methods": ["projekt"],
  "externals": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/P3Disziplin.php
```json
{
  "file_summary": "Model für die Tabelle p3_disziplinen, stellt eine Beziehung zu Projekt-Modell her.",
  " wichtige Klassen/Funktionen/Endpunkte": ["P3Disziplin", "projekt()"],
  "externe Abhängigkeiten": ["Illuminate\Database\Eloquent\Concerns\HasUuids", 
                            "Illuminate\Database\Eloquent\Model", 
                            "Projekt::class"]
}
```

---

### app/Models/Recherche/P3GeografischerFilter.php
```
{
  "file_summary": "Model für geografische Filtereinstellungen. Fügt einen Projektbezug hinzu und konfiguriert die Datenbankabfrage.",
  "main_class": "P3GeografischerFilter",
  "key_relationships": [
    "projekt" (Projekt-Beziehung)
  ],
  "external_dependencies": [
    "Illuminate\Database\Eloquent\Model", 
    "Illuminate\Database\Eloquent\Concerns\HasUuids"
  ]
}
```

---

### app/Models/Recherche/P3GraueLiteratur.php
```json
{
  "file_summary": "Eloquent-Modell für die Datenbank-Tabelle p3_graue_literatur, 
                    beinhaltet Beziehung zu Projekt-Modell und definiert gefüllte Felder."
}
```

---

### app/Models/Recherche/P4Anpassungsprotokoll.php
```
{
  "file_summary": "Die Datei \"P4Anpassungsprotokoll.php\" definiert ein Eloquent-Modell für die Tabelle \"p4_anpassungsprotokoll\". Es enthält Einstellungen für Datenbankbeziehungen und Datenformatierung.",
  "key_classes": ["P4Anpassungsprotokoll"],
  "important_functions": ["suchstring()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "P4Suchstring"]
}
```

---

### app/Models/Recherche/P4Suchstring.php
Diese Datei definiert die Datenbanktabelle 'p4_suchstrings' und bietet Zugriffsmöglichkeiten auf diese Tabelle. Die wichtigsten Funktionen sind: Projekt-Beziehung, Anpassungsprotokoll-Beziehung und Suchprotokoll-Beziehung. Externe Abhängigkeiten: Projekt-Klasse.

---

### app/Models/Recherche/P4ThesaurusMapping.php
```json
{
  "file_summary": "Das Modell P4ThesaurusMapping stellt die Mappings zwischen Projekten und Thesauri dar. Es enthält Beziehungen zu einem Projekt und bietet ein Set von Feldern für die Speicherung von Freitexten, Mesh-Termen, Emtree-Termen, PSycinfo-Termen und Anmerkungen.",
  "klasse": "P4ThesaurusMapping",
  "zugehoerige_klassen": ["Projekt"],
  "funktionen": ["projekt"],
  "externe_abhaengigkeiten": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P5PrismaZahlen.php
```json
{
  "file_summary": "Die Datei P5PrismaZahlen.php stellt eine Datenbank-Abstraktion für die Tabelle 'p5_prisma_zahlen' bereit. Sie ermöglicht das Verwalten von Zahlen-Daten in dieser Tabelle, einschließlich Projektbeziehungen und Datenformatierung.",
  "important_classes": ["P5PrismaZahlen"],
  "key_functions": ["projekt", "fillable", "casts"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P5ScreeningEntscheidung.php
```
{
  "file_summary": "Eloquent-Modell für die Tabelle p5_screening_entscheidungen. Enthält Beziehungen zu P5Treffer und spezifische Einstellungen für Datenkategorien.",
  "important_classes_and_functions": ["P5ScreeningEntscheidung", "treffer"],
  "externale_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P5ScreeningKriterium.php
Eloquent-Model für P5ScreeningKriterium-Tabelle. Definiert Beziehung zu Projekt-Modell und legt Tabellenfelder fest.

---

### app/Models/Recherche/P5ToolEntscheidung.php
```json
{
  "file_summary": "Modellklasse für Entscheidungen in P5-Tools, 
    beinhaltet Datenbankzugriff auf Tabelle p5_tool_entscheidung,
    enthält Beziehung zur Projekt-Tabelle und UUID-Funktionen."
}
```

---

### app/Models/Recherche/P5Treffer.php
```json
{
  "file_summary": "Modell für Datenbanktabelle p5_treffer mit Beziehungen zu anderen Tabellen. Die Tabelle enthält Informationen über Recherche-Treffer, wie Titel, Autoren, Jahr und DOI. Es gibt Beziehungen zu Projekt, Duplikat und Screening-Entscheidung.",
  "key_classes": ["P5Treffer"],
  "important_functions": ["projekt", "duplikatOriginal", "duplikate", "screeningEntscheidungen", "qualitaetsbewertung", "datenextraktion"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P6Luckenanalyse.php
```
{
  "file_summary": "Die Datei P6Luckenanalyse.php stellt ein Eloquent-Modell für die Datenbanktabelle p6_luckenanalyse dar. Es bietet Zugriff auf Projekt-Daten und ermöglicht die Beziehung zwischen Luckenanalysen und Projekten.",
  "key_classes": ["P6Luckenanalyse"],
  "key_functions": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P6Qualitaetsbewertung.php
```
{
  "file_summary": "Eloquent-Modell für die Tabelle \"p6_qualitaetsbewertung\", 
                  mit Beziehungen zu P5Treffer und Kasten zur Datenkategorisierung."
}
```

---

### app/Models/Recherche/P7Datenextraktion.php
Ein Datenzugriff-Modell für die Tabelle p7_datenextraktion. Es enthält ein BelongsTo-Verhältnis zu P5Treffer und ermöglicht den Zugriff auf Felder wie land, stichprobe_kontext und qualitaetsurteil.

---

### app/Models/Recherche/P7GradeEinschaetzung.php
```json
{
  "file_summary": "Die Datei P7GradeEinschaetzung.php stellt ein Model für die Datenbanktabelle p7_grade_einschaetzung dar. Sie ermöglicht das Hinzufügen, Bearbeiten und Lesen von Einträgen in dieser Tabelle. Das Modell enthält Beziehungen zu anderen Tabellen sowie Kastierungen für bestimmte Spalten.",
  "wichtige_Klassen": ["P7GradeEinschaetzung", "Projekt"],
  "wichtige_Methoden/Funktionen": ["projekt"],
  "extern": ["Illuminate\Database\Eloquent\Concerns\HasUuids", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P7MusterKonsistenz.php
```json
{
  "file_summary": "Eloquent-Model für die Tabelle p7_muster_konsistenz. Definiert Beziehungen und Datenkonvertierungen.",
  "key_classes": ["P7MusterKonsistenz"],
  "important_functions": ["projekt()"],
  "external_dependencies": ["Projekt", "Illuminate\Database\Eloquent\Model"]
}
```

---

### app/Models/Recherche/P7SyntheseMethode.php
Datenbank-Abfrage zum Projekt P7-Synthese-Methode: Definiert eine Eloquent-Model-Klasse für die Tabelle p7_synthese_methode und stellt den Bezug zu einer Projektinstanz her.

---

### app/Models/Recherche/P8Limitation.php
Diese Datei definiert die Datenbank-Entität P8Limitation. Sie stellt eine Relation zwischen Projekt und Limitierung her und kann neue Datensätze in der Tabelle p8_limitationen einfügen.

---

### app/Models/Recherche/P8Reproduzierbarkeitspruefung.php
Modell für Tabelle p8_reproduzierbarkeitspruefung: Projekt-Beziehungen und Spaltenkasten.

---

### app/Models/Recherche/P8Suchprotokoll.php
```json
{
  "file_summary": "Diese Datei definiert eine Datenbank-Tabelle 'p8_suchprotokoll' und stellt einen Eloquent-Model für sie bereit. Sie enthält Beziehungen zu einer anderen Tabelle und spezifiziert, welche Spalten gefüllt werden können.",
  "key_classes": ["P8Suchprotokoll"],
  "important_methods": ["suchstring"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Illuminate\Database\Eloquent\Concerns\HasUuids"]
}
```

---

### app/Models/Recherche/P8UpdatePlan.php
```json
{
  "file_summary": "Die Datei P8UpdatePlan.php definiert eine Datenbank-Tabelle und eine Eloquent-Model-Klasse zur Abwicklung von Aktualisierungsplänen für Projekte.",
  "key_classes": ["P8UpdatePlan"],
  "important_methods": ["projekt()"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Model", "Projekt"]
}
```

---

### app/Models/Recherche/PaperEmbedding.php
Papier-Embedding-Modell für die Datenbank. Enthält Beziehungen zu Projekt und ermöglicht den Zugriff auf Papier-Embeddings-Daten.

---

### app/Models/Recherche/Phase.php
Datenbank-Modell für die Phase einer Recherche. Enthält Beziehungen zur Projekt-Tabelle.

---

### app/Models/Recherche/Projekt.php
```
{
  "file_summary": "Diese Datei definiert die Datenbank-Relationen für Projekte. Sie enthält Eloquent-Mॉडel für das Projekt-Modell und Beziehungen zu anderen Meldern, wie User, Phasen, Kriterien usw.",
  "key_classes": ["Projekt", "User"],
  "key_functions": ["getActivitylogOptions", "user", "phasen", ...],
  "external_dependencies": ["Spatie/Activitylog"]
}
```

---

### database/factories/UserFactory.php
```json
{
  "file_summary": "Factory für Benutzer-Daten, die in der Datenbank erstellt werden können. Die Factory bietet Methoden zum Generieren von fiktiven Benutzerdaten und zum Setzen von Status (z.B. E-Mail-Verifizierung) oder Konfiguration zweistufiger Authentifizierung.",
  "key_classes": ["UserFactory"],
  "important_functions": ["definition", "unverified", "withoutTwoFactor"],
  "external_dependencies": ["Illuminate\Database\Eloquent\Factories\Factory", "Illuminate\Support\Facades\Hash", "Illuminate\Support\Str"]
}
```

---

### database/factories/Recherche/ProjektFactory.php
```
{
  "file_summary": "Factory-Klasse für die Erzeugung von Projekt-Objekten mit fiktiven Daten",
  "main_class": "ProjektFactory",
  "key_methods": ["definition"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "App\Models\User"]
}
```

---

### database/migrations/0001_01_01_000000_create_users_table.php
```
{
  "file_summary": "Migrationsdatei zur Erstellung von Tabelleben für Benutzer, Passwörter und Sitzungen. Führt eine DB-Migration durch, um die notwendigen Tabelleinhalte zu erstellen.",
  "key_classes_functions_endpoints": {
    "Migration": "",
    "Blueprint": "",
    "Schema": ""
  },
  "external_dependencies": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/0001_01_01_000001_create_cache_table.php
Diese Datei erstellt bzw. löscht zwei Tabellen im Datenbank-System: 'cache' für Cachewerte und 'cache_locks' für Lockmechanismus zur Cache-Daten-Auswahl.

---

### database/migrations/0001_01_01_000002_create_jobs_table.php
Migration für die Erstellung von Tabellen in der Datenbank zur Verwaltung von Aufträgen und fehlgeschlagenen Aufträgen. Die Migration erstellt drei Tabellen: 'jobs', 'job_batches' und 'failed_jobs'.

---

### database/migrations/2025_09_02_075243_add_two_factor_columns_to_users_table.php
```json
{
  "file_summary": "Migration zum Hinzufügen von Zwei-Faktor-Authentifizierung zu Benutzer-Tabelle\n- Fügt drei Spalten für Zwei-Faktor-Authentifizierung zu der Benutzer-Tabelle hinzu.\n- Verwaltet die Änderungen durch Einfügen und Entfernen von Spalten über Migrationen.\n- Involvierte Klassen: Migration, Blueprint, Schema\n- Externe Abhängigkeiten: Illuminate\Database\Migrations\Migration, Illuminate\Database\Schema\Blueprint, Illuminate\Support\Facades\Schema"
}
```

---

### database/migrations/2026_03_30_100000_create_contacts_table.php
Migration für die Datenbank-Tabell 'contacts' erstellen und löschen. Die Tabelle enthält Felder wie 'name', 'email', 'project_type' und 'message'.

---

### database/migrations/2026_03_30_100001_create_page_views_table.php
Migration zur Erstellung einer Datenbanktabelle für Seitenaufrufe. Die Tabelle 'page_views' wird mit den Spalten 'id', 'visits' und 'timestamps' erstellt.

---

### database/migrations/2026_03_31_000001_create_permission_tables.php
```
{
  "file_summary": "Migrationsdatei für die Erstellung von Tabellen zur Verwaltung von Berechtigungen und Rollen. Importiert Konfigurationen aus config/permission.php. Befüllt Datenbank mit Tabellen: permissions, roles, model_has_permissions, model_has_roles und role_has_permissions.",
  "important_classes": ["Migration", "Blueprint"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint", "Illuminate\Support\Facades\Schema", "config/permission.php"]
}
```

---

### database/migrations/2026_03_31_000002_add_details_to_page_views_table.php
Migration zum Hinzufügen von Spalten zu der page_views-Tabelle: path, ip_anonymous und user_agent. Die Migration kann rückgängig gemacht werden, indem die Spalten gelöscht werden.

---

### database/migrations/2026_03_31_000003_create_consents_table.php
```json
{
  "file_summary": "Migration zur Erstellung einer Tabelle für Einwilligungen. 
  Die Tabelle enthält Spalten für den Benutzer, die IP-Adresse (optional), 
  Kategorien der Einwilligung und Zeitstempel für Einwilligung und Widerruf.
  Verwendete Klassen: Migration, Blueprint, Schema.
  Externe Abhängigkeiten: Illuminate\Database."
}
```

---

### database/migrations/2026_03_31_099999_create_recherche_tables_sqlite.php
```
{
  "file_summary": "Erstellt Datenbank-Tabellen für Recherche-Projekte in SQLite, einschließlich Benutzer-, Projekt-, Phasen-, Treffer- und Warnsignal-Tabellen.",
  "key_classes": ["Illuminate\Database\Migrations\Migration", "Illuminate\Database\Schema\Blueprint"],
  "main_function": "up",
  "external_dependencies": ["DB", "Schema"]
}
```

---

### database/migrations/2026_03_31_100000_create_recherche_extensions_and_enums.php
Dieses Script erstellt oder aktualisiert Datenbank-Typen und -Extensions im PostgreSQL-DBMS. Es legt Enums für diverse Anwendungsfälle an und prüft deren Existenz, um Idempotenz sicherzustellen. Die Migration verwendet das Laravel-Migration-Framework und setzt somit auf das Framework 'Illuminate' ab.

---

### database/migrations/2026_03_31_100001_create_projekte_and_phasen_tables.php
Migration zur Erstellung von Datenbanktabellen für Projekte und Phasen. Fügt die Tabellen 'projekte' und 'phasen' hinzu, wenn sie noch nicht existieren.

---

### database/migrations/2026_03_31_100002_create_p1_tables.php
Migration zur Erstellung von Tabellen in einer PostgreSQL-Datenbank. Die Tabelle 'p1_strukturmodell_wahl' enthält Informationen über die Wahl eines Strukturmodells, die Tabelle 'p1_komponenten' enthält Komponentendaten und die Tabelle 'p1_kriterien' enthält Kriterieninformationen.

---

### database/migrations/2026_03_31_100003_create_p2_tables.php
Dieses Skript erstellt oder aktualisiert Tabelle in der Datenbank für das Projekt p2. Es enthält die Tabellen "p2_review_typ_entscheidung", "p2_mapping_suchstring_komponenten", "p2_trefferlisten" und "p2_cluster".

---

### database/migrations/2026_03_31_100004_create_p3_tables.php
```
{
  "file_summary": "Die Datei erstellt oder löscht Tabellen in der Datenbank, je nachdem welche bereits existieren. Die Tabellen sind: p3_disziplinen, p3_datenbankmatrix, p3_geografische_filter und p3_graue_literatur.",
  "classes": ["Migration"],
  "functions": ["up", "down"],
  "endpoints": [null],
  "external_dependencies": [
    {"name": "Illuminate\Database\Migrations\Migration"},
    {"name": "Illuminate\Database\Schema\Blueprint"},
    {"name": "Illuminate\Support\Facades\DB"},
    {"name": "Illuminate\Support\Facades\Schema"}
  ]
}
```

---

### database/migrations/2026_03_31_100005_create_p4_tables.php
```
{
  "file_summary": "Migration der Datenbank für P4-Tabelle: 
    - p4_suchstrings (Suchstrings und Filter)
    - p4_thesaurus_mapping (Thesaurus-Mapping)
    - p4_anpassungsprotokoll (Anpassungsprotokoll für Suchstrings)

    Importiert von: Illuminate\Database\Migrations\Migration
    Abhängigkeiten: DB, Schema, Blueprint"
}
```

---

### database/migrations/2026_03_31_100006_create_p5_tables.php
Migration für die Erstellung von Tabellen im Datenbank-Modell 'P5'. Die Migration erstellt folgende Tabellen: p5_prisma_zahlen, p5_screening_kriterien, p5_treffer, p5_screening_entscheidungen und p5_tool_entscheidung. Externe Abhängigkeiten: PostgreSQL-Datenbank.

---

### database/migrations/2026_03_31_100007_create_p6_tables.php
```
{
  "file_summary": "Diese Datei erstellt oder löscht zwei Datenbanktabellen mit entsprechenden Feldern und Beziehungen. 
                   Die Tabellen sind 'p6_qualitaetsbewertung' und 'p6_luckenanalyse'. 
                   Es werden UUIDs als Primärschlüssel verwendet, sowie Beziehungen zu anderen Tabellen (p5_treffer, projekte).",
  "key_classes": ["Migration", "Blueprint"],
  "important_functions": ["up", "down"],
  "external_dependencies": ["Illuminate\Database\Migrations\Migration", 
                           "Illuminate\Database\Schema\Blueprint", 
                           "Illuminate\Support\Facades\DB", 
                           "Illuminate\Support\Facades\Schema"]
}
```

---

### database/migrations/2026_03_31_100008_create_p7_tables.php
Diese Datei erstellt bzw. löscht vier Tabelle im PostgreSQL-Datenbank-System: p7_synthese_methode, p7_datenextraktion, p7_muster_konsistenz und p7_grade_einschaetzung. Die Tabelle p7_synthese_methode enthält die gewählte Methode für ein Projekt, p7_datenextraktion enthält Informationen zu den Daten, die extrahiert wurden, p7_muster_konsistenz enthält Hinweise auf Konsistenzmuster und p7_grade_einschaetzung enthält eine Einschätzung der Qualität der Studien. Externe Abhängigkeiten sind z.B. die Tabelle 'projekte'.

---

### database/migrations/2026_03_31_100009_create_p8_tables.php
Dieses Skript erstellt oder aktualisiert vier Datenbanktabellen: p8_suchprotokoll, p8_limitationen, p8_reproduzierbarkeitspruefung und p8_update_plan. Es wird dabei von der PostgreSQL-Datenbank verwendet.

---

### database/migrations/2026_03_31_100010_create_recherche_indices.php
Diese Datei erstellt und löscht Indices in der PostgreSQL-Datenbank für die Recherche-Funktionen. Wichtigste Klassen/Funktionen sind die Migration und die Statements, um die Indices zu erstellen oder zu löschen. Externe Abhängigkeiten sind die Datenbank-Abstraktion (DB) von Laravel.

---

### database/migrations/2026_03_31_100011_create_prisma_function.php
Erstellt und löscht PostgreSQL-Funktion 'berechne_prisma_zahlen' für Berechnung von PRISMA-2020-Kennzahlen.

---

### database/migrations/2026_03_31_200000_create_chat_messages_table.php
Migration zur Erstellung der Tabelle 'chat_messages'. Fügt eine neue Tabelle zum Chat-System hinzu, um Nachrichten und deren zugehörige Informationen zu speichern. Die Tabelle enthält Spalten für die eindeutige ID, den Benutzer, das Rolle (User oder Assistent), Inhalt der Nachricht und den Zeitpunkt der Erstellung. Außerdem wird eine Fremdschluessel-Beziehung zum 'users'-Table eingerichtet.

---

### database/migrations/2026_03_31_200001_create_webhooks_table.php
Migration für die Erstellung der Tabelle 'webhooks' und Anpassungen an der Tabelle 'chat_messages'. Die Tabelle 'webhooks' enthält Informationen zu Webhooks, wie Name, Slug, URL und frontend-Beziehungen. Außerdem wird eine Fremdschlüsselbeziehung zur Tabelle 'users' hergestellt. In der Tabelle 'chat_messages' wird ein neuer Spalte 'webhook_id' hinzugefügt, die auf die Tabelle 'webhooks' verweist.

---

### database/migrations/2026_03_31_200002_create_paper_embeddings_table.php
Migration zur Erstellung der Tabelle "paper_embeddings" im PostgreSQL-Datenbank-System, um Vektordaten für Papierinhalte zu speichern. Die Tabelle enthält Spalten wie Titel, Textstücke und Metadaten sowie eine Vektorspalte für die Embeddings-Darstellung.

---

### database/migrations/2026_04_01_090000_create_activity_log_table.php
```
{
  "file_summary": "Definiert eine Migration, um die 'activity_log'-Tabelle in der Datenbank zu erstellen oder zu löschen.",
  "key_classes": ["Migration"],
  "main_functions": ["up", "down"],
  "external_dependencies": [
    "Illuminate\Database\Migrations\Migration",
    "Illuminate\Database\Schema\Blueprint",
    "Illuminate\Support\Facades\Schema"
  ]
}
```

---

### database/migrations/2026_04_01_100000_add_status_to_users_table.php
```
{
  "file_summary": "Migration für PostgreSQL und SQLite: Hinzufügen der Spalte \"status\" zur Tabelle \"users\" mit möglichen Werten aus dem Enum-Typ \"user_status\".",
  "important_classes": ["Migration"],
  "main_functions": ["up", "down"],
  "external_dependencies": [
    {"name": "Illuminate\Database\Migrations\Migration"},
    {"name": "Illuminate\Database\Schema\Blueprint"},
    {"name": "Illuminate\Support\Facades\DB"},
    {"name": "Illuminate\Support\Facades\Schema"}
  ]
}
```

---

### database/migrations/2026_04_01_200000_add_frontend_object_to_webhooks.php
```
{
  "file_summary": "Diese Datei fügt eine neue Spalte \"frontend_object\" zur Tabelle \"webhooks\" hinzu und definiert ein PostgreSQL-Enum für mögliche Webhook-Frontend-Objekte. Dabei werden die Datenbankstrukturänderungen in beide Richtungen (aufwärts und abwärts) verarbeitet.",
  "classes": ["Migration"],
  "functions": ["up", "down"],
  "endpunkte": [],
  "externale Abhängigkeiten": ["Illuminate\Database\Migrations\Migration", "Illuminate\Support\Facades\DB", "Illuminate\Support\Facades\Schema"]
}
```

---

### database/seeders/DatabaseSeeder.php
Die Datei DatabaseSeeder.php dient als Starter für die Datenbank-Initialisierung. Sie ruft die Rollen und Recherche-Demo-Seeds auf und erstellt einen Testbenutzer mit einer bestimmten Rolle.

---

### database/seeders/RechercheDemoSeeder.php
Dieses Skript seedet Daten für ein Demo-Projekt in der Recherche-Datenbank. Es erstellt Benutzer, Projekt und Phasen. Außerdem werden P1-Komponenten, Einschluss- und Ausschlusskriterien, Trefferlisten (P2) und Suchstrings (P4) geplant. Der Ablauf ist als Sequenz von Methodenaufrufen definiert: seedWebhooks, seedProjekt, seedPhasen, seedP1, seedP2, seedP4, seedP5, seedP6, seedP7.

---

### database/seeders/RoleSeeder.php
Diese Datei ist ein Seeder für die Datenbank und übernimmt die Rolle der Admin- und User-Rollen. Es handelt sich um eine Art Initialisierungskonfiguration, die vor der ersten Verwendung des Systems ausgeführt wird.

---

### database/sql/create_langdock_agent_user.sql
Diese Datei erstellt einen DB-User 'langdock_agent' mit eingeschränktem Zugriff auf Recherche-Tabelle. Es gibt eine Reihe von GRANTs und REVOKEs, um den User zu konfigurieren. Die wichtigsten Klassen sind keine explizit definierten, da es sich hier um SQL-Script handelt. Wichtige Funktionen sind die Rolleerstellung und -konfiguration sowie der Zugriff auf spezifische Tabellen. Externe Abhängigkeiten gibt es in Form von PostgreSQL- und MCP-DB-Zugriffen.

---

## temp_dummy

### .github/pull_request_template.md
Das Template für Pull-Requests enthält eine Beschreibung der geänderten Funktionen und eine Checkliste zur Überprüfung der Änderungen.

---

## tests

### tests/Pest.php
```
{
  "file_summary": "Definiert Testfunktionen und Erwartungen für eine Testumgebung. 
                   Bindet die Testcase-Klasse an einen spezifischen PHPUnit-Testfall.
                   Definiert Erwartungsmethoden, wie z.B. 'toBeOne'.
                   Bietet globale Funktionen als Hilfestellungen.",
  "imported_classes": ["PHPUnit\Framework\TestCase", "Illuminate\Foundation\Testing\RefreshDatabase"],
  "important_functions": ["extend", "expect", "something"]
}
```

---

### tests/TestCase.php
Testfall-Implementierung für Laravel-Anwendungen. Vererbt von BaseTestCase. Leeres Abstract-Class, wird spezifisch in konkreten Test-Cases implementiert.

---

### tests/Feature/ContactTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Kontaktformular-Funktion. Es werden verschiedene Szenarien getestet, wie z.B. die Übermittlung eines validen Formulars, die Übermittlung ohne Pflichtfelder und Fehlerfälle wie fehlende oder ungültige E-Mail-Adressen. Die wichtigsten Klassen sind \"Contact\" und der Routendefinition \"contact.store\".",
  "klassen_funktionen_endpunkte": ["Contact", "contact.store"],
  "externe_abhaengigkeiten": ["App\Models\Contact"]
}
```

---

### tests/Feature/DashboardTest.php
Diese Datei enthält Funktionen zur Testsicherung der Dashboardeinstellungen. Die wichtigsten Klassen sind 'User' und die 'Dashboard'-Route. Es werden Tests für Gäste und authentifizierte Benutzer durchgeführt.

---

### tests/Feature/ExampleTest.php
Dies ist ein Test-File, welches die Funktion eines Controllers überprüft. Es gibt keinen Code für eigentliche Funktionen dieses Files sondern es wird eine HTTP-Anfrage gestellt und der Statuscode (200) geprüft.

---

### tests/Feature/IngestPaperJobTest.php
Diese Datei enthält Unit-Tests für die IngestPaperJob-Klasse, die es ermöglicht, wissenschaftliche Artikel zu integrieren. Die Tests überprüfen den erfolgreichen Dispatch des Jobs, die korrekte Aufnahme von Parametern und die Handhabung von Fehlern bei Aufrufen der Ollama-API sowie die Implementierung von Retry-Mechanismen und Chunking für lange Texte.

---

### tests/Feature/LegalPagesTest.php
Diese Datei enthält Unit-Tests für die Rendering-Funktion von Impressum-, DSGVO- und AGB-Seiten. Die Tests überprüfen, ob diese Seiten korrekt gerendert werden können.

---

### tests/Feature/PaperRagTest.php
```
{
  "file_summary": "Diese Datei enthält Funktionen zum Testen von API-Anfragen für die Integration von Paper-Inhaltsdaten. Es werden verschiedene Szenarien getestet, wie z.B. der Austausch von Daten zwischen verschiedenen Diensten, Validierung von Eingabedaten und Fehlerhandling.",
  "klassen_funktionen_endpunkte": [
    "\App\Jobs\IngestPaperJob",
    "mcpHeaders()",
    "ollamaUrl()",
    "/api/papers/ingest",
    "/api/papers/rag-search"
  ],
  "externe_abhaengigkeiten": ["Illuminate\Support\Facades\Config", "Illuminate\Support\Facades\Http", "Illuminate\Support\Facades\Queue"]
}
```

---

### tests/Feature/ProjektPolicyTest.php
```json
{
  "file_summary": "Diese Datei definiert Tests für die Projekt-Policy. Es werden Szenarien getestet, in denen ein Besitzer seine eigenen Projekte ansehen, bearbeiten und löschen kann, während Nicht-Besitzer nicht Zugriff auf diese Aktionen haben. Darüber hinaus wird der Erstellungsprozess für neue Projekte von einer sich authentifizierenden Nutzer getestet.",
  "important_classes": ["ProjektPolicy", "User", "Projekt"],
  "important_functions": ["view", "update", "delete", "create"],
  "external_dependencies": ["App\Models\Recherche\Projekt", "App\Models\User"]
}
```

---

### tests/Feature/RechercheTest.php
Diese Datei enthält Unit-Tests für die Recherche-Funktionen. Sie testet die Schaffung von Forschungsprojekten, den Abgleich der Eingabe mit der Datenbank und die Zugriffsbeschränkungen auf Projekte.

---

### tests/Feature/WebhookTest.php
Diese Datei enthält Unit-Tests für die Webhook-Funktionen von Langdock. Die Tests überprüfen die Funktionalität der Webhooks in verschiedenen Szenarien, wie z.B. gültiger und ungültiger Signatur, fehlender oder ungültigen Projekts-ID und User-ID. Die wichtigsten Klassen und Funktionen sind die Webhook-Kontroller und die Queue-Funktionen von Laravel. Externe Abhängigkeiten sind die Config- und Queue-Facades von Laravel.

---

### tests/Feature/WelcomeTest.php
Diese Datei enthält Unit-Tests für die Funktionen der Startseite. Die Tests überprüfen, ob die Startseite erfolgreich geladen wird, ob die Besucherzähler korrekt aktualisiert wird und ob bestimmte Links angezeigt werden.

---

### tests/Feature/Auth/AuthenticationTest.php
Diese Datei enthält Funktionen für Authentifizierungs- und Autorisierungstests. Die wichtigsten Klassen sind User und die Laravel-Fortify-Funktionen. Externe Abhängigkeiten sind die route()-, get()- und post()-Funktionen sowie die factory()-Methode der User-Klasse.

---

### tests/Feature/Auth/EmailVerificationTest.php
Diese Datei enthält Tests für die Email-Verifizierung in einer Authentifizierungs-Anwendung. Die wichtigsten Funktionen sind die Prüfung der Anzeige des Verifizierungs-Bildschirms, die Überprüfung der E-Mail-Adresse und die Behandlung von bereits verifizierten Nutzern.

---

### tests/Feature/Auth/PasswordConfirmationTest.php
```
{
  "file_summary": "Diese Datei enthält einen Testfall für die Authentifizierung, insbesondere die Bestätigung des Passworts. Sie überprüft, ob eine Benutzer mit erfolgreichem Login auf das Passwort-Bestätigungs-Screen zugreifen kann.",
  "important_classes_functions_endpoints": {
    "Klasse": "App\Models\User",
    "Funktion": "factory()->create()",
    "Endpunkt": "route('password.confirm')",
    "externe Abhängigkeit": "DB-Zugriff, via User-Factory"
  }
}
```

---

### tests/Feature/Auth/PasswordResetTest.php
Bereitet und testet die Funktionen für das Zurücksetzen von Passwörtern. Dazu gehören der Link zum Passwort zurücksetzen, das Senden einer Bestätigungsnotifikation an den Benutzer und die Überprüfung der erfolgreichen Passwortänderung.

---

### tests/Feature/Auth/RegistrationTest.php
Diese Datei enthält zwei Einheitstests für die Registrierungsfunktion. Der erste Test prüft, ob die Registrierungsseite angezeigt wird. Der zweite Test überprüft, ob neue Benutzer erfolgreich registriert werden können.

---

### tests/Feature/Auth/TwoFactorChallengeTest.php
Tests für Authentifizierung und zwei-Faktor-Authentifizierung mit Laravel Fortify

---

### tests/Feature/Chat/BigResearchChatTest.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Chat-Funktionen. Es werden verschiedene Szenarien getestet, wie z.B. das Senden von Nachrichten, das Validieren von Pflichtfeldern, das Behandeln von HTTP-Fehlern und der Verbindungsausfall. Außerdem wird die Funktion zum Löschen der eigenen Nachrichten und die Renderng einer Liste mit Chat-Nachrichten getestet.",
  "main_classes": ["ChatMessage", "User", "Webhook", "Http"],
  "main_functions": ["sendMessage", "clearHistory", "getChatMessages"],
  "external_dependencies": ["Illuminate\Support\Facades\Http"]
}
```

---

### tests/Feature/Recherche/PhaseP1Test.php
```json
{
  "file_summary": "Diese Datei enthält eine Sammlung von Einheitstests für die Phase P1 einer Recherchearbeit. Es werden Funktionen wie Strukturmodellwahl, Komponenten- und Kriterienmanagement getestet. Die Tests prüfen die Erstellung, Bearbeitung und Löschung dieser Elemente sowie die Validierung von Pflichtfeldern.",
  "important_classes": ["P1Strukturmodellwahl", "P1Komponente", "P1Kriterium"],
  "function_endpoints": ["newSmw", "saveSmw", "editSmw", "deleteSmw", "newKomp", "saveKomp", "editKomp", "deleteKomp", "newKrit", "saveKrit", "editKrit"],
  "external_dependencies": ["App\Models\Recherche\", \"User\", \"Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP2Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für den Phase-P2-Prozess. Sie überprüfen die Funktionalität von Review-Typen, Clustern und Mappings. Die Tests decken sowohl Erstellungs-, Bearbeitungs- als auch Löschvorgänge ab.",
  "key_classes": ["P2ReviewTypEntscheidung", "P2Cluster", "P2MappingSuchstringKomponente", "P2Trefferliste"],
  "important_functions": ["create", "update", "delete"],
  "external_dependencies": ["App\Models\Recherche\", \"Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP3Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für den Phase-P3-Prozess der Recherche-Funktion. 
    Es werden Tests durchgeführt für die Datenbankmatrix, Disziplin, geografischen Filter und graue Literatur.
    Die wichtigsten Klassen sind P3Datenbankmatrix, P3Disziplin, P3GeografischerFilter und P3GraueLiteratur.
    Externe Abhängigkeiten bestehen zu den Modellen Projekt, User und Livewire/Volt.",
  "main_classes": ["P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"],
  "main_functions": ["create", "edit", "delete"],
  "external_dependencies": ["Projekt", "User", "Livewire/Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP4Test.php
```json
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Features 'Suchstring', 'ThesaurusMapping' und 'Anpassungsprotokoll' in der Recherche-Phase P4. 
  Es werden Funktionen wie Erstellen, Bearbeiten und Löschen von Suchstrings, Thesaurus-Mappings und Anpassungsprotokollen getestet.
  Die wichtigsten Klassen sind: P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll und Volt (Livewire).
  Externe Abhängigkeiten: Datenbank (insbesondere 'p4_suchstrings', 'p4_thesaurus_mapping' und 'p4_anpassungsprotokoll')",
}
```

---

### tests/Feature/Recherche/PhaseP5Test.php
Diese Datei enthält Tests für die Phase P5 der Recherche-Funktion. Sie prüft die Funktionalität von Prisma-Zahlen, Screening-Kriterien, Tool-Entscheidungen und Screening-Entscheidungen. Die Tests decken sowohl Erstellung als auch Bearbeiten und Löschen dieser Datenobjekte ab. Zudem werden Validierungsprüfungen durchgeführt, um sicherzustellen, dass erforderliche Felder nicht leer sind. Externe Abhängigkeiten sind die Models-Recherche-Modelle (Projekt, P5PrismaZahlen usw.) und die Livewire-Volt-Klasse für das Unit-Testing der Views.

---

### tests/Feature/Recherche/PhaseP6Test.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Phase P6 in der Recherche-Funktion.
Die Tests überprüfen die Funktionen von Qualitätsbewertung und Lückenanalyse, einschließlich Erstellung, Bearbeitung und Löschen.
Außerdem wird die Authentifizierung getestet, um sicherzustellen, dass nur autorisierte Benutzer Zugriff haben.",
  "wichtige Klassen": ["P6Qualitaetsbewertung", "P6Luckenanalyse"],
  "wichtige Funktionen": ["newQb", "saveQb", "editQb", "deleteQb", "newLa", "saveLa", "editLa", "deleteLa"],
  "externe Abhängigkeiten": ["App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/PhaseP7Test.php
```
{
  "file_summary": "Die Datei implementiert Tests für die Phase P7 des Recherche-Prozesses, 
                  um zu überprüfen, ob Benutzer synthese-Methode erstellen, bearbeiten und löschen können,
                  Datenextraktion erstellen, bearbeiten und löschen können, Muster & Konsistenz erstellen,
                  bearbeiten und löschen können sowie eine Grade-Einschätzung erstellen, bearbeiten
                  und löschen können. Externe Abhängigkeiten: App\Models, Livewire\Volt."
}
```

---

### tests/Feature/Recherche/PhaseP8Test.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für die Phase P8 der Recherche-Funktionalität. Sie umfasst Tests für Suchprotokolle, Limitationen, Reproduzierbarkeitsprüfungen und Update-Pläne. Die wichtigsten Klassen sind P8Suchprotokoll, P8Limitation, P8Reproduzierbarkeitspruefung und P8UpdatePlan.",
  "key_classes": ["P8Suchprotokoll", "P8Limitation", "P8Reproduzierbarkeitspruefung", "P8UpdatePlan"],
  "important_functions": ["create", "delete", "saveSp", "saveLim", "saveRep", "saveUp", "assertDatabaseHas", "assertDatabaseMissing"],
  "external_dependencies": ["App\Models\User", "Livewire\Volt\Volt"]
}
```

---

### tests/Feature/Recherche/ProjektDetailTest.php
```
{
  "file_summary": "Diese Datei enthält Unit-Tests für den Projekt-Detail-Bereich einer Recherche-Anwendung. 
                   Die Tests prüfen das Rendern des Standard-Tabs, die Wechselbarkeit der Tabs, die Clamping-Funktion, 
                   die Auslieferung von 403-Fehlern für Nicht-Eigentümer und die Renderung spezifischer Phasen.
                   Die wichtigsten Klassen sind Projekt und User. Externe Abhängigkeiten sind Livewire (Volt) und die Eloquent-Modell-Paket."
}
```

---

### tests/Feature/Settings/AppearanceTest.php
Diese Datei enthält Unit-Tests für die Einstellungen-Ansicht. Sie überprüft, ob die Ansicht korrekt rendern und wie sie sich verhält, wenn ein eingeloggter Benutzer darauf zugreift.

---

### tests/Feature/Settings/PasswordUpdateTest.php
```json
{
  "file_summary": "Diese Datei enthält Einheitstests für die Funktion zum Ändern eines Benutzerpassworts. Es werden zwei Testfälle durchgeführt: der erste testet, ob das Passwort erfolgreich aktualisiert wird, wenn ein gültiges aktuelles Passwort eingegeben wird, und der zweite testet, ob ein Fehler auftritt, wenn ein falsches aktuellen Passwort eingegeben wird. Die wichtigsten Klassen sind User, Hash und Volt.",
  "key_classes": ["User", "Hash", "Volt"],
  "external_dependencies": ["Illuminate\Support\Facades\Hash"]
}
```

---

### tests/Feature/Settings/ProfileUpdateTest.php
Diese Datei enthält Tests für die Benutzerverwaltungsfunktionen, wie Profilupdate, Email-Verifizierung und Benutzerlöschung. Die wichtigsten Klassen sind User, Volt (ein Livewire-Komponenten-Test-Framework) und die Route 'profile.edit'. Externe Abhängigkeiten sind die Livewire-API und die Database-Zugriffe über Eloquent.

---

### tests/Feature/Settings/TwoFactorAuthenticationTest.php
```
{
  "file_summary": "Diese Datei enthält Tests für die Funktionen der Zwei-Faktor-Authentifizierung, insbesondere für die Auflistung der Einstellungen und den Abschluss der Bestätigung. Es werden verschiedene Szenarien getestet, wie z.B. ob die Zwei-Faktor-Authentifizierung aktiviert ist und ob der Benutzer das Passwort bestätigen muss.",
  "imported_classes": ["App\Models\User", "Laravel\\Fortify\\Features"],
  "extern_dependencies": ["database (User)", "routes ('two-factor.show')"]
}
```

---

### tests/Feature/Settings/WebhooksTest.php
```
{
  "file_summary": "Diese Datei testet Funktionen für die Verwaltung von Webhooks in der Anwendung. Es gibt Tests für das Speichern und Löschen von Webhooks für Dashboard Chat und Recherche starten, sowie ein Test für den Aufruf der bestehenden Webhooks und einen Test für die Isolation bei der Löschung von fremden Webhooks.",
  "important_classes": ["Webhook", "User"],
  "important_functions": ["saveDashboard", "clearDashboard", "saveRecherche", "clearRecherche", "mount"],
  "external_dependencies": ["App\Models\User", "App\Models\Webhook", "Livewire\Volt\Volt"]
}
```

---

### tests/Unit/ExampleTest.php
Unittest für PHP-Einheiten, beispielsweise Logik von true ist wahr.

---

## ui

### app/Filament/Resources/ContactResource/Pages/ListContacts.php
```json
{
  "file_summary": "Diese Datei definiert eine Liste für Kontakte im Administrationsbereich von Filament.\n\
Es handelt sich um eine Vererbung der Klasse ListRecords und legt die Ressource ContactResource fest.",
  "key_classes_functions_endpoints": {
    "Klasse": ["ListContacts", "ContactResource"],
    "Endpunkte": ["*"],
    "Funktionen": []
  },
  "externe_abhaengigkeiten": {
    "Imports": ["App\\Filament\\Resources\\ContactResource\\Pages\\ListRecords"],
    "APIs/DB-Zugriffe": [""]
  }
}
```

---

### app/Filament/Resources/ContactResource/Pages/ViewContact.php
Diese Datei definiert eine View-Page für das Kontaktressourcen-Modul in Filament. Sie erstellt eine Ansicht, um einen einzelnen Kontakt anzuzeigen und bearbeiten zu können. Die wichtigste Klasse ist der "ViewContact"-Controller.

---

### app/Filament/Resources/UserResource/Pages/EditUser.php
Definiert die "Bearbeiten-Benutzer"-Seite für den Benutzereingabebereich von Filament.

---

### app/Filament/Resources/UserResource/Pages/ListUsers.php
Definiert eine Liste von Benutzerressourcen für die Webanwendung. Hauptklasse: ListUsers, externe Abhängigkeit: Filament/Resources.

---

### resources/views/dashboard.blade.php
Diese Datei stellt die Dashboard-Ansicht für Benutzer bereit. Sie enthält eine Übersicht über das letzte Projekt, Quicklinks zu Recherche und Admin-Funktionen sowie einen Chat-Widget.

---

### resources/views/welcome.blade.php
Die Datei 'welcome.blade.php' ist eine Blade-Template-Datei für die Benutzeroberfläche von Linn Games. Sie enthält HTML-Codes und CSS-Stile für die Darstellung des Inhalts der Website, einschließlich einer herkömmlichen Navigationsmenü-Erscheinungsbild und einer animierten Hintergrund-Simulation. Der Template-Code verwendet Blade-Syntax zur Einbindung von Daten aus dem Laravel-Modell.

---

### resources/views/components/action-message.blade.php
Diese Datei erstellt ein Benachrichtigungsfeld für die Anzeige von Aktionen (z.B. "Gespeichert").

---

### resources/views/components/app-logo-icon.blade.php
Dieses Blade-Template generiert ein SVG-Bild, das den App-Logo-Icon darstellt. Es verwendet eine detaillierte Pfadbeschreibung, um die Logo-Elemente zu zeichnen. Die verwendeten Farben und Rundungen können durch Attribute übergeben werden.

---

### resources/views/components/app-logo.blade.php
Zeigt das Logo und den Namen des Laravel Starter Kits an. Die Datei enthält einen Div-Block, der ein Logo-Icon und den Namen des Kits zeigt.

---

### resources/views/components/auth-header.blade.php
```
{
  "file_summary": "Bildet ein Benutzerschnittstelle für Authentifizierungsinformationen ab. 
    - Übernimmt Titel und Beschreibung aus der Oberfläche.
    - Darstellung eines H1-Titels und einer Beschreibung unterhalb davon."
}
```

---

### resources/views/components/auth-session-status.blade.php
Bereitet Authentifizierungsstatus-Anzeige vor, zeigt Anmeldestatus als Div an, wenn $status true ist.

---

### resources/views/components/placeholder-pattern.blade.php
```json
{
  "file_summary": "Eine Blade-Vorlage für ein SVG-Muster, das über eine generierte ID verfügbar ist. Die Vorlage kann mit benannten Eigenschaften angesprochen werden, um die Attribute des SVG-Elements zu konfigurieren.",
  "key_classes_functions_endpoints": {
    "Keine spezifischen Klasse, Funktion oder Endpunkt sind in dieser Beschreibung erwähnt."
  },
  "external_dependencies": []
}
```

---

### resources/views/components/layouts/app.blade.php
```json
{
  "file_summary": "Blade-Template für Layouts, um Seiteninhalte anzuzeigen. 
                    Ermöglicht es, Seiten mit einem standardisierten Layout zu erstellen.
                    Wichtigste Komponente: x-layouts.app.sidebar,
                    das einen Sidebar-Layout enthält."
}
```

---

### resources/views/components/layouts/auth.blade.php
Bereitstellt eine einfache Authentifizierungs-Layout-Komponente mit Titel und Inhaltsbereich

---

### resources/views/components/layouts/app/header.blade.php
Der Header-Layout der Anwendung beinhaltet eine navigierbare Headerbar mit Links zu verschiedenen Seiten und einem User-Dropdown-Menü. Es wird verwendet, um die Navigation innerhalb der Anwendung zu steuern.

---

### resources/views/components/layouts/app/sidebar.blade.php
Das Blade-Template für die Sidebar eines Webanwendungs Layouts. Es enthält Navigationselemente, einen Benutzermenü und Links zu externen Ressourcen.

---

### resources/views/components/layouts/auth/card.blade.php
```json
{
  "file_summary": "Diese Datei definiert ein authentifizierbares Kartenlayout, das für verschiedene Authentifizierungscards verwendet werden kann. Die wichtigsten Funktionen sind: 
  - Einbindung des App-Logos und eines Links zur Startseite.
  - Eine Karte mit einem Slot, der durch eine Variable $slot übersetzt wird.
  - Ein Cookie-Konsents-Modul und Livewire-Konfiguration.
  Externe Abhängigkeiten sind: 
  - Inklusion von partials.head im Kopfabschnitt."
}
```

---

### resources/views/components/layouts/auth/simple.blade.php
Diese Datei enthält eine einfache Authentifizierungslayout-Struktur für eine Anwendung. Sie besteht aus einer HTML-Struktur, die den Head und Body bereitstellt, sowie Inklusions von Teilkomponenten (partial.blade.php) und Event-Listen (wire:navigate). Die Layout-Struktur ermöglicht das Anwenden eines Platzhalters ($slot), der durch eine Oberfläche ersetzt werden kann. Außerdem wird die Cookie Consent Komponente eingeschlossen.

---

### resources/views/components/layouts/auth/split.blade.php
Bereitstellung einer authentifizierten Seitenlayout-Struktur, die einen Header mit Logo und Slogan enthält und Platz für Inhalt bietet.

---

### resources/views/components/settings/layout.blade.php
Dieses Blade-Templates stellt eine Einstellungs-Oberfläche für das Profil dar. Es enthält Navigationselemente, ein Inhaltsbereich und kann durch die Verwendung von slots angepasst werden.

---

### resources/views/emails/contact-confirmation.blade.php
HTML-Template für eine Bestätigungsmail nach Kontaktanfrage. Dazu gehören: Die Ansprache des Nutzers, eine Zusammenfassung der Anfrage und ein Abschlusswort.

---

### resources/views/emails/contact-inquiry.blade.php
Diese Datei enthält eine E-Mail-Vorlage für Kontaktanfragen. Sie stellt die von Benutzern gesendeten Kontaktdaten und Nachrichten in einer Tabelle dar. Die wichtigsten Klassen sind nicht direkt nennenswert, da es sich um eine Vorlage handelt. Externe Abhängigkeiten beziehen sich auf die verfügbaren Daten ($contact), die wahrscheinlich von einem Controller oder Modell bereitgestellt werden.

---

### resources/views/emails/deploy-notification.blade.php
Dieser Blade-Template beschreibt eine E-Mail-Nachricht, die an Benutzer gesendet wird, wenn ein Deployvorgang erfolgreich ist. Die E-Mail enthält Informationen zum Zeitpunkt des Deploys, der URL und der Umgebung.

---

### resources/views/layouts/legal.blade.php
Datenbank-unabhängige Seite für die Darstellung von rechtlichen Inhalten. Enthält Header, Footer und Layout-Elemente.

---

### resources/views/legal/agb.blade.php
Diese Datei enthält die Allgemeinen Geschäftsbedingungen (AGB) für das Online-Spiel und die Website von Linn.Games. Sie regeln die Nutzung des Dienstes, einschließlich Registrierung, Benutzerkonto, Nutzungsregeln, Kosten und Widerrufsrecht.

---

### resources/views/legal/dsgvo.blade.php
Diese Datei enthält die Datenschutz-Erklärung für die Website. Sie erklärt, wie personenbezogene Daten gesammelt und verwendet werden, einschließlich der Registrierungsdaten, Authentifizierungsdaten, Spieldaten, Nutzungs- und Verbindungsdaten sowie Kontaktdaten. Die Erklärung erläutert auch die Rechte der Betroffenen, wie z.B. das Recht auf Auskunft, Berichtigung, Löschung, Einschränkung der Verarbeitung und Widerspruch. Externe Abhängigkeiten sind keine vorhanden.

---

### resources/views/legal/impressum.blade.php
Diese Datei ist Teil der Legal-UI des Projekts und dient als Vorlage für das Impressum-Seite. Sie erweitert die layouts.legal Blade-Datei und legt den Titel und den Inhalt fest.

---

### resources/views/livewire/auth/confirm-password.blade.php
Blade-Vorlage für die Bestätigung eines Passworts. Hier wird ein Formular zur Eingabe des aktuellen Passworts angezeigt und nach dem Absenden einer POST-Anfrage wird die Passwortbestätigung auf Serverseite überprüft.

---

### resources/views/livewire/auth/forgot-password.blade.php
Diese Datei enthält eine Blade-Vorlage für das Passwort-Erstellen-Prozess. Die wichtigsten Funktionen sind: Ein Formular zum eingeben des E-Mail-Adressen, um einen Passwort-Wiederherstellungslink zu erhalten. Außerdem gibt es eine Status-Anzeige und Links zur Login-Seite.

---

### resources/views/livewire/auth/login.blade.php
Diese Datei ist Teil der UI und enthält die Login-Seite für Benutzer. Sie beinhaltet eine Form, um den Benutzernamen und das Passwort einzugeben, sowie Links zum Vergessen des Passworts und zur Registrierung.

---

### resources/views/livewire/auth/register.blade.php
Ein Blade-Template für die Registrierung eines Benutzers, der einen Formularbereich mit Eingabefeldern (Name, E-Mail-Adresse, Passwort und Bestätigung des Passworts) enthält. Nach dem Absenden des Formulars wird das Ergebnis über eine Session status abgefangen.

---

### resources/views/livewire/auth/reset-password.blade.php
Diese Datei ist Teil der authentifizierungsrelevanten Benutzeroberfläche. Sie enthält ein Formular zur Wiederherstellung eines Passworts, bei dem die Benutzer ihre neue Passwörter eingeben und bestätigen können. Die Datei verwendet Laravel-Livewire zum Erstellen einer interaktiven Oberfläche und verfügt über einige Sicherheitsfunktionen wie CSRF-Schutz und Eingabefehler-Handling.

---

### resources/views/livewire/auth/two-factor-challenge.blade.php
```
{
  "file_summary": "Diese Blade-View handelt sich um ein Formular für die Authentifizierung mit einem zweiten Faktor. 
                  Es enthält zwei verschiedene Eingabefelder: eines für eine Sicherheitscode und eines für einen Ausreißekode.
                  Wenn kein Ausreißekode eingegeben wird, gibt es 6 Felder für den Eingabe des Sicherheitscodes mit einer speziellen Handhabung,
                  um die Eingabe von Zahlen zu erleichtern. Bei Auswahl des Ausreißekodesfelds verschwindet das Sicherheitscode-Feld und 
                  erscheint stattdessen ein normaler Texteingabefelder für den Ausreißekode.
                  Nach dem Absenden des Formulars wird die Eingabe an eine Route weitergeleitet, um sie zu verarbeiten.",
  "important_classes": [
    "x-layouts.auth",
    "form"
  ],
  "important_functions": [],
  "external_dependencies": ["Livewire"]
}
```

---

### resources/views/livewire/auth/verify-email.blade.php
E-Mail-Verifizierungsseite, die einen Benutzer auffordert, seine E-Mail-Adresse zu bestätigen und einen Link zum Überprüfen anzuklicken oder den Bestätigungslink erneut anfordern zu können.

---

### resources/views/livewire/chat/big-research-chat.blade.php
```json
{
  "file_summary": "Livewire-Komponente für den Chat der Big-Research-Chat-Integration.
Wichtigste Funktionen: 
- Anzeige von Chat-Meldungen (User & Assistant)
- Erstellen neuer Chat-Meldungen durch Benutzerinput
- Löschen des gesamten Chatverlaufs

Klassen/Funktionen/Endpunkte:
- getWebhook()
- getChatMessages()
- sendMessage()
- clearHistory()

Externe Abhängigkeiten: 
- App\Models\ChatMessage
- App\Models\Webhook
- Illuminate\Support\Facades\Auth
- Illuminate\Support\Facades\Http"
}
```

---

### resources/views/livewire/recherche/phase-p1.blade.php
```
{
  "file_summary": "Diese Datei ist Teil einer Livewire-Komponente für die Projekt-Recherche. Sie enthält CRUD-Funktionen für Strukturmodellwahl, Komponente, Kriterium und Warnsignal.",
  "important_classes": ["Livewire\Volt\Component", "App\Models\Recherche\{Projekt, P1Strukturmodellwahl, P1Komponente, P1Kriterium, P1Warnsignal}"],
  "external_dependencies": [
    {"type": "DB-Zugriff", "class": "P1Strukturmodellwahl", "model": "App\Models\Recherche\P1Strukturmodellwahl"},
    {"type": "DB-Zugriff", "class": "P1Komponente", "model": "App\Models\Recherche\P1Komponente"},
    {"type": "DB-Zugriff", "class": "P1Kriterium", "model": "App\Models\Recherche\P1Kriterium"},
    {"type": "DB-Zugriff", "class": "P1Warnsignal", "model": "App\Models\Recherche\P1Warnsignal"}
  ]
}
```

---

### resources/views/livewire/recherche/phase-p2.blade.php
```
{
  "file_summary": "Laravel Livewire-Komponente für Projektrecherche: Phase P2. CRUD-Funktionen für Review-Typ-Entscheidung, Cluster, Mapping-Suchstring-Komponente und Trefferliste.",
  "classes": ["P2ReviewTypEntscheidung", "P2Cluster", "P2MappingSuchstringKomponente", "P2Trefferliste"],
  "functions": ["newRev", "saveRev", "editRev", "deleteRev", ..., "cancelTref"],
  "endpoints": [],
  "external_dependencies": [
    "App\Models\Recherche",
    "Illuminate\Support\Facades\Auth"
  ]
}
```

---

### resources/views/livewire/recherche/phase-p3.blade.php
```
{
  "file_summary": "Die Datei enthält eine Livewire-Komponente für die Projektphase P3. Sie ermöglicht das Erstellen und Bearbeiten von Datenbankmatrizen, Disziplinen, geografischen Filtern und grauer Literatur.\n"
  "main_classes": ["Component", "P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"],
  "key_functions": ["newDb", "saveDb", "editDb", "deleteDb", "cancelDb", ...],
  "external_dependencies": [
    {"App\Models\Recherche", "Projekt", "P3Datenbankmatrix", "P3Disziplin", "P3GeografischerFilter", "P3GraueLiteratur"},
    {"Illuminate\Support\Facades\Auth"}
  ]
}
```

---

### resources/views/livewire/recherche/phase-p4.blade.php
```
{
  "file_summary": "Diese Datei ist Teil einer Livewire-Komponente für das Erstellen und Verwalten von Suchstrings, Thesaurus-Mappings und Anpassungsprotokollen innerhalb eines Projekts. Sie umfasst Funktionen zum Erstellen, Bearbeiten und Löschen dieser Entitäten sowie die Verbindung mit der Datenbank. Die wichtigsten Klassen sind P4Suchstring, P4ThesaurusMapping und P4Anpassungsprotokoll.",
  "main_classes": ["P4Suchstring", "P4ThesaurusMapping", "P4Anpassungsprotokoll"],
  "functions_endpunkte": ["newSs", "saveSs", "editSs", "deleteSs", "cancelSs", "newTh", "saveTh", "editTh", "deleteTh", "cancelTh", "newAp", "saveAp", "editAp", "deleteAp", "cancelAp"],
  "imports": ["App\Models\Recherche\{Projekt, P4Suchstring, P4ThesaurusMapping, P4Anpassungsprotokoll}", "Illuminate\Support\Facades\Auth", "Livewire\Volt\Component"]
}
```

---

### resources/views/livewire/recherche/phase-p5.blade.php
```
{
  "file_summary": "Die Datei handle die Benutzereingaben und -aktionen für das Projekt \"Recherche\". Es handelt sich um eine LiveWire-Component, die verschiedene Funktionen bereitstellt, wie z.B.\n\
   \u2022 Prisma-Zahlen erfassen und bearbeiten\n\
   \u2022 Screening-Kriterien erstellen und bearbeiten\n\
   \u2022 Tools für die Recherche auswählen und bearbeiten\n\
   \u2022 Treffer sichten, entscheiden und protokollieren.\n\
Die Datei verwendet verschiedene Modelle von Eloquent (eine ORM) und Livewire Components.",
  "key_classes": ["Livewire\Volt\Component", "App\Models\Recherche\{Projekt, P5Treffer, P5ScreeningKriterium, P5ScreeningEntscheidung, P5ToolEntscheidung, P5PrismaZahlen}"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p6.blade.php
```
{
  "file_summary": "Diese Datei ist ein Livewire-Komponenten-Template für die Phase P6 einer Recherche. Sie bietet Funktionalität zum Hinzufügen, Bearbeiten und Löschen von Qualitätsbewertungen (Risk of Bias) und Lückenanalysen. Die Komponente zeigt eine Liste der Treffer, Bewerungen und Lücken sowie ein Formular für die Eingabe von Daten.",
  "kernklassen": ["P6Qualitaetsbewertung", "P6Luckenanalyse"],
  "externe_abhaengigkeiten": ["App\Models\Recherche\Projekt", "App\Models\Recherche\P5Treffer"],
  "wichtige_functionen": ["newQb", "saveQb", "editQb", "deleteQb"]
}
```

---

### resources/views/livewire/recherche/phase-p7.blade.php
```
{
  "file_summary": "Dies ist eine Livewire-Komponente für die Projekt-Phase P7. Sie bietet CRUD-Funktionen (Create, Read, Update, Delete) für die Verwaltung von Synthese-Methode, Datenextraktion, Muster und Konsistenz sowie GRADE-Einschätzungen. Die Komponente ist authentifiziert und beschränkt den Zugriff auf Projekte des aktuellen Benutzers.",
  "important_classes": ["Livewire\Volt\Component", "App\Models\Recherche\{Projekt, P5Treffer, P7SyntheseMethode, P7Datenextraktion, P7MusterKonsistenz, P7GradeEinschaetzung}"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth"]
}
```

---

### resources/views/livewire/recherche/phase-p8.blade.php
```
{
  "file_summary": "Livewire-Komponente für die Verwaltung von Phasen in einem Projekt. Hier können Suchprotokolle, Limitationen, Reproduzierbarkeit und Update-Pläne erstellt, bearbeitet oder gelöscht werden.",
  "key_classes_functions_endpoints": [
    "P8Suchprotokoll", "P8Limitation", "P8Reproduzierbarkeitspruefung", "P8UpdatePlan",
    "newSp", "saveSp", "editSp", "deleteSp",
    "newLim", "saveLim", "editLim", "deleteLim",
    "newRep", "saveRep", "editRep", "deleteRep",
    "newUp", "saveUp", "editUp", "deleteUp"
  ],
  "external_dependencies": [
    "App\Models\Recherche", "Illuminate\Support\Facades\Auth", "Livewire\Volt\Component"
  ]
}
```

---

### resources/views/livewire/recherche/projekt-detail.blade.php
```json
{
  "file_summary": "Diese Datei ist eine Livewire-Komponente für das Projekt-Detail-Format. Sie enthält ein Header-Bereich, einen Tab-Strips und Inhalt für jede Phase des Projekts. Die Komponente verwendet die Projekt-Modelle für Datenbankzugriff und Authentifizierung zum Verhindern der Zugriffsrechte von unbefugten Benutzern.",
  "classes": [
    "Livewire\Volt\Component",
    "App\Models\Recherche\Projekt"
  ],
  "endpunkte": [],
  "imports": [
    "Illuminate\Support\Facades\Auth",
    "Livewire\Attributes\Url"
  ]
}
```

---

### resources/views/livewire/recherche/projekt-liste.blade.php
```
{
  "file_summary": "Darstellung von Projekt-Listen mit Livewire. 
  Projekte werden aus der Datenbank abgefragt und in einer Liste angezeigt.
  Benutzer können einzelne Projekte löschen, wofür eine Bestätigung erforderlich ist.
  Externe Abhängigkeiten: Illuminate\Support\Facades\Auth,
  App\Models\Recherche\Projekt,
  Livewire\Volt\Component."
}
```

---

### resources/views/livewire/recherche/research-input.blade.php
Livewire-Komponente für die Rechercheneingabe, die die Erstellung eines Projekts und den Start einer Recherche durchführt. Die wichtigsten Klassen sind Projekt und TriggerLangdockAgent. Externe Abhängigkeiten: App\Jobs\TriggerLangdockAgent, App\Models\Recherche\Projekt, Illuminate\Support\Facades\Auth, Livewire\Volt\Component.

---

### resources/views/livewire/settings/appearance.blade.php
```
{
  "file_summary": "Dokumentiert die Einstellungen für das Aussehen der Benutzeroberfläche. Der User kann zwischen drei Themenmodi wählen: Licht, Dunkel und Systemfarbe.",
  " wichtigste-Klassen-Funktionen-Endpunkte": [
    "Livewire\Volt\Component",
    "setAppearance() Funktion zum Setzen des aktuellen Erscheinungsbilds",
    "#appearance-toggle", 
    "appearance-btn" Klasse für die Themen-Buttons
  ],
  "externe-Abhängigkeiten": ["localStorage"],
  "zweck": "Das Datei stellt eine Oberfläche zur Verfügung, um das Aussehen der Benutzeroberfläche zu ändern."
}
```

---

### resources/views/livewire/settings/delete-user-form.blade.php
Diese Blade-Vorlage stellt eine Form zum Löschen eines Benutzerkontos dar. Sie enthält eine Modal-Box, in der das Benutzerkonto gelöscht werden kann, wenn der Benutzer seinen Passwort bestätigt.

---

### resources/views/livewire/settings/password.blade.php
```
{
  "file_summary": "Diese Datei dient als Blade-Vorlage für die Überprüfung und Aktualisierung des Passworts eines Benutzers. Sie verwendet Livewire zur Datenübertragung und -verarbeitung.",
  "key_classes": ["Livewire\Volt\Component"],
  "important_functions": ["updatePassword"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth", "Illuminate\Validation\Rules\Password", "Illuminate\Validation\ValidationException"]
}
```

---

### resources/views/livewire/settings/profile.blade.php
```json
{
  "file_summary": "Das Blade-Template für das Profilbereich in der Livewire-Anwendung. Es ermöglicht Benutzern, ihre Profileinformationen zu aktualisieren und bietet auch die Möglichkeit, eine E-Mail-Bestätigung zu versenden.",
  "wichtige_Klassen_Funktionen": ["Component", "User", "Auth"],
  "externe_Abhängigkeiten": ["Illuminate\Support\Facades\Auth", "Illuminate\Support\Facades\Session"]
}
```

---

### resources/views/livewire/settings/two-factor.blade.php
```json
{
  "file_summary": "Zwei-Faktor-Authentifizierung (2FA) einrichten und verwalten. 
    Die Datei bietet Funktionen für die Aktivierung/Deaktivierung von 2FA, QR-Code generieren, Authentikationscode validieren, Setup-Daten laden, Modal-Boxe und Fehler-Hanterung.",
  "important_classes": ["Livewire\\Component"],
  "main_functions": ["enable", "disable", "confirmTwoFactor", "showVerificationIfNecessary"],
  "external_dependencies": ["Laravel\Fortify", "Laravel\Fortify\Actions", "Livewire"]
}
```

---

### resources/views/livewire/settings/webhooks.blade.php
```
{
  "file_summary": "Diese Datei stellt eine Livewire-Komponente für die Einstellung von Webhooks bereit. Die Komponente ermöglicht es Benutzern, Webhooks für bestimmte Frontend-Objekte zu konfigurieren und zu speichern. Es gibt zwei Formulare: eines für den Dashboard-Chat und eines für das Starten einer neuen Recherche.",
  "important_classes": ["Livewire\Volt\Component"],
  "external_dependencies": [
    {"name": "App\Models\Webhook"},
    {"name": "Illuminate\Support\Facades\Auth"},
    {"name": "Illuminate\Support\Str"}
  ]
}
```

---

### resources/views/livewire/settings/two-factor/recovery-codes.blade.php
```
{
  "file_summary": "Dieses Blade-Template zeigt die Recovery-Codes für die zweifaktorische Authentifizierung an und ermöglicht es dem Benutzer, neue Codes zu generieren. Die Recovery-Codes werden vom Benutzer gespeichert und können zum Wiederherstellen des Zugriffs auf das Konto verwendet werden.",
  "key_classes": ["Livewire\Volt\Component"],
  "important_functions": ["regenerateRecoveryCodes", "loadRecoveryCodes"],
  "external_dependencies": ["Laravel\\Fortify\\Actions\\GenerateNewRecoveryCodes"]
}
```

---

### resources/views/partials/head.blade.php
Diese Datei enthält HTML-Metadaten, Titel und Stylesheets für die Website. Sie verbindet auch externe Fonts und initiiert den Dark-Modus auf Basis von localStorage-Einstellungen.

---

### resources/views/partials/settings-heading.blade.php
```
{
  "file_summary": "Diese Blade-Vorlage generiert ein Headline-Element für die Einstellungen-Ansicht. Es besteht aus einem Titel, einer Beschreibung und einem Horizontal-Separator.",
  "key_classes_functions_endpoints": {
    "Blade-Vorlage": "settings-heading",
    "Hauptfunktionen": [
      "Erstellen des Headline-Elements"
    ]
  },
  "externe_abhaengigkeiten": [
    "__('Settings') und __('Manage your profile and account settings')" verwenden die Laravel-Zahlungsfunktion
  ]
}
```

---

### resources/views/recherche/index.blade.php
Das Layout für die Recherchefunktionen. Es enthält Eingabefelder und Listen für Projekte sowie einen Titel.

---

### resources/views/recherche/show.blade.php
Liefert die Detailansicht eines Projekts in der Recherche-UI.
Hauptfunktion ist das Anzeigen des Projektdetails.

---

## uncategorized

### Readme.md
Laravel-Anwendung zur Verwaltung von Forschungsprojekten mit KI-gestützter systematischer Literaturrecherche. Die Anwendung umfasst Benutzerverwaltung, Kontaktformular, Recherche-Tools, Admin-Panel (Filament), CI/CD und Docker-Services.

---

### artisan
```json
{
  "file_summary": "Bootstrap-Datei für die Anwendung, bereitet Laravel vor und führt Befehle aus.",
  "main_classes_functions_endpoints": [
    "Illuminate\Foundation\Application",
    "$app",
    "handleCommand"
  ],
  "external_dependencies": ["vendor/autoload.php", "/bootstrap/app.php"]
}
```

---

### basemodel.md
Dieses SQL-Script stellt ein Datenbankmodell zur systematischen Literaturrecherche bereit. Es umfasst 8 Phasen und enthält Tabellen für Projekte, Phase-Tracking, Fragemodellierung, Explorative Mapping-Suche, Datenbankauswahl, geografische Filterung und Graue Literatur. Die Tabelle p4_suchstrings ermöglicht die Entwicklung von Suchstrings mit optionalen Feldeinschränkungen und gesetzten Filtern.

---

### CLAUDE.md
Diese Datei bietet Anleitung für den Einsatz von Claude Code in diesem Repository. Sie enthält Befehle, Architektur und Stackinformationen, sowie wichtige Klassen, Funktionen und Endpunkte.

---

### composer.json
```
{
  "file_summary": "Composer-Datei zur Konfiguration der Laravel-Anwendung. 
    - Definiert Abhängigkeiten (Laravel-Framework, Filament, Fortify) und Konfigurationen.
    - Automatisiert Setup-Prozess nach Installation.
    - Konfiguriert Autoloading und Paket-Installation."
}
```

---

### CONTRIBUTING.md
Diese Datei beschreibt die Konventionen und Standards für den Beitrag an das Projekt app.linn.games. Es wird eine Struktur vorgegeben, wie Branches erstellt werden sollten (z.B. feature/, fix/, docs/), sowie ein Merge-Fluss für Pull Requests beschrieben. Darüber hinaus werden Arbeitsabläufe pro Issue erläutert und Standards für lokale Entwicklung, Coding-Standards, Datenbank-Änderungen und Tests vorgegeben.

---

### deploy.sh
Dieses Skript `deploy.sh` dient der Automatisierung des Produktionsbuilds und -deploys für die Anwendung app.linn.games. Es überprüft Voraussetzungen, baut die Docker-Images, migriert die Datenbank und optimiert den Cache.

---

### package.json
Paket-Datei mit Vite-Build-Einstellungen und Dependency-Manifest.

---

### phpunit.xml
PHPUnit-Konfigurationsdatei zur Ausführung von Einheitstests und Funktionsabfragen. Die Datei konfiguriert die Testumgebung, einschließlich der zu testenden Klassen, des Bootstrap-Prozesses und der Umgebungsvariablen.

---

### vite.config.js
Konfiguriert Vite-Entwicklungsumgebung für Laravel-Projekt. Integriert Laravel-Vite-Plugin und TailwindCSS.

---

### .dockerignore
```
{
  "file_summary": "Diese Datei ignoriert bestimmte Ordner und Dateien für die Docker-Entwicklung. 
  Hauptfunktionen: Ignorierung von Node_Modulen, Vendor-Ordner und Framework-Cache.
  Wichtige Klassen/Funktionen/Endpunkte: Keine direkten Bezugnahme auf spezifische Klassen oder Funktionen.
  Externe Abhängigkeiten: Docker-Kommandozeilenbefehle (docker-compose), PHP-Tools wie PHPUnit und PHPStan."
}
```

---

### .editorconfig
```json
{
  "file_summary": "Konfiguration für Code-Formatierung und -Struktur\n\
Wichtige Funktionen:\n\
- Überprüft und anpasst Code-Einstellungen (Beispiel: Dateiendung, Zeilenformat)\n\
- Bestimmt die Indent-Größe basierend auf der Dateiendung (yaml, yml usw.)\n\
- Führt Einstellungen für Markdown-Dokumente an\n\
- Überprüft und korrigiert trailing Whitespaces\n\
Externe Abhängigkeiten: Keine"
}
```

---

### app/Actions/Fortify/CreateNewUser.php
Diese Datei implementiert das Interface \Laravel\Fortify\Contracts\CreatesNewUsers und ermöglicht es, neue Benutzer zu erstellen. Es validiert die Eingabe und speichert sie in der Datenbank. Die wichtigsten Klassen sind User und Validator. Externe Abhängigkeiten beinhalten Laravel's Fortify-Framework und das Illuminate-Validation-Framework.

---

### app/Actions/Fortify/PasswordValidationRules.php
Definiert Regeln zur Validierung von Passwörtern. Wird durch das Treiber "PasswordValidationRules" aufgerufen.

---

### app/Actions/Fortify/ResetUserPassword.php
```
{
  "file_summary": "Die Datei ResetUserPassword.php ist Teil der Fortify-Erkennung im Laravel-Frame-_
  werk. Sie implementiert die Schnittstelle ResetsUserPasswords und bietet eine Methode zum Zurücksetzen eines Benutzerpassworts.
  
  - Die wichtigsten Klassen sind User und Validator, sowie die eigene Implementierung von ResetsUserPasswords.
  
  - Externe Abhängigkeiten beinhalten die Verwendung der Laravel-Fortify-Contracts und des PasswordValidationRules-Trait."
}
```

---

### app/Filament/Resources/ContactResource.php
```json
{
  "file_summary": "Definiert eine Ressourcen-Klasse für das Modell \"Contact\", die Formulare und Tabellen für den Kontakt-Übersichtsbereich bereitstellt.",
  "key_classes": ["ContactResource"],
  "key_functions": ["form", "table", "getPages"],
  "external_dependencies": [
    {"name": "App\Models\Contact"},
    {"name": "Filament\Forms"},
    {"name": "Filament\Schemas\Schema"},
    {"name": "Filament\Resources\Resource"},
    {"name": "Filament\Tables"},
    {"name": "Filament\Tables\Table"}
  ]
}
```

---

### app/Filament/Resources/UserResource.php
Definiert eine Ressource für Benutzer in Filament, die Daten zur Bearbeitung und Darstellung von Nutzern bietet. Die Ressource enthält Formularelemente, Tabellenspalten und Seiten zum Anzeigen und Bearbeiten von Nutzern.

---

### app/Http/Middleware/TrackPageView.php
```json
{
  "file_summary": "Middleware zur Zählung von Seiteaufrufen: 
- Prüft ob GET-Anfrage eines Bot ist,
- Wenn nicht: Erstellt oder aktualisiert eine PageView-Einträge in der Datenbank,
- Inkrementiert die Besuche dieses Eintrags.
Klassen/Endpunkte: TrackPageView
Abhängigkeiten: App\Models\PageView, Illuminate\Http\Request, Symfony\Component\HttpFoundation\Response"
}
```

---

### app/Http/Middleware/VerifyLangdockSignature.php
Middleware zur Überprüfung von Signaturen für Langdock-Webhooks. Überprüft den Timestamp und die Signatur sowie erwartet, dass die Signatur nicht bereits in der Cache-Tabelle ist.

---

### app/Http/Middleware/VerifyMcpToken.php
Verifiziert den MCP-Token in der Header-Authentifizierung. Importiert Token aus Config und ermöglicht Weiterleitung an nächste Middleware.

---

### app/Jobs/IngestPaperJob.php
```
{
  "file_summary": "Diese Datei implementiert eine Queue-Job-Klasse, die Papiere in Embeddings (Einfügungen) umwandelt und diese in der Datenbank speichert. Sie verwendet den Ollama-Dienst für Text-Einfügungen und die Eloquent-Abfragefunktion von Laravel für die Datenbankoperationen.",
  "key_clases": ["IngestPaperJob"],
  "important_functions": ["handle", "chunkText"],
  "endpoints": ["/api/embeddings"],
  "external_dependencies": ["Illuminate\Support\Facades\Http", "Illuminate\Support\Facades\DB"]
}
```

---

### app/Jobs/TriggerLangdockAgent.php
```
{
  "file_summary": "Diese Datei enthält eine Job-Klasse für die Auslösung von Langdock-Agenten. Sie überprüft, ob ein User bereits einen solchen Job ausgelöst hat und startet ihn nur einmal pro Projekt. Der Job sendet dann ein Webhook-Event an den Langdock-Agent und wartet auf eine erfolgreiche Antwort.",
  " wichtigste Klassen/Funktionen/Endpunkte": "TriggerLangdockAgent, \App\Models\Webhook, \App\Models\User",
  "externe Abhängigkeiten": "Illuminate\Http\Client\Facades\Http (für HTTP-Anfragen), Illuminate\Support\Facades\Log (für Logging)"
}
```

---

### app/Livewire/Actions/Logout.php
```json
{
  "file_summary": "Diese Datei handelt es sich um einen Logout-Handler, der den aktuellen Benutzer aus der Anwendung abmelden kann. Der Handler verwendet Auth und Session-Funktionen von Laravel für die Abmeldung.",
  "important_classes": ["Logout"],
  "key_functions": ["__invoke", "logout", "invalidate", "regenerateToken"],
  "external_dependencies": ["Illuminate\Support\Facades\Auth", "Illuminate\Support\Facades\Session"]
}
```

---

### app/Mail/ContactConfirmationMail.php
Diese Datei sendet eine Bestätigungs-E-Mail an einen Kontakt. Es werden die Klassen Contact und Mailable verwendet. Externe Abhängigkeiten sind die Modelle von Laravel und die Views für die E-Mails.

---

### app/Mail/ContactInquiryMail.php
```
{
  "file_summary": "Diese Datei definiert eine Mail-Klasse für Kontaktanfragen. Sie verwendet die Laravel-Mail-Funktionen und übernimmt die Daten eines Contact-Objekts. Die wichtigsten Funktionen sind der Konstruktor, der Envelope und der Content. Externe Abhängigkeiten sind die 'App\Models\Contact'-Klasse und die Laravel-Mail-Klassen."
}
```

---

### app/Mail/DeployNotificationMail.php
Diese Datei verantwortet die Erstellung von E-Mail-Nachrichten zur Deployment-Notifikation.

---

### app/Policies/ProjektPolicy.php
```
{
  "file_summary": "Dokumentiert die Zugriffs-Policies für Projekte. 
    Bietet Methoden für das Anzeigen, Erstellen, Bearbeiten und Löschen von Projekten.
    Verwendet User- und Projekt-Models."
}
```

---

### app/Providers/AppServiceProvider.php
Dienstanbieter für das Laravel-App-Projekt, der Policies und Rate-Limits konfiguriert.

---

### app/Providers/FortifyServiceProvider.php
```json
{
  "file_summary": "Dieses Fortify-Service-Provider-Klasse konfiguriert die Sicherheitsfunktionen der Anwendung. Es umfasst die Konfiguration von Aktionen für Benutzererstellung, Passwort-Restart und -Reset sowie die Definition von Ansichten für Login, Registrierung und Passwort-Restart.",
  "wichtige_Klassen": ["FortifyServiceProvider", "CreateNewUser", "ResetUserPassword"],
  "externale_Abhängigkeiten": ["Illuminate\Cache\RateLimiting\Limit", "Laravel\Fortify\Fortify"]
}
```

---

### app/Providers/VoltServiceProvider.php
Datenbank-Serviceprovider für Livewire-Volt. Registriert und bootet den Volt Service, der das Mounten von Livewire Views ermöglicht.

---

### app/Providers/Filament/AdminPanelProvider.php
```
{
  "file_summary": "Diese Datei konfiguriert den Administrations-Panel-Provider für die Anwendung. Es werden Mittelweiser und Authentifizierungsmethoden festgelegt, um Zugriff auf das Administrationspanel zu ermöglichen.",
  " wichtigste Klassen oder Funktionen": "AdminPanelProvider, Panel",
  "Externe Abhängigkeiten": [
    "Filament\Http\Middleware\Authenticate",
    "Filament\Http\Middleware\AuthenticateSession",
    "Illuminate\Cookie\Middleware\AddQueuedCookiesToResponse",
    "Illuminate\Cookie\Middleware\EncryptCookies",
    "Illuminate\Foundation\Http\Middleware\VerifyCsrfToken"
  ]
}
```

---

### bootstrap/app.php
Eintragspunkt für die Bootstrap-App-Konfiguration. Konfiguriert Routing, Middleware und Ausnahmen für Laravel-Anwendung.

---

### bootstrap/providers.php
Registriert Diensteanbieter für Laravel-Anwendung, darunter App- und Admin-Panel-Einstellungen sowie Fortify-Sicherheitsdienste.

---

### bootstrap/cache/.gitignore
Diese Datei speichert eine Liste von Dateien, die nicht in die Versionierung aufgenommen werden sollen. Dazu zählen alle Dateien (`*`), außer der aktuellen `.gitignore`-Datei (`!.gitignore`).

---

### docker/common/nginx/default.conf
Diese Datei konfiguriert den Nginx-Webserver für eine Anwendung. Sie enthält Konfigurationen für verschiedene Funktionen wie Livewire-Vorlagen, statische Dateien, PHP-Anfragen, Authentifizierung und Proxying. Externe Abhängigkeiten sind die Docker-Umgebung und der PostgreSQL-Datenbank-Server. Die wichtigsten Klassen oder Endpunkte sind /livewire/, /messages/ und /ollama/.

---

### docker/common/nginx/docker-entrypoint.sh
Docker-Eintragspunkt für Nginx-Container; konfiguriert Template-Datei und ersetzt Auth-Token in Nginx-Konfiguration

---

### docker/common/nginx/dockerfile
Diese Datei erstellt eine Nginx-Docker-Image mit einer konfigurierten Nginx-Konfiguration und Eintrittspunkt zum Injecten von Geheimnissen. Sie kopiert statische Anwendungsdateien in die Container-Dateisystem.

---

### docker/common/nginx/nginx.conf
Konfiguration für Nginx-Server zum Hosten von Laravel-Anwendungen. Verwendet X-Forwarded-For-Headers, gzip-Komprimierung und beschränkt die Dateigröße auf 100 MB.

---

### docker/common/php-fpm/dockerfile
Docker-Datei für die PHP-FPM-Umgebung. Die Datei erstellt zwei Docker-Bilder: eines für die Produktionsumgebung und eines für die Testumgebung. Sie installiert alle erforderlichen PHP-Erweiterungen, stellt Composer und npm bereit und kopiert die Anwendungskodierung in das Container-Verzeichnis.

---

### docker/common/php-fpm/entrypoint.sh
Dieses Script ist ein Docker-Eintragspunkt für eine PHP-Webanwendung. Es sync't Frontend-Build-Artefakte, wartet auf die Datenbank-Zugängigkeit und führt Migrations- und Caching-Operationen aus.

---

### docker/common/postgres/dockerfile
```json
{
  "file_summary": "Dieses Dockerfile erstellt eine Postgres-Datenbank mit zusätzlichen Erweiterungen (hypopg und pgvector).\n\
   Es basiert auf der offiziellen Postgres-Image-Version 16.\n\
   Die Datei installiert die Pakete postgresql-16-hypopg und postgresql-16-pgvector."
}
```

---

### docker/common/postgres/init-extensions.sql
Dieser SQL-Skript ist für die Initialisierung von PostgreSQL-Erweiterungen verantwortlich. Es erstellt drei Erweiterungen: hypopg, pg_stat_statements und vector.

---

### docs/overview-2026-04-02_0617.md
Diese Datei enthält eine Dokumentation zur Funktionsübersicht der Anwendung 'app.linn.games'. Sie beschreibt Controller, Modelle, Routen und Konfigurationsdateien. Die Anwendung verfügt über diverse Funktionen wie Kontaktanfragen, Webhooks, Paper-Rag-Operationen und Datenbankzugriffe.

---

### public/index.php
```
{
  "file_summary": "Laravel-Anwendung initialisiert und HTTP-Anfrage gehandelt. 
  - Überprüft Maintenance-Modus
  - Autoloader lädt Composer-Packages
  - Bootstrapet Laravel und handelt Anfrage ab
  Hauptschwerpunkte: Application, Request
  Externe Abhängigkeiten: Composer, Laravel/Framework"
}
```

---

### public/robots.txt
Diese Datei definiert die Zugriffsrechte für Web-Crawler wie Google. Sie enthält eine Anweisung, dass alle User-Agenten keinen Zugriff erhalten sollen.

---

### public/site.webmanifest
Diese Datei stellt die Manifestdatei für das Web-App-Manifest bereit, die Einstellungen für die Anzeige des Webbrowsers enthält. Sie beinhaltet Icons, Farben und weitere Anzeigeeinstellungen.

---

### public/.htaccess
Konfiguriert Apache für Benutzerauthentifizierung, XSRF-Token und Weiterleitung nicht existierender URLs auf den Front Controller.

---

### resources/css/app.css
Dies ist die CSS-Konfigurationsdatei für das Projekt. Sie importiert Tailwindcss und definiert einige Themes und Variablen für Farben und Schriftarten. Die Anpassungen werden dann auf Basis der Theme-Einstellungen (light oder dark) vorgenommen.

---

### resources/css/welcome.css
Diese Datei enthält CSS-Stile für die Visualisierung einer Website. Sie definiert Farben, Schattierungen und Effekte sowie einige grundlegende Layout-Elemente wie Grid-Überlagerung und Scroll-Reveal-Animationen. Die wichtigsten Klassen sind `animated-bg`, `particles`, `cursor-glow`, `grid-overlay` und `reveal`. Externe Abhängigkeiten sind keine vorhanden.

---

### resources/js/welcome.js
```
{
  "file_summary": "Dieser JavaScript-Code behandelt verschiedene Funktionen auf der Website.\
   \
   - Der Footer wird mit dem aktuellen Jahr befüllt,\
   - Die Snake3D-Karte kann geöffnet und geschlossen werden, und das entsprechende iframe wird geladen,\
   - Das Mobile-Navigation-Menü kann ausgeklappt werden, und die Header-Überladeneffekte werden animiert,\
   - Ein Kursor-Licht-Effekt wird aktiviert,\
   - Scroll-Reveal-Animierungen werden für verschiedene Elemente auf der Seite eingerichtet,\
   - Ein Contact-Formular wurde erstellt, das bei Senden eine Anfrage an den Server sendet,\
   \
   Die wichtigsten Funktionen sind: loadSnake3DIframe(), toggleMenu() und submitContactForm().\
   \
   Externe Abhängigkeiten: fetch-API für HTTP-Anfragen und IntersectionObserver für Scroll-Beobachtung."
}
```

---

### storage/logs/.gitignore
Git-Ignore-Datei für das Log-Speicherungssystem. Alle Dateien außer .gitignore ignorieren.

---

### .claude/agents/code-quality-architect.md
Die Datei beschreibt einen Agenten namens 'code-quality-architect', der für die Planung großer Aufgaben, die Überprüfung von Code für Redundanzen und beste Praktiken sowie die Validierung von Konfigurationen gegenüber dem Zielumfeld zuständig ist. Der Agent analysiert das System, identifiziert redundante Code-Patterns, überprüft bestehende Standards, validiert Konfigurationen und erstellt umfassende Analysen zur Identifizierung potenzieller Probleme.

---

### .github/copilot-instructions.md
Diese Datei beschreibt die Verwendung von Copilot in einem Laravel-Projekt. Sie enthält Konventionen für Sprache, Code und Commits sowie Regeln für den Stack, die Tests und die Docker-Arbeit. Die wichtigsten Klassen oder Funktionen sind nicht explizit genannt.

---

### .github/instructions/app_linn_games_custom.instructions.md
Die Datei beschreibt die Architektur und Konventionen der proprietären Research-Management-Plattform app.linn.games. Sie enthält Informationen über den Datenmodell, Authentifizierung, Autorisierung, KI-Integration und externe Abhängigkeiten.

---

### .github/instructions/docker-dev-workflow.instructions.md
Docker Dev-Workflow und -Setup für das Projekt app.linn.games. Konfigurationen für Container, Volumes, Bind-Mounts und Rebuilds nach Code-Änderungen.

---
